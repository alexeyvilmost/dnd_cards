// src/rules-worker/server.ts
import { createServer } from "node:http";
import { randomBytes } from "node:crypto";

// src/rules-core/determinism.ts
function createStrictRngTape(entries) {
  const tape = entries.map((entry) => ({ ...entry }));
  let index = 0;
  const readEntry = (requestedSides) => {
    if (!Number.isInteger(requestedSides) || requestedSides < 2) {
      throw new Error(`Invalid requested die sides: ${requestedSides}`);
    }
    const entry = tape[index];
    if (!entry) throw new Error(`RNG tape exhausted after ${index} draws`);
    if (!Number.isInteger(entry.sides) || entry.sides < 2) {
      throw new Error(`Invalid die sides at ${entry.label}: ${entry.sides}`);
    }
    if (!Number.isInteger(entry.value) || entry.value < 1 || entry.value > entry.sides) {
      throw new Error(`Invalid d${entry.sides} result at ${entry.label}: ${entry.value}`);
    }
    if (entry.sides !== requestedSides) {
      throw new Error(
        `RNG die mismatch at ${entry.label}: engine requested d${requestedSides}, tape declares d${entry.sides}`
      );
    }
    index += 1;
    return entry.value;
  };
  const rng = Object.assign(
    () => {
      const entry = tape[index];
      if (!entry) throw new Error(`RNG tape exhausted after ${index} draws`);
      throw new Error(`RNG draw at ${entry.label} did not declare requested die sides`);
    },
    { rollDie: readEntry }
  );
  return {
    rng,
    consumed: () => index,
    remaining: () => tape.length - index,
    assertExhausted: () => {
      if (index !== tape.length) {
        const unused = tape.slice(index).map((entry) => entry.label).join(", ");
        throw new Error(`RNG tape has ${tape.length - index} unused draws: ${unused}`);
      }
    }
  };
}
function createLogicalClock(start = 0) {
  let value = start;
  return () => {
    value += 1;
    return value;
  };
}
function createSequentialIdFactory(prefix = "id", start = 0) {
  let value = start;
  return () => {
    value += 1;
    return `${prefix}-${value}`;
  };
}
function normalizeCanonical(value, seen) {
  if (value === null || typeof value === "string" || typeof value === "boolean") return value;
  if (typeof value === "number") {
    if (!Number.isFinite(value)) throw new Error("Canonical JSON cannot contain a non-finite number");
    return Object.is(value, -0) ? 0 : value;
  }
  if (Array.isArray(value)) return value.map((item) => normalizeCanonical(item, seen));
  if (typeof value !== "object") {
    throw new Error(`Canonical JSON cannot contain ${typeof value}`);
  }
  if (seen.has(value)) throw new Error("Canonical JSON cannot contain cycles");
  seen.add(value);
  const source2 = value;
  const result = {};
  for (const key of Object.keys(source2).sort()) {
    const item = source2[key];
    if (item === void 0) continue;
    result[key] = normalizeCanonical(item, seen);
  }
  seen.delete(value);
  return result;
}
function canonicalStringify(value) {
  return JSON.stringify(normalizeCanonical(value, /* @__PURE__ */ new Set()));
}
async function canonicalSha256(value) {
  if (!globalThis.crypto?.subtle) throw new Error("Web Crypto SHA-256 is unavailable");
  const bytes = new TextEncoder().encode(canonicalStringify(value));
  const digest = await globalThis.crypto.subtle.digest("SHA-256", bytes);
  return `sha256:${[...new Uint8Array(digest)].map((byte) => byte.toString(16).padStart(2, "0")).join("")}`;
}

// src/rules-core/actionTargeting.ts
var ActionTargetingDefinitionError = class extends Error {
  constructor(message) {
    super(message);
    this.name = "ActionTargetingDefinitionError";
  }
};
function object(value, label) {
  if (!value || typeof value !== "object" || Array.isArray(value)) {
    throw new ActionTargetingDefinitionError(`${label} must be an object`);
  }
  return value;
}
function finiteNonNegative(value, label) {
  if (typeof value !== "number" || !Number.isFinite(value) || value < 0) {
    throw new ActionTargetingDefinitionError(`${label} must be a finite non-negative number`);
  }
  return value;
}
function positiveInteger(value, fallback, label) {
  const resolved = value === void 0 ? fallback : value;
  if (!Number.isInteger(resolved) || Number(resolved) < 1) {
    throw new ActionTargetingDefinitionError(`${label} must be a positive integer`);
  }
  return Number(resolved);
}
function nonNegativeInteger(value, label) {
  if (!Number.isInteger(value) || Number(value) < 0) {
    throw new ActionTargetingDefinitionError(`${label} must be a non-negative integer`);
  }
  return Number(value);
}
function legacyRangeFt(targeting) {
  const raw = String(targeting.range ?? "");
  if (/касание|touch/i.test(raw)) return 5;
  if (/на себя|self/i.test(raw)) return 0;
  const match = raw.match(/\d+(?:[.,]\d+)?/);
  if (match) return Number(match[0].replace(",", "."));
  const area = targeting.area;
  if (area && typeof area === "object" && !Array.isArray(area)) {
    const size = Number(area.size);
    if (Number.isFinite(size) && size > 0) return size;
  }
  return 5;
}
function validateArea(value) {
  if (value === void 0) return;
  const area = object(value, "targeting.area");
  if (!["sphere", "cube", "cone", "line", "cylinder", "emanation"].includes(String(area.kind ?? ""))) {
    throw new ActionTargetingDefinitionError("targeting.area.kind is invalid");
  }
  const numericKeys = ["size_ft", "radius_ft"];
  const declared = numericKeys.filter((key) => area[key] !== void 0);
  if (declared.length > 1) {
    throw new ActionTargetingDefinitionError("targeting.area must declare only one numeric size authority");
  }
  if (declared.length === 1) {
    const size = finiteNonNegative(area[declared[0]], `targeting.area.${declared[0]}`);
    if (size <= 0) throw new ActionTargetingDefinitionError(`targeting.area.${declared[0]} must be positive`);
  }
  if (area.size !== void 0) {
    const size = Number(area.size);
    if (!Number.isFinite(size) || size <= 0) {
      throw new ActionTargetingDefinitionError("legacy targeting.area.size must be positive");
    }
  }
}
function compileMechanicsTargeting(mechanics) {
  const targeting = object(mechanics.targeting ?? {}, "mechanics.targeting");
  const shape = String(targeting.shape ?? "single");
  if (!["self", "single", "multi", "multiple", "area", "aura"].includes(shape)) {
    throw new ActionTargetingDefinitionError(`targeting.shape ${shape} is invalid`);
  }
  validateArea(targeting.area);
  const hasDomain = targeting.domain !== void 0 || targeting.actor_targets !== void 0;
  let actorTargets;
  if (hasDomain) {
    if (!["world", "actor", "mixed"].includes(String(targeting.domain ?? ""))) {
      throw new ActionTargetingDefinitionError("targeting.domain must be world, actor, or mixed");
    }
    if (typeof targeting.actor_targets !== "boolean") {
      throw new ActionTargetingDefinitionError("targeting.actor_targets must be boolean");
    }
    const domain = targeting.domain;
    actorTargets = targeting.actor_targets;
    const expectsExplicitActorTargets = domain === "mixed" || domain === "actor" && shape !== "self";
    if (actorTargets !== expectsExplicitActorTargets) {
      throw new ActionTargetingDefinitionError("targeting.domain contradicts targeting.actor_targets");
    }
  } else {
    actorTargets = shape !== "self";
  }
  const area = shape === "area";
  const multiple = shape === "multi" || shape === "multiple";
  const self = shape === "self";
  const fallbackMaximum = area ? 8 : multiple ? 3 : 1;
  const maxTargets = positiveInteger(targeting.max_targets, fallbackMaximum, "targeting.max_targets");
  const minTargets = targeting.min_targets === void 0 ? area || !actorTargets ? 0 : 1 : nonNegativeInteger(targeting.min_targets, "targeting.min_targets");
  if (minTargets > maxTargets) {
    throw new ActionTargetingDefinitionError("targeting.min_targets cannot exceed targeting.max_targets");
  }
  const rangeFt = targeting.range_ft === void 0 ? legacyRangeFt(targeting) : finiteNonNegative(targeting.range_ft, "targeting.range_ft");
  const requiresLineOfSight = targeting.requires_line_of_sight === void 0 ? !self : targeting.requires_line_of_sight;
  if (typeof requiresLineOfSight !== "boolean") {
    throw new ActionTargetingDefinitionError("targeting.requires_line_of_sight must be boolean");
  }
  for (const key of ["requires_sight", "requires_willing", "requires_unarmored"]) {
    if (targeting[key] !== void 0 && typeof targeting[key] !== "boolean") {
      throw new ActionTargetingDefinitionError(`targeting.${key} must be boolean`);
    }
  }
  if (targeting.requires_stonework_contact !== void 0 && targeting.requires_stonework_contact !== true) {
    throw new ActionTargetingDefinitionError("targeting.requires_stonework_contact must be true when present");
  }
  if (targeting.requires_touch !== void 0 && typeof targeting.requires_touch !== "boolean") {
    throw new ActionTargetingDefinitionError("targeting.requires_touch must be boolean");
  }
  const defaultRelations = self ? ["self"] : ["self", "ally", "enemy", "neutral"];
  let allowedRelations = defaultRelations;
  if (targeting.allowed_relations !== void 0) {
    if (!Array.isArray(targeting.allowed_relations) || targeting.allowed_relations.some((relation) => !["self", "ally", "enemy", "neutral"].includes(String(relation))) || new Set(targeting.allowed_relations).size !== targeting.allowed_relations.length) {
      throw new ActionTargetingDefinitionError("targeting.allowed_relations must contain unique actor relations");
    }
    allowedRelations = [...targeting.allowed_relations];
    if (self && (allowedRelations.length !== 1 || allowedRelations[0] !== "self")) {
      throw new ActionTargetingDefinitionError(
        "self targeting requires allowed_relations to contain exactly self"
      );
    }
    if (!self && actorTargets && allowedRelations.length === 0) {
      throw new ActionTargetingDefinitionError("actor targeting requires at least one allowed relation");
    }
    if (!self && !actorTargets && allowedRelations.length !== 0) {
      throw new ActionTargetingDefinitionError("world targeting cannot allow actor relations");
    }
  }
  return {
    minTargets,
    maxTargets,
    rangeFt,
    requiresLineOfSight,
    ...targeting.requires_sight === true ? { requiresSight: true } : {},
    allowedRelations,
    ...targeting.requires_willing === true ? { requiresWilling: true } : {},
    ...targeting.requires_unarmored === true ? { requiresUnarmored: true } : {},
    ...targeting.requires_touch === true ? { requiresTouch: true } : {},
    ...targeting.requires_stonework_contact === true ? { requiresStoneworkContact: true } : {}
  };
}
var DECLARED_TARGETING_KEYS = [
  "shape",
  "domain",
  "actor_targets",
  "min_targets",
  "max_targets",
  "range_ft",
  "requires_line_of_sight",
  "allowed_relations"
];
function compileDeclaredMechanicsTargeting(mechanics) {
  const targeting = object(mechanics.targeting, "mechanics.targeting");
  const missing = DECLARED_TARGETING_KEYS.filter((key) => !Object.prototype.hasOwnProperty.call(targeting, key));
  if (missing.length) {
    throw new ActionTargetingDefinitionError(
      `mechanics.targeting is missing explicit ${missing.join(", ")}`
    );
  }
  if (targeting.shape === "area" || targeting.area !== void 0) {
    const area = object(targeting.area, "targeting.area");
    if (!Object.prototype.hasOwnProperty.call(area, "kind")) {
      throw new ActionTargetingDefinitionError("targeting.area.kind must be explicit");
    }
    const numericAuthority = ["size_ft", "radius_ft"].filter((key) => Object.prototype.hasOwnProperty.call(area, key));
    if (numericAuthority.length !== 1) {
      throw new ActionTargetingDefinitionError(
        "targeting.area requires exactly one explicit size_ft or radius_ft"
      );
    }
    if (Object.prototype.hasOwnProperty.call(area, "size")) {
      throw new ActionTargetingDefinitionError(
        "legacy targeting.area.size is not allowed in certified content"
      );
    }
  }
  return compileMechanicsTargeting(mechanics);
}

// src/engine/events.ts
function rollEvent(label, roll) {
  return { type: "roll", label, roll };
}
function damageEvent(amount, damageType, roll) {
  return { type: "damage", amount, damageType, roll };
}
function healingEvent(amount, roll) {
  return { type: "healing", amount, roll };
}
function tempHpEvent(amount) {
  return { type: "temp_hp", amount };
}
function narrativeEvent(text) {
  return { type: "narrative", text };
}
function resourceSpentEvent(resource, amount, remaining) {
  return { type: "resource_spent", resource, amount, remaining };
}
function resourceRestoredEvent(resource, amount, current) {
  return { type: "resource_restored", resource, amount, current };
}
function itemConsumedEvent(cardId, amount, remaining, name) {
  return { type: "item_consumed", cardId, amount, remaining, ...name ? { name } : {} };
}
function itemAddedEvent(cardId, qty, total, name) {
  return { type: "item_added", cardId, qty, total, ...name ? { name } : {} };
}
function conditionAppliedEvent(condition) {
  return { type: "condition_applied", condition };
}
function turnEndedEvent() {
  return { type: "turn_ended" };
}

// src/engine/cost.ts
function costAmount(entry) {
  const raw = entry.amount;
  if (raw == null) return 1;
  const n = typeof raw === "number" ? raw : Number(raw);
  return Number.isFinite(n) ? Math.max(0, Math.floor(n)) : 1;
}
function isItemCost(entry) {
  return String(entry.resource ?? "") === "item";
}
function inventoryQty(state, cardId) {
  return state.inventory.reduce((s, r) => r.cardId === cardId ? s + r.qty : s, 0);
}
function spendInventory(state, cardId, qty) {
  let remaining = qty;
  const order = state.inventory.map((r, i) => ({ r, i })).filter((x) => x.r.cardId === cardId).sort((a, b) => (a.r.containerId ? 1 : 0) - (b.r.containerId ? 1 : 0) || a.i - b.i);
  const take = /* @__PURE__ */ new Map();
  for (const { r, i } of order) {
    if (remaining <= 0) break;
    const t = Math.min(r.qty, remaining);
    take.set(i, t);
    remaining -= t;
  }
  const inventory = state.inventory.map((row, i) => take.has(i) ? { ...row, qty: row.qty - (take.get(i) ?? 0) } : { ...row }).filter((row) => row.qty > 0);
  return { ...state, inventory };
}
function costKey(entry) {
  const resource = String(entry.resource ?? "");
  if (resource === "spell_slot" && entry.level != null) return `spell_slot_${entry.level}`;
  return resource;
}
function runtimeCostKey(state, entry) {
  const key = costKey(entry);
  if (key !== "hit_die") return key;
  const requested = typeof entry.die === "string" ? `hit_dice_${entry.die.toLowerCase()}` : null;
  if (requested && requested in state.resources) return requested;
  const available = Object.keys(state.resources).filter((candidate) => candidate.startsWith("hit_dice_d") && (state.resources[candidate] ?? 0) > 0).sort((a, b) => Number(b.slice("hit_dice_d".length)) - Number(a.slice("hit_dice_d".length)));
  return available[0] ?? "hit_die";
}
function canPay(state, cost) {
  const itemNeed = /* @__PURE__ */ new Map();
  const resNeed = /* @__PURE__ */ new Map();
  for (const entry of cost) {
    const need = costAmount(entry);
    if (isItemCost(entry)) {
      const cardId = String(entry.card_id ?? "");
      itemNeed.set(cardId, (itemNeed.get(cardId) ?? 0) + need);
    } else {
      const key = runtimeCostKey(state, entry);
      resNeed.set(key, (resNeed.get(key) ?? 0) + need);
    }
  }
  const missing = [];
  for (const [cardId, need] of itemNeed) {
    if (!cardId || inventoryQty(state, cardId) < need) missing.push(`item:${cardId}`);
  }
  for (const [key, need] of resNeed) {
    if ((state.resources[key] ?? 0) < need) missing.push(key);
  }
  return { ok: missing.length === 0, missing };
}
function pay(state, cost) {
  const check = canPay(state, cost);
  if (!check.ok) return { state, events: [] };
  let next = state;
  const resources = { ...state.resources };
  const events = [];
  for (const entry of cost) {
    const need = costAmount(entry);
    if (isItemCost(entry)) {
      const cardId = String(entry.card_id ?? "");
      next = spendInventory(next, cardId, need);
      const name = typeof entry.name === "string" ? entry.name : void 0;
      events.push(itemConsumedEvent(cardId, need, inventoryQty(next, cardId), name));
      continue;
    }
    const key = runtimeCostKey(state, entry);
    resources[key] = (resources[key] ?? 0) - need;
    events.push(resourceSpentEvent(key, need, resources[key]));
  }
  return { state: { ...next, resources }, events };
}

// src/engine/random.ts
function drawDie(rng, sides) {
  if (!Number.isInteger(sides) || sides < 2) {
    throw new Error(`Invalid requested die sides: ${sides}`);
  }
  const dieAware = rng;
  if (typeof dieAware.rollDie === "function") {
    const result = dieAware.rollDie(sides);
    if (!Number.isInteger(result) || result < 1 || result > sides) {
      throw new Error(`Die-aware RNG returned invalid d${sides} result: ${result}`);
    }
    return result;
  }
  const unit = rng();
  if (!Number.isFinite(unit) || unit < 0 || unit >= 1) {
    throw new Error(`RNG must return a finite value in [0, 1), got ${unit}`);
  }
  return Math.floor(unit * sides) + 1;
}

// src/engine/formula.ts
var FormulaError = class extends Error {
  constructor(message) {
    super(message);
    this.name = "FormulaError";
  }
};
var MissingVariableError = class extends FormulaError {
  constructor(variable) {
    super(`\u041F\u0435\u0440\u0435\u043C\u0435\u043D\u043D\u0430\u044F \u0444\u043E\u0440\u043C\u0443\u043B\u044B \u043D\u0435\u0434\u043E\u0441\u0442\u0443\u043F\u043D\u0430: ${variable}`);
    this.variable = variable;
    this.name = "MissingVariableError";
  }
};
var MARKERS = /* @__PURE__ */ new Set(["weapon", "auto"]);
var ABILITY_LABEL_RU = {
  str: "\u0421\u0418\u041B",
  dex: "\u041B\u0412\u041A",
  con: "\u0422\u0415\u041B",
  int: "\u0418\u041D\u0422",
  wis: "\u041C\u0414\u0420",
  cha: "\u0425\u0410\u0420"
};
function defaultRng() {
  return Math.random();
}
function tokenize(input) {
  const s = input.trim().replace(/[\u00a0\u202f\u2007\u2009\u200b]/g, " ").replace(/\s+/g, " ");
  const tokens = [];
  let i = 0;
  while (i < s.length) {
    const ch = s[i];
    if (ch === " ") {
      i++;
      continue;
    }
    if (ch === "(") {
      tokens.push({ t: "lparen" });
      i++;
      continue;
    }
    if (ch === ")") {
      tokens.push({ t: "rparen" });
      i++;
      continue;
    }
    if ("+-*/,".includes(ch)) {
      tokens.push({ t: "op", v: ch });
      i++;
      continue;
    }
    const diceMatch = s.slice(i).match(/^(\d+)d(\d+)/i);
    if (diceMatch) {
      tokens.push({ t: "dice", count: Number(diceMatch[1]), sides: Number(diceMatch[2]) });
      i += diceMatch[0].length;
      continue;
    }
    const bareDiceMatch = s.slice(i).match(/^d(\d+)/i);
    if (bareDiceMatch) {
      tokens.push({ t: "dice", count: 1, sides: Number(bareDiceMatch[1]) });
      i += bareDiceMatch[0].length;
      continue;
    }
    const scalingMatch = s.slice(i).match(/^class_level:([a-z0-9_-]+)\s*\/\s*(\d+)\s+d(\d+)/i);
    if (scalingMatch) {
      const classId = scalingMatch[1];
      const divisor = Number(scalingMatch[2]);
      const sides = Number(scalingMatch[3]);
      tokens.push({ t: "id", v: `__scaling__:${classId}:${divisor}:${sides}` });
      i += scalingMatch[0].length;
      continue;
    }
    const varScalingMatch = s.slice(i).match(/^([a-zA-Z_][a-zA-Z0-9_:]*)(?:\s*\/\s*(\d+))?\s+d(\d+)/i);
    if (varScalingMatch) {
      const varId = varScalingMatch[1];
      if (!MARKERS.has(varId.toLowerCase())) {
        const divisor = Number(varScalingMatch[2] || 1);
        const sides = Number(varScalingMatch[3]);
        tokens.push({ t: "id", v: `__scaling_var__:${divisor}:${sides}:${varId}` });
        i += varScalingMatch[0].length;
        continue;
      }
    }
    const idMatch = s.slice(i).match(/^[a-zA-Z_][a-zA-Z0-9_:]*/);
    if (idMatch) {
      tokens.push({ t: "id", v: idMatch[0] });
      i += idMatch[0].length;
      continue;
    }
    const numMatch = s.slice(i).match(/^\d+(?:\.\d+)?/);
    if (numMatch) {
      tokens.push({ t: "num", v: Number(numMatch[0]) });
      i += numMatch[0].length;
      continue;
    }
    throw new FormulaError(`\u041D\u0435\u0438\u0437\u0432\u0435\u0441\u0442\u043D\u044B\u0439 \u0441\u0438\u043C\u0432\u043E\u043B \u0432 \u0444\u043E\u0440\u043C\u0443\u043B\u0435 \xAB${input}\xBB \u043E\u043A\u043E\u043B\u043E \xAB${s.slice(i, i + 8)}\xBB`);
  }
  return tokens;
}
function rollDice(count, sides, sink) {
  let sum = 0;
  const n = Math.max(0, Math.floor(count));
  for (let i = 0; i < n; i++) {
    const result = drawDie(sink.rng, sides);
    if (sink.detailed) sink.dice.push({ sides, result });
    sum += result;
  }
  return sum;
}
function addModifier(sink, value, source2, reason) {
  if (sink.detailed && value !== 0) {
    sink.modifiers.push({ value, source: source2, reason });
  }
  return value;
}
function isDicePending(v) {
  return typeof v === "object" && v !== null && v.kind === "dice";
}
function pendingDice(count, sides) {
  return { kind: "dice", count: Math.max(0, Math.floor(count)), sides };
}
function forceNumber(v, sink) {
  if (typeof v === "string") {
    throw new FormulaError(`\u041C\u0430\u0440\u043A\u0435\u0440 \xAB${v}\xBB \u043D\u0435\u043B\u044C\u0437\u044F \u0438\u0441\u043F\u043E\u043B\u044C\u0437\u043E\u0432\u0430\u0442\u044C \u0432 \u0430\u0440\u0438\u0444\u043C\u0435\u0442\u0438\u043A\u0435`);
  }
  if (isDicePending(v)) return rollDice(v.count, v.sides, sink);
  return v;
}
function multiplyValues(left, right, sink) {
  if (typeof left === "string" || typeof right === "string") {
    throw new FormulaError("\u041C\u0430\u0440\u043A\u0435\u0440\u044B weapon/auto \u043D\u0435\u043B\u044C\u0437\u044F \u0443\u043C\u043D\u043E\u0436\u0430\u0442\u044C");
  }
  if (typeof left === "number" && isDicePending(right)) {
    return pendingDice(left * right.count, right.sides);
  }
  if (isDicePending(left) && typeof right === "number") {
    return rollDice(left.count, left.sides, sink) * right;
  }
  if (isDicePending(left) && isDicePending(right)) {
    throw new FormulaError("\u041D\u0435\u043B\u044C\u0437\u044F \u0443\u043C\u043D\u043E\u0436\u0430\u0442\u044C \u043A\u043E\u0441\u0442\u044C \u043D\u0430 \u043A\u043E\u0441\u0442\u044C");
  }
  return left * right;
}
function resolveNumericScalar(id, ctx) {
  const lower = id.toLowerCase();
  if (MARKERS.has(lower)) {
    throw new FormulaError(`\u041C\u0430\u0440\u043A\u0435\u0440 \xAB${id}\xBB \u043D\u0435\u043B\u044C\u0437\u044F \u0438\u0441\u043F\u043E\u043B\u044C\u0437\u043E\u0432\u0430\u0442\u044C \u043A\u0430\u043A \u0447\u0438\u0441\u043B\u043E \u043A\u043E\u0441\u0442\u0435\u0439`);
  }
  if (lower === "prof_bonus" || lower === "prof") return ctx.profBonus ?? 0;
  if (lower === "self_level") return ctx.selfLevel ?? 0;
  if (lower === "spellcasting") return ctx.spellcastingMod ?? 0;
  if (lower === "spell_slot_above") return ctx.spellSlotAbove ?? 0;
  if (lower === "rage_bonus") return ctx.rageBonus ?? 0;
  if (lower === "character_speed") return ctx.characterSpeed ?? 0;
  if (lower === "weapon_mod") return ctx.weaponMod ?? 0;
  if (lower.startsWith("class_level:")) {
    const classId = lower.slice("class_level:".length);
    return ctx.classLevels?.[classId] ?? 0;
  }
  const ability = lower;
  if (ability in ABILITY_LABEL_RU) return ctx.abilityMods?.[ability] ?? 0;
  const variable = ctx.variables?.[lower] ?? ctx.variables?.[id];
  if (variable !== void 0) {
    if (typeof variable === "number") return variable;
    throw new FormulaError(`\xAB${id}\xBB \u2014 \u043A\u043E\u0441\u0442\u044C, \u0430 \u043D\u0435 \u0447\u0438\u0441\u043B\u043E; \u043D\u0435\u043B\u044C\u0437\u044F \u043F\u0438\u0441\u0430\u0442\u044C \xAB${id} d\u2026\xBB`);
  }
  throw new MissingVariableError(id);
}
function resolveId(id, sink) {
  const lower = id.toLowerCase();
  const { ctx } = sink;
  if (MARKERS.has(lower)) return lower;
  if (lower.startsWith("__scaling__:")) {
    const [, classId, divStr, sidesStr] = lower.split(":");
    const level = ctx.classLevels?.[classId] ?? 0;
    const count = Math.ceil(level / Number(divStr));
    return pendingDice(count, Number(sidesStr));
  }
  if (lower.startsWith("__scaling_self__:")) {
    const [, divStr, sidesStr] = lower.split(":");
    const level = ctx.selfLevel ?? 0;
    const count = Math.ceil(level / Number(divStr));
    return pendingDice(count, Number(sidesStr));
  }
  if (lower.startsWith("__scaling_var__:")) {
    const rest = id.slice("__scaling_var__:".length);
    const m = /^(\d+):(\d+):(.+)$/i.exec(rest);
    if (!m) throw new FormulaError(`\u0411\u0438\u0442\u044B\u0439 scaling-\u0442\u043E\u043A\u0435\u043D: ${id}`);
    const divisor = Number(m[1]);
    const sides = Number(m[2]);
    const varId = m[3];
    const n = resolveNumericScalar(varId, sink.ctx);
    const count = Math.max(0, Math.ceil(n / (divisor || 1)));
    return pendingDice(count, sides);
  }
  if (lower === "prof_bonus" || lower === "prof") {
    return addModifier(sink, ctx.profBonus ?? 0, "\u0411\u041C", "\u0431\u043E\u043D\u0443\u0441 \u043C\u0430\u0441\u0442\u0435\u0440\u0441\u0442\u0432\u0430");
  }
  if (lower === "self_level") {
    return addModifier(sink, ctx.selfLevel ?? 0, "\u0443\u0440\u043E\u0432\u0435\u043D\u044C", "\u0443\u0440\u043E\u0432\u0435\u043D\u044C \u043F\u0435\u0440\u0441\u043E\u043D\u0430\u0436\u0430");
  }
  if (lower === "spellcasting") {
    return addModifier(sink, ctx.spellcastingMod ?? 0, "\u0437\u0430\u043A\u043B\u0438\u043D.", "\u043C\u043E\u0434\u0438\u0444\u0438\u043A\u0430\u0442\u043E\u0440 \u0437\u0430\u043A\u043B\u0438\u043D\u0430\u043D\u0438\u0439");
  }
  if (lower === "spell_slot_above") {
    return addModifier(sink, ctx.spellSlotAbove ?? 0, "\u044F\u0447\u0435\u0439\u043A\u0430+", "\u0443\u0440\u043E\u0432\u0435\u043D\u044C \u044F\u0447\u0435\u0439\u043A\u0438 \u0432\u044B\u0448\u0435");
  }
  if (lower === "rage_bonus") {
    return addModifier(sink, ctx.rageBonus ?? 0, "\u044F\u0440\u043E\u0441\u0442\u044C", "\u0431\u043E\u043D\u0443\u0441 \u044F\u0440\u043E\u0441\u0442\u0438");
  }
  if (lower === "character_speed") {
    return addModifier(sink, ctx.characterSpeed ?? 0, "\u0441\u043A\u043E\u0440\u043E\u0441\u0442\u044C", "\u0441\u043A\u043E\u0440\u043E\u0441\u0442\u044C \u043F\u0435\u0440\u0441\u043E\u043D\u0430\u0436\u0430");
  }
  if (lower === "weapon_mod") {
    return addModifier(sink, ctx.weaponMod ?? 0, "\u043E\u0440\u0443\u0436\u0438\u0435", "\u043C\u043E\u0434\u0438\u0444\u0438\u043A\u0430\u0442\u043E\u0440 \u0445\u0430\u0440\u0430\u043A\u0442\u0435\u0440\u0438\u0441\u0442\u0438\u043A\u0438 \u0430\u0442\u0430\u043A\u0438");
  }
  if (lower.startsWith("class_level:")) {
    const classId = lower.slice("class_level:".length);
    const v = ctx.classLevels?.[classId] ?? 0;
    return addModifier(sink, v, `\u0443\u0440.${classId}`, "\u0443\u0440\u043E\u0432\u0435\u043D\u044C \u043A\u043B\u0430\u0441\u0441\u0430");
  }
  const ability = lower;
  if (ability in ABILITY_LABEL_RU) {
    const v = ctx.abilityMods?.[ability] ?? 0;
    return addModifier(sink, v, ABILITY_LABEL_RU[ability], "\u043C\u043E\u0434\u0438\u0444\u0438\u043A\u0430\u0442\u043E\u0440 \u0445\u0430\u0440\u0430\u043A\u0442\u0435\u0440\u0438\u0441\u0442\u0438\u043A\u0438");
  }
  const variable = ctx.variables?.[lower] ?? ctx.variables?.[id];
  if (variable !== void 0) {
    if (typeof variable === "number") {
      return addModifier(sink, variable, id, "\u043F\u0435\u0440\u0435\u043C\u0435\u043D\u043D\u0430\u044F");
    }
    return pendingDice(variable.count, variable.sides);
  }
  throw new MissingVariableError(id);
}
function parseExpr(tokens, pos, sink) {
  let left = parseTerm(tokens, pos, sink);
  while (pos.i < tokens.length) {
    const tok = tokens[pos.i];
    if (tok.t !== "op" || tok.v !== "+" && tok.v !== "-") break;
    pos.i++;
    const right = parseTerm(tokens, pos, sink);
    const a = forceNumber(left, sink);
    const b = forceNumber(right, sink);
    left = tok.v === "+" ? a + b : a - b;
  }
  return left;
}
function parseTerm(tokens, pos, sink) {
  let left = parseFactor(tokens, pos, sink);
  while (pos.i < tokens.length) {
    const tok = tokens[pos.i];
    if (tok.t === "op" && (tok.v === "*" || tok.v === "/")) {
      pos.i++;
      const right = parseFactor(tokens, pos, sink);
      if (tok.v === "*") {
        left = multiplyValues(left, right, sink);
      } else {
        left = forceNumber(left, sink) / forceNumber(right, sink);
      }
      continue;
    }
    if (typeof left === "number" && tok.t === "dice") {
      pos.i++;
      left = multiplyValues(left, pendingDice(tok.count, tok.sides), sink);
      continue;
    }
    if (typeof left === "number" && tok.t === "id" && tokens[pos.i + 1]?.t !== "lparen") {
      try {
        const peek = resolveId(tok.v, { ...sink, detailed: false });
        if (isDicePending(peek)) {
          pos.i++;
          left = multiplyValues(left, peek, sink);
          continue;
        }
      } catch {
      }
    }
    break;
  }
  return left;
}
function parseFunctionCall(name, tokens, pos, sink) {
  pos.i++;
  const args = [];
  while (pos.i < tokens.length && tokens[pos.i].t !== "rparen") {
    const v = parseExpr(tokens, pos, sink);
    args.push(forceNumber(v, sink));
    const sep = tokens[pos.i];
    if (sep?.t === "op" && sep.v === ",") pos.i++;
    else break;
  }
  if (tokens[pos.i]?.t !== "rparen") throw new FormulaError("\u041E\u0436\u0438\u0434\u0430\u043B\u0430\u0441\u044C \u0437\u0430\u043A\u0440\u044B\u0432\u0430\u044E\u0449\u0430\u044F \u0441\u043A\u043E\u0431\u043A\u0430");
  pos.i++;
  const fn = name.toLowerCase();
  if (fn === "min") return Math.min(...args);
  if (fn === "max") return Math.max(...args);
  throw new FormulaError(`\u041D\u0435\u0438\u0437\u0432\u0435\u0441\u0442\u043D\u0430\u044F \u0444\u0443\u043D\u043A\u0446\u0438\u044F \u0444\u043E\u0440\u043C\u0443\u043B\u044B: ${name}`);
}
function parseFactor(tokens, pos, sink) {
  const tok = tokens[pos.i];
  if (!tok) throw new FormulaError("\u041D\u0435\u0437\u0430\u0432\u0435\u0440\u0448\u0451\u043D\u043D\u0430\u044F \u0444\u043E\u0440\u043C\u0443\u043B\u0430");
  if (tok.t === "num") {
    pos.i++;
    return tok.v;
  }
  if (tok.t === "dice") {
    pos.i++;
    return pendingDice(tok.count, tok.sides);
  }
  if (tok.t === "id") {
    const id = tok.v;
    pos.i++;
    if (tokens[pos.i]?.t === "lparen") {
      return parseFunctionCall(id, tokens, pos, sink);
    }
    return resolveId(id, sink);
  }
  if (tok.t === "lparen") {
    pos.i++;
    const val = parseExpr(tokens, pos, sink);
    if (tokens[pos.i]?.t !== "rparen") throw new FormulaError("\u041E\u0436\u0438\u0434\u0430\u043B\u0430\u0441\u044C \u0437\u0430\u043A\u0440\u044B\u0432\u0430\u044E\u0449\u0430\u044F \u0441\u043A\u043E\u0431\u043A\u0430");
    pos.i++;
    return val;
  }
  if (tok.t === "op" && tok.v === "-") {
    pos.i++;
    const val = parseFactor(tokens, pos, sink);
    return -forceNumber(val, sink);
  }
  throw new FormulaError(`\u041D\u0435\u043E\u0436\u0438\u0434\u0430\u043D\u043D\u044B\u0439 \u0442\u043E\u043A\u0435\u043D: ${JSON.stringify(tok)}`);
}
function evalTokens(tokens, ctx, detailed) {
  const sink = {
    ctx,
    rng: ctx.rng ?? defaultRng,
    dice: [],
    modifiers: [],
    detailed
  };
  const pos = { i: 0 };
  const result = parseExpr(tokens, pos, sink);
  if (pos.i < tokens.length) throw new FormulaError("\u041B\u0438\u0448\u043D\u0438\u0435 \u0441\u0438\u043C\u0432\u043E\u043B\u044B \u0432 \u0444\u043E\u0440\u043C\u0443\u043B\u0435");
  if (typeof result === "string") return result;
  if (isDicePending(result)) return rollDice(result.count, result.sides, sink);
  return result;
}
function evaluate(formula, ctx = {}) {
  if (typeof formula === "number") return formula;
  const trimmed = formula.trim();
  if (!trimmed) throw new FormulaError("\u041F\u0443\u0441\u0442\u0430\u044F \u0444\u043E\u0440\u043C\u0443\u043B\u0430");
  if (MARKERS.has(trimmed.toLowerCase())) return trimmed.toLowerCase();
  return evalTokens(tokenize(trimmed), ctx, false);
}
function formatModifier(m) {
  const sign = m.value >= 0 ? "+" : "";
  return `${sign}${m.value} ${m.source}`;
}
function buildRollText(dice, modifiers, total) {
  const parts = [];
  if (dice.length) {
    const bySides = /* @__PURE__ */ new Map();
    for (const d of dice) {
      if (!bySides.has(d.sides)) bySides.set(d.sides, []);
      bySides.get(d.sides).push(d.result);
    }
    for (const [sides, results] of bySides) {
      parts.push(`\u043A${sides}: ${results.join("+")}`);
    }
  }
  for (const m of modifiers) parts.push(formatModifier(m));
  return parts.length ? `${parts.join(" ")} = ${total}` : String(total);
}
function rollFormula(formula, ctx = {}, opts) {
  const trimmed = formula.trim();
  const fctx = { ...ctx, rng: opts?.rng ?? ctx.rng };
  const sink = {
    ctx: fctx,
    rng: opts?.rng ?? fctx.rng ?? defaultRng,
    dice: [],
    modifiers: [],
    detailed: true
  };
  if (MARKERS.has(trimmed.toLowerCase())) {
    throw new FormulaError(`\u0424\u043E\u0440\u043C\u0443\u043B\u0430-\u043C\u0430\u0440\u043A\u0435\u0440 \xAB${trimmed}\xBB \u043D\u0435 \u0431\u0440\u043E\u0441\u0430\u0435\u0442\u0441\u044F`);
  }
  const tokens = tokenize(trimmed);
  const pos = { i: 0 };
  const result = parseExpr(tokens, pos, sink);
  if (pos.i < tokens.length) throw new FormulaError(`\u041B\u0438\u0448\u043D\u0438\u0435 \u0441\u0438\u043C\u0432\u043E\u043B\u044B \u0432 \u0444\u043E\u0440\u043C\u0443\u043B\u0435 \xAB${formula}\xBB`);
  if (typeof result === "string") throw new FormulaError(`\u0424\u043E\u0440\u043C\u0443\u043B\u0430-\u043C\u0430\u0440\u043A\u0435\u0440 \xAB${result}\xBB \u043D\u0435 \u0431\u0440\u043E\u0441\u0430\u0435\u0442\u0441\u044F`);
  const rolled = isDicePending(result) ? rollDice(result.count, result.sides, sink) : result;
  const extra = (opts?.modifiers || []).reduce((s, m) => s + m.value, 0);
  const allModifiers = [...sink.modifiers, ...opts?.modifiers || []];
  const total = rolled + extra;
  return {
    total,
    dice: sink.dice,
    modifiers: allModifiers,
    text: buildRollText(sink.dice, allModifiers, total)
  };
}

// src/engine/conditions.ts
var ATTACK = (extra) => ({ applies_to: { roll: "attack" }, op: "disadvantage", ...extra });
var ADV_AGAINST = { applies_to: { roll: "attack" }, op: "advantage", scope: "target" };
var SPEED0 = { applies_to: { roll: "speed" }, op: "set", value: "0" };
var INIT = (op) => ({ applies_to: { roll: "initiative" }, op });
var AUTOFAIL = (ability) => ({ applies_to: { roll: "saving_throw", filter: { ability } }, op: "auto_fail" });
var WITHIN_5_FT = {
  kind: "distance_to_condition_owner",
  subject: "roller",
  operator: "lte",
  feet: 5
};
var BEYOND_5_FT = {
  kind: "distance_to_condition_owner",
  subject: "roller",
  operator: "gt",
  feet: 5
};
var ADV_AGAINST_WITHIN_5 = {
  applies_to: { roll: "attack" },
  op: "advantage",
  scope: "target",
  when: [WITHIN_5_FT]
};
var DIS_AGAINST_BEYOND_5 = {
  applies_to: { roll: "attack" },
  op: "disadvantage",
  scope: "target",
  when: [BEYOND_5_FT]
};
var AUTOCRIT_WITHIN_5 = {
  applies_to: { roll: "attack" },
  op: "auto_crit",
  scope: "target",
  when: [WITHIN_5_FT]
};
var DENY = (cap) => ({ applies_to: { roll: cap }, op: "deny" });
var AUTOFAIL_CHECK = (sense) => ({
  applies_to: { roll: "ability_check", filter: { sense } },
  op: "auto_fail"
});
var SOURCE_VISIBLE = { kind: "condition_source_in_line_of_sight" };
var TARGET_IS_SOURCE = { kind: "roll_target_is_condition_source" };
var TARGET_IS_NOT_SOURCE = { kind: "roll_target_is_not_condition_source" };
var ROLLER_IS_SOURCE = { kind: "roller_is_condition_source" };
var TARGET_CANNOT_SEE_OWNER = {
  kind: "observer_can_see_condition_owner",
  observer: "roll_target",
  value: false
};
var ROLLER_CANNOT_SEE_OWNER = {
  kind: "observer_can_see_condition_owner",
  observer: "roller",
  value: false
};
var BUILTIN_CONDITION_RULES = {
  blinded: {
    id: "blinded",
    label: "\u041E\u0441\u043B\u0435\u043F\u043B\u0451\u043D",
    modifiers: [AUTOFAIL_CHECK("sight"), ATTACK(), ADV_AGAINST],
    worldFacts: {
      cannot_see: true
    }
  },
  charmed: {
    id: "charmed",
    label: "\u041E\u0447\u0430\u0440\u043E\u0432\u0430\u043D",
    modifiers: [
      // The holder cannot attack/damage the exact source of this condition.
      { applies_to: { roll: "harm" }, op: "deny", when: [TARGET_IS_SOURCE] },
      // The exact source gets Advantage on social checks against the holder.
      {
        applies_to: { roll: "ability_check", filter: { interaction: "social" } },
        op: "advantage",
        scope: "target",
        when: [ROLLER_IS_SOURCE]
      }
    ]
  },
  deafened: {
    id: "deafened",
    label: "\u041E\u0433\u043B\u043E\u0445\u0448\u0438\u0439",
    modifiers: [AUTOFAIL_CHECK("hearing")]
  },
  exhaustion: {
    id: "exhaustion",
    label: "\u0418\u0441\u0442\u043E\u0449\u0435\u043D\u0438\u0435",
    // One persisted condition instance == one level. Repeating these additive
    // payloads naturally gives -2/-5 per level without an Exhaustion branch.
    modifiers: [
      { applies_to: { roll: "d20" }, op: "add", value: "-2" },
      { applies_to: { roll: "speed" }, op: "add", value: "-5" }
    ],
    stacking: { mode: "levels", max: 6 },
    longRest: { removeLevels: 1 },
    thresholds: [{ atLevel: 6, outcome: "death" }]
  },
  frightened: {
    id: "frightened",
    label: "\u0418\u0441\u043F\u0443\u0433\u0430\u043D",
    modifiers: [
      ATTACK({ when: [SOURCE_VISIBLE] }),
      { applies_to: { roll: "ability_check" }, op: "disadvantage", when: [SOURCE_VISIBLE] },
      {
        applies_to: { roll: "movement_toward_condition_source" },
        op: "deny",
        // Unlike the D20 penalty, the prohibition on willingly approaching
        // the source remains even when the source is out of sight.
        when: [TARGET_IS_SOURCE]
      }
    ]
  },
  grappled: {
    id: "grappled",
    label: "\u0421\u0445\u0432\u0430\u0447\u0435\u043D",
    modifiers: [ATTACK({ when: [TARGET_IS_NOT_SOURCE] }), SPEED0]
  },
  incapacitated: {
    id: "incapacitated",
    label: "\u041D\u0435\u0434\u0435\u0435\u0441\u043F\u043E\u0441\u043E\u0431\u0435\u043D",
    modifiers: [
      INIT("disadvantage"),
      DENY("action"),
      DENY("bonus_action"),
      DENY("reaction"),
      DENY("concentration"),
      DENY("speech")
    ]
  },
  invisible: {
    id: "invisible",
    label: "\u041D\u0435\u0432\u0438\u0434\u0438\u043C",
    modifiers: [
      { applies_to: { roll: "attack" }, op: "advantage", when: [TARGET_CANNOT_SEE_OWNER] },
      {
        applies_to: { roll: "attack" },
        op: "disadvantage",
        scope: "target",
        when: [ROLLER_CANNOT_SEE_OWNER]
      },
      INIT("advantage")
    ],
    worldFacts: {
      cannot_be_targeted_by_requires_sight_unless_seen: true
    },
    note: "\u041D\u0435\u043B\u044C\u0437\u044F \u0432\u044B\u0431\u0440\u0430\u0442\u044C \u0446\u0435\u043B\u044C\u044E \u044D\u0444\u0444\u0435\u043A\u0442\u0430, \u0442\u0440\u0435\u0431\u0443\u044E\u0449\u0435\u0433\u043E \u0432\u0438\u0434\u0435\u0442\u044C \u0446\u0435\u043B\u044C, \u0435\u0441\u043B\u0438 \u0441\u043E\u0437\u0434\u0430\u0442\u0435\u043B\u044C \u044D\u0444\u0444\u0435\u043A\u0442\u0430 \u043D\u0435 \u0441\u043F\u043E\u0441\u043E\u0431\u0435\u043D \u0432\u0430\u0441 \u0432\u0438\u0434\u0435\u0442\u044C."
  },
  paralyzed: {
    id: "paralyzed",
    label: "\u041F\u0430\u0440\u0430\u043B\u0438\u0437\u043E\u0432\u0430\u043D",
    modifiers: [ADV_AGAINST, SPEED0, AUTOFAIL("str"), AUTOFAIL("dex"), AUTOCRIT_WITHIN_5],
    includes: ["incapacitated"]
  },
  petrified: {
    id: "petrified",
    label: "\u041E\u043A\u0430\u043C\u0435\u043D\u0435\u043B",
    modifiers: [ADV_AGAINST, SPEED0, AUTOFAIL("str"), AUTOFAIL("dex")],
    payloads: [
      { kind: "resistance", damage_type: "all", value: "resistance" },
      { kind: "condition_immunity", condition: "poisoned" }
    ],
    includes: ["incapacitated"],
    worldFacts: {
      transformed_to_inanimate_substance: true,
      equipment_transformed_with_owner: "nonmagical_worn_and_carried",
      weight_multiplier: 10,
      aging_paused: true
    },
    note: "\u0422\u0440\u0430\u043D\u0441\u0444\u043E\u0440\u043C\u0430\u0446\u0438\u044F \u0432 \u0432\u0435\u0449\u0435\u0441\u0442\u0432\u043E, \u0434\u0435\u0441\u044F\u0442\u0438\u043A\u0440\u0430\u0442\u043D\u044B\u0439 \u0432\u0435\u0441 \u0438 \u043F\u0440\u0435\u043A\u0440\u0430\u0449\u0435\u043D\u0438\u0435 \u0441\u0442\u0430\u0440\u0435\u043D\u0438\u044F \u043E\u0441\u0442\u0430\u044E\u0442\u0441\u044F \u044F\u0432\u043D\u044B\u043C\u0438 \u0444\u0430\u043A\u0442\u0430\u043C\u0438 \u043C\u0438\u0440\u0430."
  },
  poisoned: {
    id: "poisoned",
    label: "\u041E\u0442\u0440\u0430\u0432\u043B\u0435\u043D",
    modifiers: [ATTACK(), { applies_to: { roll: "ability_check" }, op: "disadvantage" }]
  },
  prone: {
    id: "prone",
    label: "\u0420\u0430\u0441\u043F\u043B\u0430\u0441\u0442\u0430\u043D",
    modifiers: [ATTACK(), ADV_AGAINST_WITHIN_5, DIS_AGAINST_BEYOND_5],
    worldFacts: {
      movement_options: ["crawl", "stand", "magic"],
      stand_cost: "half_speed"
    },
    note: "\u041C\u043E\u0436\u043D\u043E \u043F\u043E\u043B\u0437\u0442\u0438, \u043F\u043E\u0442\u0440\u0430\u0442\u0438\u0442\u044C \u043F\u043E\u043B\u043E\u0432\u0438\u043D\u0443 \u0421\u043A\u043E\u0440\u043E\u0441\u0442\u0438 \u043D\u0430 \u0432\u0441\u0442\u0430\u0432\u0430\u043D\u0438\u0435 \u0438\u043B\u0438 \u043F\u0440\u0435\u043A\u0440\u0430\u0442\u0438\u0442\u044C \u0441\u043E\u0441\u0442\u043E\u044F\u043D\u0438\u0435 \u043C\u0430\u0433\u0438\u0435\u0439."
  },
  restrained: {
    id: "restrained",
    label: "\u041E\u043F\u0443\u0442\u0430\u043D",
    modifiers: [ATTACK(), { applies_to: { roll: "saving_throw", filter: { ability: "dex" } }, op: "disadvantage" }, ADV_AGAINST, SPEED0]
  },
  stunned: {
    id: "stunned",
    label: "\u041E\u0448\u0435\u043B\u043E\u043C\u043B\u0451\u043D",
    modifiers: [ADV_AGAINST, AUTOFAIL("str"), AUTOFAIL("dex")],
    includes: ["incapacitated"]
  },
  unconscious: {
    id: "unconscious",
    label: "\u0411\u0435\u0437 \u0441\u043E\u0437\u043D\u0430\u043D\u0438\u044F",
    // PHB 2024 does NOT grant Paralyzed. Its mechanically similar clauses are
    // declared directly so predicates for Paralyzed never match Unconscious.
    modifiers: [ADV_AGAINST, SPEED0, AUTOFAIL("str"), AUTOFAIL("dex"), AUTOCRIT_WITHIN_5],
    includes: ["incapacitated", "prone"],
    // Когда «Без сознания» заканчивается — вы остаётесь Опрокинутым.
    leaves: ["prone"],
    worldFacts: {
      drops_held_items: true,
      unaware_of_surroundings: true
    },
    note: "\u0412\u044B \u043D\u0435 \u043E\u0441\u043E\u0437\u043D\u0430\u0451\u0442\u0435 \u043E\u043A\u0440\u0443\u0436\u0435\u043D\u0438\u0435 \u0438 \u0440\u043E\u043D\u044F\u0435\u0442\u0435 \u0432\u0441\u0451, \u0447\u0442\u043E \u0434\u0435\u0440\u0436\u0438\u0442\u0435."
  }
};
var registry = { ...BUILTIN_CONDITION_RULES };
function conditionRule(value) {
  return registry[value] ?? null;
}
function conditionLabel(value) {
  return registry[value]?.label ?? value;
}
function conditionModifierPayloads(value, seen = /* @__PURE__ */ new Set()) {
  if (seen.has(value)) return [];
  seen.add(value);
  const rule = registry[value];
  if (!rule) return [];
  const out = [...rule.modifiers];
  for (const inc of rule.includes ?? []) out.push(...conditionModifierPayloads(inc, seen));
  return out;
}
function conditionRuntimePayloads(value, seen = /* @__PURE__ */ new Set()) {
  if (seen.has(value)) return [];
  seen.add(value);
  const rule = registry[value];
  if (!rule) return [];
  const out = [...rule.payloads ?? []];
  for (const inc of rule.includes ?? []) out.push(...conditionRuntimePayloads(inc, seen));
  return out;
}
function conditionLevel(state, value) {
  const count = state.activeEffects.filter((entry) => {
    const mechanics = entry.mechanics;
    return mechanics.kind === "condition" && String(mechanics.value ?? "") === value;
  }).length;
  return registry[value]?.stacking?.mode === "levels" ? count : Math.min(1, count);
}
function conditionStacking(value) {
  return registry[value]?.stacking ?? { mode: "binary" };
}
function activeConditionWorldFactValues(state, key) {
  const direct = state.activeEffects.flatMap((entry) => {
    const mechanics = entry.mechanics;
    return mechanics.kind === "condition" && typeof mechanics.value === "string" ? [mechanics.value] : [];
  });
  const conditions = expandConditionSet(direct);
  return [...conditions].flatMap((value) => {
    const facts = registry[value]?.worldFacts;
    return facts && Object.prototype.hasOwnProperty.call(facts, key) ? [facts[key]] : [];
  });
}
function activeConditionWorldFactEnabled(state, key) {
  return activeConditionWorldFactValues(state, key).some((value) => value === true);
}
function conditionThresholdOutcomes(state) {
  return Object.values(registry).flatMap((rule) => {
    const level = conditionLevel(state, rule.id);
    return (rule.thresholds ?? []).filter((threshold) => level >= threshold.atLevel).map((threshold) => ({ condition: rule.id, level, outcome: threshold.outcome }));
  });
}
function conditionLongRestEntries(activeEffects) {
  const retained = [];
  const removed = [];
  const byCondition = /* @__PURE__ */ new Map();
  for (const entry of activeEffects) {
    const mechanics = entry.mechanics;
    if (mechanics.kind !== "condition") {
      retained.push(entry);
      continue;
    }
    const value = String(mechanics.value ?? "");
    const current = byCondition.get(value) ?? [];
    current.push(entry);
    byCondition.set(value, current);
  }
  for (const [value, entries] of byCondition) {
    const declaredRemoval = registry[value]?.longRest?.removeLevels;
    const removeLevels = Math.max(0, Math.floor(declaredRemoval ?? 0));
    const split = Math.min(removeLevels, entries.length);
    removed.push(...entries.slice(0, split));
    retained.push(...entries.slice(split));
  }
  return { retained, removed };
}
function conditionLeaves(value) {
  return registry[value]?.leaves ?? [];
}
function expandConditionSet(values) {
  const out = /* @__PURE__ */ new Set();
  const visit = (v) => {
    if (out.has(v)) return;
    out.add(v);
    for (const inc of registry[v]?.includes ?? []) visit(inc);
  };
  for (const v of values) visit(v);
  return out;
}
var CONDITION_OPTIONS = Object.values(BUILTIN_CONDITION_RULES).map((r) => ({ id: r.id, label: r.label }));

// src/engine/mechanicsView.ts
var PAYLOAD_KINDS = /* @__PURE__ */ new Set([
  "modifier",
  "damage",
  "healing",
  "resource",
  "condition",
  "movement",
  "narrative",
  "temp_hp",
  "set_value",
  "boon",
  "transform",
  "reroll",
  "grant_action",
  "resistance",
  "variable",
  "add_item",
  "condition_immunity"
]);
function isPayload(obj) {
  const kind = String(obj.kind ?? "");
  return PAYLOAD_KINDS.has(kind);
}
function payloadsFromEffects(effects) {
  const out = [];
  for (const eff of effects) {
    const results = eff.result ?? eff.results;
    if (Array.isArray(results)) out.push(...results);
  }
  return out;
}
function payloadsOf(mechOrPayload) {
  if (!mechOrPayload || typeof mechOrPayload !== "object") return [];
  if (isPayload(mechOrPayload)) return [mechOrPayload];
  const effects = mechOrPayload.effects;
  if (Array.isArray(effects)) return payloadsFromEffects(effects);
  const interactions = mechOrPayload.interactions;
  if (Array.isArray(interactions)) return payloadsFromEffects(interactions);
  return [];
}

// src/engine/cardRegistry.ts
var registry2 = /* @__PURE__ */ new Map();
function getCard(id) {
  return registry2.get(id);
}

// src/rules-core/weaponProfile.ts
var PROFILE_KEYS = /* @__PURE__ */ new Set([
  "weapon_type",
  "proficiency_category",
  "attack_ability",
  "damage_lines",
  "versatile_grip",
  "default_attack_mode",
  "attack_modes",
  "properties",
  "heavy",
  "mastery_effect_id",
  "ammo",
  "enchantment",
  "attunement"
]);
var DAMAGE_LINE_KEYS = /* @__PURE__ */ new Set(["dice", "type"]);
var MELEE_MODE_KEYS = /* @__PURE__ */ new Set(["kind", "reach_ft"]);
var RANGED_MODE_KEYS = /* @__PURE__ */ new Set(["kind", "normal_ft", "long_ft"]);
var AMMO_KEYS = /* @__PURE__ */ new Set(["card_id", "name"]);
var ENCHANTMENT_KEYS = /* @__PURE__ */ new Set(["attack_bonus", "damage_bonus", "extra_damage_lines"]);
var ATTUNEMENT_KEYS = /* @__PURE__ */ new Set(["required"]);
var HEAVY_KEYS = /* @__PURE__ */ new Set(["minimum_ability_score", "ability_by_mode", "consequence"]);
var HEAVY_ABILITY_KEYS = /* @__PURE__ */ new Set(["melee", "ranged"]);
var NORMALIZED_PROPERTIES = /* @__PURE__ */ new Set([
  "ammunition",
  "finesse",
  "heavy",
  "light",
  "reach",
  "thrown",
  "two_handed",
  "versatile"
]);
var STABLE_ID = /^[a-z][a-z0-9_]*$/;
var DICE = /^[1-9]\d*d(?:[2468]|1[02]|20|100)$/;
function record(value) {
  return value && typeof value === "object" && !Array.isArray(value) ? value : null;
}
function exactKeys(value, allowed) {
  return Object.keys(value).every((key) => allowed.has(key));
}
function positiveNumber(value) {
  return typeof value === "number" && Number.isFinite(value) && value > 0;
}
function nonNegativeInteger2(value) {
  return Number.isSafeInteger(value) && Number(value) >= 0;
}
function nonBlank(value) {
  return typeof value === "string" && value.trim().length > 0;
}
function parseDamageLine(value, path) {
  const line = record(value);
  if (!line || !exactKeys(line, DAMAGE_LINE_KEYS)) return `${path} must contain only dice and type`;
  if (typeof line.dice !== "string" || !DICE.test(line.dice)) {
    return `${path}.dice must be a normalized dice expression`;
  }
  if (!nonBlank(line.type) || !STABLE_ID.test(line.type)) {
    return `${path}.type must be a normalized non-blank damage type`;
  }
  return { dice: line.dice, type: line.type };
}
function parseAttackMode(value, path) {
  const mode = record(value);
  if (!mode) return `${path} must be an object`;
  if (mode.kind === "melee") {
    if (!exactKeys(mode, MELEE_MODE_KEYS) || !positiveNumber(mode.reach_ft)) {
      return `${path} melee mode requires only a positive reach_ft`;
    }
    return { kind: "melee", reachFt: mode.reach_ft };
  }
  if (mode.kind === "ranged") {
    if (!exactKeys(mode, RANGED_MODE_KEYS) || !positiveNumber(mode.normal_ft) || !positiveNumber(mode.long_ft) || mode.long_ft < mode.normal_ft) {
      return `${path} ranged mode requires positive normal_ft/long_ft with long_ft >= normal_ft`;
    }
    return { kind: "ranged", normalFt: mode.normal_ft, longFt: mode.long_ft };
  }
  return `${path}.kind must be melee or ranged`;
}
function parseWeaponProfile(card) {
  const mechanics = record(card.mechanics);
  const raw = mechanics ? record(mechanics.weapon_profile) : null;
  const label = card.id || "weapon";
  if (!raw) return { valid: false, issue: `${label}: mechanics.weapon_profile is required` };
  if (!exactKeys(raw, PROFILE_KEYS)) {
    return { valid: false, issue: `${label}: mechanics.weapon_profile contains unsupported fields` };
  }
  if (!nonBlank(raw.weapon_type) || !STABLE_ID.test(raw.weapon_type)) {
    return { valid: false, issue: `${label}: weapon_profile.weapon_type is invalid` };
  }
  if (raw.proficiency_category !== "simple" && raw.proficiency_category !== "martial") {
    return { valid: false, issue: `${label}: weapon_profile.proficiency_category is invalid` };
  }
  if (!["str", "dex", "finesse"].includes(String(raw.attack_ability))) {
    return { valid: false, issue: `${label}: weapon_profile.attack_ability is invalid` };
  }
  if (!Array.isArray(raw.damage_lines) || raw.damage_lines.length === 0) {
    return { valid: false, issue: `${label}: weapon_profile.damage_lines must be non-empty` };
  }
  const damageLines = [];
  for (let index = 0; index < raw.damage_lines.length; index += 1) {
    const parsed = parseDamageLine(raw.damage_lines[index], `weapon_profile.damage_lines[${index}]`);
    if (typeof parsed === "string") return { valid: false, issue: `${label}: ${parsed}` };
    damageLines.push(parsed);
  }
  let versatileGrip;
  if (raw.versatile_grip !== void 0) {
    const parsed = parseDamageLine(raw.versatile_grip, "weapon_profile.versatile_grip");
    if (typeof parsed === "string") return { valid: false, issue: `${label}: ${parsed}` };
    versatileGrip = parsed;
  }
  if (raw.default_attack_mode !== "melee" && raw.default_attack_mode !== "ranged") {
    return { valid: false, issue: `${label}: weapon_profile.default_attack_mode is invalid` };
  }
  if (!Array.isArray(raw.attack_modes) || raw.attack_modes.length === 0) {
    return { valid: false, issue: `${label}: weapon_profile.attack_modes must be non-empty` };
  }
  const attackModes = [];
  for (let index = 0; index < raw.attack_modes.length; index += 1) {
    const parsed = parseAttackMode(raw.attack_modes[index], `weapon_profile.attack_modes[${index}]`);
    if (typeof parsed === "string") return { valid: false, issue: `${label}: ${parsed}` };
    attackModes.push(parsed);
  }
  if (new Set(attackModes.map((mode) => mode.kind)).size !== attackModes.length) {
    return { valid: false, issue: `${label}: weapon_profile.attack_modes contains duplicate kinds` };
  }
  if (!attackModes.some((mode) => mode.kind === raw.default_attack_mode)) {
    return { valid: false, issue: `${label}: default attack mode is not declared` };
  }
  if (!Array.isArray(raw.properties) || raw.properties.some((property) => typeof property !== "string" || !NORMALIZED_PROPERTIES.has(property)) || new Set(raw.properties).size !== raw.properties.length) {
    return { valid: false, issue: `${label}: weapon_profile.properties must be unique normalized properties` };
  }
  if (raw.versatile_grip !== void 0 !== raw.properties.includes("versatile")) {
    return { valid: false, issue: `${label}: versatile property and versatile_grip must be declared together` };
  }
  let heavyRule;
  if (raw.heavy !== void 0) {
    const heavy = record(raw.heavy);
    const abilities = heavy ? record(heavy.ability_by_mode) : null;
    if (!heavy || !exactKeys(heavy, HEAVY_KEYS) || !Number.isSafeInteger(heavy.minimum_ability_score) || Number(heavy.minimum_ability_score) <= 0 || !abilities || !exactKeys(abilities, HEAVY_ABILITY_KEYS) || abilities.melee !== "str" || abilities.ranged !== "dex" || heavy.consequence !== "attack_disadvantage") {
      return { valid: false, issue: `${label}: weapon_profile.heavy is invalid` };
    }
    heavyRule = {
      minimumAbilityScore: Number(heavy.minimum_ability_score),
      abilityByMode: { melee: "str", ranged: "dex" },
      consequence: "attack_disadvantage"
    };
  }
  if (heavyRule !== void 0 !== raw.properties.includes("heavy")) {
    return { valid: false, issue: `${label}: heavy property and heavy declaration must agree` };
  }
  if (attackModes.some((mode) => mode.kind === "ranged") && !raw.properties.includes("ammunition") && !raw.properties.includes("thrown")) {
    return { valid: false, issue: `${label}: ranged attack mode requires ammunition or thrown` };
  }
  if (!nonBlank(raw.mastery_effect_id)) {
    return { valid: false, issue: `${label}: weapon_profile.mastery_effect_id is required` };
  }
  let ammo;
  if (raw.ammo === null) {
    ammo = null;
  } else {
    const ammoValue = record(raw.ammo);
    if (!ammoValue || !exactKeys(ammoValue, AMMO_KEYS) || !nonBlank(ammoValue.card_id) || ammoValue.name !== void 0 && !nonBlank(ammoValue.name)) {
      return { valid: false, issue: `${label}: weapon_profile.ammo must be null or a stable card reference` };
    }
    ammo = {
      cardId: ammoValue.card_id,
      ...typeof ammoValue.name === "string" ? { name: ammoValue.name } : {}
    };
  }
  if (ammo !== null !== raw.properties.includes("ammunition")) {
    return { valid: false, issue: `${label}: ammunition property and ammo reference must agree` };
  }
  const enchantment = record(raw.enchantment);
  if (!enchantment || !exactKeys(enchantment, ENCHANTMENT_KEYS) || !nonNegativeInteger2(enchantment.attack_bonus) || !nonNegativeInteger2(enchantment.damage_bonus) || !Array.isArray(enchantment.extra_damage_lines)) {
    return { valid: false, issue: `${label}: weapon_profile.enchantment is invalid` };
  }
  const extraDamageLines = [];
  for (let index = 0; index < enchantment.extra_damage_lines.length; index += 1) {
    const parsed = parseDamageLine(
      enchantment.extra_damage_lines[index],
      `weapon_profile.enchantment.extra_damage_lines[${index}]`
    );
    if (typeof parsed === "string") return { valid: false, issue: `${label}: ${parsed}` };
    extraDamageLines.push(parsed);
  }
  const attunement = record(raw.attunement);
  if (!attunement || !exactKeys(attunement, ATTUNEMENT_KEYS) || typeof attunement.required !== "boolean") {
    return { valid: false, issue: `${label}: weapon_profile.attunement is invalid` };
  }
  return {
    valid: true,
    profile: {
      weaponType: raw.weapon_type,
      proficiencyCategory: raw.proficiency_category,
      attackAbility: raw.attack_ability,
      damageLines,
      ...versatileGrip ? { versatileGrip } : {},
      defaultAttackMode: raw.default_attack_mode,
      attackModes,
      properties: [...raw.properties],
      ...heavyRule ? { heavyRule } : {},
      masteryEffectId: raw.mastery_effect_id,
      ammo,
      enchantment: {
        attackBonus: enchantment.attack_bonus,
        damageBonus: enchantment.damage_bonus,
        extraDamageLines
      },
      attunement: { required: attunement.required }
    }
  };
}
function evaluateWeaponHeavyRule(profile, mode, abilityScores) {
  if (!profile.heavyRule) return null;
  const ability = profile.heavyRule.abilityByMode[mode];
  const score = abilityScores?.[ability];
  if (!Number.isInteger(score) || Number(score) < 1) {
    return { valid: false, issue: `Heavy weapon requires an authoritative ${ability} ability score` };
  }
  return {
    valid: true,
    disadvantage: Number(score) < profile.heavyRule.minimumAbilityScore,
    ability,
    threshold: profile.heavyRule.minimumAbilityScore
  };
}
function weaponAttackMode(profile, kind) {
  return profile.attackModes.find((mode) => mode.kind === kind);
}
function weaponAttackModeAtDistance(profile, distanceFt) {
  if (!Number.isFinite(distanceFt) || distanceFt < 0) return void 0;
  const melee = weaponAttackMode(profile, "melee");
  if (melee?.kind === "melee" && distanceFt <= melee.reachFt) return melee;
  const ranged = weaponAttackMode(profile, "ranged");
  if (ranged?.kind === "ranged" && distanceFt <= ranged.longFt) return ranged;
  return void 0;
}

// src/engine/equipment.ts
function cardProps(card) {
  const p = card.properties;
  if (!p) return [];
  if (Array.isArray(p)) return p.map(String);
  if (typeof p === "object") return Object.values(p).map(String);
  return [];
}
function isShield(card) {
  return card.type === "shield" || card.defense_type === "shield";
}
function isArmorCard(card) {
  if (isShield(card)) return false;
  if (card.defense_type && card.defense_type !== "none") return true;
  if (card.type !== "chest" && card.slot !== "body") return false;
  const props = cardProps(card).map((value) => value.toLowerCase());
  if (props.includes("cloth") || props.includes("clothing")) return false;
  const formula = String(card.bonus_value ?? "");
  return /dex/i.test(formula) || Number(formula.match(/\d+/)?.[0] ?? 0) > 10;
}
function isWearingArmor(state, cards = []) {
  const bodyId = state?.equipment?.body;
  if (!bodyId) return false;
  const card = cards.find((candidate) => candidate.id === bodyId);
  return !!card && isArmorCard(card);
}

// src/engine/circumstances.ts
function activeConditionsOf(state) {
  const raw = [];
  if (!state) return /* @__PURE__ */ new Set();
  for (const e of state.activeEffects) {
    const m = e.mechanics;
    if (m?.kind === "condition" && m.value) raw.push(String(m.value));
  }
  return expandConditionSet(raw);
}
function evaluateCondition(cond, ctx) {
  const kind = String(cond.kind ?? "");
  switch (kind) {
    case "any_of": {
      const of = cond.of ?? [];
      return of.length === 0 || of.some((c) => evaluateCondition(c, ctx));
    }
    case "all_of": {
      const of = cond.of ?? [];
      return of.every((c) => evaluateCondition(c, ctx));
    }
    case "not": {
      const of = cond.of;
      return of ? !evaluateCondition(of, ctx) : true;
    }
    // ПРЕДМЕТНЫЕ ПРЕДИКАТЫ (S2). id из cond.id | cond.value. Оживляют when-гейты «пока предмет X
    // надет/в сумке/настроен» (S2/S6). ВАЖНО: enforced лишь там, где collectModifiers получает evalCtx
    // (боевые броски — execute/turn). Лист (breakdown/AC/ручной бросок) evalCtx пока не передаёт → when
    // там не блокирует (пре-существующее поведение ВСЕХ when-предикатов; сквозной evalCtx — отдельная
    // задача к S6). Closed-by-default: нет id/state → false, и НИКОГДА не бросаем (мягкие guard'ы).
    case "item_equipped": {
      const id = String(cond.id ?? cond.value ?? "");
      if (!id || !ctx.state) return false;
      return Object.values(ctx.state.equipment ?? {}).some((v) => v === id);
    }
    case "item_carried": {
      const id = String(cond.id ?? cond.value ?? "");
      if (!id || !ctx.state) return false;
      if (Object.values(ctx.state.equipment ?? {}).some((v) => v === id)) return true;
      return ((ctx.state.inventory ?? []).find((r) => r.cardId === id)?.qty ?? 0) > 0;
    }
    case "attuned": {
      const id = String(cond.id ?? cond.value ?? "");
      return !!id && (ctx.character?.attunedIds?.includes(id) ?? false);
    }
    case "wearing_armor":
      return isWearingArmor(ctx.state, [
        ...ctx.character?.equippedCards ?? [],
        ...ctx.character?.knownCards ?? []
      ]);
    case "you_have_condition":
      return ctx.activeConditions?.has(String(cond.value)) ?? false;
    case "target_has_condition":
      return ctx.targetConditions?.has(String(cond.value)) ?? false;
    case "save_avoids_condition":
      return ctx.savedConditions?.has(String(cond.value)) ?? false;
    case "condition_source_in_line_of_sight": {
      const sourceId = ctx.conditionSourceId;
      return !!sourceId && ctx.conditionSourceFacts?.[sourceId]?.lineOfSight === true;
    }
    case "roll_target_is_condition_source":
      return !!ctx.conditionSourceId && !!ctx.rollTargetActorId && ctx.rollTargetActorId === ctx.conditionSourceId;
    case "roll_target_is_not_condition_source":
      return !!ctx.conditionSourceId && !!ctx.rollTargetActorId && ctx.rollTargetActorId !== ctx.conditionSourceId;
    case "roller_is_condition_source":
      return !!ctx.conditionSourceId && !!ctx.rollerActorId && ctx.rollerActorId === ctx.conditionSourceId;
    case "distance_to_condition_owner": {
      const ownerId = ctx.conditionOwnerId;
      const subjectId = cond.subject === "roll_target" ? ctx.rollTargetActorId : ctx.rollerActorId;
      if (!ownerId || !subjectId) return false;
      const distance = ctx.distancesFt?.[subjectId]?.[ownerId] ?? ctx.distancesFt?.[ownerId]?.[subjectId];
      const feet = Number(cond.feet);
      if (typeof distance !== "number" || !Number.isFinite(distance) || !Number.isFinite(feet) || feet < 0) return false;
      const observedDistanceFt = distance;
      if (cond.operator === "lte") return observedDistanceFt <= feet;
      if (cond.operator === "lt") return observedDistanceFt < feet;
      if (cond.operator === "gte") return observedDistanceFt >= feet;
      if (cond.operator === "gt") return observedDistanceFt > feet;
      if (cond.operator === "eq") return observedDistanceFt === feet;
      return false;
    }
    case "observer_can_see_condition_owner": {
      const ownerId = ctx.conditionOwnerId;
      const observerId = cond.observer === "roll_target" ? ctx.rollTargetActorId : ctx.rollerActorId;
      if (!ownerId || !observerId || typeof cond.value !== "boolean") return false;
      return ctx.visibility?.[observerId]?.[ownerId] === cond.value;
    }
    case "condition":
      return ctx.savedConditions?.has(String(cond.id ?? cond.value)) ?? false;
    case "has_advantage":
      return ctx.advantageSoFar === "advantage";
    case "attack_weapon_property": {
      const properties = ctx.event?.data?.weaponProperties;
      return ctx.event?.kind === "hit" && Array.isArray(properties) && properties.map(String).includes(String(cond.value));
    }
    case "attack_range":
      return ctx.event?.kind === "hit" && ctx.event.data?.attackRange === cond.value;
    case "attack_advantage_state":
      return ctx.event?.kind === "hit" && ctx.event.data?.advantage === cond.value;
    case "nearby_eligible_ally_to_target":
      return ctx.event?.kind === "hit" && ctx.event.data?.nearbyEligibleAllyToTarget === true;
    case "d20_equals":
      return ctx.lastD20 != null && ctx.lastD20 === Number(cond.value);
    case "narrative":
      return true;
    default:
      return false;
  }
}
function matchesWhen(when, ctx) {
  if (!when || when.length === 0) return true;
  if (!ctx) return true;
  return when.every((c) => evaluateCondition(c, ctx));
}

// src/engine/rollRules.ts
var D20_RULE_OPS = /* @__PURE__ */ new Set(["reroll", "set_die", "crit_range", "outcome", "on_roll", "bonus_die"]);
var DAMAGE_RULE_OPS = /* @__PURE__ */ new Set(["die_bonus", "explode"]);
var ROLL_RULE_OPS = /* @__PURE__ */ new Set([...D20_RULE_OPS, ...DAMAGE_RULE_OPS]);
var num = (v, d = 0) => {
  const n = Number(v);
  return Number.isFinite(n) ? n : d;
};
function matchesNatural(natural, spec) {
  if (spec == null || typeof spec !== "object") return false;
  const s = spec;
  if (s.eq != null) return natural === num(s.eq, NaN);
  if (s.min != null || s.max != null) {
    const min = s.min != null ? num(s.min, -Infinity) : -Infinity;
    const max = s.max != null ? num(s.max, Infinity) : Infinity;
    return natural >= min && natural <= max;
  }
  return false;
}
function d20Faces(rules) {
  let faces = 20;
  for (const r of rules) if (r.op === "set_die") faces = Math.max(faces, num(r.faces ?? r.value, 20));
  return faces;
}
function critRangeShift(rules) {
  let s = 0;
  for (const r of rules) if (r.op === "crit_range") s += num(r.value ?? r.shift, 0);
  return s;
}
function shouldReroll(rules, natural) {
  return rules.some((r) => r.op === "reroll" && (r.natural != null ? matchesNatural(natural, r.natural) : natural <= num(r.value, 1)));
}
function d20DieBonus(rules, faces) {
  let b = 0;
  for (const r of rules) if (r.op === "die_bonus" && num(r.applies_to?.die) === faces) b += num(r.value, 0);
  return b;
}
function rollD20BonusDice(rules, rng) {
  const dice = [];
  for (const rule of rules) {
    if (rule.op !== "bonus_die") continue;
    const rawFaces = Math.floor(num(rule.faces ?? rule.die ?? rule.value, 0));
    if (rawFaces < 2) continue;
    const faces = rawFaces;
    const count = Math.max(1, Math.floor(num(rule.count, 1)));
    const source2 = typeof rule.source === "string" && rule.source.trim() ? rule.source.trim() : void 0;
    const sign = num(rule.sign, 1) < 0 ? -1 : 1;
    for (let index = 0; index < count; index += 1) {
      dice.push({
        sides: faces,
        result: drawDie(rng, faces),
        ...source2 ? { source: source2 } : {},
        ...sign < 0 ? { sign } : {}
      });
    }
  }
  return dice;
}
function outcomeOverride(rules, natural) {
  for (const r of rules) if (r.op === "outcome" && matchesNatural(natural, r.natural)) return String(r.value ?? r.outcome ?? "");
  return void 0;
}
function rollTriggers(rules, natural) {
  const out = [];
  for (const r of rules) {
    if (r.op !== "on_roll" || !matchesNatural(natural, r.natural)) continue;
    const then = r.then ?? r.result;
    if (Array.isArray(then)) out.push(...then);
  }
  return out;
}
function applyDamageDieRules(dice, rules, opts) {
  let delta = 0;
  const out = dice.map((d) => ({ ...d }));
  const exRule = rules.find((r) => r.op === "explode");
  const limit = opts.explodeLimit ?? (exRule ? num(exRule.limit ?? exRule.value, 0) : 0);
  if (limit > 0) {
    let budget = limit;
    for (let i = 0; i < out.length && budget > 0; i += 1) {
      const d = out[i];
      if (d.discarded || d.result < d.sides) continue;
      const nv = drawDie(opts.rng, d.sides);
      out.push({ sides: d.sides, result: nv });
      delta += nv;
      budget -= 1;
    }
  }
  for (const r of rules) {
    if (r.op !== "die_bonus") continue;
    const dieSize = num(r.applies_to?.die);
    const v = num(r.value, 0);
    if (!dieSize || !v) continue;
    for (const d of out) if (!d.discarded && d.sides === dieSize) {
      d.result += v;
      delta += v;
    }
  }
  return { dice: out, delta };
}

// src/engine/modifiers.ts
var D20_TESTS = /* @__PURE__ */ new Set(["attack", "saving_throw", "ability_check", "initiative"]);
function foldAdvantage(hasAdvantage, hasDisadvantage) {
  if (hasAdvantage && hasDisadvantage) return "none";
  if (hasAdvantage) return "advantage";
  if (hasDisadvantage) return "disadvantage";
  return "none";
}
function matchFilter(effectFilter, queryFilter) {
  if (!effectFilter || Object.keys(effectFilter).length === 0) return true;
  if (!queryFilter) return false;
  for (const [k, v] of Object.entries(effectFilter)) {
    if (queryFilter[k] !== v) return false;
  }
  return true;
}
function collectFromPayload(payload, opts, sourceName, out) {
  if (payload.kind !== "modifier") return;
  if (String(payload.scope ?? "self") === "target") return;
  const applies = payload.applies_to;
  if (!applies) return;
  const rollMatch = applies.roll === opts.roll || applies.roll === "d20" && D20_TESTS.has(String(opts.roll));
  if (!rollMatch) return;
  if (!matchFilter(applies.filter, opts.filter)) return;
  const whenCtx = opts.evalCtx ? { ...opts.evalCtx, advantageSoFar: out.advantage } : void 0;
  if (!matchesWhen(payload.when, whenCtx)) return;
  if (ROLL_RULE_OPS.has(String(payload.op ?? ""))) {
    out.rules.push(payload);
    return;
  }
  if (payload.op === "advantage" || payload.op === "disadvantage") {
    if (payload.op === "advantage") out.hasAdvantage = true;
    else out.hasDisadvantage = true;
    out.advantage = foldAdvantage(out.hasAdvantage, out.hasDisadvantage);
    return;
  }
  if (payload.op === "auto_fail") {
    out.autoFail = true;
    return;
  }
  if (payload.op === "deny") {
    out.denied = true;
    return;
  }
  if ((payload.op == null || payload.op === "add") && payload.value != null) {
    const raw = String(payload.value).replace(/^\+/, "");
    let value;
    try {
      const r = evaluate(raw, opts.formulaCtx ?? {});
      value = typeof r === "number" ? r : void 0;
    } catch {
      value = void 0;
    }
    if (value != null && !Number.isNaN(value) && value !== 0) {
      out.modifiers.push({ value, source: String(payload.source ?? sourceName) });
    }
    return;
  }
  if (payload.op === "set" || payload.op === "multiply" || payload.op === "upgrade" || payload.op === "downgrade") {
    if (payload.value == null) return;
    let value;
    try {
      const r = evaluate(String(payload.value).replace(/^\+/, ""), opts.formulaCtx ?? {});
      value = typeof r === "number" ? r : void 0;
    } catch {
      value = void 0;
    }
    if (value == null || Number.isNaN(value)) return;
    out.ops.push({ op: payload.op, value, priority: Number(payload.priority ?? 0) || 0, source: String(payload.source ?? sourceName) });
  }
}
function collectModifiers(state, passives, opts) {
  const out = {
    modifiers: [],
    advantage: "none",
    hasAdvantage: false,
    hasDisadvantage: false,
    ops: [],
    autoFail: false,
    denied: false,
    rules: []
  };
  for (const effect of state.activeEffects) {
    const src = effect.name || "\u044D\u0444\u0444\u0435\u043A\u0442";
    for (const payload of payloadsOf(effect.mechanics)) {
      collectFromPayload(payload, opts, src, out);
      if (payload.kind === "condition" && payload.value) {
        const conditionOpts = {
          ...opts,
          evalCtx: {
            ...opts.evalCtx ?? {},
            conditionSourceId: effect.sourceId,
            // Runtime collection is always for the condition carrier. Prefer
            // persisted ownership, fall back to the stable roller identity.
            conditionOwnerId: effect.ownerId ?? opts.evalCtx?.rollerActorId
          }
        };
        for (const rule of conditionModifierPayloads(String(payload.value))) {
          collectFromPayload({ kind: "modifier", ...rule }, conditionOpts, String(payload.value), out);
        }
      }
    }
  }
  for (const mech of passives) {
    const mode = mech.activation?.mode;
    if (mode != null && mode !== "passive") continue;
    const src = String(mech.name ?? "\u043F\u0430\u0441\u0441\u0438\u0432\u043A\u0430");
    for (const payload of payloadsOf(mech)) {
      collectFromPayload(payload, opts, src, out);
    }
  }
  return out;
}
var OP_REASON = {
  multiply: "\u043C\u043D\u043E\u0436\u0438\u0442\u0435\u043B\u044C",
  downgrade: "\u043D\u0435 \u0432\u044B\u0448\u0435",
  upgrade: "\u043D\u0435 \u043D\u0438\u0436\u0435",
  set: "\u0443\u0441\u0442\u0430\u043D\u043E\u0432\u043B\u0435\u043D\u043E"
};
var OP_ORDER = { multiply: 0, downgrade: 1, upgrade: 2, set: 3 };
function foldModifiers(base2, result) {
  const parts = [...result.modifiers];
  let value = base2 + result.modifiers.reduce((s, m) => s + m.value, 0);
  const ops = [...result.ops].sort((a, b) => OP_ORDER[a.op] - OP_ORDER[b.op] || a.priority - b.priority);
  for (const op of ops) {
    const before = value;
    if (op.op === "multiply") value = Math.trunc(value * op.value);
    else if (op.op === "downgrade") value = Math.min(value, op.value);
    else if (op.op === "upgrade") value = Math.max(value, op.value);
    else value = op.value;
    if (value !== before) parts.push({ value: value - before, source: op.source, reason: OP_REASON[op.op] });
  }
  return { value, parts };
}
function collectRollModifiers(state, passives, appliesTo) {
  return collectModifiers(state, passives, appliesTo);
}
var ACTION_CAPABILITIES = ["action", "bonus_action", "reaction", "concentration"];
function deniedCapabilities(state, passives = []) {
  const out = /* @__PURE__ */ new Set();
  for (const cap of ACTION_CAPABILITIES) {
    if (collectModifiers(state, passives, { roll: cap }).denied) out.add(cap);
  }
  return out;
}
function conditionCapabilityDenied(state, capability, evalCtx, passives = []) {
  return collectModifiers(state, passives, { roll: capability, evalCtx }).denied;
}

// src/mechanics/grantSemantics.ts
function materializeChoiceGrant(template, selectedValue) {
  if (!template?.kind) return null;
  const { value_into: valueInto, ...payload } = template;
  const field = typeof valueInto === "string" && valueInto.trim() ? valueInto : "value";
  return { ...payload, [field]: selectedValue };
}

// src/mechanics/expandChoices.ts
function selectedChoicePayloads(choice, selected) {
  const out = [];
  const opts = choice.options || {};
  const items = Array.isArray(opts.items) ? opts.items : [];
  for (const value of selected) {
    const item = items.find((it) => String(it.id) === value);
    if (item && Array.isArray(item.grants)) {
      out.push(...item.grants);
      continue;
    }
    const template = choice.apply || choice.grant || {};
    const projected = materializeChoiceGrant(template, value);
    if (projected) {
      out.push(projected);
      continue;
    }
    if (String(opts.source) === "feat") {
      out.push({ kind: "grant_feat", value });
    } else if (String(opts.source) === "item") {
      const q = item && item.qty != null ? Number(item.qty) : 1;
      out.push({ kind: "add_item", card_id: value, qty: Math.max(1, Math.floor(q) || 1) });
    }
  }
  return out;
}
var RESIST_LEVELS = /* @__PURE__ */ new Set(["immunity", "resistance", "vulnerability"]);
function normalizeChoicePayload(p) {
  if (p.kind === "resistance" && p.damage_type == null && p.value != null && !RESIST_LEVELS.has(String(p.value))) {
    return { ...p, damage_type: p.value, value: "resistance" };
  }
  return p;
}

// src/engine/dispatch.ts
function listenerFrom(mech, name) {
  const act = mech.activation;
  const mode = String(act?.mode ?? "");
  if (mode !== "triggered" && mode !== "reaction") return null;
  const uses = mech.uses;
  const trig = act?.trigger;
  return {
    id: String(mech.id ?? name),
    name,
    mechanics: mech,
    mode,
    cost: act?.cost ?? [],
    usesPer: uses?.per != null ? String(uses.per) : void 0,
    optional: act?.optional === true || trig?.prompt === true
  };
}
function collectListeners(ev, state, passives, evalCtx) {
  const out = [];
  const sources = [
    ...state.activeEffects.map((e) => ({ name: e.name, mech: e.mechanics })),
    ...passives.map((m, i) => ({ name: String(m.name ?? `\u043F\u0430\u0441\u0441\u0438\u0432\u043A\u0430 ${i}`), mech: m }))
  ];
  for (const { name, mech } of sources) {
    if (!mech || typeof mech !== "object") continue;
    const act = mech.activation;
    const trig = act?.trigger;
    if (!trig || String(trig.event ?? "") !== ev.kind) continue;
    const trigTiming = trig.timing != null ? String(trig.timing) : void 0;
    if (ev.timing && trigTiming && trigTiming !== ev.timing) continue;
    if (!matchesWhen(
      trig.circumstances,
      { ...evalCtx ?? {}, event: { kind: ev.kind, ...ev.data ? { data: ev.data } : {} } }
    )) continue;
    const lm = listenerFrom(mech, name);
    if (lm) out.push(lm);
  }
  return out;
}
function isAuto(m) {
  return m.mode === "triggered" && m.cost.length === 0 && !m.optional;
}
function toOffer(m, ev) {
  return {
    listenerId: m.id,
    name: m.name,
    mechanics: m.mechanics,
    cost: m.cost,
    event: { kind: ev.kind, ...ev.timing ? { timing: ev.timing } : {} }
  };
}

// src/engine/concentration.ts
function concentrationEntry(state) {
  return state.activeEffects.find((e) => e.mechanics?.kind === "concentration") ?? null;
}
function storedEffectIds(entry) {
  const ids = entry.mechanics.effectIds;
  return Array.isArray(ids) ? ids.filter((id) => typeof id === "string") : [];
}
function fallbackLinkedEffects(state, entry, preserveIds = /* @__PURE__ */ new Set()) {
  return state.activeEffects.filter((effect) => {
    if (effect.id === entry.id || preserveIds.has(effect.id)) return false;
    const duration = effect.mechanics.duration;
    return duration?.concentration === true && effect.source === entry.source;
  });
}
function linkedEffects(state, entry, preserveIds = /* @__PURE__ */ new Set()) {
  const ids = storedEffectIds(entry);
  if (ids.length) {
    const linked = new Set(ids);
    return state.activeEffects.filter((effect) => linked.has(effect.id) && !preserveIds.has(effect.id));
  }
  return fallbackLinkedEffects(state, entry, preserveIds);
}
function removeConcentrationGroup(state, entry, preserveIds = /* @__PURE__ */ new Set()) {
  const removed = linkedEffects(state, entry, preserveIds);
  const removedIds = /* @__PURE__ */ new Set([entry.id, ...removed.map((effect) => effect.id)]);
  return {
    state: {
      ...state,
      activeEffects: state.activeEffects.filter((effect) => !removedIds.has(effect.id))
    },
    removed
  };
}
function dropConcentration(state, reason) {
  const prev = concentrationEntry(state);
  if (!prev) return { state, events: [] };
  const removed = removeConcentrationGroup(state, prev);
  return {
    state: removed.state,
    events: [
      { type: "effect_expired", name: prev.name },
      ...removed.removed.map((effect) => ({ type: "effect_expired", name: effect.name })),
      { type: "narrative", text: `\u041A\u043E\u043D\u0446\u0435\u043D\u0442\u0440\u0430\u0446\u0438\u044F \u043F\u043E\u0442\u0435\u0440\u044F\u043D\u0430 (${reason}).` }
    ]
  };
}
function concentrationDC(damage) {
  return Math.min(30, Math.max(10, Math.floor(damage / 2)));
}

// src/engine/roll.ts
function formatMod(m) {
  const sign = m.value >= 0 ? "+" : "";
  return `${sign}${m.value} ${m.source}`;
}
function buildD20Text(dice, modifiers, total, target2, outcome, dieBonus = 0, bonusDice = []) {
  const parts = [];
  const kept = dice.filter((d) => !d.discarded);
  const faces = kept[0]?.sides ?? dice[0]?.sides ?? 20;
  const label = `\u043A${faces}`;
  const discarded = dice.filter((d) => d.discarded);
  if (kept.length === 1) {
    const dropTxt = discarded.length ? ` (\u043E\u0442\u0431\u0440\u043E\u0448\u0435\u043D\u043E ${discarded.map((d) => d.result).join(", ")})` : "";
    parts.push(`${label}: ${kept[0].result}${dropTxt}`);
  } else if (kept.length > 1) {
    parts.push(`${label}: ${kept.map((d) => d.result).join(", ")}`);
  } else if (dice.length === 2) {
    const k = dice.find((d) => !d.discarded);
    const disc = dice.find((d) => d.discarded);
    if (k && disc) parts.push(`${label}: ${k.result} (\u043E\u0442\u0431\u0440\u043E\u0448\u0435\u043D\u043E ${disc.result})`);
  }
  if (dieBonus) parts.push(`${dieBonus >= 0 ? "+" : ""}${dieBonus} \u043A\u043E\u0441\u0442\u044C`);
  if (bonusDice.length) {
    parts.push(bonusDice.map((die, index) => {
      const negative = die.sign === -1;
      const operator = negative ? "\u2212" : index === 0 ? "+" : "+";
      return `${operator} \u043A${die.sides}: ${die.result}${die.source ? ` (${die.source})` : ""}`;
    }).join(" "));
  }
  for (const m of modifiers) parts.push(formatMod(m));
  let text = parts.join(" ");
  if (parts.length > 1 || modifiers.length) text += ` = ${total}`;
  else if (kept.length) text = `${label}: ${kept[0].result}`;
  if (target2) {
    const tlabel = target2.type === "ac" ? "\u041A\u0417" : "\u0421\u041B";
    text += ` \u043F\u0440\u043E\u0442\u0438\u0432 ${tlabel} ${target2.value}`;
    if (outcome === "crit") text += " \u2014 \u043A\u0440\u0438\u0442";
    else if (outcome === "crit_miss") text += " \u2014 \u043A\u0440\u0438\u0442. \u043F\u0440\u043E\u043C\u0430\u0445";
    else if (outcome === "hit") text += " \u2014 \u043F\u043E\u043F\u0430\u0434\u0430\u043D\u0438\u0435";
    else if (outcome === "miss") text += " \u2014 \u043F\u0440\u043E\u043C\u0430\u0445";
    else if (outcome === "success") text += " \u2014 \u0443\u0441\u043F\u0435\u0445";
    else if (outcome === "fail") text += " \u2014 \u043F\u0440\u043E\u0432\u0430\u043B";
  }
  return text;
}
function rollD20(opts) {
  const rng = opts.rng;
  const advantage = opts.advantage ?? "none";
  const modifiers = opts.modifiers ?? [];
  const modSum = modifiers.reduce((s, m) => s + m.value, 0);
  const rules = opts.rules ?? [];
  const faces = d20Faces(rules);
  const dice = [];
  let natural;
  if (advantage === "advantage" || advantage === "disadvantage") {
    const d1 = drawDie(rng, faces);
    const d2 = drawDie(rng, faces);
    const takeHigh = advantage === "advantage";
    const kept = takeHigh ? Math.max(d1, d2) : Math.min(d1, d2);
    const dropped = takeHigh ? Math.min(d1, d2) : Math.max(d1, d2);
    dice.push({ sides: faces, result: kept });
    dice.push({ sides: faces, result: dropped, discarded: true });
    natural = kept;
  } else {
    natural = drawDie(rng, faces);
    dice.push({ sides: faces, result: natural });
  }
  if (shouldReroll(rules, natural)) {
    const kept = dice.find((d) => !d.discarded);
    if (kept) kept.discarded = true;
    natural = drawDie(rng, faces);
    dice.push({ sides: faces, result: natural });
  }
  const dieBonus = d20DieBonus(rules, faces);
  const bonusDice = rollD20BonusDice(rules, rng);
  const bonusDiceTotal = bonusDice.reduce((sum, die) => sum + die.result * (die.sign ?? 1), 0);
  const total = natural + dieBonus + bonusDiceTotal + modSum;
  const critAt = (opts.critRange ?? 20) + critRangeShift(rules);
  let outcome;
  if (opts.target) {
    if (opts.target.type === "ac") {
      if (natural <= 1) outcome = "miss";
      else if (natural >= critAt) outcome = "crit";
      else outcome = total >= opts.target.value ? "hit" : "miss";
    } else {
      outcome = total >= opts.target.value ? "success" : "fail";
    }
  }
  const forced = outcomeOverride(rules, natural);
  if (forced) outcome = forced;
  const triggered = rollTriggers(rules, natural);
  return {
    kind: "d20",
    dice: [...dice, ...bonusDice],
    advantage,
    modifiers,
    total,
    target: opts.target,
    outcome,
    text: buildD20Text(dice, modifiers, total, opts.target, outcome, dieBonus, bonusDice),
    ...triggered.length ? { triggered } : {}
  };
}
function retargetAttackRoll(roll, targetAc) {
  if (roll.kind !== "d20") throw new Error("Only d20 attack rolls can be retargeted");
  const natural = roll.dice.find((die) => !die.discarded)?.result;
  if (natural == null) throw new Error("Attack roll has no kept die");
  const outcome = roll.outcome === "crit" || roll.outcome === "crit_miss" ? roll.outcome : natural <= 1 ? "miss" : roll.total >= targetAc ? "hit" : "miss";
  const baseText = roll.text.replace(/ против КЗ .*$/, "");
  const suffix = outcome === "crit" ? " \u2014 \u043A\u0440\u0438\u0442" : outcome === "crit_miss" ? " \u2014 \u043A\u0440\u0438\u0442. \u043F\u0440\u043E\u043C\u0430\u0445" : outcome === "hit" ? " \u2014 \u043F\u043E\u043F\u0430\u0434\u0430\u043D\u0438\u0435" : " \u2014 \u043F\u0440\u043E\u043C\u0430\u0445";
  return {
    ...roll,
    target: { type: "ac", value: targetAc },
    outcome,
    text: `${baseText} \u043F\u0440\u043E\u0442\u0438\u0432 \u041A\u0417 ${targetAc}${suffix}`
  };
}

// utils/weapon_types.json
var weapon_types_default = {
  name: "weapon_types",
  russian_name: "\u0422\u0438\u043F\u044B \u043E\u0440\u0443\u0436\u0438\u044F",
  type: "weapon_type",
  basic: [
    {
      name: "simple_melee",
      russian_name: "\u041F\u0440\u043E\u0441\u0442\u043E\u0435 \u0440\u0443\u043A\u043E\u043F\u0430\u0448\u043D\u043E\u0435",
      weapons: [
        {
          name: "quarterstaff",
          russian_name: "\u0411\u043E\u0435\u0432\u043E\u0439 \u043F\u043E\u0441\u043E\u0445"
        },
        {
          name: "mace",
          russian_name: "\u0411\u0443\u043B\u0430\u0432\u0430"
        },
        {
          name: "club",
          russian_name: "\u0414\u0443\u0431\u0438\u043D\u043A\u0430"
        },
        {
          name: "dagger",
          russian_name: "\u041A\u0438\u043D\u0436\u0430\u043B"
        },
        {
          name: "spear",
          russian_name: "\u041A\u043E\u043F\u044C\u0451"
        },
        {
          name: "light_hammer",
          russian_name: "\u041B\u0451\u0433\u043A\u0438\u0439 \u043C\u043E\u043B\u043E\u0442"
        },
        {
          name: "javelin",
          russian_name: "\u041C\u0435\u0442\u0430\u0442\u0435\u043B\u044C\u043D\u043E\u0435 \u043A\u043E\u043F\u044C\u0451"
        },
        {
          name: "greatclub",
          russian_name: "\u041F\u0430\u043B\u0438\u0446\u0430"
        },
        {
          name: "handaxe",
          russian_name: "\u0420\u0443\u0447\u043D\u043E\u0439 \u0442\u043E\u043F\u043E\u0440"
        },
        {
          name: "sickle",
          russian_name: "\u0421\u0435\u0440\u043F"
        }
      ]
    },
    {
      name: "simple_ranged",
      russian_name: "\u041F\u0440\u043E\u0441\u0442\u043E\u0435 \u0434\u0430\u043B\u044C\u043D\u043E\u0431\u043E\u0439\u043D\u043E\u0435",
      weapons: [
        {
          name: "light_crossbow",
          russian_name: "\u0410\u0440\u0431\u0430\u043B\u0435\u0442, \u043B\u0451\u0433\u043A\u0438\u0439"
        },
        {
          name: "dart",
          russian_name: "\u0414\u0440\u043E\u0442\u0438\u043A"
        },
        {
          name: "shortbow",
          russian_name: "\u041A\u043E\u0440\u043E\u0442\u043A\u0438\u0439 \u043B\u0443\u043A"
        },
        {
          name: "sling",
          russian_name: "\u041F\u0440\u0430\u0449\u0430"
        }
      ]
    },
    {
      name: "martial_melee",
      russian_name: "\u0412\u043E\u0438\u043D\u0441\u043A\u043E\u0435 \u0440\u0443\u043A\u043E\u043F\u0430\u0448\u043D\u043E\u0435",
      weapons: [
        {
          name: "halberd",
          russian_name: "\u0410\u043B\u0435\u0431\u0430\u0440\u0434\u0430"
        },
        {
          name: "war_pick",
          russian_name: "\u0411\u043E\u0435\u0432\u0430\u044F \u043A\u0438\u0440\u043A\u0430"
        },
        {
          name: "warhammer",
          russian_name: "\u0411\u043E\u0435\u0432\u043E\u0439 \u043C\u043E\u043B\u043E\u0442"
        },
        {
          name: "battleaxe",
          russian_name: "\u0411\u043E\u0435\u0432\u043E\u0439 \u0442\u043E\u043F\u043E\u0440"
        },
        {
          name: "glaive",
          russian_name: "\u0413\u043B\u0435\u0444\u0430"
        },
        {
          name: "greatsword",
          russian_name: "\u0414\u0432\u0443\u0440\u0443\u0447\u043D\u044B\u0439 \u043C\u0435\u0447"
        },
        {
          name: "lance",
          russian_name: "\u0414\u043B\u0438\u043D\u043D\u043E\u0435 \u043A\u043E\u043F\u044C\u0451"
        },
        {
          name: "longsword",
          russian_name: "\u0414\u043B\u0438\u043D\u043D\u044B\u0439 \u043C\u0435\u0447"
        },
        {
          name: "whip",
          russian_name: "\u041A\u043D\u0443\u0442"
        },
        {
          name: "shortsword",
          russian_name: "\u041A\u043E\u0440\u043E\u0442\u043A\u0438\u0439 \u043C\u0435\u0447"
        },
        {
          name: "maul",
          russian_name: "\u041C\u043E\u043B\u043E\u0442"
        },
        {
          name: "morningstar",
          russian_name: "\u041C\u043E\u0440\u0433\u0435\u043D\u0448\u0442\u0435\u0440\u043D"
        },
        {
          name: "pike",
          russian_name: "\u041F\u0438\u043A\u0430"
        },
        {
          name: "rapier",
          russian_name: "\u0420\u0430\u043F\u0438\u0440\u0430"
        },
        {
          name: "greataxe",
          russian_name: "\u0421\u0435\u043A\u0438\u0440\u0430"
        },
        {
          name: "scimitar",
          russian_name: "\u0421\u043A\u0438\u043C\u0438\u0442\u0430\u0440"
        },
        {
          name: "trident",
          russian_name: "\u0422\u0440\u0435\u0437\u0443\u0431\u0435\u0446"
        },
        {
          name: "flail",
          russian_name: "\u0426\u0435\u043F"
        }
      ]
    },
    {
      name: "martial_ranged",
      russian_name: "\u0412\u043E\u0438\u043D\u0441\u043A\u043E\u0435 \u0434\u0430\u043B\u044C\u043D\u043E\u0431\u043E\u0439\u043D\u043E\u0435",
      weapons: [
        {
          name: "hand_crossbow",
          russian_name: "\u0410\u0440\u0431\u0430\u043B\u0435\u0442, \u0440\u0443\u0447\u043D\u043E\u0439"
        },
        {
          name: "heavy_crossbow",
          russian_name: "\u0410\u0440\u0431\u0430\u043B\u0435\u0442, \u0442\u044F\u0436\u0451\u043B\u044B\u0439"
        },
        {
          name: "longbow",
          russian_name: "\u0414\u043B\u0438\u043D\u043D\u044B\u0439 \u043B\u0443\u043A"
        },
        {
          name: "blowgun",
          russian_name: "\u0414\u0443\u0445\u043E\u0432\u0430\u044F \u0442\u0440\u0443\u0431\u043A\u0430"
        },
        {
          name: "musket",
          russian_name: "\u041C\u0443\u0448\u043A\u0435\u0442"
        },
        {
          name: "pistol",
          russian_name: "\u041F\u0438\u0441\u0442\u043E\u043B\u044C"
        },
        {
          name: "net",
          russian_name: "\u0421\u0435\u0442\u044C"
        }
      ]
    }
  ]
};

// src/engine/weapon.ts
function normalizeWeaponProficiencyId(value) {
  const normalized3 = value.trim().toLowerCase().replace(/[\s-]+/g, "_");
  if (["all_weapon", "all_weapons", "weapon", "weapons"].includes(normalized3)) return "all";
  if (["simple_weapon", "simple_weapons"].includes(normalized3)) return "simple";
  if (["martial_weapon", "martial_weapons"].includes(normalized3)) return "martial";
  return normalized3;
}
var WEAPON_GROUP_BY_TYPE = new Map(
  (weapon_types_default.basic ?? []).flatMap((group) => {
    const groupId = normalizeWeaponProficiencyId(String(group.name ?? ""));
    return (group.weapons ?? []).flatMap((weapon) => {
      const weaponType = normalizeWeaponProficiencyId(String(weapon.name ?? ""));
      return groupId && weaponType ? [[weaponType, groupId]] : [];
    });
  })
);
function isWeaponProficient(character, weaponType, declaredCategory) {
  if (character.weaponProficiencies === void 0) return true;
  const normalizedType = normalizeWeaponProficiencyId(String(weaponType ?? ""));
  if (!normalizedType) return false;
  const grants = new Set(character.weaponProficiencies.map(normalizeWeaponProficiencyId));
  if (grants.has("all") || grants.has(normalizedType)) return true;
  const group = WEAPON_GROUP_BY_TYPE.get(normalizedType);
  if (!group && !declaredCategory) return false;
  const broadCategory = declaredCategory ?? (group?.startsWith("simple_") ? "simple" : group?.startsWith("martial_") ? "martial" : group);
  return (group ? grants.has(group) : false) || (broadCategory ? grants.has(broadCategory) : false);
}
function cardById(ctx, id) {
  if (!id) return void 0;
  return (ctx.equippedCards ?? []).find((c) => c.id === id) ?? (ctx.knownCards ?? []).find((c) => c.id === id);
}
function pickAbility(profile, character) {
  const best = () => (character.abilityMods.dex ?? 0) > (character.abilityMods.str ?? 0) ? "dex" : "str";
  return profile.attackAbility === "finesse" ? best() : profile.attackAbility;
}
function itemBonusesActive(card, profile, character) {
  if (!profile.attunement.required) return true;
  if (character.attunedIds == null) return false;
  return character.attunedIds.includes(card.id);
}
function weaponDamages(profile, twoHandedGrip, magic) {
  const out = profile.damageLines.map((line) => ({ ...line }));
  if (twoHandedGrip && profile.versatileGrip) out[0] = { ...profile.versatileGrip };
  if (magic) out.push(...profile.enchantment.extraDamageLines.map((line) => ({ ...line })));
  return out;
}
function cardToWeapon(card, character, twoHandedGrip = false) {
  const parsed = parseWeaponProfile(card);
  if (!parsed.valid) return null;
  const { profile } = parsed;
  const magic = itemBonusesActive(card, profile, character);
  const damages = weaponDamages(profile, twoHandedGrip, magic);
  return {
    cardId: card.id,
    name: card.name,
    dice: damages[0].dice,
    ability: pickAbility(profile, character),
    damageType: damages[0].type,
    damages,
    enchant: magic ? profile.enchantment.attackBonus : 0,
    attackEnchant: magic ? profile.enchantment.attackBonus : 0,
    damageEnchant: magic ? profile.enchantment.damageBonus : 0,
    properties: [...profile.properties],
    ...profile.heavyRule ? {
      heavyRule: {
        ...profile.heavyRule,
        abilityByMode: { ...profile.heavyRule.abilityByMode }
      }
    } : {},
    weaponType: profile.weaponType,
    proficiencyCategory: profile.proficiencyCategory,
    defaultAttackMode: profile.defaultAttackMode,
    attackModes: profile.attackModes.map((mode) => ({ ...mode })),
    // Искусность — НЕ магическое свойство: работает и без настройки (гейт — выбор персонажа).
    mastery: profile.masteryEffectId
  };
}
function weaponContext(character, hand, equipment) {
  const slot = hand === "main" ? "main_hand" : "off_hand";
  if (equipment) {
    const card2 = cardById(character, equipment[slot]);
    const twoHandedGrip = hand === "main" && !equipment.off_hand;
    if (card2?.type === "weapon") return cardToWeapon(card2, character, twoHandedGrip);
    return null;
  }
  const weapons = (character.equippedCards ?? []).filter((c) => c.type === "weapon");
  const card = hand === "main" ? weapons[0] : weapons[1];
  if (!card) return null;
  return cardToWeapon(card, character);
}
function matchedAttackEffect(mechanics) {
  const effects = Array.isArray(mechanics?.effects) ? mechanics.effects : [];
  for (const e of effects) {
    if (String(e.resolution ?? "") !== "attack_roll") continue;
    if (String(e.attack_kind ?? "") === "unarmed") return e;
    const onHit = Array.isArray(e.on_hit) ? e.on_hit : [];
    if (onHit.some((p) => p.dice === "weapon")) return e;
  }
  return null;
}
function weaponAttackKind(mechanics) {
  const e = matchedAttackEffect(mechanics);
  if (!e) return null;
  if (String(e.attack_kind ?? "") === "unarmed") return "unarmed";
  const tags = Array.isArray(e.tags) ? e.tags.map(String) : [];
  return tags.includes("off_hand") ? "off" : "main";
}
function attackRangeFromEffect(effect, hand, character, equipment) {
  if (String(effect.attack_kind ?? "") === "unarmed") return "melee";
  const onHit = Array.isArray(effect.on_hit) ? effect.on_hit : [];
  if (!onHit.some((p) => p.dice === "weapon")) return void 0;
  const w = weaponContext(character, hand, equipment);
  if (!w) return void 0;
  const declared = String(effect.attack_kind ?? "");
  const requested = declared === "weapon_ranged" ? "ranged" : declared === "weapon_melee" ? "melee" : w.defaultAttackMode;
  return w.attackModes.some((mode) => mode.kind === requested) ? requested : void 0;
}
function attackRollQueryFacts(effect, hand, character, equipment) {
  const declaredKind = String(effect.attack_kind ?? "").toLowerCase();
  if (declaredKind === "unarmed") return { attackKind: "unarmed" };
  if (declaredKind === "spell") return { attackKind: "spell" };
  const onHit = Array.isArray(effect.on_hit) ? effect.on_hit : [];
  const isWeaponAttack = declaredKind.startsWith("weapon") || onHit.some((payload) => payload.dice === "weapon");
  if (!isWeaponAttack) return { attackKind: "spell" };
  const weapon = weaponContext(character, hand, equipment);
  return {
    attackKind: "weapon",
    ...weapon ? { weaponCategory: weapon.defaultAttackMode } : {}
  };
}
function extraAttackSourceFromEffect(effect, hand, character, equipment) {
  const tags = Array.isArray(effect.tags) ? effect.tags.map(String) : [];
  const canonicalLightExtra = tags.includes("light_property_extra_attack");
  if (!canonicalLightExtra && (hand !== "off" || !tags.includes("two_weapon"))) return "none";
  const main = weaponContext(character, "main", equipment);
  const off = weaponContext(character, "off", equipment);
  if (!main || !off || main.cardId === off.cardId) return "other";
  const mainLight = main.properties.map((value) => value.toLowerCase()).includes("light");
  const offLight = off.properties.map((value) => value.toLowerCase()).includes("light");
  return mainLight && offLight ? "light_property" : "other";
}
var EQUIPPED_WEAPON_AMMO_RESOURCE = "equipped_weapon_ammo";
function selectedWeaponForMechanics(mechanics, equipment, cardsById) {
  const kind = weaponAttackKind(mechanics);
  if (kind !== "main" && kind !== "off") return null;
  const slotId = kind === "main" ? equipment?.main_hand : equipment?.off_hand;
  const weapon = slotId ? cardsById.get(slotId) : void 0;
  if (!slotId || !weapon || weapon.type !== "weapon") {
    throw new Error(`weapon action requires a weapon in the ${kind} hand`);
  }
  const parsed = parseWeaponProfile(weapon);
  if (!parsed.valid) throw new Error(parsed.issue);
  return { hand: kind, weapon, profile: parsed.profile };
}
function bindEquippedWeaponProfileTargeting(mechanics, equipment, cardsById) {
  if (!mechanics) return {};
  const selected = selectedWeaponForMechanics(mechanics, equipment, cardsById);
  if (!selected) return mechanics;
  const targeting = mechanics.targeting;
  if (!targeting || typeof targeting !== "object" || Array.isArray(targeting)) {
    throw new Error("weapon action requires explicit mechanics.targeting");
  }
  const ranges = selected.profile.attackModes.map((mode) => mode.kind === "melee" ? mode.reachFt : mode.longFt);
  if (!ranges.length || ranges.some((range) => !Number.isFinite(range) || range <= 0)) {
    throw new Error(`${selected.weapon.id}: weapon_profile has no usable attack range`);
  }
  return {
    ...mechanics,
    targeting: { ...targeting, range_ft: Math.max(...ranges) }
  };
}
function declaredWeaponAmmo(weapon) {
  const parsed = parseWeaponProfile(weapon);
  if (!parsed.valid) throw new Error(parsed.issue);
  return parsed.profile.ammo;
}
function bindEquippedWeaponAmmoCost(mechanics, equipment, cardsById) {
  if (!mechanics) return {};
  const activation = mechanics.activation;
  if (!Array.isArray(activation?.cost)) return mechanics;
  const contextual = activation.cost.filter((entry) => entry?.resource === EQUIPPED_WEAPON_AMMO_RESOURCE);
  if (!contextual.length) return mechanics;
  if (contextual.length !== 1) {
    throw new Error("activation.cost must contain at most one equipped_weapon_ammo entry");
  }
  const marker = contextual[0];
  if (Object.keys(marker).some((key) => !["resource", "amount"].includes(key)) || !Number.isSafeInteger(marker.amount) || Number(marker.amount) <= 0) {
    throw new Error("activation.cost equipped_weapon_ammo requires only a positive integer amount");
  }
  const kind = weaponAttackKind(mechanics);
  if (kind !== "main" && kind !== "off") {
    throw new Error("activation.cost equipped_weapon_ammo requires a main/off weapon attack");
  }
  const selected = selectedWeaponForMechanics(mechanics, equipment, cardsById);
  if (!selected || selected.hand !== kind) {
    throw new Error(`activation.cost equipped_weapon_ammo requires a weapon in the ${kind} hand`);
  }
  const { weapon } = selected;
  const ammo = declaredWeaponAmmo(weapon);
  const cost = activation.cost.flatMap((entry) => {
    if (entry.resource !== EQUIPPED_WEAPON_AMMO_RESOURCE) return [entry];
    if (!ammo) return [];
    return [{
      resource: "item",
      card_id: ammo.cardId,
      amount: Number(marker.amount),
      ...ammo.name ? { name: ammo.name } : {}
    }];
  });
  return { ...mechanics, activation: { ...activation, cost } };
}
function bindEquippedWeaponActionContext(mechanics, equipment, cardsById) {
  const targeted = bindEquippedWeaponProfileTargeting(mechanics, equipment, cardsById);
  return bindEquippedWeaponAmmoCost(targeted, equipment, cardsById);
}

// src/engine/weaponMastery2024.ts
var WEAPON_MASTERY_CLEAVE_USE_PREFIX = "system:dnd5e-2024:weapon-mastery:cleave:";
var WEAPON_MASTERY_NICK_USE_PREFIX = "system:dnd5e-2024:weapon-mastery:nick:";
function weaponMasteryCleaveUseKey(turnKey) {
  return `${WEAPON_MASTERY_CLEAVE_USE_PREFIX}${turnKey}`;
}
function weaponMasteryNickUseKey(turnKey) {
  return `${WEAPON_MASTERY_NICK_USE_PREFIX}${turnKey}`;
}
function hasUsedCleaveThisTurn(firedThisTurn) {
  return (firedThisTurn ?? []).some((key) => key.startsWith(WEAPON_MASTERY_CLEAVE_USE_PREFIX));
}
var ABILITIES = /* @__PURE__ */ new Set(["str", "dex", "con", "int", "wis", "cha"]);
var CREATURE_SIZE_INDEX = {
  tiny: 0,
  small: 1,
  medium: 2,
  large: 3,
  huge: 4,
  gargantuan: 5
};
function nonBlankString(value) {
  return typeof value === "string" && value.trim() === value && value.length > 0;
}
function positiveFiniteNumber(value) {
  return typeof value === "number" && Number.isFinite(value) && value > 0;
}
function hasExactKeys(primitive, expected) {
  const actual = Object.keys(primitive).sort();
  const sortedExpected = [...expected].sort();
  return actual.length === sortedExpected.length && actual.every((key, index) => key === sortedExpected[index]);
}
function validPrimitiveFields(primitive) {
  switch (primitive.type) {
    case "topple":
      return hasExactKeys(primitive, ["type", "saveAbility", "dc", "condition", "choiceId"]) && ABILITIES.has(String(primitive.saveAbility)) && nonBlankString(primitive.dc) && nonBlankString(primitive.condition) && nonBlankString(primitive.choiceId);
    case "sap":
      return hasExactKeys(primitive, ["type", "consume", "expires"]) && primitive.consume === "next" && primitive.expires === "start_of_source_next_turn";
    case "slow":
      return hasExactKeys(primitive, [
        "type",
        "penaltyFt",
        "requiresDamage",
        "expires",
        "choiceId"
      ]) && positiveFiniteNumber(primitive.penaltyFt) && typeof primitive.requiresDamage === "boolean" && primitive.expires === "start_of_source_next_turn" && nonBlankString(primitive.choiceId);
    case "vex":
      return hasExactKeys(primitive, [
        "type",
        "consume",
        "targetLocked",
        "requiresDamage",
        "expires"
      ]) && primitive.consume === "next" && typeof primitive.targetLocked === "boolean" && typeof primitive.requiresDamage === "boolean" && primitive.expires === "end_of_source_next_turn";
    case "push":
      return hasExactKeys(primitive, ["type", "maxDistanceFt", "maxTargetSize", "choiceId"]) && positiveFiniteNumber(primitive.maxDistanceFt) && typeof primitive.maxTargetSize === "string" && Object.prototype.hasOwnProperty.call(CREATURE_SIZE_INDEX, primitive.maxTargetSize) && nonBlankString(primitive.choiceId);
    case "graze":
      return hasExactKeys(primitive, ["type", "damage", "choiceId"]) && nonBlankString(primitive.damage) && nonBlankString(primitive.choiceId);
    case "nick":
      return hasExactKeys(primitive, ["type", "timing", "maximumPerTurn"]) && primitive.timing === "attack_action" && primitive.maximumPerTurn === 1;
    case "cleave":
      return hasExactKeys(primitive, [
        "type",
        "maximumPerTurn",
        "secondaryWithinPrimaryFt",
        "sameWeapon",
        "positiveAbilityModifier",
        "expires"
      ]) && primitive.maximumPerTurn === 1 && positiveFiniteNumber(primitive.secondaryWithinPrimaryFt) && primitive.sameWeapon === true && primitive.positiveAbilityModifier === false && primitive.expires === "end_of_turn";
    default:
      return false;
  }
}
function weaponMasteryPrimitive(mechanics) {
  const value = mechanics?.weapon_mastery;
  if (!value || typeof value !== "object" || Array.isArray(value)) return null;
  const primitive = value;
  return validPrimitiveFields(primitive) ? primitive : null;
}
function isUseChoiceSelected(choices, choiceId) {
  const raw = choices?.[choiceId];
  const value = Array.isArray(raw) ? raw[0] : raw;
  return ["use", "yes", "true", "apply"].includes(String(value ?? "").toLowerCase());
}
function weaponMasteryEvent(primitive) {
  if (primitive.type === "graze") return "miss";
  if (primitive.type === "nick") return "passive";
  return "hit";
}
function declaredDuration(expiry) {
  switch (expiry) {
    case "start_of_source_next_turn":
      return { type: "until_start_of_source_next_turn" };
    case "end_of_source_next_turn":
      return { type: "until_end_of_source_next_turn" };
    case "end_of_turn":
      return { type: "until_end_of_turn" };
  }
}
function compileWeaponMasteryEffects(primitive, facts) {
  switch (primitive.type) {
    case "topple":
      if (!isUseChoiceSelected(facts.choices, primitive.choiceId)) {
        return [];
      }
      return [{
        resolution: "save",
        who: "target",
        ability: primitive.saveAbility,
        dc: primitive.dc,
        on_fail: [{ kind: "condition", value: primitive.condition }],
        on_success: []
      }];
    case "sap":
      return [{
        resolution: "auto",
        who: "target",
        result: [{
          kind: "modifier",
          applies_to: { roll: "attack" },
          op: "disadvantage",
          consume: primitive.consume,
          duration: declaredDuration(primitive.expires),
          stack_id: "weapon-mastery:sap",
          stack_type: "overwrite"
        }]
      }];
    case "slow":
      if (primitive.requiresDamage && !facts.dealtDamage) return [];
      if (!isUseChoiceSelected(facts.choices, primitive.choiceId)) {
        return [];
      }
      return [{
        resolution: "auto",
        who: "target",
        result: [{
          kind: "modifier",
          applies_to: { roll: "speed" },
          op: "add",
          value: String(-Math.abs(primitive.penaltyFt)),
          duration: declaredDuration(primitive.expires),
          stack_id: "weapon-mastery:slow",
          stack_type: "overwrite"
        }]
      }];
    case "vex": {
      if (primitive.requiresDamage && !facts.dealtDamage) return [];
      if (!facts.targetActorId) return [];
      return [{
        resolution: "auto",
        result: [{
          kind: "modifier",
          applies_to: {
            roll: "attack",
            ...primitive.targetLocked ? { filter: { targetActorId: facts.targetActorId } } : {}
          },
          op: "advantage",
          consume: primitive.consume,
          duration: declaredDuration(primitive.expires),
          stack_id: primitive.targetLocked ? `weapon-mastery:vex:${facts.targetActorId}` : "weapon-mastery:vex",
          stack_type: "overwrite"
        }]
      }];
    }
    case "push": {
      const maxDistanceFt = primitive.maxDistanceFt;
      const raw = facts.choices?.[primitive.choiceId];
      const selected = Array.isArray(raw) ? raw[0] : raw;
      const distanceFt = selected == null ? 0 : Number(selected);
      const targetEligible = Number.isInteger(facts.targetSize) && facts.targetSize <= CREATURE_SIZE_INDEX[primitive.maxTargetSize];
      if (!targetEligible || !Number.isFinite(distanceFt) || distanceFt <= 0 || distanceFt > maxDistanceFt) return [];
      return [{
        resolution: "auto",
        who: "target",
        result: [{ kind: "movement", value: "push", distance: distanceFt }]
      }];
    }
    case "graze":
      if (!isUseChoiceSelected(facts.choices, primitive.choiceId)) {
        return [];
      }
      return [{
        resolution: "auto",
        who: "target",
        result: [{
          kind: "damage",
          amount: primitive.damage,
          // Graze deals the same type as the weapon's primary damage. `weapon`
          // is not a damage type and would bypass typed resistance/immunity.
          type: facts.weapon.damageType,
          suppress_damage_modifiers: true
        }]
      }];
    case "nick":
      return [];
    case "cleave":
      if (facts.attackRange !== "melee" || !facts.targetActorId || hasUsedCleaveThisTurn(facts.firedThisTurn)) return [];
      return [{
        resolution: "auto",
        result: [{
          kind: "attack_follow_up",
          follow_up: "cleave",
          weaponCardId: facts.weapon.cardId,
          primaryTargetActorId: facts.targetActorId,
          ...facts.attackActionId ? { attackActionId: facts.attackActionId } : {},
          ...facts.attackCommandId ? { openedByCommandId: facts.attackCommandId } : {},
          ...facts.sourceEntityId ? { sourceEntityId: facts.sourceEntityId } : {},
          secondaryWithinPrimaryFt: primitive.secondaryWithinPrimaryFt,
          duration: declaredDuration(primitive.expires),
          stack_id: "weapon-mastery:cleave-window",
          stack_type: "overwrite"
        }]
      }];
  }
}
function actorWeaponHasMasteryPrimitive(input) {
  const { weapon } = input;
  if (!weapon?.mastery || !weapon.weaponType || !(input.selectedWeaponTypes ?? []).includes(weapon.weaponType)) return false;
  const mechanics = input.masteryEffects?.[weapon.mastery]?.mechanics;
  const primitive = weaponMasteryPrimitive(
    mechanics && typeof mechanics === "object" && !Array.isArray(mechanics) ? mechanics : void 0
  );
  return primitive?.type === input.type;
}

// src/engine/mastery.ts
function masteryEvent(mech) {
  const activation = mech?.activation;
  const trigger = activation?.trigger;
  return String(trigger?.event ?? "hit") === "miss" ? "miss" : "hit";
}
function knowsMastery(weapon, masteries) {
  if (!weapon?.mastery || !weapon.weaponType) return false;
  return (masteries ?? []).includes(weapon.weaponType);
}
function activeMastery(ctx, state, hand, facts = {}) {
  const weapon = weaponContext(ctx.character, hand, state.equipment);
  if (!knowsMastery(weapon, ctx.character.weaponMasteries)) return null;
  const rec = ctx.masteryEffects?.[weapon.mastery];
  const mech = rec?.mechanics;
  if (!mech || typeof mech !== "object") return null;
  const primitive = weaponMasteryPrimitive(mech);
  if (Object.prototype.hasOwnProperty.call(mech, "weapon_mastery") && !primitive) return null;
  if (!primitive && String(mech.activation?.mode ?? "triggered") === "passive") return null;
  const weaponMod = ctx.character.abilityMods[weapon.ability] ?? 0;
  const mechanics = primitive ? {
    ...mech,
    activation: {
      mode: weaponMasteryEvent(primitive) === "passive" ? "passive" : "triggered",
      trigger: { event: weaponMasteryEvent(primitive) }
    },
    effects: compileWeaponMasteryEffects(primitive, {
      weapon,
      weaponMod,
      targetActorId: ctx.target?.id,
      targetSize: ctx.target?.size,
      attackRange: facts.attackRange,
      dealtDamage: facts.dealtDamage,
      choices: ctx.choices,
      firedThisTurn: state.firedThisTurn,
      attackActionId: ctx.attackActionId,
      attackCommandId: ctx.attackCommandId,
      sourceEntityId: weapon.mastery
    })
  } : mech;
  return {
    id: weapon.mastery,
    name: String(rec?.name ?? "\u0418\u0441\u043A\u0443\u0441\u043D\u043E\u0441\u0442\u044C"),
    mechanics,
    event: primitive ? weaponMasteryEvent(primitive) === "miss" ? "miss" : "hit" : masteryEvent(mech),
    weaponMod,
    ...primitive ? { primitive } : {}
  };
}

// src/engine/execute.ts
var ABILITY_LABEL = {
  str: "\u0421\u0418\u041B",
  dex: "\u041B\u0412\u041A",
  con: "\u0422\u0415\u041B",
  int: "\u0418\u041D\u0422",
  wis: "\u041C\u0414\u0420",
  cha: "\u0425\u0410\u0420"
};
var InsufficientResourcesError = class extends Error {
  constructor(missing) {
    super(`INSUFFICIENT_RESOURCES: ${missing.join(", ")}`);
    this.missing = missing;
    this.name = "InsufficientResourcesError";
  }
};
var MechanicsExecutionError = class extends Error {
  constructor(code, path, message, options) {
    super(`${code} at ${path}: ${message}`, options);
    this.code = code;
    this.path = path;
    this.name = "MechanicsExecutionError";
  }
};
function cloneState(state) {
  return {
    ...state,
    hp: { ...state.hp },
    resources: { ...state.resources },
    maxResources: { ...state.maxResources },
    equipment: { ...state.equipment },
    inventory: state.inventory.map((r) => ({ ...r })),
    activeEffects: state.activeEffects.map((e) => ({ ...e })),
    firedThisTurn: state.firedThisTurn ? [...state.firedThisTurn] : void 0,
    firedThisRest: state.firedThisRest ? [...state.firedThisRest] : void 0
  };
}
function passivesFromCtx(ctx) {
  return ctx.passives ?? [];
}
function formulaCtx(ctx) {
  return {
    abilityMods: ctx.character.abilityMods,
    profBonus: ctx.character.profBonus,
    selfLevel: ctx.character.level,
    classLevels: ctx.character.classLevels,
    spellcastingMod: ctx.character.spellcastingMod,
    variables: ctx.character.variables,
    // Искусность 2024: модификатор характеристики атаки текущим оружием (weapon_mod).
    // Проставляется только на прогоне механики мастерства (см. runAttackRoll).
    weaponMod: ctx.weaponMod,
    rng: ctx.rng
  };
}
function targetFormulaCtx(target2) {
  const cc = target2?.characterContext;
  if (!cc) return null;
  return {
    abilityMods: cc.abilityMods,
    profBonus: cc.profBonus,
    selfLevel: cc.level,
    classLevels: cc.classLevels,
    spellcastingMod: cc.spellcastingMod,
    variables: cc.variables
  };
}
var EXECUTABLE_RESOLUTIONS = /* @__PURE__ */ new Set(["auto", "attack_roll", "save", "ability_check"]);
var EXECUTABLE_PAYLOAD_KINDS = /* @__PURE__ */ new Set([
  "damage",
  "healing",
  "reduce_damage",
  "temp_hp",
  "condition",
  "resource",
  "modifier",
  "attack_follow_up",
  "grant_sense",
  "resistance",
  "set_value",
  "grant_effect",
  "choice",
  "add_item",
  "movement",
  "boon",
  "reroll",
  "transform",
  "narrative"
]);
var ABILITY_KEYS2 = /* @__PURE__ */ new Set(["str", "dex", "con", "int", "wis", "cha"]);
var ATTACK_ABILITIES = /* @__PURE__ */ new Set([...ABILITY_KEYS2, "auto", "spellcasting"]);
var MODIFIER_OPS = /* @__PURE__ */ new Set([
  "add",
  "set",
  "advantage",
  "disadvantage",
  "reroll",
  "multiply",
  "upgrade",
  "downgrade",
  "auto_fail",
  "auto_crit",
  "deny",
  "set_die",
  "crit_range",
  "outcome",
  "on_roll",
  "die_bonus",
  "bonus_die",
  "explode"
]);
var NUMERIC_MODIFIER_OPS = /* @__PURE__ */ new Set(["add", "set", "multiply", "upgrade", "downgrade", "crit_range", "die_bonus"]);
var MOVEMENT_MODES = /* @__PURE__ */ new Set(["push", "pull", "teleport", "extra_speed", "double", "knock_prone", "move"]);
var RESISTANCE_LEVELS = /* @__PURE__ */ new Set(["resistance", "immunity", "vulnerability"]);
var MAX_PREFLIGHT_DEPTH = 12;
var PREFLIGHT_RNG = () => 0.5;
function isDict(value) {
  return value !== null && typeof value === "object" && !Array.isArray(value);
}
function mechanicsError(code, path, message, cause) {
  return new MechanicsExecutionError(code, path, message, cause === void 0 ? void 0 : { cause });
}
function preflightFormulaCtx(ctx, targetOwned = false) {
  const base2 = targetOwned ? targetFormulaCtx(ctx.target) ?? formulaCtx(ctx) : formulaCtx(ctx);
  return { ...base2, rng: PREFLIGHT_RNG };
}
function assertFiniteFormula(value, path, ctx, targetOwned = false) {
  if (typeof value !== "string" && typeof value !== "number") {
    throw mechanicsError("INVALID_FORMULA", path, "expected a formula string or number");
  }
  try {
    const result = evaluate(value, preflightFormulaCtx(ctx, targetOwned));
    if (typeof result !== "number" || !Number.isFinite(result)) {
      throw new FormulaError("formula must resolve to a finite number");
    }
    return result;
  } catch (error) {
    throw mechanicsError(
      "INVALID_FORMULA",
      path,
      error instanceof Error ? error.message : String(error),
      error
    );
  }
}
function explicitAbility(value, path, supported, label) {
  if (typeof value !== "string" || !supported.has(value)) {
    throw mechanicsError(
      "INVALID_PAYLOAD",
      path,
      `${label} requires an explicit supported ability`
    );
  }
  return value;
}
function explicitPositiveDc(value, path, ctx) {
  if (value === void 0) {
    throw mechanicsError("INVALID_PAYLOAD", path, "resolution requires an explicit DC formula");
  }
  const dc = assertFiniteFormula(value, path, ctx);
  if (dc <= 0) {
    throw mechanicsError("INVALID_FORMULA", path, "DC formula must resolve to a positive number");
  }
  return dc;
}
function explicitTargetArmorClass(ctx) {
  const ac = ctx.target?.ac;
  if (typeof ac !== "number" || !Number.isFinite(ac) || ac <= 0) {
    throw mechanicsError(
      "INVALID_MECHANICS",
      "context.target.ac",
      "attack resolution requires an explicit positive finite target AC"
    );
  }
  return ac;
}
function explicitCharacterAbilityModifier(ctx, ability) {
  const modifier = ctx.character?.abilityMods?.[ability];
  if (typeof modifier !== "number" || !Number.isFinite(modifier)) {
    throw mechanicsError(
      "INVALID_MECHANICS",
      `context.character.abilityMods.${ability}`,
      "resolution requires an explicit finite actor ability modifier"
    );
  }
  return modifier;
}
function explicitCharacterProficiencyBonus(ctx) {
  const bonus = ctx.character?.profBonus;
  if (typeof bonus !== "number" || !Number.isFinite(bonus)) {
    throw mechanicsError(
      "INVALID_MECHANICS",
      "context.character.profBonus",
      "D20 resolution requires an explicit finite actor proficiency bonus"
    );
  }
  return bonus;
}
function explicitTargetSaveModifier(ctx, ability) {
  const targetCharacter = ctx.target?.characterContext;
  if (targetCharacter) {
    const base2 = targetCharacter.abilityMods[ability];
    if (typeof base2 !== "number" || !Number.isFinite(base2) || typeof targetCharacter.profBonus !== "number" || !Number.isFinite(targetCharacter.profBonus)) {
      throw mechanicsError(
        "INVALID_MECHANICS",
        `context.target.characterContext.abilityMods.${ability}`,
        "save resolution requires explicit finite target ability and proficiency facts"
      );
    }
    return base2 + (targetCharacter.saveProficiencies?.includes(ability) ? targetCharacter.profBonus : 0);
  }
  const modifier = ctx.target?.saveMods?.[ability];
  if (typeof modifier !== "number" || !Number.isFinite(modifier)) {
    throw mechanicsError(
      "INVALID_MECHANICS",
      `context.target.saveMods.${ability}`,
      "save resolution requires an explicit finite target save modifier"
    );
  }
  return modifier;
}
function explicitContestSkills(effect, path, ctx) {
  if (!Array.isArray(effect.contest_vs) || effect.contest_vs.length === 0 || effect.contest_vs.some((skill) => typeof skill !== "string" || !skill.trim()) || new Set(effect.contest_vs).size !== effect.contest_vs.length) {
    throw mechanicsError(
      "INVALID_PAYLOAD",
      `${path}.contest_vs`,
      "contest requires explicit distinct non-empty defending skills"
    );
  }
  const skills = effect.contest_vs;
  for (const skill of skills) {
    const modifier = ctx.target?.checkMods?.[skill];
    if (typeof modifier !== "number" || !Number.isFinite(modifier)) {
      throw mechanicsError(
        "INVALID_MECHANICS",
        `context.target.checkMods.${skill}`,
        "contest requires an explicit finite modifier for every defending skill"
      );
    }
  }
  return skills;
}
function resolveCostAmount(value, path, ctx) {
  if (value === void 0) return 1;
  if (typeof value !== "string" && typeof value !== "number") {
    throw mechanicsError("INVALID_FORMULA", path, "cost amount must be a deterministic formula");
  }
  try {
    const result = evaluate(value, {
      ...preflightFormulaCtx(ctx),
      rng: () => {
        throw new FormulaError("activation cost cannot contain a random die");
      }
    });
    if (typeof result !== "number" || !Number.isFinite(result) || !Number.isInteger(result) || result <= 0) {
      throw new FormulaError("cost amount must resolve to a positive integer");
    }
    return result;
  } catch (error) {
    throw mechanicsError(
      "INVALID_FORMULA",
      path,
      error instanceof Error ? error.message : String(error),
      error
    );
  }
}
function preflightActivationCost(mechanics, ctx) {
  if (mechanics.activation === void 0) return [];
  if (!isDict(mechanics.activation)) {
    throw mechanicsError("INVALID_MECHANICS", "mechanics.activation", "activation must be an object");
  }
  const activation = mechanics.activation;
  if (activation.cost === void 0) return [];
  if (!Array.isArray(activation.cost)) {
    throw mechanicsError("INVALID_MECHANICS", "mechanics.activation.cost", "activation cost must be an array");
  }
  return activation.cost.map((entry, index) => {
    const path = `mechanics.activation.cost[${index}]`;
    if (!isDict(entry)) {
      throw mechanicsError("INVALID_PAYLOAD", path, "cost entry must be an object");
    }
    if (typeof entry.resource !== "string" || !entry.resource.trim()) {
      throw mechanicsError("INVALID_PAYLOAD", `${path}.resource`, "cost resource must be non-empty");
    }
    if (entry.resource === "item" && (typeof entry.card_id !== "string" || !entry.card_id.trim())) {
      throw mechanicsError("INVALID_PAYLOAD", `${path}.card_id`, "item cost requires a non-empty card_id");
    }
    if (entry.level !== void 0 && (!Number.isInteger(entry.level) || Number(entry.level) < 0 || Number(entry.level) > 9)) {
      throw mechanicsError("INVALID_PAYLOAD", `${path}.level`, "cost level must be an integer from 0 to 9");
    }
    return {
      ...entry,
      resource: entry.resource,
      amount: resolveCostAmount(entry.amount, `${path}.amount`, ctx)
    };
  });
}
function assertPayloadArray(value, path, state, ctx, targetOwned, depth, hand = "main") {
  if (!Array.isArray(value)) {
    throw mechanicsError("INVALID_PAYLOAD", path, "expected an array of payloads");
  }
  value.forEach((payload, index) => preflightPayload(
    payload,
    `${path}[${index}]`,
    state,
    ctx,
    targetOwned,
    depth + 1,
    hand
  ));
}
function grantEffectSlugs(payload, path) {
  const slugs = [];
  if (payload.value !== void 0) {
    if (typeof payload.value !== "string" || !payload.value.trim()) {
      throw mechanicsError("INVALID_PAYLOAD", `${path}.value`, "expected a non-empty effect reference");
    }
    slugs.push(payload.value);
  }
  if (payload.values !== void 0) {
    if (!Array.isArray(payload.values) || payload.values.some((value) => typeof value !== "string" || !value.trim())) {
      throw mechanicsError("INVALID_PAYLOAD", `${path}.values`, "expected non-empty effect references");
    }
    slugs.push(...payload.values);
  }
  if (!slugs.length) {
    throw mechanicsError("INVALID_PAYLOAD", path, "grant_effect has no effect reference");
  }
  return [...new Set(slugs)];
}
function preflightModifier(payload, path, state, ctx, targetOwned, depth, hand) {
  const op = String(payload.op ?? "add");
  if (!MODIFIER_OPS.has(op)) {
    throw mechanicsError("INVALID_PAYLOAD", `${path}.op`, `unsupported modifier operation \xAB${op}\xBB`);
  }
  if (!isDict(payload.applies_to)) {
    throw mechanicsError("INVALID_PAYLOAD", `${path}.applies_to`, "modifier must declare its engine query");
  }
  if (NUMERIC_MODIFIER_OPS.has(op)) {
    const formula = typeof payload.value === "string" ? payload.value.replace(/^\+/, "") : payload.value;
    assertFiniteFormula(formula, `${path}.value`, ctx, targetOwned);
  }
  if (op === "set_die" || op === "bonus_die") {
    const faces = Number(payload.faces ?? payload.die ?? payload.value);
    if (!Number.isFinite(faces) || faces < 2) {
      throw mechanicsError("INVALID_PAYLOAD", path, `${op} requires at least two die faces`);
    }
  }
  if (op === "explode") {
    const limit = payload.limit ?? payload.value;
    if (limit === void 0) {
      throw mechanicsError("INVALID_PAYLOAD", path, "explode requires a limit formula");
    }
    assertFiniteFormula(limit, `${path}.${payload.limit === void 0 ? "value" : "limit"}`, ctx, targetOwned);
  }
  if (op === "outcome" && typeof (payload.value ?? payload.outcome) !== "string") {
    throw mechanicsError("INVALID_PAYLOAD", path, "outcome requires a string result");
  }
  if (op === "on_roll") {
    const nested = payload.then ?? payload.result;
    assertPayloadArray(
      nested,
      `${path}.${payload.then === void 0 ? "result" : "then"}`,
      state,
      ctx,
      targetOwned,
      depth,
      hand
    );
  }
}
function preflightChoice(payload, path, state, ctx, targetOwned, depth, hand) {
  const id = String(payload.id ?? "choice");
  const raw = ctx.choices?.[id];
  if (raw == null) {
    throw mechanicsError("MISSING_CHOICE", path, `runtime choice \xAB${id}\xBB was not resolved`);
  }
  const selected = (Array.isArray(raw) ? raw : [raw]).filter((value) => typeof value === "string" && value.length > 0);
  const expected = Math.max(1, Math.floor(Number(payload.count ?? 1)) || 1);
  if (selected.length !== expected || new Set(selected).size !== selected.length) {
    throw mechanicsError(
      "INVALID_CHOICE",
      path,
      `runtime choice \xAB${id}\xBB requires ${expected} distinct selection(s)`
    );
  }
  const expanded = selectedChoicePayloads(payload, selected).map(normalizeChoicePayload);
  if (!expanded.length) {
    throw mechanicsError("INVALID_CHOICE", path, `runtime choice \xAB${id}\xBB has no executable selected branch`);
  }
  expanded.forEach((candidate, index) => preflightPayload(
    candidate,
    `${path}.selected[${index}]`,
    state,
    ctx,
    targetOwned,
    depth + 1,
    hand
  ));
}
function preflightPayload(value, path, state, ctx, targetOwned, depth, hand = "main") {
  if (depth > MAX_PREFLIGHT_DEPTH) {
    throw mechanicsError("INVALID_PAYLOAD", path, "payload nesting exceeds the engine limit");
  }
  if (!isDict(value)) {
    throw mechanicsError("INVALID_PAYLOAD", path, "payload must be an object");
  }
  const kind = typeof value.kind === "string" ? value.kind : "";
  if (!EXECUTABLE_PAYLOAD_KINDS.has(kind)) {
    throw mechanicsError("UNKNOWN_PAYLOAD", `${path}.kind`, `executor does not own payload kind \xAB${kind || "?"}\xBB`);
  }
  switch (kind) {
    case "damage": {
      const base2 = value.amount ?? value.dice;
      if (base2 == null) throw mechanicsError("INVALID_PAYLOAD", path, "damage requires amount or dice");
      if (typeof value.type !== "string" || !value.type.trim()) {
        throw mechanicsError(
          "INVALID_PAYLOAD",
          `${path}.type`,
          "damage requires an explicit non-empty damage type"
        );
      }
      if (base2 === "weapon" && value.type !== "weapon") {
        throw mechanicsError(
          "INVALID_PAYLOAD",
          `${path}.type`,
          "weapon damage dice require the explicit weapon damage type"
        );
      }
      if ((base2 === "weapon" || value.type === "weapon") && !weaponContext(ctx.character, hand, state.equipment)) {
        throw mechanicsError(
          "INVALID_MECHANICS",
          "context.character.equipment",
          `weapon damage requires an explicit equipped ${hand}-hand weapon`
        );
      }
      if (base2 !== "weapon") {
        assertFiniteFormula(withScaling(String(base2), value, ctx), `${path}.${value.amount == null ? "dice" : "amount"}`, ctx);
      }
      const explode = value.explode;
      if (isDict(explode) && explode.limit !== void 0) {
        assertFiniteFormula(explode.limit, `${path}.explode.limit`, ctx);
      } else if (explode !== void 0 && typeof explode !== "boolean") {
        throw mechanicsError("INVALID_PAYLOAD", `${path}.explode`, "expected a boolean or an object");
      }
      break;
    }
    case "healing":
    case "reduce_damage":
    case "temp_hp": {
      if (value.amount == null) throw mechanicsError("INVALID_PAYLOAD", path, `${kind} requires amount`);
      assertFiniteFormula(withScaling(String(value.amount), value, ctx), `${path}.amount`, ctx);
      break;
    }
    case "condition": {
      if (typeof value.value !== "string" || !value.value.trim()) {
        throw mechanicsError("INVALID_PAYLOAD", `${path}.value`, "condition id must be non-empty");
      }
      const op = String(value.op ?? "apply");
      if (op !== "apply" && op !== "remove") {
        throw mechanicsError("INVALID_PAYLOAD", `${path}.op`, `unsupported condition operation \xAB${op}\xBB`);
      }
      break;
    }
    case "resource": {
      const id = value.id ?? value.resource;
      if (typeof id !== "string" || !id.trim()) {
        throw mechanicsError("INVALID_PAYLOAD", path, "resource id must be non-empty");
      }
      const op = String(value.op ?? "grant");
      if (op !== "grant" && op !== "restore") {
        throw mechanicsError("INVALID_PAYLOAD", `${path}.op`, `executor does not support resource operation \xAB${op}\xBB`);
      }
      if (value.amount !== void 0) assertFiniteFormula(value.amount, `${path}.amount`, ctx, targetOwned);
      break;
    }
    case "modifier":
      preflightModifier(value, path, state, ctx, targetOwned, depth, hand);
      break;
    case "attack_follow_up": {
      if (typeof value.follow_up !== "string" || !value.follow_up.trim()) {
        throw mechanicsError("INVALID_PAYLOAD", path, "attack_follow_up requires a typed follow_up");
      }
      break;
    }
    case "grant_sense": {
      if (typeof value.sense !== "string" || !value.sense.trim()) {
        throw mechanicsError("INVALID_PAYLOAD", `${path}.sense`, "sense must be non-empty");
      }
      const range = Number(value.range);
      if (!Number.isFinite(range) || range <= 0) {
        throw mechanicsError("INVALID_PAYLOAD", `${path}.range`, "sense range must be positive");
      }
      break;
    }
    case "resistance": {
      if (typeof value.damage_type !== "string" || !value.damage_type.trim()) {
        throw mechanicsError("INVALID_PAYLOAD", `${path}.damage_type`, "damage type must be non-empty");
      }
      if (!RESISTANCE_LEVELS.has(String(value.value ?? ""))) {
        throw mechanicsError("INVALID_PAYLOAD", `${path}.value`, "invalid damage adjustment level");
      }
      break;
    }
    case "set_value": {
      const target2 = typeof value.target === "string" ? value.target : "";
      const knownTarget = ["hp", "current_hp", "temp_hp", "max_hp", "hp_max", "ac_base"].includes(target2) || target2 in state.resources || target2 in state.maxResources;
      if (!knownTarget) {
        throw mechanicsError("INVALID_PAYLOAD", `${path}.target`, `unknown runtime target \xAB${target2 || "?"}\xBB`);
      }
      assertFiniteFormula(value.formula ?? value.value, `${path}.${value.formula === void 0 ? "value" : "formula"}`, ctx, targetOwned);
      break;
    }
    case "grant_effect": {
      for (const slug of grantEffectSlugs(value, path)) {
        const granted = ctx.grantedEffects?.[slug];
        if (!granted || !isDict(granted.mechanics)) {
          throw mechanicsError(
            "UNRESOLVED_GRANT_EFFECT",
            path,
            `effect reference \xAB${slug}\xBB is not present in ExecuteContext.grantedEffects`
          );
        }
      }
      break;
    }
    case "choice":
      preflightChoice(value, path, state, ctx, targetOwned, depth, hand);
      break;
    case "add_item": {
      const cardId = value.card_id ?? value.value;
      if (typeof cardId !== "string" || !cardId.trim()) {
        throw mechanicsError("INVALID_PAYLOAD", path, "add_item requires card_id or value");
      }
      const qty = Number(value.qty ?? value.amount ?? 1);
      if (!Number.isFinite(qty) || qty <= 0) {
        throw mechanicsError("INVALID_PAYLOAD", path, "add_item quantity must be positive");
      }
      break;
    }
    case "movement": {
      const mode = String(value.value ?? "move");
      if (!MOVEMENT_MODES.has(mode)) {
        throw mechanicsError("INVALID_PAYLOAD", `${path}.value`, `unsupported movement mode \xAB${mode}\xBB`);
      }
      if (value.distance !== void 0) assertFiniteFormula(value.distance, `${path}.distance`, ctx, targetOwned);
      break;
    }
    // These primitives either persist a typed effect or emit an explicit
    // narrative/event and intentionally do not require numeric fields.
    case "boon":
    case "reroll":
    case "transform":
    case "narrative":
      break;
    default:
      break;
  }
}
function preflightEffect(value, path, state, ctx) {
  if (!isDict(value)) throw mechanicsError("INVALID_PAYLOAD", path, "effect must be an object");
  const hand = resolveHand(value);
  if (value.kind !== void 0) {
    if (value.kind !== "choice") {
      throw mechanicsError(
        "UNKNOWN_PAYLOAD",
        `${path}.kind`,
        `payload kind \xAB${String(value.kind)}\xBB cannot be used at the interaction layer`
      );
    }
    preflightPayload(value, path, state, ctx, String(value.who ?? "self") === "target", 0, hand);
    return;
  }
  const resolution = typeof value.resolution === "string" ? value.resolution : "";
  if (!EXECUTABLE_RESOLUTIONS.has(resolution)) {
    throw mechanicsError("UNKNOWN_RESOLUTION", `${path}.resolution`, `executor does not own resolution \xAB${resolution || "?"}\xBB`);
  }
  const targetOwned = String(value.who ?? (resolution === "auto" ? "self" : "target")) === "target";
  const outcomes = resolution === "auto" ? ["result", "results"] : resolution === "attack_roll" ? ["on_hit", "on_crit", "on_miss"] : resolution === "save" ? ["on_fail", "on_success"] : ["on_success"];
  if (resolution === "auto" && value.result === void 0 && value.results === void 0) {
    throw mechanicsError("INVALID_PAYLOAD", path, "auto resolution requires result or results");
  }
  for (const key of outcomes) {
    if (value[key] !== void 0) {
      assertPayloadArray(value[key], `${path}.${key}`, state, ctx, targetOwned, 0, hand);
    }
  }
  if (resolution === "attack_roll") {
    const ability = explicitAbility(
      value.ability,
      `${path}.ability`,
      ATTACK_ABILITIES,
      "attack resolution"
    );
    explicitTargetArmorClass(ctx);
    explicitCharacterProficiencyBonus(ctx);
    if (ability === "spellcasting" && (typeof ctx.character.spellcastingMod !== "number" || !Number.isFinite(ctx.character.spellcastingMod))) {
      throw mechanicsError(
        "INVALID_MECHANICS",
        "context.character.spellcastingMod",
        "spell attack requires an explicit finite spellcasting modifier"
      );
    }
    if (ability === "auto") {
      const weapon = weaponContext(ctx.character, resolveHand(value), state.equipment);
      if (!weapon) {
        throw mechanicsError(
          "INVALID_MECHANICS",
          "context.character.equipment",
          "automatic attack ability requires an explicit equipped weapon"
        );
      }
      explicitCharacterAbilityModifier(ctx, weapon.ability);
    } else if (ability !== "spellcasting") {
      explicitCharacterAbilityModifier(ctx, ability);
    }
  } else if (resolution === "save") {
    const ability = explicitAbility(
      value.ability,
      `${path}.ability`,
      ABILITY_KEYS2,
      "save resolution"
    );
    explicitPositiveDc(value.dc, `${path}.dc`, ctx);
    const hasDeferredTargetAuthority = ctx.deferTargetSaves === true && targetOwned && ctx.target?.runtimeState !== void 0 && ctx.deferredSaveSource !== void 0;
    if (ctx.forceSaveOutcome == null && !hasDeferredTargetAuthority) {
      explicitTargetSaveModifier(ctx, ability);
    }
  } else if (resolution === "ability_check") {
    const ability = explicitAbility(
      value.ability,
      `${path}.ability`,
      ABILITY_KEYS2,
      "ability-check resolution"
    );
    explicitCharacterAbilityModifier(ctx, ability);
    explicitCharacterProficiencyBonus(ctx);
    const hasDc = value.dc !== void 0;
    const hasContest = value.contest_vs !== void 0;
    if (hasDc === hasContest) {
      throw mechanicsError(
        "INVALID_PAYLOAD",
        path,
        "ability check requires exactly one explicit dc or contest_vs declaration"
      );
    }
    if (value.mode !== void 0 && value.mode !== (hasDc ? "dc" : "contest")) {
      throw mechanicsError(
        "INVALID_PAYLOAD",
        `${path}.mode`,
        "ability-check mode contradicts its explicit dc or contest_vs declaration"
      );
    }
    if (hasDc) {
      explicitPositiveDc(value.dc, `${path}.dc`, ctx);
    } else {
      explicitContestSkills(value, path, ctx);
    }
  }
}
function preflightMechanicsExecution(state, mechanics, ctx) {
  if (!isDict(mechanics)) {
    throw mechanicsError("INVALID_MECHANICS", "mechanics", "mechanics must be an object");
  }
  if (Object.prototype.hasOwnProperty.call(mechanics, "primitive") && ctx.externalPrimitiveHandled !== true) {
    const primitive = isDict(mechanics.primitive) ? String(mechanics.primitive.type ?? "?") : "?";
    throw mechanicsError(
      "CANONICAL_PRIMITIVE_REQUIRED",
      "mechanics.primitive",
      `primitive \xAB${primitive}\xBB requires a canonical rules-core hand-off`
    );
  }
  if (mechanics.interactions !== void 0 && mechanics.effects === void 0) {
    throw mechanicsError(
      "INVALID_MECHANICS",
      "mechanics.interactions",
      "legacy executor accepts the effects container only"
    );
  }
  const cost = preflightActivationCost(mechanics, ctx);
  if (mechanics.effects === void 0) {
    if (mechanics.kind !== void 0) {
      throw mechanicsError("INVALID_MECHANICS", "mechanics.kind", "a payload cannot be executed as an action");
    }
    return cost;
  }
  if (!Array.isArray(mechanics.effects)) {
    throw mechanicsError("INVALID_MECHANICS", "mechanics.effects", "effects must be an array");
  }
  mechanics.effects.forEach((effect, index) => preflightEffect(
    effect,
    `mechanics.effects[${index}]`,
    state,
    ctx
  ));
  return cost;
}
function evalCtxOf(state, ctx) {
  return {
    character: ctx.character,
    state,
    target: ctx.target,
    activeConditions: activeConditionsOf(state),
    // C10: состояния ЦЕЛИ — чтобы предикат target_has_condition («преимущество, пока цель
    // распластана/опутана») гейтился данными, а не молча давал false. Пустое множество, если цели нет.
    targetConditions: activeConditionsOf(ctx.target?.runtimeState),
    rollerActorId: ctx.selfId,
    rollTargetActorId: ctx.target?.id,
    conditionSourceFacts: ctx.conditionSourceFacts,
    distancesFt: ctx.conditionRelationFacts?.distancesFt,
    visibility: ctx.conditionRelationFacts?.visibility
  };
}
function projectedAgainst(target2, roll, attackRange, evalCtx, queryFilter) {
  const out = {
    modifiers: [],
    rules: [],
    advantage: "none",
    hasAdvantage: false,
    hasDisadvantage: false,
    autoCrit: false
  };
  const st = target2?.runtimeState;
  if (!st) return out;
  const tctx = targetFormulaCtx(target2);
  const consider = (m, source2, conditionSourceId) => {
    if (String(m.scope ?? "self") !== "target") return;
    const applies = m.applies_to;
    if (!applies || applies.roll !== roll) return;
    const effectFilter = applies.filter;
    if (effectFilter && Object.entries(effectFilter).some(([key, value]) => queryFilter?.[key] !== value)) return;
    if (!matchesWhen(m.when, {
      ...evalCtx ?? {},
      conditionSourceId,
      conditionOwnerId: target2?.id
    })) return;
    const range = m.range;
    if (range === "melee" || range === "ranged") {
      if (attackRange !== range) return;
    }
    const op = String(m.op ?? "");
    if (op === "auto_crit") {
      out.autoCrit = true;
    } else if (op === "bonus_die") {
      out.rules.push(m);
    } else if (op === "advantage" || op === "disadvantage") {
      if (op === "advantage") out.hasAdvantage = true;
      else out.hasDisadvantage = true;
      out.advantage = foldAdvantage(out.hasAdvantage, out.hasDisadvantage);
    } else if (op === "add" && m.value != null) {
      const raw = String(m.value).replace(/^\+/, "");
      let v;
      try {
        const r = evaluate(raw, tctx ?? {});
        v = typeof r === "number" ? r : void 0;
      } catch {
        v = void 0;
      }
      if (v != null && !Number.isNaN(v) && v !== 0) out.modifiers.push({ value: v, source: source2 });
    }
  };
  for (const e of st.activeEffects) {
    const mech = e.mechanics;
    if (mech?.kind === "condition" && mech.value) {
      for (const rule of conditionModifierPayloads(String(mech.value))) {
        consider(rule, String(mech.value), e.sourceId);
      }
    } else {
      for (const p of payloadsOf(mech)) if (p.kind === "modifier") consider(p, e.name);
    }
  }
  return out;
}
function resistanceRuleFor(state, ctx, damageType) {
  const rank = (l) => l === "immunity" ? 3 : l === "resistance" ? 2 : l === "vulnerability" ? 1 : 0;
  const scan = (mech) => {
    const payloads = mech?.kind === "condition" && mech.value ? conditionRuntimePayloads(String(mech.value)) : payloadsOf(mech);
    for (const p of payloads) {
      const declaredType = String(p.damage_type ?? "");
      if (p.kind === "resistance" && (declaredType === damageType || declaredType === "all")) {
        return String(p.value ?? "");
      }
    }
    return null;
  };
  const stableSources = (owner, fallback) => {
    const declared = Array.isArray(owner.sourceEntityIds) ? owner.sourceEntityIds.filter((id) => typeof id === "string" && Boolean(id)) : [];
    return [...new Set(declared.length ? declared : fallback)].sort();
  };
  let result = { level: null, sourceEntityIds: [] };
  for (const effect of state.activeEffects) {
    const level = scan(effect.mechanics);
    if (rank(level) > rank(result.level)) {
      result = { level, sourceEntityIds: stableSources(effect.mechanics, [effect.id]) };
    }
  }
  for (const passive of passivesFromCtx(ctx)) {
    const level = scan(passive);
    if (rank(level) > rank(result.level)) {
      result = { level, sourceEntityIds: stableSources(passive, []) };
    }
  }
  return result;
}
function applyResistance(amount, level) {
  if (level === "immunity") return 0;
  if (level === "resistance") return Math.floor(amount / 2);
  if (level === "vulnerability") return amount * 2;
  return amount;
}
function targetSaveMod(target2, ability) {
  const cc = target2?.characterContext;
  if (cc) {
    const base2 = cc.abilityMods[ability];
    const prof = cc.saveProficiencies?.includes(ability) ? cc.profBonus : 0;
    return base2 + prof;
  }
  return target2.saveMods[ability];
}
function evalDc(formula, ctx) {
  const normalized3 = formula.replace(/\s+/g, "");
  const v = evaluate(normalized3, formulaCtx(ctx));
  if (typeof v !== "number") throw new FormulaError(`DC \u0444\u043E\u0440\u043C\u0443\u043B\u0430 \xAB${formula}\xBB \u043D\u0435 \u0447\u0438\u0441\u043B\u043E`);
  const spellClass = ctx.spell?.sourceClass;
  const state = ctx.selfRuntime;
  if (!spellClass || !state) return v;
  const collected = collectModifiers(state, passivesFromCtx(ctx), {
    roll: "spell_save_dc",
    filter: { spellClass },
    formulaCtx: formulaCtx(ctx),
    evalCtx: evalCtxOf(state, ctx)
  });
  return foldModifiers(v, collected).value;
}
function expiryFromDuration(duration) {
  const t = duration?.type;
  if (t === "until_start_of_next_turn") return "start_of_next_turn";
  if (t === "until_end_of_turn") return "end_of_turn";
  if (t === "until_long_rest") return "long_rest";
  return void 0;
}
function resolveDuration(duration) {
  if (duration?.type === "rounds" || duration?.type === "minutes" || duration?.type === "hours") {
    const raw = Math.floor(Number(duration.amount));
    const multiplier = duration.type === "minutes" ? 10 : duration.type === "hours" ? 600 : 1;
    const rounds = raw * multiplier;
    return { roundsLeft: Number.isFinite(rounds) && rounds > 0 ? rounds : 1 };
  }
  return { expiry: expiryFromDuration(duration) ?? "manual" };
}
function sourceTurnMetadata(duration, ctx, ownerActorId) {
  const type = String(duration?.type ?? "");
  const boundary = type === "until_start_of_source_next_turn" ? "start" : type === "until_end_of_source_next_turn" ? "end" : null;
  if (!boundary || !ctx.selfId || !ownerActorId) return void 0;
  return {
    expiry: "source_turn",
    ownerId: ownerActorId,
    sourceId: ctx.selfId,
    sourceTurnExpiry: {
      sourceActorId: ctx.selfId,
      ownerActorId,
      boundary
    }
  };
}
function withScaling(base2, payload, ctx) {
  const scaling = payload.scaling;
  const dice = scaling?.dice;
  if (!scaling || typeof dice !== "string" || !dice) return base2;
  const per = String(scaling.per ?? "");
  let steps = 0;
  if (per === "spell_slot_above") {
    const baseLevel = ctx.spell?.baseLevel ?? 0;
    const castLevel = ctx.spell?.castLevel ?? baseLevel;
    steps = Math.max(0, castLevel - baseLevel);
  } else if (per === "character_level" || per === "cantrip") {
    const lvl = ctx.character.level;
    steps = (lvl >= 5 ? 1 : 0) + (lvl >= 11 ? 1 : 0) + (lvl >= 17 ? 1 : 0);
  }
  if (steps <= 0) return base2;
  return `${base2}${` + ${dice}`.repeat(steps)}`;
}
function resolveHand(effect) {
  const tags = effect.tags;
  return tags?.includes("off_hand") ? "off" : "main";
}
function attackAbilityMods(effect, ctx, hand, state) {
  const mods = [];
  const ability = String(effect.ability);
  const attackKind = attackRollQueryFacts(effect, hand, ctx.character, state.equipment).attackKind;
  const currentWeapon = attackKind === "weapon" ? weaponContext(ctx.character, hand, state.equipment) : null;
  const proficiencyBonus = attackKind !== "weapon" || currentWeapon !== null && isWeaponProficient(
    ctx.character,
    currentWeapon.weaponType,
    currentWeapon.proficiencyCategory
  ) ? ctx.character.profBonus : 0;
  if (ability === "spellcasting") {
    mods.push({ value: ctx.character.spellcastingMod, source: "\u0437\u0430\u043A\u043B\u0438\u043D.", reason: "\u043C\u043E\u0434\u0438\u0444\u0438\u043A\u0430\u0442\u043E\u0440 \u0437\u0430\u043A\u043B\u0438\u043D\u0430\u043D\u0438\u0439" });
    if (proficiencyBonus) {
      mods.push({ value: proficiencyBonus, source: "\u0411\u041C", reason: "\u0431\u043E\u043D\u0443\u0441 \u043C\u0430\u0441\u0442\u0435\u0440\u0441\u0442\u0432\u0430" });
    }
  } else if (ability === "auto") {
    const w = currentWeapon ?? weaponContext(ctx.character, hand, state.equipment);
    if (w) {
      mods.push({
        value: ctx.character.abilityMods[w.ability],
        source: ABILITY_LABEL[w.ability],
        reason: "\u043C\u043E\u0434\u0438\u0444\u0438\u043A\u0430\u0442\u043E\u0440 \u0445\u0430\u0440\u0430\u043A\u0442\u0435\u0440\u0438\u0441\u0442\u0438\u043A\u0438"
      });
      if (w.attackEnchant) {
        mods.push({ value: w.attackEnchant, source: `+${w.attackEnchant}`, reason: "\u0437\u0430\u0447\u0430\u0440\u043E\u0432\u0430\u043D\u0438\u0435 \u043E\u0440\u0443\u0436\u0438\u044F" });
      }
    }
    if (proficiencyBonus) {
      mods.push({ value: proficiencyBonus, source: "\u0411\u041C", reason: "\u0431\u043E\u043D\u0443\u0441 \u043C\u0430\u0441\u0442\u0435\u0440\u0441\u0442\u0432\u0430" });
    }
  } else {
    const key = ability;
    mods.push({
      value: ctx.character.abilityMods[key],
      source: ABILITY_LABEL[key] ?? ability,
      reason: "\u043C\u043E\u0434\u0438\u0444\u0438\u043A\u0430\u0442\u043E\u0440 \u0445\u0430\u0440\u0430\u043A\u0442\u0435\u0440\u0438\u0441\u0442\u0438\u043A\u0438"
    });
    if (proficiencyBonus) {
      mods.push({ value: proficiencyBonus, source: "\u0411\u041C", reason: "\u0431\u043E\u043D\u0443\u0441 \u043C\u0430\u0441\u0442\u0435\u0440\u0441\u0442\u0432\u0430" });
    }
  }
  return mods;
}
function stackKeyOf(mech) {
  if (!mech) return void 0;
  if (mech.stack_id != null) return String(mech.stack_id);
  if (mech.kind === "condition" && mech.value != null) return `cond:${String(mech.value)}`;
  return void 0;
}
function stackApply(state, entry, payload) {
  const stackId = stackKeyOf(payload);
  const add = () => ({ ...state, activeEffects: [...state.activeEffects, entry] });
  if (!stackId) return add();
  const stackType = String(payload.stack_type ?? "overwrite");
  const same4 = state.activeEffects.filter((e) => stackKeyOf(e.mechanics) === stackId);
  const others = state.activeEffects.filter((e) => stackKeyOf(e.mechanics) !== stackId);
  if (stackType === "stack") return add();
  if (stackType === "ignore") return same4.length ? state : add();
  if (stackType === "additive") {
    if (!same4.length) return add();
    const merged = same4.map((e) => ({
      ...e,
      roundsLeft: (e.roundsLeft ?? 0) + (entry.roundsLeft ?? 0) || void 0
    }));
    return { ...state, activeEffects: [...others, ...merged] };
  }
  const priority = Number(payload.stack_priority ?? 0);
  const maxPrio = same4.reduce((m, e) => Math.max(m, Number(e.mechanics.stack_priority ?? 0)), -Infinity);
  if (same4.length && priority < maxPrio) return state;
  return { ...state, activeEffects: [...others, entry] };
}
function runtimeEffectId(ctx, prefix, ordinal) {
  return ctx.nextId?.() ?? `${prefix}-${ordinal}-${Date.now()}`;
}
function applyModifierPayload(state, payload, source2, events, ctx, ownerActorId) {
  const duration = payload.duration;
  const relative = sourceTurnMetadata(duration, ctx, ownerActorId);
  const resolved = relative ? { expiry: relative.expiry } : resolveDuration(duration);
  const { roundsLeft, expiry } = resolved;
  const entry = {
    id: runtimeEffectId(ctx, "fx", state.activeEffects.length),
    name: source2,
    mechanics: payload,
    roundsLeft,
    // C6: раньше не выставлялся — модификатор с duration.rounds не истекал
    expiry,
    source: source2,
    ...relative ?? {}
  };
  events.push({ type: "effect_applied", name: source2 });
  return stackApply(state, entry, payload);
}
function applySensePayload(state, payload, source2, events, ctx, ownerActorId) {
  const sense = String(payload.sense ?? "");
  const range = Number(payload.range);
  if (!sense || !Number.isFinite(range) || range <= 0) {
    throw mechanicsError("INVALID_PAYLOAD", "runtime.payload", `grant_sense \xAB${sense || "?"}\xBB has invalid range`);
  }
  const { roundsLeft, expiry } = resolveDuration(payload.duration);
  const entry = {
    id: runtimeEffectId(ctx, "sense", state.activeEffects.length),
    name: source2,
    mechanics: payload,
    roundsLeft,
    expiry,
    source: source2,
    ...ownerActorId ? { ownerId: ownerActorId } : {},
    ...ctx.selfId ? { sourceId: ctx.selfId } : {}
  };
  events.push({ type: "effect_applied", name: source2 });
  return stackApply(state, entry, payload);
}
function applyAcBaseMethod(state, payload, source2, events, ctx) {
  const { roundsLeft, expiry } = resolveDuration(payload.duration);
  const entry = {
    id: runtimeEffectId(ctx, "ac", state.activeEffects.length),
    name: source2,
    mechanics: payload,
    roundsLeft,
    expiry,
    source: source2
  };
  events.push({ type: "effect_applied", name: source2 });
  return stackApply(state, entry, payload);
}
function applyGrantEffect(state, slugs, granted, events, ctx, ownerActorId) {
  let next = state;
  for (const slug of slugs) {
    const rec = granted?.[slug];
    const rawMech = rec?.mechanics ?? void 0;
    if (!isDict(rawMech)) {
      throw mechanicsError(
        "UNRESOLVED_GRANT_EFFECT",
        "runtime.payload",
        `effect reference \xAB${slug}\xBB was not resolved`
      );
    }
    const mech = {
      ...rawMech,
      stack_id: rawMech.stack_id ?? slug,
      ...rec?.repeatable ? { stack_type: "stack" } : {}
    };
    const name = String(rec?.name ?? rawMech.name ?? slug);
    const { roundsLeft, expiry } = resolveDuration(mech.duration);
    const entry = {
      id: runtimeEffectId(ctx, `grant-${slug}`, next.activeEffects.length),
      name,
      mechanics: mech,
      roundsLeft,
      expiry,
      source: name,
      ...ownerActorId ? { ownerId: ownerActorId } : {},
      ...ctx.selfId ? { sourceId: ctx.selfId } : {}
    };
    events.push({ type: "effect_applied", name });
    next = stackApply(next, entry, mech);
  }
  return next;
}
function applyResistancePayload(state, payload, source2, events, ctx) {
  const { roundsLeft, expiry } = resolveDuration(payload.duration);
  const entry = {
    id: runtimeEffectId(ctx, "res", state.activeEffects.length),
    name: source2,
    mechanics: payload,
    roundsLeft,
    expiry,
    source: source2
  };
  events.push({ type: "effect_applied", name: source2 });
  return stackApply(state, entry, payload);
}
function applySetValue(state, payload, fctx, events) {
  const target2 = String(payload.target ?? "");
  const raw = String(payload.formula ?? payload.value ?? "").replace(/\s+/g, "");
  if (!raw) throw mechanicsError("INVALID_FORMULA", "runtime.payload", "set_value formula is empty");
  const evaluated = evaluate(raw, fctx);
  if (typeof evaluated !== "number" || !Number.isFinite(evaluated)) {
    throw new FormulaError(`set_value \xAB${target2}\xBB: \u0444\u043E\u0440\u043C\u0443\u043B\u0430 \xAB${raw}\xBB \u043D\u0435 \u044F\u0432\u043B\u044F\u0435\u0442\u0441\u044F \u043A\u043E\u043D\u0435\u0447\u043D\u044B\u043C \u0447\u0438\u0441\u043B\u043E\u043C`);
  }
  const val = Math.floor(evaluated);
  const next = cloneState(state);
  switch (target2) {
    case "hp":
    case "current_hp":
      next.hp.current = Math.max(0, Math.min(next.hp.max, val));
      events.push(narrativeEvent(`\u0425\u0438\u0442\u044B \u0443\u0441\u0442\u0430\u043D\u043E\u0432\u043B\u0435\u043D\u044B: ${next.hp.current}`));
      break;
    case "temp_hp":
      next.hp.temp = Math.max(0, val);
      events.push(tempHpEvent(next.hp.temp));
      break;
    case "max_hp":
    case "hp_max":
      next.hp.max = Math.max(1, val);
      if (next.hp.current > next.hp.max) next.hp.current = next.hp.max;
      events.push(narrativeEvent(`\u041C\u0430\u043A\u0441. \u0445\u0438\u0442\u044B \u0443\u0441\u0442\u0430\u043D\u043E\u0432\u043B\u0435\u043D\u044B: ${next.hp.max}`));
      break;
    case "ac_base":
      events.push(narrativeEvent("set_value ac_base \u2014 \u0432\u044B\u0447\u0438\u0441\u043B\u044F\u0435\u0442\u0441\u044F \u043A\u0430\u043A \u043C\u0435\u0442\u043E\u0434 \u041A\u0417 (armorClassValue), \u043D\u0435 \u0440\u0430\u043D\u0442\u0430\u0439\u043C-\u043C\u0443\u0442\u0430\u0446\u0438\u044F."));
      break;
    default: {
      if (target2 && (target2 in next.maxResources || target2 in next.resources)) {
        const before = next.resources[target2] ?? 0;
        next.resources[target2] = Math.max(0, val);
        const delta = next.resources[target2] - before;
        events.push(delta > 0 ? resourceRestoredEvent(target2, delta, next.resources[target2]) : narrativeEvent(`\u0420\u0435\u0441\u0443\u0440\u0441 \xAB${target2}\xBB \u0443\u0441\u0442\u0430\u043D\u043E\u0432\u043B\u0435\u043D: ${next.resources[target2]}`));
      } else {
        throw mechanicsError("INVALID_PAYLOAD", "runtime.payload", `set_value has unknown target \xAB${target2}\xBB`);
      }
    }
  }
  return next;
}
function applyHealing(state, payload, ctx, events) {
  if (collectModifiers(state, [], { roll: "healing" }).denied) {
    events.push(narrativeEvent("\u041B\u0435\u0447\u0435\u043D\u0438\u0435 \u0437\u0430\u0431\u043B\u043E\u043A\u0438\u0440\u043E\u0432\u0430\u043D\u043E \u0434\u0435\u0439\u0441\u0442\u0432\u0443\u044E\u0449\u0438\u043C \u044D\u0444\u0444\u0435\u043A\u0442\u043E\u043C."));
    return state;
  }
  const formula = withScaling(String(payload.amount ?? "0"), payload, ctx);
  const fr = rollFormula(formula, formulaCtx(ctx), { rng: ctx.rng });
  const next = cloneState(state);
  next.hp.current = Math.min(next.hp.max, next.hp.current + fr.total);
  events.push(healingEvent(fr.total, {
    kind: "healing",
    advantage: "none",
    dice: fr.dice,
    modifiers: fr.modifiers,
    total: fr.total,
    text: fr.text
  }));
  return next;
}
function applyReduceDamage(state, payload, ctx, events) {
  const formula = withScaling(String(payload.amount ?? "0"), payload, ctx);
  const fr = rollFormula(formula, formulaCtx(ctx), { rng: ctx.rng });
  events.push({
    type: "damage_reduction",
    amount: fr.total,
    roll: { kind: "damage", advantage: "none", dice: fr.dice, modifiers: fr.modifiers, total: fr.total, text: fr.text }
  });
  return state;
}
function applyTempHp(state, payload, ctx, events) {
  const formula = withScaling(String(payload.amount ?? "0"), payload, ctx);
  const fr = rollFormula(formula, formulaCtx(ctx), { rng: ctx.rng });
  const next = cloneState(state);
  next.hp.temp = Math.max(next.hp.temp, fr.total);
  events.push(tempHpEvent(fr.total));
  return next;
}
function applyCondition(state, payload, source2, events, ctx, sourceId, conditionImmunities = ctx.conditionImmunities) {
  const condition = String(payload.value ?? "");
  if (!condition) return state;
  const op = String(payload.op ?? "apply");
  if (op === "remove") {
    const kept = state.activeEffects.filter((e) => {
      const m = e.mechanics;
      const match = m?.kind === "condition" && String(m.value ?? "") === condition;
      if (match) events.push({ type: "effect_expired", name: e.name });
      return !match;
    });
    const present = new Set(
      kept.filter((e) => e.mechanics?.kind === "condition").map((e) => String(e.mechanics.value ?? ""))
    );
    const additions = [];
    for (const leave of conditionLeaves(condition)) {
      if (present.has(leave)) continue;
      additions.push({
        id: runtimeEffectId(ctx, `cond-leave-${leave}`, state.activeEffects.length + additions.length),
        name: conditionLabel(leave),
        mechanics: { kind: "condition", value: leave, op: "apply" },
        expiry: "manual",
        source: `\u043E\u0441\u0442\u0430\u043B\u043E\u0441\u044C \u043E\u0442 \xAB${condition}\xBB`
      });
      events.push(conditionAppliedEvent(leave));
    }
    return { ...state, activeEffects: [...kept, ...additions] };
  }
  const normalizeTag = (value) => value.trim().toLowerCase().replaceAll("-", "_").replaceAll(" ", "_");
  const causeTags = new Set(
    (Array.isArray(payload.causeTags) ? payload.causeTags : Array.isArray(payload.cause_tags) ? payload.cause_tags : []).filter((tag) => typeof tag === "string").map(normalizeTag)
  );
  const conditionOwnedImmunities = state.activeEffects.flatMap((entry2) => {
    const mechanics = entry2.mechanics;
    if (mechanics.kind !== "condition" || !mechanics.value) return [];
    return conditionRuntimePayloads(String(mechanics.value)).flatMap((candidate) => candidate.kind === "condition_immunity" && candidate.condition ? [{
      condition: String(candidate.condition),
      ...Array.isArray(candidate.requiredCauseTags) ? { requiredCauseTags: candidate.requiredCauseTags.map(String) } : {},
      sourceEntityIds: [entry2.id]
    }] : []);
  });
  const immunity = [...conditionImmunities ?? [], ...conditionOwnedImmunities].find((candidate) => normalizeTag(candidate.condition) === normalizeTag(condition) && (candidate.requiredCauseTags ?? []).every((tag) => causeTags.has(normalizeTag(tag))));
  if (immunity) {
    events.push({
      type: "condition_immune",
      condition,
      sourceEntityIds: [...immunity.sourceEntityIds]
    });
    return state;
  }
  const { roundsLeft, expiry } = resolveDuration(payload.duration);
  const stacking = conditionStacking(condition);
  const existingLevel = conditionLevel(state, condition);
  if (stacking.mode === "levels" && stacking.max != null && existingLevel >= stacking.max) {
    events.push(narrativeEvent(`${conditionLabel(condition)}: \u0434\u043E\u0441\u0442\u0438\u0433\u043D\u0443\u0442 \u043C\u0430\u043A\u0441\u0438\u043C\u0430\u043B\u044C\u043D\u044B\u0439 \u0443\u0440\u043E\u0432\u0435\u043D\u044C ${stacking.max}.`));
    return state;
  }
  const persistedPayload = stacking.mode === "levels" ? { ...payload, stack_type: "stack" } : payload;
  const entry = {
    id: runtimeEffectId(ctx, "cond", state.activeEffects.length),
    name: condition,
    mechanics: persistedPayload,
    roundsLeft,
    expiry,
    source: source2,
    // E: id наложившего (кастера) — для реляционных правил (Очарованный ↛ очаровавший).
    ...sourceId ? { sourceId } : {}
  };
  events.push(conditionAppliedEvent(condition));
  const next = stackApply(state, entry, persistedPayload);
  const threshold = conditionLevel(next, condition);
  if (threshold > existingLevel) {
    const outcome = conditionRule(condition)?.thresholds?.find((candidate) => threshold >= candidate.atLevel);
    if (outcome) events.push(narrativeEvent(
      `${conditionLabel(condition)}: \u0443\u0440\u043E\u0432\u0435\u043D\u044C ${threshold}, \u043F\u043E\u0440\u043E\u0433 ${outcome.atLevel} \u2192 ${outcome.outcome}.`
    ));
  }
  return next;
}
function applyResource(state, payload, ctx, events) {
  let key = String(payload.id ?? payload.resource ?? "");
  if (!key) throw mechanicsError("INVALID_PAYLOAD", "runtime.payload", "resource id is empty");
  if (key === "spell_slot" && payload.level != null) key = `spell_slot_${payload.level}`;
  const evaluated = evaluate(payload.amount == null ? 1 : String(payload.amount), formulaCtx(ctx));
  if (typeof evaluated !== "number" || !Number.isFinite(evaluated)) {
    throw new FormulaError(`resource \xAB${key}\xBB: amount must resolve to a finite number`);
  }
  const amount = Math.max(0, Math.floor(evaluated));
  const op = String(payload.op ?? "grant");
  if (op !== "grant" && op !== "restore") {
    throw mechanicsError("INVALID_PAYLOAD", "runtime.payload", `unsupported resource operation \xAB${op}\xBB`);
  }
  const next = cloneState(state);
  const current = next.resources[key] ?? 0;
  if (op === "restore") {
    const max = next.maxResources[key];
    next.resources[key] = max != null ? Math.min(max, current + amount) : current + amount;
  } else {
    next.resources[key] = current + amount;
  }
  const gained = next.resources[key] - current;
  if (gained > 0) events.push(resourceRestoredEvent(key, gained, next.resources[key]));
  return next;
}
function collectDamageModifiers(ctx, state, filter, weaponMod) {
  return collectModifiers(state, passivesFromCtx(ctx), {
    roll: "damage",
    filter,
    formulaCtx: { ...formulaCtx(ctx), ...weaponMod !== void 0 ? { weaponMod } : {} },
    evalCtx: evalCtxOf(state, ctx)
  }).modifiers;
}
function damageRules(ctx, state) {
  return collectModifiers(state, passivesFromCtx(ctx), {
    roll: "damage",
    formulaCtx: formulaCtx(ctx),
    evalCtx: evalCtxOf(state, ctx)
  }).rules;
}
function explodeLimitOf(payload, ctx) {
  const ex = payload.explode;
  if (ex == null || ex === false) return void 0;
  const raw = typeof ex === "object" ? ex.limit ?? ex.times : ex;
  if (raw == null || raw === true) return void 0;
  try {
    const v = evaluate(String(raw).replace(/\s+/g, ""), formulaCtx(ctx));
    if (typeof v === "number") return Math.max(0, Math.floor(v));
  } catch {
  }
  const n = Number(raw);
  return Number.isFinite(n) ? Math.max(0, Math.floor(n)) : void 0;
}
function doubleDice(formula) {
  return String(formula).replace(/(\d+)d(\d+)/gi, (_m, n, s) => `${Number(n) * 2}d${s}`);
}
function resolveDamageAmounts(payload, ctx, state, hand, crit = false, attackFacts) {
  const handWeapon = weaponContext(ctx.character, hand, state.equipment);
  const declaredDamageType = String(payload.type).trim();
  const dmgRules = damageRules(ctx, state);
  const explodeLimit = explodeLimitOf(payload, ctx);
  const damageRng = ctx.damageRng ?? ctx.rng;
  if (payload.dice === "weapon") {
    if (!handWeapon) {
      throw mechanicsError(
        "INVALID_MECHANICS",
        "context.character.equipment",
        `weapon damage requires an explicit equipped ${hand}-hand weapon`
      );
    }
    const lines = handWeapon.damages;
    const ab = String(payload.ability ?? "auto");
    return lines.map((line, i) => {
      const fr = rollFormula(crit ? doubleDice(line.dice) : line.dice, formulaCtx(ctx), { rng: damageRng });
      const ruled = applyDamageDieRules(fr.dice, dmgRules, { explodeLimit, rng: damageRng });
      let total = fr.total + ruled.delta;
      let extraMods = [];
      if (i === 0) {
        const weaponMods = [];
        const weaponDamageAbilityMod = handWeapon ? ctx.character.abilityMods[handWeapon.ability] ?? 0 : void 0;
        const attackWeaponMod = attackFacts?.weaponMod ?? weaponDamageAbilityMod;
        if (ab === "auto" && handWeapon) {
          weaponMods.push({
            value: weaponDamageAbilityMod ?? 0,
            source: ABILITY_LABEL[handWeapon.ability]
          });
        } else if (ab === "spellcasting") {
          weaponMods.push({
            value: ctx.character.spellcastingMod ?? 0,
            source: "\u0411\u0430\u0437\u043E\u0432\u0430\u044F \u0445\u0430\u0440\u0430\u043A\u0442\u0435\u0440\u0438\u0441\u0442\u0438\u043A\u0430 \u0437\u0430\u043A\u043B\u0438\u043D\u0430\u043D\u0438\u0439"
          });
        } else if (ab !== "auto" && ab !== "none") {
          weaponMods.push({
            value: ctx.character.abilityMods[ab] ?? 0,
            source: ABILITY_LABEL[ab]
          });
        }
        if (handWeapon?.damageEnchant) {
          weaponMods.push({ value: handWeapon.damageEnchant, source: "\u0417\u0430\u0447\u0430\u0440\u043E\u0432\u0430\u043D\u0438\u0435 \u043E\u0440\u0443\u0436\u0438\u044F" });
        }
        const usedAbility = ab === "auto" ? handWeapon?.ability : ab !== "none" && ab !== "spellcasting" ? ab : void 0;
        extraMods = [
          ...weaponMods,
          ...collectDamageModifiers(ctx, state, {
            hand,
            ...usedAbility ? { ability: usedAbility } : {},
            attackKind: attackFacts?.attackKind ?? "weapon",
            extraAttackSource: attackFacts?.extraAttackSource ?? "none",
            abilityModifierAlreadyIncluded: ab !== "none"
          }, attackWeaponMod)
        ];
        for (const m of extraMods) total += m.value;
      }
      return {
        amount: total,
        damageType: line.type,
        roll: { kind: "damage", advantage: "none", dice: ruled.dice, modifiers: [...fr.modifiers, ...extraMods], total, text: fr.text }
      };
    });
  }
  let damageType = declaredDamageType;
  if (damageType === "weapon") {
    if (!handWeapon) {
      throw mechanicsError(
        "INVALID_MECHANICS",
        "context.character.equipment",
        `weapon damage requires an explicit equipped ${hand}-hand weapon`
      );
    }
    damageType = handWeapon.damageType;
  }
  const flat = payload.amount != null ? String(payload.amount) : payload.dice != null ? String(payload.dice) : null;
  if (flat != null) {
    const scaled = withScaling(flat, payload, ctx);
    const fr = rollFormula(crit ? doubleDice(scaled) : scaled, formulaCtx(ctx), { rng: damageRng });
    const ruled = applyDamageDieRules(fr.dice, dmgRules, { explodeLimit, rng: damageRng });
    const usedAbility = payload.ability != null && payload.ability !== "auto" && payload.ability !== "none" ? payload.ability : void 0;
    const extraMods = payload.suppress_damage_modifiers === true ? [] : collectDamageModifiers(ctx, state, usedAbility ? { ability: usedAbility } : {});
    let total = fr.total + ruled.delta;
    for (const m of extraMods) total += m.value;
    return [{
      amount: total,
      damageType,
      roll: { kind: "damage", advantage: "none", dice: ruled.dice, modifiers: [...fr.modifiers, ...extraMods], total, text: fr.text }
    }];
  }
  return [{ amount: 0, damageType }];
}
function applyBoon(state, p, source2, events, ctx) {
  const die = String(p.die ?? "\u043A6").replace(/d/i, "\u043A");
  const name = `\u0422\u0430\u043B\u043E\u043D ${die}${p.id ? ` (${source2})` : ""}`;
  const entry = {
    id: runtimeEffectId(ctx, "boon", state.activeEffects.length),
    name,
    mechanics: p,
    expiry: "manual",
    source: source2
  };
  const next = { ...state, activeEffects: [...state.activeEffects, entry] };
  events.push({ type: "effect_applied", name, sourceAction: source2 });
  events.push(narrativeEvent(
    `\u0422\u0430\u043B\u043E\u043D ${die}: \u043F\u043E\u043B\u0443\u0447\u0430\u0442\u0435\u043B\u044C \u0434\u043E\u0431\u0430\u0432\u043B\u044F\u0435\u0442 ${die} \u043A \u0431\u0440\u043E\u0441\u043A\u0443 \u0430\u0442\u0430\u043A\u0438, \u043F\u0440\u043E\u0432\u0435\u0440\u043A\u0435 \u0438\u043B\u0438 \u0441\u043F\u0430\u0441\u0431\u0440\u043E\u0441\u043A\u0443${p.expires ? ` (\u0438\u0441\u0442\u0435\u043A\u0430\u0435\u0442: ${p.expires})` : ""}. \u0421\u043D\u0438\u043C\u0438\u0442\u0435 \u044D\u0444\u0444\u0435\u043A\u0442 \u043F\u0440\u0438 \u0438\u0441\u043F\u043E\u043B\u044C\u0437\u043E\u0432\u0430\u043D\u0438\u0438.`
  ));
  return next;
}
function applyTransform(state, p, source2, events, ctx) {
  const formName = String(p.form ?? p.value ?? p.into ?? "\u0414\u0438\u043A\u0438\u0439 \u043E\u0431\u043B\u0438\u043A");
  const entry = {
    id: runtimeEffectId(ctx, "form", state.activeEffects.length),
    name: `\u041E\u0431\u043B\u0438\u043A: ${formName}`,
    mechanics: p,
    expiry: "manual",
    source: source2
  };
  const next = { ...state, activeEffects: [...state.activeEffects, entry] };
  events.push({ type: "effect_applied", name: entry.name, sourceAction: source2 });
  events.push(narrativeEvent(
    `\u041F\u0440\u0435\u0432\u0440\u0430\u0449\u0435\u043D\u0438\u0435 (${source2}): \u0438\u0441\u043F\u043E\u043B\u044C\u0437\u0443\u0439\u0442\u0435 \u0441\u0442\u0430\u0442-\u0431\u043B\u043E\u043A \u0437\u0432\u0435\u0440\u044F${p.max_cr != null ? ` (\u041F\u041E \u2264 ${p.max_cr})` : ""}; \u043C\u0435\u043D\u0442\u0430\u043B\u044C\u043D\u044B\u0435 \u0445\u0430\u0440\u0430\u043A\u0442\u0435\u0440\u0438\u0441\u0442\u0438\u043A\u0438 \u0438 \u0441\u043F\u0430\u0441\u0431\u0440\u043E\u0441\u043A\u0438 \u041C\u0414\u0420/\u0418\u041D\u0422/\u0425\u0410\u0420 \u2014 \u0432\u0430\u0448\u0438. \u0421\u043D\u0438\u043C\u0438\u0442\u0435 \u044D\u0444\u0444\u0435\u043A\u0442 \u043F\u0440\u0438 \u0432\u043E\u0437\u0432\u0440\u0430\u0442\u0435.`
  ));
  return next;
}
function invQtyOf(state, cardId) {
  return state.inventory.find((r) => r.cardId === cardId)?.qty ?? 0;
}
function addItemToInventory(state, cardId, qty) {
  const inventory = state.inventory.map((r) => ({ ...r }));
  const row = inventory.find((r) => r.cardId === cardId && r.containerId == null);
  if (row) row.qty += qty;
  else inventory.push({ cardId, qty });
  return { ...state, inventory };
}
function applyPayloads(payloads, state, ctx, events, source2, hand, halfDamage = false, whoTarget = false, targetRef = { mutated: false }, crit = false, attackFacts) {
  let next = state;
  const route = (mutate) => {
    if (whoTarget && targetRef.aliasesSelf) {
      next = mutate(next);
    } else if (whoTarget && targetRef.state) {
      targetRef.state = mutate(targetRef.state);
      targetRef.mutated = true;
    } else {
      next = mutate(next);
    }
  };
  for (const p of payloads) {
    const kind = String(p.kind ?? "");
    switch (kind) {
      case "damage": {
        const routedTarget = whoTarget ? targetRef.aliasesSelf ? next : targetRef.state : void 0;
        if (routedTarget) {
          const tctx = {
            ...ctx,
            character: ctx.target?.characterContext ?? ctx.character,
            passives: ctx.target?.passives ?? ctx.passives
          };
          let damagedTarget = routedTarget;
          for (const dmg of resolveDamageAmounts(p, ctx, next, hand, crit, attackFacts)) {
            const amount = halfDamage ? Math.floor(dmg.amount / 2) : dmg.amount;
            const res = applyIncomingDamage(damagedTarget, amount, tctx, {
              damageType: dmg.damageType,
              roll: dmg.roll
            });
            damagedTarget = res.state;
            events.push(...res.events);
          }
          if (targetRef.aliasesSelf) {
            next = damagedTarget;
          } else {
            targetRef.state = damagedTarget;
            targetRef.mutated = true;
          }
        } else {
          for (const dmg of resolveDamageAmounts(p, ctx, next, hand, crit, attackFacts)) {
            const amount = halfDamage ? Math.floor(dmg.amount / 2) : dmg.amount;
            events.push(damageEvent(amount, dmg.damageType, dmg.roll));
          }
        }
        break;
      }
      case "healing":
        route((s) => applyHealing(s, p, ctx, events));
        break;
      case "reduce_damage":
        route((s) => applyReduceDamage(s, p, ctx, events));
        break;
      case "temp_hp":
        route((s) => applyTempHp(s, p, ctx, events));
        break;
      case "condition":
        route((s) => applyCondition(
          s,
          p,
          source2,
          events,
          ctx,
          ctx.selfId,
          whoTarget ? ctx.target?.conditionImmunities : ctx.conditionImmunities
        ));
        break;
      case "resource":
        route((s) => applyResource(s, p, ctx, events));
        break;
      case "modifier":
        route((s) => applyModifierPayload(
          s,
          p,
          source2,
          events,
          ctx,
          whoTarget ? ctx.target?.id : ctx.selfId
        ));
        break;
      // Typed, short-lived opportunity consumed by an authoritative follow-up
      // command (Cleave today; reusable for later attack riders).
      case "attack_follow_up":
        route((s) => applyModifierPayload(
          s,
          p,
          source2,
          events,
          ctx,
          whoTarget ? ctx.target?.id : ctx.selfId
        ));
        break;
      case "grant_sense":
        route((s) => applySensePayload(
          s,
          p,
          source2,
          events,
          ctx,
          whoTarget ? ctx.target?.id : ctx.selfId
        ));
        break;
      case "resistance":
        route((s) => applyResistancePayload(s, p, source2, events, ctx));
        break;
      case "set_value": {
        if (p.target === "ac_base") {
          route((s) => applyAcBaseMethod(s, p, source2, events, ctx));
          break;
        }
        const fctx = whoTarget && targetRef.state ? targetFormulaCtx(ctx.target) ?? formulaCtx(ctx) : formulaCtx(ctx);
        route((s) => applySetValue(s, p, fctx, events));
        break;
      }
      case "grant_effect": {
        const slugs = [];
        if (typeof p.value === "string" && p.value) slugs.push(p.value);
        if (Array.isArray(p.values)) {
          for (const v of p.values) if (typeof v === "string" && v) slugs.push(v);
        }
        route((s) => applyGrantEffect(
          s,
          slugs,
          ctx.grantedEffects,
          events,
          ctx,
          whoTarget ? ctx.target?.id : ctx.selfId
        ));
        break;
      }
      case "variable":
        throw mechanicsError(
          "UNKNOWN_PAYLOAD",
          "runtime.payload.kind",
          "variable is a build primitive and has no RuntimeState interpreter"
        );
      case "value_method":
        throw mechanicsError(
          "UNKNOWN_PAYLOAD",
          "runtime.payload.kind",
          "value_method is a build primitive and cannot be activated"
        );
      case "choice": {
        const raw = ctx.choices?.[String(p.id ?? "choice")];
        const vals = raw == null ? [] : Array.isArray(raw) ? raw : [raw];
        if (vals.length) {
          const sub = selectedChoicePayloads(p, vals).map(normalizeChoicePayload).filter((sp) => !String(sp.kind).startsWith("grant_"));
          next = applyPayloads(
            sub,
            next,
            ctx,
            events,
            source2,
            hand,
            halfDamage,
            whoTarget,
            targetRef,
            crit,
            attackFacts
          );
        }
        break;
      }
      case "add_item": {
        const cardId = String(p.card_id ?? p.value ?? "");
        const qty = Math.max(1, Math.floor(Number(p.qty ?? p.amount ?? 1)) || 1);
        if (cardId) {
          next = addItemToInventory(next, cardId, qty);
          const name = typeof p.name === "string" ? p.name : void 0;
          events.push(itemAddedEvent(cardId, qty, invQtyOf(next, cardId), name));
        } else {
          throw mechanicsError("INVALID_PAYLOAD", "runtime.payload", "add_item has no card_id/value");
        }
        break;
      }
      case "movement": {
        const evaluated = evaluate(p.distance == null ? 0 : String(p.distance), formulaCtx(ctx));
        if (typeof evaluated !== "number" || !Number.isFinite(evaluated)) {
          throw new FormulaError("movement distance must resolve to a finite number");
        }
        events.push({
          type: "movement",
          mode: String(p.value ?? "move"),
          distanceFt: Math.max(0, evaluated)
        });
        break;
      }
      case "boon":
        route((s) => applyBoon(s, p, source2, events, ctx));
        break;
      case "reroll": {
        const which = String(p.which ?? "d20").replace(/d/i, "\u043A");
        const keep = p.keep === "either" ? "\u043E\u0441\u0442\u0430\u0432\u044C\u0442\u0435 \u043B\u044E\u0431\u043E\u0439 \u0438\u0437 \u0434\u0432\u0443\u0445 \u0440\u0435\u0437\u0443\u043B\u044C\u0442\u0430\u0442\u043E\u0432" : "\u0438\u0441\u043F\u043E\u043B\u044C\u0437\u0443\u0439\u0442\u0435 \u043D\u043E\u0432\u044B\u0439 \u0440\u0435\u0437\u0443\u043B\u044C\u0442\u0430\u0442";
        events.push(narrativeEvent(`\u041F\u0435\u0440\u0435\u0431\u0440\u043E\u0441 ${which}: \u043F\u0435\u0440\u0435\u0431\u0440\u043E\u0441\u044C\u0442\u0435 \u043A\u043E\u0441\u0442\u044C \u2014 ${keep}.`));
        break;
      }
      case "transform":
        route((s) => applyTransform(s, p, source2, events, ctx));
        break;
      case "narrative":
        events.push(narrativeEvent(String(p.description ?? p.text ?? "")));
        break;
      default:
        throw mechanicsError("UNKNOWN_PAYLOAD", "runtime.payload.kind", `executor does not own payload kind \xAB${kind || "?"}\xBB`);
    }
  }
  return next;
}
function modifierMatchesRoll(payload, roll, filter, evalCtx, scope = "self") {
  if (payload.kind !== "modifier" || payload.consume !== "next") return false;
  if (String(payload.scope ?? "self") !== scope) return false;
  const applies = payload.applies_to;
  const rollMatches = applies?.roll === roll || applies?.roll === "d20" && ["attack", "saving_throw", "ability_check", "initiative"].includes(roll);
  if (!rollMatches) return false;
  const requiredFilter = applies?.filter;
  if (requiredFilter) {
    if (!filter) return false;
    for (const [key, value] of Object.entries(requiredFilter)) {
      if (filter[key] !== value) return false;
    }
  }
  return matchesWhen(payload.when, evalCtx);
}
function consumeNextRollEffects(state, roll, events, options = {}) {
  const expired = [];
  const activeEffects = state.activeEffects.filter((entry) => {
    const consumes = payloadsOf(entry.mechanics).some((payload) => modifierMatchesRoll(payload, roll, options.filter, options.evalCtx, options.scope));
    if (consumes) expired.push(entry);
    return !consumes;
  });
  if (!expired.length) return state;
  expired.forEach((entry) => events.push({ type: "effect_expired", name: entry.name }));
  return { ...state, activeEffects };
}
function expireEffectsForTrigger(state, trigger, events) {
  const expired = [];
  const activeEffects = state.activeEffects.filter((entry) => {
    const mechanics = entry.mechanics;
    const triggers = [
      ...Array.isArray(mechanics.hidden_end_triggers) ? mechanics.hidden_end_triggers.map(String) : [],
      ...Array.isArray(mechanics.end_triggers) ? mechanics.end_triggers.map(String) : []
    ];
    if (!triggers.includes(trigger)) return true;
    expired.push(entry);
    return false;
  });
  if (!expired.length) return state;
  expired.forEach((entry) => events.push({ type: "effect_expired", name: entry.name }));
  return { ...state, activeEffects };
}
function runAttackRoll(effect, state, ctx, events, source2, pending, targetRef = { mutated: false }, deferredSaves = []) {
  const whoTarget = String(effect.who ?? "target") === "target" && (!!targetRef.state || targetRef.aliasesSelf === true);
  const hand = resolveHand(effect);
  const ac = ctx.target.ac;
  const passives = passivesFromCtx(ctx);
  const currentWeapon = weaponContext(ctx.character, hand, state.equipment);
  const attackRange = attackRangeFromEffect(effect, hand, ctx.character, state.equipment);
  const heavy = currentWeapon ? evaluateWeaponHeavyRule(
    currentWeapon,
    attackRange ?? currentWeapon.defaultAttackMode,
    ctx.character.abilityScores
  ) : null;
  if (heavy && !heavy.valid) throw new Error(heavy.issue);
  const attackFacts = attackRollQueryFacts(effect, hand, ctx.character, state.equipment);
  const attackFilter = {
    ...attackFacts,
    ...ctx.target?.id ? { targetActorId: ctx.target.id } : {},
    ...ctx.spell?.sourceClass ? { spellClass: ctx.spell.sourceClass } : {}
  };
  const collected = collectModifiers(state, passives, {
    roll: "attack",
    filter: attackFilter,
    formulaCtx: formulaCtx(ctx),
    evalCtx: evalCtxOf(state, ctx)
  });
  const projected = projectedAgainst(
    ctx.target,
    "attack",
    attackRange,
    evalCtxOf(state, ctx),
    attackFilter
  );
  const mods = [...attackAbilityMods(effect, ctx, hand, state), ...collected.modifiers, ...projected.modifiers];
  const roll = ctx.forcedAttackRoll ? retargetAttackRoll(ctx.forcedAttackRoll, ac) : rollD20({
    // C7: объединяем флаги обоих проходов — и преим., и помеха (свои + от цели) → none.
    advantage: foldAdvantage(
      collected.hasAdvantage || projected.hasAdvantage,
      collected.hasDisadvantage || projected.hasDisadvantage || Boolean(heavy?.valid && heavy.disadvantage)
    ),
    modifiers: mods,
    target: { type: "ac", value: ac },
    rng: ctx.rng,
    rules: [...collected.rules, ...projected.rules]
    // свои правила + проекция цели (Blade Ward)
  });
  events.push(rollEvent("\u0410\u0442\u0430\u043A\u0430", roll));
  let next = consumeNextRollEffects(state, "attack", events, {
    filter: attackFilter,
    evalCtx: evalCtxOf(state, ctx)
  });
  if (targetRef.state) {
    const consumedTarget = consumeNextRollEffects(targetRef.state, "attack", events, {
      scope: "target",
      evalCtx: evalCtxOf(state, ctx)
    });
    if (consumedTarget !== targetRef.state) {
      targetRef.state = consumedTarget;
      targetRef.mutated = true;
    }
  }
  next = expireEffectsForTrigger(next, "actor_makes_attack_roll", events);
  if (ctx.pauseAfterAttackRoll) return next;
  if (Array.isArray(roll.triggered) && roll.triggered.length) {
    next = applyPayloads(roll.triggered, next, ctx, events, source2, hand, false, whoTarget, targetRef);
  }
  const outcome = roll.outcome === "hit" && projected.autoCrit ? "crit" : roll.outcome;
  if (roll.outcome === "hit" && projected.autoCrit) {
    events.push(narrativeEvent("\u0410\u0432\u0442\u043E\u043A\u0440\u0438\u0442: \u043F\u043E\u043F\u0430\u0434\u0430\u043D\u0438\u0435 \u0432\u0431\u043B\u0438\u0437\u0438 \u0441\u0442\u0430\u043D\u043E\u0432\u0438\u0442\u0441\u044F \u043A\u0440\u0438\u0442\u0438\u0447\u0435\u0441\u043A\u0438\u043C (\u0441\u043E\u0441\u0442\u043E\u044F\u043D\u0438\u0435 \u0446\u0435\u043B\u0438)."));
  }
  if (outcome === "hit" || outcome === "crit") {
    const useCritPayloads = outcome === "crit" && Array.isArray(effect.on_crit);
    const payloads = useCritPayloads ? effect.on_crit : effect.on_hit;
    const critDouble = outcome === "crit" && !useCritPayloads;
    const attackAbility = String(effect.ability);
    const attackWeaponMod = attackAbility === "spellcasting" ? ctx.character.spellcastingMod : attackAbility === "auto" ? ctx.character.abilityMods[currentWeapon.ability] : ctx.character.abilityMods[attackAbility];
    const attackDamageFacts = {
      attackKind: attackFacts.attackKind,
      extraAttackSource: extraAttackSourceFromEffect(
        effect,
        hand,
        ctx.character,
        state.equipment
      ),
      weaponMod: attackWeaponMod
    };
    const damageEventStart = events.length;
    if (Array.isArray(payloads)) {
      next = applyPayloads(
        payloads,
        next,
        ctx,
        events,
        source2,
        hand,
        false,
        whoTarget,
        targetRef,
        critDouble,
        attackDamageFacts
      );
    }
    const dealtDamage = events.slice(damageEventStart).some((entry) => entry.type === "damage" && entry.amount > 0);
    next = runMastery(
      next,
      ctx,
      events,
      pending,
      targetRef,
      hand,
      "hit",
      deferredSaves,
      { attackRange, dealtDamage }
    );
    const declaredProperties = Array.isArray(effect.weapon_properties) ? effect.weapon_properties.map(String) : [];
    next = emitEvent({
      kind: "hit",
      source: "self",
      data: {
        advantage: roll.advantage,
        attackRange: attackRange ?? "unknown",
        weaponProperties: [.../* @__PURE__ */ new Set([...currentWeapon?.properties ?? [], ...declaredProperties])],
        nearbyEligibleAllyToTarget: ctx.attackFacts?.nearbyEligibleAllyToTarget === true,
        critical: outcome === "crit"
      }
    }, next, ctx, events, pending, targetRef, deferredSaves);
    if (outcome === "crit") next = emitEvent(
      { kind: "crit", source: "self" },
      next,
      ctx,
      events,
      pending,
      targetRef,
      deferredSaves
    );
  } else {
    if (Array.isArray(effect.on_miss)) {
      next = applyPayloads(effect.on_miss, next, ctx, events, source2, hand, false, whoTarget, targetRef);
    }
    next = runMastery(
      next,
      ctx,
      events,
      pending,
      targetRef,
      hand,
      "miss",
      deferredSaves,
      { attackRange, dealtDamage: false }
    );
    next = emitEvent(
      { kind: "miss", source: "self" },
      next,
      ctx,
      events,
      pending,
      targetRef,
      deferredSaves
    );
  }
  return next;
}
function runMastery(state, ctx, events, pending, targetRef, hand, event, deferredSaves, facts) {
  const m = activeMastery(ctx, state, hand, facts);
  if (!m || m.event !== event) return state;
  const effects = m.mechanics.effects;
  if (!Array.isArray(effects) || !effects.length) return state;
  events.push(narrativeEvent(`\u0418\u0441\u043A\u0443\u0441\u043D\u043E\u0441\u0442\u044C: ${m.name}`));
  const mctx = {
    ...ctx,
    weaponMod: m.weaponMod,
    deferredSaveSource: {
      kind: "weapon_mastery",
      entityId: m.id,
      name: m.name,
      weaponMod: m.weaponMod
    }
  };
  return runMechanicEffects(
    effects,
    state,
    mctx,
    events,
    m.name,
    pending,
    targetRef,
    false,
    deferredSaves
  );
}
function savedConditionsOf(effect) {
  const onFail = effect.on_fail;
  if (!Array.isArray(onFail)) return [];
  return onFail.filter((p) => p.kind === "condition" && p.value != null).map((p) => String(p.value));
}
function runSave(effect, state, ctx, events, source2, targetRef = { mutated: false }, deferredSaves = []) {
  const whoTarget = String(effect.who ?? "target") === "target" && (!!targetRef.state || targetRef.aliasesSelf === true);
  const dcFormula = String(effect.dc);
  const dc = evalDc(dcFormula, ctx);
  const ability = String(effect.ability);
  if (ctx.deferTargetSaves && whoTarget && targetRef.state && ctx.deferredSaveSource) {
    deferredSaves.push({
      source: { ...ctx.deferredSaveSource },
      effect: { ...effect },
      ability,
      dc,
      avoidsConditions: savedConditionsOf(effect)
    });
    return state;
  }
  const targetState = ctx.target?.runtimeState;
  const collected = targetState ? collectModifiers(targetState, [], {
    roll: "saving_throw",
    filter: { ability },
    evalCtx: { state: targetState, activeConditions: activeConditionsOf(targetState), savedConditions: new Set(savedConditionsOf(effect)) }
  }) : { modifiers: [], advantage: "none", autoFail: false, rules: [] };
  let success;
  if (ctx.forceSaveOutcome != null) {
    success = ctx.forceSaveOutcome === "success";
  } else if (collected.autoFail) {
    events.push(narrativeEvent(`\u0421\u043F\u0430\u0441\u0431\u0440\u043E\u0441\u043E\u043A ${ABILITY_LABEL[ability]} \u2014 \u0430\u0432\u0442\u043E\u043F\u0440\u043E\u0432\u0430\u043B (\u0441\u043E\u0441\u0442\u043E\u044F\u043D\u0438\u0435 \u0446\u0435\u043B\u0438).`));
    success = false;
  } else {
    const saveMod = targetSaveMod(ctx.target, ability);
    const roll = rollD20({
      advantage: collected.advantage,
      modifiers: [{ value: saveMod, source: "\u0446\u0435\u043B\u044C" }, ...collected.modifiers],
      target: { type: "dc", value: dc },
      rng: ctx.rng,
      rules: collected.rules
    });
    events.push(rollEvent("\u0421\u043F\u0430\u0441\u0431\u0440\u043E\u0441\u043E\u043A", { ...roll, kind: "save" }));
    success = ctx.planning ? false : roll.outcome === "success";
  }
  let next = state;
  if (targetState && targetRef.state) {
    const consumedTarget = consumeNextRollEffects(targetRef.state, "saving_throw", events, {
      filter: { ability },
      evalCtx: {
        state: targetRef.state,
        activeConditions: activeConditionsOf(targetRef.state),
        savedConditions: new Set(savedConditionsOf(effect))
      }
    });
    if (consumedTarget !== targetRef.state) {
      targetRef.state = consumedTarget;
      targetRef.mutated = true;
    }
  } else {
    next = consumeNextRollEffects(next, "saving_throw", events, {
      filter: { ability },
      evalCtx: evalCtxOf(next, ctx)
    });
  }
  const payloads = success ? effect.on_success : effect.on_fail;
  if (!Array.isArray(payloads)) return next;
  const half = success && payloads.some((p) => p.on_success === "half");
  return applyPayloads(payloads, next, ctx, events, source2, "main", half, whoTarget, targetRef);
}
function readTargetSave(mechanics, ctx) {
  const effects = mechanics.effects;
  if (!Array.isArray(effects)) return null;
  const effectIndex = effects.findIndex((effect) => String(effect.resolution ?? "") === "save" && String(effect.who ?? "target") === "target");
  if (effectIndex < 0) return null;
  const eff = effects[effectIndex];
  const path = `mechanics.effects[${effectIndex}]`;
  const ability = explicitAbility(
    eff.ability,
    `${path}.ability`,
    ABILITY_KEYS2,
    "save resolution"
  );
  explicitPositiveDc(eff.dc, `${path}.dc`, ctx);
  const dc = evalDc(String(eff.dc), ctx);
  const half = Array.isArray(eff.on_success) && eff.on_success.some((p) => p.on_success === "half");
  return { ability, dc, half, avoidsConditions: savedConditionsOf(eff) };
}
function runAbilityCheck(effect, state, ctx, events, source2, targetRef = { mutated: false }) {
  const ability = String(effect.ability);
  const skill = String(effect.skill ?? "");
  const prof = skill && ctx.character.skillExpertise?.includes(skill) ? ctx.character.profBonus * 2 : skill && ctx.character.skillProficiencies?.includes(skill) ? ctx.character.profBonus : 0;
  const attackerTotal = ctx.character.abilityMods[ability] + prof;
  const sense = String(effect.requires_sense ?? effect.sense ?? "");
  const checkFilter = {
    ability,
    ...skill ? { skill } : {},
    ...sense === "sight" || sense === "hearing" ? { sense } : {}
  };
  const collected = collectModifiers(state, passivesFromCtx(ctx), {
    roll: "ability_check",
    filter: checkFilter,
    formulaCtx: formulaCtx(ctx),
    evalCtx: evalCtxOf(state, ctx)
  });
  let next = state;
  let success;
  if (collected.autoFail) {
    events.push(narrativeEvent(
      `\u041F\u0440\u043E\u0432\u0435\u0440\u043A\u0430${sense ? ` (${sense})` : ""} \u2014 \u0430\u0432\u0442\u043E\u043F\u0440\u043E\u0432\u0430\u043B (\u0441\u043E\u0441\u0442\u043E\u044F\u043D\u0438\u0435).`
    ));
    success = false;
  } else {
    const attRoll = rollD20({
      advantage: collected.advantage,
      modifiers: [
        { value: attackerTotal, source: skill || ABILITY_LABEL[ability] },
        ...collected.modifiers
      ],
      ...effect.dc == null ? {} : { target: { type: "dc", value: evalDc(String(effect.dc), ctx) } },
      rng: ctx.rng,
      rules: collected.rules
    });
    events.push(rollEvent(skill ? `\u041F\u0440\u043E\u0432\u0435\u0440\u043A\u0430 (${skill})` : "\u041F\u0440\u043E\u0432\u0435\u0440\u043A\u0430", { ...attRoll, kind: "check" }));
    next = consumeNextRollEffects(state, "ability_check", events, {
      filter: checkFilter,
      evalCtx: evalCtxOf(state, ctx)
    });
    if (effect.dc != null) {
      success = attRoll.total >= evalDc(String(effect.dc), ctx);
    } else {
      const contestVs = effect.contest_vs;
      const checkMods = ctx.target.checkMods;
      const defSkill = contestVs.reduce(
        (best, candidate) => checkMods[candidate] > checkMods[best] ? candidate : best,
        contestVs[0]
      );
      const defMod = checkMods[defSkill];
      const defRoll = rollD20({ modifiers: [{ value: defMod, source: defSkill }], rng: ctx.rng });
      events.push(rollEvent(`\u041E\u0442\u0432\u0435\u0442 (${defSkill})`, { ...defRoll, kind: "check" }));
      success = attRoll.total > defRoll.total;
    }
  }
  if (!success) return next;
  const onSuccess = effect.on_success;
  if (!Array.isArray(onSuccess)) return next;
  const whoTarget = String(effect.who ?? "target") === "target" && (!!targetRef.state || targetRef.aliasesSelf === true);
  return applyPayloads(onSuccess, next, ctx, events, source2, "main", false, whoTarget, targetRef);
}
function runMechanicEffects(effects, state, ctx, events, sourceName, pending, targetRef = { mutated: false }, criticalDamage = false, deferredSaves = []) {
  let next = state;
  for (const eff of effects) {
    const resolution = String(eff.resolution ?? "");
    try {
      if (eff.kind === "choice") {
        const whoTarget = String(eff.who ?? "self") === "target" && (!!targetRef.state || targetRef.aliasesSelf === true);
        next = applyPayloads([eff], next, ctx, events, sourceName, "main", false, whoTarget, targetRef);
        continue;
      }
      if (resolution === "auto") {
        const results = eff.result ?? eff.results;
        if (Array.isArray(results)) {
          const whoTarget = String(eff.who ?? "self") === "target" && (!!targetRef.state || targetRef.aliasesSelf === true);
          next = applyPayloads(results, next, ctx, events, sourceName, "main", false, whoTarget, targetRef, criticalDamage);
        }
        continue;
      }
      if (resolution === "attack_roll") {
        next = runAttackRoll(eff, next, ctx, events, sourceName, pending, targetRef, deferredSaves);
        continue;
      }
      if (resolution === "save") {
        next = runSave(eff, next, ctx, events, sourceName, targetRef, deferredSaves);
        continue;
      }
      if (resolution === "ability_check") {
        next = runAbilityCheck(eff, next, ctx, events, sourceName, targetRef);
        continue;
      }
      throw mechanicsError(
        "UNKNOWN_RESOLUTION",
        "runtime.effect.resolution",
        `executor does not own resolution \xAB${resolution || "?"}\xBB`
      );
    } catch (e) {
      if (e instanceof MissingVariableError) {
        throw mechanicsError(
          "INVALID_FORMULA",
          "runtime.effect.formula",
          `variable \xAB${e.variable}\xBB is unavailable for effect \xAB${sourceName}\xBB`,
          e
        );
      }
      if (e instanceof FormulaError) {
        throw mechanicsError(
          "INVALID_FORMULA",
          "runtime.effect.formula",
          `effect \xAB${sourceName}\xBB cannot be evaluated: ${e.message}`,
          e
        );
      }
      throw e;
    }
  }
  return next;
}
var MAX_EVENT_CASCADE = 16;
var cascadeBudget = /* @__PURE__ */ new WeakMap();
function beginCascade(ctx) {
  cascadeBudget.set(ctx, { n: 0, warned: false });
}
function cascadeAllows(ctx, events) {
  let b = cascadeBudget.get(ctx);
  if (!b) {
    b = { n: 0, warned: false };
    cascadeBudget.set(ctx, b);
  }
  b.n += 1;
  if (b.n > MAX_EVENT_CASCADE) {
    if (!b.warned) {
      b.warned = true;
      events.push(narrativeEvent(`\u041A\u0430\u0441\u043A\u0430\u0434 \u0441\u043E\u0431\u044B\u0442\u0438\u0439 \u043F\u0440\u0435\u0432\u044B\u0441\u0438\u043B \u043B\u0438\u043C\u0438\u0442 (${MAX_EVENT_CASCADE}) \u2014 \u0434\u0430\u043B\u044C\u043D\u0435\u0439\u0448\u0438\u0435 \u0442\u0440\u0438\u0433\u0433\u0435\u0440\u044B \u043E\u0441\u0442\u0430\u043D\u043E\u0432\u043B\u0435\u043D\u044B \u0432\u043E \u0438\u0437\u0431\u0435\u0436\u0430\u043D\u0438\u0435 \u0437\u0430\u0446\u0438\u043A\u043B\u0438\u0432\u0430\u043D\u0438\u044F.`));
    }
    return false;
  }
  return true;
}
function emitEvent(ev, state, ctx, events, pending, targetRef = { mutated: false }, deferredSaves = []) {
  const triggers = ctx.triggers ?? [];
  const listeners = collectListeners(ev, state, [...passivesFromCtx(ctx), ...triggers], evalCtxOf(state, ctx));
  if (!listeners.length) return state;
  if (!cascadeAllows(ctx, events)) return state;
  let next = state;
  for (const lm of listeners) {
    if (!isAuto(lm)) {
      pending.push(toOffer(lm, ev));
      continue;
    }
    const per = lm.usesPer;
    const firedTurn = new Set(next.firedThisTurn ?? []);
    const firedRest = new Set(next.firedThisRest ?? []);
    if (per === "turn" && firedTurn.has(lm.id)) continue;
    if (per && per !== "turn" && firedRest.has(lm.id)) continue;
    if (per === "turn") {
      firedTurn.add(lm.id);
      next = { ...next, firedThisTurn: [...firedTurn] };
    } else if (per) {
      firedRest.add(lm.id);
      next = { ...next, firedThisRest: [...firedRest] };
    }
    const effs = lm.mechanics.effects ?? [];
    if (effs.length) {
      events.push(narrativeEvent(`\u0421\u0440\u0430\u0431\u043E\u0442\u0430\u043B\u043E: ${lm.name}`));
      next = runMechanicEffects(
        effs,
        next,
        ctx,
        events,
        lm.name,
        pending,
        targetRef,
        ev.kind === "hit" && ev.data?.critical === true,
        deferredSaves
      );
    }
  }
  return next;
}
function executeAction(state, mechanics, ctx) {
  const cost = preflightMechanicsExecution(state, mechanics, ctx);
  beginCascade(ctx);
  let next = cloneState(state);
  const events = [];
  const pending = [];
  const deferredSaves = [];
  const hasSeparateTarget = ctx.target?.runtimeState != null && !(ctx.selfId != null && ctx.target.id === ctx.selfId);
  const aliasesSelf = ctx.selfId != null && ctx.target?.id === ctx.selfId;
  const targetRef = {
    state: hasSeparateTarget ? cloneState(ctx.target.runtimeState) : void 0,
    mutated: false,
    aliasesSelf
  };
  if (cost.length) {
    const check = canPay(next, cost);
    if (!check.ok) throw new InsufficientResourcesError(check.missing);
    const paid = pay(next, cost);
    next = paid.state;
    events.push(...paid.events);
  }
  const effects = mechanics.effects;
  if (Array.isArray(effects)) {
    const sourceName = String(mechanics.name ?? "\u0434\u0435\u0439\u0441\u0442\u0432\u0438\u0435");
    next = runMechanicEffects(
      effects,
      next,
      ctx,
      events,
      sourceName,
      pending,
      targetRef,
      false,
      deferredSaves
    );
  }
  if (ctx.spell && !ctx.suppressSpellCastEvent) {
    next = emitEvent(
      { kind: "spell_cast", source: "self", data: { level: ctx.spell.castLevel ?? ctx.spell.baseLevel } },
      next,
      ctx,
      events,
      pending,
      targetRef,
      deferredSaves
    );
    if (ctx.spell.components?.verbal === true) {
      next = expireEffectsForTrigger(
        next,
        "actor_casts_spell_with_verbal_component",
        events
      );
    }
  }
  void ctx.character;
  return {
    state: next,
    events,
    ...pending.length ? { pendingReactions: pending } : {},
    ...deferredSaves.length ? { deferredTargetSaves: deferredSaves } : {},
    ...targetRef.mutated ? { targetState: targetRef.state } : {}
  };
}
function applyIncomingDamage(state, amount, ctx, opts) {
  beginCascade(ctx);
  let next = cloneState(state);
  const events = [];
  const pending = [];
  const raw = Math.max(0, Math.floor(amount));
  const damageType = opts?.damageType ?? "\u0443\u0440\u043E\u043D";
  const resistanceRule = resistanceRuleFor(next, ctx, damageType);
  const level = resistanceRule.level;
  const resisted = applyResistance(raw, level);
  if (level && resisted !== raw) {
    const label = level === "immunity" ? "\u0438\u043C\u043C\u0443\u043D\u0438\u0442\u0435\u0442" : level === "resistance" ? "\u0441\u043E\u043F\u0440\u043E\u0442\u0438\u0432\u043B\u0435\u043D\u0438\u0435" : "\u0443\u044F\u0437\u0432\u0438\u043C\u043E\u0441\u0442\u044C";
    events.push({
      type: "narrative",
      text: `${label} \u043A \xAB${damageType}\xBB: ${raw} \u2192 ${resisted}`,
      damageAdjustment: {
        damageType,
        adjustment: level,
        before: raw,
        after: resisted,
        sourceEntityIds: resistanceRule.sourceEntityIds
      }
    });
  }
  const reduction = Math.max(0, Math.floor(opts?.damageReduction ?? 0));
  const dmg = Math.max(0, resisted - reduction);
  if (reduction > 0) {
    events.push(narrativeEvent(`\u0421\u043D\u0438\u0436\u0435\u043D\u0438\u0435 \u0443\u0440\u043E\u043D\u0430: ${resisted} \u2192 ${dmg} (\u2212${Math.min(reduction, resisted)})`));
  }
  const absorbed = Math.min(next.hp.temp, dmg);
  next.hp.temp -= absorbed;
  next.hp.current = Math.max(0, next.hp.current - (dmg - absorbed));
  events.push(damageEvent(dmg, damageType, opts?.roll));
  const conc = concentrationEntry(next);
  if (conc && dmg > 0 && ctx.forceSaveOutcome == null) {
    const dc = concentrationDC(dmg);
    const collected = collectModifiers(next, passivesFromCtx(ctx), {
      roll: "saving_throw",
      filter: { ability: "con" },
      formulaCtx: formulaCtx(ctx),
      evalCtx: evalCtxOf(next, ctx)
    });
    const conMod = opts?.conSaveBonus ?? (ctx.character.abilityMods.con ?? 0);
    const advantage = opts?.crit ? collected.advantage === "advantage" ? "none" : "disadvantage" : collected.advantage;
    const roll = rollD20({
      advantage,
      modifiers: [{ value: conMod, source: "\u0422\u0415\u041B" }, ...collected.modifiers],
      target: { type: "dc", value: dc },
      rng: ctx.rng
    });
    events.push(rollEvent(`\u041A\u043E\u043D\u0446\u0435\u043D\u0442\u0440\u0430\u0446\u0438\u044F (\u0421\u041B ${dc})`, { ...roll, kind: "save" }));
    if (roll.outcome !== "success") {
      const dropped = dropConcentration(next, `\u043F\u0440\u043E\u0432\u0430\u043B \u0441\u043F\u0430\u0441\u0431\u0440\u043E\u0441\u043A\u0430 \u0421\u041B ${dc}`);
      next = dropped.state;
      events.push(...dropped.events);
    }
  }
  next = emitEvent({ kind: "damage_taken", source: "self", data: { amount: dmg } }, next, ctx, events, pending);
  if (next.hp.current === 0 && state.hp.current > 0) {
    next = emitEvent({ kind: "reduced_to_0_hp", source: "self" }, next, ctx, events, pending);
  }
  return { state: next, events, ...pending.length ? { pendingReactions: pending } : {} };
}

// src/engine/derivedValue.ts
function pickBestMethod(methods, additive = []) {
  const applicable = methods.filter((m) => m.applicable !== false);
  let best;
  for (const m of applicable) {
    if (!best || m.value > best.value) best = m;
  }
  const rejected5 = applicable.filter((m) => m !== best).map((m) => ({ name: m.name, value: m.value }));
  const additiveSum = additive.reduce((s, p) => s + p.value, 0);
  return {
    value: (best?.value ?? 0) + additiveSum,
    parts: [...best?.parts ?? [], ...additive],
    ...best ? {
      selectedMethod: {
        name: best.name,
        reason: best.reason ?? (applicable.length > 1 ? "\u0432\u044B\u0431\u0440\u0430\u043D \u043D\u0430\u0438\u0431\u043E\u043B\u044C\u0448\u0438\u0439 \u043F\u0440\u0438\u043C\u0435\u043D\u0438\u043C\u044B\u0439 \u0440\u0435\u0437\u0443\u043B\u044C\u0442\u0430\u0442" : "\u0435\u0434\u0438\u043D\u0441\u0442\u0432\u0435\u043D\u043D\u044B\u0439 \u043F\u0440\u0438\u043C\u0435\u043D\u0438\u043C\u044B\u0439 \u0441\u043F\u043E\u0441\u043E\u0431")
      }
    } : {},
    ...rejected5.length ? { rejected: rejected5 } : {}
  };
}

// src/engine/ac.ts
function formulaCtx2(character) {
  return {
    abilityMods: character.abilityMods,
    profBonus: character.profBonus,
    selfLevel: character.level,
    classLevels: character.classLevels
  };
}
function evalNum(formula, character) {
  const v = evaluate(formula, formulaCtx2(character));
  if (typeof v !== "number") throw new Error(`\u0424\u043E\u0440\u043C\u0443\u043B\u0430 \u041A\u0417 \xAB${formula}\xBB \u0434\u043E\u043B\u0436\u043D\u0430 \u0431\u044B\u0442\u044C \u0447\u0438\u0441\u043B\u043E\u043C`);
  return v;
}
function tryEvalNum(formula, character) {
  try {
    return evalNum(formula, character);
  } catch {
    return null;
  }
}
function parseFlatBonus(raw) {
  const m = raw.match(/^\+?(-?\d+(?:\.\d+)?)/);
  return m ? Number(m[1]) : 0;
}
function acBaseFormulas(mechs) {
  const out = [];
  const consider = (p) => {
    if (p && p.kind === "set_value" && p.target === "ac_base") {
      const f = String(p.formula ?? p.value ?? "");
      if (f) out.push(f);
    }
  };
  for (const mech of mechs) {
    if (!mech || typeof mech !== "object") continue;
    consider(mech);
    const effects = mech.effects ?? mech.interactions;
    if (!Array.isArray(effects)) continue;
    for (const eff of effects) {
      consider(eff);
      const results = eff.result ?? eff.results;
      if (Array.isArray(results)) for (const r of results) consider(r);
    }
  }
  return out;
}
function allKnownCards(character) {
  const map = /* @__PURE__ */ new Map();
  for (const c of character.equippedCards ?? []) map.set(c.id, c);
  for (const c of character.knownCards ?? []) map.set(c.id, c);
  return [...map.values()];
}
function resolveCard(id, cards) {
  return cards.find((c) => c.id === id) ?? getCard(id);
}
function isNonArmorBody(card) {
  const props = (card.properties ?? []).map((p) => String(p).toLowerCase());
  if (props.includes("cloth") || props.includes("clothing")) return true;
  const raw = String(card.bonus_value ?? "").trim();
  if (!raw) return true;
  if (/dex/i.test(raw)) return false;
  return parseFlatBonus(raw) <= 10;
}
function armorFromState(state, cards) {
  const bodyId = state.equipment.body;
  if (!bodyId) return void 0;
  const card = resolveCard(bodyId, cards);
  if (!card || isNonArmorBody(card)) return void 0;
  return card;
}
function shieldFromState(state, cards) {
  for (const slot of ["off_hand", "main_hand"]) {
    const id = state.equipment[slot];
    if (!id) continue;
    const card = resolveCard(id, cards);
    if (card && (card.type === "shield" || card.defense_type === "shield")) return card;
  }
  return void 0;
}
function armorAc(armor, character, parts) {
  const raw = armor.bonus_value ?? "10";
  if (/dex/i.test(raw)) {
    const baseFormula = raw.replace(/\s*\+\s*min\([^)]+\)/gi, "").replace(/\s*\+\s*dex.*$/i, "").trim();
    const base2 = tryEvalNum(baseFormula || "10", character);
    const total = tryEvalNum(raw, character);
    if (base2 === null || total === null) return null;
    const dexPart = total - base2;
    parts.push({ value: base2, source: armor.name, reason: "\u0434\u043E\u0441\u043F\u0435\u0445" });
    if (dexPart) {
      parts.push({ value: dexPart, source: "\u041B\u0412\u041A", reason: "\u043C\u043E\u0434\u0438\u0444\u0438\u043A\u0430\u0442\u043E\u0440 \u0445\u0430\u0440\u0430\u043A\u0442\u0435\u0440\u0438\u0441\u0442\u0438\u043A\u0438" });
    }
    return total;
  }
  const fixed = tryEvalNum(raw, character);
  if (fixed === null) return null;
  parts.push({ value: fixed, source: armor.name, reason: "\u0434\u043E\u0441\u043F\u0435\u0445" });
  return fixed;
}
function computeAC(character, state, passives, cardIndex) {
  const cards = allKnownCards(character);
  const byId = cardIndex ?? new Map(cards.map((c) => [c.id, c]));
  for (const [id, c] of byId) if (!cards.find((x) => x.id === id)) cards.push(c);
  const armor = armorFromState(state, cards);
  const shield = shieldFromState(state, cards);
  const methods = [];
  const rejected5 = [];
  const dex = character.abilityMods.dex ?? 0;
  const unarmoredBase = () => {
    const baseParts = [{ value: 10, source: "\u0431\u0430\u0437\u0430", reason: "\u0431\u0435\u0437 \u0434\u043E\u0441\u043F\u0435\u0445\u0430" }];
    if (dex) baseParts.push({ value: dex, source: "\u041B\u0412\u041A", reason: "\u043C\u043E\u0434\u0438\u0444\u0438\u043A\u0430\u0442\u043E\u0440 \u0445\u0430\u0440\u0430\u043A\u0442\u0435\u0440\u0438\u0441\u0442\u0438\u043A\u0438" });
    return { name: "\u0411\u0435\u0437 \u0434\u043E\u0441\u043F\u0435\u0445\u0430", value: 10 + dex, parts: baseParts };
  };
  if (armor) {
    const parts = [];
    const value = armorAc(armor, character, parts);
    if (value !== null) {
      methods.push({ name: armor.name, value, parts });
    } else {
      rejected5.push({ name: `${armor.name}: \u0444\u043E\u0440\u043C\u0443\u043B\u0430 \u041A\u0417 \xAB${armor.bonus_value}\xBB \u043D\u0435 \u0440\u0430\u0441\u043F\u043E\u0437\u043D\u0430\u043D\u0430`, value: 0 });
      methods.push(unarmoredBase());
    }
  } else {
    methods.push(unarmoredBase());
    const acMechs = [...passives, ...state.activeEffects.map((e) => e.mechanics)];
    for (const formula of acBaseFormulas(acMechs)) {
      const value = tryEvalNum(formula, character);
      if (value === null) {
        rejected5.push({ name: `\u0417\u0430\u0449\u0438\u0442\u0430 \u0431\u0435\u0437 \u0434\u043E\u0441\u043F\u0435\u0445\u043E\u0432: \u0444\u043E\u0440\u043C\u0443\u043B\u0430 \xAB${formula}\xBB \u043D\u0435 \u0440\u0430\u0441\u043F\u043E\u0437\u043D\u0430\u043D\u0430`, value: 0 });
        continue;
      }
      methods.push({
        name: "\u0417\u0430\u0449\u0438\u0442\u0430 \u0431\u0435\u0437 \u0434\u043E\u0441\u043F\u0435\u0445\u043E\u0432",
        value,
        parts: [{ value, source: "\u0417\u0430\u0449\u0438\u0442\u0430 \u0431\u0435\u0437 \u0434\u043E\u0441\u043F\u0435\u0445\u043E\u0432", reason: "\u043F\u0430\u0441\u0441\u0438\u0432\u043A\u0430" }]
      });
    }
  }
  const additive = [];
  if (shield?.bonus_value) {
    const bonus = parseFlatBonus(shield.bonus_value);
    if (bonus) additive.push({ value: bonus, source: "\u0429\u0438\u0442", reason: shield.name });
  }
  const result = pickBestMethod(methods, additive);
  if (rejected5.length) {
    result.rejected = [...result.rejected ?? [], ...rejected5];
  }
  return result;
}
function armorClassValue(character, state, passives) {
  const cards = allKnownCards(character);
  const wearingArmor = armorFromState(state, cards) !== void 0;
  const base2 = computeAC(character, state, passives);
  const fx = collectModifiers(state, passives, {
    roll: "ac",
    filter: { wearingArmor },
    formulaCtx: {
      abilityMods: character.abilityMods,
      profBonus: character.profBonus,
      selfLevel: character.level,
      classLevels: character.classLevels,
      spellcastingMod: character.spellcastingMod,
      characterSpeed: character.characterSpeed,
      variables: character.variables
    },
    evalCtx: { character, state }
  });
  const folded = foldModifiers(base2.value, fx);
  return {
    value: folded.value,
    parts: [...base2.parts, ...folded.parts],
    ...base2.selectedMethod ? { selectedMethod: base2.selectedMethod } : {},
    ...base2.rejected ? { rejected: base2.rejected } : {}
  };
}

// src/engine/resources.ts
var TURN_KEYS = ["action", "bonus_action", "reaction"];
function resourcesRestoredOnShortRest(maxResources, recharge, recovery) {
  return Object.keys(maxResources).filter((k) => {
    if (TURN_KEYS.includes(k)) return false;
    if (k.startsWith("hit_dice_")) return false;
    if (recovery && Object.prototype.hasOwnProperty.call(recovery, k)) {
      return recovery[k]?.short_rest != null;
    }
    if (!recharge) return k !== "heroic_inspiration";
    return recharge[k] === "short_rest";
  });
}
function resourceAmountRestoredOnShortRest(resourceKey, current, maximum, recovery) {
  if (!Number.isSafeInteger(current) || !Number.isSafeInteger(maximum) || current < 0 || maximum < 0 || current >= maximum) return 0;
  const missing = maximum - current;
  if (!recovery || !Object.prototype.hasOwnProperty.call(recovery, resourceKey)) return missing;
  const rule = recovery[resourceKey]?.short_rest;
  if (!rule || rule.mode !== "fixed" || !Number.isSafeInteger(rule.amount) || rule.amount <= 0) {
    return 0;
  }
  return Math.min(missing, rule.amount);
}
function resourceAmountRestoredOnLongRest(resourceKey, current, maximum, recovery) {
  if (!recovery || !Object.prototype.hasOwnProperty.call(recovery, resourceKey)) return null;
  if (!Number.isSafeInteger(current) || !Number.isSafeInteger(maximum) || current < 0 || maximum < 0 || current >= maximum) return 0;
  const rule = recovery[resourceKey]?.long_rest;
  if (!rule || rule.mode !== "full") return 0;
  return maximum - current;
}

// src/engine/turn.ts
var TURN_KEYS2 = ["action", "bonus_action", "reaction"];
var ABILITY_LABEL2 = {
  str: "\u0421\u0418\u041B",
  dex: "\u041B\u0412\u041A",
  con: "\u0422\u0415\u041B",
  int: "\u0418\u041D\u0422",
  wis: "\u041C\u0414\u0420",
  cha: "\u0425\u0410\u0420"
};
var ABILITY_KEYS3 = /* @__PURE__ */ new Set(["str", "dex", "con", "int", "wis", "cha"]);
function cloneState2(state) {
  return {
    ...state,
    hp: { ...state.hp },
    resources: { ...state.resources },
    maxResources: { ...state.maxResources },
    equipment: { ...state.equipment },
    inventory: state.inventory.map((r) => ({ ...r })),
    activeEffects: state.activeEffects.map((e) => ({ ...e }))
  };
}
function passivesFromCtx2(ctx) {
  return ctx.passives ?? [];
}
function execCtxOf(ctx) {
  return {
    character: ctx,
    rng: ctx.rng ?? (() => Math.random()),
    passives: passivesFromCtx2(ctx)
  };
}
function formulaCtxOf(ctx) {
  return {
    abilityMods: ctx.abilityMods,
    profBonus: ctx.profBonus,
    selfLevel: ctx.level,
    classLevels: ctx.classLevels,
    variables: ctx.variables
  };
}
function restoreTurnResources(state) {
  const resources = { ...state.resources };
  for (const key of TURN_KEYS2) {
    if (state.maxResources[key] != null) resources[key] = state.maxResources[key];
  }
  return { ...state, resources };
}
function advanceRoundEffects(state, expireStartBoundary = false) {
  const events = [];
  const kept = [];
  for (const e of state.activeEffects) {
    if (expireStartBoundary && e.expiry === "start_of_next_turn") {
      events.push({ type: "effect_expired", name: e.name });
      continue;
    }
    if (e.roundsLeft != null) {
      const left = e.roundsLeft - 1;
      if (left <= 0) {
        events.push({ type: "effect_expired", name: e.name });
        continue;
      }
      kept.push({ ...e, roundsLeft: left });
      continue;
    }
    kept.push(e);
  }
  return { state: { ...state, activeEffects: kept }, events };
}
function startTurn(state, ctx, options = {}) {
  let next = cloneState2(state);
  const events = [{ type: "turn_started" }];
  const pending = [];
  next = { ...next, firedThisTurn: [] };
  next = restoreTurnResources(next);
  if (options.advanceRoundDurations !== false) {
    const expired = advanceRoundEffects(next, true);
    next = expired.state;
    events.push(...expired.events);
  }
  if (ctx) next = emitEvent({ kind: "turn_start", source: "self" }, next, execCtxOf(ctx), events, pending);
  return { state: next, events, ...pending.length ? { pendingReactions: pending } : {} };
}
function saveEndsError(code, path, message, cause) {
  return new MechanicsExecutionError(
    code,
    path,
    message,
    cause === void 0 ? void 0 : { cause }
  );
}
function preflightSaveEnds(state, ctx) {
  return state.activeEffects.map((effect, index) => {
    const mechanics = effect.mechanics;
    if (!mechanics || typeof mechanics !== "object" || Array.isArray(mechanics) || !Object.prototype.hasOwnProperty.call(mechanics, "save_ends")) {
      return null;
    }
    const path = `runtime.activeEffects[${index}].mechanics.save_ends`;
    const declaration = mechanics.save_ends;
    if (!declaration || typeof declaration !== "object" || Array.isArray(declaration)) {
      throw saveEndsError("INVALID_PAYLOAD", path, "save_ends must be an object");
    }
    const se = declaration;
    if (typeof se.ability !== "string" || !ABILITY_KEYS3.has(se.ability)) {
      throw saveEndsError(
        "INVALID_PAYLOAD",
        `${path}.ability`,
        "save_ends requires an explicit supported ability"
      );
    }
    if (se.dc === void 0) {
      throw saveEndsError(
        "INVALID_PAYLOAD",
        `${path}.dc`,
        "save_ends requires an explicit DC formula"
      );
    }
    let dc;
    try {
      const value = evaluate(se.dc, {
        ...formulaCtxOf(ctx),
        rng: () => {
          throw new FormulaError("save_ends DC cannot contain a random die");
        }
      });
      if (typeof value !== "number" || !Number.isFinite(value) || value <= 0) {
        throw new FormulaError("save_ends DC must resolve to a positive finite number");
      }
      dc = value;
    } catch (error) {
      throw saveEndsError(
        "INVALID_FORMULA",
        `${path}.dc`,
        error instanceof Error ? error.message : String(error),
        error
      );
    }
    const ability = se.ability;
    const abilityModifier = ctx.abilityMods[ability];
    if (typeof abilityModifier !== "number" || !Number.isFinite(abilityModifier)) {
      throw saveEndsError(
        "INVALID_MECHANICS",
        `context.character.abilityMods.${ability}`,
        "save_ends requires an explicit finite owner ability modifier"
      );
    }
    const proficient = (ctx.saveProficiencies ?? []).includes(ability);
    if (proficient && (typeof ctx.profBonus !== "number" || !Number.isFinite(ctx.profBonus))) {
      throw saveEndsError(
        "INVALID_MECHANICS",
        "context.character.profBonus",
        "save_ends proficiency requires an explicit finite proficiency bonus"
      );
    }
    return {
      ability,
      dc,
      modifier: abilityModifier + (proficient ? ctx.profBonus : 0)
    };
  });
}
function rollSaveEnds(state, ctx, declaration, name, events, rng, avoidedCondition) {
  const { ability, dc, modifier } = declaration;
  const collected = collectModifiers(state, passivesFromCtx2(ctx), {
    roll: "saving_throw",
    filter: { ability },
    formulaCtx: formulaCtxOf(ctx),
    evalCtx: {
      state,
      activeConditions: activeConditionsOf(state),
      savedConditions: new Set(avoidedCondition ? [avoidedCondition] : [])
    }
  });
  const roll = rollD20({
    advantage: collected.advantage,
    modifiers: [{ value: modifier, source: ABILITY_LABEL2[ability] }, ...collected.modifiers],
    target: { type: "dc", value: dc },
    rng
  });
  events.push(rollEvent(`\u0421\u043F\u0430\u0441\u0431\u0440\u043E\u0441\u043E\u043A \u0432 \u043A\u043E\u043D\u0446\u0435 \u0445\u043E\u0434\u0430 \u2014 ${name} (\u0421\u041B ${dc})`, { ...roll, kind: "save" }));
  return roll.outcome === "success";
}
function endTurn(state, ctx, options = {}) {
  const saveEndsDeclarations = preflightSaveEnds(state, ctx);
  let next = cloneState2(state);
  const events = [turnEndedEvent()];
  const pending = [];
  const rng = ctx.rng ?? (() => Math.random());
  const kept = [];
  for (const [index, e] of next.activeEffects.entries()) {
    if (e.expiry === "end_of_turn") {
      events.push({ type: "effect_expired", name: e.name });
      continue;
    }
    const mechanics = e.mechanics;
    const se = mechanics?.save_ends;
    const declaration = saveEndsDeclarations[index];
    if (se && declaration && String(se.timing ?? "end_of_turn") === "end_of_turn") {
      if (rollSaveEnds(
        next,
        ctx,
        declaration,
        e.name,
        events,
        rng,
        String(e.mechanics.value ?? "")
      )) {
        events.push({ type: "effect_expired", name: e.name });
        continue;
      }
    }
    kept.push(e);
  }
  next = { ...next, activeEffects: kept };
  if (options.advanceRoundDurations !== false) {
    const advanced = advanceRoundEffects(next);
    next = advanced.state;
    events.push(...advanced.events);
  }
  next = emitEvent({ kind: "turn_end", source: "self" }, next, execCtxOf(ctx), events, pending);
  return { state: next, events, ...pending.length ? { pendingReactions: pending } : {} };
}
function shortRest(state, ctx) {
  let next = cloneState2(state);
  const events = [{ type: "short_rest" }];
  const pending = [];
  const recharge = ctx.resourceRecharge;
  const recovery = ctx.resourceRecovery;
  for (const key of resourcesRestoredOnShortRest(next.maxResources, recharge, recovery)) {
    const max = next.maxResources[key] ?? 0;
    const before = next.resources[key] ?? 0;
    const amount = resourceAmountRestoredOnShortRest(key, before, max, recovery);
    if (amount > 0) {
      const current = before + amount;
      next.resources[key] = current;
      events.push({ type: "resource_restored", resource: key, amount, current });
    }
  }
  const kept = next.activeEffects.filter((e) => e.roundsLeft == null || e.roundsLeft > 600);
  for (const e of next.activeEffects) {
    if (e.roundsLeft != null && e.roundsLeft <= 600) events.push({ type: "effect_expired", name: e.name });
  }
  next.activeEffects = kept.map((e) => e.roundsLeft != null ? { ...e, roundsLeft: e.roundsLeft - 600 } : e);
  next = emitEvent({ kind: "short_rest", source: "self" }, next, execCtxOf(ctx), events, pending);
  return { state: next, events, ...pending.length ? { pendingReactions: pending } : {} };
}
function longRest(state, ctx) {
  let next = cloneState2(state);
  const events = [{ type: "long_rest" }];
  const pending = [];
  next.hp.current = next.hp.max;
  next.hp.temp = 0;
  const afterElapsedTime = [];
  for (const effect of next.activeEffects) {
    if (effect.expiry === "long_rest" || effect.expiry === "until_rest") {
      events.push({ type: "effect_expired", name: effect.name });
      continue;
    }
    if (effect.roundsLeft != null) {
      if (effect.roundsLeft <= 4800) {
        events.push({ type: "effect_expired", name: effect.name });
        continue;
      }
      afterElapsedTime.push({ ...effect, roundsLeft: effect.roundsLeft - 4800 });
      continue;
    }
    afterElapsedTime.push(effect);
  }
  const conditionRest = conditionLongRestEntries(afterElapsedTime);
  for (const removed of conditionRest.removed) {
    events.push({ type: "effect_expired", name: removed.name });
  }
  next.activeEffects = conditionRest.retained;
  next.firedThisRest = [];
  next = emitEvent({ kind: "long_rest", source: "self" }, next, execCtxOf(ctx), events, pending);
  for (const key of Object.keys(next.maxResources)) {
    if (ctx.resourceRecharge?.[key] === "never") continue;
    const max = next.maxResources[key] ?? 0;
    const before = next.resources[key] ?? 0;
    const declared = resourceAmountRestoredOnLongRest(
      key,
      before,
      max,
      ctx.resourceRecovery
    );
    if (declared === 0) continue;
    next.resources[key] = declared == null ? max : before + declared;
  }
  return { state: next, events, ...pending.length ? { pendingReactions: pending } : {} };
}

// src/rules-core/worldSpellPolicies.ts
var MANAGED_TYPES = /* @__PURE__ */ new Set([
  "light_world_object",
  "burning_hands_objects",
  "detect_magic_world_sensing",
  "minor_illusion_world_object",
  "dancing_lights_world",
  "druidcraft_world",
  "mending_world",
  "detect_poison_disease_world",
  "purify_food_drink_world",
  "prestidigitation_world",
  "magic_missile"
]);
function record2(value, label) {
  if (!value || typeof value !== "object" || Array.isArray(value)) {
    throw new Error(`${label} must be an object`);
  }
  return value;
}
function exactKeys2(value, expected, label) {
  const actual = Object.keys(value).sort();
  const wanted = [...expected].sort();
  if (JSON.stringify(actual) !== JSON.stringify(wanted)) {
    throw new Error(`${label} must declare exactly: ${wanted.join(", ")}`);
  }
}
function number(value, label, options = {}) {
  if (typeof value !== "number" || !Number.isFinite(value) || value < 0 || options.positive && value <= 0 || options.integer && !Number.isInteger(value)) {
    const qualifier = `${options.positive ? "positive " : "non-negative "}${options.integer ? "integer" : "number"}`;
    throw new Error(`${label} must be a finite ${qualifier}`);
  }
  return value;
}
function boolean(value, label) {
  if (typeof value !== "boolean") throw new Error(`${label} must be boolean`);
  return value;
}
function nonEmptyString(value, label) {
  if (typeof value !== "string" || !value.trim() || value !== value.trim()) {
    throw new Error(`${label} must be a stable non-empty string`);
  }
  return value;
}
function parseBlockers(value, label) {
  const blockers = record2(value, label);
  const materials = ["stone", "common_metal", "lead", "wood", "dirt", "other"];
  exactKeys2(blockers, materials, label);
  return Object.fromEntries(materials.map((material) => {
    const raw = blockers[material];
    if (raw === null) return [material, null];
    const threshold = record2(raw, `${label}.${material}`);
    exactKeys2(threshold, ["threshold_inches", "comparison"], `${label}.${material}`);
    if (threshold.comparison !== "gte" && threshold.comparison !== "gt") {
      throw new Error(`${label}.${material}.comparison must be gte or gt`);
    }
    return [material, {
      thresholdInches: number(
        threshold.threshold_inches,
        `${label}.${material}.threshold_inches`
      ),
      comparison: threshold.comparison
    }];
  }));
}
function parseTargeting(mechanics, primitiveType) {
  const raw = record2(mechanics.targeting, `${primitiveType}.targeting`);
  for (const key of [
    "domain",
    "actor_targets",
    "range_ft",
    "allowed_relations",
    "requires_line_of_sight",
    "shape"
  ]) {
    if (raw[key] === void 0) throw new Error(`${primitiveType}.targeting.${key} is required`);
  }
  const compiled = compileMechanicsTargeting(mechanics);
  const domain = raw.domain;
  const allowedRelations = compiled.allowedRelations;
  const requiresTouch = raw.requires_touch === void 0 ? false : boolean(raw.requires_touch, `${primitiveType}.targeting.requires_touch`);
  let area;
  if (raw.area !== void 0) {
    const rawArea = record2(raw.area, `${primitiveType}.targeting.area`);
    const kind = rawArea.kind;
    area = {
      kind,
      ...rawArea.size_ft === void 0 ? {} : { sizeFt: number(rawArea.size_ft, `${primitiveType}.targeting.area.size_ft`, { positive: true }) },
      ...rawArea.radius_ft === void 0 ? {} : { radiusFt: number(rawArea.radius_ft, `${primitiveType}.targeting.area.radius_ft`, { positive: true }) }
    };
  }
  return {
    domain,
    actorTargets: raw.actor_targets,
    shape: raw.shape,
    rangeFt: compiled.rangeFt,
    requiresLineOfSight: compiled.requiresLineOfSight,
    requiresTouch,
    allowedRelations,
    ...area ? { area } : {}
  };
}
function requireTargeting(input) {
  if (input.targeting.domain !== input.domain || input.targeting.actorTargets !== input.actorTargets) {
    throw new Error(`${input.primitiveType} targeting domain is inconsistent`);
  }
  const selfTarget = input.targeting.shape === "self";
  const relationPolicyIsValid = selfTarget ? input.targeting.allowedRelations.length === 1 && input.targeting.allowedRelations[0] === "self" : input.actorTargets === input.targeting.allowedRelations.length > 0;
  if (!relationPolicyIsValid) {
    throw new Error(`${input.primitiveType} targeting relations contradict actor_targets`);
  }
  if (input.targeting.shape !== input.shape) {
    throw new Error(`${input.primitiveType} targeting shape is inconsistent`);
  }
  if (input.requiresTouch !== void 0 && input.targeting.requiresTouch !== input.requiresTouch) {
    throw new Error(`${input.primitiveType} targeting touch policy is inconsistent`);
  }
  if (input.area) {
    if (input.targeting.area?.kind !== input.area.kind || input.targeting.area[input.area.metric] === void 0) {
      throw new Error(`${input.primitiveType} targeting area is inconsistent`);
    }
  }
}
var PREFLIGHT_CHARACTER = {
  abilityMods: { str: 0, dex: 0, con: 0, int: 0, wis: 0, cha: 0 },
  profBonus: 2,
  level: 1
};
var PREFLIGHT_STATE = {
  hp: { current: 1, max: 1, temp: 0 },
  resources: {},
  maxResources: {},
  equipment: {},
  inventory: [],
  activeEffects: []
};
function parsePerDartEffect(value) {
  const effect = record2(value, "magic_missile.policy.per_dart_effect");
  if (effect.resolution !== "auto" || effect.who !== "target" || !Array.isArray(effect.result)) {
    throw new Error("magic_missile per_dart_effect must be an auto target interaction");
  }
  if (!effect.result.some((payload) => payload && typeof payload === "object" && !Array.isArray(payload) && payload.kind === "damage" && typeof payload.type === "string" && Boolean(String(payload.type).trim()))) {
    throw new Error("magic_missile per_dart_effect must declare damage type and formula");
  }
  try {
    preflightMechanicsExecution(PREFLIGHT_STATE, { effects: [effect] }, {
      character: PREFLIGHT_CHARACTER,
      rng: () => 0.5
    });
  } catch (error) {
    throw new Error(`magic_missile per_dart_effect is invalid: ${error instanceof Error ? error.message : String(error)}`);
  }
  return JSON.parse(JSON.stringify(effect));
}
function parseWorldSpellPolicy(mechanics) {
  const primitiveValue = mechanics.primitive;
  if (!primitiveValue || typeof primitiveValue !== "object" || Array.isArray(primitiveValue)) {
    return { status: "not_applicable" };
  }
  const primitive = primitiveValue;
  if (!MANAGED_TYPES.has(primitive.type)) {
    return { status: "not_applicable" };
  }
  const primitiveType = primitive.type;
  try {
    exactKeys2(primitive, ["type", "policy"], `${primitiveType}.primitive`);
    const raw = record2(primitive.policy, `${primitiveType}.policy`);
    const targeting = parseTargeting(mechanics, primitiveType);
    let policy;
    switch (primitiveType) {
      case "light_world_object": {
        exactKeys2(raw, [
          "max_object_size",
          "exclude_carried_by_other",
          "bright_radius_ft",
          "dim_additional_radius_ft",
          "duration_rounds",
          "max_active_per_source"
        ], `${primitiveType}.policy`);
        const maxObjectSize = raw.max_object_size;
        if (!["tiny", "small", "medium", "large", "huge", "gargantuan"].includes(String(maxObjectSize))) {
          throw new Error(`${primitiveType}.policy.max_object_size is invalid`);
        }
        policy = {
          maxObjectSize,
          excludeCarriedByOther: boolean(raw.exclude_carried_by_other, `${primitiveType}.policy.exclude_carried_by_other`),
          brightRadiusFt: number(raw.bright_radius_ft, `${primitiveType}.policy.bright_radius_ft`, { positive: true }),
          dimAdditionalRadiusFt: number(raw.dim_additional_radius_ft, `${primitiveType}.policy.dim_additional_radius_ft`, { positive: true }),
          durationRounds: number(raw.duration_rounds, `${primitiveType}.policy.duration_rounds`, { positive: true, integer: true }),
          maxActivePerSource: number(raw.max_active_per_source, `${primitiveType}.policy.max_active_per_source`, { positive: true, integer: true })
        };
        requireTargeting({ primitiveType, targeting, domain: "world", shape: "single", actorTargets: false, requiresTouch: true });
        break;
      }
      case "burning_hands_objects": {
        exactKeys2(raw, ["require_in_area", "require_flammable", "exclude_carried"], `${primitiveType}.policy`);
        policy = {
          requireInArea: boolean(raw.require_in_area, `${primitiveType}.policy.require_in_area`),
          requireFlammable: boolean(raw.require_flammable, `${primitiveType}.policy.require_flammable`),
          excludeCarried: boolean(raw.exclude_carried, `${primitiveType}.policy.exclude_carried`)
        };
        requireTargeting({ primitiveType, targeting, domain: "mixed", shape: "area", actorTargets: true, area: { kind: "cone", metric: "sizeFt" } });
        break;
      }
      case "detect_magic_world_sensing": {
        exactKeys2(raw, ["blockers", "aura_requires_line_of_sight", "reveal_spell_school_only"], `${primitiveType}.policy`);
        policy = {
          blockers: parseBlockers(raw.blockers, `${primitiveType}.policy.blockers`),
          auraRequiresLineOfSight: boolean(raw.aura_requires_line_of_sight, `${primitiveType}.policy.aura_requires_line_of_sight`),
          revealSpellSchoolOnly: boolean(raw.reveal_spell_school_only, `${primitiveType}.policy.reveal_spell_school_only`)
        };
        requireTargeting({ primitiveType, targeting, domain: "actor", shape: "self", actorTargets: false, area: { kind: "emanation", metric: "radiusFt" } });
        break;
      }
      case "minor_illusion_world_object": {
        exactKeys2(raw, ["image_max_cube_side_ft", "duration_rounds", "max_active_per_source", "study_ability", "study_skill"], `${primitiveType}.policy`);
        if (raw.study_ability !== "int" || raw.study_skill !== "investigation") {
          throw new Error(`${primitiveType} study policy is invalid`);
        }
        policy = {
          imageMaxCubeSideFt: number(raw.image_max_cube_side_ft, `${primitiveType}.policy.image_max_cube_side_ft`, { positive: true }),
          durationRounds: number(raw.duration_rounds, `${primitiveType}.policy.duration_rounds`, { positive: true, integer: true }),
          maxActivePerSource: number(raw.max_active_per_source, `${primitiveType}.policy.max_active_per_source`, { positive: true, integer: true }),
          studyAbility: "int",
          studySkill: "investigation"
        };
        requireTargeting({ primitiveType, targeting, domain: "world", shape: "single", actorTargets: false });
        break;
      }
      case "dancing_lights_world": {
        exactKeys2(raw, [
          "min_individual_lights",
          "max_individual_lights",
          "combined_form_object_count",
          "required_separation_ft",
          "max_move_ft",
          "dim_radius_ft",
          "duration_rounds"
        ], `${primitiveType}.policy`);
        const minIndividualLights = number(raw.min_individual_lights, `${primitiveType}.policy.min_individual_lights`, { positive: true, integer: true });
        const maxIndividualLights = number(raw.max_individual_lights, `${primitiveType}.policy.max_individual_lights`, { positive: true, integer: true });
        if (minIndividualLights > maxIndividualLights) throw new Error(`${primitiveType} light counts are inconsistent`);
        policy = {
          minIndividualLights,
          maxIndividualLights,
          combinedFormObjectCount: number(raw.combined_form_object_count, `${primitiveType}.policy.combined_form_object_count`, { positive: true, integer: true }),
          requiredSeparationFt: number(raw.required_separation_ft, `${primitiveType}.policy.required_separation_ft`, { positive: true }),
          maxMoveFt: number(raw.max_move_ft, `${primitiveType}.policy.max_move_ft`, { positive: true }),
          dimRadiusFt: number(raw.dim_radius_ft, `${primitiveType}.policy.dim_radius_ft`, { positive: true }),
          durationRounds: number(raw.duration_rounds, `${primitiveType}.policy.duration_rounds`, { positive: true, integer: true })
        };
        requireTargeting({ primitiveType, targeting, domain: "world", shape: "multiple", actorTargets: false });
        break;
      }
      case "druidcraft_world": {
        exactKeys2(raw, ["sensory_cube_side_ft", "weather_duration_rounds"], `${primitiveType}.policy`);
        policy = {
          sensoryCubeSideFt: number(raw.sensory_cube_side_ft, `${primitiveType}.policy.sensory_cube_side_ft`, { positive: true }),
          weatherDurationRounds: number(raw.weather_duration_rounds, `${primitiveType}.policy.weather_duration_rounds`, { positive: true, integer: true })
        };
        requireTargeting({ primitiveType, targeting, domain: "world", shape: "single", actorTargets: false });
        break;
      }
      case "mending_world": {
        exactKeys2(raw, ["max_break_dimension_ft"], `${primitiveType}.policy`);
        policy = { maxBreakDimensionFt: number(raw.max_break_dimension_ft, `${primitiveType}.policy.max_break_dimension_ft`, { positive: true }) };
        requireTargeting({ primitiveType, targeting, domain: "world", shape: "single", actorTargets: false, requiresTouch: true });
        break;
      }
      case "detect_poison_disease_world": {
        exactKeys2(raw, ["blockers"], `${primitiveType}.policy`);
        policy = {
          blockers: parseBlockers(raw.blockers, `${primitiveType}.policy.blockers`)
        };
        requireTargeting({ primitiveType, targeting, domain: "actor", shape: "self", actorTargets: false, area: { kind: "emanation", metric: "radiusFt" } });
        break;
      }
      case "purify_food_drink_world": {
        exactKeys2(raw, ["require_in_area", "exclude_magical"], `${primitiveType}.policy`);
        policy = {
          requireInArea: boolean(raw.require_in_area, `${primitiveType}.policy.require_in_area`),
          excludeMagical: boolean(raw.exclude_magical, `${primitiveType}.policy.exclude_magical`)
        };
        requireTargeting({ primitiveType, targeting, domain: "world", shape: "area", actorTargets: false, area: { kind: "sphere", metric: "radiusFt" } });
        break;
      }
      case "prestidigitation_world": {
        exactKeys2(raw, ["max_volume_cubic_ft", "max_active_effects", "attachment_duration_rounds", "creation_source_turn_endings"], `${primitiveType}.policy`);
        policy = {
          maxVolumeCubicFt: number(raw.max_volume_cubic_ft, `${primitiveType}.policy.max_volume_cubic_ft`, { positive: true }),
          maxActiveEffects: number(raw.max_active_effects, `${primitiveType}.policy.max_active_effects`, { positive: true, integer: true }),
          attachmentDurationRounds: number(raw.attachment_duration_rounds, `${primitiveType}.policy.attachment_duration_rounds`, { positive: true, integer: true }),
          creationSourceTurnEndings: number(raw.creation_source_turn_endings, `${primitiveType}.policy.creation_source_turn_endings`, { positive: true, integer: true })
        };
        requireTargeting({ primitiveType, targeting, domain: "world", shape: "single", actorTargets: false });
        break;
      }
      case "magic_missile": {
        exactKeys2(raw, [
          "base_slot_level",
          "max_slot_level",
          "base_dart_count",
          "darts_per_slot_above",
          "allocation_choice_id",
          "simultaneous",
          "per_dart_effect"
        ], `${primitiveType}.policy`);
        const baseSlotLevel = number(raw.base_slot_level, `${primitiveType}.policy.base_slot_level`, { positive: true, integer: true });
        const maxSlotLevel = number(raw.max_slot_level, `${primitiveType}.policy.max_slot_level`, { positive: true, integer: true });
        if (baseSlotLevel > maxSlotLevel || maxSlotLevel > 9) throw new Error(`${primitiveType} slot-level policy is inconsistent`);
        policy = {
          baseSlotLevel,
          maxSlotLevel,
          baseDartCount: number(raw.base_dart_count, `${primitiveType}.policy.base_dart_count`, { positive: true, integer: true }),
          dartsPerSlotAbove: number(raw.darts_per_slot_above, `${primitiveType}.policy.darts_per_slot_above`, { positive: true, integer: true }),
          allocationChoiceId: nonEmptyString(raw.allocation_choice_id, `${primitiveType}.policy.allocation_choice_id`),
          simultaneous: boolean(raw.simultaneous, `${primitiveType}.policy.simultaneous`),
          perDartEffect: parsePerDartEffect(raw.per_dart_effect)
        };
        requireTargeting({ primitiveType, targeting, domain: "actor", shape: "multiple", actorTargets: true });
        const maximumDarts = policy.baseDartCount + (policy.maxSlotLevel - policy.baseSlotLevel) * policy.dartsPerSlotAbove;
        const maxTargets = Number(mechanics.targeting.max_targets);
        if (!Number.isInteger(maxTargets) || maxTargets !== maximumDarts) {
          throw new Error(`${primitiveType}.targeting.max_targets must equal maximum dart count ${maximumDarts}`);
        }
        break;
      }
    }
    return { status: "valid", primitiveType, targeting, policy };
  } catch (error) {
    return {
      status: "invalid",
      issue: error instanceof Error ? error.message : String(error)
    };
  }
}
function magicMissileDartCount(policy, castLevel) {
  if (!Number.isInteger(castLevel) || castLevel < policy.baseSlotLevel || castLevel > policy.maxSlotLevel) return null;
  return policy.baseDartCount + (castLevel - policy.baseSlotLevel) * policy.dartsPerSlotAbove;
}

// src/rules-core/weaponActionPolicies.ts
var WEAPON_ATTACK_PRIMITIVE = "weapon_attack";
var LIGHT_WEAPON_EXTRA_ATTACK_PRIMITIVE = "light_weapon_extra_attack";
function object2(value) {
  return value && typeof value === "object" && !Array.isArray(value) ? value : null;
}
function exactKeys3(value, expected) {
  const actual = Object.keys(value).sort();
  const normalized3 = [...expected].sort();
  return actual.length === normalized3.length && actual.every((key, index) => key === normalized3[index]);
}
function weaponEffectIssue(action, hand) {
  const effects = Array.isArray(action.mechanics.effects) ? action.mechanics.effects : [];
  if (effects.length !== 1) return `${action.id} must declare exactly one weapon attack effect`;
  const effect = object2(effects[0]);
  if (!effect || effect.resolution !== "attack_roll" || effect.vs !== "ac" || effect.ability !== "auto" || !["weapon_melee", "weapon_ranged"].includes(String(effect.attack_kind ?? ""))) {
    return `${action.id} has an invalid weapon attack-roll declaration`;
  }
  const tags = Array.isArray(effect.tags) ? effect.tags.map(String) : [];
  const onHit = Array.isArray(effect.on_hit) ? effect.on_hit : [];
  if (onHit.length !== 1) return `${action.id} must declare exactly one weapon damage payload`;
  const damage = object2(onHit[0]);
  if (!damage || damage.kind !== "damage" || damage.dice !== "weapon" || damage.type !== "weapon" || damage.ability !== (hand === "main" ? "auto" : "none")) {
    return `${action.id} has an invalid weapon damage declaration`;
  }
  if (hand === "main" && tags.includes("off_hand")) {
    return `${action.id} main-hand primitive declares off_hand`;
  }
  if (hand === "off" && (!tags.includes("off_hand") || !tags.includes("two_weapon"))) {
    return `${action.id} Light extra attack must declare off_hand and two_weapon`;
  }
  return null;
}
function targetingIssue(action) {
  const targeting = action.targeting;
  if (!targeting || targeting.minTargets !== 1 || targeting.maxTargets !== 1 || !Number.isFinite(targeting.rangeFt) || targeting.rangeFt <= 0 || typeof targeting.requiresLineOfSight !== "boolean" || !targeting.allowedRelations.length) {
    return `${action.id} has an invalid single-target weapon contract`;
  }
  return null;
}
function activationCostIssue(input) {
  const activation = object2(input.action.mechanics.activation);
  if (!activation || activation.mode !== "active" || !Array.isArray(activation.cost)) {
    return { issue: `${input.action.id} requires an active activation cost` };
  }
  const cost = activation.cost.map(object2);
  if (cost.some((entry) => !entry)) {
    return { issue: `${input.action.id} activation cost contains a malformed entry` };
  }
  const entries = cost;
  if (entries.some((entry) => typeof entry.resource !== "string" || !entry.resource.trim())) {
    return { issue: `${input.action.id} activation cost requires stable resource keys` };
  }
  const timing = entries.filter((entry) => entry.resource === input.timingResource);
  if (timing.length !== 1 || timing[0].amount !== void 0 && timing[0].amount !== 1 || entries.some((entry) => entry.resource === (input.timingResource === "action" ? "bonus_action" : "action"))) {
    return { issue: `${input.action.id} must declare exactly one ${input.timingResource} cost` };
  }
  const contextual = entries.filter((entry) => entry.resource === "equipped_weapon_ammo");
  if (contextual.length > 1) {
    return { issue: `${input.action.id} declares ambiguous equipped_weapon_ammo costs` };
  }
  if (input.phase === "template") {
    if (contextual.length !== 1) {
      return { issue: `${input.action.id} template requires exactly one equipped_weapon_ammo marker` };
    }
    if (!Number.isSafeInteger(contextual[0].amount) || Number(contextual[0].amount) <= 0 || contextual[0].card_id !== void 0) {
      return { issue: `${input.action.id} equipped_weapon_ammo marker is invalid` };
    }
  } else {
    if (contextual.length) {
      return { issue: `${input.action.id} bound cost still contains equipped_weapon_ammo` };
    }
  }
  return { cost: entries.map((entry) => ({ ...entry })) };
}
function parseDeclaredWeaponActionPolicy(action, phase) {
  if (action.kind !== "nonSpell") {
    return { status: "invalid", issue: `${action.id} weapon primitive must be non-spell` };
  }
  const primitive = object2(action.mechanics.primitive);
  if (!primitive || !exactKeys3(primitive, ["type"])) {
    return { status: "invalid", issue: `${action.id} has an invalid weapon primitive declaration` };
  }
  const type = primitive.type;
  const policy = type === WEAPON_ATTACK_PRIMITIVE ? {
    primitive: WEAPON_ATTACK_PRIMITIVE,
    hand: "main",
    timingResource: "action"
  } : type === LIGHT_WEAPON_EXTRA_ATTACK_PRIMITIVE ? {
    primitive: LIGHT_WEAPON_EXTRA_ATTACK_PRIMITIVE,
    hand: "off",
    timingResource: "bonus_action"
  } : null;
  if (!policy) {
    return { status: "invalid", issue: `${action.id} is not a declared weapon action primitive` };
  }
  const effectIssue = weaponEffectIssue(action, policy.hand);
  if (effectIssue) return { status: "invalid", issue: effectIssue };
  const targetIssue = targetingIssue(action);
  if (targetIssue) return { status: "invalid", issue: targetIssue };
  const activation = activationCostIssue({
    action,
    phase,
    timingResource: policy.timingResource
  });
  if ("issue" in activation) return { status: "invalid", issue: activation.issue };
  return {
    status: "valid",
    policy: {
      primitive: policy.primitive,
      hand: policy.hand,
      timingResource: policy.timingResource,
      activationCost: activation.cost
    }
  };
}

// src/character/sheetCombatCertification.generated.json
var sheetCombatCertification_generated_default = {
  schemaVersion: 1,
  artifactVersion: "1.0.0",
  source: {
    ruleset: {
      systemId: "dnd5e-2024",
      releaseId: "prod-snapshot@2026-08-06.micro-mvp-l1.overlay.1.10.0",
      contentHash: "sha256:4ee64d32fffe6b88e797a10ae89207d5f88c0f2214cc16df043d6b9464e9f056",
      errataVersion: "phb-2024-errata-v1"
    },
    release: {
      id: "prod-snapshot@2026-08-06.micro-mvp-l1.overlay.1.10.0",
      systemId: "dnd5e-2024",
      rulesetVersion: "2024",
      errataVersion: "phb-2024-errata-v1",
      sourceReleaseId: "prod-snapshot@2026-08-06.micro-mvp-l1.v2",
      sourceContentHash: "sha256:95556fca97630dd76af1cdc5e991635f42eca5379d85432c23075c0cb0bfb17d",
      overlayHash: "sha256:bb83dc32c63f72c7c29cb81128aa309e580f36eeb17bc6f72db6bd437836ffbc",
      contentHash: "sha256:4ee64d32fffe6b88e797a10ae89207d5f88c0f2214cc16df043d6b9464e9f056",
      releaseHash: "sha256:04678a044c4dc809d213e01e392bc0f16562d5103ee96e070089c1edf7e7100b"
    }
  },
  summary: {
    rootCount: 448,
    combatRootCount: 448,
    actionOccurrenceCount: 1440,
    uniqueActionCount: 11
  },
  coverage: [
    {
      stableKey: "class.cleric|species.dragonborn|background.acolyte|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.dragonborn|background.acolyte|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.dragonborn|background.acolyte|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.dragonborn|background.acolyte|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.dragonborn|background.criminal|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.dragonborn|background.criminal|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.dragonborn|background.criminal|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.dragonborn|background.criminal|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.dragonborn|background.sage|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.dragonborn|background.sage|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.dragonborn|background.sage|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.dragonborn|background.sage|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.dragonborn|background.soldier|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.dragonborn|background.soldier|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.dragonborn|background.soldier|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.dragonborn|background.soldier|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.dwarf|background.acolyte|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.dwarf|background.acolyte|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.dwarf|background.acolyte|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.dwarf|background.acolyte|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.dwarf|background.criminal|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.dwarf|background.criminal|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.dwarf|background.criminal|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.dwarf|background.criminal|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.dwarf|background.sage|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.dwarf|background.sage|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.dwarf|background.sage|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.dwarf|background.sage|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.dwarf|background.soldier|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.dwarf|background.soldier|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.dwarf|background.soldier|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.dwarf|background.soldier|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.elf|background.acolyte|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.elf|background.acolyte|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.elf|background.acolyte|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.elf|background.acolyte|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.elf|background.criminal|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.elf|background.criminal|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.elf|background.criminal|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.elf|background.criminal|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.elf|background.sage|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.elf|background.sage|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.elf|background.sage|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.elf|background.sage|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.elf|background.soldier|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.elf|background.soldier|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.elf|background.soldier|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.elf|background.soldier|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.human|background.acolyte|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.human|background.acolyte|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.human|background.acolyte|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.human|background.acolyte|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.human|background.criminal|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.human|background.criminal|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.human|background.criminal|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.human|background.criminal|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.human|background.sage|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.human|background.sage|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.human|background.sage|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.human|background.sage|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.human|background.soldier|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.human|background.soldier|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.human|background.soldier|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.cleric|species.human|background.soldier|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.dragonborn|background.acolyte|feat.alert",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.dragonborn|background.acolyte|feat.magic-initiate",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.dragonborn|background.acolyte|feat.skilled",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.dragonborn|background.acolyte|feat.tough",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.dragonborn|background.criminal|feat.alert",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.dragonborn|background.criminal|feat.magic-initiate",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.dragonborn|background.criminal|feat.skilled",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.dragonborn|background.criminal|feat.tough",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.dragonborn|background.sage|feat.alert",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.dragonborn|background.sage|feat.magic-initiate",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.dragonborn|background.sage|feat.skilled",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.dragonborn|background.sage|feat.tough",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.dragonborn|background.soldier|feat.alert",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.dragonborn|background.soldier|feat.magic-initiate",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.dragonborn|background.soldier|feat.skilled",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.dragonborn|background.soldier|feat.tough",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.dwarf|background.acolyte|feat.alert",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.dwarf|background.acolyte|feat.magic-initiate",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.dwarf|background.acolyte|feat.skilled",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.dwarf|background.acolyte|feat.tough",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.dwarf|background.criminal|feat.alert",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.dwarf|background.criminal|feat.magic-initiate",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.dwarf|background.criminal|feat.skilled",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.dwarf|background.criminal|feat.tough",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.dwarf|background.sage|feat.alert",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.dwarf|background.sage|feat.magic-initiate",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.dwarf|background.sage|feat.skilled",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.dwarf|background.sage|feat.tough",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.dwarf|background.soldier|feat.alert",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.dwarf|background.soldier|feat.magic-initiate",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.dwarf|background.soldier|feat.skilled",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.dwarf|background.soldier|feat.tough",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.elf|background.acolyte|feat.alert",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.elf|background.acolyte|feat.magic-initiate",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.elf|background.acolyte|feat.skilled",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.elf|background.acolyte|feat.tough",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.elf|background.criminal|feat.alert",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.elf|background.criminal|feat.magic-initiate",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.elf|background.criminal|feat.skilled",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.elf|background.criminal|feat.tough",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.elf|background.sage|feat.alert",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.elf|background.sage|feat.magic-initiate",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.elf|background.sage|feat.skilled",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.elf|background.sage|feat.tough",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.elf|background.soldier|feat.alert",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.elf|background.soldier|feat.magic-initiate",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.elf|background.soldier|feat.skilled",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.elf|background.soldier|feat.tough",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.human|background.acolyte|feat.alert",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.human|background.acolyte|feat.magic-initiate",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.human|background.acolyte|feat.skilled",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.human|background.acolyte|feat.tough",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.human|background.criminal|feat.alert",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.human|background.criminal|feat.magic-initiate",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.human|background.criminal|feat.skilled",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.human|background.criminal|feat.tough",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.human|background.sage|feat.alert",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.human|background.sage|feat.magic-initiate",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.human|background.sage|feat.skilled",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.human|background.sage|feat.tough",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.human|background.soldier|feat.alert",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.human|background.soldier|feat.magic-initiate",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.human|background.soldier|feat.skilled",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.druid|species.human|background.soldier|feat.tough",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.dragonborn|background.acolyte|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.dragonborn|background.acolyte|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.dragonborn|background.acolyte|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.dragonborn|background.acolyte|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.dragonborn|background.criminal|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.dragonborn|background.criminal|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.dragonborn|background.criminal|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.dragonborn|background.criminal|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.dragonborn|background.sage|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.dragonborn|background.sage|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.dragonborn|background.sage|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.dragonborn|background.sage|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.dragonborn|background.soldier|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.dragonborn|background.soldier|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.dragonborn|background.soldier|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.dragonborn|background.soldier|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.dwarf|background.acolyte|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.dwarf|background.acolyte|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.dwarf|background.acolyte|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.dwarf|background.acolyte|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.dwarf|background.criminal|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.dwarf|background.criminal|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.dwarf|background.criminal|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.dwarf|background.criminal|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.dwarf|background.sage|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.dwarf|background.sage|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.dwarf|background.sage|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.dwarf|background.sage|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.dwarf|background.soldier|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.dwarf|background.soldier|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.dwarf|background.soldier|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.dwarf|background.soldier|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.elf|background.acolyte|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.elf|background.acolyte|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.elf|background.acolyte|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.elf|background.acolyte|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.elf|background.criminal|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.elf|background.criminal|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.elf|background.criminal|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.elf|background.criminal|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.elf|background.sage|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.elf|background.sage|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.elf|background.sage|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.elf|background.sage|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.elf|background.soldier|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.elf|background.soldier|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.elf|background.soldier|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.elf|background.soldier|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.human|background.acolyte|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.human|background.acolyte|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.human|background.acolyte|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.human|background.acolyte|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.human|background.criminal|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.human|background.criminal|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.human|background.criminal|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.human|background.criminal|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.human|background.sage|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.human|background.sage|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.human|background.sage|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.human|background.sage|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.human|background.soldier|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.human|background.soldier|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.human|background.soldier|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.fighter|species.human|background.soldier|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.dragonborn|background.acolyte|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.dragonborn|background.acolyte|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.dragonborn|background.acolyte|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.dragonborn|background.acolyte|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.dragonborn|background.criminal|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.dragonborn|background.criminal|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.dragonborn|background.criminal|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.dragonborn|background.criminal|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.dragonborn|background.sage|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.dragonborn|background.sage|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.dragonborn|background.sage|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.dragonborn|background.sage|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.dragonborn|background.soldier|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.dragonborn|background.soldier|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.dragonborn|background.soldier|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.dragonborn|background.soldier|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.dwarf|background.acolyte|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.dwarf|background.acolyte|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.dwarf|background.acolyte|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.dwarf|background.acolyte|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.dwarf|background.criminal|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.dwarf|background.criminal|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.dwarf|background.criminal|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.dwarf|background.criminal|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.dwarf|background.sage|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.dwarf|background.sage|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.dwarf|background.sage|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.dwarf|background.sage|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.dwarf|background.soldier|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.dwarf|background.soldier|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.dwarf|background.soldier|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.dwarf|background.soldier|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.elf|background.acolyte|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.elf|background.acolyte|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.elf|background.acolyte|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.elf|background.acolyte|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.elf|background.criminal|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.elf|background.criminal|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.elf|background.criminal|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.elf|background.criminal|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.elf|background.sage|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.elf|background.sage|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.elf|background.sage|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.elf|background.sage|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.elf|background.soldier|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.elf|background.soldier|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.elf|background.soldier|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.elf|background.soldier|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.human|background.acolyte|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.human|background.acolyte|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.human|background.acolyte|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.human|background.acolyte|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.human|background.criminal|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.human|background.criminal|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.human|background.criminal|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.human|background.criminal|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.human|background.sage|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.human|background.sage|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.human|background.sage|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.human|background.sage|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.human|background.soldier|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.human|background.soldier|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.human|background.soldier|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.rogue|species.human|background.soldier|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.sorcerer|species.dragonborn|background.acolyte|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.sorcerer|species.dragonborn|background.acolyte|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@51832580-68f5-4e96-8afe-93e4af045283"
      ]
    },
    {
      stableKey: "class.sorcerer|species.dragonborn|background.acolyte|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.sorcerer|species.dragonborn|background.acolyte|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.sorcerer|species.dragonborn|background.criminal|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.sorcerer|species.dragonborn|background.criminal|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@51832580-68f5-4e96-8afe-93e4af045283"
      ]
    },
    {
      stableKey: "class.sorcerer|species.dragonborn|background.criminal|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.sorcerer|species.dragonborn|background.criminal|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.sorcerer|species.dragonborn|background.sage|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.sorcerer|species.dragonborn|background.sage|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@51832580-68f5-4e96-8afe-93e4af045283"
      ]
    },
    {
      stableKey: "class.sorcerer|species.dragonborn|background.sage|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.sorcerer|species.dragonborn|background.sage|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.sorcerer|species.dragonborn|background.soldier|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.sorcerer|species.dragonborn|background.soldier|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@51832580-68f5-4e96-8afe-93e4af045283"
      ]
    },
    {
      stableKey: "class.sorcerer|species.dragonborn|background.soldier|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.sorcerer|species.dragonborn|background.soldier|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.sorcerer|species.dwarf|background.acolyte|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.sorcerer|species.dwarf|background.acolyte|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@51832580-68f5-4e96-8afe-93e4af045283"
      ]
    },
    {
      stableKey: "class.sorcerer|species.dwarf|background.acolyte|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.sorcerer|species.dwarf|background.acolyte|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.sorcerer|species.dwarf|background.criminal|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.sorcerer|species.dwarf|background.criminal|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@51832580-68f5-4e96-8afe-93e4af045283"
      ]
    },
    {
      stableKey: "class.sorcerer|species.dwarf|background.criminal|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.sorcerer|species.dwarf|background.criminal|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.sorcerer|species.dwarf|background.sage|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.sorcerer|species.dwarf|background.sage|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@51832580-68f5-4e96-8afe-93e4af045283"
      ]
    },
    {
      stableKey: "class.sorcerer|species.dwarf|background.sage|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.sorcerer|species.dwarf|background.sage|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.sorcerer|species.dwarf|background.soldier|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.sorcerer|species.dwarf|background.soldier|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@51832580-68f5-4e96-8afe-93e4af045283"
      ]
    },
    {
      stableKey: "class.sorcerer|species.dwarf|background.soldier|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.sorcerer|species.dwarf|background.soldier|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.sorcerer|species.elf|background.acolyte|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.sorcerer|species.elf|background.acolyte|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@51832580-68f5-4e96-8afe-93e4af045283"
      ]
    },
    {
      stableKey: "class.sorcerer|species.elf|background.acolyte|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.sorcerer|species.elf|background.acolyte|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.sorcerer|species.elf|background.criminal|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.sorcerer|species.elf|background.criminal|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@51832580-68f5-4e96-8afe-93e4af045283"
      ]
    },
    {
      stableKey: "class.sorcerer|species.elf|background.criminal|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.sorcerer|species.elf|background.criminal|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.sorcerer|species.elf|background.sage|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.sorcerer|species.elf|background.sage|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@51832580-68f5-4e96-8afe-93e4af045283"
      ]
    },
    {
      stableKey: "class.sorcerer|species.elf|background.sage|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.sorcerer|species.elf|background.sage|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.sorcerer|species.elf|background.soldier|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.sorcerer|species.elf|background.soldier|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@51832580-68f5-4e96-8afe-93e4af045283"
      ]
    },
    {
      stableKey: "class.sorcerer|species.elf|background.soldier|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.sorcerer|species.elf|background.soldier|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.sorcerer|species.human|background.acolyte|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.sorcerer|species.human|background.acolyte|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@51832580-68f5-4e96-8afe-93e4af045283"
      ]
    },
    {
      stableKey: "class.sorcerer|species.human|background.acolyte|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.sorcerer|species.human|background.acolyte|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.sorcerer|species.human|background.criminal|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.sorcerer|species.human|background.criminal|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@51832580-68f5-4e96-8afe-93e4af045283"
      ]
    },
    {
      stableKey: "class.sorcerer|species.human|background.criminal|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.sorcerer|species.human|background.criminal|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.sorcerer|species.human|background.sage|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.sorcerer|species.human|background.sage|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@51832580-68f5-4e96-8afe-93e4af045283"
      ]
    },
    {
      stableKey: "class.sorcerer|species.human|background.sage|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.sorcerer|species.human|background.sage|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.sorcerer|species.human|background.soldier|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.sorcerer|species.human|background.soldier|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@51832580-68f5-4e96-8afe-93e4af045283"
      ]
    },
    {
      stableKey: "class.sorcerer|species.human|background.soldier|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.sorcerer|species.human|background.soldier|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.dragonborn|background.acolyte|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.dragonborn|background.acolyte|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.dragonborn|background.acolyte|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.dragonborn|background.acolyte|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.dragonborn|background.criminal|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.dragonborn|background.criminal|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.dragonborn|background.criminal|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.dragonborn|background.criminal|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.dragonborn|background.sage|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.dragonborn|background.sage|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.dragonborn|background.sage|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.dragonborn|background.sage|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.dragonborn|background.soldier|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.dragonborn|background.soldier|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.dragonborn|background.soldier|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.dragonborn|background.soldier|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.dwarf|background.acolyte|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.dwarf|background.acolyte|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.dwarf|background.acolyte|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.dwarf|background.acolyte|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.dwarf|background.criminal|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.dwarf|background.criminal|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.dwarf|background.criminal|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.dwarf|background.criminal|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.dwarf|background.sage|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.dwarf|background.sage|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.dwarf|background.sage|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.dwarf|background.sage|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.dwarf|background.soldier|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.dwarf|background.soldier|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.dwarf|background.soldier|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.dwarf|background.soldier|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.elf|background.acolyte|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.elf|background.acolyte|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.elf|background.acolyte|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.elf|background.acolyte|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.elf|background.criminal|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.elf|background.criminal|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.elf|background.criminal|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.elf|background.criminal|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.elf|background.sage|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.elf|background.sage|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.elf|background.sage|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.elf|background.sage|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.elf|background.soldier|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.elf|background.soldier|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.elf|background.soldier|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.elf|background.soldier|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.human|background.acolyte|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.human|background.acolyte|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.human|background.acolyte|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.human|background.acolyte|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.human|background.criminal|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.human|background.criminal|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.human|background.criminal|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.human|background.criminal|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.human|background.sage|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.human|background.sage|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.human|background.sage|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.human|background.sage|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.human|background.soldier|feat.alert",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.human|background.soldier|feat.magic-initiate",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.human|background.soldier|feat.skilled",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.warlock|species.human|background.soldier|feat.tough",
      actionIds: [
        "6812f780-de8a-4509-9253-b4be711a9212",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ]
    },
    {
      stableKey: "class.wizard|species.dragonborn|background.acolyte|feat.alert",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.dragonborn|background.acolyte|feat.magic-initiate",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.dragonborn|background.acolyte|feat.skilled",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.dragonborn|background.acolyte|feat.tough",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.dragonborn|background.criminal|feat.alert",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.dragonborn|background.criminal|feat.magic-initiate",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.dragonborn|background.criminal|feat.skilled",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.dragonborn|background.criminal|feat.tough",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.dragonborn|background.sage|feat.alert",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.dragonborn|background.sage|feat.magic-initiate",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.dragonborn|background.sage|feat.skilled",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.dragonborn|background.sage|feat.tough",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.dragonborn|background.soldier|feat.alert",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.dragonborn|background.soldier|feat.magic-initiate",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.dragonborn|background.soldier|feat.skilled",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.dragonborn|background.soldier|feat.tough",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.dwarf|background.acolyte|feat.alert",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.dwarf|background.acolyte|feat.magic-initiate",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.dwarf|background.acolyte|feat.skilled",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.dwarf|background.acolyte|feat.tough",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.dwarf|background.criminal|feat.alert",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.dwarf|background.criminal|feat.magic-initiate",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.dwarf|background.criminal|feat.skilled",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.dwarf|background.criminal|feat.tough",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.dwarf|background.sage|feat.alert",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.dwarf|background.sage|feat.magic-initiate",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.dwarf|background.sage|feat.skilled",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.dwarf|background.sage|feat.tough",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.dwarf|background.soldier|feat.alert",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.dwarf|background.soldier|feat.magic-initiate",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.dwarf|background.soldier|feat.skilled",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.dwarf|background.soldier|feat.tough",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.elf|background.acolyte|feat.alert",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.elf|background.acolyte|feat.magic-initiate",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.elf|background.acolyte|feat.skilled",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.elf|background.acolyte|feat.tough",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.elf|background.criminal|feat.alert",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.elf|background.criminal|feat.magic-initiate",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.elf|background.criminal|feat.skilled",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.elf|background.criminal|feat.tough",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.elf|background.sage|feat.alert",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.elf|background.sage|feat.magic-initiate",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.elf|background.sage|feat.skilled",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.elf|background.sage|feat.tough",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.elf|background.soldier|feat.alert",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.elf|background.soldier|feat.magic-initiate",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.elf|background.soldier|feat.skilled",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.elf|background.soldier|feat.tough",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.human|background.acolyte|feat.alert",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.human|background.acolyte|feat.magic-initiate",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.human|background.acolyte|feat.skilled",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.human|background.acolyte|feat.tough",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.human|background.criminal|feat.alert",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.human|background.criminal|feat.magic-initiate",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.human|background.criminal|feat.skilled",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.human|background.criminal|feat.tough",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.human|background.sage|feat.alert",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.human|background.sage|feat.magic-initiate",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.human|background.sage|feat.skilled",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.human|background.sage|feat.tough",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.human|background.soldier|feat.alert",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.human|background.soldier|feat.magic-initiate",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.human|background.soldier|feat.skilled",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      stableKey: "class.wizard|species.human|background.soldier|feat.tough",
      actionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "6812f780-de8a-4509-9253-b4be711a9212",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    }
  ],
  actions: [
    {
      id: "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
      name: "\u0412\u043E\u043B\u043D\u0430 \u0433\u0440\u043E\u043C\u0430",
      mechanics: {
        activation: {
          cast_time: {
            amount: 1,
            unit: "action"
          },
          cost: [
            {
              resource: "action"
            },
            {
              amount: 1,
              level: 1,
              resource: "spell_slot"
            }
          ],
          mode: "active"
        },
        effects: [
          {
            ability: "con",
            dc: "8 + prof + spellcasting",
            on_fail: [
              {
                dice: "2d8",
                kind: "damage",
                scaling: {
                  dice: "1d8",
                  per: "spell_slot_above"
                },
                type: "thunder"
              },
              {
                distance: 10,
                kind: "movement",
                value: "push"
              }
            ],
            on_success: [
              {
                dice: "2d8",
                kind: "damage",
                on_success: "half",
                scaling: {
                  dice: "1d8",
                  per: "spell_slot_above"
                },
                type: "thunder"
              }
            ],
            resolution: "save",
            who: "target"
          }
        ],
        interaction: {
          intent: "harmful"
        },
        primitive: {
          exclude_carried_objects: true,
          exclude_secured_objects: true,
          object_area_requirement: "entirely_in_area",
          object_max_distance_ft: 15,
          object_push_distance_ft: 10,
          type: "area_object_push"
        },
        spell_class_list_ids: [
          "CLASS-bard",
          "CLASS-druid",
          "CLASS-sorcerer",
          "CLASS-wizard"
        ],
        targeting: {
          actor_targets: true,
          allowed_relations: [
            "ally",
            "enemy",
            "neutral"
          ],
          area: {
            kind: "cube",
            size_ft: 15
          },
          domain: "mixed",
          filter: "creature",
          max_targets: 8,
          min_targets: 0,
          range: "15 \u0444\u0443\u0442\u043E\u0432",
          range_ft: 15,
          requires_line_of_sight: false,
          shape: "area"
        }
      },
      sourceEntityIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0",
        "932a15ad-a7c5-4062-b13a-899933b94231"
      ],
      targeting: {
        minTargets: 0,
        maxTargets: 8,
        rangeFt: 15,
        requiresLineOfSight: false,
        allowedRelations: [
          "ally",
          "enemy",
          "neutral"
        ]
      },
      kind: "spell",
      spell: {
        level: 1,
        sourceClass: "CLASS-druid",
        ritual: false,
        classListIds: [
          "CLASS-bard",
          "CLASS-druid",
          "CLASS-sorcerer",
          "CLASS-wizard"
        ],
        components: {
          verbal: true,
          somatic: true,
          material: false
        }
      }
    },
    {
      id: "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
      name: "\u0412\u043E\u043B\u043D\u0430 \u0433\u0440\u043E\u043C\u0430",
      mechanics: {
        activation: {
          cast_time: {
            amount: 1,
            unit: "action"
          },
          cost: [
            {
              resource: "action"
            },
            {
              amount: 1,
              level: 1,
              resource: "spell_slot"
            }
          ],
          mode: "active"
        },
        effects: [
          {
            ability: "con",
            dc: "8 + prof + spellcasting",
            on_fail: [
              {
                dice: "2d8",
                kind: "damage",
                scaling: {
                  dice: "1d8",
                  per: "spell_slot_above"
                },
                type: "thunder"
              },
              {
                distance: 10,
                kind: "movement",
                value: "push"
              }
            ],
            on_success: [
              {
                dice: "2d8",
                kind: "damage",
                on_success: "half",
                scaling: {
                  dice: "1d8",
                  per: "spell_slot_above"
                },
                type: "thunder"
              }
            ],
            resolution: "save",
            who: "target"
          }
        ],
        interaction: {
          intent: "harmful"
        },
        primitive: {
          exclude_carried_objects: true,
          exclude_secured_objects: true,
          object_area_requirement: "entirely_in_area",
          object_max_distance_ft: 15,
          object_push_distance_ft: 10,
          type: "area_object_push"
        },
        spell_class_list_ids: [
          "CLASS-bard",
          "CLASS-druid",
          "CLASS-sorcerer",
          "CLASS-wizard"
        ],
        targeting: {
          actor_targets: true,
          allowed_relations: [
            "ally",
            "enemy",
            "neutral"
          ],
          area: {
            kind: "cube",
            size_ft: 15
          },
          domain: "mixed",
          filter: "creature",
          max_targets: 8,
          min_targets: 0,
          range: "15 \u0444\u0443\u0442\u043E\u0432",
          range_ft: 15,
          requires_line_of_sight: false,
          shape: "area"
        }
      },
      sourceEntityIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0",
        "d3b22b24-a4f1-4dab-8038-c89bfee62843"
      ],
      targeting: {
        minTargets: 0,
        maxTargets: 8,
        rangeFt: 15,
        requiresLineOfSight: false,
        allowedRelations: [
          "ally",
          "enemy",
          "neutral"
        ]
      },
      kind: "spell",
      spell: {
        level: 1,
        sourceClass: "CLASS-wizard",
        ritual: false,
        classListIds: [
          "CLASS-bard",
          "CLASS-druid",
          "CLASS-sorcerer",
          "CLASS-wizard"
        ],
        components: {
          verbal: true,
          somatic: true,
          material: false
        }
      }
    },
    {
      id: "6812f780-de8a-4509-9253-b4be711a9212",
      name: "\u0410\u0442\u0430\u043A\u0430 \u0432\u0442\u043E\u0440\u043E\u0439 \u0440\u0443\u043A\u043E\u0439",
      mechanics: {
        activation: {
          cost: [
            {
              resource: "bonus_action"
            },
            {
              amount: 1,
              resource: "equipped_weapon_ammo"
            }
          ],
          mode: "active"
        },
        effects: [
          {
            ability: "auto",
            attack_kind: "weapon_melee",
            on_hit: [
              {
                ability: "none",
                dice: "weapon",
                kind: "damage",
                type: "weapon"
              }
            ],
            resolution: "attack_roll",
            tags: [
              "off_hand",
              "two_weapon"
            ],
            vs: "ac"
          }
        ],
        name: "\u0410\u0442\u0430\u043A\u0430 \u0432\u0442\u043E\u0440\u043E\u0439 \u0440\u0443\u043A\u043E\u0439",
        primitive: {
          type: "light_weapon_extra_attack"
        },
        targeting: {
          actor_targets: true,
          allowed_relations: [
            "ally",
            "enemy",
            "neutral"
          ],
          domain: "actor",
          max_targets: 1,
          min_targets: 1,
          range_ft: 600,
          requires_line_of_sight: true,
          shape: "single"
        }
      },
      sourceEntityIds: [
        "6812f780-de8a-4509-9253-b4be711a9212"
      ],
      targeting: {
        minTargets: 1,
        maxTargets: 1,
        rangeFt: 600,
        requiresLineOfSight: true,
        allowedRelations: [
          "ally",
          "enemy",
          "neutral"
        ]
      },
      kind: "nonSpell"
    },
    {
      id: "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
      name: "\u041E\u0433\u043D\u0435\u043D\u043D\u044B\u0435 \u043B\u0430\u0434\u043E\u043D\u0438",
      mechanics: {
        activation: {
          cast_time: {
            amount: 1,
            unit: "action"
          },
          cost: [
            {
              resource: "action"
            },
            {
              amount: 1,
              level: 1,
              resource: "spell_slot"
            }
          ],
          mode: "active"
        },
        effects: [
          {
            ability: "dex",
            dc: "8 + prof + spellcasting",
            on_fail: [
              {
                dice: "3d6",
                kind: "damage",
                scaling: {
                  dice: "1d6",
                  per: "spell_slot_above"
                },
                type: "fire"
              }
            ],
            on_success: [
              {
                dice: "3d6",
                kind: "damage",
                on_success: "half",
                scaling: {
                  dice: "1d6",
                  per: "spell_slot_above"
                },
                type: "fire"
              }
            ],
            resolution: "save",
            who: "target"
          }
        ],
        interaction: {
          intent: "harmful"
        },
        primitive: {
          policy: {
            exclude_carried: true,
            require_flammable: true,
            require_in_area: true
          },
          type: "burning_hands_objects"
        },
        spell_class_list_ids: [
          "CLASS-sorcerer",
          "CLASS-wizard"
        ],
        targeting: {
          actor_targets: true,
          allowed_relations: [
            "ally",
            "enemy",
            "neutral"
          ],
          area: {
            kind: "cone",
            size_ft: 15
          },
          domain: "mixed",
          filter: "creature",
          max_targets: 8,
          min_targets: 0,
          range: "15 \u0444\u0443\u0442\u043E\u0432",
          range_ft: 15,
          requires_line_of_sight: false,
          shape: "area"
        }
      },
      sourceEntityIds: [
        "732c2287-dd4d-46cd-9315-67447549abfa",
        "66a58227-41e6-4c56-b548-1253d98f6f09"
      ],
      targeting: {
        minTargets: 0,
        maxTargets: 8,
        rangeFt: 15,
        requiresLineOfSight: false,
        allowedRelations: [
          "ally",
          "enemy",
          "neutral"
        ]
      },
      kind: "spell",
      spell: {
        level: 1,
        sourceClass: "CLASS-sorcerer",
        ritual: false,
        classListIds: [
          "CLASS-sorcerer",
          "CLASS-wizard"
        ],
        components: {
          verbal: true,
          somatic: true,
          material: false
        }
      }
    },
    {
      id: "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
      name: "\u041E\u0433\u043D\u0435\u043D\u043D\u044B\u0435 \u043B\u0430\u0434\u043E\u043D\u0438",
      mechanics: {
        activation: {
          cast_time: {
            amount: 1,
            unit: "action"
          },
          cost: [
            {
              resource: "action"
            },
            {
              amount: 1,
              level: 1,
              resource: "spell_slot"
            }
          ],
          mode: "active"
        },
        effects: [
          {
            ability: "dex",
            dc: "8 + prof + spellcasting",
            on_fail: [
              {
                dice: "3d6",
                kind: "damage",
                scaling: {
                  dice: "1d6",
                  per: "spell_slot_above"
                },
                type: "fire"
              }
            ],
            on_success: [
              {
                dice: "3d6",
                kind: "damage",
                on_success: "half",
                scaling: {
                  dice: "1d6",
                  per: "spell_slot_above"
                },
                type: "fire"
              }
            ],
            resolution: "save",
            who: "target"
          }
        ],
        interaction: {
          intent: "harmful"
        },
        primitive: {
          policy: {
            exclude_carried: true,
            require_flammable: true,
            require_in_area: true
          },
          type: "burning_hands_objects"
        },
        spell_class_list_ids: [
          "CLASS-sorcerer",
          "CLASS-wizard"
        ],
        targeting: {
          actor_targets: true,
          allowed_relations: [
            "ally",
            "enemy",
            "neutral"
          ],
          area: {
            kind: "cone",
            size_ft: 15
          },
          domain: "mixed",
          filter: "creature",
          max_targets: 8,
          min_targets: 0,
          range: "15 \u0444\u0443\u0442\u043E\u0432",
          range_ft: 15,
          requires_line_of_sight: false,
          shape: "area"
        }
      },
      sourceEntityIds: [
        "732c2287-dd4d-46cd-9315-67447549abfa",
        "d3b22b24-a4f1-4dab-8038-c89bfee62843"
      ],
      targeting: {
        minTargets: 0,
        maxTargets: 8,
        rangeFt: 15,
        requiresLineOfSight: false,
        allowedRelations: [
          "ally",
          "enemy",
          "neutral"
        ]
      },
      kind: "spell",
      spell: {
        level: 1,
        sourceClass: "CLASS-wizard",
        ritual: false,
        classListIds: [
          "CLASS-sorcerer",
          "CLASS-wizard"
        ],
        components: {
          verbal: true,
          somatic: true,
          material: false
        }
      }
    },
    {
      id: "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
      name: "\u0412\u043E\u043B\u0448\u0435\u0431\u043D\u0430\u044F \u0441\u0442\u0440\u0435\u043B\u0430",
      mechanics: {
        activation: {
          cast_time: {
            amount: 1,
            unit: "action"
          },
          cost: [
            {
              resource: "action"
            },
            {
              amount: 1,
              level: 1,
              resource: "spell_slot"
            }
          ],
          mode: "active"
        },
        effects: [],
        interaction: {
          intent: "harmful"
        },
        primitive: {
          policy: {
            allocation_choice_id: "magic_missile_dart_targets",
            base_dart_count: 3,
            base_slot_level: 1,
            darts_per_slot_above: 1,
            max_slot_level: 9,
            per_dart_effect: {
              resolution: "auto",
              result: [
                {
                  dice: "1d4 + 1",
                  kind: "damage",
                  type: "force"
                }
              ],
              who: "target"
            },
            simultaneous: true
          },
          type: "magic_missile"
        },
        spell_class_list_ids: [
          "CLASS-sorcerer",
          "CLASS-wizard"
        ],
        targeting: {
          actor_targets: true,
          allowed_relations: [
            "self",
            "ally",
            "enemy",
            "neutral"
          ],
          domain: "actor",
          filter: "creature",
          max_targets: 11,
          min_targets: 1,
          range: "120 \u0444\u0443\u0442\u043E\u0432",
          range_ft: 120,
          requires_line_of_sight: true,
          requires_sight: true,
          shape: "multiple"
        }
      },
      sourceEntityIds: [
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59",
        "51832580-68f5-4e96-8afe-93e4af045283",
        "2023be11-201d-4f3a-b9c3-a8019b972b80"
      ],
      targeting: {
        minTargets: 1,
        maxTargets: 11,
        rangeFt: 120,
        requiresLineOfSight: true,
        requiresSight: true,
        allowedRelations: [
          "self",
          "ally",
          "enemy",
          "neutral"
        ]
      },
      kind: "spell",
      spell: {
        level: 1,
        ritual: false,
        classListIds: [
          "CLASS-sorcerer",
          "CLASS-wizard"
        ],
        components: {
          verbal: true,
          somatic: true,
          material: false
        }
      }
    },
    {
      id: "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
      name: "\u0412\u043E\u043B\u0448\u0435\u0431\u043D\u0430\u044F \u0441\u0442\u0440\u0435\u043B\u0430",
      mechanics: {
        activation: {
          cast_time: {
            amount: 1,
            unit: "action"
          },
          cost: [
            {
              resource: "action"
            },
            {
              amount: 1,
              level: 1,
              resource: "spell_slot"
            }
          ],
          mode: "active"
        },
        effects: [],
        interaction: {
          intent: "harmful"
        },
        primitive: {
          policy: {
            allocation_choice_id: "magic_missile_dart_targets",
            base_dart_count: 3,
            base_slot_level: 1,
            darts_per_slot_above: 1,
            max_slot_level: 9,
            per_dart_effect: {
              resolution: "auto",
              result: [
                {
                  dice: "1d4 + 1",
                  kind: "damage",
                  type: "force"
                }
              ],
              who: "target"
            },
            simultaneous: true
          },
          type: "magic_missile"
        },
        spell_class_list_ids: [
          "CLASS-sorcerer",
          "CLASS-wizard"
        ],
        targeting: {
          actor_targets: true,
          allowed_relations: [
            "self",
            "ally",
            "enemy",
            "neutral"
          ],
          domain: "actor",
          filter: "creature",
          max_targets: 11,
          min_targets: 1,
          range: "120 \u0444\u0443\u0442\u043E\u0432",
          range_ft: 120,
          requires_line_of_sight: true,
          requires_sight: true,
          shape: "multiple"
        }
      },
      sourceEntityIds: [
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59",
        "66a58227-41e6-4c56-b548-1253d98f6f09"
      ],
      targeting: {
        minTargets: 1,
        maxTargets: 11,
        rangeFt: 120,
        requiresLineOfSight: true,
        requiresSight: true,
        allowedRelations: [
          "self",
          "ally",
          "enemy",
          "neutral"
        ]
      },
      kind: "spell",
      spell: {
        level: 1,
        sourceClass: "CLASS-sorcerer",
        ritual: false,
        classListIds: [
          "CLASS-sorcerer",
          "CLASS-wizard"
        ],
        components: {
          verbal: true,
          somatic: true,
          material: false
        }
      }
    },
    {
      id: "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
      name: "\u0412\u043E\u043B\u0448\u0435\u0431\u043D\u0430\u044F \u0441\u0442\u0440\u0435\u043B\u0430",
      mechanics: {
        activation: {
          cast_time: {
            amount: 1,
            unit: "action"
          },
          cost: [
            {
              resource: "action"
            },
            {
              amount: 1,
              level: 1,
              resource: "spell_slot"
            }
          ],
          mode: "active"
        },
        effects: [],
        interaction: {
          intent: "harmful"
        },
        primitive: {
          policy: {
            allocation_choice_id: "magic_missile_dart_targets",
            base_dart_count: 3,
            base_slot_level: 1,
            darts_per_slot_above: 1,
            max_slot_level: 9,
            per_dart_effect: {
              resolution: "auto",
              result: [
                {
                  dice: "1d4 + 1",
                  kind: "damage",
                  type: "force"
                }
              ],
              who: "target"
            },
            simultaneous: true
          },
          type: "magic_missile"
        },
        spell_class_list_ids: [
          "CLASS-sorcerer",
          "CLASS-wizard"
        ],
        targeting: {
          actor_targets: true,
          allowed_relations: [
            "self",
            "ally",
            "enemy",
            "neutral"
          ],
          domain: "actor",
          filter: "creature",
          max_targets: 11,
          min_targets: 1,
          range: "120 \u0444\u0443\u0442\u043E\u0432",
          range_ft: 120,
          requires_line_of_sight: true,
          requires_sight: true,
          shape: "multiple"
        }
      },
      sourceEntityIds: [
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59",
        "d3b22b24-a4f1-4dab-8038-c89bfee62843"
      ],
      targeting: {
        minTargets: 1,
        maxTargets: 11,
        rangeFt: 120,
        requiresLineOfSight: true,
        requiresSight: true,
        allowedRelations: [
          "self",
          "ally",
          "enemy",
          "neutral"
        ]
      },
      kind: "spell",
      spell: {
        level: 1,
        sourceClass: "CLASS-wizard",
        ritual: false,
        classListIds: [
          "CLASS-sorcerer",
          "CLASS-wizard"
        ],
        components: {
          verbal: true,
          somatic: true,
          material: false
        }
      }
    },
    {
      id: "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d",
      name: "\u0410\u0442\u0430\u043A\u0430 \u043E\u0440\u0443\u0436\u0438\u0435\u043C",
      mechanics: {
        activation: {
          cost: [
            {
              resource: "action"
            },
            {
              amount: 1,
              resource: "equipped_weapon_ammo"
            }
          ],
          mode: "active"
        },
        effects: [
          {
            ability: "auto",
            attack_kind: "weapon_melee",
            on_hit: [
              {
                ability: "auto",
                dice: "weapon",
                kind: "damage",
                type: "weapon"
              }
            ],
            resolution: "attack_roll",
            vs: "ac"
          }
        ],
        name: "\u0410\u0442\u0430\u043A\u0430 \u043E\u0440\u0443\u0436\u0438\u0435\u043C",
        primitive: {
          type: "weapon_attack"
        },
        targeting: {
          actor_targets: true,
          allowed_relations: [
            "ally",
            "enemy",
            "neutral"
          ],
          domain: "actor",
          max_targets: 1,
          min_targets: 1,
          range_ft: 600,
          requires_line_of_sight: true,
          shape: "single"
        }
      },
      sourceEntityIds: [
        "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d"
      ],
      targeting: {
        minTargets: 1,
        maxTargets: 1,
        rangeFt: 600,
        requiresLineOfSight: true,
        allowedRelations: [
          "ally",
          "enemy",
          "neutral"
        ]
      },
      kind: "nonSpell"
    },
    {
      id: "d87f4507-849f-450b-b328-0198cb011587@51832580-68f5-4e96-8afe-93e4af045283",
      name: "\u0429\u0438\u0442",
      mechanics: {
        activation: {
          cast_time: {
            amount: 1,
            unit: "reaction"
          },
          cost: [
            {
              resource: "reaction"
            },
            {
              amount: 1,
              level: 1,
              resource: "spell_slot"
            }
          ],
          mode: "reaction",
          trigger: {
            event: "hit_by_attack",
            events: [
              "hit_by_attack",
              "targeted_by_magic_missile"
            ]
          }
        },
        effects: [
          {
            resolution: "auto",
            result: [
              {
                applies_to: {
                  roll: "ac"
                },
                duration: {
                  type: "until_start_of_next_turn"
                },
                kind: "modifier",
                magic_missile_immunity: true,
                op: "add",
                value: "+5"
              },
              {
                description: "\u0414\u0435\u0439\u0441\u0442\u0432\u0443\u0435\u0442 \u0434\u043E \u043D\u0430\u0447\u0430\u043B\u0430 \u0432\u0430\u0448\u0435\u0433\u043E \u0441\u043B\u0435\u0434\u0443\u044E\u0449\u0435\u0433\u043E \u0445\u043E\u0434\u0430, \u0432 \u0442.\u0447. \u043F\u0440\u043E\u0442\u0438\u0432 \u0432\u044B\u0437\u0432\u0430\u0432\u0448\u0435\u0439 \u0430\u0442\u0430\u043A\u0438; \u0438\u043C\u043C\u0443\u043D\u0438\u0442\u0435\u0442 \u043A \u0412\u043E\u043B\u0448\u0435\u0431\u043D\u043E\u0439 \u0441\u0442\u0440\u0435\u043B\u0435.",
                kind: "narrative"
              }
            ]
          }
        ],
        spell_class_list_ids: [
          "CLASS-sorcerer",
          "CLASS-wizard"
        ],
        targeting: {
          actor_targets: false,
          allowed_relations: [
            "self"
          ],
          domain: "actor",
          max_targets: 1,
          min_targets: 0,
          range_ft: 0,
          requires_line_of_sight: false,
          shape: "self"
        }
      },
      sourceEntityIds: [
        "d87f4507-849f-450b-b328-0198cb011587",
        "51832580-68f5-4e96-8afe-93e4af045283",
        "2023be11-201d-4f3a-b9c3-a8019b972b80"
      ],
      targeting: {
        minTargets: 0,
        maxTargets: 1,
        rangeFt: 0,
        requiresLineOfSight: false,
        allowedRelations: [
          "self"
        ]
      },
      kind: "spell",
      spell: {
        level: 1,
        ritual: false,
        classListIds: [
          "CLASS-sorcerer",
          "CLASS-wizard"
        ],
        components: {
          verbal: true,
          somatic: true,
          material: false
        }
      }
    },
    {
      id: "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard",
      name: "\u0429\u0438\u0442",
      mechanics: {
        activation: {
          cast_time: {
            amount: 1,
            unit: "reaction"
          },
          cost: [
            {
              resource: "reaction"
            },
            {
              amount: 1,
              level: 1,
              resource: "spell_slot"
            }
          ],
          mode: "reaction",
          trigger: {
            event: "hit_by_attack",
            events: [
              "hit_by_attack",
              "targeted_by_magic_missile"
            ]
          }
        },
        effects: [
          {
            resolution: "auto",
            result: [
              {
                applies_to: {
                  roll: "ac"
                },
                duration: {
                  type: "until_start_of_next_turn"
                },
                kind: "modifier",
                magic_missile_immunity: true,
                op: "add",
                value: "+5"
              },
              {
                description: "\u0414\u0435\u0439\u0441\u0442\u0432\u0443\u0435\u0442 \u0434\u043E \u043D\u0430\u0447\u0430\u043B\u0430 \u0432\u0430\u0448\u0435\u0433\u043E \u0441\u043B\u0435\u0434\u0443\u044E\u0449\u0435\u0433\u043E \u0445\u043E\u0434\u0430, \u0432 \u0442.\u0447. \u043F\u0440\u043E\u0442\u0438\u0432 \u0432\u044B\u0437\u0432\u0430\u0432\u0448\u0435\u0439 \u0430\u0442\u0430\u043A\u0438; \u0438\u043C\u043C\u0443\u043D\u0438\u0442\u0435\u0442 \u043A \u0412\u043E\u043B\u0448\u0435\u0431\u043D\u043E\u0439 \u0441\u0442\u0440\u0435\u043B\u0435.",
                kind: "narrative"
              }
            ]
          }
        ],
        spell_class_list_ids: [
          "CLASS-sorcerer",
          "CLASS-wizard"
        ],
        targeting: {
          actor_targets: false,
          allowed_relations: [
            "self"
          ],
          domain: "actor",
          max_targets: 1,
          min_targets: 0,
          range_ft: 0,
          requires_line_of_sight: false,
          shape: "self"
        }
      },
      sourceEntityIds: [
        "d87f4507-849f-450b-b328-0198cb011587",
        "d3b22b24-a4f1-4dab-8038-c89bfee62843"
      ],
      targeting: {
        minTargets: 0,
        maxTargets: 1,
        rangeFt: 0,
        requiresLineOfSight: false,
        allowedRelations: [
          "self"
        ]
      },
      kind: "spell",
      spell: {
        level: 1,
        sourceClass: "CLASS-wizard",
        ritual: false,
        classListIds: [
          "CLASS-sorcerer",
          "CLASS-wizard"
        ],
        components: {
          verbal: true,
          somatic: true,
          material: false
        }
      }
    }
  ],
  accessSignaturesByAction: {
    "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid": [
      {
        grants: [
          {
            grantId: "spell-grant:CLASS-druid:34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
            actionId: "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-druid",
            sourceId: "CLASS-druid",
            access: "always_prepared",
            level: 1,
            spellcastingAbility: "wis",
            ritual: false,
            slotResource: "spell_slot_1",
            freeUseResource: null
          }
        ],
        preparedSources: []
      }
    ],
    "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard": [
      {
        grants: [
          {
            grantId: "spell-grant:CLASS-wizard:34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
            actionId: "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
            sourceId: "CLASS-wizard",
            access: "spellbook",
            level: 1,
            spellcastingAbility: "int",
            ritual: false,
            slotResource: "spell_slot_1",
            freeUseResource: null
          }
        ],
        preparedSources: [
          {
            sourceId: "CLASS-wizard",
            capacity: 4,
            availableActionIds: [
              "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
              "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
              "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
              "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard",
              "db6433bf-f779-4b65-8766-0feffefd1930@CLASS-wizard",
              "f3098f61-1ea7-4816-9819-1903388cfe06@CLASS-wizard"
            ],
            preparedActionIds: [
              "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
              "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
              "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
              "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
            ]
          }
        ]
      }
    ],
    "6812f780-de8a-4509-9253-b4be711a9212": [
      {
        grants: [],
        preparedSources: []
      }
    ],
    "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer": [
      {
        grants: [
          {
            grantId: "spell-grant:CLASS-sorcerer:732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
            actionId: "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-sorcerer",
            sourceId: "CLASS-sorcerer",
            access: "known",
            level: 1,
            spellcastingAbility: "cha",
            ritual: false,
            slotResource: "spell_slot_1",
            freeUseResource: null
          }
        ],
        preparedSources: []
      }
    ],
    "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard": [
      {
        grants: [
          {
            grantId: "spell-grant:CLASS-wizard:732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
            actionId: "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
            sourceId: "CLASS-wizard",
            access: "spellbook",
            level: 1,
            spellcastingAbility: "int",
            ritual: false,
            slotResource: "spell_slot_1",
            freeUseResource: null
          }
        ],
        preparedSources: [
          {
            sourceId: "CLASS-wizard",
            capacity: 4,
            availableActionIds: [
              "25d868ad-0714-4fce-b5b2-2931dd5646bc@CLASS-wizard",
              "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
              "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
              "aa2adb3d-101b-4e70-97f1-69dc9f0debf8@CLASS-wizard",
              "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard",
              "db6433bf-f779-4b65-8766-0feffefd1930@CLASS-wizard"
            ],
            preparedActionIds: [
              "25d868ad-0714-4fce-b5b2-2931dd5646bc@CLASS-wizard",
              "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
              "aa2adb3d-101b-4e70-97f1-69dc9f0debf8@CLASS-wizard",
              "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
            ]
          }
        ]
      },
      {
        grants: [
          {
            grantId: "spell-grant:CLASS-wizard:732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
            actionId: "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
            sourceId: "CLASS-wizard",
            access: "spellbook",
            level: 1,
            spellcastingAbility: "int",
            ritual: false,
            slotResource: "spell_slot_1",
            freeUseResource: null
          }
        ],
        preparedSources: [
          {
            sourceId: "CLASS-wizard",
            capacity: 4,
            availableActionIds: [
              "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
              "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
              "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
              "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard",
              "db6433bf-f779-4b65-8766-0feffefd1930@CLASS-wizard",
              "f3098f61-1ea7-4816-9819-1903388cfe06@CLASS-wizard"
            ],
            preparedActionIds: [
              "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
              "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
              "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
              "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
            ]
          }
        ]
      }
    ],
    "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283": [
      {
        grants: [
          {
            grantId: "spell-grant:FEAT-0009:938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
            actionId: "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
            sourceId: "FEAT-0009",
            access: "always_prepared",
            level: 1,
            spellcastingAbility: "int",
            ritual: false,
            slotResource: "spell_slot_1",
            freeUseResource: "freeuse-938a86fd-e0bf-4845-b4b3-acfe2fed2b59"
          }
        ],
        preparedSources: []
      }
    ],
    "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer": [
      {
        grants: [
          {
            grantId: "spell-grant:CLASS-sorcerer:938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
            actionId: "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-sorcerer",
            sourceId: "CLASS-sorcerer",
            access: "known",
            level: 1,
            spellcastingAbility: "cha",
            ritual: false,
            slotResource: "spell_slot_1",
            freeUseResource: null
          }
        ],
        preparedSources: []
      }
    ],
    "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard": [
      {
        grants: [
          {
            grantId: "spell-grant:CLASS-wizard:938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
            actionId: "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
            sourceId: "CLASS-wizard",
            access: "spellbook",
            level: 1,
            spellcastingAbility: "int",
            ritual: false,
            slotResource: "spell_slot_1",
            freeUseResource: null
          }
        ],
        preparedSources: [
          {
            sourceId: "CLASS-wizard",
            capacity: 4,
            availableActionIds: [
              "25d868ad-0714-4fce-b5b2-2931dd5646bc@CLASS-wizard",
              "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
              "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
              "aa2adb3d-101b-4e70-97f1-69dc9f0debf8@CLASS-wizard",
              "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard",
              "db6433bf-f779-4b65-8766-0feffefd1930@CLASS-wizard"
            ],
            preparedActionIds: [
              "25d868ad-0714-4fce-b5b2-2931dd5646bc@CLASS-wizard",
              "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
              "aa2adb3d-101b-4e70-97f1-69dc9f0debf8@CLASS-wizard",
              "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
            ]
          }
        ]
      },
      {
        grants: [
          {
            grantId: "spell-grant:CLASS-wizard:938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
            actionId: "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
            sourceId: "CLASS-wizard",
            access: "spellbook",
            level: 1,
            spellcastingAbility: "int",
            ritual: false,
            slotResource: "spell_slot_1",
            freeUseResource: null
          }
        ],
        preparedSources: [
          {
            sourceId: "CLASS-wizard",
            capacity: 4,
            availableActionIds: [
              "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
              "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
              "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
              "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard",
              "db6433bf-f779-4b65-8766-0feffefd1930@CLASS-wizard",
              "f3098f61-1ea7-4816-9819-1903388cfe06@CLASS-wizard"
            ],
            preparedActionIds: [
              "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
              "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
              "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
              "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
            ]
          }
        ]
      }
    ],
    "ae7b59a2-2eee-412f-aee6-6323b4c1fb4d": [
      {
        grants: [],
        preparedSources: []
      }
    ],
    "d87f4507-849f-450b-b328-0198cb011587@51832580-68f5-4e96-8afe-93e4af045283": [
      {
        grants: [
          {
            grantId: "spell-grant:FEAT-0009:d87f4507-849f-450b-b328-0198cb011587@51832580-68f5-4e96-8afe-93e4af045283",
            actionId: "d87f4507-849f-450b-b328-0198cb011587@51832580-68f5-4e96-8afe-93e4af045283",
            sourceId: "FEAT-0009",
            access: "always_prepared",
            level: 1,
            spellcastingAbility: "int",
            ritual: false,
            slotResource: "spell_slot_1",
            freeUseResource: "freeuse-d87f4507-849f-450b-b328-0198cb011587"
          }
        ],
        preparedSources: []
      }
    ],
    "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard": [
      {
        grants: [
          {
            grantId: "spell-grant:CLASS-wizard:d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard",
            actionId: "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard",
            sourceId: "CLASS-wizard",
            access: "spellbook",
            level: 1,
            spellcastingAbility: "int",
            ritual: false,
            slotResource: "spell_slot_1",
            freeUseResource: null
          }
        ],
        preparedSources: [
          {
            sourceId: "CLASS-wizard",
            capacity: 4,
            availableActionIds: [
              "25d868ad-0714-4fce-b5b2-2931dd5646bc@CLASS-wizard",
              "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
              "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
              "aa2adb3d-101b-4e70-97f1-69dc9f0debf8@CLASS-wizard",
              "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard",
              "db6433bf-f779-4b65-8766-0feffefd1930@CLASS-wizard"
            ],
            preparedActionIds: [
              "25d868ad-0714-4fce-b5b2-2931dd5646bc@CLASS-wizard",
              "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
              "aa2adb3d-101b-4e70-97f1-69dc9f0debf8@CLASS-wizard",
              "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
            ]
          }
        ]
      },
      {
        grants: [
          {
            grantId: "spell-grant:CLASS-wizard:d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard",
            actionId: "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard",
            sourceId: "CLASS-wizard",
            access: "spellbook",
            level: 1,
            spellcastingAbility: "int",
            ritual: false,
            slotResource: "spell_slot_1",
            freeUseResource: null
          }
        ],
        preparedSources: [
          {
            sourceId: "CLASS-wizard",
            capacity: 4,
            availableActionIds: [
              "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
              "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
              "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
              "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard",
              "db6433bf-f779-4b65-8766-0feffefd1930@CLASS-wizard",
              "f3098f61-1ea7-4816-9819-1903388cfe06@CLASS-wizard"
            ],
            preparedActionIds: [
              "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
              "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
              "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
              "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
            ]
          }
        ]
      }
    ]
  },
  preparedSourceProfiles: [
    {
      sourceId: "CLASS-wizard",
      capacity: 4,
      availableActionIds: [
        "25d868ad-0714-4fce-b5b2-2931dd5646bc@CLASS-wizard",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "aa2adb3d-101b-4e70-97f1-69dc9f0debf8@CLASS-wizard",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard",
        "db6433bf-f779-4b65-8766-0feffefd1930@CLASS-wizard"
      ],
      preparedActionIds: [
        "25d868ad-0714-4fce-b5b2-2931dd5646bc@CLASS-wizard",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "aa2adb3d-101b-4e70-97f1-69dc9f0debf8@CLASS-wizard",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    },
    {
      sourceId: "CLASS-wizard",
      capacity: 4,
      availableActionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard",
        "db6433bf-f779-4b65-8766-0feffefd1930@CLASS-wizard",
        "f3098f61-1ea7-4816-9819-1903388cfe06@CLASS-wizard"
      ],
      preparedActionIds: [
        "34518f38-b737-4a91-88ac-d5858d2d04a0@CLASS-wizard",
        "732c2287-dd4d-46cd-9315-67447549abfa@CLASS-wizard",
        "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@CLASS-wizard",
        "d87f4507-849f-450b-b328-0198cb011587@CLASS-wizard"
      ]
    }
  ],
  magicInitiate: {
    grantSourceId: "FEAT-0009",
    originFeatEntityId: "51832580-68f5-4e96-8afe-93e4af045283",
    actions: [
      {
        actionId: "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
        sourceEntityIds: [
          "938a86fd-e0bf-4845-b4b3-acfe2fed2b59",
          "51832580-68f5-4e96-8afe-93e4af045283",
          "2023be11-201d-4f3a-b9c3-a8019b972b80"
        ],
        grantSignatures: [
          {
            grantId: "spell-grant:FEAT-0009:938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
            actionId: "938a86fd-e0bf-4845-b4b3-acfe2fed2b59@51832580-68f5-4e96-8afe-93e4af045283",
            sourceId: "FEAT-0009",
            access: "always_prepared",
            level: 1,
            spellcastingAbility: "int",
            ritual: false,
            slotResource: "spell_slot_1",
            freeUseResource: "freeuse-938a86fd-e0bf-4845-b4b3-acfe2fed2b59"
          }
        ]
      },
      {
        actionId: "d87f4507-849f-450b-b328-0198cb011587@51832580-68f5-4e96-8afe-93e4af045283",
        sourceEntityIds: [
          "d87f4507-849f-450b-b328-0198cb011587",
          "51832580-68f5-4e96-8afe-93e4af045283",
          "2023be11-201d-4f3a-b9c3-a8019b972b80"
        ],
        grantSignatures: [
          {
            grantId: "spell-grant:FEAT-0009:d87f4507-849f-450b-b328-0198cb011587@51832580-68f5-4e96-8afe-93e4af045283",
            actionId: "d87f4507-849f-450b-b328-0198cb011587@51832580-68f5-4e96-8afe-93e4af045283",
            sourceId: "FEAT-0009",
            access: "always_prepared",
            level: 1,
            spellcastingAbility: "int",
            ritual: false,
            slotResource: "spell_slot_1",
            freeUseResource: "freeuse-d87f4507-849f-450b-b328-0198cb011587"
          }
        ]
      }
    ]
  },
  sourceProjectionHash: "sha256:d45f8d97601e6eba0640e6b1c0d95eaf26fc67cb6889187de45a3b5769d187ca",
  contentHash: "sha256:c7d28beda4ee169df4a424c1c547fd12f68dec1756f3cf5083a91a3485bbf22b"
};

// src/character/sheetCombatCertifiedCatalog.ts
var SHEET_COMBAT_CERTIFICATION_SCHEMA_VERSION = 1;
var SHEET_COMBAT_CERTIFICATION_ARTIFACT_VERSION = "1.0.0";
var SHEET_COMBAT_CERTIFICATION_EXPECTED_ROOT_COUNT = 448;
var SHEET_COMBAT_CERTIFICATION_EXPECTED_ACTION_COUNT = 11;
var MAGIC_INITIATE_WIZARD_GRANT_SOURCE_ID = "FEAT-0009";
var COMBAT_PRIMITIVES = /* @__PURE__ */ new Set([
  "burning_hands_objects",
  "area_object_push",
  "magic_missile",
  WEAPON_ATTACK_PRIMITIVE,
  LIGHT_WEAPON_EXTRA_ATTACK_PRIMITIVE
]);
var SPELL_ACCESS_KINDS = /* @__PURE__ */ new Set([
  "cantrip",
  "known",
  "spellbook",
  "always_prepared",
  "innate",
  "ritual_only"
]);
var SHA256 = /^sha256:[0-9a-f]{64}$/;
function clone(value) {
  return JSON.parse(JSON.stringify(value));
}
function object3(value) {
  return value && typeof value === "object" && !Array.isArray(value) ? value : null;
}
function requiredObject(value, label) {
  const parsed = object3(value);
  if (!parsed) throw new Error(`Sheet combat certification ${label} must be an object`);
  return parsed;
}
function requiredString(value, label) {
  if (typeof value !== "string" || !value.length) {
    throw new Error(`Sheet combat certification ${label} must be a non-empty string`);
  }
  return value;
}
function requiredInteger(value, label, minimum = 0) {
  if (!Number.isSafeInteger(value) || Number(value) < minimum) {
    throw new Error(`Sheet combat certification ${label} must be an integer >= ${minimum}`);
  }
  return Number(value);
}
function exactKeys4(value, keys, label) {
  const actual = Object.keys(value).sort();
  const expected = [...keys].sort();
  if (canonicalStringify(actual) !== canonicalStringify(expected)) {
    throw new Error(`Sheet combat certification ${label} must declare exactly: ${expected.join(", ")}`);
  }
}
function sortedUnique(values) {
  return [...new Set(values)].sort((left, right) => left.localeCompare(right));
}
function exactStringArray(value, label) {
  if (!Array.isArray(value) || value.some((item) => typeof item !== "string" || !item.length)) {
    throw new Error(`Sheet combat certification ${label} must be a string array`);
  }
  const strings = value;
  if (new Set(strings).size !== strings.length) {
    throw new Error(`Sheet combat certification ${label} must not contain duplicates`);
  }
  const normalized3 = sortedUnique(strings);
  if (canonicalStringify(strings) !== canonicalStringify(normalized3)) {
    throw new Error(`Sheet combat certification ${label} must be sorted`);
  }
  return normalized3;
}
function isSheetCombatAction(action) {
  const primitive = object3(action.mechanics.primitive)?.type;
  if (typeof primitive === "string" && COMBAT_PRIMITIVES.has(primitive)) return true;
  const activation = object3(action.mechanics.activation);
  const trigger = object3(activation?.trigger);
  return Array.isArray(trigger?.events) && trigger.events.includes("targeted_by_magic_missile");
}
function projectCertifiedSpellGrant(grant) {
  return {
    grantId: grant.grantId,
    actionId: grant.actionId,
    sourceId: grant.sourceId,
    access: grant.access,
    level: grant.level,
    spellcastingAbility: grant.spellcastingAbility,
    ritual: grant.ritual === true,
    slotResource: grant.slotResource ?? null,
    freeUseResource: grant.freeUseResource ?? null
  };
}
function parseGrantProjection(value, label) {
  const raw = requiredObject(value, label);
  exactKeys4(raw, [
    "grantId",
    "actionId",
    "sourceId",
    "access",
    "level",
    "spellcastingAbility",
    "ritual",
    "slotResource",
    "freeUseResource"
  ], label);
  const access = requiredString(raw.access, `${label}.access`);
  if (!SPELL_ACCESS_KINDS.has(access)) {
    throw new Error(`Sheet combat certification ${label}.access is unsupported`);
  }
  const spellcastingAbility = requiredString(
    raw.spellcastingAbility,
    `${label}.spellcastingAbility`
  );
  if (!["str", "dex", "con", "int", "wis", "cha"].includes(spellcastingAbility)) {
    throw new Error(`Sheet combat certification ${label}.spellcastingAbility is unsupported`);
  }
  if (typeof raw.ritual !== "boolean") {
    throw new Error(`Sheet combat certification ${label}.ritual must be boolean`);
  }
  const nullableResource = (resource, field) => {
    if (resource === null) return null;
    return requiredString(resource, `${label}.${field}`);
  };
  return {
    grantId: requiredString(raw.grantId, `${label}.grantId`),
    actionId: requiredString(raw.actionId, `${label}.actionId`),
    sourceId: requiredString(raw.sourceId, `${label}.sourceId`),
    access,
    level: requiredInteger(raw.level, `${label}.level`),
    spellcastingAbility,
    ritual: raw.ritual,
    slotResource: nullableResource(raw.slotResource, "slotResource"),
    freeUseResource: nullableResource(raw.freeUseResource, "freeUseResource")
  };
}
function projectCertifiedPreparedSource(input) {
  return {
    sourceId: input.sourceId,
    capacity: input.capacity,
    availableActionIds: sortedUnique(input.availableActionIds),
    preparedActionIds: sortedUnique(input.preparedActionIds)
  };
}
function parsePreparedSourceProjection(value, label) {
  const raw = requiredObject(value, label);
  exactKeys4(raw, [
    "sourceId",
    "capacity",
    "availableActionIds",
    "preparedActionIds"
  ], label);
  const result = {
    sourceId: requiredString(raw.sourceId, `${label}.sourceId`),
    capacity: requiredInteger(raw.capacity, `${label}.capacity`),
    availableActionIds: exactStringArray(raw.availableActionIds, `${label}.availableActionIds`),
    preparedActionIds: exactStringArray(raw.preparedActionIds, `${label}.preparedActionIds`)
  };
  if (result.preparedActionIds.length !== result.capacity) {
    throw new Error(`Sheet combat certification ${label} prepared count differs from capacity`);
  }
  const available = new Set(result.availableActionIds);
  if (result.preparedActionIds.some((actionId) => !available.has(actionId))) {
    throw new Error(`Sheet combat certification ${label} prepares an unavailable action`);
  }
  return result;
}
function projectCertifiedActorAccess(actor2, actionId) {
  const access = actor2.spellcastingAccess;
  const grants = (access?.grants ?? []).filter((grant) => grant.actionId === actionId).map(projectCertifiedSpellGrant).sort((left, right) => canonicalStringify(left).localeCompare(canonicalStringify(right)));
  const preparedSources = Object.entries(access?.preparedSources ?? {}).map(([key, source2]) => {
    if (!source2) throw new Error(`Actor ${actor2.id} prepared source ${key} is missing`);
    if (key !== source2.sourceId) {
      throw new Error(`Actor ${actor2.id} prepared source key ${key} differs from ${source2.sourceId}`);
    }
    return projectCertifiedPreparedSource(source2);
  }).sort((left, right) => left.sourceId.localeCompare(right.sourceId));
  return { grants, preparedSources };
}
function parseAccessProjection(value, label) {
  const raw = requiredObject(value, label);
  exactKeys4(raw, ["grants", "preparedSources"], label);
  if (!Array.isArray(raw.grants) || !Array.isArray(raw.preparedSources)) {
    throw new Error(`Sheet combat certification ${label} grants/preparedSources must be arrays`);
  }
  const grants = raw.grants.map((grant, index) => parseGrantProjection(grant, `${label}.grants[${index}]`));
  const preparedSources = raw.preparedSources.map((source2, index) => parsePreparedSourceProjection(source2, `${label}.preparedSources[${index}]`));
  const sortedGrants = [...grants].sort((left, right) => canonicalStringify(left).localeCompare(canonicalStringify(right)));
  const sortedSources = [...preparedSources].sort((left, right) => left.sourceId.localeCompare(right.sourceId));
  if (canonicalStringify(grants) !== canonicalStringify(sortedGrants) || canonicalStringify(preparedSources) !== canonicalStringify(sortedSources)) {
    throw new Error(`Sheet combat certification ${label} must be canonically sorted`);
  }
  if (new Set(preparedSources.map((source2) => source2.sourceId)).size !== preparedSources.length) {
    throw new Error(`Sheet combat certification ${label} has duplicate prepared sources`);
  }
  return { grants, preparedSources };
}
function sheetCombatCertificationSourceProjection(artifact) {
  return {
    source: artifact.source,
    summary: artifact.summary,
    coverage: artifact.coverage,
    actions: artifact.actions,
    accessSignaturesByAction: artifact.accessSignaturesByAction,
    preparedSourceProfiles: artifact.preparedSourceProfiles,
    magicInitiate: artifact.magicInitiate
  };
}
function sheetCombatCertificationContent(artifact) {
  const { contentHash: _contentHash, ...content } = artifact;
  return content;
}
function validateRulesetAndRelease(artifact) {
  const { ruleset, release } = artifact.source;
  if (ruleset.systemId !== "dnd5e-2024" || release.systemId !== "dnd5e-2024" || ruleset.releaseId !== release.id || ruleset.contentHash !== release.contentHash || ruleset.errataVersion !== release.errataVersion) {
    throw new Error("Sheet combat certification source ruleset/release identity differs");
  }
  for (const [field, value] of Object.entries({
    "ruleset.contentHash": ruleset.contentHash,
    "release.sourceContentHash": release.sourceContentHash,
    "release.overlayHash": release.overlayHash,
    "release.contentHash": release.contentHash,
    "release.releaseHash": release.releaseHash
  })) {
    if (!SHA256.test(value)) {
      throw new Error(`Sheet combat certification ${field} is not a SHA-256 pin`);
    }
  }
}
function validateArtifactEnvelopeShape(raw) {
  exactKeys4(raw, [
    "schemaVersion",
    "artifactVersion",
    "source",
    "summary",
    "coverage",
    "actions",
    "accessSignaturesByAction",
    "preparedSourceProfiles",
    "magicInitiate",
    "sourceProjectionHash",
    "contentHash"
  ], "artifact");
  const source2 = requiredObject(raw.source, "source");
  exactKeys4(source2, ["ruleset", "release"], "source");
  exactKeys4(requiredObject(source2.ruleset, "source.ruleset"), [
    "systemId",
    "releaseId",
    "contentHash",
    "errataVersion"
  ], "source.ruleset");
  exactKeys4(requiredObject(source2.release, "source.release"), [
    "id",
    "systemId",
    "rulesetVersion",
    "errataVersion",
    "sourceReleaseId",
    "sourceContentHash",
    "overlayHash",
    "contentHash",
    "releaseHash"
  ], "source.release");
  exactKeys4(requiredObject(raw.summary, "summary"), [
    "rootCount",
    "combatRootCount",
    "actionOccurrenceCount",
    "uniqueActionCount"
  ], "summary");
  if (!Array.isArray(raw.coverage) || !Array.isArray(raw.actions) || !Array.isArray(raw.preparedSourceProfiles)) {
    throw new Error("Sheet combat certification coverage/actions/profiles must be arrays");
  }
  raw.coverage.forEach((row, index) => exactKeys4(
    requiredObject(row, `coverage[${index}]`),
    ["stableKey", "actionIds"],
    `coverage[${index}]`
  ));
  requiredObject(raw.accessSignaturesByAction, "accessSignaturesByAction");
  const magicInitiate = requiredObject(raw.magicInitiate, "magicInitiate");
  exactKeys4(magicInitiate, [
    "grantSourceId",
    "originFeatEntityId",
    "actions"
  ], "magicInitiate");
  if (!Array.isArray(magicInitiate.actions)) {
    throw new Error("Sheet combat certification magicInitiate.actions must be an array");
  }
  magicInitiate.actions.forEach((row, index) => exactKeys4(
    requiredObject(row, `magicInitiate.actions[${index}]`),
    ["actionId", "sourceEntityIds", "grantSignatures"],
    `magicInitiate.actions[${index}]`
  ));
}
function validateArtifactDenominator(artifact) {
  const { summary } = artifact;
  if (summary.rootCount !== SHEET_COMBAT_CERTIFICATION_EXPECTED_ROOT_COUNT || summary.uniqueActionCount !== SHEET_COMBAT_CERTIFICATION_EXPECTED_ACTION_COUNT || artifact.coverage.length !== SHEET_COMBAT_CERTIFICATION_EXPECTED_ROOT_COUNT || artifact.actions.length !== SHEET_COMBAT_CERTIFICATION_EXPECTED_ACTION_COUNT) {
    throw new Error(
      `Sheet combat certification does not cover the ${SHEET_COMBAT_CERTIFICATION_EXPECTED_ROOT_COUNT}-root/${SHEET_COMBAT_CERTIFICATION_EXPECTED_ACTION_COUNT}-action denominator`
    );
  }
  const stableKeys = artifact.coverage.map((row) => row.stableKey);
  if (new Set(stableKeys).size !== stableKeys.length || canonicalStringify(stableKeys) !== canonicalStringify([...stableKeys].sort())) {
    throw new Error("Sheet combat certification coverage roots must be unique and sorted");
  }
  const actionIds = artifact.actions.map((action) => action.id);
  if (new Set(actionIds).size !== actionIds.length || canonicalStringify(actionIds) !== canonicalStringify([...actionIds].sort())) {
    throw new Error("Sheet combat certification actions must be unique and sorted");
  }
  const actionIdSet = new Set(actionIds);
  let combatRootCount = 0;
  let actionOccurrenceCount = 0;
  const coveredActionIds = /* @__PURE__ */ new Set();
  for (const row of artifact.coverage) {
    requiredString(row.stableKey, "coverage.stableKey");
    const rowActionIds = exactStringArray(row.actionIds, `${row.stableKey}.actionIds`);
    if (rowActionIds.length) combatRootCount += 1;
    actionOccurrenceCount += rowActionIds.length;
    for (const actionId of rowActionIds) {
      if (!actionIdSet.has(actionId)) {
        throw new Error(`Sheet combat certification root ${row.stableKey} names unknown ${actionId}`);
      }
      coveredActionIds.add(actionId);
    }
  }
  if (combatRootCount !== summary.combatRootCount || actionOccurrenceCount !== summary.actionOccurrenceCount || canonicalStringify([...coveredActionIds].sort()) !== canonicalStringify(actionIds)) {
    throw new Error("Sheet combat certification coverage summary or action union differs");
  }
}
function validateArtifactActions(artifact) {
  for (const action of artifact.actions) {
    if (!object3(action) || typeof action.id !== "string" || !object3(action.mechanics) || !isSheetCombatAction(action)) {
      throw new Error("Sheet combat certification contains a non-combat or malformed action");
    }
    const primitive = object3(action.mechanics.primitive)?.type;
    if (primitive === WEAPON_ATTACK_PRIMITIVE || primitive === LIGHT_WEAPON_EXTRA_ATTACK_PRIMITIVE) {
      const parsed2 = parseDeclaredWeaponActionPolicy(action, "template");
      if (parsed2.status !== "valid") {
        throw new Error(
          `Sheet combat certification is stale: ${action.id} ${parsed2.issue}`
        );
      }
      continue;
    }
    if (primitive !== "burning_hands_objects" && primitive !== "magic_missile") continue;
    const parsed = parseWorldSpellPolicy(action.mechanics);
    if (parsed.status !== "valid" || parsed.primitiveType !== primitive) {
      throw new Error(
        `Sheet combat certification is stale: ${action.id} ${parsed.status === "invalid" ? parsed.issue : "has no strict data-owned policy"}`
      );
    }
  }
}
function validateAccessAndPreparation(artifact) {
  const actionIds = artifact.actions.map((action) => action.id);
  const signatureKeys = Object.keys(artifact.accessSignaturesByAction).sort();
  if (canonicalStringify(signatureKeys) !== canonicalStringify(actionIds)) {
    throw new Error("Sheet combat certification access signatures do not cover every exact action");
  }
  const parsedProfiles = artifact.preparedSourceProfiles.map((profile, index) => parsePreparedSourceProjection(profile, `preparedSourceProfiles[${index}]`));
  const canonicalProfiles = parsedProfiles.map(canonicalStringify);
  if (new Set(canonicalProfiles).size !== canonicalProfiles.length || canonicalStringify(canonicalProfiles) !== canonicalStringify([...canonicalProfiles].sort())) {
    throw new Error("Sheet combat certification prepared source profiles must be unique and sorted");
  }
  const certifiedProfiles = new Set(canonicalProfiles);
  const referencedProfiles = /* @__PURE__ */ new Set();
  for (const actionId of actionIds) {
    const action = artifact.actions.find((candidate) => candidate.id === actionId);
    const rawSignatures = artifact.accessSignaturesByAction[actionId];
    if (!Array.isArray(rawSignatures) || !rawSignatures.length) {
      throw new Error(`Sheet combat certification has no actor access signature for ${actionId}`);
    }
    const signatures = rawSignatures.map((signature, index) => parseAccessProjection(signature, `accessSignaturesByAction.${actionId}[${index}]`));
    const canonicalSignatures = signatures.map(canonicalStringify);
    if (new Set(canonicalSignatures).size !== canonicalSignatures.length || canonicalStringify(canonicalSignatures) !== canonicalStringify([...canonicalSignatures].sort())) {
      throw new Error(`Sheet combat certification signatures for ${actionId} must be unique and sorted`);
    }
    for (const signature of signatures) {
      if (action.kind === "spell" && !signature.grants.length || action.kind === "nonSpell" && signature.grants.length > 0 || signature.grants.some((grant) => grant.actionId !== actionId)) {
        throw new Error(`Sheet combat certification access for ${actionId} has wrong grants`);
      }
      for (const profile of signature.preparedSources) {
        const canonical = canonicalStringify(profile);
        if (!certifiedProfiles.has(canonical)) {
          throw new Error(`Sheet combat certification access for ${actionId} has unknown preparation`);
        }
        referencedProfiles.add(canonical);
      }
    }
  }
  if (canonicalStringify([...referencedProfiles].sort()) !== canonicalStringify([...certifiedProfiles].sort())) {
    throw new Error("Sheet combat certification has an unreferenced prepared source profile");
  }
}
function validateMagicInitiateProvenance(artifact) {
  const provenance = artifact.magicInitiate;
  if (provenance.grantSourceId !== MAGIC_INITIATE_WIZARD_GRANT_SOURCE_ID || typeof provenance.originFeatEntityId !== "string" || !provenance.originFeatEntityId.length || !Array.isArray(provenance.actions) || provenance.actions.length !== 2) {
    throw new Error("Sheet combat certification Magic Initiate provenance is incomplete");
  }
  const expectedActionIds = Object.entries(artifact.accessSignaturesByAction).filter(([, signatures]) => signatures.some((signature) => signature.grants.some(
    (grant) => grant.sourceId === MAGIC_INITIATE_WIZARD_GRANT_SOURCE_ID
  ))).map(([actionId]) => actionId).sort();
  const actualActionIds = provenance.actions.map((row) => row.actionId);
  if (canonicalStringify(actualActionIds) !== canonicalStringify(expectedActionIds)) {
    throw new Error("Sheet combat certification Magic Initiate action coverage differs");
  }
  const actionById = new Map(artifact.actions.map((action) => [action.id, action]));
  for (const row of provenance.actions) {
    const action = actionById.get(row.actionId);
    if (!action || canonicalStringify(row.sourceEntityIds) !== canonicalStringify(action.sourceEntityIds) || !row.sourceEntityIds.includes(provenance.originFeatEntityId)) {
      throw new Error(`Sheet combat certification Magic Initiate provenance differs for ${row.actionId}`);
    }
    const expectedGrants = artifact.accessSignaturesByAction[row.actionId].flatMap((signature) => signature.grants).filter((grant) => grant.sourceId === MAGIC_INITIATE_WIZARD_GRANT_SOURCE_ID).map(canonicalStringify);
    const actualGrants = row.grantSignatures.map((grant, index) => canonicalStringify(
      parseGrantProjection(grant, `magicInitiate.${row.actionId}.grantSignatures[${index}]`)
    ));
    if (canonicalStringify(sortedUnique(actualGrants)) !== canonicalStringify(sortedUnique(expectedGrants))) {
      throw new Error(`Sheet combat certification Magic Initiate grants differ for ${row.actionId}`);
    }
  }
}
async function certifySheetCombatArtifact(input) {
  const raw = requiredObject(input, "artifact");
  if (raw.schemaVersion !== SHEET_COMBAT_CERTIFICATION_SCHEMA_VERSION || raw.artifactVersion !== SHEET_COMBAT_CERTIFICATION_ARTIFACT_VERSION) {
    throw new Error("Sheet combat certification schema/version is unsupported");
  }
  validateArtifactEnvelopeShape(raw);
  if (!SHA256.test(String(raw.sourceProjectionHash ?? "")) || !SHA256.test(String(raw.contentHash ?? ""))) {
    throw new Error("Sheet combat certification hashes are not pinned");
  }
  const artifact = clone(input);
  const expectedContentHash = await canonicalSha256(sheetCombatCertificationContent(artifact));
  if (expectedContentHash !== artifact.contentHash) {
    throw new Error("Sheet combat certification content hash mismatch");
  }
  const expectedSourceHash = await canonicalSha256(
    sheetCombatCertificationSourceProjection(artifact)
  );
  if (expectedSourceHash !== artifact.sourceProjectionHash) {
    throw new Error("Sheet combat certification source projection hash mismatch");
  }
  validateRulesetAndRelease(artifact);
  validateArtifactDenominator(artifact);
  validateArtifactActions(artifact);
  validateAccessAndPreparation(artifact);
  validateMagicInitiateProvenance(artifact);
  const actions = artifact.actions.map(clone);
  const byId = new Map(actions.map((action) => [action.id, action]));
  return {
    ruleset: clone(artifact.source.ruleset),
    actions,
    catalog: {
      getAction: (id) => byId.get(id),
      listActions: () => actions
    },
    accessSignaturesByAction: clone(artifact.accessSignaturesByAction),
    preparedSourceProfiles: clone(artifact.preparedSourceProfiles),
    artifact
  };
}
async function loadCertifiedSheetCombatCatalog() {
  return certifySheetCombatArtifact(sheetCombatCertification_generated_default);
}
function assertCertifiedPreparedSources(actor2, certified) {
  const access = actor2.spellcastingAccess;
  const preparedEntries = Object.entries(access?.preparedSources ?? {});
  const actualSourceIds = preparedEntries.map(([sourceId]) => sourceId).sort();
  const spellbookBySource = /* @__PURE__ */ new Map();
  for (const grant of access?.grants ?? []) {
    if (grant.access !== "spellbook") continue;
    const actionIds = spellbookBySource.get(grant.sourceId) ?? [];
    actionIds.push(grant.actionId);
    spellbookBySource.set(grant.sourceId, actionIds);
  }
  const expectedSourceIds = [...spellbookBySource.keys()].sort();
  if (canonicalStringify(actualSourceIds) !== canonicalStringify(expectedSourceIds)) {
    throw new Error(`Actor ${actor2.id} prepared source set differs from certified spellbook access`);
  }
  const certifiedProfiles = new Set(certified.preparedSourceProfiles.map(canonicalStringify));
  for (const [sourceKey2, source2] of preparedEntries) {
    if (!source2 || source2.sourceId !== sourceKey2) {
      throw new Error(`Actor ${actor2.id} prepared source ${sourceKey2} has invalid identity`);
    }
    if (new Set(source2.availableActionIds).size !== source2.availableActionIds.length || new Set(source2.preparedActionIds).size !== source2.preparedActionIds.length || source2.preparedActionIds.length !== source2.capacity) {
      throw new Error(`Actor ${actor2.id} prepared source ${sourceKey2} has invalid capacity or duplicates`);
    }
    const projected = projectCertifiedPreparedSource(source2);
    const grantedActionIds = sortedUnique(spellbookBySource.get(sourceKey2) ?? []);
    if (canonicalStringify(projected.availableActionIds) !== canonicalStringify(grantedActionIds)) {
      throw new Error(`Actor ${actor2.id} available spells for ${sourceKey2} differ from its grants`);
    }
    if (projected.preparedActionIds.some((actionId) => !projected.availableActionIds.includes(actionId))) {
      throw new Error(`Actor ${actor2.id} prepares an unavailable spell for ${sourceKey2}`);
    }
    if (!certifiedProfiles.has(canonicalStringify(projected))) {
      throw new Error(`Actor ${actor2.id} prepared source ${sourceKey2} differs from certified access`);
    }
  }
}
function assertCertifiedSheetCombatActorAccess(actor2, actionIds, certified) {
  if (new Set(actionIds).size !== actionIds.length) {
    throw new Error(`Actor ${actor2.id} has duplicate certified combat action IDs`);
  }
  assertCertifiedPreparedSources(actor2, certified);
  for (const actionId of actionIds) {
    const action = certified.catalog.getAction(actionId);
    if (!action) throw new Error(`Action ${actionId} is outside certified combat access`);
    if (!actor2.capabilities.actionIds.includes(actionId)) {
      throw new Error(`Actor ${actor2.id} capability set does not grant ${actionId}`);
    }
    if (action.kind !== "spell") continue;
    const expected = new Set(
      (certified.accessSignaturesByAction[actionId] ?? []).map(canonicalStringify)
    );
    const actual = projectCertifiedActorAccess(actor2, actionId);
    if (!actual.grants.length || !expected.has(canonicalStringify(actual))) {
      throw new Error(`Actor ${actor2.id} access for ${actionId} differs from certified access`);
    }
  }
}

// src/rules-core/worldObjects.ts
function cloneObject(object5) {
  return JSON.parse(JSON.stringify(object5));
}
function cloneObjects(objects) {
  return Object.fromEntries(Object.entries(objects).map(([id, object5]) => [id, cloneObject(object5)]));
}
var REQUIRED_WORLD_OBJECT_KEYS = /* @__PURE__ */ new Set(["id", "name", "kind", "size"]);
function itemInstanceIssue(object5) {
  if (object5.itemCardId !== void 0 && (object5.kind !== "item" || typeof object5.itemCardId !== "string" || !object5.itemCardId.trim())) {
    return "World object itemCardId requires a non-blank item identity";
  }
  if (object5.attunedToActorId !== void 0 && (object5.kind !== "item" || typeof object5.attunedToActorId !== "string" || !object5.attunedToActorId.trim())) {
    return "World object attunement requires a non-blank item owner";
  }
  if (object5.heldByActorId === void 0 !== (object5.heldInHand === void 0) || object5.heldByActorId !== void 0 && (typeof object5.heldByActorId !== "string" || !object5.heldByActorId.trim() || !["main_hand", "off_hand"].includes(object5.heldInHand ?? "") || object5.carriedByActorId !== object5.heldByActorId)) {
    return "World object held identity must match its canonical carrier and hand";
  }
  return null;
}
function worldObjectLedgerIssue(objects, actorIds) {
  const occupiedHands = /* @__PURE__ */ new Set();
  for (const object5 of Object.values(objects)) {
    const itemIssue = itemInstanceIssue(object5);
    if (itemIssue) return `${object5.id}: ${itemIssue}`;
    if (object5.attunedToActorId && actorIds && !actorIds.has(object5.attunedToActorId)) {
      return `${object5.id}: attunement owner is not a world actor`;
    }
    if (!object5.heldByActorId || !object5.heldInHand) continue;
    if (actorIds && !actorIds.has(object5.heldByActorId)) {
      return `${object5.id}: holder is not a world actor`;
    }
    const key = `${object5.heldByActorId}\0${object5.heldInHand}`;
    if (occupiedHands.has(key)) {
      return `${object5.id}: canonical held-item hand is already occupied`;
    }
    occupiedHands.add(key);
  }
  return null;
}
function evolveWorldObjectEvent(objects, event) {
  const next = cloneObjects(objects);
  if (event.type === "WorldObjectCreated") {
    if (next[event.object.id]) throw new Error(`World object ${event.object.id} already exists`);
    const issue2 = itemInstanceIssue(event.object);
    if (issue2) throw new Error(issue2);
    next[event.object.id] = cloneObject(event.object);
    const ledgerIssue2 = worldObjectLedgerIssue(next);
    if (ledgerIssue2) throw new Error(ledgerIssue2);
    return next;
  }
  const current = next[event.objectId];
  if (!current) throw new Error(`Unknown world object ${event.objectId}`);
  if (event.type === "WorldObjectRemoved") {
    delete next[event.objectId];
    return next;
  }
  if (event.type === "WorldObjectObserved") return next;
  if (event.patch.id !== void 0 && event.patch.id !== event.objectId) {
    throw new Error("A world-object patch cannot change object identity");
  }
  if (Object.hasOwn(event.patch, "itemCardId") && event.patch.itemCardId !== current.itemCardId || event.unset?.includes("itemCardId")) {
    throw new Error("A world-object patch cannot change or unset immutable itemCardId");
  }
  const patched = { ...current, ...JSON.parse(JSON.stringify(event.patch)) };
  for (const key of event.unset ?? []) {
    if (REQUIRED_WORLD_OBJECT_KEYS.has(key)) {
      throw new Error(`A world-object patch cannot unset required field ${key}`);
    }
    delete patched[key];
  }
  const issue = itemInstanceIssue(patched);
  if (issue) throw new Error(issue);
  next[event.objectId] = patched;
  const ledgerIssue = worldObjectLedgerIssue(next);
  if (ledgerIssue) throw new Error(ledgerIssue);
  return next;
}
function validFacts(facts) {
  return Number.isInteger(facts.boardRevision) && facts.boardRevision >= 0 && Number.isFinite(facts.distanceFt) && facts.distanceFt >= 0;
}
function advanceWorldObjectRounds(input) {
  if (!Number.isInteger(input.rounds) || input.rounds < 0) {
    throw new Error("World-object time passage requires a non-negative integer round count");
  }
  const objects = cloneObjects(input.objects);
  const events = [];
  if (input.rounds === 0) return { objects, events };
  for (const object5 of Object.values(objects).sort((left, right) => left.id.localeCompare(right.id))) {
    if (object5.roundsLeft !== void 0) {
      if (object5.roundsLeft <= input.rounds) {
        delete objects[object5.id];
        events.push({
          type: "WorldObjectRemoved",
          objectId: object5.id,
          reason: "duration_expired"
        });
        continue;
      }
      object5.roundsLeft -= input.rounds;
      events.push({
        type: "WorldObjectPatched",
        objectId: object5.id,
        patch: { roundsLeft: object5.roundsLeft },
        reason: "duration_advanced"
      });
    }
    if (object5.prestidigitation?.length) {
      const remaining = object5.prestidigitation.filter((attachment) => attachment.roundsLeft > input.rounds).map((attachment) => ({
        ...attachment,
        roundsLeft: attachment.roundsLeft - input.rounds
      }));
      if (remaining.length !== object5.prestidigitation.length) {
        if (remaining.length) object5.prestidigitation = remaining;
        else delete object5.prestidigitation;
        events.push({
          type: "WorldObjectPatched",
          objectId: object5.id,
          patch: remaining.length ? { prestidigitation: remaining } : {},
          ...remaining.length ? {} : { unset: ["prestidigitation"] },
          reason: "prestidigitation_duration_expired"
        });
      } else {
        object5.prestidigitation = remaining;
        events.push({
          type: "WorldObjectPatched",
          objectId: object5.id,
          patch: { prestidigitation: remaining },
          reason: "prestidigitation_duration_advanced"
        });
      }
    }
    if (!object5.illumination) continue;
    if (object5.illumination.roundsLeft <= input.rounds) {
      delete object5.illumination;
      events.push({
        type: "WorldObjectPatched",
        objectId: object5.id,
        patch: {},
        unset: ["illumination"],
        reason: "light_duration_expired"
      });
      continue;
    }
    object5.illumination.roundsLeft -= input.rounds;
    events.push({
      type: "WorldObjectPatched",
      objectId: object5.id,
      patch: { illumination: cloneObject(object5).illumination },
      reason: "light_duration_advanced"
    });
  }
  return { objects, events };
}
function attachLight(input) {
  const target2 = input.objects[input.targetObjectId];
  if (!target2) throw new Error(`Unknown world object ${input.targetObjectId}`);
  if (!validFacts(input.facts) || input.facts.distanceFt > input.targeting.rangeFt || input.targeting.requiresTouch && (input.facts.touched !== true || input.facts.distanceFt !== 0)) {
    throw new Error("Light requires explicit object facts within its declared targeting geometry");
  }
  const sizes = ["tiny", "small", "medium", "large", "huge", "gargantuan"];
  if (sizes.indexOf(target2.size) > sizes.indexOf(input.policy.maxObjectSize)) {
    throw new Error(`Light cannot target an object larger than ${input.policy.maxObjectSize}`);
  }
  if (input.policy.excludeCarriedByOther && target2.carriedByActorId && target2.carriedByActorId !== input.sourceActorId) {
    throw new Error("Light cannot target an object worn or carried by someone else");
  }
  const objects = cloneObjects(input.objects);
  const events = [];
  const existing = Object.values(objects).filter((object5) => object5.illumination?.sourceActorId === input.sourceActorId && object5.illumination.sourceActionId === input.sourceActionId).sort((left, right) => left.id.localeCompare(right.id));
  const replacements = existing.slice(0, Math.max(0, existing.length - input.policy.maxActivePerSource + 1));
  for (const object5 of replacements) {
    delete object5.illumination;
    events.push({
      type: "WorldObjectPatched",
      objectId: object5.id,
      patch: {},
      unset: ["illumination"],
      reason: "light_replaced"
    });
  }
  const illumination = {
    id: input.attachmentId,
    sourceActorId: input.sourceActorId,
    sourceActionId: input.sourceActionId,
    brightRadiusFt: input.policy.brightRadiusFt,
    dimAdditionalRadiusFt: input.policy.dimAdditionalRadiusFt,
    roundsLeft: input.policy.durationRounds
  };
  objects[target2.id] = { ...objects[target2.id], illumination };
  events.push({
    type: "WorldObjectPatched",
    objectId: target2.id,
    patch: { illumination },
    reason: "light_attached"
  });
  return { objects, events };
}
function igniteBurningHandsObjects(input) {
  const objects = cloneObjects(input.objects);
  const events = [];
  for (const objectId of Object.keys(input.factsByObject).sort()) {
    const object5 = objects[objectId];
    const facts = input.factsByObject[objectId];
    const areaSizeFt = input.targeting.area?.sizeFt;
    if (!object5 || !facts || !validFacts(facts) || facts.distanceFt > input.targeting.rangeFt || areaSizeFt !== void 0 && facts.distanceFt > areaSizeFt || input.policy.requireInArea && facts.inArea !== true) continue;
    if (input.policy.requireFlammable && !object5.flammable || input.policy.excludeCarried && object5.carriedByActorId || object5.ignited) continue;
    object5.ignited = true;
    events.push({
      type: "WorldObjectPatched",
      objectId,
      patch: { ignited: true },
      reason: "burning_hands_ignition"
    });
  }
  return { objects, events };
}
function pushWorldObjects(input) {
  if (!Number.isFinite(input.policy.distanceFt) || input.policy.distanceFt <= 0 || !Number.isFinite(input.policy.maxObjectDistanceFt) || input.policy.maxObjectDistanceFt <= 0 || input.policy.areaRequirement !== "entirely_in_area") {
    throw new Error("Forced object push policy is malformed");
  }
  const objects = cloneObjects(input.objects);
  const events = [];
  for (const objectId of Object.keys(input.factsByObject).sort()) {
    const object5 = objects[objectId];
    const facts = input.factsByObject[objectId];
    if (!object5 || !facts || !validFacts(facts) || facts.entirelyInArea !== true || facts.distanceFt > input.policy.maxObjectDistanceFt) continue;
    if (input.policy.excludeSecured && object5.secured === true || input.policy.excludeCarried && object5.carriedByActorId) continue;
    const displacementFt = (object5.displacementFt ?? 0) + input.policy.distanceFt;
    object5.displacementFt = displacementFt;
    events.push({
      type: "WorldObjectPatched",
      objectId,
      patch: { displacementFt },
      reason: "forced_object_push"
    });
  }
  return { objects, events };
}
function magicBlockedBy(layers, policy) {
  return layers.some((layer) => {
    if (!Number.isFinite(layer.thicknessInches) || layer.thicknessInches < 0) return true;
    const threshold = policy[layer.material];
    if (!threshold) return false;
    return threshold.comparison === "gte" ? layer.thicknessInches >= threshold.thresholdInches : layer.thicknessInches > threshold.thresholdInches;
  });
}
function observeDetectMagic(input) {
  const senseRangeFt = input.targeting.area?.radiusFt;
  if (senseRangeFt === void 0) throw new Error("Detect Magic requires a numeric sensing radius");
  const aura = input.object.magicalAura;
  const sensed = validFacts(input.facts) && input.facts.distanceFt <= senseRangeFt && !magicBlockedBy(input.blockingLayers, input.policy.blockers) && aura !== void 0;
  if (!sensed) return { sensed: false, auraVisible: false };
  const auraVisible = input.revealAura && (!input.policy.auraRequiresLineOfSight || input.facts.lineOfSight) && aura?.visible === true;
  return {
    sensed: true,
    auraVisible,
    ...auraVisible && (!input.policy.revealSpellSchoolOnly || aura?.createdBySpell === true) && aura.school ? { school: aura.school } : {}
  };
}
function createMinorIllusion(input) {
  if (input.objects[input.id]) throw new Error(`World object ${input.id} already exists`);
  if (!input.description.trim()) throw new Error("Minor Illusion description is required");
  if (!Number.isInteger(input.spellSaveDc) || input.spellSaveDc < 1) {
    throw new Error("Minor Illusion requires a positive integer spell save DC");
  }
  if (input.form === "image" && (!Number.isFinite(input.imageCubeSideFt) || input.imageCubeSideFt <= 0 || input.imageCubeSideFt > input.policy.imageMaxCubeSideFt)) {
    throw new Error(`Minor Illusion image requires an explicit cube side up to ${input.policy.imageMaxCubeSideFt} feet`);
  }
  const objects = cloneObjects(input.objects);
  const events = [];
  const existingIllusions = Object.values(objects).filter((existing) => existing.illusion && existing.sourceActorId === input.sourceActorId && existing.sourceActionId === input.sourceActionId).sort((left, right) => left.id.localeCompare(right.id));
  const replacements = existingIllusions.slice(
    0,
    Math.max(0, existingIllusions.length - input.policy.maxActivePerSource + 1)
  );
  for (const existing of replacements) {
    delete objects[existing.id];
    events.push({
      type: "WorldObjectRemoved",
      objectId: existing.id,
      reason: "minor_illusion_replaced"
    });
  }
  const object5 = {
    id: input.id,
    name: "Minor Illusion",
    kind: "spell_effect",
    size: "medium",
    sourceActorId: input.sourceActorId,
    sourceActionId: input.sourceActionId,
    roundsLeft: input.policy.durationRounds,
    magicalAura: { school: "illusion", createdBySpell: true, visible: true },
    illusion: {
      form: input.form,
      description: input.description.trim(),
      spellSaveDc: input.spellSaveDc,
      studyAbility: input.policy.studyAbility,
      studySkill: input.policy.studySkill,
      ...input.form === "image" ? { imageCubeSideFt: input.imageCubeSideFt } : {},
      discernedByActorIds: [],
      physicallyRevealedToActorIds: []
    }
  };
  return {
    objects: { ...objects, [input.id]: object5 },
    events: [...events, { type: "WorldObjectCreated", object: cloneObject(object5) }]
  };
}
function addObserver(values, actorId) {
  return [.../* @__PURE__ */ new Set([...values, actorId])].sort((left, right) => left.localeCompare(right));
}
function studyMinorIllusion(input) {
  const object5 = input.objects[input.objectId];
  if (!object5?.illusion) throw new Error(`${input.objectId} is not an illusion`);
  const objects = cloneObjects(input.objects);
  const events = [];
  if (input.checkTotal >= object5.illusion.spellSaveDc) {
    const discernedByActorIds = addObserver(object5.illusion.discernedByActorIds, input.actorId);
    objects[input.objectId].illusion = { ...objects[input.objectId].illusion, discernedByActorIds };
    events.push({
      type: "WorldObjectPatched",
      objectId: input.objectId,
      patch: { illusion: objects[input.objectId].illusion },
      reason: "minor_illusion_studied"
    });
    events.push({
      type: "WorldObjectObserved",
      objectId: input.objectId,
      actorId: input.actorId,
      observation: "illusion_discerned"
    });
  }
  return { objects, events };
}
function physicallyRevealMinorIllusion(input) {
  const object5 = input.objects[input.objectId];
  if (!object5?.illusion) throw new Error(`${input.objectId} is not an illusion`);
  if (object5.illusion.form !== "image") {
    throw new Error(`${input.objectId} is a sound illusion and cannot be physically revealed`);
  }
  const objects = cloneObjects(input.objects);
  const physicallyRevealedToActorIds = addObserver(
    object5.illusion.physicallyRevealedToActorIds,
    input.actorId
  );
  objects[input.objectId].illusion = { ...objects[input.objectId].illusion, physicallyRevealedToActorIds };
  return {
    objects,
    events: [
      {
        type: "WorldObjectPatched",
        objectId: input.objectId,
        patch: { illusion: objects[input.objectId].illusion },
        reason: "minor_illusion_physical_interaction"
      },
      {
        type: "WorldObjectObserved",
        objectId: input.objectId,
        actorId: input.actorId,
        observation: "illusion_physically_revealed"
      }
    ]
  };
}

// src/rules-core/attackSequence.ts
var UNARMED_STRIKE_OPTIONS = /* @__PURE__ */ new Set(["damage", "grapple", "shove"]);
var ATTACK_ENTRY_KINDS = /* @__PURE__ */ new Set([
  "weapon_attack",
  "unarmed_strike",
  "replacement"
]);
function nonBlank2(value) {
  return typeof value === "string" && value.trim().length > 0;
}
function attackSequenceInvariantHolds(state) {
  if (!nonBlank2(state.id) || !nonBlank2(state.actorId)) return false;
  if (!Number.isInteger(state.totalAttacks) || state.totalAttacks < 1) return false;
  if (!Number.isInteger(state.attacksRemaining) || state.attacksRemaining < 0 || state.attacksRemaining > state.totalAttacks) return false;
  if (!Array.isArray(state.entries) || !Array.isArray(state.usedReplacementKeys)) return false;
  if (state.entries.length !== state.totalAttacks - state.attacksRemaining) return false;
  if (state.entries.some((entry, index) => entry.ordinal !== index + 1 || !ATTACK_ENTRY_KINDS.has(entry.kind) || !nonBlank2(entry.actionId) || !Array.isArray(entry.sourceEntityIds) || !entry.sourceEntityIds.length || entry.sourceEntityIds.some((sourceId) => !nonBlank2(sourceId)) || new Set(entry.sourceEntityIds).size !== entry.sourceEntityIds.length || entry.kind === "weapon_attack" && entry.weaponCardId !== void 0 && !nonBlank2(entry.weaponCardId) || entry.kind === "replacement" && !nonBlank2(entry.replacementKey) || entry.kind === "unarmed_strike" && !UNARMED_STRIKE_OPTIONS.has(entry.option))) return false;
  if (state.usedReplacementKeys.some((key) => !nonBlank2(key))) return false;
  const replacementKeys = [...new Set(state.entries.flatMap((entry) => entry.kind === "replacement" ? [entry.replacementKey] : []))].sort();
  return JSON.stringify(replacementKeys) === JSON.stringify(state.usedReplacementKeys);
}
function beginAttackSequence(input) {
  if (!nonBlank2(input.id) || !nonBlank2(input.actorId)) {
    throw new Error("Attack action requires stable sequence and actor IDs");
  }
  if (!Number.isInteger(input.totalAttacks) || input.totalAttacks < 1) {
    throw new Error("Attack action requires a positive integer attack count");
  }
  return {
    id: input.id,
    actorId: input.actorId,
    totalAttacks: input.totalAttacks,
    attacksRemaining: input.totalAttacks,
    entries: [],
    usedReplacementKeys: []
  };
}
function appendEntry(state, entry) {
  if (!attackSequenceInvariantHolds(state)) throw new Error("Invalid attack sequence state");
  if (state.attacksRemaining < 1) throw new Error("Attack sequence has no remaining attacks");
  if (!nonBlank2(entry.actionId) || !entry.sourceEntityIds.length || entry.sourceEntityIds.some((sourceId) => !nonBlank2(sourceId)) || new Set(entry.sourceEntityIds).size !== entry.sourceEntityIds.length) {
    throw new Error("Attack sequence entry requires stable action and source IDs");
  }
  return {
    ...state,
    attacksRemaining: state.attacksRemaining - 1,
    entries: [...state.entries, {
      ...entry,
      ordinal: state.entries.length + 1,
      sourceEntityIds: [...entry.sourceEntityIds]
    }],
    usedReplacementKeys: [...state.usedReplacementKeys]
  };
}
function performSequenceAttack(input) {
  if (input.weaponCardId !== void 0 && !nonBlank2(input.weaponCardId)) {
    throw new Error("Weapon attack requires a stable weapon Card ID");
  }
  return appendEntry(input.sequence, {
    kind: "weapon_attack",
    actionId: input.actionId,
    sourceEntityIds: [...input.sourceEntityIds],
    ...input.weaponCardId === void 0 ? {} : { weaponCardId: input.weaponCardId }
  });
}
function performWeaponSequenceAttack(input) {
  return performSequenceAttack(input);
}
function performUnarmedStrike(input) {
  if (!UNARMED_STRIKE_OPTIONS.has(input.option)) {
    throw new Error(`Unsupported Unarmed Strike option ${String(input.option)}`);
  }
  return appendEntry(input.sequence, {
    kind: "unarmed_strike",
    actionId: input.actionId,
    option: input.option,
    sourceEntityIds: [...input.sourceEntityIds]
  });
}
function replaceSequenceAttack(input) {
  if (!attackSequenceInvariantHolds(input.sequence)) throw new Error("Invalid attack sequence state");
  if (!nonBlank2(input.replacementKey)) throw new Error("Attack replacement requires a stable key");
  if (input.oncePerSequence !== false && input.sequence.usedReplacementKeys.includes(input.replacementKey)) {
    throw new Error(`Attack replacement ${input.replacementKey} was already used in this Attack action`);
  }
  const next = appendEntry(input.sequence, {
    kind: "replacement",
    actionId: input.actionId,
    replacementKey: input.replacementKey,
    sourceEntityIds: [...input.sourceEntityIds]
  });
  return {
    ...next,
    usedReplacementKeys: [.../* @__PURE__ */ new Set([
      ...next.usedReplacementKeys,
      input.replacementKey
    ])].sort()
  };
}
function attackSequenceComplete(state) {
  return attackSequenceInvariantHolds(state) && state.attacksRemaining === 0;
}

// src/rules-core/systemActions.ts
var SYSTEM_ACTION_IDS = Object.freeze({
  attack: "core.action.attack",
  weaponAttack: "core.attack.weapon",
  lightExtraAttack: "core.bonus-action.light-extra-attack",
  unarmedDamage: "core.attack.unarmed.damage",
  unarmedGrapple: "core.attack.unarmed.grapple",
  unarmedShove: "core.attack.unarmed.shove",
  escapeGrapple: "core.action.escape-grapple",
  releaseGrapple: "core.free.release-grapple"
});
var immutable = (value) => Object.freeze(value);
var source = (slug) => immutable([
  `system:dnd5e-2024:${slug}`
]);
var target = (shape, rangeFt) => immutable({ shape, rangeFt });
var ATTACK_ACTION = immutable({
  id: SYSTEM_ACTION_IDS.attack,
  name: "Attack",
  rulesetSystemId: "dnd5e-2024",
  sourceEntityIds: source("attack-action"),
  targeting: target("self", 0),
  kind: "attack_action",
  timing: "action",
  actionCost: 1,
  attackBudgetSource: "compiled_actor_profile",
  weaponEquipAllowance: "one_before_or_after_each_attack",
  movementBetweenAttacks: true
});
var WEAPON_ATTACK = immutable({
  id: SYSTEM_ACTION_IDS.weaponAttack,
  name: "Weapon Attack",
  rulesetSystemId: "dnd5e-2024",
  sourceEntityIds: source("weapon-attack"),
  targeting: target("single_creature", "equipped_weapon"),
  kind: "attack_entry",
  timing: "attack_entry",
  entryKind: "weapon_attack",
  consumesAttacks: 1,
  resolution: "attack_roll",
  weaponRequirement: "owned_equipped_weapon",
  proficiencyRule: "weapon_proficiency"
});
var LIGHT_PROPERTY_EXTRA_ATTACK = immutable({
  id: SYSTEM_ACTION_IDS.lightExtraAttack,
  name: "Light Property: Extra Attack",
  rulesetSystemId: "dnd5e-2024",
  sourceEntityIds: source("light-property-extra-attack"),
  targeting: target("single_creature", "equipped_weapon"),
  kind: "light_property_extra_attack",
  timing: "bonus_action",
  bonusActionCost: 1,
  resolution: "attack_roll",
  qualifyingSource: "persisted_attack_action_light_weapon_entry",
  extraWeaponRequirement: "different_owned_equipped_light_weapon",
  abilityModifierRule: "omit_unless_two_weapon_fighting",
  maximumPerAttackAction: 1
});
var UNARMED_DAMAGE = immutable({
  id: SYSTEM_ACTION_IDS.unarmedDamage,
  name: "Unarmed Strike: Damage",
  rulesetSystemId: "dnd5e-2024",
  sourceEntityIds: source("unarmed-strike:damage"),
  targeting: target("single_creature", 5),
  kind: "attack_entry",
  timing: "attack_entry",
  entryKind: "unarmed_strike",
  consumesAttacks: 1,
  unarmedOption: "damage",
  resolution: "attack_roll",
  attackAbility: "str",
  proficiencyRule: "always",
  damageFormula: "max(0,1+str)",
  damageType: "bludgeoning",
  requiresFreeHand: false
});
var STR_DEX_SAVE_OPTIONS = immutable(["str", "dex"]);
var UNARMED_GRAPPLE = immutable({
  id: SYSTEM_ACTION_IDS.unarmedGrapple,
  name: "Unarmed Strike: Grapple",
  rulesetSystemId: "dnd5e-2024",
  sourceEntityIds: source("unarmed-strike:grapple"),
  targeting: target("single_creature", 5),
  kind: "attack_entry",
  timing: "attack_entry",
  entryKind: "unarmed_strike",
  consumesAttacks: 1,
  maxTargetSizeDifference: 1,
  unarmedOption: "grapple",
  resolution: "saving_throw",
  saveAbilityOptions: STR_DEX_SAVE_OPTIONS,
  saveDcFormula: "8+str+prof",
  targetMayVoluntarilyFailSave: true,
  requiresFreeHand: true,
  grappleCapacityPerPart: 1,
  failedSaveEffect: "grappled",
  escapeDcFormula: "8+str+prof",
  automaticEndTriggers: immutable([
    "grappler_incapacitated",
    "distance_exceeds_range"
  ])
});
var UNARMED_SHOVE = immutable({
  id: SYSTEM_ACTION_IDS.unarmedShove,
  name: "Unarmed Strike: Shove",
  rulesetSystemId: "dnd5e-2024",
  sourceEntityIds: source("unarmed-strike:shove"),
  targeting: target("single_creature", 5),
  kind: "attack_entry",
  timing: "attack_entry",
  entryKind: "unarmed_strike",
  consumesAttacks: 1,
  maxTargetSizeDifference: 1,
  unarmedOption: "shove",
  resolution: "saving_throw",
  saveAbilityOptions: STR_DEX_SAVE_OPTIONS,
  saveDcFormula: "8+str+prof",
  targetMayVoluntarilyFailSave: true,
  requiresFreeHand: false,
  failedSaveChoices: immutable(["push_5ft", "prone"])
});
var ESCAPE_GRAPPLE = immutable({
  id: SYSTEM_ACTION_IDS.escapeGrapple,
  name: "Escape Grapple",
  rulesetSystemId: "dnd5e-2024",
  sourceEntityIds: source("escape-grapple"),
  targeting: target("self", 0),
  kind: "grapple_lifecycle",
  timing: "action",
  actionCost: 1,
  resolution: "ability_check",
  skillOptions: immutable(["athletics", "acrobatics"]),
  dcSource: "persisted_grapple_escape_dc"
});
var RELEASE_GRAPPLE = immutable({
  id: SYSTEM_ACTION_IDS.releaseGrapple,
  name: "Release Grapple",
  rulesetSystemId: "dnd5e-2024",
  sourceEntityIds: source("release-grapple"),
  targeting: target("grapple_relation", 0),
  kind: "grapple_lifecycle",
  timing: "free",
  actionCost: 0,
  resolution: "automatic",
  controller: "grappler",
  allowedAtAnyTime: true
});
var SYSTEM_ACTION_DEFINITIONS = immutable([
  ATTACK_ACTION,
  WEAPON_ATTACK,
  LIGHT_PROPERTY_EXTRA_ATTACK,
  UNARMED_DAMAGE,
  UNARMED_GRAPPLE,
  UNARMED_SHOVE,
  ESCAPE_GRAPPLE,
  RELEASE_GRAPPLE
]);
var SYSTEM_ACTION_BY_ID = new Map(
  SYSTEM_ACTION_DEFINITIONS.map((definition) => [definition.id, definition])
);
function getSystemActionDefinition(id) {
  return SYSTEM_ACTION_BY_ID.get(id);
}

// src/rules-core/findFamiliar.ts
var FIND_FAMILIAR_BASE_FORMS = [
  "bat",
  "cat",
  "frog",
  "hawk",
  "lizard",
  "octopus",
  "owl",
  "rat",
  "raven",
  "spider",
  "weasel"
];
var PACT_CHAIN_SPECIAL_FAMILIAR_FORMS = [
  "imp",
  "pseudodragon",
  "quasit",
  "skeleton",
  "slaad_tadpole",
  "sphinx_of_wonder",
  "sprite",
  "venomous_snake"
];
var FAMILIAR_SPIRIT_TYPES = ["celestial", "fey", "fiend"];
var PACT_CHAIN_ATTACK_REPLACEMENT_KEY = "pact-chain:familiar-attack";
function record3(value) {
  return value !== null && typeof value === "object" && !Array.isArray(value) ? value : null;
}
function exactKeys5(value, expected) {
  const actual = Object.keys(value).sort();
  const wanted = [...expected].sort();
  return actual.length === wanted.length && actual.every((key, index) => key === wanted[index]);
}
function positiveFiniteNumber2(value) {
  return typeof value === "number" && Number.isFinite(value) && value > 0 ? value : null;
}
function positiveInteger2(value) {
  return typeof value === "number" && Number.isInteger(value) && value > 0 ? value : null;
}
function parseFindFamiliarMechanicsPolicy(mechanicsValue) {
  const mechanics = record3(mechanicsValue);
  const primitive = record3(mechanics?.primitive);
  if (!primitive || primitive.type !== "find_familiar") {
    return { status: "invalid", issue: "Find Familiar requires a find_familiar primitive" };
  }
  if (!exactKeys5(primitive, ["type", "materialCostResource", "policy"])) {
    return { status: "invalid", issue: "Find Familiar primitive has missing or unknown fields" };
  }
  const materialCostResource = typeof primitive.materialCostResource === "string" && primitive.materialCostResource.trim() === primitive.materialCostResource && primitive.materialCostResource.length > 0 ? primitive.materialCostResource : null;
  const raw = record3(primitive.policy);
  if (!materialCostResource || !raw || !exactKeys5(raw, [
    "connection_range_ft",
    "reappear_range_ft",
    "ritual_casting_added_seconds"
  ])) {
    return { status: "invalid", issue: "Find Familiar requires an exact material resource and policy" };
  }
  const connectionRangeFt = positiveFiniteNumber2(raw.connection_range_ft);
  const reappearRangeFt = positiveFiniteNumber2(raw.reappear_range_ft);
  const ritualCastingAddedSeconds = positiveInteger2(raw.ritual_casting_added_seconds);
  if (connectionRangeFt === null || reappearRangeFt === null || ritualCastingAddedSeconds === null) {
    return { status: "invalid", issue: "Find Familiar policy values must be positive finite ranges and whole seconds" };
  }
  return {
    status: "valid",
    materialCostResource,
    policy: { connectionRangeFt, reappearRangeFt, ritualCastingAddedSeconds }
  };
}
function validMechanicsPolicy(value) {
  const connectionRangeFt = positiveFiniteNumber2(value?.connectionRangeFt);
  const reappearRangeFt = positiveFiniteNumber2(value?.reappearRangeFt);
  const ritualCastingAddedSeconds = positiveInteger2(value?.ritualCastingAddedSeconds);
  if (connectionRangeFt === null || reappearRangeFt === null || ritualCastingAddedSeconds === null) {
    throw new Error("Find Familiar mechanics policy is invalid");
  }
  return { connectionRangeFt, reappearRangeFt, ritualCastingAddedSeconds };
}
function clone2(value) {
  return JSON.parse(JSON.stringify(value));
}
function stableId(value, label) {
  const normalized3 = value.trim();
  if (!normalized3) throw new Error(`${label} requires a stable non-empty id`);
  return normalized3;
}
function finiteNonNegative2(value, label) {
  if (!Number.isFinite(value) || value < 0) throw new Error(`${label} must be finite and non-negative`);
  return value;
}
function nonNegativeInteger3(value, label) {
  if (!Number.isInteger(value) || value < 0) throw new Error(`${label} must be a non-negative integer`);
  return value;
}
function policySource(policy) {
  if (policy.kind !== "base" && policy.kind !== "pact_chain") {
    throw new Error("Find Familiar has an unknown extension policy");
  }
  return stableId(policy.sourceEntityId, "Find Familiar policy source");
}
function titleFromId(id) {
  return id.split("_").map((part) => `${part.charAt(0).toUpperCase()}${part.slice(1)}`).join(" ");
}
function injectedFormsById(forms2) {
  const result = /* @__PURE__ */ new Map();
  const reserved = /* @__PURE__ */ new Set([
    ...FIND_FAMILIAR_BASE_FORMS,
    ...PACT_CHAIN_SPECIAL_FAMILIAR_FORMS
  ]);
  for (const value of forms2) {
    const candidate = record3(value);
    if (!candidate || typeof candidate.id !== "string" || typeof candidate.name !== "string" || typeof candidate.statBlockId !== "string") {
      throw new Error("Injected familiar form requires stable id, name, and stat block");
    }
    const id = stableId(candidate.id, "Injected familiar form");
    const name = candidate.name.trim();
    const statBlockId = stableId(candidate.statBlockId, `${id} stat block`);
    if (!name) throw new Error(`${id} requires a non-empty familiar form name`);
    if (candidate.creatureType !== "beast" || candidate.challengeRating !== 0) {
      throw new Error(`${id} must be a CR 0 Beast`);
    }
    if (reserved.has(id)) throw new Error(`Injected familiar form ${id} collides with a reserved form`);
    if (result.has(id)) throw new Error(`Injected familiar form ${id} is duplicated`);
    result.set(id, {
      id,
      name,
      statBlockId,
      creatureType: "beast",
      challengeRating: 0
    });
  }
  return result;
}
function injectedFamiliarFormCatalogIssue(value) {
  const catalog = record3(value);
  if (!catalog || catalog.schemaVersion !== 1 || typeof catalog.catalogId !== "string" || !catalog.catalogId.trim() || catalog.catalogId !== catalog.catalogId.trim()) {
    return "Injected familiar catalog requires schema version 1 and a canonical stable id";
  }
  if (!Array.isArray(catalog.forms)) return "Injected familiar catalog requires a forms array";
  let normalized3;
  try {
    normalized3 = [...injectedFormsById(catalog.forms).values()].sort((left, right) => left.id.localeCompare(right.id));
  } catch {
    return "Injected familiar catalog contains an invalid form";
  }
  for (const [index, expected] of normalized3.entries()) {
    const actual = record3(catalog.forms[index]);
    if (!actual || actual.id !== expected.id || actual.name !== expected.name || actual.statBlockId !== expected.statBlockId || actual.creatureType !== "beast" || actual.challengeRating !== 0) {
      return "Injected familiar catalog must be canonical and sorted by form id";
    }
  }
  return null;
}
function trustedInjectedCatalog(validation) {
  const catalog = validation?.injectedFormCatalog;
  if (!catalog) return void 0;
  const issue = injectedFamiliarFormCatalogIssue(catalog);
  if (issue) throw new Error(issue);
  return catalog;
}
function resolveFamiliarForm(input) {
  policySource(input.policy);
  const formId = stableId(input.formId, "Familiar form");
  const catalog = trustedInjectedCatalog(input.validation);
  const injected = new Map((catalog?.forms ?? []).map((form) => [form.id, form]));
  if (FIND_FAMILIAR_BASE_FORMS.includes(formId)) {
    return {
      id: formId,
      name: titleFromId(formId),
      statBlockId: `phb2024.beast.${formId}`,
      eligibility: "base_standard",
      baseCreatureType: "beast",
      challengeRating: 0
    };
  }
  const injectedForm = injected.get(formId);
  if (injectedForm) {
    return {
      id: injectedForm.id,
      name: injectedForm.name,
      statBlockId: injectedForm.statBlockId,
      eligibility: "injected_cr0_beast",
      baseCreatureType: "beast",
      challengeRating: 0,
      injectedFormProof: {
        catalogId: catalog.catalogId,
        formId: injectedForm.id,
        statBlockId: injectedForm.statBlockId
      }
    };
  }
  if (PACT_CHAIN_SPECIAL_FAMILIAR_FORMS.includes(formId)) {
    if (input.policy.kind !== "pact_chain") {
      throw new Error(`Familiar form ${formId} requires Pact of the Chain`);
    }
    return {
      id: formId,
      name: titleFromId(formId),
      statBlockId: `phb2024.pact_chain.${formId}`,
      eligibility: "pact_chain_special"
    };
  }
  throw new Error(`Unknown eligible familiar form ${formId}`);
}
function formIssue(value, extension, validation) {
  const form = record3(value);
  if (!form || typeof form.id !== "string" || !form.id.trim() || typeof form.name !== "string" || !form.name.trim() || typeof form.statBlockId !== "string" || !form.statBlockId.trim()) {
    return "Familiar form must retain stable identity and stat-block provenance";
  }
  if (form.eligibility === "base_standard") {
    if (!FIND_FAMILIAR_BASE_FORMS.includes(form.id) || form.statBlockId !== `phb2024.beast.${form.id}` || form.baseCreatureType !== "beast" || form.challengeRating !== 0 || form.injectedFormProof !== void 0) {
      return "Persisted standard familiar form is not a PHB CR 0 Beast";
    }
    return null;
  }
  if (form.eligibility === "injected_cr0_beast") {
    const reserved = FIND_FAMILIAR_BASE_FORMS.includes(form.id) || PACT_CHAIN_SPECIAL_FAMILIAR_FORMS.includes(form.id);
    if (reserved || form.baseCreatureType !== "beast" || form.challengeRating !== 0) {
      return "Persisted injected familiar form is not a CR 0 Beast";
    }
    const catalog = validation?.injectedFormCatalog;
    if (!catalog) return "Persisted injected familiar form requires a trusted validation catalog";
    const catalogIssue3 = injectedFamiliarFormCatalogIssue(catalog);
    if (catalogIssue3) return catalogIssue3;
    const proof = record3(form.injectedFormProof);
    const candidate = catalog.forms.find((entry) => entry.id === form.id);
    return proof && proof.catalogId === catalog.catalogId && proof.formId === form.id && proof.statBlockId === form.statBlockId && candidate?.name === form.name && candidate.statBlockId === form.statBlockId && candidate.creatureType === "beast" && candidate.challengeRating === 0 ? null : "Persisted injected familiar form does not match its trusted catalog proof";
  }
  if (form.eligibility === "pact_chain_special") {
    return extension === "pact_chain" && PACT_CHAIN_SPECIAL_FAMILIAR_FORMS.includes(form.id) && form.statBlockId === `phb2024.pact_chain.${form.id}` && form.injectedFormProof === void 0 ? null : "Persisted special familiar form requires Pact of the Chain";
  }
  return "Persisted familiar form has unknown eligibility";
}
function stableDistinctIds(value) {
  return Array.isArray(value) && value.every((id) => typeof id === "string" && !!id && id === id.trim()) && new Set(value).size === value.length;
}
function familiarStateIssue(value, validation) {
  const state = record3(value);
  if (!state || state.schemaVersion !== 1) return "Familiar state requires schema version 1";
  if (typeof state.actorId !== "string" || !state.actorId.trim() || typeof state.ownerActorId !== "string" || !state.ownerActorId.trim() || typeof state.sourceEntityId !== "string" || !state.sourceEntityId.trim() || state.actorId === state.ownerActorId) {
    return "Familiar state requires distinct stable actor, owner, and source identities";
  }
  if (state.extension !== "base" && state.extension !== "pact_chain") {
    return "Familiar state has an unknown extension policy";
  }
  const issue = formIssue(state.form, state.extension, validation);
  if (issue) return issue;
  if (!FAMILIAR_SPIRIT_TYPES.includes(state.spiritType)) {
    return "Familiar spirit type must be Celestial, Fey, or Fiend";
  }
  if (!["present", "pocket_dimension", "disappeared_zero_hp"].includes(String(state.presence))) {
    return "Familiar presence is invalid";
  }
  const initiative = record3(state.initiative);
  const emptyInitiative = initiative?.mode === "own" && initiative.d20Roll === null && initiative.modifier === null && initiative.total === null;
  const rolledInitiative = initiative?.mode === "own" && Number.isInteger(initiative.d20Roll) && Number(initiative.d20Roll) >= 1 && Number(initiative.d20Roll) <= 20 && Number.isInteger(initiative.modifier) && Number.isInteger(initiative.total) && Number(initiative.total) === Number(initiative.d20Roll) + Number(initiative.modifier);
  if (!emptyInitiative && !rolledInitiative) return "Familiar must retain a valid independent Initiative";
  if (typeof state.reactionAvailable !== "boolean") return "Familiar Reaction availability is invalid";
  if (state.sharedSenses !== null) {
    const senses = record3(state.sharedSenses);
    if (!senses || !Number.isInteger(senses.activatedOnOwnerTurn) || Number(senses.activatedOnOwnerTurn) < 0 || senses.expiresAtOwnerTurnStart !== Number(senses.activatedOnOwnerTurn) + 1 || senses.includesFormSpecialSenses !== true) {
      return "Familiar shared-senses expiry is invalid";
    }
  }
  const carriedItemIds = state.carriedItemIds;
  const wornItemIds = state.wornItemIds;
  if (!stableDistinctIds(carriedItemIds) || !stableDistinctIds(wornItemIds) || carriedItemIds.some((id) => wornItemIds.includes(id))) {
    return "Familiar equipment IDs must be stable, distinct, and in exactly one location";
  }
  if (state.presence !== "present" && (state.sharedSenses !== null || carriedItemIds.length > 0 || wornItemIds.length > 0)) {
    return "A non-present familiar cannot retain shared senses or equipment";
  }
  if (state.allyToOwnerAndAllies !== true || state.actsIndependently !== true || state.obeysOwnerCommands !== true || state.canAttackNormally !== false) {
    return "Familiar combat-role invariants are invalid";
  }
  return null;
}
function validState(value, validation) {
  const issue = familiarStateIssue(value, validation);
  if (issue) throw new Error(issue);
  return value;
}
function validPolicyForState(familiar, policy) {
  const sourceEntityId = policySource(policy);
  if (familiar.extension !== policy.kind || familiar.sourceEntityId !== sourceEntityId) {
    throw new Error("Familiar is not owned by this extension policy");
  }
  return sourceEntityId;
}
function requireOwner(familiar, ownerActorId, validation) {
  const owner = stableId(ownerActorId, "Familiar owner");
  if (validState(familiar, validation).ownerActorId !== owner) {
    throw new Error("Familiar is not owned by this actor");
  }
  return owner;
}
function requirePresent(familiar) {
  if (familiar.presence !== "present") throw new Error("Familiar must be present");
}
function initialFamiliar(input) {
  return {
    schemaVersion: 1,
    actorId: input.actorId,
    ownerActorId: input.ownerActorId,
    sourceEntityId: input.policy.sourceEntityId,
    extension: input.policy.kind,
    form: clone2(input.form),
    spiritType: input.spiritType,
    presence: "present",
    initiative: { mode: "own", d20Roll: null, modifier: null, total: null },
    reactionAvailable: true,
    sharedSenses: null,
    carriedItemIds: [],
    wornItemIds: [],
    allyToOwnerAndAllies: true,
    actsIndependently: true,
    obeysOwnerCommands: true,
    canAttackNormally: false
  };
}
function castFindFamiliar(input) {
  const actorId = stableId(input.familiarActorId, "Familiar actor");
  const ownerActorId = stableId(input.ownerActorId, "Familiar owner");
  if (actorId === ownerActorId) throw new Error("A familiar cannot be its own owner");
  const sourceEntityId = policySource(input.policy);
  const slots = nonNegativeInteger3(input.resources.level1SpellSlots, "Level-1 spell slots");
  const incense = finiteNonNegative2(input.resources.incenseGp, "Find Familiar incense");
  const offering = finiteNonNegative2(input.incenseOfferingGp, "Find Familiar incense offering");
  const materialCostGp = positiveInteger2(input.materialCostGp);
  const baseCastingTimeSeconds = positiveInteger2(input.baseCastingTimeSeconds);
  const mechanicsPolicy = validMechanicsPolicy(input.mechanicsPolicy);
  if (materialCostGp === null || baseCastingTimeSeconds === null) {
    throw new Error("Find Familiar requires positive declared material cost and casting time");
  }
  if (offering < materialCostGp || offering > incense) {
    throw new Error(`Find Familiar consumes an available incense offering worth at least ${materialCostGp} GP`);
  }
  if (!FAMILIAR_SPIRIT_TYPES.includes(input.spiritType)) {
    throw new Error("Find Familiar requires Celestial, Fey, or Fiend spirit type");
  }
  if (!["spell_slot", "ritual", "pact_chain_magic_action"].includes(input.method)) {
    throw new Error("Find Familiar has an unknown casting method");
  }
  if (input.method === "pact_chain_magic_action" && input.policy.kind !== "pact_chain") {
    throw new Error("Only Pact of the Chain grants the Magic-action Find Familiar cast");
  }
  const spellSlotsExpended = input.method === "spell_slot" ? 1 : 0;
  if (spellSlotsExpended && slots < 1) throw new Error("Find Familiar spell-slot cast requires a level-1 slot");
  const form = resolveFamiliarForm({
    formId: input.formId,
    policy: { ...input.policy, sourceEntityId },
    validation: input.validation
  });
  let familiar;
  let created = true;
  let changedForm = false;
  if (input.existingFamiliar) {
    const existing = validState(input.existingFamiliar, input.validation);
    if (existing.ownerActorId !== ownerActorId || existing.actorId !== actorId) {
      throw new Error("Recasting Find Familiar must transform the owner\u2019s existing familiar actor");
    }
    if (existing.spiritType !== input.spiritType) {
      throw new Error("Recasting Find Familiar changes form, not the familiar spirit type");
    }
    created = false;
    changedForm = existing.form.id !== form.id;
    familiar = {
      ...clone2(existing),
      sourceEntityId,
      extension: input.policy.kind,
      form: clone2(form),
      presence: "present"
    };
  } else {
    familiar = initialFamiliar({
      actorId,
      ownerActorId,
      policy: { ...input.policy, sourceEntityId },
      form,
      spiritType: input.spiritType
    });
  }
  validState(familiar, input.validation);
  const castingDuration = input.method === "pact_chain_magic_action" ? { kind: "magic_action" } : input.method === "ritual" ? {
    kind: "timed",
    baseSeconds: baseCastingTimeSeconds,
    ritualAddedSeconds: mechanicsPolicy.ritualCastingAddedSeconds,
    totalSeconds: baseCastingTimeSeconds + mechanicsPolicy.ritualCastingAddedSeconds
  } : {
    kind: "timed",
    baseSeconds: baseCastingTimeSeconds,
    ritualAddedSeconds: 0,
    totalSeconds: baseCastingTimeSeconds
  };
  return {
    familiar,
    resources: {
      level1SpellSlots: slots - spellSlotsExpended,
      incenseGp: incense - offering
    },
    consumedIncenseGp: offering,
    spellSlotsExpended,
    castingTime: input.method === "pact_chain_magic_action" ? "magic_action" : input.method === "ritual" ? "ritual" : "one_hour",
    castingDuration,
    created,
    changedForm
  };
}
function disappear(input) {
  const familiar = validState(input.familiar, input.validation);
  requirePresent(familiar);
  const droppedItemIds = [...familiar.carriedItemIds, ...familiar.wornItemIds].sort((left, right) => left.localeCompare(right));
  return {
    familiar: input.forever ? null : {
      ...clone2(familiar),
      presence: input.presence,
      sharedSenses: null,
      carriedItemIds: [],
      wornItemIds: []
    },
    droppedItemIds,
    reason: input.reason
  };
}
function familiarDropsToZeroHp(familiar, validation) {
  return disappear({
    familiar,
    presence: "disappeared_zero_hp",
    reason: "zero_hp",
    forever: false,
    validation
  });
}
function dismissFamiliar(input) {
  requireOwner(input.familiar, input.ownerActorId, input.validation);
  if (input.mode !== "temporary" && input.mode !== "forever") {
    throw new Error("Familiar dismissal mode must be temporary or forever");
  }
  return disappear({
    familiar: input.familiar,
    presence: "pocket_dimension",
    reason: input.mode === "temporary" ? "temporary_dismissal" : "forever_dismissal",
    forever: input.mode === "forever",
    validation: input.validation
  });
}
function reappearFamiliar(input) {
  requireOwner(input.familiar, input.ownerActorId, input.validation);
  const distance = finiteNonNegative2(input.distanceFt, "Familiar reappearance distance");
  const policy = validMechanicsPolicy(input.mechanicsPolicy);
  if (input.familiar.presence !== "pocket_dimension") {
    throw new Error("Only a temporarily dismissed familiar can reappear");
  }
  if (distance > policy.reappearRangeFt || input.unoccupiedSpace !== true) {
    throw new Error(`Familiar must reappear in an unoccupied space within ${policy.reappearRangeFt} feet`);
  }
  return { ...clone2(input.familiar), presence: "present" };
}
function activateFamiliarSharedSenses(input) {
  requireOwner(input.familiar, input.ownerActorId, input.validation);
  requirePresent(input.familiar);
  const distance = finiteNonNegative2(input.distanceFt, "Familiar shared-senses distance");
  const ownerTurn = nonNegativeInteger3(input.ownerTurn, "Owner turn");
  const policy = validMechanicsPolicy(input.mechanicsPolicy);
  if (distance > policy.connectionRangeFt) {
    throw new Error(`Familiar shared senses require the familiar within ${policy.connectionRangeFt} feet`);
  }
  return {
    ...clone2(input.familiar),
    sharedSenses: {
      activatedOnOwnerTurn: ownerTurn,
      expiresAtOwnerTurnStart: ownerTurn + 1,
      includesFormSpecialSenses: true
    }
  };
}
function startOwnerTurnForFamiliar(input) {
  requireOwner(input.familiar, input.ownerActorId, input.validation);
  const ownerTurn = nonNegativeInteger3(input.ownerTurn, "Owner turn");
  if (!input.familiar.sharedSenses || ownerTurn < input.familiar.sharedSenses.expiresAtOwnerTurnStart) {
    return clone2(input.familiar);
  }
  return { ...clone2(input.familiar), sharedSenses: null };
}
function setFamiliarInitiative(input) {
  validState(input.familiar, input.validation);
  requirePresent(input.familiar);
  if (stableId(input.familiarActorId, "Familiar Initiative actor") !== input.familiar.actorId) {
    throw new Error("Familiar must roll its own Initiative");
  }
  if (!Number.isInteger(input.d20Roll) || input.d20Roll < 1 || input.d20Roll > 20 || !Number.isInteger(input.modifier)) {
    throw new Error("Familiar Initiative requires an explicit d20 roll and integer modifier");
  }
  return {
    ...clone2(input.familiar),
    initiative: {
      mode: "own",
      d20Roll: input.d20Roll,
      modifier: input.modifier,
      total: input.d20Roll + input.modifier
    }
  };
}
function startFamiliarTurn(input) {
  validState(input.familiar, input.validation);
  requirePresent(input.familiar);
  if (stableId(input.familiarActorId, "Familiar turn actor") !== input.familiar.actorId) {
    throw new Error("Only the familiar\u2019s own turn refreshes its Reaction");
  }
  return { ...clone2(input.familiar), reactionAvailable: true };
}
function deliverTouchSpellThroughFamiliar(input) {
  const ownerActorId = requireOwner(input.familiar, input.ownerActorId, input.validation);
  requirePresent(input.familiar);
  const distance = finiteNonNegative2(input.distanceFt, "Familiar touch-delivery distance");
  const spellActionId = stableId(input.spellActionId, "Touch spell action");
  const policy = validMechanicsPolicy(input.mechanicsPolicy);
  if (input.spellRange !== "touch") throw new Error("Familiar can deliver only a Touch-range spell");
  if (distance > policy.connectionRangeFt) {
    throw new Error(`Familiar touch delivery requires the familiar within ${policy.connectionRangeFt} feet`);
  }
  if (!input.familiar.reactionAvailable) throw new Error("Familiar Reaction is unavailable");
  return {
    familiar: { ...clone2(input.familiar), reactionAvailable: false },
    delivery: {
      spellActionId,
      casterActorId: ownerActorId,
      deliveryActorId: input.familiar.actorId,
      range: "touch",
      reactionSpent: true
    }
  };
}
function substitutePactChainFamiliarAttack(input) {
  const ownerActorId = requireOwner(input.familiar, input.ownerActorId, input.validation);
  const sourceEntityId = validPolicyForState(input.familiar, input.policy);
  requirePresent(input.familiar);
  if (input.sequence.actorId !== ownerActorId) {
    throw new Error("Pact Chain can replace only its owner\u2019s Attack-action attack");
  }
  if (!input.familiar.reactionAvailable) throw new Error("Familiar Reaction is unavailable");
  const familiarAttackActionId = stableId(input.familiarAttackActionId, "Familiar attack action");
  const sequence = replaceSequenceAttack({
    sequence: input.sequence,
    actionId: familiarAttackActionId,
    replacementKey: PACT_CHAIN_ATTACK_REPLACEMENT_KEY,
    sourceEntityIds: [sourceEntityId, input.familiar.form.statBlockId]
  });
  return {
    sequence,
    familiar: { ...clone2(input.familiar), reactionAvailable: false },
    attackingActorId: input.familiar.actorId,
    reactionSpent: true
  };
}

// src/rules-core/familiarActorCatalog.ts
var A = (id, name, bonus, damage, mode = "melee", range = [5]) => ({
  id,
  name,
  economy: "action",
  kind: "attack",
  offensive: true,
  attack: mode === "melee" ? { mode, bonus, reachFt: range[0], damage } : { mode, bonus, normalRangeFt: range[0], longRangeFt: range[1], damage }
});
var D = (average, type, formula) => ({ average, ...formula ? { formula } : {}, type });
var ABILITIES2 = ["str", "dex", "con", "int", "wis", "cha"];
var stats = (scores, mods, saves) => ({
  abilityScores: Object.fromEntries(ABILITIES2.map((key, index) => [key, scores[index]])),
  abilityMods: Object.fromEntries(ABILITIES2.map((key, index) => [key, mods[index]])),
  saves: Object.fromEntries(ABILITIES2.map((key, index) => [key, saves[index]]))
});
var trait = (id, mechanics = {}) => ({ id, mechanics });
var base = (formId, name, data) => ({
  formId,
  name,
  eligibility: "base_standard",
  statBlockId: `mm2025.creature.${formId}`,
  selectionStatBlockId: `phb2024.beast.${formId}`,
  sourceEntityId: `official.dnd2024.mm2025.${formId}`,
  sourceLocator: `https://www.dndbeyond.com/sources/dnd/br-2024/creature-stat-blocks#${formId}`,
  proficiencyBonus: 2,
  ...data
});
var chain = (formId, name, data, sourceLocator) => ({
  formId,
  name,
  eligibility: "pact_chain_special",
  statBlockId: `mm2025.creature.${formId}`,
  selectionStatBlockId: `phb2024.pact_chain.${formId}`,
  sourceEntityId: `official.dnd2024.mm2025.${formId}`,
  sourceLocator: sourceLocator ?? `https://www.dndbeyond.com/sources/dnd/br-2024/creature-stat-blocks#${formId}`,
  proficiencyBonus: 2,
  ...data
});
var emptyDefenses = {
  resistances: [],
  vulnerabilities: [],
  damageImmunities: [],
  conditionImmunities: [],
  gear: []
};
var forms = [
  base("bat", "Bat", { size: "tiny", nativeCreatureType: "beast", ac: 12, hp: { max: 1, formula: "1d4-1" }, ...stats([2, 15, 8, 2, 12, 4], [-4, 2, -1, -4, 1, -3], [-4, 2, -1, -4, 1, -3]), initiativeModifier: 2, speeds: { walk: 5, fly: 30 }, skills: {}, senses: { passivePerception: 11, blindsightFt: 60 }, languages: [], ...emptyDefenses, traits: [], actions: [A("mm2025.bat.bite", "Bite", 4, [D(1, "piercing")])] }),
  base("cat", "Cat", { size: "tiny", nativeCreatureType: "beast", ac: 12, hp: { max: 2, formula: "1d4" }, ...stats([3, 15, 10, 3, 12, 7], [-4, 2, 0, -4, 1, -2], [-4, 4, 0, -4, 1, -2]), initiativeModifier: 2, speeds: { walk: 40, climb: 40 }, skills: { perception: 3, stealth: 4 }, senses: { passivePerception: 13, darkvisionFt: 60 }, languages: [], ...emptyDefenses, traits: [trait("jumper", { jumpAbility: "dex" })], actions: [A("mm2025.cat.scratch", "Scratch", 4, [D(1, "slashing")])] }),
  base("frog", "Frog", { size: "tiny", nativeCreatureType: "beast", ac: 11, hp: { max: 1, formula: "1d4-1" }, ...stats([1, 13, 8, 1, 8, 3], [-5, 1, -1, -5, -1, -4], [-5, 1, -1, -5, -1, -4]), initiativeModifier: 1, speeds: { walk: 20, swim: 20 }, skills: { perception: 1, stealth: 3 }, senses: { passivePerception: 11, darkvisionFt: 30 }, languages: [], ...emptyDefenses, traits: [trait("amphibious"), trait("standing_leap", { longJumpFt: 10, highJumpFt: 5, runningStartRequired: false })], actions: [A("mm2025.frog.bite", "Bite", 3, [D(1, "piercing")])] }),
  base("hawk", "Hawk", { size: "tiny", nativeCreatureType: "beast", ac: 13, hp: { max: 1, formula: "1d4-1" }, ...stats([5, 16, 8, 2, 14, 6], [-3, 3, -1, -4, 2, -2], [-3, 3, -1, -4, 2, -2]), initiativeModifier: 3, speeds: { walk: 10, fly: 60 }, skills: { perception: 6 }, senses: { passivePerception: 16 }, languages: [], ...emptyDefenses, traits: [], actions: [A("mm2025.hawk.talons", "Talons", 5, [D(1, "slashing")])] }),
  base("lizard", "Lizard", { size: "tiny", nativeCreatureType: "beast", ac: 10, hp: { max: 2, formula: "1d4" }, ...stats([2, 11, 10, 1, 8, 3], [-4, 0, 0, -5, -1, -4], [-4, 0, 0, -5, -1, -4]), initiativeModifier: 0, speeds: { walk: 20, climb: 20 }, skills: {}, senses: { passivePerception: 9, darkvisionFt: 30 }, languages: [], ...emptyDefenses, traits: [trait("spider_climb")], actions: [A("mm2025.lizard.bite", "Bite", 2, [D(1, "piercing")])] }),
  base("octopus", "Octopus", { size: "small", nativeCreatureType: "beast", ac: 12, hp: { max: 3, formula: "1d6" }, ...stats([4, 15, 11, 3, 10, 4], [-3, 2, 0, -4, 0, -3], [-3, 2, 0, -4, 0, -3]), initiativeModifier: 2, speeds: { walk: 5, swim: 30 }, skills: { perception: 2, stealth: 6 }, senses: { passivePerception: 12, darkvisionFt: 30 }, languages: [], ...emptyDefenses, traits: [trait("compression", { minimumGapInches: 1 }), trait("water_breathing", { onlyUnderwater: true })], actions: [A("mm2025.octopus.tentacles", "Tentacles", 4, [D(1, "bludgeoning")]), { id: "mm2025.octopus.ink-cloud", name: "Ink Cloud", economy: "reaction", kind: "utility", offensive: false, uses: { count: 1, recharge: "day" }, mechanics: { trigger: "creature_ends_turn_within_5ft_underwater", cubeSideFt: 5, heavilyObscuredMinutes: 1, moveUpToSwimSpeed: true } }] }),
  base("owl", "Owl", { size: "tiny", nativeCreatureType: "beast", ac: 11, hp: { max: 1, formula: "1d4-1" }, ...stats([3, 13, 8, 2, 12, 7], [-4, 1, -1, -4, 1, -2], [-4, 1, -1, -4, 1, -2]), initiativeModifier: 1, speeds: { walk: 5, fly: 60 }, skills: { perception: 5, stealth: 5 }, senses: { passivePerception: 15, darkvisionFt: 120 }, languages: [], ...emptyDefenses, traits: [trait("flyby", { avoidsOpportunityAttackOnFlyOut: true })], actions: [A("mm2025.owl.talons", "Talons", 3, [D(1, "slashing")])] }),
  base("rat", "Rat", { size: "tiny", nativeCreatureType: "beast", ac: 10, hp: { max: 1, formula: "1d4-1" }, ...stats([2, 11, 9, 2, 10, 4], [-4, 0, -1, -4, 0, -3], [-4, 0, -1, -4, 0, -3]), initiativeModifier: 0, speeds: { walk: 20, climb: 20 }, skills: { perception: 2 }, senses: { passivePerception: 12, darkvisionFt: 30 }, languages: [], ...emptyDefenses, traits: [trait("agile", { avoidsOpportunityAttackOnReachExit: true })], actions: [A("mm2025.rat.bite", "Bite", 2, [D(1, "piercing")])] }),
  base("raven", "Raven", { size: "tiny", nativeCreatureType: "beast", ac: 12, hp: { max: 2, formula: "1d4" }, ...stats([2, 14, 10, 5, 13, 6], [-4, 2, 0, -3, 1, -2], [-4, 2, 0, -3, 1, -2]), initiativeModifier: 2, speeds: { walk: 10, fly: 50 }, skills: { perception: 3 }, senses: { passivePerception: 13 }, languages: [], ...emptyDefenses, traits: [trait("mimicry", { discernCheck: "wisdom_insight", dc: 10 })], actions: [A("mm2025.raven.beak", "Beak", 4, [D(1, "piercing")])] }),
  base("spider", "Spider", { size: "tiny", nativeCreatureType: "beast", ac: 12, hp: { max: 1, formula: "1d4-1" }, ...stats([2, 14, 8, 1, 10, 2], [-4, 2, -1, -5, 0, -4], [-4, 2, -1, -5, 0, -4]), initiativeModifier: 2, speeds: { walk: 20, climb: 20 }, skills: { stealth: 4 }, senses: { passivePerception: 10, darkvisionFt: 30 }, languages: [], ...emptyDefenses, traits: [trait("spider_climb"), trait("web_walker", { ignoresWebMovementRestrictions: true, locatesCreaturesOnSameWeb: true })], actions: [A("mm2025.spider.bite", "Bite", 4, [D(1, "piercing"), D(2, "poison", "1d4")])] }),
  base("weasel", "Weasel", { size: "tiny", nativeCreatureType: "beast", ac: 13, hp: { max: 1, formula: "1d4-1" }, ...stats([3, 16, 8, 2, 12, 3], [-4, 3, -1, -4, 1, -4], [-4, 3, -1, -4, 1, -4]), initiativeModifier: 3, speeds: { walk: 30, climb: 30 }, skills: { acrobatics: 5, perception: 3, stealth: 5 }, senses: { passivePerception: 13, darkvisionFt: 60 }, languages: [], ...emptyDefenses, traits: [], actions: [A("mm2025.weasel.bite", "Bite", 5, [D(1, "piercing")])] }),
  chain("imp", "Imp", { size: "tiny", nativeCreatureType: "fiend:devil", ac: 13, hp: { max: 21, formula: "6d4+6" }, ...stats([6, 17, 13, 11, 12, 14], [-2, 3, 1, 0, 1, 2], [-2, 3, 1, 0, 1, 2]), initiativeModifier: 3, speeds: { walk: 20, fly: 40 }, skills: { deception: 4, insight: 3, stealth: 5 }, senses: { passivePerception: 11, darkvisionFt: 120, magicalDarknessDoesNotImpede: true }, languages: ["common", "infernal"], resistances: ["cold"], vulnerabilities: [], damageImmunities: ["fire", "poison"], conditionImmunities: ["poisoned"], gear: [], traits: [trait("magic_resistance")], actions: [A("mm2025.imp.sting", "Sting", 5, [D(6, "piercing", "1d6+3"), D(7, "poison", "2d6")]), { id: "mm2025.imp.invisibility", name: "Invisibility", economy: "action", kind: "spell", offensive: false, spell: { spellId: "invisibility", ability: "cha", componentsRequired: false, target: "self" } }, { id: "mm2025.imp.shape-shift", name: "Shape-Shift", economy: "action", kind: "utility", offensive: false, mechanics: { forms: [{ id: "rat", speeds: { walk: 20 } }, { id: "raven", speeds: { walk: 20, fly: 60 } }, { id: "spider", speeds: { walk: 20, climb: 20 } }], changesOnlySpeed: true, equipmentTransforms: false } }] }),
  chain("pseudodragon", "Pseudodragon", { size: "tiny", nativeCreatureType: "dragon", ac: 14, hp: { max: 10, formula: "3d4+3" }, ...stats([6, 15, 13, 10, 12, 10], [-2, 2, 1, 0, 1, 0], [-2, 2, 1, 0, 1, 0]), initiativeModifier: 2, speeds: { walk: 15, fly: 60 }, skills: { perception: 5, stealth: 4 }, senses: { passivePerception: 15, blindsightFt: 10, darkvisionFt: 60 }, languages: [], understandsButCannotSpeak: ["common", "draconic"], ...emptyDefenses, traits: [trait("magic_resistance")], actions: [{ id: "mm2025.pseudodragon.multiattack", name: "Multiattack", economy: "action", kind: "multiattack", offensive: true, mechanics: { attacks: [{ actionId: "mm2025.pseudodragon.bite", count: 2 }] } }, A("mm2025.pseudodragon.bite", "Bite", 4, [D(4, "piercing", "1d4+2")]), { id: "mm2025.pseudodragon.sting", name: "Sting", economy: "action", kind: "save", offensive: true, save: { ability: "con", dc: 12, rangeFt: 5, damage: [D(5, "poison", "2d4")], effects: [{ condition: "poisoned", durationMinutes: 60 }, { condition: "unconscious", whileCondition: "poisoned", endsOnDamageOrWakeActionWithinFt: 5 }] } }] }),
  chain("quasit", "Quasit", { size: "tiny", nativeCreatureType: "fiend:demon", ac: 13, hp: { max: 25, formula: "10d4" }, ...stats([5, 17, 10, 7, 10, 10], [-3, 3, 0, -2, 0, 0], [-3, 3, 0, -2, 0, 0]), initiativeModifier: 3, speeds: { walk: 40 }, skills: { stealth: 5 }, senses: { passivePerception: 10, darkvisionFt: 120 }, languages: ["abyssal", "common"], resistances: ["cold", "fire", "lightning"], vulnerabilities: [], damageImmunities: ["poison"], conditionImmunities: ["poisoned"], gear: [], traits: [trait("magic_resistance")], actions: [A("mm2025.quasit.rend", "Rend", 5, [D(5, "slashing", "1d4+3")]), { id: "mm2025.quasit.invisibility", name: "Invisibility", economy: "action", kind: "spell", offensive: false, spell: { spellId: "invisibility", ability: "cha", componentsRequired: false, target: "self" } }, { id: "mm2025.quasit.scare", name: "Scare", economy: "action", kind: "save", offensive: true, uses: { count: 1, recharge: "day" }, save: { ability: "wis", dc: 10, rangeFt: 20, effects: [{ condition: "frightened", repeatAt: "end_of_each_target_turn", maximumDurationMinutes: 1 }] } }, { id: "mm2025.quasit.shape-shift", name: "Shape-Shift", economy: "action", kind: "utility", offensive: false, mechanics: { forms: [{ id: "bat", speeds: { walk: 10, fly: 40 } }, { id: "centipede", speeds: { walk: 40, climb: 40 } }, { id: "toad", speeds: { walk: 40, swim: 40 } }], changesOnlySpeed: true, equipmentTransforms: false } }] }),
  chain("skeleton", "Skeleton", { size: "medium", nativeCreatureType: "undead", ac: 14, hp: { max: 13, formula: "2d8+4" }, ...stats([10, 16, 15, 6, 8, 5], [0, 3, 2, -2, -1, -3], [0, 3, 2, -2, -1, -3]), initiativeModifier: 3, speeds: { walk: 30 }, skills: {}, senses: { passivePerception: 9, darkvisionFt: 60 }, languages: [], understandsButCannotSpeak: ["common", "one_other_language"], resistances: [], vulnerabilities: ["bludgeoning"], damageImmunities: ["poison"], conditionImmunities: ["exhaustion", "poisoned"], gear: ["shortbow", "shortsword"], traits: [], actions: [A("mm2025.skeleton.shortsword", "Shortsword", 5, [D(6, "piercing", "1d6+3")]), A("mm2025.skeleton.shortbow", "Shortbow", 5, [D(6, "piercing", "1d6+3")], "ranged", [80, 320])] }),
  chain("slaad_tadpole", "Slaad Tadpole", { size: "tiny", nativeCreatureType: "aberration", ac: 12, hp: { max: 7, formula: "3d4" }, ...stats([7, 15, 10, 3, 5, 3], [-2, 2, 0, -4, -3, -4], [-2, 2, 0, -4, -3, -4]), initiativeModifier: 2, speeds: { walk: 30, burrow: 10 }, skills: { stealth: 4 }, senses: { passivePerception: 7, darkvisionFt: 60 }, languages: [], understandsButCannotSpeak: ["slaad"], resistances: ["acid", "cold", "fire", "lightning", "thunder"], vulnerabilities: [], damageImmunities: [], conditionImmunities: [], gear: [], traits: [trait("magic_resistance")], actions: [A("mm2025.slaad-tadpole.bite", "Bite", 4, [D(5, "piercing", "1d6+2")])] }, "https://www.dndbeyond.com/monsters/4775842-slaad-tadpole"),
  chain("sphinx_of_wonder", "Sphinx of Wonder", { size: "tiny", nativeCreatureType: "celestial", ac: 13, hp: { max: 24, formula: "7d4+7" }, ...stats([6, 17, 13, 15, 12, 11], [-2, 3, 1, 2, 1, 0], [-2, 3, 1, 2, 1, 0]), initiativeModifier: 3, speeds: { walk: 20, fly: 40 }, skills: { arcana: 4, religion: 4, stealth: 5 }, senses: { passivePerception: 11, darkvisionFt: 60 }, languages: ["celestial", "common"], resistances: ["necrotic", "psychic", "radiant"], vulnerabilities: [], damageImmunities: [], conditionImmunities: [], gear: [], traits: [trait("magic_resistance")], actions: [A("mm2025.sphinx-of-wonder.rend", "Rend", 5, [D(5, "slashing", "1d4+3"), D(7, "radiant", "2d6")]), { id: "mm2025.sphinx-of-wonder.burst-of-ingenuity", name: "Burst of Ingenuity", economy: "reaction", kind: "utility", offensive: false, uses: { count: 2, recharge: "day" }, mechanics: { trigger: "creature_within_30ft_makes_ability_check_or_save", modifier: 2, rangeFt: 30 } }] }),
  chain("sprite", "Sprite", { size: "tiny", nativeCreatureType: "fey", ac: 15, hp: { max: 10, formula: "4d4" }, ...stats([3, 18, 10, 14, 13, 11], [-4, 4, 0, 2, 1, 0], [-4, 4, 0, 2, 1, 0]), initiativeModifier: 4, speeds: { walk: 10, fly: 40 }, skills: { perception: 3, stealth: 8 }, senses: { passivePerception: 13 }, languages: ["common", "elvish", "sylvan"], ...emptyDefenses, traits: [], actions: [A("mm2025.sprite.needle-sword", "Needle Sword", 6, [D(6, "piercing", "1d4+4")]), { ...A("mm2025.sprite.enchanting-bow", "Enchanting Bow", 6, [D(1, "piercing")], "ranged", [40, 160]), mechanics: { onHitCondition: "charmed", expiresAt: "start_of_source_next_turn" } }, { id: "mm2025.sprite.heart-sight", name: "Heart Sight", economy: "action", kind: "save", offensive: false, save: { ability: "cha", dc: 10, rangeFt: 5, effects: [{ reveals: ["emotions", "alignment"] }], automaticFailureCreatureTypes: ["celestial", "fiend", "undead"] } }, { id: "mm2025.sprite.invisibility", name: "Invisibility", economy: "action", kind: "spell", offensive: false, spell: { spellId: "invisibility", ability: "cha", componentsRequired: false, target: "self" } }] }),
  chain("venomous_snake", "Venomous Snake", { size: "tiny", nativeCreatureType: "beast", ac: 12, hp: { max: 5, formula: "2d4" }, ...stats([2, 15, 11, 1, 10, 3], [-4, 2, 0, -5, 0, -4], [-4, 2, 0, -5, 0, -4]), initiativeModifier: 2, speeds: { walk: 30, swim: 30 }, skills: {}, senses: { passivePerception: 10, blindsightFt: 10 }, languages: [], ...emptyDefenses, traits: [], actions: [A("mm2025.venomous-snake.bite", "Bite", 4, [D(4, "piercing", "1d4+2"), D(3, "poison", "1d6")])] })
];
function canonicalize(value) {
  if (Array.isArray(value)) return value.map(canonicalize);
  if (value !== null && typeof value === "object") {
    return Object.fromEntries(Object.entries(value).sort(([left], [right]) => left.localeCompare(right)).map(([key, entry]) => [key, canonicalize(entry)]));
  }
  return value;
}
function canonicalFamiliarCatalogJson(value) {
  return JSON.stringify(canonicalize(value));
}
function familiarCatalogContentHash(value) {
  const text = canonicalFamiliarCatalogJson(value);
  let hash = 2166136261;
  for (let index = 0; index < text.length; index += 1) {
    hash ^= text.charCodeAt(index);
    hash = Math.imul(hash, 16777619) >>> 0;
  }
  return `fnv1a32:${hash.toString(16).padStart(8, "0")}`;
}
function deepFreeze(value) {
  if (value !== null && typeof value === "object" && !Object.isFrozen(value)) {
    Object.freeze(value);
    for (const nested of Object.values(value)) deepFreeze(nested);
  }
  return value;
}
var catalogWithoutHash = {
  schemaVersion: 1,
  catalogId: "dnd2024.familiar-stat-blocks.mm2025.v1",
  sourceVersion: "phb2024-errata-v1.mm2025.dndbeyond-live-2026-08-04",
  sourceLocator: "https://www.dndbeyond.com/sources/dnd/br-2024/creature-stat-blocks",
  forms
};
var FAMILIAR_ACTOR_CATALOG = deepFreeze({
  ...catalogWithoutHash,
  contentHash: familiarCatalogContentHash(catalogWithoutHash)
});
function familiarActorCatalogIssue(value) {
  if (value === null || typeof value !== "object" || Array.isArray(value)) return "Familiar actor catalog must be an object";
  const candidate = value;
  if (candidate.schemaVersion !== 1 || candidate.catalogId !== FAMILIAR_ACTOR_CATALOG.catalogId) return "Familiar actor catalog identity is not supported";
  if (!Array.isArray(candidate.forms) || candidate.forms.length !== FAMILIAR_ACTOR_CATALOG.forms.length) return "Familiar actor catalog must contain exactly 19 canonical forms";
  if (candidate.contentHash !== FAMILIAR_ACTOR_CATALOG.contentHash) return "Familiar actor catalog content hash does not match the pinned ruleset";
  return canonicalFamiliarCatalogJson(candidate) === canonicalFamiliarCatalogJson(FAMILIAR_ACTOR_CATALOG) ? null : "Familiar actor catalog content is forged or noncanonical";
}
function familiarActorTemplateIssue(value) {
  if (value === null || typeof value !== "object" || Array.isArray(value)) return "Familiar actor template must be an object";
  const formId = value.formId;
  if (typeof formId !== "string") return "Familiar actor template requires a stable form id";
  const expected = FAMILIAR_ACTOR_CATALOG.forms.find((entry) => entry.formId === formId);
  if (!expected) return "Familiar actor template form is not in the pinned catalog";
  return canonicalFamiliarCatalogJson(value) === canonicalFamiliarCatalogJson(expected) ? null : "Familiar actor template is forged or noncanonical";
}
function getFamiliarActorTemplate(formId, catalog = FAMILIAR_ACTOR_CATALOG) {
  const catalogIssue3 = familiarActorCatalogIssue(catalog);
  if (catalogIssue3) throw new Error(catalogIssue3);
  if (!formId || formId !== formId.trim()) throw new Error("Familiar form requires a canonical stable id");
  const template = catalog.forms.find((entry) => entry.formId === formId);
  if (!template) throw new Error(`Familiar form ${formId} is outside the pinned catalog boundary`);
  return template;
}
function materializeFamiliarActor(input) {
  const familiarIssue = familiarStateIssue(input.familiar);
  if (familiarIssue) throw new Error(familiarIssue);
  const templateIssue = familiarActorTemplateIssue(input.template);
  if (templateIssue) throw new Error(templateIssue);
  if (!input.ownerControllerId || input.ownerControllerId !== input.ownerControllerId.trim()) throw new Error("Familiar owner controller requires a canonical stable id");
  if (input.familiar.presence !== "present") throw new Error("Only a present familiar can be materialized as an actor");
  if (input.familiar.form.id !== input.template.formId || input.familiar.form.statBlockId !== input.template.selectionStatBlockId) throw new Error("Familiar state does not match the pinned stat template");
  const sourceIds = [input.familiar.sourceEntityId, input.template.sourceEntityId];
  const actionSources = Object.fromEntries(input.template.actions.map((action) => [action.id, sourceIds]));
  const size = input.template.size === "tiny" ? 0 : input.template.size === "small" ? 1 : 2;
  const attackAuthorization = input.familiar.extension === "pact_chain" ? "owner_attack_replacement_only" : "forbidden";
  return {
    id: input.familiar.actorId,
    name: input.template.name,
    kind: "summonedActor",
    controllerId: input.ownerControllerId,
    ac: input.template.ac,
    // Stat-block actions remain discoverable below, but no ordinary UseAction/Attack capability is minted.
    capabilities: { actionIds: [], featureSources: actionSources },
    character: {
      abilityScores: { ...input.template.abilityScores },
      abilityMods: { ...input.template.abilityMods },
      profBonus: 2,
      level: 0,
      characterSpeed: input.template.speeds.walk,
      baseSpeed: input.template.speeds.walk,
      saveProficiencies: [],
      skillProficiencies: Object.keys(input.template.skills)
    },
    runtime: {
      hp: { current: input.template.hp.max, max: input.template.hp.max, temp: 0 },
      resources: { action: 1, bonus_action: 1, reaction: input.familiar.reactionAvailable ? 1 : 0 },
      maxResources: { action: 1, bonus_action: 1, reaction: 1 },
      equipment: {},
      inventory: [],
      activeEffects: []
    },
    passives: input.template.traits.map((entry) => ({ id: entry.id, mechanics: entry.mechanics, sourceEntityId: input.template.sourceEntityId })),
    attackProfile: { attacksPerAction: 1, size, reachFt: 5, graspingParts: [], sourceEntityIds: sourceIds },
    familiarMetadata: {
      ownerActorId: input.familiar.ownerActorId,
      spiritType: input.familiar.spiritType,
      nativeCreatureType: input.template.nativeCreatureType,
      effectiveCreatureType: input.familiar.spiritType,
      formId: input.template.formId,
      statBlockId: input.template.statBlockId,
      sourceEntityId: input.template.sourceEntityId,
      size: input.template.size,
      initiativeModifier: input.template.initiativeModifier,
      speeds: input.template.speeds,
      saves: input.template.saves,
      skills: input.template.skills,
      senses: input.template.senses,
      languages: input.template.languages,
      understandsButCannotSpeak: input.template.understandsButCannotSpeak ?? [],
      resistances: input.template.resistances,
      vulnerabilities: input.template.vulnerabilities,
      damageImmunities: input.template.damageImmunities,
      conditionImmunities: input.template.conditionImmunities,
      gear: input.template.gear,
      actions: input.template.actions,
      canInitiateAttackAction: false,
      attackAuthorization
    }
  };
}

// src/rules-core/familiarRuntime.ts
var FIND_FAMILIAR_PRIMITIVE = "find_familiar";
var FIND_FAMILIAR_FORM_CHOICE = "find_familiar_form";
var FIND_FAMILIAR_SPIRIT_CHOICE = "find_familiar_spirit";
var FIND_FAMILIAR_CAST_PATH_CHOICE = "find_familiar_cast_path";
function clone3(value) {
  return JSON.parse(JSON.stringify(value));
}
function record4(value) {
  return value !== null && typeof value === "object" && !Array.isArray(value) ? value : null;
}
function findFamiliarMaterialCost(action) {
  const primitive = record4(action.mechanics.primitive);
  if (primitive?.type !== FIND_FAMILIAR_PRIMITIVE || typeof primitive.materialCostResource !== "string" || !primitive.materialCostResource.trim()) return null;
  const activation = record4(action.mechanics.activation);
  if (!Array.isArray(activation?.cost)) return null;
  const matching = activation.cost.filter((entry) => record4(entry)?.resource === primitive.materialCostResource);
  if (matching.length !== 1) return null;
  const cost = record4(matching[0]);
  const binding = record4(cost.binding);
  if (!Number.isInteger(cost.amount) || Number(cost.amount) <= 0 || cost.recharge !== "never" || binding?.kind !== "currency" || !["gold", "silver", "copper"].includes(String(binding.currency ?? ""))) return null;
  return {
    resource: primitive.materialCostResource,
    amount: Number(cost.amount),
    binding: {
      kind: "currency",
      currency: binding.currency
    },
    recharge: "never"
  };
}
function same(left, right) {
  return canonicalFamiliarCatalogJson(left) === canonicalFamiliarCatalogJson(right);
}
function familiarActorsOwnedBy(world, ownerActorId) {
  return Object.values(world.actors).filter((actor2) => actor2.familiarState?.ownerActorId === ownerActorId).sort((left, right) => left.id.localeCompare(right.id));
}
function requireOwnedFamiliar(world, ownerActorId, familiarActorId) {
  const actor2 = world.actors[familiarActorId];
  return actor2?.familiarState?.ownerActorId === ownerActorId ? actor2 : null;
}
function rollFamiliarInitiative(input) {
  const dieAware = input.rng;
  const d20Roll = typeof dieAware.rollDie === "function" ? dieAware.rollDie(20) : (() => {
    const random = input.rng();
    if (!Number.isFinite(random) || random < 0 || random >= 1) {
      throw new Error("Familiar Initiative RNG must return a value in [0, 1)");
    }
    return Math.floor(random * 20) + 1;
  })();
  return setFamiliarInitiative({
    familiar: input.familiar,
    familiarActorId: input.familiar.actorId,
    d20Roll,
    modifier: input.modifier
  });
}
function materializeCanonicalFamiliarActor(input) {
  const template = getFamiliarActorTemplate(input.familiar.form.id);
  const draft = materializeFamiliarActor({
    familiar: input.familiar,
    template,
    ownerControllerId: input.owner.controllerId
  });
  return {
    ...clone3(draft),
    familiarState: clone3(input.familiar),
    familiarMetadata: {
      ...clone3(draft.familiarMetadata),
      summoningActionId: input.summoningActionId,
      catalogId: FAMILIAR_ACTOR_CATALOG.catalogId,
      catalogContentHash: FAMILIAR_ACTOR_CATALOG.contentHash
    }
  };
}
function familiarActorStateIssue(input) {
  const { actor: actor2, owner } = input;
  if (!actor2.familiarState && !actor2.familiarMetadata) return null;
  if (!actor2.familiarState || !actor2.familiarMetadata) {
    return "Familiar actor requires both canonical state and pinned metadata";
  }
  if (actor2.kind !== "summonedActor") return "Familiar state requires a summonedActor";
  const stateIssue3 = familiarStateIssue(actor2.familiarState);
  if (stateIssue3) return stateIssue3;
  if (actor2.familiarState.actorId !== actor2.id) return "Familiar state actorId must match its actor";
  if (!owner || owner.id !== actor2.familiarState.ownerActorId || owner.id === actor2.id) {
    return "Familiar owner must reference another actor in the same world";
  }
  const metadata = actor2.familiarMetadata;
  if (!metadata.summoningActionId || metadata.summoningActionId !== metadata.summoningActionId.trim() || !owner.capabilities.actionIds.includes(metadata.summoningActionId)) {
    return "Familiar summoning action must remain actor-owned by its owner";
  }
  if (metadata.catalogId !== FAMILIAR_ACTOR_CATALOG.catalogId || metadata.catalogContentHash !== FAMILIAR_ACTOR_CATALOG.contentHash) {
    return "Familiar metadata does not match the pinned catalog";
  }
  const materializableState = actor2.familiarState.presence === "present" ? actor2.familiarState : { ...actor2.familiarState, presence: "present" };
  const expected = materializeCanonicalFamiliarActor({
    familiar: materializableState,
    owner,
    summoningActionId: metadata.summoningActionId
  });
  for (const key of [
    "name",
    "kind",
    "controllerId",
    "ac",
    "capabilities",
    "character",
    "passives",
    "attackProfile",
    "familiarMetadata"
  ]) {
    if (!same(actor2[key], expected[key])) return `Familiar actor has forged ${key}`;
  }
  if (!same(actor2.runtime.maxResources, expected.runtime.maxResources)) {
    return "Familiar actor has forged maximum resources";
  }
  if (actor2.runtime.hp.max !== expected.runtime.hp.max || !Number.isInteger(actor2.runtime.hp.current) || actor2.runtime.hp.current < 0 || actor2.runtime.hp.current > actor2.runtime.hp.max) {
    return "Familiar actor has invalid hit points for its pinned form";
  }
  const reaction = actor2.runtime.resources.reaction ?? 0;
  if (reaction !== (actor2.familiarState.reactionAvailable ? 1 : 0)) {
    return "Familiar Reaction resource must mirror canonical familiar state";
  }
  if (actor2.familiarState.presence === "present" && actor2.runtime.hp.current < 1) {
    return "A zero-HP familiar cannot remain present";
  }
  if (actor2.familiarState.presence === "disappeared_zero_hp" && actor2.runtime.hp.current !== 0) {
    return "A familiar disappeared at zero HP must retain zero current HP";
  }
  const chain2 = owner.warlockPacts?.chain;
  if (actor2.familiarState.extension === "pact_chain") {
    if (!chain2 || chain2.ownerActorId !== owner.id || chain2.sourceEntityId !== actor2.familiarState.sourceEntityId || chain2.template.findFamiliarActionId !== metadata.summoningActionId || !chain2.activeFamiliar || chain2.activeFamiliar.actorId !== actor2.id || chain2.activeFamiliar.ownerActorId !== owner.id || chain2.activeFamiliar.formId !== actor2.familiarState.form.id || chain2.activeFamiliar.sourceEntityId !== actor2.familiarState.sourceEntityId || chain2.activeFamiliar.reactionAvailable !== actor2.familiarState.reactionAvailable) {
      return "Pact Chain familiar projection must exactly mirror canonical familiar state";
    }
  } else if (chain2?.activeFamiliar?.actorId === actor2.id) {
    return "A base familiar cannot occupy the Pact Chain projection";
  }
  return null;
}
function pactChainProjection(familiar) {
  return {
    actorId: familiar.actorId,
    ownerActorId: familiar.ownerActorId,
    formId: familiar.form.id,
    sourceEntityId: familiar.sourceEntityId,
    reactionAvailable: familiar.reactionAvailable
  };
}
function familiarAttackAbility(actor2, action) {
  const preferred = action.attack?.mode === "ranged" ? ["dex", "str", "cha", "wis", "int", "con"] : ["str", "dex", "cha", "wis", "int", "con"];
  const requiredModifier = action.attack.bonus - actor2.character.profBonus;
  return preferred.find((ability) => actor2.character.abilityMods[ability] === requiredModifier) ?? preferred[0];
}
function familiarAttackRuleAction(actor2, familiarActionId) {
  const metadata = actor2.familiarMetadata;
  if (!actor2.familiarState || !metadata) return null;
  const definition = metadata.actions.find((entry) => entry.id === familiarActionId);
  if (!definition || definition.kind !== "attack" || !definition.attack) return null;
  const attack = definition.attack;
  const rangeFt = attack.mode === "melee" ? attack.reachFt ?? 5 : attack.longRangeFt ?? attack.normalRangeFt ?? 0;
  const sourceEntityIds = actor2.capabilities.featureSources?.[definition.id];
  if (!sourceEntityIds?.length) return null;
  return {
    id: definition.id,
    name: definition.name,
    kind: "nonSpell",
    sourceEntityIds: [...sourceEntityIds],
    targeting: {
      minTargets: 1,
      maxTargets: 1,
      rangeFt,
      requiresLineOfSight: true,
      allowedRelations: ["ally", "enemy", "neutral"]
    },
    mechanics: {
      activation: { mode: "reaction", cost: [] },
      effects: [{
        resolution: "attack_roll",
        ability: familiarAttackAbility(actor2, definition),
        attack_kind: attack.mode === "melee" ? "melee" : "ranged",
        on_hit: attack.damage.map((part) => ({
          kind: "damage",
          dice: part.formula ?? String(part.average),
          type: part.type,
          ability: "none"
        }))
      }]
    }
  };
}
function canonicalTouchSpell(action) {
  return action.kind === "spell" && action.targeting?.requiresTouch === true && action.targeting.rangeFt === 5;
}

// src/rules-core/protection.ts
var PROTECTION_2024_CAPABILITY_ID = "fighting_style.protection.reaction";
var PROTECTION_2024_MAX_DISTANCE_FT = 5;
var FACT_SOURCES = /* @__PURE__ */ new Set(["scenario", "board", "gm_ruling"]);
var ATTACK_ROLL_STAGES = /* @__PURE__ */ new Set([
  "before_roll",
  "rolled",
  "resolved"
]);
function record5(value) {
  return value !== null && typeof value === "object" && !Array.isArray(value) ? value : null;
}
function stableId2(value) {
  return typeof value === "string" && value.length > 0 && value.trim() === value;
}
function validRevision(value) {
  return Number.isInteger(value) && Number(value) >= 0;
}
function validDistance(value) {
  return typeof value === "number" && Number.isFinite(value) && value >= 0;
}
function validFactSource(value) {
  return typeof value === "string" && FACT_SOURCES.has(value);
}
function parseReactionFacts(value) {
  const facts = record5(value);
  if (!facts || !validFactSource(facts.factsSource) || !validRevision(facts.worldRevision) || !stableId2(facts.attackId) || !stableId2(facts.protectorActorId) || !stableId2(facts.attackerActorId) || !stableId2(facts.targetActorId) || typeof facts.attackRollStage !== "string" || !ATTACK_ROLL_STAGES.has(facts.attackRollStage) || typeof facts.protectorCanSeeAttacker !== "boolean" || typeof facts.protectorHoldingShield !== "boolean" || typeof facts.protectorReactionAvailable !== "boolean" || !validDistance(facts.protectorDistanceToTargetFt)) return null;
  return facts;
}
function getProtection2024Eligibility(value) {
  const facts = parseReactionFacts(value);
  if (!facts) return { eligible: false, reason: "invalid_facts" };
  if (facts.attackRollStage !== "before_roll") {
    return { eligible: false, reason: "wrong_timing" };
  }
  if (facts.targetActorId === facts.protectorActorId) {
    return { eligible: false, reason: "target_is_protector" };
  }
  if (!facts.protectorCanSeeAttacker) {
    return { eligible: false, reason: "attacker_not_visible" };
  }
  if (!facts.protectorHoldingShield) {
    return { eligible: false, reason: "shield_not_held" };
  }
  if (!facts.protectorReactionAvailable) {
    return { eligible: false, reason: "reaction_unavailable" };
  }
  if (facts.protectorDistanceToTargetFt > PROTECTION_2024_MAX_DISTANCE_FT) {
    return { eligible: false, reason: "target_out_of_range" };
  }
  return { eligible: true, facts };
}
function validSourceIds(value) {
  return Array.isArray(value) && value.length > 0 && value.every((sourceId) => stableId2(sourceId)) && new Set(value).size === value.length;
}
function protection2024SourceIssue(value, protectorActorId) {
  const source2 = record5(value);
  if (!source2 || !stableId2(protectorActorId) || source2.ownerActorId !== protectorActorId || source2.capabilityId !== PROTECTION_2024_CAPABILITY_ID || !validSourceIds(source2.sourceEntityIds)) return "invalid_source";
  return null;
}
function resolveProtection2024Reaction(input) {
  const eligibility = getProtection2024Eligibility(input.facts);
  if (!eligibility.eligible) {
    return {
      status: "rejected",
      reason: eligibility.reason,
      reactionCost: null,
      effect: null
    };
  }
  const facts = eligibility.facts;
  if (protection2024SourceIssue(input.source, facts.protectorActorId)) {
    return { status: "rejected", reason: "invalid_source", reactionCost: null, effect: null };
  }
  if (input.decision === "decline") {
    return { status: "declined", reactionCost: null, effect: null };
  }
  if (input.decision !== "use") {
    return { status: "rejected", reason: "invalid_decision", reactionCost: null, effect: null };
  }
  if (!stableId2(input.effectId)) {
    return { status: "rejected", reason: "invalid_effect_id", reactionCost: null, effect: null };
  }
  const source2 = {
    ownerActorId: input.source.ownerActorId,
    capabilityId: PROTECTION_2024_CAPABILITY_ID,
    sourceEntityIds: [...input.source.sourceEntityIds]
  };
  return {
    status: "activated",
    reactionCost: { actorId: facts.protectorActorId, resource: "reaction", spend: 1 },
    effect: {
      schemaVersion: 1,
      kind: "fighting_style_protection_2024",
      id: input.effectId,
      source: source2,
      protectorActorId: facts.protectorActorId,
      protectedTargetActorId: facts.targetActorId,
      triggeringAttackerActorId: facts.attackerActorId,
      triggeringAttackId: facts.attackId,
      activatedAtWorldRevision: facts.worldRevision,
      expiry: {
        type: "start_of_protector_next_turn",
        actorId: facts.protectorActorId
      }
    }
  };
}
function protection2024EffectIssue(value) {
  const effect = record5(value);
  if (!effect) return "not_an_object";
  if (effect.schemaVersion !== 1) return "invalid_schema";
  if (effect.kind !== "fighting_style_protection_2024") return "invalid_kind";
  if (!stableId2(effect.id)) return "invalid_effect_id";
  if (!stableId2(effect.triggeringAttackId)) return "invalid_attack_id";
  if (!stableId2(effect.protectorActorId) || !stableId2(effect.protectedTargetActorId) || !stableId2(effect.triggeringAttackerActorId)) return "invalid_actor_id";
  if (effect.protectedTargetActorId === effect.protectorActorId) return "target_is_protector";
  if (!validRevision(effect.activatedAtWorldRevision)) return "invalid_activation_revision";
  if (protection2024SourceIssue(effect.source, effect.protectorActorId)) return "invalid_source";
  const expiry = record5(effect.expiry);
  if (!expiry || expiry.type !== "start_of_protector_next_turn" || expiry.actorId !== effect.protectorActorId) return "invalid_expiry";
  return null;
}
function parseLifecycleEvent(value) {
  const event = record5(value);
  if (!event || !validFactSource(event.factsSource) || !validRevision(event.worldRevision)) return null;
  if (event.type === "turn_started") {
    return stableId2(event.actorId) ? event : null;
  }
  if (event.type === "distance_observed") {
    return stableId2(event.protectorActorId) && stableId2(event.protectedTargetActorId) && validDistance(event.distanceFt) ? event : null;
  }
  return null;
}
function advanceProtection2024Effect(effect, value) {
  if (protection2024EffectIssue(effect)) {
    return { status: "rejected", reason: "invalid_effect", effect: null };
  }
  const event = parseLifecycleEvent(value);
  if (!event) return { status: "rejected", reason: "invalid_event", effect: null };
  if (event.worldRevision <= effect.activatedAtWorldRevision) {
    return { status: "rejected", reason: "stale_event", effect: null };
  }
  if (event.type === "turn_started") {
    return event.actorId === effect.protectorActorId ? { status: "ended", reason: "protector_turn_started", effect: null } : { status: "active", reason: "other_actor_turn", effect };
  }
  if (event.protectorActorId !== effect.protectorActorId || event.protectedTargetActorId !== effect.protectedTargetActorId) {
    return { status: "active", reason: "unrelated_distance", effect };
  }
  return event.distanceFt > PROTECTION_2024_MAX_DISTANCE_FT ? { status: "ended", reason: "proximity_broken", effect: null } : { status: "active", reason: "proximity_maintained", effect };
}
function parseAttackRollFacts(value) {
  const facts = record5(value);
  if (!facts || !validFactSource(facts.factsSource) || !validRevision(facts.worldRevision) || !stableId2(facts.attackId) || !stableId2(facts.targetActorId) || typeof facts.attackRollStage !== "string" || !ATTACK_ROLL_STAGES.has(facts.attackRollStage) || !validDistance(facts.protectorDistanceToProtectedTargetFt)) return null;
  return facts;
}
function resolveProtection2024AttackRoll(effect, value) {
  if (protection2024EffectIssue(effect)) {
    return {
      status: "rejected",
      reason: "invalid_effect",
      imposeDisadvantage: false,
      effect: null
    };
  }
  const facts = parseAttackRollFacts(value);
  if (!facts) {
    return {
      status: "rejected",
      reason: "invalid_facts",
      imposeDisadvantage: false,
      effect: null
    };
  }
  if (facts.worldRevision < effect.activatedAtWorldRevision) {
    return {
      status: "rejected",
      reason: "stale_facts",
      imposeDisadvantage: false,
      effect: null
    };
  }
  if (facts.attackRollStage !== "before_roll") {
    return {
      status: "rejected",
      reason: "wrong_timing",
      imposeDisadvantage: false,
      effect: null
    };
  }
  if (facts.protectorDistanceToProtectedTargetFt > PROTECTION_2024_MAX_DISTANCE_FT) {
    return {
      status: "ended",
      reason: "proximity_broken",
      imposeDisadvantage: false,
      effect: null
    };
  }
  const applies = facts.targetActorId === effect.protectedTargetActorId;
  return {
    status: "active",
    reason: applies ? "protected_target" : "different_target",
    imposeDisadvantage: applies,
    effect
  };
}

// src/rules-core/protectionRuntime.ts
var PROTECTION_2024_EFFECT_NAME = "Fighting Style: Protection";
function clone4(value) {
  return JSON.parse(JSON.stringify(value));
}
function same2(left, right) {
  return JSON.stringify(left) === JSON.stringify(right);
}
function protectionEffectEntry(effect) {
  if (protection2024EffectIssue(effect)) throw new Error("Cannot persist an invalid Protection effect");
  return {
    id: effect.id,
    name: PROTECTION_2024_EFFECT_NAME,
    mechanics: clone4(effect),
    expiry: "manual",
    source: PROTECTION_2024_CAPABILITY_ID,
    ownerId: effect.protectorActorId,
    sourceId: effect.protectorActorId
  };
}
function protectionEffectFromEntry(entry) {
  const mechanics = entry.mechanics;
  if (mechanics.kind !== "fighting_style_protection_2024") return null;
  return clone4(entry.mechanics);
}
function actorProtectionEffects(actor2) {
  return actor2.runtime.activeEffects.flatMap((entry) => {
    const effect = protectionEffectFromEntry(entry);
    return effect ? [effect] : [];
  });
}
function protectionEffectEntryIssue(entry, owner, world) {
  const effect = protectionEffectFromEntry(entry);
  if (!effect) return null;
  const issue = protection2024EffectIssue(effect);
  if (issue) return `Protection effect is invalid: ${issue}`;
  if (effect.protectorActorId !== owner.id || entry.id !== effect.id || !stableId3(entry.name) || entry.source !== PROTECTION_2024_CAPABILITY_ID || entry.ownerId !== owner.id || entry.sourceId !== owner.id || entry.expiry !== "manual" || entry.roundsLeft !== void 0) {
    return "Protection active-effect envelope does not match its canonical effect";
  }
  const sources = owner.capabilities.featureSources?.[PROTECTION_2024_CAPABILITY_ID];
  if (protection2024SourceIssue({
    ownerActorId: owner.id,
    capabilityId: PROTECTION_2024_CAPABILITY_ID,
    sourceEntityIds: sources
  }, owner.id)) {
    return "Protection effect owner no longer has the exact source capability";
  }
  if (!same2(sources, effect.source.sourceEntityIds)) {
    return "Protection effect source differs from its actor capability";
  }
  if (world && (!world.actors[effect.protectedTargetActorId] || !world.actors[effect.triggeringAttackerActorId])) {
    return "Protection effect references an actor outside its world";
  }
  return null;
}
function actorHoldsCanonicalShield(actor2) {
  const offHandId = actor2.runtime.equipment.off_hand;
  if (!offHandId) return false;
  const card = [
    ...actor2.character.knownCards ?? [],
    ...actor2.character.equippedCards ?? []
  ].find((candidate) => candidate.id === offHandId);
  return !!card && card.type === "shield" && Array.isArray(card.properties) && card.properties.includes("shield");
}
function stableId3(value) {
  return typeof value === "string" && value.length > 0 && value.trim() === value;
}
function validCandidate(value) {
  if (!value || typeof value !== "object" || Array.isArray(value)) return false;
  const facts = value;
  return ["scenario", "board", "gm_ruling"].includes(String(facts.factsSource)) && Number.isInteger(facts.boardRevision) && Number(facts.boardRevision) >= 0 && stableId3(facts.protectorActorId) && typeof facts.protectorCanSeeAttacker === "boolean" && typeof facts.protectorDistanceToTargetFt === "number" && Number.isFinite(facts.protectorDistanceToTargetFt) && Number(facts.protectorDistanceToTargetFt) >= 0;
}
function pendingProtectionResolutionIssue(value, world) {
  if (!value || typeof value !== "object" || Array.isArray(value)) {
    return "Protection pending resolution must be an object";
  }
  const pending = value;
  if (pending.type !== "protection_reaction" || !stableId3(pending.id) || !stableId3(pending.openedByCommandId) || !Number.isInteger(pending.openedAtRevision) || pending.openedAtRevision < 0 || !Number.isInteger(pending.deadlineLogicalClock) || pending.deadlineLogicalClock < 0 || !stableId3(pending.sourceActorId) || !stableId3(pending.targetActorId) || !stableId3(pending.actionId) || !world.actors[pending.sourceActorId] || !world.actors[pending.targetActorId]) {
    return "Protection pending resolution has invalid identity or actor references";
  }
  if (!["catalog", "weapon_melee", "weapon_ranged", "unarmed_damage", "familiar_attack"].includes(pending.attackContinuationKind)) {
    return "Protection pending resolution has an invalid attack continuation kind";
  }
  const weaponContinuation = pending.attackContinuationKind === "weapon_melee" || pending.attackContinuationKind === "weapon_ranged";
  if (weaponContinuation && pending.weaponHand !== "main" && pending.weaponHand !== "off" || weaponContinuation && !stableId3(pending.weaponCardId) || !weaponContinuation && (pending.weaponHand !== void 0 || pending.weaponCardId !== void 0)) {
    return "Protection pending resolution has an invalid weapon continuation identity";
  }
  if (!Array.isArray(pending.preRollDisadvantageReasons) || pending.preRollDisadvantageReasons.some((reason) => !stableId3(reason)) || new Set(pending.preRollDisadvantageReasons).size !== pending.preRollDisadvantageReasons.length || !Array.isArray(pending.protectionCandidates) || pending.protectionCandidates.some((candidate) => !validCandidate(candidate))) {
    return "Protection pending resolution has invalid pre-roll facts";
  }
  const candidateIds = pending.protectionCandidates.map((candidate) => candidate.protectorActorId);
  if (new Set(candidateIds).size !== candidateIds.length) {
    return "Protection pending resolution has duplicate protector facts";
  }
  const protectionOwners = Object.values(world.actors).filter((actor2) => actor2.capabilities.featureSources?.[PROTECTION_2024_CAPABILITY_ID]).map((actor2) => actor2.id).sort();
  if (!same2([...candidateIds].sort(), protectionOwners)) {
    return "Protection pending resolution does not cover every source-owned protector";
  }
  if (!Array.isArray(pending.remainingReactions) || pending.remainingReactions.some(({ protectorActorId, facts }) => protectorActorId !== facts.protectorActorId || !world.actors[protectorActorId] || !getProtection2024Eligibility(facts).eligible)) {
    return "Protection pending resolution has invalid queued reactions";
  }
  const request = pending.request;
  const trigger = request?.trigger;
  const currentCandidate = request ? pending.protectionCandidates.find((candidate) => candidate.protectorActorId === request.actorId) : void 0;
  const currentProtector = request ? world.actors[request.actorId] : void 0;
  if (!request || request.type !== "reaction" || !stableId3(request.id) || !world.actors[request.actorId] || !currentCandidate || currentCandidate.boardRevision !== pending.facts.boardRevision || currentCandidate.protectorCanSeeAttacker !== true || currentCandidate.protectorDistanceToTargetFt > 5 || request.actorId === pending.targetActorId || !currentProtector || !actorHoldsCanonicalShield(currentProtector) || (currentProtector.runtime.resources.reaction ?? 0) < 1 || trigger?.type !== "protection_before_attack" || trigger.sourceActorId !== pending.sourceActorId || trigger.targetActorId !== pending.targetActorId || trigger.actionId !== pending.actionId || trigger.attackId !== pending.openedByCommandId || request.options.length !== 1 || request.options[0].actionId !== PROTECTION_2024_CAPABILITY_ID) {
    return "Protection pending resolution request does not match its attack continuation";
  }
  const remainingIds = pending.remainingReactions.map((entry) => entry.protectorActorId);
  if (new Set(remainingIds).size !== remainingIds.length || remainingIds.includes(request.actorId) || pending.remainingReactions.some((entry) => {
    const candidate = pending.protectionCandidates.find((value2) => value2.protectorActorId === entry.protectorActorId);
    return !candidate || candidate.boardRevision !== pending.facts.boardRevision || entry.facts.attackId !== pending.openedByCommandId || entry.facts.attackerActorId !== pending.sourceActorId || entry.facts.targetActorId !== pending.targetActorId || entry.facts.attackRollStage !== "before_roll" || entry.facts.factsSource !== candidate.factsSource || entry.facts.protectorCanSeeAttacker !== candidate.protectorCanSeeAttacker || entry.facts.protectorDistanceToTargetFt !== candidate.protectorDistanceToTargetFt;
  })) {
    return "Protection pending resolution queue does not match its authoritative observations";
  }
  return null;
}

// src/rules-core/warlockPacts.ts
var PACT_BLADE_DAMAGE_TYPES = ["normal", "necrotic", "psychic", "radiant"];
var PACT_BLADE_STATE_CAPABILITY = "warlock.pact.blade";
var PACT_CHAIN_STATE_CAPABILITY = "warlock.pact.chain";
var PACT_TOME_STATE_CAPABILITY = "warlock.pact.tome";
function pactBladeLifecyclePolicyIssue(value) {
  if (!value || typeof value !== "object" || Array.isArray(value)) {
    return "Pact Blade lifecycle policy must be an object";
  }
  const policy = value;
  const keys = Object.keys(policy).sort();
  const expected = [
    "continuousSeparationSecondsToEnd",
    "endOnOwnerDeath",
    "separationDistanceFt"
  ];
  if (keys.length !== expected.length || keys.some((key, index) => key !== expected[index])) {
    return "Pact Blade lifecycle policy has missing or unknown fields";
  }
  if (!Number.isFinite(policy.separationDistanceFt) || Number(policy.separationDistanceFt) < 0 || !Number.isFinite(policy.continuousSeparationSecondsToEnd) || Number(policy.continuousSeparationSecondsToEnd) <= 0 || typeof policy.endOnOwnerDeath !== "boolean") {
    return "Pact Blade lifecycle policy requires finite distance, positive duration, and death behavior";
  }
  return null;
}
function validPactBladeLifecyclePolicy(value) {
  const issue = pactBladeLifecyclePolicyIssue(value);
  if (issue) throw new Error(issue);
  return { ...value };
}
function createPactBladeBond(input) {
  if (!input.candidate.objectId.trim() || !input.candidate.weaponCardId.trim()) {
    throw new Error("Pact of the Blade requires a WorldObject and immutable weapon Card identity");
  }
  if (!["simple", "martial"].includes(input.candidate.category) || input.conjure && !input.candidate.melee) {
    throw new Error("Pact of the Blade requires a Simple or Martial Melee weapon");
  }
  if (!input.conjure && (!input.candidate.magical || input.touched !== true)) {
    throw new Error("Bonding an existing pact weapon requires a touched magic weapon");
  }
  if (input.candidate.attunedToActorId && input.candidate.attunedToActorId !== input.warlockActorId) {
    throw new Error("Pact weapon is attuned to another creature");
  }
  if (input.candidate.bondedWarlockId && input.candidate.bondedWarlockId !== input.warlockActorId) {
    throw new Error("Pact weapon is bonded to another Warlock");
  }
  const bond = {
    sourceEntityId: input.sourceEntityId,
    warlockActorId: input.warlockActorId,
    weaponObjectId: input.candidate.objectId,
    weaponCardId: input.candidate.weaponCardId,
    weaponType: input.candidate.weaponType,
    normalDamageType: input.candidate.normalDamageType,
    conjured: input.conjure,
    bondedAtRevision: input.worldRevision,
    continuousSeparationSeconds: 0,
    lastDistanceBoardRevision: null
  };
  return {
    bond,
    ...input.previousBond ? { endedPreviousBond: { ...input.previousBond } } : {},
    ...input.conjure ? {
      conjuredObject: {
        id: input.candidate.objectId,
        name: input.candidate.name,
        kind: "item",
        size: "small",
        ownerActorId: input.warlockActorId,
        carriedByActorId: input.warlockActorId,
        sourceActorId: input.warlockActorId,
        sourceActionId: input.sourceEntityId,
        magicalAura: { school: "conjuration", createdBySpell: false, visible: true },
        tags: [
          "weapon",
          input.candidate.category,
          "melee",
          input.candidate.weaponType,
          "pact_weapon",
          "spellcasting_focus"
        ].sort()
      }
    } : {}
  };
}
function pactBladeAttackProjection(input) {
  if (input.bond.weaponObjectId !== input.weaponObjectId) throw new Error("Weapon is not the active pact weapon");
  return {
    attackAbility: input.useCharisma ? "cha" : input.ordinaryAbility,
    damageAbility: input.useCharisma ? "cha" : input.ordinaryAbility,
    damageType: input.damageType === "normal" ? input.bond.normalDamageType : input.damageType,
    proficient: true,
    spellcastingFocus: true
  };
}
function advancePactBladeDistance(bond, lifecyclePolicy, distanceFt, elapsedSeconds, boardRevision) {
  const policy = validPactBladeLifecyclePolicy(lifecyclePolicy);
  if (!Number.isFinite(distanceFt) || distanceFt < 0 || !Number.isFinite(elapsedSeconds) || elapsedSeconds < 0 || boardRevision !== void 0 && (!Number.isInteger(boardRevision) || boardRevision < 0)) {
    throw new Error("Pact Blade distance lifecycle requires non-negative explicit facts");
  }
  if (boardRevision !== void 0 && bond.lastDistanceBoardRevision !== null && boardRevision < bond.lastDistanceBoardRevision) {
    throw new Error("Pact Blade distance board revision is stale");
  }
  const continuousSeparationSeconds = distanceFt > policy.separationDistanceFt ? bond.continuousSeparationSeconds + elapsedSeconds : 0;
  return continuousSeparationSeconds >= policy.continuousSeparationSecondsToEnd ? null : {
    ...bond,
    continuousSeparationSeconds,
    ...boardRevision !== void 0 ? { lastDistanceBoardRevision: boardRevision } : {}
  };
}
function distinctExact(values, count, label) {
  if (values.length !== count || new Set(values).size !== count) {
    throw new Error(`${label} requires exactly ${count} distinct selections`);
  }
}
function conjurePactTome(input) {
  const sourceEntityId = requiredId(input.sourceEntityId, "Pact Tome sourceEntityId");
  const ownerActorId = requiredId(input.ownerActorId, "Pact Tome ownerActorId");
  const bookObjectId = requiredId(input.bookObjectId, "Pact Tome bookObjectId");
  const slotResource = requiredId(input.slotResource, "Pact Tome slot resource");
  if (!["short", "long"].includes(input.rest)) throw new Error("Pact Tome requires a completed rest");
  distinctExact(input.cantripActionIds, 3, "Pact Tome cantrips");
  distinctExact(input.ritualActionIds, 2, "Pact Tome rituals");
  const optionIds = input.options.map((option) => requiredId(option.actionId, "Pact Tome option"));
  if (new Set(optionIds).size !== optionIds.length) {
    throw new Error("Pact Tome spell options must have distinct action IDs");
  }
  const byId = new Map(input.options.map((option) => [option.actionId, option]));
  const alreadyPrepared = new Set(input.alreadyPreparedActionIds);
  for (const actionId of input.cantripActionIds) {
    if (byId.get(actionId)?.level !== 0) throw new Error(`${actionId} is not an eligible cantrip`);
    if (alreadyPrepared.has(actionId)) throw new Error(`${actionId} is already prepared`);
  }
  for (const actionId of input.ritualActionIds) {
    const option = byId.get(actionId);
    if (!option || option.level !== 1 || option.ritual !== true) {
      throw new Error(`${actionId} is not an eligible level-1 ritual`);
    }
    if (alreadyPrepared.has(actionId)) throw new Error(`${actionId} is already prepared`);
  }
  const grants = [
    ...input.cantripActionIds.map((actionId) => ({
      grantId: `spell-grant:${bookObjectId}:${actionId}`,
      actionId,
      sourceId: bookObjectId,
      access: "cantrip",
      level: 0,
      spellcastingAbility: "cha"
    })),
    ...input.ritualActionIds.map((actionId) => ({
      grantId: `spell-grant:${bookObjectId}:${actionId}`,
      actionId,
      sourceId: bookObjectId,
      access: "always_prepared",
      level: 1,
      spellcastingAbility: "cha",
      ritual: true,
      slotResource
    }))
  ].sort((left, right) => left.grantId.localeCompare(right.grantId));
  const tome = {
    sourceEntityId,
    ownerActorId,
    bookObjectId,
    cantripActionIds: [...input.cantripActionIds].sort(),
    ritualActionIds: [...input.ritualActionIds].sort(),
    spellGrantIds: grants.map((grant) => grant.grantId),
    createdAfterRest: input.rest
  };
  if (input.previousTome && (input.previousTome.ownerActorId !== ownerActorId || input.previousTome.sourceEntityId !== sourceEntityId)) {
    throw new Error("Pact Tome can replace only the same actor\u2019s source-owned book");
  }
  return {
    tome,
    grants,
    bookObject: {
      id: bookObjectId,
      name: "Book of Shadows",
      kind: "item",
      size: "small",
      ownerActorId,
      carriedByActorId: ownerActorId,
      sourceActorId: ownerActorId,
      sourceActionId: sourceEntityId,
      magicalAura: { school: "conjuration", createdBySpell: false, visible: true },
      tags: ["book_of_shadows", "spellcasting_focus"]
    },
    ...input.previousTome ? { replacedBookObjectId: input.previousTome.bookObjectId } : {}
  };
}
var PACT_CHAIN_SPECIAL_FORMS = [
  "imp",
  "pseudodragon",
  "quasit",
  "skeleton",
  "slaad_tadpole",
  "sphinx_of_wonder",
  "sprite",
  "venomous_snake"
];
function requiredId(value, label) {
  if (!value.trim()) throw new Error(`${label} is required`);
  return value;
}
function createPactBladeInvocationState(input) {
  return {
    kind: "blade",
    sourceEntityId: requiredId(input.sourceEntityId, "Pact Blade sourceEntityId"),
    ownerActorId: requiredId(input.ownerActorId, "Pact Blade ownerActorId"),
    bondActionId: requiredId(input.bondActionId, "Pact Blade bondActionId"),
    lifecyclePolicy: validPactBladeLifecyclePolicy(input.lifecyclePolicy),
    activeBond: null
  };
}
function createPactTomeInvocationState(input) {
  const sourceEntityId = requiredId(input.sourceEntityId, "Pact Tome sourceEntityId");
  const ownerActorId = requiredId(input.ownerActorId, "Pact Tome ownerActorId");
  if (input.tome.sourceEntityId !== sourceEntityId || input.tome.ownerActorId !== ownerActorId) {
    throw new Error("Pact Tome state must be owned by the same source and actor");
  }
  return {
    kind: "tome",
    sourceEntityId,
    ownerActorId,
    tome: {
      ...input.tome,
      cantripActionIds: [...input.tome.cantripActionIds],
      ritualActionIds: [...input.tome.ritualActionIds],
      spellGrantIds: [...input.tome.spellGrantIds]
    }
  };
}

// src/rules-core/spellcastingAccess.ts
function resourceAvailable(resources, resource) {
  return resource !== void 0 && (resources[resource] ?? 0) > 0;
}
function isPrepared(state, grant) {
  if (grant.access !== "spellbook") return true;
  return state.preparedSources[grant.sourceId]?.preparedActionIds.includes(grant.actionId) === true;
}
function resolveSpellAccess(input) {
  const candidates = input.state.grants.filter((grant2) => grant2.actionId === input.actionId);
  if (!candidates.length) {
    return {
      status: "rejected",
      code: "SpellNotGranted",
      message: `Spell action ${input.actionId} has no actor-owned grant`
    };
  }
  let grant;
  if (input.grantId !== void 0) {
    grant = candidates.find((candidate) => candidate.grantId === input.grantId);
    if (!grant) {
      return {
        status: "rejected",
        code: "SpellSourceNotGranted",
        message: `Grant ${input.grantId} does not own spell action ${input.actionId}`
      };
    }
  } else if (candidates.length > 1) {
    return {
      status: "rejected",
      code: "SpellSourceAmbiguous",
      message: `Spell action ${input.actionId} has ${candidates.length} grants; grantId is required`
    };
  } else {
    [grant] = candidates;
  }
  const mode = input.mode ?? "normal";
  if (mode === "ritual") {
    if (!grant.ritual) {
      return {
        status: "rejected",
        code: "RitualNotAllowed",
        message: `Grant ${grant.grantId} cannot cast ${grant.actionId} as a ritual`
      };
    }
    return { status: "allowed", grant, payment: { kind: "none" } };
  }
  if (grant.access === "ritual_only") {
    return {
      status: "rejected",
      code: "SpellNormalCastNotAllowed",
      message: `Grant ${grant.grantId} can cast ${grant.actionId} only as a ritual`
    };
  }
  if (!isPrepared(input.state, grant)) {
    return {
      status: "rejected",
      code: "SpellNotPrepared",
      message: `Spell action ${grant.actionId} is in ${grant.sourceId} but is not prepared`
    };
  }
  if (grant.level === 0) return { status: "allowed", grant, payment: { kind: "none" } };
  if (grant.access === "innate" && !grant.freeUseResource && !grant.slotResource) {
    return { status: "allowed", grant, payment: { kind: "none" } };
  }
  if (input.preferFreeUse !== false && resourceAvailable(input.resources, grant.freeUseResource)) {
    return {
      status: "allowed",
      grant,
      payment: { kind: "free_use", resource: grant.freeUseResource }
    };
  }
  if (resourceAvailable(input.resources, grant.slotResource)) {
    return { status: "allowed", grant, payment: { kind: "slot", resource: grant.slotResource } };
  }
  return {
    status: "rejected",
    code: "SpellResourceUnavailable",
    message: `Spell grant ${grant.grantId} has no available free use or slot resource`
  };
}

// src/rules-core/pactTomeRuntime.ts
var PACT_TOME_RUNTIME_SCHEMA_VERSION = 1;
var PACT_TOME_RITUAL_CASTING_TIME_ADDED_SECONDS = 600;
var PACT_TOME_CATALOG_PROVENANCE = "immutable_rules_catalog";
var PACT_TOME_OWNER_DEATH_FACT_PROVENANCE = "canonical_actor_lifecycle";
function cloneJson(value) {
  return JSON.parse(JSON.stringify(value));
}
function nonBlank3(value) {
  return typeof value === "string" && value.trim().length > 0;
}
function sortedUnique2(values) {
  return [...new Set(values)].sort((left, right) => left.localeCompare(right));
}
function thrownMessage(error) {
  return String(error).replace(/^Error: /, "");
}
function reject(state, code, message) {
  return { status: "rejected", code, message, state: cloneJson(state) };
}
function catalogIssue(catalog, rulesetContentHash) {
  if (catalog.provenance !== PACT_TOME_CATALOG_PROVENANCE) {
    return "Pact Tome selections require immutable rules-catalog provenance";
  }
  if (!nonBlank3(catalog.rulesetContentHash) || catalog.rulesetContentHash !== rulesetContentHash) {
    return "Pact Tome catalog provenance does not match the authoritative ruleset hash";
  }
  if (!Array.isArray(catalog.options)) return "Pact Tome catalog options must be an array";
  for (const option of catalog.options) {
    if (!Array.isArray(option.classListIds) || option.classListIds.length === 0) {
      return `${String(option.actionId)} has no authoritative class-list provenance`;
    }
    if (option.classListIds.some((classId) => !nonBlank3(classId))) {
      return `${String(option.actionId)} has a blank class-list identity`;
    }
    if (new Set(option.classListIds).size !== option.classListIds.length) {
      return `${String(option.actionId)} repeats a class-list identity`;
    }
  }
  return null;
}
function canonicalCatalog(catalog, selectedActionIds) {
  const selected = new Set(selectedActionIds);
  return {
    provenance: PACT_TOME_CATALOG_PROVENANCE,
    rulesetContentHash: catalog.rulesetContentHash,
    options: catalog.options.filter((option) => selected.has(option.actionId)).map((option) => ({
      actionId: option.actionId,
      level: option.level,
      ritual: option.ritual,
      classListIds: sortedUnique2(option.classListIds)
    })).sort((left, right) => left.actionId.localeCompare(right.actionId))
  };
}
function activeFromConjuration(result, catalog) {
  const selectedActionIds = [...result.tome.cantripActionIds, ...result.tome.ritualActionIds];
  return {
    invocation: createPactTomeInvocationState({
      sourceEntityId: result.tome.sourceEntityId,
      ownerActorId: result.tome.ownerActorId,
      tome: result.tome
    }),
    bookObject: cloneJson(result.bookObject),
    grants: cloneJson(result.grants),
    selectedFromCatalog: canonicalCatalog(catalog, selectedActionIds)
  };
}
function activeStateIssue(active, authority) {
  const provenanceIssue = catalogIssue(active.selectedFromCatalog, authority.rulesetContentHash);
  if (provenanceIssue) return provenanceIssue;
  const tome = active.invocation.tome;
  const ritualIds = new Set(tome.ritualActionIds);
  const slotResources = sortedUnique2(active.grants.filter((grant) => ritualIds.has(grant.actionId) && nonBlank3(grant.slotResource ?? "")).map((grant) => grant.slotResource));
  try {
    const expected = conjurePactTome({
      sourceEntityId: authority.sourceEntityId,
      ownerActorId: authority.ownerActorId,
      bookObjectId: tome.bookObjectId,
      rest: tome.createdAfterRest,
      cantripActionIds: tome.cantripActionIds,
      ritualActionIds: tome.ritualActionIds,
      options: active.selectedFromCatalog.options,
      alreadyPreparedActionIds: [],
      slotResource: slotResources.length === 1 ? slotResources[0] : ""
    });
    const expectedActive = activeFromConjuration(expected, active.selectedFromCatalog);
    if (canonicalStringify(expectedActive) !== canonicalStringify(active)) {
      return "Pact Tome invocation, book, grants, or catalog provenance diverged";
    }
  } catch (error) {
    return `Invalid active Pact Tome state: ${thrownMessage(error)}`;
  }
  return null;
}
function stateIssue(state) {
  if (state.schemaVersion !== PACT_TOME_RUNTIME_SCHEMA_VERSION) {
    return "Unsupported Pact Tome runtime schema version";
  }
  if (!Number.isInteger(state.revision) || state.revision < 0) {
    return "Pact Tome runtime revision must be a non-negative integer";
  }
  const authority = state.authority;
  if (authority.capabilityId !== PACT_TOME_STATE_CAPABILITY) {
    return "Pact Tome runtime capability identity is invalid";
  }
  if (!nonBlank3(authority.ownerActorId) || !nonBlank3(authority.sourceEntityId) || !nonBlank3(authority.rulesetContentHash)) {
    return "Pact Tome runtime authority requires actor, source, and ruleset identities";
  }
  if (!Array.isArray(authority.capabilitySourceEntityIds) || !authority.capabilitySourceEntityIds.includes(authority.sourceEntityId)) {
    return "Pact Tome source is not owned by the actor capability";
  }
  if (authority.capabilitySourceEntityIds.some((sourceId) => !nonBlank3(sourceId)) || canonicalStringify(authority.capabilitySourceEntityIds) !== canonicalStringify(sortedUnique2(authority.capabilitySourceEntityIds))) {
    return "Pact Tome capability sources must be non-blank, unique, and sorted";
  }
  return state.activeTome ? activeStateIssue(state.activeTome, authority) : null;
}
function pactTomeRuntimeStateIssue(state) {
  return stateIssue(state);
}
function accessShapeIssue(access) {
  if (!Array.isArray(access.grants)) return "Actor spellcasting grants must be an array";
  const grantIds = access.grants.map((grant) => grant.grantId);
  if (grantIds.some((grantId) => !nonBlank3(grantId)) || new Set(grantIds).size !== grantIds.length) {
    return "Actor spellcasting grant identities must be non-blank and unique";
  }
  if (access.grants.some((grant) => !nonBlank3(grant.actionId) || !nonBlank3(grant.sourceId))) {
    return "Actor spellcasting grants require action and source provenance";
  }
  return null;
}
function activeAccessIssue(active, access) {
  const shapeIssue = accessShapeIssue(access);
  if (shapeIssue) return shapeIssue;
  if (!active) return null;
  const bookObjectId = active.invocation.tome.bookObjectId;
  const activeGrantIds = new Set(active.grants.map((grant) => grant.grantId));
  const grantsClaimingBook = access.grants.filter((grant) => grant.sourceId === bookObjectId);
  const grantsClaimingIds = access.grants.filter((grant) => activeGrantIds.has(grant.grantId));
  if (canonicalStringify(grantsClaimingBook) !== canonicalStringify(active.grants) || canonicalStringify(grantsClaimingIds) !== canonicalStringify(active.grants)) {
    return "Actor spellcasting access does not exactly own the active Book of Shadows grants";
  }
  return null;
}
function pactTomeOwnerDeathFactIssue(fact, expected) {
  if (!fact || typeof fact !== "object" || Array.isArray(fact)) {
    return "Pact Tome owner death requires an authoritative actor-lifecycle fact";
  }
  const value = fact;
  if (value.type !== "ActorDeathAdjudicated" || value.provenance !== PACT_TOME_OWNER_DEATH_FACT_PROVENANCE) {
    return "Pact Tome owner death fact has non-authoritative provenance";
  }
  if (typeof value.factId !== "string" || !nonBlank3(value.factId) || typeof value.adjudicatedBy !== "string" || !nonBlank3(value.adjudicatedBy)) {
    return "Pact Tome owner death fact requires stable fact and adjudicator identities";
  }
  if (value.actorId !== expected.actorId) {
    return "Pact Tome owner death fact belongs to another actor";
  }
  if (!Number.isInteger(value.observedAtWorldRevision) || value.observedAtWorldRevision !== expected.worldRevision) {
    return "Pact Tome owner death fact was observed at another world revision";
  }
  if (value.rulesetContentHash !== expected.rulesetContentHash) {
    return "Pact Tome owner death fact belongs to another ruleset";
  }
  return null;
}
function preparedActionsOutsideActiveTome(active, access) {
  const excludedSourceId = active?.invocation.tome.bookObjectId;
  const probeResources = {};
  for (const grant of access.grants) {
    if (grant.freeUseResource) probeResources[grant.freeUseResource] = 1;
    if (grant.slotResource) probeResources[grant.slotResource] = 1;
  }
  const prepared = [];
  for (const grant of access.grants) {
    if (grant.sourceId === excludedSourceId) continue;
    const resolution = resolveSpellAccess({
      state: access,
      actionId: grant.actionId,
      grantId: grant.grantId,
      mode: "normal",
      resources: probeResources
    });
    if (resolution.status === "allowed" || resolution.code === "SpellResourceUnavailable") {
      prepared.push(grant.actionId);
    }
  }
  return sortedUnique2(prepared);
}
function baseCommandRejection(state, command) {
  const issue = stateIssue(state);
  if (issue) return reject(state, "InvalidState", issue);
  if (command.schemaVersion !== PACT_TOME_RUNTIME_SCHEMA_VERSION || !nonBlank3(command.commandId) || !Number.isInteger(command.expectedRevision)) {
    return reject(state, "InvalidCommand", "Pact Tome command envelope is invalid");
  }
  if (command.expectedRevision !== state.revision) {
    return reject(state, "RevisionConflict", "Pact Tome command revision is stale");
  }
  if (command.actorId !== state.authority.ownerActorId || command.sourceEntityId !== state.authority.sourceEntityId) {
    return reject(state, "AuthorityMismatch", "Pact Tome command actor or source is not authoritative");
  }
  if (command.rulesetContentHash !== state.authority.rulesetContentHash) {
    return reject(state, "RulesetMismatch", "Pact Tome command ruleset hash is not authoritative");
  }
  return null;
}
function applied(command, event, state) {
  return {
    status: "applied",
    state: cloneJson(state),
    transition: { command: cloneJson(command), event: cloneJson(event) }
  };
}
function completeRest(state, command) {
  const provenanceIssue = catalogIssue(command.catalog, command.rulesetContentHash);
  if (provenanceIssue) return reject(state, "InvalidProvenance", provenanceIssue);
  const ownershipIssue = activeAccessIssue(state.activeTome, command.actorSpellcastingAccess);
  if (ownershipIssue) return reject(state, "GrantOwnershipMismatch", ownershipIssue);
  let result;
  try {
    result = conjurePactTome({
      sourceEntityId: command.sourceEntityId,
      ownerActorId: command.actorId,
      bookObjectId: command.bookObjectId,
      rest: command.rest,
      cantripActionIds: command.cantripActionIds,
      ritualActionIds: command.ritualActionIds,
      options: command.catalog.options,
      alreadyPreparedActionIds: preparedActionsOutsideActiveTome(
        state.activeTome,
        command.actorSpellcastingAccess
      ),
      slotResource: command.slotResource,
      ...state.activeTome ? { previousTome: state.activeTome.invocation.tome } : {}
    });
  } catch (error) {
    return reject(state, "InvalidRestSelection", thrownMessage(error));
  }
  const removedGrantIds = state.activeTome?.grants.map((grant) => grant.grantId) ?? [];
  const removableGrantIds = new Set(removedGrantIds);
  const collidesWithExistingGrant = command.actorSpellcastingAccess.grants.some((existing) => !removableGrantIds.has(existing.grantId) && (result.grants.some((grant) => grant.grantId === existing.grantId) || existing.sourceId === result.tome.bookObjectId));
  if (collidesWithExistingGrant) {
    return reject(
      state,
      "GrantOwnershipMismatch",
      "New Book of Shadows identity collides with another spellcasting source"
    );
  }
  const activeTome = activeFromConjuration(result, command.catalog);
  const nextState = {
    ...cloneJson(state),
    revision: state.revision + 1,
    activeTome
  };
  const event = {
    schemaVersion: PACT_TOME_RUNTIME_SCHEMA_VERSION,
    type: "PactTomeRestCompleted",
    commandId: command.commandId,
    revision: nextState.revision,
    actorId: command.actorId,
    sourceEntityId: command.sourceEntityId,
    rest: command.rest,
    rulesetContentHash: command.rulesetContentHash,
    activeTome: cloneJson(activeTome),
    removedBookObjectIds: result.replacedBookObjectId ? [result.replacedBookObjectId] : [],
    removedSpellGrantIds: [...removedGrantIds].sort((left, right) => left.localeCompare(right))
  };
  return applied(command, event, nextState);
}
function resourceShapeIssue(resources) {
  for (const [resource, value] of Object.entries(resources)) {
    if (!nonBlank3(resource) || !Number.isInteger(value) || value < 0) {
      return "Pact Tome cast resources must be non-negative integer counts with named keys";
    }
  }
  return null;
}
function castSpell(state, command) {
  const active = state.activeTome;
  if (!active) return reject(state, "TomeUnavailable", "No active Book of Shadows exists");
  const ownershipIssue = activeAccessIssue(active, command.actorSpellcastingAccess);
  if (ownershipIssue) return reject(state, "GrantOwnershipMismatch", ownershipIssue);
  if (command.bookObjectId !== active.invocation.tome.bookObjectId) {
    return reject(state, "GrantOwnershipMismatch", "Cast does not name the active Book of Shadows");
  }
  const grant = active.grants.find((candidate) => candidate.grantId === command.grantId);
  if (!grant || grant.actionId !== command.actionId || grant.sourceId !== command.bookObjectId) {
    return reject(state, "GrantOwnershipMismatch", "Cast is not owned by the active Book of Shadows");
  }
  if (command.mode !== "normal" && command.mode !== "ritual") {
    return reject(state, "InvalidCommand", "Pact Tome cast mode must be normal or ritual");
  }
  const resourcesIssue = resourceShapeIssue(command.resources);
  if (resourcesIssue) return reject(state, "InvalidCommand", resourcesIssue);
  const resolution = resolveSpellAccess({
    state: { grants: active.grants, preparedSources: {} },
    actionId: command.actionId,
    grantId: command.grantId,
    mode: command.mode,
    resources: command.resources
  });
  if (resolution.status === "rejected") {
    return reject(state, resolution.code, resolution.message);
  }
  const resourceChanges = resolution.payment.resource ? [{ resource: resolution.payment.resource, delta: -1 }] : [];
  const nextState = {
    ...cloneJson(state),
    revision: state.revision + 1
  };
  const event = {
    schemaVersion: PACT_TOME_RUNTIME_SCHEMA_VERSION,
    type: "PactTomeSpellCast",
    commandId: command.commandId,
    revision: nextState.revision,
    actorId: command.actorId,
    sourceEntityId: command.sourceEntityId,
    bookObjectId: command.bookObjectId,
    focusObjectId: active.bookObject.id,
    actionId: command.actionId,
    grantId: command.grantId,
    mode: command.mode,
    payment: cloneJson(resolution.payment),
    resourceChanges,
    castingTimeAddedSeconds: command.mode === "ritual" ? PACT_TOME_RITUAL_CASTING_TIME_ADDED_SECONDS : 0
  };
  return applied(command, event, nextState);
}
function adjudicateOwnerDeath(state, command) {
  const factIssue = pactTomeOwnerDeathFactIssue(command.deathFact, {
    actorId: command.actorId,
    worldRevision: command.expectedRevision,
    rulesetContentHash: command.rulesetContentHash
  });
  if (factIssue) return reject(state, "InvalidOwnerDeathFact", factIssue);
  const active = state.activeTome;
  if (!active) return reject(state, "TomeUnavailable", "No active Book of Shadows exists");
  const ownershipIssue = activeAccessIssue(active, command.actorSpellcastingAccess);
  if (ownershipIssue) return reject(state, "GrantOwnershipMismatch", ownershipIssue);
  const tome = active.invocation.tome;
  const removedActionIds = sortedUnique2([
    ...tome.cantripActionIds,
    ...tome.ritualActionIds
  ]);
  const nextState = {
    ...cloneJson(state),
    revision: state.revision + 1,
    activeTome: null
  };
  const event = {
    schemaVersion: PACT_TOME_RUNTIME_SCHEMA_VERSION,
    type: "PactTomeOwnerDied",
    commandId: command.commandId,
    revision: nextState.revision,
    actorId: command.actorId,
    sourceEntityId: command.sourceEntityId,
    rulesetContentHash: command.rulesetContentHash,
    deathFact: cloneJson(command.deathFact),
    dismissedTome: cloneJson(active.invocation),
    removedBookObjectIds: [tome.bookObjectId],
    removedSpellGrantIds: active.grants.map((grant) => grant.grantId).sort((left, right) => left.localeCompare(right)),
    removedActionIds
  };
  return applied(command, event, nextState);
}
function createPactTomeRuntimeState(input) {
  const state = {
    schemaVersion: PACT_TOME_RUNTIME_SCHEMA_VERSION,
    revision: 0,
    authority: {
      capabilityId: PACT_TOME_STATE_CAPABILITY,
      ownerActorId: input.ownerActorId,
      sourceEntityId: input.sourceEntityId,
      capabilitySourceEntityIds: sortedUnique2(input.capabilitySourceEntityIds),
      rulesetContentHash: input.rulesetContentHash
    },
    activeTome: null
  };
  const issue = stateIssue(state);
  if (issue) throw new Error(issue);
  return state;
}
function transitionPactTomeRuntime(state, command) {
  const rejection = baseCommandRejection(state, command);
  if (rejection) return rejection;
  if (command.type === "CompletePactTomeRest") return completeRest(state, command);
  if (command.type === "CastPactTomeSpell") return castSpell(state, command);
  if (command.type === "AdjudicatePactTomeOwnerDeath") {
    return adjudicateOwnerDeath(state, command);
  }
  return reject(state, "InvalidCommand", "Unknown Pact Tome runtime command");
}

// src/rules-core/pactTomeWorldAdapter.ts
function cloneJson2(value) {
  return JSON.parse(JSON.stringify(value));
}
function nonBlank4(value) {
  return typeof value === "string" && value.trim().length > 0;
}
function sortedUnique3(values) {
  return [...new Set(values)].sort((left, right) => left.localeCompare(right));
}
function sameStringSet(left, right) {
  return canonicalStringify([...left].sort()) === canonicalStringify([...right].sort());
}
function rejected(code, message) {
  return { status: "rejected", code, message };
}
function selectionIssue(selection) {
  if (!selection || typeof selection !== "object") return "Pact Tome rest selection must be an object";
  if (!nonBlank4(selection.bookObjectId)) return "Pact Tome rest selection requires a book object ID";
  if (!Array.isArray(selection.cantripActionIds) || !Array.isArray(selection.ritualActionIds)) {
    return "Pact Tome rest spell selections must be arrays";
  }
  if ([...selection.cantripActionIds, ...selection.ritualActionIds].some((actionId) => !nonBlank4(actionId))) {
    return "Pact Tome rest spell selections require non-blank action IDs";
  }
  return null;
}
function invocationSourceFromSelection(actor2, selection, catalog, capabilitySources) {
  const activeSource = actor2.warlockPacts?.tome?.sourceEntityId;
  if (activeSource) return activeSource;
  const actionIds = [...selection.cantripActionIds, ...selection.ritualActionIds];
  const actions = actionIds.map((actionId) => catalog.getAction(actionId));
  if (actions.some((action) => action === void 0)) return capabilitySources[0] ?? null;
  const commonSources = capabilitySources.filter((sourceId) => actions.every((action) => action.sourceEntityIds.includes(sourceId)));
  const authoritativeSource = capabilitySources[0];
  return authoritativeSource && commonSources.includes(authoritativeSource) ? authoritativeSource : null;
}
function immutableOption(input) {
  const action = input.catalog.getAction(input.actionId);
  if (!action) return { issue: `Unknown immutable spell action ${input.actionId}` };
  if (action.kind !== "spell") return { issue: `${input.actionId} is not an immutable spell action` };
  const spell = action.spell;
  if (!action.sourceEntityIds.includes(input.sourceEntityId)) {
    return { issue: `${input.actionId} is not scoped to this Pact Tome invocation source` };
  }
  if (typeof spell.ritual !== "boolean") {
    return { issue: `${input.actionId} lacks immutable ritual metadata` };
  }
  if (!Array.isArray(spell.classListIds) || spell.classListIds.length === 0 || spell.classListIds.some((classId) => !nonBlank4(classId)) || new Set(spell.classListIds).size !== spell.classListIds.length) {
    return { issue: `${input.actionId} lacks immutable class-list provenance` };
  }
  return {
    option: {
      actionId: action.id,
      level: spell.level,
      ritual: spell.ritual,
      classListIds: sortedUnique3(spell.classListIds)
    }
  };
}
function catalogSelection(input) {
  const options = [];
  for (const actionId of input.actionIds) {
    const derived = immutableOption({
      catalog: input.catalog,
      actionId,
      sourceEntityId: input.sourceEntityId
    });
    if (derived.issue) return { issue: derived.issue };
    options.push(derived.option);
  }
  return {
    catalog: {
      provenance: PACT_TOME_CATALOG_PROVENANCE,
      rulesetContentHash: input.rulesetContentHash,
      options
    }
  };
}
function pactSlotResource(actor2, capabilitySources) {
  const ownedSources = new Set(capabilitySources);
  const fromCapabilityGrants = sortedUnique3((actor2.spellcastingAccess?.grants ?? []).filter((grant) => ownedSources.has(grant.sourceId) && grant.level === 1 && nonBlank4(grant.slotResource)).map((grant) => grant.slotResource));
  return fromCapabilityGrants.length === 1 ? fromCapabilityGrants[0] : null;
}
function activeRuntimeState(input) {
  const invocation = input.actor.warlockPacts?.tome;
  if (!invocation) return { active: null };
  if (invocation.ownerActorId !== input.actor.id || invocation.sourceEntityId !== input.sourceEntityId) {
    return { issue: "Actor Pact Tome state does not match its capability owner and source" };
  }
  const bookObject = input.world.objects[invocation.tome.bookObjectId];
  if (!bookObject) return { issue: "Active Book of Shadows object is missing from the world" };
  const access = input.actor.spellcastingAccess;
  if (!access) return { issue: "Active Pact Tome actor has no spellcasting access" };
  const grants = invocation.tome.spellGrantIds.map((grantId) => access.grants.find((grant) => grant.grantId === grantId));
  if (grants.some((grant) => grant === void 0)) {
    return { issue: "Active Book of Shadows grant is missing from actor spellcasting access" };
  }
  const selected = catalogSelection({
    catalog: input.catalog,
    actionIds: [...invocation.tome.cantripActionIds, ...invocation.tome.ritualActionIds],
    sourceEntityId: input.sourceEntityId,
    rulesetContentHash: input.world.ruleset.contentHash
  });
  if (selected.issue) return { issue: selected.issue };
  selected.catalog.options.sort((left, right) => left.actionId.localeCompare(right.actionId));
  return {
    active: {
      invocation: cloneJson2(invocation),
      bookObject: cloneJson2(bookObject),
      grants: cloneJson2(grants),
      selectedFromCatalog: selected.catalog
    }
  };
}
function componentState(input) {
  let state;
  try {
    state = createPactTomeRuntimeState({
      ownerActorId: input.actor.id,
      sourceEntityId: input.sourceEntityId,
      capabilitySourceEntityIds: input.capabilitySources,
      rulesetContentHash: input.world.ruleset.contentHash
    });
  } catch (error) {
    return { issue: String(error).replace(/^Error: /, "") };
  }
  const active = activeRuntimeState(input);
  if (active.issue) return { issue: active.issue };
  state.revision = input.world.revision;
  state.activeTome = active.active ?? null;
  const issue = pactTomeRuntimeStateIssue(state);
  return issue ? { issue } : { state };
}
function planPactTomeRestTransition(input) {
  const actor2 = input.world.actors[input.actorId];
  if (!actor2) return rejected("ActorNotFound", `Unknown actor ${input.actorId}`);
  const issue = selectionIssue(input.selection);
  if (issue) return rejected("InvalidSelection", issue);
  const capabilitySources = actor2.capabilities.featureSources?.[PACT_TOME_STATE_CAPABILITY];
  if (!capabilitySources) {
    return rejected("FeatureNotGranted", `${actor2.id} does not own Pact of the Tome`);
  }
  const sourceEntityId = invocationSourceFromSelection(
    actor2,
    input.selection,
    input.catalog,
    capabilitySources
  );
  if (!sourceEntityId || !capabilitySources.includes(sourceEntityId)) {
    return rejected("FeatureNotGranted", "Pact Tome selection has no actor-owned invocation source");
  }
  const selectedActionIds = [
    ...input.selection.cantripActionIds,
    ...input.selection.ritualActionIds
  ];
  const derived = catalogSelection({
    catalog: input.catalog,
    actionIds: selectedActionIds,
    sourceEntityId,
    rulesetContentHash: input.world.ruleset.contentHash
  });
  if (derived.issue) return rejected("InvalidCatalogAction", derived.issue);
  const runtime = componentState({
    world: input.world,
    actor: actor2,
    catalog: input.catalog,
    sourceEntityId,
    capabilitySources
  });
  if (runtime.issue) return rejected("InvalidWorldState", runtime.issue);
  const slotResource = pactSlotResource(actor2, capabilitySources);
  if (!slotResource) {
    return rejected("InvalidWorldState", "Pact Tome actor has no level-1 Pact Magic slot resource");
  }
  const result = transitionPactTomeRuntime(runtime.state, {
    schemaVersion: PACT_TOME_RUNTIME_SCHEMA_VERSION,
    type: "CompletePactTomeRest",
    commandId: input.commandId,
    expectedRevision: input.world.revision,
    rulesetContentHash: input.world.ruleset.contentHash,
    actorId: actor2.id,
    sourceEntityId,
    rest: input.rest,
    bookObjectId: input.selection.bookObjectId,
    cantripActionIds: [...input.selection.cantripActionIds],
    ritualActionIds: [...input.selection.ritualActionIds],
    catalog: derived.catalog,
    actorSpellcastingAccess: cloneJson2(actor2.spellcastingAccess ?? {
      grants: [],
      preparedSources: {}
    }),
    slotResource
  });
  if (result.status === "rejected") return rejected(result.code, result.message);
  const event = result.transition.event;
  return {
    status: "applied",
    transition: result.transition,
    event
  };
}
function planPactTomeOwnerDeathTransition(input) {
  const actor2 = input.world.actors[input.actorId];
  if (!actor2) return rejected("ActorNotFound", `Unknown actor ${input.actorId}`);
  const invocation = actor2.warlockPacts?.tome;
  if (!invocation) return rejected("TomeUnavailable", `${actor2.id} has no active Book of Shadows`);
  const capabilitySources = actor2.capabilities.featureSources?.[PACT_TOME_STATE_CAPABILITY];
  if (!capabilitySources || !capabilitySources.includes(invocation.sourceEntityId)) {
    return rejected("FeatureNotGranted", `${actor2.id} has no authoritative Pact Tome source`);
  }
  const runtime = componentState({
    world: input.world,
    actor: actor2,
    catalog: input.catalog,
    sourceEntityId: invocation.sourceEntityId,
    capabilitySources
  });
  if (runtime.issue) return rejected("InvalidWorldState", runtime.issue);
  const result = transitionPactTomeRuntime(runtime.state, {
    schemaVersion: PACT_TOME_RUNTIME_SCHEMA_VERSION,
    type: "AdjudicatePactTomeOwnerDeath",
    commandId: input.commandId,
    expectedRevision: input.world.revision,
    rulesetContentHash: input.world.ruleset.contentHash,
    actorId: actor2.id,
    sourceEntityId: invocation.sourceEntityId,
    deathFact: cloneJson2(input.deathFact),
    actorSpellcastingAccess: cloneJson2(actor2.spellcastingAccess)
  });
  if (result.status === "rejected") return rejected(result.code, result.message);
  const event = result.transition.event;
  return {
    status: "applied",
    transition: result.transition,
    event
  };
}
function eventComponentState(world, actor2, event) {
  const capabilitySources = actor2.capabilities.featureSources?.[PACT_TOME_STATE_CAPABILITY] ?? [];
  const state = createPactTomeRuntimeState({
    ownerActorId: actor2.id,
    sourceEntityId: event.sourceEntityId,
    capabilitySourceEntityIds: capabilitySources,
    rulesetContentHash: world.ruleset.contentHash
  });
  state.revision = event.revision;
  state.activeTome = cloneJson2(event.activeTome);
  return state;
}
function evolvePactTomeRestCompleted(world, event) {
  const actor2 = world.actors[event.actorId];
  if (!actor2) throw new Error(`Cannot evolve Pact Tome for unknown actor ${event.actorId}`);
  if (event.revision !== world.revision + 1 || event.rulesetContentHash !== world.ruleset.contentHash) {
    throw new Error("Pact Tome rest event revision or ruleset provenance diverged");
  }
  const component = eventComponentState(world, actor2, event);
  const componentIssue = pactTomeRuntimeStateIssue(component);
  if (componentIssue) throw new Error(`Invalid Pact Tome rest event: ${componentIssue}`);
  const previous = actor2.warlockPacts?.tome;
  if (previous) {
    const previousIssue = pactTomeActorWorldIssue(world, actor2.id);
    if (previousIssue) {
      throw new Error(`Pact Tome rest cannot replace invalid previous state: ${previousIssue}`);
    }
  }
  const expectedRemovedBooks = previous ? [previous.tome.bookObjectId] : [];
  const expectedRemovedGrants = previous ? [...previous.tome.spellGrantIds].sort() : [];
  if (!sameStringSet(event.removedBookObjectIds, expectedRemovedBooks) || !sameStringSet(event.removedSpellGrantIds, expectedRemovedGrants)) {
    throw new Error("Pact Tome rest event does not replace exactly the previous source-owned book");
  }
  const access = actor2.spellcastingAccess ?? { grants: [], preparedSources: {} };
  const removedGrantIds = new Set(event.removedSpellGrantIds);
  const retainedGrants = access.grants.filter((grant) => !removedGrantIds.has(grant.grantId));
  const newGrantIds = new Set(event.activeTome.grants.map((grant) => grant.grantId));
  if (retainedGrants.some((grant) => newGrantIds.has(grant.grantId) || grant.sourceId === event.activeTome.invocation.tome.bookObjectId)) {
    throw new Error("Pact Tome rest event collides with a retained spellcasting source");
  }
  const objects = { ...world.objects };
  for (const objectId of event.removedBookObjectIds) {
    delete objects[objectId];
  }
  if (objects[event.activeTome.bookObject.id]) {
    throw new Error(`Pact Tome rest cannot overwrite object ${event.activeTome.bookObject.id}`);
  }
  objects[event.activeTome.bookObject.id] = cloneJson2(event.activeTome.bookObject);
  const removedActionIds = new Set(previous ? [...previous.tome.cantripActionIds, ...previous.tome.ritualActionIds] : []);
  const addedActionIds = [
    ...event.activeTome.invocation.tome.cantripActionIds,
    ...event.activeTome.invocation.tome.ritualActionIds
  ];
  const actionIds = sortedUnique3([
    ...actor2.capabilities.actionIds.filter((actionId) => !removedActionIds.has(actionId)),
    ...addedActionIds
  ]);
  const nextActor = {
    ...actor2,
    capabilities: { ...actor2.capabilities, actionIds },
    spellcastingAccess: {
      grants: [...retainedGrants, ...cloneJson2(event.activeTome.grants)].sort((left, right) => left.grantId.localeCompare(right.grantId)),
      preparedSources: cloneJson2(access.preparedSources)
    },
    warlockPacts: {
      ...actor2.warlockPacts,
      tome: cloneJson2(event.activeTome.invocation)
    }
  };
  return {
    ...world,
    actors: { ...world.actors, [actor2.id]: nextActor },
    objects
  };
}
function evolvePactTomeOwnerDied(world, event) {
  const actor2 = world.actors[event.actorId];
  if (!actor2) throw new Error(`Cannot dismiss Pact Tome for unknown actor ${event.actorId}`);
  if (event.revision !== world.revision + 1 || event.rulesetContentHash !== world.ruleset.contentHash) {
    throw new Error("Pact Tome owner-death event revision or ruleset provenance diverged");
  }
  const factIssue = pactTomeOwnerDeathFactIssue(event.deathFact, {
    actorId: actor2.id,
    worldRevision: world.revision,
    rulesetContentHash: world.ruleset.contentHash
  });
  if (factIssue) throw new Error(`Invalid Pact Tome owner-death fact: ${factIssue}`);
  const invocation = actor2.warlockPacts?.tome;
  if (!invocation) throw new Error(`Actor ${actor2.id} has no active Pact Tome to dismiss`);
  const worldIssue2 = pactTomeActorWorldIssue(world, actor2.id);
  if (worldIssue2) throw new Error(`Pact Tome owner-death cannot remove foreign state: ${worldIssue2}`);
  if (event.sourceEntityId !== invocation.sourceEntityId || canonicalStringify(event.dismissedTome) !== canonicalStringify(invocation)) {
    throw new Error("Pact Tome owner-death event names a foreign invocation");
  }
  const expectedBookIds = [invocation.tome.bookObjectId];
  const expectedGrantIds = [...invocation.tome.spellGrantIds].sort();
  const expectedActionIds = sortedUnique3([
    ...invocation.tome.cantripActionIds,
    ...invocation.tome.ritualActionIds
  ]);
  if (!sameStringSet(event.removedBookObjectIds, expectedBookIds) || !sameStringSet(event.removedSpellGrantIds, expectedGrantIds) || !sameStringSet(event.removedActionIds, expectedActionIds)) {
    throw new Error("Pact Tome owner-death event does not remove exactly the active source-owned state");
  }
  const objects = { ...world.objects };
  const bookObjectId = invocation.tome.bookObjectId;
  delete objects[bookObjectId];
  const removedGrantIds = new Set(event.removedSpellGrantIds);
  const removedActionIds = new Set(event.removedActionIds);
  const access = actor2.spellcastingAccess;
  const nextPacts = { ...actor2.warlockPacts };
  delete nextPacts.tome;
  const nextActor = {
    ...actor2,
    capabilities: {
      ...actor2.capabilities,
      actionIds: actor2.capabilities.actionIds.filter((actionId) => !removedActionIds.has(actionId))
    },
    spellcastingAccess: {
      grants: access.grants.filter((grant) => !removedGrantIds.has(grant.grantId)),
      preparedSources: cloneJson2(access.preparedSources)
    },
    warlockPacts: nextPacts
  };
  if (Object.keys(nextPacts).length === 0) delete nextActor.warlockPacts;
  return {
    ...world,
    actors: { ...world.actors, [actor2.id]: nextActor },
    objects
  };
}
function pactTomeActorWorldIssue(world, actorId) {
  const actor2 = world.actors[actorId];
  if (!actor2) return `Unknown actor ${actorId}`;
  const invocation = actor2.warlockPacts?.tome;
  if (!invocation) return null;
  if (invocation.ownerActorId !== actor2.id || invocation.tome.ownerActorId !== actor2.id || invocation.tome.sourceEntityId !== invocation.sourceEntityId || !actor2.capabilities.featureSources?.[PACT_TOME_STATE_CAPABILITY]?.includes(invocation.sourceEntityId)) {
    return "Pact Tome actor has foreign owner or invocation-source state";
  }
  const object5 = world.objects[invocation.tome.bookObjectId];
  if (!object5) return "Pact Tome actor references a missing Book of Shadows";
  if (object5.kind !== "item" || object5.ownerActorId !== actor2.id || object5.carriedByActorId !== actor2.id || object5.sourceActorId !== actor2.id || object5.sourceActionId !== invocation.sourceEntityId || !object5.tags?.includes("book_of_shadows") || !object5.tags.includes("spellcasting_focus")) {
    return "Pact Tome actor references an invalid physical Book of Shadows focus";
  }
  const access = actor2.spellcastingAccess;
  if (!access) return "Pact Tome actor has no spellcasting access";
  const cantripActionIds = invocation.tome.cantripActionIds;
  const ritualActionIds = invocation.tome.ritualActionIds;
  const selectedActions = [...cantripActionIds, ...ritualActionIds];
  if (cantripActionIds.length !== 3 || ritualActionIds.length !== 2 || new Set(selectedActions).size !== 5 || invocation.tome.spellGrantIds.length !== 5) {
    return "Pact Tome actor does not own exactly three cantrips, two rituals, and five grants";
  }
  const scoped = access.grants.filter((grant) => grant.sourceId === invocation.tome.bookObjectId);
  if (!sameStringSet(scoped.map((grant) => grant.grantId), invocation.tome.spellGrantIds)) {
    return "Pact Tome spell grants diverge from the active book";
  }
  if (!selectedActions.every((actionId) => actor2.capabilities.actionIds.includes(actionId))) {
    return "Pact Tome selected action is absent from actor capabilities";
  }
  for (const grant of scoped) {
    if (cantripActionIds.includes(grant.actionId)) {
      if (grant.access !== "cantrip" || grant.level !== 0 || grant.spellcastingAbility !== "cha") {
        return "Pact Tome cantrip grant has invalid source semantics";
      }
    } else if (ritualActionIds.includes(grant.actionId)) {
      if (grant.access !== "always_prepared" || grant.level !== 1 || grant.ritual !== true || !nonBlank4(grant.slotResource) || grant.spellcastingAbility !== "cha") {
        return "Pact Tome ritual grant has invalid source semantics";
      }
    } else {
      return "Pact Tome grant points outside the active book selections";
    }
  }
  return null;
}
function pactTomeSpellCastAudit(input) {
  const actor2 = input.world.actors[input.actorId];
  if (!actor2) return { status: "rejected", message: `Unknown actor ${input.actorId}` };
  const invocation = actor2.warlockPacts?.tome;
  if (!invocation) return { status: "not_pact_tome" };
  const bookObjectId = invocation.tome.bookObjectId;
  const selectedCantrip = invocation.tome.cantripActionIds.includes(input.actionId);
  const selectedRitual = invocation.tome.ritualActionIds.includes(input.actionId);
  if (!selectedCantrip && !selectedRitual) {
    return input.sourceId === bookObjectId ? { status: "rejected", message: "Book of Shadows grant points outside its selected spells" } : { status: "not_pact_tome" };
  }
  const worldIssue2 = pactTomeActorWorldIssue(input.world, actor2.id);
  if (worldIssue2) return { status: "rejected", message: worldIssue2 };
  if (input.sourceId !== bookObjectId || !invocation.tome.spellGrantIds.includes(input.grantId)) {
    return { status: "rejected", message: "Selected Pact Tome spell is not sourced by its active book" };
  }
  const grant = actor2.spellcastingAccess.grants.find((candidate) => candidate.grantId === input.grantId);
  if (!grant || grant.actionId !== input.actionId || grant.sourceId !== bookObjectId) {
    return { status: "rejected", message: "Selected Pact Tome spell grant diverges from its active book" };
  }
  if (selectedCantrip) {
    if (grant.access !== "cantrip" || grant.level !== 0 || input.mode !== "normal" || input.payment.kind !== "none") {
      return { status: "rejected", message: "Pact Tome cantrip requires its canonical no-cost cast" };
    }
  } else if (grant.access !== "always_prepared" || grant.level !== 1 || grant.ritual !== true || !grant.slotResource || (input.mode === "ritual" ? input.payment.kind !== "none" : input.payment.kind !== "slot" || input.payment.resource !== grant.slotResource)) {
    return { status: "rejected", message: "Pact Tome ritual has invalid access or payment semantics" };
  }
  return {
    status: "ready",
    focusObjectId: bookObjectId,
    castingTimeAddedSeconds: input.mode === "ritual" ? PACT_TOME_RITUAL_CASTING_TIME_ADDED_SECONDS : 0
  };
}

// src/rules-core/pactBladeRuntime.ts
var PACT_BLADE_RUNTIME_SCHEMA_VERSION = 1;
var PACT_BLADE_CATALOG_PROVENANCE = "immutable_rules_catalog";
var PACT_BLADE_WORLD_PROVENANCE = "canonical_world_state";
var PACT_BLADE_TURN_PROVENANCE = "canonical_turn_state";
function cloneJson3(value) {
  return JSON.parse(JSON.stringify(value));
}
function nonBlank5(value) {
  return typeof value === "string" && value.trim().length > 0;
}
function sortedUnique4(values) {
  return [...new Set(values)].sort((left, right) => left.localeCompare(right));
}
function normalized(values) {
  return (values ?? []).map((value) => value.trim().toLowerCase().replace(/[ -]+/g, "_"));
}
function reject2(state, code, message) {
  return { status: "rejected", code, message, state: cloneJson3(state) };
}
function pactBladeWeaponCardSnapshot(card) {
  if (!nonBlank5(card.id) || !nonBlank5(card.card_number) || !nonBlank5(card.name) || card.type !== "weapon") {
    return "Pact Blade requires a complete immutable weapon Card identity and damage profile";
  }
  const parsed = parseWeaponProfile(card);
  if (!parsed.valid) return parsed.issue;
  const { profile } = parsed;
  const hasDeclaredEnchantment = profile.enchantment.attackBonus > 0 || profile.enchantment.damageBonus > 0 || profile.enchantment.extraDamageLines.length > 0;
  return {
    id: card.id,
    cardNumber: card.card_number,
    name: card.name,
    weaponType: profile.weaponType,
    category: profile.proficiencyCategory,
    range: profile.defaultAttackMode,
    normalDamageType: profile.damageLines[0].type,
    properties: sortedUnique4(profile.properties),
    tags: [],
    enchantBonus: hasDeclaredEnchantment ? profile.enchantment.attackBonus : null,
    requiresAttunement: profile.attunement.required
  };
}
function magicWeaponFromFacts(card, object5) {
  const objectTags = normalized(object5.tags);
  const magicTags = /* @__PURE__ */ new Set(["magic", "magical", "magic_weapon", "\u043C\u0430\u0433\u0438\u0447\u0435\u0441\u043A\u043E\u0435", "\u043C\u0430\u0433\u0438\u0447\u0435\u0441\u043A\u0438\u0439"]);
  return card.enchantBonus !== null || card.requiresAttunement || objectTags.some((tag) => magicTags.has(tag));
}
function weaponSnapshotIssue(card) {
  if (!nonBlank5(card.id) || !nonBlank5(card.cardNumber) || !nonBlank5(card.name) || !nonBlank5(card.weaponType) || !nonBlank5(card.normalDamageType) || !["simple", "martial"].includes(card.category) || !["melee", "ranged"].includes(card.range) || !Array.isArray(card.properties) || !Array.isArray(card.tags) || card.tags.length !== 0 || card.properties.some((value) => !nonBlank5(value)) || card.tags.some((value) => !nonBlank5(value)) || canonicalStringify(card.properties) !== canonicalStringify(sortedUnique4(card.properties)) || canonicalStringify(card.tags) !== canonicalStringify(sortedUnique4(card.tags)) || card.enchantBonus !== null && (!Number.isSafeInteger(card.enchantBonus) || card.enchantBonus < 0) || typeof card.requiresAttunement !== "boolean") {
    return "Pact Blade immutable weapon-profile snapshot is malformed";
  }
  return null;
}
function catalogIssue2(catalog, expectedHash) {
  if (catalog.provenance !== PACT_BLADE_CATALOG_PROVENANCE) {
    return "Pact Blade requires immutable rules-catalog provenance";
  }
  if (!nonBlank5(catalog.rulesetContentHash) || catalog.rulesetContentHash !== expectedHash) {
    return "Pact Blade catalog hash does not match the authoritative ruleset";
  }
  if (!Array.isArray(catalog.cards)) return "Pact Blade catalog cards must be an array";
  const ids = catalog.cards.map((card) => card?.id);
  if (ids.some((id) => !nonBlank5(id)) || new Set(ids).size !== ids.length) {
    return "Pact Blade catalog Card identities must be non-blank and unique";
  }
  return null;
}
function worldIssue(world, expectedHash) {
  if (world.provenance !== PACT_BLADE_WORLD_PROVENANCE) {
    return "Pact Blade requires canonical WorldState provenance";
  }
  if (!nonBlank5(world.rulesetContentHash) || world.rulesetContentHash !== expectedHash) {
    return "Pact Blade WorldState hash does not match the authoritative ruleset";
  }
  if (!Number.isInteger(world.worldRevision) || world.worldRevision < 0) {
    return "Pact Blade WorldState revision must be a non-negative integer";
  }
  if (!Array.isArray(world.weaponObjects)) return "Pact Blade weapon objects must be an array";
  const objectIds = world.weaponObjects.map((record9) => record9?.object?.id);
  if (objectIds.some((id) => !nonBlank5(id)) || new Set(objectIds).size !== objectIds.length) {
    return "Pact Blade WorldObject identities must be non-blank and unique";
  }
  for (const record9 of world.weaponObjects) {
    if (!nonBlank5(record9.weaponCardId) || !nonBlank5(record9.object.name) || record9.object.kind !== "item" || !nonBlank5(record9.object.size)) {
      return `Pact Blade WorldObject ${record9.object.id} has an invalid Card bridge or item shape`;
    }
    if (!Array.isArray(record9.touchedByActorIds) || record9.touchedByActorIds.some((actorId) => !nonBlank5(actorId)) || canonicalStringify(record9.touchedByActorIds) !== canonicalStringify(sortedUnique4(record9.touchedByActorIds))) {
      return `Pact Blade WorldObject ${record9.object.id} touch facts must be non-blank, unique, and sorted`;
    }
    if (record9.attunedToActorId !== void 0 && !nonBlank5(record9.attunedToActorId) || record9.bondedWarlockActorId !== void 0 && !nonBlank5(record9.bondedWarlockActorId)) {
      return `Pact Blade WorldObject ${record9.object.id} has an invalid attunement or bond identity`;
    }
  }
  return null;
}
function turnIssue(turn, actorId) {
  if (turn.provenance !== PACT_BLADE_TURN_PROVENANCE) {
    return "Pact Blade requires canonical turn-state provenance";
  }
  if (!Number.isInteger(turn.turnRevision) || turn.turnRevision < 0 || !nonBlank5(turn.turnId)) {
    return "Pact Blade turn authority requires a valid revision and turn identity";
  }
  if (turn.activeActorId !== actorId) return "Only the active actor can use the Pact Blade Bonus Action";
  if (!Number.isInteger(turn.bonusActionsRemaining) || turn.bonusActionsRemaining < 1) {
    return "Pact Blade requires one available Bonus Action";
  }
  return null;
}
function activeStateIssue2(active, authority, observedWorldRevision) {
  const invocation = active.invocation;
  const bond = invocation.activeBond;
  if (invocation.kind !== "blade" || invocation.sourceEntityId !== authority.sourceEntityId || invocation.ownerActorId !== authority.ownerActorId || invocation.bondActionId !== authority.bondActionId || canonicalStringify(invocation.lifecyclePolicy) !== canonicalStringify(authority.lifecyclePolicy) || !bond) {
    return "Active Pact Blade invocation is not owned by the authoritative actor and source";
  }
  const snapshotIssue = weaponSnapshotIssue(active.weaponCard);
  if (snapshotIssue) {
    return "Active Pact Blade immutable Card snapshot is invalid";
  }
  if (active.weaponCardId !== active.weaponCard.id || bond.weaponCardId !== active.weaponCardId || active.weaponObject.id !== bond.weaponObjectId || active.weaponObject.kind !== "item" || bond.sourceEntityId !== authority.sourceEntityId || bond.warlockActorId !== authority.ownerActorId || bond.weaponType !== active.weaponCard.weaponType || bond.normalDamageType !== active.weaponCard.normalDamageType) {
    return "Active Pact Blade Card, WorldObject, and bond identities diverged";
  }
  if (!Number.isInteger(bond.bondedAtRevision) || bond.bondedAtRevision < 0 || bond.bondedAtRevision > observedWorldRevision || !Number.isFinite(bond.continuousSeparationSeconds) || bond.continuousSeparationSeconds < 0 || bond.continuousSeparationSeconds >= authority.lifecyclePolicy.continuousSeparationSecondsToEnd) {
    return "Active Pact Blade bond revision or distance lifecycle is invalid";
  }
  if (active.boundAtRulesetContentHash !== authority.rulesetContentHash || bond.lastDistanceBoardRevision !== active.lastBoardRevision || active.lastBoardRevision !== null && (!Number.isInteger(active.lastBoardRevision) || active.lastBoardRevision < 0)) {
    return "Active Pact Blade provenance or board revision is invalid";
  }
  if (bond.conjured && (active.weaponObject.ownerActorId !== authority.ownerActorId || active.weaponObject.sourceActorId !== authority.ownerActorId || active.weaponObject.sourceActionId !== authority.sourceEntityId || !normalized(active.weaponObject.tags).includes("pact_weapon"))) {
    return "Conjured Pact Blade WorldObject is not source-owned";
  }
  return null;
}
function stateIssue2(state) {
  if (state.schemaVersion !== PACT_BLADE_RUNTIME_SCHEMA_VERSION) {
    return "Unsupported Pact Blade runtime schema version";
  }
  if (!Number.isInteger(state.revision) || state.revision < 0 || !Number.isInteger(state.observedWorldRevision) || state.observedWorldRevision < 0) {
    return "Pact Blade runtime revisions must be non-negative integers";
  }
  const authority = state.authority;
  if (authority.capabilityId !== PACT_BLADE_STATE_CAPABILITY) {
    return "Pact Blade runtime capability identity is invalid";
  }
  if (!nonBlank5(authority.ownerActorId) || !nonBlank5(authority.sourceEntityId) || !nonBlank5(authority.bondActionId) || !nonBlank5(authority.rulesetContentHash)) {
    return "Pact Blade runtime authority requires actor, source, action, and ruleset identities";
  }
  const lifecycleIssue = pactBladeLifecyclePolicyIssue(authority.lifecyclePolicy);
  if (lifecycleIssue) return lifecycleIssue;
  if (!Array.isArray(authority.capabilitySourceEntityIds) || !authority.capabilitySourceEntityIds.includes(authority.sourceEntityId)) {
    return "Pact Blade source is not owned by the actor capability";
  }
  if (authority.capabilitySourceEntityIds.some((sourceId) => !nonBlank5(sourceId)) || canonicalStringify(authority.capabilitySourceEntityIds) !== canonicalStringify(sortedUnique4(authority.capabilitySourceEntityIds))) {
    return "Pact Blade capability sources must be non-blank, unique, and sorted";
  }
  return state.activeBlade ? activeStateIssue2(state.activeBlade, authority, state.observedWorldRevision) : null;
}
function baseCommandRejection2(state, command) {
  const issue = stateIssue2(state);
  if (issue) return reject2(state, "InvalidState", issue);
  if (command.schemaVersion !== PACT_BLADE_RUNTIME_SCHEMA_VERSION || !nonBlank5(command.commandId) || !Number.isInteger(command.expectedRevision) || command.expectedRevision < 0) {
    return reject2(state, "InvalidCommand", "Pact Blade command envelope is invalid");
  }
  if (command.expectedRevision !== state.revision) {
    return reject2(state, "RevisionConflict", "Pact Blade command revision is stale");
  }
  if (command.actorId !== state.authority.ownerActorId || command.sourceEntityId !== state.authority.sourceEntityId) {
    return reject2(state, "AuthorityMismatch", "Pact Blade command actor or source is not authoritative");
  }
  if (command.rulesetContentHash !== state.authority.rulesetContentHash) {
    return reject2(state, "RulesetMismatch", "Pact Blade command ruleset hash is not authoritative");
  }
  return null;
}
function applied2(state, command, event) {
  return {
    status: "applied",
    state: cloneJson3(state),
    transition: { command: cloneJson3(command), event: cloneJson3(event) }
  };
}
function catalogCard(state, catalog, cardId) {
  const issue = catalogIssue2(catalog, state.authority.rulesetContentHash);
  if (issue) return reject2(state, "InvalidProvenance", issue);
  const card = catalog.cards.find((candidate) => candidate.id === cardId);
  if (!card) return reject2(state, "IllegalWeapon", `Unknown immutable weapon Card ${cardId}`);
  const snapshot = pactBladeWeaponCardSnapshot(card);
  if (typeof snapshot === "string") return reject2(state, "IllegalWeapon", snapshot);
  return snapshot;
}
function checkedWorld(state, world) {
  const issue = worldIssue(world, state.authority.rulesetContentHash);
  if (issue) return reject2(state, "InvalidProvenance", issue);
  if (world.worldRevision < state.observedWorldRevision) {
    return reject2(state, "WorldRevisionConflict", "Pact Blade WorldState revision is stale");
  }
  return null;
}
function activeWorldRecord(state, world) {
  const active = state.activeBlade;
  const record9 = world.weaponObjects.find((candidate) => candidate.object.id === active.invocation.activeBond?.weaponObjectId);
  if (!record9 || record9.weaponCardId !== active.weaponCardId) {
    return reject2(state, "WeaponMismatch", "Active Pact Blade Card and WorldObject bridge is unavailable");
  }
  if (record9.bondedWarlockActorId && record9.bondedWarlockActorId !== state.authority.ownerActorId) {
    return reject2(state, "BondedToAnother", "Active pact weapon is bonded to another Warlock");
  }
  return record9;
}
function bondBlade(state, command) {
  const snapshot = catalogCard(state, command.catalog, command.weaponCardId);
  if ("status" in snapshot) return snapshot;
  if (command.mode === "conjure" && snapshot.range !== "melee") {
    return reject2(state, "IllegalWeapon", "A conjured Pact Blade must be a Simple or Martial Melee weapon");
  }
  const worldRejection = checkedWorld(state, command.world);
  if (worldRejection) return worldRejection;
  const turnProblem = turnIssue(command.turn, command.actorId);
  if (turnProblem) return reject2(state, "TurnUnavailable", turnProblem);
  if (!nonBlank5(command.weaponObjectId)) {
    return reject2(state, "WorldObjectConflict", "Pact Blade requires a unique WorldObject identity");
  }
  const previous = state.activeBlade;
  if (previous) {
    const previousRecord = command.world.weaponObjects.find((record9) => record9.object.id === previous.weaponObject.id && record9.weaponCardId === previous.weaponCardId);
    if (!previousRecord) {
      return reject2(state, "WeaponMismatch", "The prior active pact weapon is absent from WorldState");
    }
    if (previous.weaponObject.id === command.weaponObjectId) {
      return reject2(state, "WorldObjectConflict", "The named WorldObject is already the active pact weapon");
    }
  }
  const existing = command.world.weaponObjects.find((record9) => record9.object.id === command.weaponObjectId);
  let object5;
  let magical = false;
  let touched = false;
  let attunedToActorId;
  let bondedWarlockId;
  if (command.mode === "conjure") {
    if (existing) {
      return reject2(state, "WorldObjectConflict", "Conjured pact weapon WorldObject identity already exists");
    }
  } else if (command.mode === "touch_existing") {
    if (!existing || existing.weaponCardId !== snapshot.id) {
      return reject2(state, "WeaponMismatch", "Touched WorldObject does not bridge to the selected immutable Card");
    }
    touched = existing.touchedByActorIds.includes(command.actorId);
    if (!touched) return reject2(state, "TouchRequired", "Warlock is not touching the existing weapon");
    magical = magicWeaponFromFacts(snapshot, existing.object);
    if (!magical) return reject2(state, "MagicWeaponRequired", "An existing pact weapon must be magical");
    attunedToActorId = existing.attunedToActorId;
    bondedWarlockId = existing.bondedWarlockActorId;
    if (attunedToActorId && attunedToActorId !== command.actorId) {
      return reject2(state, "AttunedToAnother", "Pact weapon is attuned to another creature");
    }
    if (bondedWarlockId && bondedWarlockId !== command.actorId) {
      return reject2(state, "BondedToAnother", "Pact weapon is bonded to another Warlock");
    }
  } else {
    return reject2(state, "InvalidCommand", "Pact Blade bond mode is invalid");
  }
  const result = createPactBladeBond({
    sourceEntityId: command.sourceEntityId,
    warlockActorId: command.actorId,
    worldRevision: command.world.worldRevision,
    candidate: {
      objectId: command.weaponObjectId,
      weaponCardId: snapshot.id,
      name: snapshot.name,
      weaponType: snapshot.weaponType,
      category: snapshot.category,
      melee: snapshot.range === "melee",
      magical,
      normalDamageType: snapshot.normalDamageType,
      ...attunedToActorId ? { attunedToActorId } : {},
      ...bondedWarlockId ? { bondedWarlockId } : {}
    },
    ...previous?.invocation.activeBond ? { previousBond: previous.invocation.activeBond } : {},
    conjure: command.mode === "conjure",
    touched
  });
  object5 = result.conjuredObject ?? cloneJson3(existing.object);
  const invocation = {
    ...createPactBladeInvocationState({
      sourceEntityId: command.sourceEntityId,
      ownerActorId: command.actorId,
      bondActionId: state.authority.bondActionId,
      lifecyclePolicy: state.authority.lifecyclePolicy
    }),
    activeBond: result.bond
  };
  const activeBlade = {
    invocation,
    weaponCardId: snapshot.id,
    weaponCard: cloneJson3(snapshot),
    weaponObject: cloneJson3(object5),
    boundAtRulesetContentHash: command.rulesetContentHash,
    lastBoardRevision: null
  };
  const nextState = {
    ...cloneJson3(state),
    revision: state.revision + 1,
    observedWorldRevision: command.world.worldRevision,
    activeBlade
  };
  const removedWorldObjectIds = previous?.invocation.activeBond?.conjured ? [previous.weaponObject.id] : [];
  const event = {
    schemaVersion: PACT_BLADE_RUNTIME_SCHEMA_VERSION,
    type: "PactBladeBonded",
    commandId: command.commandId,
    revision: nextState.revision,
    actorId: command.actorId,
    sourceEntityId: command.sourceEntityId,
    rulesetContentHash: command.rulesetContentHash,
    worldRevision: command.world.worldRevision,
    turnId: command.turn.turnId,
    turnRevision: command.turn.turnRevision,
    actionCost: { kind: "bonus_action", amount: 1 },
    activeBlade: cloneJson3(activeBlade),
    ...result.endedPreviousBond ? { endedPreviousBond: result.endedPreviousBond } : {},
    removedWorldObjectIds,
    upsertWorldObjects: result.conjuredObject ? [cloneJson3(result.conjuredObject)] : [],
    removedWeaponBridgeObjectIds: [...removedWorldObjectIds],
    upsertWeaponBridges: result.conjuredObject ? [{ weaponObjectId: result.conjuredObject.id, weaponCardId: snapshot.id }] : [],
    clearPactBondObjectIds: previous ? [previous.weaponObject.id] : [],
    setPactBondObjectId: object5.id
  };
  return applied2(nextState, command, event);
}
function projectAttack(state, command) {
  const active = state.activeBlade;
  if (!active) return reject2(state, "BladeUnavailable", "No active Pact Blade bond exists");
  const snapshot = catalogCard(state, command.catalog, command.weaponCardId);
  if ("status" in snapshot) return snapshot;
  const worldRejection = checkedWorld(state, command.world);
  if (worldRejection) return worldRejection;
  const record9 = activeWorldRecord(state, command.world);
  if ("status" in record9) return record9;
  if (command.weaponCardId !== active.weaponCardId || command.weaponObjectId !== active.weaponObject.id || canonicalStringify(snapshot) !== canonicalStringify(active.weaponCard)) {
    return reject2(state, "WeaponMismatch", "Attack does not use the active immutable Pact Blade Card and WorldObject");
  }
  if (command.abilityChoice !== "ordinary" && command.abilityChoice !== "charisma" || command.ordinaryAbility !== "str" && command.ordinaryAbility !== "dex" || command.ordinaryAbility === "dex" && !snapshot.properties.includes("finesse") || !PACT_BLADE_DAMAGE_TYPES.includes(command.damageType)) {
    return reject2(state, "IllegalAttackChoice", "Pact Blade attack ability or damage choice is illegal");
  }
  const projection = pactBladeAttackProjection({
    bond: active.invocation.activeBond,
    weaponObjectId: command.weaponObjectId,
    useCharisma: command.abilityChoice === "charisma",
    ordinaryAbility: command.ordinaryAbility,
    damageType: command.damageType
  });
  const nextState = {
    ...cloneJson3(state),
    revision: state.revision + 1,
    observedWorldRevision: command.world.worldRevision,
    activeBlade: {
      ...cloneJson3(active),
      weaponObject: cloneJson3(record9.object)
    }
  };
  const event = {
    schemaVersion: PACT_BLADE_RUNTIME_SCHEMA_VERSION,
    type: "PactBladeAttackProjected",
    commandId: command.commandId,
    revision: nextState.revision,
    actorId: command.actorId,
    sourceEntityId: command.sourceEntityId,
    rulesetContentHash: command.rulesetContentHash,
    worldRevision: command.world.worldRevision,
    weaponCardId: command.weaponCardId,
    weaponObjectId: command.weaponObjectId,
    projection
  };
  return applied2(nextState, command, event);
}
function distanceFactsIssue(facts, active, ownerActorId) {
  if (!["scenario", "board", "gm_ruling"].includes(facts.factsSource) || !Number.isInteger(facts.boardRevision) || facts.boardRevision < 0 || facts.actorId !== ownerActorId || facts.weaponObjectId !== active.weaponObject.id || !Number.isFinite(facts.distanceFt) || facts.distanceFt < 0 || !Number.isFinite(facts.elapsedSeconds) || facts.elapsedSeconds < 0) {
    return "Pact Blade distance lifecycle requires authoritative non-negative explicit facts";
  }
  if (active.lastBoardRevision !== null && facts.boardRevision < active.lastBoardRevision) {
    return "Pact Blade distance board revision is stale";
  }
  return null;
}
function advanceDistance(state, command) {
  const active = state.activeBlade;
  if (!active) return reject2(state, "BladeUnavailable", "No active Pact Blade bond exists");
  const worldRejection = checkedWorld(state, command.world);
  if (worldRejection) return worldRejection;
  const record9 = activeWorldRecord(state, command.world);
  if ("status" in record9) return record9;
  const factsProblem = distanceFactsIssue(command.facts, active, command.actorId);
  if (factsProblem) return reject2(state, "InvalidDistanceFacts", factsProblem);
  const previousBond = active.invocation.activeBond;
  const advanced = advancePactBladeDistance(
    previousBond,
    active.invocation.lifecyclePolicy,
    command.facts.distanceFt,
    command.facts.elapsedSeconds,
    command.facts.boardRevision
  );
  const nextActive = advanced ? {
    ...cloneJson3(active),
    invocation: { ...cloneJson3(active.invocation), activeBond: advanced },
    weaponObject: cloneJson3(record9.object),
    lastBoardRevision: command.facts.boardRevision
  } : null;
  const nextState = {
    ...cloneJson3(state),
    revision: state.revision + 1,
    observedWorldRevision: command.world.worldRevision,
    activeBlade: nextActive
  };
  const removedWorldObjectIds = !advanced && previousBond.conjured ? [active.weaponObject.id] : [];
  const event = {
    schemaVersion: PACT_BLADE_RUNTIME_SCHEMA_VERSION,
    type: "PactBladeDistanceAdvanced",
    commandId: command.commandId,
    revision: nextState.revision,
    actorId: command.actorId,
    sourceEntityId: command.sourceEntityId,
    rulesetContentHash: command.rulesetContentHash,
    worldRevision: command.world.worldRevision,
    facts: cloneJson3(command.facts),
    previousBond: cloneJson3(previousBond),
    activeBlade: cloneJson3(nextActive),
    pactState: cloneJson3(nextActive?.invocation ?? createPactBladeInvocationState({
      sourceEntityId: state.authority.sourceEntityId,
      ownerActorId: state.authority.ownerActorId,
      bondActionId: state.authority.bondActionId,
      lifecyclePolicy: state.authority.lifecyclePolicy
    })),
    bondEnded: advanced === null,
    removedWorldObjectIds,
    removedWeaponBridgeObjectIds: [...removedWorldObjectIds]
  };
  return applied2(nextState, command, event);
}
function ownerDeathFactsIssue(facts, ownerActorId, worldRevision, rulesetContentHash) {
  if (!facts || facts.type !== "ActorDeathAdjudicated" || facts.provenance !== "canonical_actor_lifecycle" || !nonBlank5(facts.factId) || !nonBlank5(facts.adjudicatedBy) || facts.actorId !== ownerActorId || !Number.isInteger(facts.observedAtWorldRevision) || facts.observedAtWorldRevision !== worldRevision || facts.rulesetContentHash !== rulesetContentHash) {
    return "Pact Blade death lifecycle requires an authoritative explicit owner-death fact";
  }
  return null;
}
function endOnOwnerDeath(state, command) {
  const active = state.activeBlade;
  if (!active) return reject2(state, "BladeUnavailable", "No active Pact Blade bond exists");
  if (!active.invocation.lifecyclePolicy.endOnOwnerDeath) {
    return reject2(state, "InvalidDeathFacts", "Pact Blade declaration does not end this bond on owner death");
  }
  const worldRejection = checkedWorld(state, command.world);
  if (worldRejection) return worldRejection;
  const record9 = activeWorldRecord(state, command.world);
  if ("status" in record9) return record9;
  const factsProblem = ownerDeathFactsIssue(
    command.deathFact,
    command.actorId,
    command.world.worldRevision,
    command.rulesetContentHash
  );
  if (factsProblem) return reject2(state, "InvalidDeathFacts", factsProblem);
  const previousBond = active.invocation.activeBond;
  const pactState = createPactBladeInvocationState({
    sourceEntityId: state.authority.sourceEntityId,
    ownerActorId: state.authority.ownerActorId,
    bondActionId: state.authority.bondActionId,
    lifecyclePolicy: state.authority.lifecyclePolicy
  });
  const removedWorldObjectIds = previousBond.conjured ? [active.weaponObject.id] : [];
  const nextState = {
    ...cloneJson3(state),
    revision: state.revision + 1,
    observedWorldRevision: command.world.worldRevision,
    activeBlade: null
  };
  const event = {
    schemaVersion: PACT_BLADE_RUNTIME_SCHEMA_VERSION,
    type: "PactBladeEndedOnOwnerDeath",
    commandId: command.commandId,
    revision: nextState.revision,
    actorId: command.actorId,
    sourceEntityId: command.sourceEntityId,
    rulesetContentHash: command.rulesetContentHash,
    worldRevision: command.world.worldRevision,
    deathFact: cloneJson3(command.deathFact),
    previousBond: cloneJson3(previousBond),
    pactState,
    removedWorldObjectIds,
    removedWeaponBridgeObjectIds: [...removedWorldObjectIds]
  };
  return applied2(nextState, command, event);
}
function createPactBladeRuntimeState(input) {
  const state = {
    schemaVersion: PACT_BLADE_RUNTIME_SCHEMA_VERSION,
    revision: 0,
    observedWorldRevision: input.observedWorldRevision ?? 0,
    authority: {
      capabilityId: PACT_BLADE_STATE_CAPABILITY,
      ownerActorId: input.ownerActorId,
      sourceEntityId: input.sourceEntityId,
      bondActionId: input.bondActionId,
      capabilitySourceEntityIds: sortedUnique4(input.capabilitySourceEntityIds),
      rulesetContentHash: input.rulesetContentHash,
      lifecyclePolicy: cloneJson3(input.lifecyclePolicy)
    },
    activeBlade: null
  };
  const issue = stateIssue2(state);
  if (issue) throw new Error(issue);
  return state;
}
function transitionPactBladeRuntime(state, command) {
  const rejection = baseCommandRejection2(state, command);
  if (rejection) return rejection;
  if (command.type === "BondPactBlade") return bondBlade(state, command);
  if (command.type === "ProjectPactBladeAttack") return projectAttack(state, command);
  if (command.type === "AdvancePactBladeDistance") return advanceDistance(state, command);
  if (command.type === "EndPactBladeOnOwnerDeath") return endOnOwnerDeath(state, command);
  return reject2(state, "InvalidCommand", "Unknown Pact Blade runtime command");
}

// src/rules-core/warlockPactDeclaration.ts
function object4(value) {
  return value && typeof value === "object" && !Array.isArray(value) ? value : null;
}
function nonBlank6(value) {
  return typeof value === "string" && value.trim() === value && value.length > 0 ? value : null;
}
function pactBladeLifecyclePolicy(value) {
  const policy = object4(value);
  if (!policy) return null;
  const keys = Object.keys(policy).sort();
  const expected = [
    "continuous_separation_seconds_to_end",
    "end_on_owner_death",
    "separation_distance_ft"
  ];
  if (keys.length !== expected.length || keys.some((key, index) => key !== expected[index])) {
    return null;
  }
  if (!Number.isFinite(policy.separation_distance_ft) || Number(policy.separation_distance_ft) < 0 || !Number.isFinite(policy.continuous_separation_seconds_to_end) || Number(policy.continuous_separation_seconds_to_end) <= 0 || typeof policy.end_on_owner_death !== "boolean") return null;
  return {
    separationDistanceFt: Number(policy.separation_distance_ft),
    continuousSeparationSecondsToEnd: Number(policy.continuous_separation_seconds_to_end),
    endOnOwnerDeath: policy.end_on_owner_death
  };
}
function hasGrantSpell(mechanics, reference) {
  const effects = Array.isArray(mechanics.effects) ? mechanics.effects : [];
  const matches = effects.flatMap((effect) => {
    const row = object4(effect);
    const result = Array.isArray(row?.result) ? row.result : [];
    return result.filter((payload) => {
      const grant = object4(payload);
      return grant?.kind === "grant_spell" && grant.value === reference;
    });
  });
  return matches.length === 1;
}
function hasRestChoice(mechanics, choiceId) {
  const effects = Array.isArray(mechanics.effects) ? mechanics.effects : [];
  return effects.some((effect) => {
    const choice = object4(effect);
    return choice?.kind === "choice" && choice.id === choiceId && choice.context === "in_play" && choice.resolution === "on_rest";
  });
}
function bindWarlockPactDeclaration(mechanicsValue) {
  const mechanics = object4(mechanicsValue);
  const primitive = object4(mechanics?.primitive);
  const activation = object4(mechanics?.activation);
  if (!mechanics || !primitive || !activation) return null;
  const capabilityId = nonBlank6(primitive.stateCapability);
  if (primitive.type === "pact_blade_bond") {
    const lifecyclePolicy = pactBladeLifecyclePolicy(primitive.policy);
    return activation.mode === "active" && capabilityId === PACT_BLADE_STATE_CAPABILITY && lifecyclePolicy ? {
      kind: "blade",
      primitiveType: "pact_blade_bond",
      capabilityId: PACT_BLADE_STATE_CAPABILITY,
      lifecyclePolicy
    } : null;
  }
  if (primitive.type === "pact_chain_familiar") {
    const grantedSpell = nonBlank6(primitive.grantedSpell);
    return activation.mode === "passive" && capabilityId === PACT_CHAIN_STATE_CAPABILITY && grantedSpell && hasGrantSpell(mechanics, grantedSpell) ? {
      kind: "chain",
      primitiveType: "pact_chain_familiar",
      capabilityId: PACT_CHAIN_STATE_CAPABILITY,
      grantedSpell
    } : null;
  }
  if (primitive.type === "pact_tome_book") {
    const cantripChoiceId = nonBlank6(primitive.cantripChoiceId);
    const ritualChoiceId = nonBlank6(primitive.ritualChoiceId);
    const slotResource = nonBlank6(primitive.slotResource);
    return activation.mode === "passive" && capabilityId === PACT_TOME_STATE_CAPABILITY && cantripChoiceId && ritualChoiceId && cantripChoiceId !== ritualChoiceId && primitive.bookObjectKind === "book_of_shadows" && slotResource && hasRestChoice(mechanics, cantripChoiceId) && hasRestChoice(mechanics, ritualChoiceId) ? {
      kind: "tome",
      primitiveType: "pact_tome_book",
      capabilityId: PACT_TOME_STATE_CAPABILITY,
      cantripChoiceId,
      ritualChoiceId,
      bookObjectKind: "book_of_shadows",
      slotResource
    } : null;
  }
  return null;
}

// src/rules-core/pactBladeWorldAdapter.ts
function cloneJson4(value) {
  return JSON.parse(JSON.stringify(value));
}
function nonBlank7(value) {
  return typeof value === "string" && value.trim().length > 0;
}
function sortedUnique5(values) {
  return [...new Set(values)].sort((left, right) => left.localeCompare(right));
}
function normalized2(values) {
  if (values == null) return [];
  if (!Array.isArray(values) || values.some((value) => typeof value !== "string")) return null;
  return values.map((value) => value.trim().toLowerCase().replace(/[ -]+/g, "_"));
}
function rejected2(code, message) {
  return { status: "rejected", code, message };
}
function cardResolver(catalog) {
  const resolver = catalog.getCard;
  return typeof resolver === "function" ? resolver.bind(catalog) : null;
}
function catalogCard2(catalog, cardId) {
  const resolver = cardResolver(catalog);
  if (!resolver) {
    return { issue: rejected2(
      "CatalogCardResolverUnavailable",
      "The authoritative RulesCatalog does not expose getCard"
    ) };
  }
  const card = resolver(cardId);
  if (!card || card.id !== cardId) {
    return { issue: rejected2("InvalidCatalogCard", `Unknown immutable weapon Card ${cardId}`) };
  }
  const snapshot = pactBladeWeaponCardSnapshot(card);
  if (typeof snapshot === "string") {
    return { issue: rejected2("InvalidCatalogCard", snapshot) };
  }
  return { card: cloneJson4(card), snapshot };
}
function itemObject(world, objectId) {
  return world.objects[objectId];
}
function canonicalBond(invocation) {
  if (!invocation || !nonBlank7(invocation.weaponCardId)) {
    return "Active Pact Blade bond has no immutable weaponCardId";
  }
  const lastDistanceBoardRevision = invocation.lastDistanceBoardRevision ?? null;
  if (lastDistanceBoardRevision !== null && (!Number.isInteger(lastDistanceBoardRevision) || lastDistanceBoardRevision < 0)) {
    return "Active Pact Blade bond has an invalid lastDistanceBoardRevision";
  }
  return {
    ...cloneJson4(invocation),
    weaponCardId: invocation.weaponCardId,
    lastDistanceBoardRevision
  };
}
function actorInvocation(actor2) {
  const preview = actor2;
  const invocation = preview.warlockPacts?.blade;
  if (!invocation) return "Actor Pact Blade invocation state is missing";
  const bond = invocation.activeBond ? canonicalBond(invocation.activeBond) : null;
  if (typeof bond === "string") return bond;
  return {
    kind: invocation.kind,
    sourceEntityId: invocation.sourceEntityId,
    ownerActorId: invocation.ownerActorId,
    bondActionId: invocation.bondActionId,
    lifecyclePolicy: cloneJson4(invocation.lifecyclePolicy),
    activeBond: bond
  };
}
function capabilityContext(actor2) {
  const sources = actor2.capabilities.featureSources?.[PACT_BLADE_STATE_CAPABILITY];
  if (!Array.isArray(sources)) {
    return { issue: rejected2("FeatureNotGranted", `${actor2.id} does not own Pact of the Blade`) };
  }
  if (sources.some((sourceId) => !nonBlank7(sourceId))) {
    return { issue: rejected2("InvalidWorldState", "Pact Blade capability sources must be non-blank") };
  }
  const invocation = actorInvocation(actor2);
  if (typeof invocation === "string") {
    return { issue: rejected2("InvalidWorldState", invocation) };
  }
  if (invocation.kind !== "blade" || invocation.ownerActorId !== actor2.id || !sources.includes(invocation.sourceEntityId) || !actor2.capabilities.actionIds.includes(invocation.bondActionId)) {
    return {
      issue: rejected2(
        "InvalidWorldState",
        "Pact Blade invocation owner, source, or bond action diverges from actor capabilities"
      )
    };
  }
  return {
    invocation,
    capabilitySources: sortedUnique5(sources)
  };
}
function bondActionIssue(actor2, invocation, catalog) {
  const action = catalog.getAction(invocation.bondActionId);
  if (!action || action.kind !== "nonSpell") {
    return "Pact Blade bond action is absent from the immutable catalog";
  }
  if (!action.sourceEntityIds.includes(invocation.sourceEntityId) || !actor2.capabilities.actionIds.includes(action.id)) {
    return "Pact Blade bond action is not scoped to the actor-owned invocation";
  }
  const declaration = bindWarlockPactDeclaration(action.mechanics);
  if (declaration?.kind !== "blade" || canonicalStringify(declaration.lifecyclePolicy) !== canonicalStringify(invocation.lifecyclePolicy)) {
    return "Pact Blade lifecycle state diverges from its immutable mechanics declaration";
  }
  return null;
}
function bondedWarlocks(world, objectId) {
  return Object.values(world.actors).flatMap((candidate) => {
    const invocation = candidate.warlockPacts?.blade;
    return invocation?.activeBond?.weaponObjectId === objectId ? [candidate.id] : [];
  }).sort((left, right) => left.localeCompare(right));
}
function objectRecord(world, objectId, touchedByActorId) {
  const object5 = itemObject(world, objectId);
  if (!object5 || object5.kind !== "item" || !nonBlank7(object5.itemCardId)) {
    return `Pact Blade object ${objectId} is not an item with an immutable itemCardId bridge`;
  }
  const warlocks = bondedWarlocks(world, objectId);
  if (warlocks.length > 1) return `Pact Blade object ${objectId} is bonded by multiple Warlocks`;
  return {
    object: cloneJson4(object5),
    weaponCardId: object5.itemCardId,
    touchedByActorIds: touchedByActorId ? [touchedByActorId] : [],
    ...object5.attunedToActorId ? { attunedToActorId: object5.attunedToActorId } : {},
    ...warlocks[0] ? { bondedWarlockActorId: warlocks[0] } : {}
  };
}
function worldAuthority(world, objectIds, touchedObjectId, touchedByActorId) {
  const records = [];
  for (const objectId of sortedUnique5(objectIds)) {
    const record9 = objectRecord(
      world,
      objectId,
      objectId === touchedObjectId ? touchedByActorId : void 0
    );
    if (typeof record9 === "string") return record9;
    records.push(record9);
  }
  return {
    provenance: PACT_BLADE_WORLD_PROVENANCE,
    rulesetContentHash: world.ruleset.contentHash,
    worldRevision: world.revision,
    weaponObjects: records
  };
}
function activeState(input) {
  const bond = input.invocation.activeBond;
  if (!bond) return null;
  const object5 = itemObject(input.world, bond.weaponObjectId);
  if (!object5 || object5.kind !== "item") return "Active Pact Blade WorldObject is missing or not an item";
  if (!nonBlank7(object5.itemCardId) || object5.itemCardId !== bond.weaponCardId) {
    return "Active Pact Blade Card and WorldObject itemCardId bridge diverged";
  }
  const derived = catalogCard2(input.catalog, bond.weaponCardId);
  if (derived.issue) return derived.issue.message;
  if (bond.weaponType !== derived.snapshot.weaponType || bond.normalDamageType !== derived.snapshot.normalDamageType) {
    return "Active Pact Blade bond diverges from its immutable weapon Card";
  }
  const warlocks = bondedWarlocks(input.world, object5.id);
  if (warlocks.length !== 1 || warlocks[0] !== input.actor.id) {
    return "Active Pact Blade WorldObject has an invalid Warlock bond owner";
  }
  if (object5.attunedToActorId && object5.attunedToActorId !== input.actor.id) {
    return "Active pact weapon is attuned to another creature";
  }
  if (object5.heldByActorId === void 0 !== (object5.heldInHand === void 0) || object5.heldByActorId !== void 0 && object5.carriedByActorId !== object5.heldByActorId) {
    return "Pact Blade held-item identity is incomplete or diverges from its carrier";
  }
  if (bond.conjured && (object5.ownerActorId !== input.actor.id || object5.carriedByActorId !== input.actor.id || object5.sourceActorId !== input.actor.id || object5.sourceActionId !== input.invocation.sourceEntityId || !normalized2(object5.tags)?.includes("pact_weapon"))) {
    return "Conjured Pact Blade object is not source-owned";
  }
  return {
    invocation: cloneJson4(input.invocation),
    weaponCardId: bond.weaponCardId,
    weaponCard: cloneJson4(derived.snapshot),
    weaponObject: cloneJson4(object5),
    boundAtRulesetContentHash: input.world.ruleset.contentHash,
    lastBoardRevision: bond.lastDistanceBoardRevision
  };
}
function componentState2(input) {
  let state;
  try {
    state = createPactBladeRuntimeState({
      ownerActorId: input.actor.id,
      sourceEntityId: input.invocation.sourceEntityId,
      bondActionId: input.invocation.bondActionId,
      capabilitySourceEntityIds: input.capabilitySources,
      rulesetContentHash: input.world.ruleset.contentHash,
      lifecyclePolicy: input.invocation.lifecyclePolicy,
      observedWorldRevision: input.world.revision
    });
  } catch (error) {
    return String(error).replace(/^Error: /, "");
  }
  const active = activeState(input);
  if (typeof active === "string") return active;
  state.revision = input.world.revision;
  state.activeBlade = active;
  return state;
}
function context(input) {
  const actor2 = input.world.actors[input.actorId];
  if (!actor2) return { issue: rejected2("ActorNotFound", `Unknown actor ${input.actorId}`) };
  const capability = capabilityContext(actor2);
  if (capability.issue) return { issue: capability.issue };
  const actionIssue = bondActionIssue(actor2, capability.invocation, input.catalog);
  if (actionIssue) return { issue: rejected2("InvalidCatalogAction", actionIssue) };
  const state = componentState2({
    world: input.world,
    catalog: input.catalog,
    actor: actor2,
    invocation: capability.invocation,
    capabilitySources: capability.capabilitySources
  });
  if (typeof state === "string") return { issue: rejected2("InvalidWorldState", state) };
  return {
    actor: actor2,
    invocation: capability.invocation,
    capabilitySources: capability.capabilitySources,
    state
  };
}
function touchFactsIssue(facts) {
  if (!facts || !["scenario", "board", "gm_ruling"].includes(facts.factsSource) || !Number.isInteger(facts.boardRevision) || facts.boardRevision < 0 || !Number.isFinite(facts.distanceFt) || facts.distanceFt < 0 || typeof facts.lineOfSight !== "boolean" || typeof facts.touched !== "boolean") {
    return "Pact Blade touch requires authoritative, well-formed object facts";
  }
  if (facts.touched !== true) return "Pact Blade existing-weapon bond requires an explicit touch fact";
  return null;
}
function turnAuthority(world, actor2) {
  if (world.scene.mode === "encounter") {
    const activeActorId = world.scene.initiative[world.scene.activeIndex];
    if (!world.scene.turnStarted || activeActorId !== actor2.id) {
      return { issue: "Pact Blade Bonus Action requires the actor's started encounter turn" };
    }
    return {
      turn: {
        provenance: PACT_BLADE_TURN_PROVENANCE,
        turnRevision: world.revision,
        turnId: `encounter:${world.scene.round}:${world.scene.activeIndex}:${actor2.id}`,
        activeActorId,
        bonusActionsRemaining: actor2.runtime.resources.bonus_action ?? 0
      }
    };
  }
  return {
    turn: {
      provenance: PACT_BLADE_TURN_PROVENANCE,
      turnRevision: world.revision,
      turnId: `exploration:${world.revision}:${actor2.id}`,
      activeActorId: actor2.id,
      bonusActionsRemaining: actor2.runtime.resources.bonus_action ?? 0
    }
  };
}
function boundObject(object5, weaponCardId, held) {
  return {
    ...cloneJson4(object5),
    itemCardId: weaponCardId,
    ...held ? { heldByActorId: held.actorId, heldInHand: held.hand } : {}
  };
}
function canonicalActive(active, lastDistanceBoardRevision, held) {
  const bond = active.invocation.activeBond;
  return {
    ...cloneJson4(active),
    invocation: {
      ...cloneJson4(active.invocation),
      activeBond: {
        ...cloneJson4(bond),
        weaponCardId: active.weaponCardId,
        lastDistanceBoardRevision
      }
    },
    weaponObject: boundObject(active.weaponObject, active.weaponCardId, held)
  };
}
function canonicalBondEvent(event, selection) {
  const cloned = cloneJson4(event);
  const {
    activeBlade: _runtimeActive,
    endedPreviousBond: _runtimePrevious,
    upsertWorldObjects: _runtimeObjects,
    ...base2
  } = cloned;
  const held = selection.mode === "conjure" ? { actorId: event.actorId, hand: selection.conjureHand } : void 0;
  const activeBlade = canonicalActive(event.activeBlade, null, held);
  const previous = event.endedPreviousBond;
  return {
    ...base2,
    mode: selection.mode,
    ...selection.conjureHand ? { conjureHand: selection.conjureHand } : {},
    ...selection.touchFacts ? { touchFacts: cloneJson4(selection.touchFacts) } : {},
    activeBlade,
    ...previous ? { endedPreviousBond: cloneJson4(previous) } : {},
    upsertWorldObjects: event.upsertWorldObjects.map((object5) => boundObject(object5, event.activeBlade.weaponCardId, held))
  };
}
function canonicalDistanceEvent(event) {
  const previous = event.previousBond;
  const activeBlade = event.activeBlade ? canonicalActive(event.activeBlade, event.facts.boardRevision) : null;
  return {
    ...cloneJson4(event),
    previousBond: cloneJson4(previous),
    activeBlade,
    pactState: activeBlade ? cloneJson4(activeBlade.invocation) : {
      ...cloneJson4(event.pactState),
      activeBond: null
    }
  };
}
function canonicalOwnerDeathEvent(event) {
  return {
    ...cloneJson4(event),
    previousBond: cloneJson4(event.previousBond),
    pactState: {
      ...cloneJson4(event.pactState),
      activeBond: null
    }
  };
}
function planPactBladeBondTransition(input) {
  const resolved = context(input);
  if (resolved.issue) return resolved.issue;
  if (!input.selection || typeof input.selection !== "object" || !["conjure", "touch_existing"].includes(input.selection.mode) || !nonBlank7(input.selection.weaponCardId) || !nonBlank7(input.selection.weaponObjectId)) {
    return rejected2("InvalidWorldState", "Pact Blade bond selection is malformed");
  }
  if (input.selection.mode === "conjure" && !["main_hand", "off_hand"].includes(input.selection.conjureHand ?? "")) {
    return rejected2("InvalidWorldState", "A conjured Pact Blade requires an explicit hand");
  }
  if (input.selection.mode === "touch_existing" && input.selection.conjureHand !== void 0) {
    return rejected2("InvalidWorldState", "An existing touched weapon does not accept a conjured hand");
  }
  const derived = catalogCard2(input.catalog, input.selection.weaponCardId);
  if (derived.issue) return derived.issue;
  if (input.selection.mode === "conjure" && input.selection.touchFacts !== void 0) {
    return rejected2("InvalidTouchFacts", "Conjuring a Pact Blade does not accept touch facts");
  }
  if (input.selection.mode === "touch_existing") {
    const issue = touchFactsIssue(input.selection.touchFacts);
    if (issue) return rejected2("InvalidTouchFacts", issue);
  }
  if (input.selection.mode === "conjure") {
    const replaceableObjectId = resolved.invocation.activeBond?.conjured ? resolved.invocation.activeBond.weaponObjectId : void 0;
    const occupant = Object.values(input.world.objects).find((object5) => object5.id !== replaceableObjectId && object5.heldByActorId === input.actorId && object5.heldInHand === input.selection.conjureHand);
    if (occupant) {
      return rejected2(
        "InvalidWorldState",
        `Cannot conjure Pact Blade into occupied ${input.selection.conjureHand}`
      );
    }
  }
  const turn = turnAuthority(input.world, resolved.actor);
  if (turn.issue) return rejected2("InvalidTurnState", turn.issue);
  const objectIds = [
    ...resolved.invocation.activeBond ? [resolved.invocation.activeBond.weaponObjectId] : [],
    ...input.selection.mode === "touch_existing" ? [input.selection.weaponObjectId] : []
  ];
  const authority = worldAuthority(
    input.world,
    objectIds,
    input.selection.mode === "touch_existing" ? input.selection.weaponObjectId : void 0,
    input.selection.mode === "touch_existing" ? input.actorId : void 0
  );
  if (typeof authority === "string") return rejected2("InvalidWorldState", authority);
  const result = transitionPactBladeRuntime(resolved.state, {
    schemaVersion: PACT_BLADE_RUNTIME_SCHEMA_VERSION,
    type: "BondPactBlade",
    commandId: input.commandId,
    expectedRevision: input.world.revision,
    rulesetContentHash: input.world.ruleset.contentHash,
    actorId: input.actorId,
    sourceEntityId: resolved.invocation.sourceEntityId,
    mode: input.selection.mode,
    weaponCardId: input.selection.weaponCardId,
    weaponObjectId: input.selection.weaponObjectId,
    catalog: {
      provenance: PACT_BLADE_CATALOG_PROVENANCE,
      rulesetContentHash: input.world.ruleset.contentHash,
      cards: [derived.card]
    },
    world: authority,
    turn: turn.turn
  });
  if (result.status === "rejected") return rejected2(result.code, result.message);
  const runtimeEvent = result.transition.event;
  return {
    status: "applied",
    transition: result.transition,
    event: canonicalBondEvent(runtimeEvent, input.selection)
  };
}
function planPactBladeAttackProjection(input) {
  const resolved = context(input);
  if (resolved.issue) return resolved.issue;
  const active = resolved.invocation.activeBond;
  if (!active) return rejected2("BladeUnavailable", "No active Pact Blade bond exists");
  if (!input.selection || typeof input.selection !== "object" || !nonBlank7(input.selection.weaponObjectId) || !["main_hand", "off_hand"].includes(input.selection.hand) || !["str", "dex", "cha"].includes(input.selection.abilityChoice) || !PACT_BLADE_DAMAGE_TYPES.includes(input.selection.damageType)) {
    return rejected2("IllegalAttackChoice", "Pact Blade attack selection is malformed");
  }
  const object5 = itemObject(input.world, active.weaponObjectId);
  if (object5?.carriedByActorId !== input.actorId || object5.heldByActorId !== input.actorId || object5.heldInHand !== input.selection.hand) {
    return rejected2(
      "WeaponNotHeld",
      "The active Pact Blade object and Card must be held in the declared hand"
    );
  }
  const derived = catalogCard2(input.catalog, active.weaponCardId);
  if (derived.issue) return derived.issue;
  const authority = worldAuthority(
    input.world,
    [active.weaponObjectId]
  );
  const result = transitionPactBladeRuntime(resolved.state, {
    schemaVersion: PACT_BLADE_RUNTIME_SCHEMA_VERSION,
    type: "ProjectPactBladeAttack",
    commandId: input.commandId,
    expectedRevision: input.world.revision,
    rulesetContentHash: input.world.ruleset.contentHash,
    actorId: input.actorId,
    sourceEntityId: resolved.invocation.sourceEntityId,
    weaponCardId: active.weaponCardId,
    weaponObjectId: input.selection.weaponObjectId,
    abilityChoice: input.selection.abilityChoice === "cha" ? "charisma" : "ordinary",
    ordinaryAbility: input.selection.abilityChoice === "dex" ? "dex" : "str",
    damageType: input.selection.damageType,
    catalog: {
      provenance: PACT_BLADE_CATALOG_PROVENANCE,
      rulesetContentHash: input.world.ruleset.contentHash,
      cards: [derived.card]
    },
    world: authority
  });
  if (result.status === "rejected") return rejected2(result.code, result.message);
  return {
    status: "applied",
    transition: result.transition,
    event: cloneJson4(result.transition.event)
  };
}
function distanceFactsIssue2(facts) {
  if (!facts || typeof facts !== "object" || !["scenario", "board", "gm_ruling"].includes(facts.factsSource) || !Number.isInteger(facts.boardRevision) || facts.boardRevision < 0 || !Number.isFinite(facts.distanceFt) || facts.distanceFt < 0 || !Number.isFinite(facts.elapsedSeconds) || facts.elapsedSeconds < 0) {
    return "Pact Blade distance requires authoritative, non-negative explicit facts";
  }
  return null;
}
function planPactBladeDistanceTransition(input) {
  const resolved = context(input);
  if (resolved.issue) return resolved.issue;
  const active = resolved.invocation.activeBond;
  if (!active) return rejected2("BladeUnavailable", "No active Pact Blade bond exists");
  const issue = distanceFactsIssue2(input.facts);
  if (issue) return rejected2("InvalidDistanceFacts", issue);
  const authority = worldAuthority(
    input.world,
    [active.weaponObjectId]
  );
  const result = transitionPactBladeRuntime(resolved.state, {
    schemaVersion: PACT_BLADE_RUNTIME_SCHEMA_VERSION,
    type: "AdvancePactBladeDistance",
    commandId: input.commandId,
    expectedRevision: input.world.revision,
    rulesetContentHash: input.world.ruleset.contentHash,
    actorId: input.actorId,
    sourceEntityId: resolved.invocation.sourceEntityId,
    world: authority,
    facts: {
      ...cloneJson4(input.facts),
      actorId: input.actorId,
      weaponObjectId: input.weaponObjectId
    }
  });
  if (result.status === "rejected") return rejected2(result.code, result.message);
  return {
    status: "applied",
    transition: result.transition,
    event: canonicalDistanceEvent(result.transition.event)
  };
}
function ownerDeathFactsIssue2(facts) {
  if (!facts || typeof facts !== "object" || facts.type !== "ActorDeathAdjudicated" || facts.provenance !== "canonical_actor_lifecycle" || !nonBlank7(facts.factId) || !nonBlank7(facts.adjudicatedBy) || !Number.isInteger(facts.observedAtWorldRevision)) {
    return "Pact Blade requires an authoritative explicit owner-death lifecycle fact";
  }
  return null;
}
function planPactBladeOwnerDeathTransition(input) {
  const resolved = context(input);
  if (resolved.issue) return resolved.issue;
  const active = resolved.invocation.activeBond;
  if (!active) return rejected2("BladeUnavailable", "No active Pact Blade bond exists");
  const issue = ownerDeathFactsIssue2(input.deathFact);
  if (issue) return rejected2("InvalidDeathFacts", issue);
  const authority = worldAuthority(
    input.world,
    [active.weaponObjectId]
  );
  const result = transitionPactBladeRuntime(resolved.state, {
    schemaVersion: PACT_BLADE_RUNTIME_SCHEMA_VERSION,
    type: "EndPactBladeOnOwnerDeath",
    commandId: input.commandId,
    expectedRevision: input.world.revision,
    rulesetContentHash: input.world.ruleset.contentHash,
    actorId: input.actorId,
    sourceEntityId: resolved.invocation.sourceEntityId,
    world: authority,
    deathFact: cloneJson4(input.deathFact)
  });
  if (result.status === "rejected") return rejected2(result.code, result.message);
  return {
    status: "applied",
    transition: result.transition,
    event: canonicalOwnerDeathEvent(
      result.transition.event
    )
  };
}
function spellComponents(action) {
  if (action.kind !== "spell") return { issue: `${action.id} is not an immutable spell action` };
  const components = action.spell.components;
  if (!components || typeof components.verbal !== "boolean" || typeof components.somatic !== "boolean" || typeof components.material !== "boolean") {
    return { issue: `${action.id} lacks canonical V/S/M component metadata` };
  }
  return { components };
}
function planPactBladeMaterialFocus(input) {
  const resolved = context(input);
  if (resolved.issue) return resolved.issue;
  const active = resolved.invocation.activeBond;
  if (!active) return rejected2("BladeUnavailable", "No active Pact Blade bond exists");
  if (input.weaponObjectId !== active.weaponObjectId) {
    return rejected2("WeaponMismatch", "Spell focus is not the active Pact Blade object");
  }
  const object5 = itemObject(input.world, active.weaponObjectId);
  if (object5?.carriedByActorId !== input.actorId || object5.heldByActorId !== input.actorId || object5.heldInHand !== input.hand) {
    return rejected2("WeaponNotHeld", "The active Pact Blade must be held in the declared hand to use it as a focus");
  }
  const action = input.catalog.getAction(input.actionId);
  if (!action) return rejected2("InvalidCatalogAction", `Unknown immutable action ${input.actionId}`);
  const derived = spellComponents(action);
  if (derived.issue) return rejected2("InvalidCatalogAction", derived.issue);
  if (derived.components.material !== true) {
    return rejected2(
      "MaterialComponentRequired",
      "A Pact Blade substitutes only an eligible Material spell component"
    );
  }
  return {
    status: "applied",
    event: {
      type: "PactBladeMaterialFocusProjected",
      commandId: input.commandId,
      revision: input.world.revision + 1,
      worldRevision: input.world.revision,
      rulesetContentHash: input.world.ruleset.contentHash,
      actorId: input.actorId,
      sourceEntityId: resolved.invocation.sourceEntityId,
      actionId: action.id,
      weaponObjectId: active.weaponObjectId,
      weaponCardId: active.weaponCardId,
      focusHand: input.hand,
      components: { ...derived.components, material: true },
      replacesMaterialComponent: true,
      preservesCostlyAndConsumedMaterials: true,
      replacesVerbalComponent: false,
      replacesSomaticComponent: false
    }
  };
}
function same3(left, right) {
  return canonicalStringify(left) === canonicalStringify(right);
}
function committedInvocation(actor2, invocation) {
  return {
    ...actor2,
    warlockPacts: {
      ...actor2.warlockPacts,
      blade: cloneJson4(invocation)
    }
  };
}
function authorizedEventActor(world, event) {
  const actor2 = world.actors[event.actorId];
  if (!actor2) throw new Error(`Cannot evolve Pact Blade for unknown actor ${event.actorId}`);
  if (event.revision !== world.revision + 1 || event.worldRevision !== world.revision || event.rulesetContentHash !== world.ruleset.contentHash) {
    throw new Error("Pact Blade event revision or ruleset provenance diverged");
  }
  const invocation = actorInvocation(actor2);
  if (typeof invocation === "string" || invocation.sourceEntityId !== event.sourceEntityId || !actor2.capabilities.featureSources?.[PACT_BLADE_STATE_CAPABILITY]?.includes(event.sourceEntityId)) {
    throw new Error("Pact Blade event does not belong to an actor-owned invocation");
  }
  return { actor: actor2, invocation };
}
function applyAuthorizedPactBladeBonded(world, event) {
  const { actor: actor2, invocation } = authorizedEventActor(world, event);
  const previous = invocation.activeBond;
  if (previous === null !== (event.endedPreviousBond === void 0) || previous !== null && !same3(previous, event.endedPreviousBond)) {
    throw new Error("Pact Blade bond event does not replace exactly the active bond");
  }
  if (event.activeBlade.invocation.activeBond === null || event.activeBlade.invocation.sourceEntityId !== invocation.sourceEntityId || event.activeBlade.invocation.ownerActorId !== actor2.id || event.activeBlade.weaponCardId !== event.activeBlade.invocation.activeBond.weaponCardId || event.activeBlade.weaponObject.id !== event.activeBlade.invocation.activeBond.weaponObjectId || event.activeBlade.weaponObject.itemCardId !== event.activeBlade.weaponCardId || event.setPactBondObjectId !== event.activeBlade.weaponObject.id) {
    throw new Error("Pact Blade bond event has an inconsistent active Card/Object bridge");
  }
  const expectedRemoved = previous?.conjured ? [previous.weaponObjectId] : [];
  const expectedCleared = previous ? [previous.weaponObjectId] : [];
  if (!same3([...event.removedWorldObjectIds].sort(), expectedRemoved.sort()) || !same3([...event.clearPactBondObjectIds].sort(), expectedCleared.sort()) || event.actionCost.kind !== "bonus_action" || event.actionCost.amount !== 1 || (actor2.runtime.resources.bonus_action ?? 0) < 1) {
    throw new Error("Pact Blade bond event has invalid replacement or Bonus Action effects");
  }
  const activeObject = event.activeBlade.weaponObject;
  if (event.mode === "conjure") {
    const survivingOccupant = Object.values(world.objects).find((object5) => !expectedRemoved.includes(object5.id) && object5.heldByActorId === actor2.id && object5.heldInHand === event.conjureHand);
    if (world.objects[activeObject.id] || !event.conjureHand || survivingOccupant !== void 0 || event.upsertWorldObjects.length !== 1 || !same3(event.upsertWorldObjects[0], activeObject) || activeObject.heldByActorId !== actor2.id || activeObject.heldInHand !== event.conjureHand || activeObject.carriedByActorId !== actor2.id || !event.activeBlade.invocation.activeBond.conjured) {
      throw new Error("Pact Blade conjure event has invalid held-item authority");
    }
  } else {
    if (event.conjureHand !== void 0 || event.upsertWorldObjects.length !== 0 || event.touchFacts?.touched !== true || event.activeBlade.invocation.activeBond.conjured || !same3(world.objects[activeObject.id], activeObject)) {
      throw new Error("Pact Blade touch event does not preserve the existing item instance");
    }
  }
  const objects = { ...world.objects };
  for (const objectId of expectedRemoved) delete objects[objectId];
  if (event.mode === "conjure") objects[activeObject.id] = cloneJson4(activeObject);
  const nextActor = committedInvocation(actor2, event.activeBlade.invocation);
  nextActor.runtime = {
    ...actor2.runtime,
    resources: {
      ...actor2.runtime.resources,
      bonus_action: actor2.runtime.resources.bonus_action - 1
    }
  };
  return {
    ...world,
    actors: { ...world.actors, [actor2.id]: nextActor },
    objects
  };
}
function applyAuthorizedPactBladeDistanceAdvanced(world, event) {
  const { actor: actor2, invocation } = authorizedEventActor(world, event);
  const previous = invocation.activeBond;
  if (!previous || !same3(previous, event.previousBond) || event.facts.actorId !== actor2.id || event.facts.weaponObjectId !== previous.weaponObjectId) {
    throw new Error("Pact Blade distance event does not observe the active bond");
  }
  const expected = advancePactBladeDistance(
    previous,
    invocation.lifecyclePolicy,
    event.facts.distanceFt,
    event.facts.elapsedSeconds,
    event.facts.boardRevision
  );
  const expectedRemoved = expected === null && previous.conjured ? [previous.weaponObjectId] : [];
  if (event.bondEnded !== (expected === null) || !same3(event.pactState.activeBond, expected) || !same3([...event.removedWorldObjectIds].sort(), expectedRemoved.sort()) || expected === null !== (event.activeBlade === null)) {
    throw new Error("Pact Blade distance event diverges from its persisted threshold facts");
  }
  if (event.activeBlade && (!same3(event.activeBlade.invocation.activeBond, expected) || event.activeBlade.weaponObject.id !== previous.weaponObjectId || event.activeBlade.weaponObject.itemCardId !== previous.weaponCardId)) {
    throw new Error("Pact Blade distance event has an inconsistent active Card/Object bridge");
  }
  const objects = { ...world.objects };
  for (const objectId of expectedRemoved) delete objects[objectId];
  return {
    ...world,
    actors: {
      ...world.actors,
      [actor2.id]: committedInvocation(actor2, event.pactState)
    },
    objects
  };
}
function applyAuthorizedPactBladeEndedOnOwnerDeath(world, event) {
  const { actor: actor2, invocation } = authorizedEventActor(world, event);
  const previous = invocation.activeBond;
  const factIssue = ownerDeathFactsIssue2(event.deathFact);
  if (!previous || factIssue || event.deathFact.actorId !== actor2.id || event.deathFact.observedAtWorldRevision !== world.revision || event.deathFact.rulesetContentHash !== world.ruleset.contentHash || !same3(previous, event.previousBond) || event.pactState.activeBond !== null) {
    throw new Error("Pact Blade owner-death event does not dismiss the exact active bond");
  }
  const expectedRemoved = previous.conjured ? [previous.weaponObjectId] : [];
  if (!same3([...event.removedWorldObjectIds].sort(), expectedRemoved.sort())) {
    throw new Error("Pact Blade owner-death event removes foreign world objects");
  }
  const objects = { ...world.objects };
  for (const objectId of expectedRemoved) delete objects[objectId];
  return {
    ...world,
    actors: {
      ...world.actors,
      [actor2.id]: committedInvocation(actor2, event.pactState)
    },
    objects
  };
}

// src/rules-core/reducer.ts
function ownerWithFamiliarProjection(owner, familiar) {
  const chain2 = owner.warlockPacts?.chain;
  if (!chain2) {
    if (familiar?.extension === "pact_chain") {
      throw new Error(`Actor ${owner.id} has no Pact Chain state for its familiar`);
    }
    return owner;
  }
  if (familiar && familiar.extension !== "pact_chain") {
    if (chain2.activeFamiliar?.actorId !== familiar.actorId) return owner;
    return {
      ...owner,
      warlockPacts: {
        ...owner.warlockPacts,
        chain: { ...chain2, activeFamiliar: null }
      }
    };
  }
  return {
    ...owner,
    warlockPacts: {
      ...owner.warlockPacts,
      chain: {
        ...chain2,
        activeFamiliar: familiar ? pactChainProjection(familiar) : null
      }
    }
  };
}
function sceneWithoutActor(world, actorId) {
  if (world.scene.mode !== "encounter") return world.scene;
  const removedIndex = world.scene.initiative.indexOf(actorId);
  if (removedIndex < 0) return world.scene;
  const initiative = world.scene.initiative.filter((id) => id !== actorId);
  if (initiative.length < 2) throw new Error("A familiar lifecycle event cannot invalidate Initiative");
  let activeIndex = world.scene.activeIndex;
  if (removedIndex < activeIndex) activeIndex -= 1;
  else if (removedIndex === activeIndex) activeIndex %= initiative.length;
  return { ...world.scene, initiative, activeIndex };
}
function sceneWithPresentFamiliar(world, actor2) {
  if (world.scene.mode !== "encounter" || actor2.familiarState?.presence !== "present" || world.scene.initiative.includes(actor2.id)) return world.scene;
  if (actor2.familiarState.initiative.total === null) {
    throw new Error(`Present familiar ${actor2.id} requires its own Initiative before joining combat`);
  }
  const initiative = [...world.scene.initiative];
  const ownerIndex = initiative.indexOf(actor2.familiarState.ownerActorId);
  initiative.splice(ownerIndex < 0 ? initiative.length : ownerIndex + 1, 0, actor2.id);
  return { ...world.scene, initiative };
}
function sameJson(left, right) {
  return JSON.stringify(left) === JSON.stringify(right);
}
function evolve(world, payload) {
  switch (payload.type) {
    case "ActorDeathAdjudicated": {
      const actor2 = world.actors[payload.actorId];
      if (!actor2 || actor2.lifecycle?.status !== "alive") {
        throw new Error(`Cannot adjudicate death for non-living actor ${payload.actorId}`);
      }
      if (payload.provenance !== "canonical_actor_lifecycle" || !payload.factId || payload.factId.trim() !== payload.factId || !payload.adjudicatedBy || payload.adjudicatedBy.trim() !== payload.adjudicatedBy || payload.observedAtWorldRevision !== world.revision || payload.rulesetContentHash !== world.ruleset.contentHash) {
        throw new Error("Actor death event has invalid lifecycle authority");
      }
      return {
        ...world,
        actors: {
          ...world.actors,
          [actor2.id]: {
            ...actor2,
            lifecycle: {
              status: "dead",
              adjudication: JSON.parse(JSON.stringify(payload))
            }
          }
        }
      };
    }
    case "ActorRuntimePatched": {
      const actor2 = world.actors[payload.actorId];
      if (!actor2) throw new Error(`Cannot evolve unknown actor ${payload.actorId}`);
      const {
        firedThisTurn,
        firedThisRest,
        ...regularPatch
      } = payload.patch;
      const runtime = { ...actor2.runtime, ...regularPatch };
      if (firedThisTurn === null) delete runtime.firedThisTurn;
      else if (firedThisTurn !== void 0) runtime.firedThisTurn = firedThisTurn;
      if (firedThisRest === null) delete runtime.firedThisRest;
      else if (firedThisRest !== void 0) runtime.firedThisRest = firedThisRest;
      return {
        ...world,
        actors: {
          ...world.actors,
          [payload.actorId]: { ...actor2, runtime }
        }
      };
    }
    case "EquipmentChanged": {
      const actor2 = world.actors[payload.actorId];
      if (!actor2) throw new Error(`Cannot evolve equipment for unknown actor ${payload.actorId}`);
      if (payload.operation !== "don_armor" || payload.equipment.body !== payload.cardId) {
        throw new Error(`Invalid equipment event for ${payload.actorId}`);
      }
      const changedNonBodySlot = [.../* @__PURE__ */ new Set([
        ...Object.keys(actor2.runtime.equipment),
        ...Object.keys(payload.equipment)
      ])].find((slot) => slot !== "body" && (actor2.runtime.equipment[slot] ?? null) !== (payload.equipment[slot] ?? null));
      if (changedNonBodySlot) {
        throw new Error(`Armor event for ${payload.actorId} changed unrelated slot ${changedNonBodySlot}`);
      }
      const ended = new Set(payload.endedEffectIds);
      if (ended.size !== payload.endedEffectIds.length) {
        throw new Error(`Equipment event for ${payload.actorId} contains duplicate ended effect IDs`);
      }
      const existing = new Set(actor2.runtime.activeEffects.map((effect) => effect.id));
      const unknownEnded = payload.endedEffectIds.find((effectId) => !existing.has(effectId));
      if (unknownEnded) {
        throw new Error(`Equipment event for ${payload.actorId} ends unknown effect ${unknownEnded}`);
      }
      const expectedEndedEffectIds = actor2.runtime.activeEffects.flatMap((effect) => {
        const mechanics = effect.mechanics;
        return Array.isArray(mechanics.end_triggers) && mechanics.end_triggers.map(String).includes("wearer_dons_armor") ? [effect.id] : [];
      });
      if (JSON.stringify(payload.endedEffectIds) !== JSON.stringify(expectedEndedEffectIds)) {
        throw new Error(`Armor event for ${payload.actorId} has an invalid ended-effect set`);
      }
      return {
        ...world,
        actors: {
          ...world.actors,
          [payload.actorId]: {
            ...actor2,
            runtime: {
              ...actor2.runtime,
              equipment: { ...payload.equipment },
              activeEffects: actor2.runtime.activeEffects.filter((effect) => !ended.has(effect.id))
            }
          }
        }
      };
    }
    case "SceneSet":
      return { ...world, scene: payload.scene };
    case "ActionDeclared":
    case "EngineEventRecorded":
    case "DecisionRecorded":
      return world;
    case "AttackActionStarted": {
      const value = payload.attackAction;
      const actor2 = world.actors[value.actorId];
      if (!actor2 || world.attackActions[value.id] || value.status !== "open" || value.sequence.actorId !== value.actorId || value.sequence.id !== value.id || value.sequence.entries.length !== 0 || value.sequence.totalAttacks !== actor2.attackProfile?.attacksPerAction || value.declaredActionId === void 0 !== (value.declaredActionSourceEntityIds === void 0) || value.declaredActionId !== void 0 && (typeof value.declaredActionId !== "string" || !value.declaredActionId.length || !Array.isArray(value.declaredActionSourceEntityIds) || !value.declaredActionSourceEntityIds.length || value.declaredActionSourceEntityIds.some((id) => typeof id !== "string" || !id.length) || new Set(value.declaredActionSourceEntityIds).size !== value.declaredActionSourceEntityIds.length) || !attackSequenceInvariantHolds(value.sequence)) {
        throw new Error(`Invalid Attack action ${value.id}`);
      }
      if (Object.values(world.attackActions).some((entry) => entry.actorId === value.actorId && entry.status === "open")) {
        throw new Error(`Actor ${value.actorId} already has an open Attack action`);
      }
      return {
        ...world,
        attackActions: {
          ...world.attackActions,
          [value.id]: JSON.parse(JSON.stringify(value))
        }
      };
    }
    case "AttackEntryCommitted": {
      const attackAction = world.attackActions[payload.attackActionId];
      if (!attackAction || attackAction.status !== "open" || attackAction.blockedByResolutionId) {
        throw new Error(`Cannot append to inactive Attack action ${payload.attackActionId}`);
      }
      const entry = payload.entry;
      const expectedSystemActionId = entry.kind === "weapon_attack" ? SYSTEM_ACTION_IDS.weaponAttack : entry.kind === "unarmed_strike" ? entry.option === "damage" ? SYSTEM_ACTION_IDS.unarmedDamage : entry.option === "grapple" ? SYSTEM_ACTION_IDS.unarmedGrapple : SYSTEM_ACTION_IDS.unarmedShove : null;
      if (expectedSystemActionId) {
        const system = getSystemActionDefinition(expectedSystemActionId);
        if (entry.actionId !== expectedSystemActionId || system.sourceEntityIds.some((sourceId) => !entry.sourceEntityIds.includes(sourceId))) {
          throw new Error(`Attack entry ${entry.ordinal} shadows a ruleset-owned system action`);
        }
      }
      let sequence;
      if (entry.kind === "weapon_attack") {
        if (!entry.weaponCardId) throw new Error("Canonical weapon entry requires a Card ID");
        sequence = performWeaponSequenceAttack({
          sequence: attackAction.sequence,
          actionId: entry.actionId,
          weaponCardId: entry.weaponCardId,
          sourceEntityIds: [...entry.sourceEntityIds]
        });
      } else if (entry.kind === "unarmed_strike") {
        sequence = performUnarmedStrike({
          sequence: attackAction.sequence,
          actionId: entry.actionId,
          option: entry.option,
          sourceEntityIds: [...entry.sourceEntityIds]
        });
      } else {
        sequence = replaceSequenceAttack({
          sequence: attackAction.sequence,
          actionId: entry.actionId,
          replacementKey: entry.replacementKey,
          sourceEntityIds: [...entry.sourceEntityIds]
        });
      }
      if (!sameJson(sequence.entries.at(-1), entry)) {
        throw new Error(`Attack entry ${entry.ordinal} is not the canonical next entry`);
      }
      return {
        ...world,
        attackActions: {
          ...world.attackActions,
          [attackAction.id]: { ...attackAction, sequence }
        }
      };
    }
    case "AttackActionBlocked": {
      const attackAction = world.attackActions[payload.attackActionId];
      if (!attackAction || attackAction.status !== "open" || attackAction.blockedByResolutionId) {
        throw new Error(`Cannot block inactive Attack action ${payload.attackActionId}`);
      }
      return {
        ...world,
        attackActions: {
          ...world.attackActions,
          [attackAction.id]: { ...attackAction, blockedByResolutionId: payload.resolutionId }
        }
      };
    }
    case "AttackActionUnblocked": {
      const attackAction = world.attackActions[payload.attackActionId];
      if (!attackAction || attackAction.status !== "open" || attackAction.blockedByResolutionId !== payload.resolutionId) {
        throw new Error(`Cannot unblock Attack action ${payload.attackActionId}`);
      }
      const next = { ...attackAction };
      delete next.blockedByResolutionId;
      return {
        ...world,
        attackActions: { ...world.attackActions, [attackAction.id]: next }
      };
    }
    case "AttackActionClosed": {
      const attackAction = world.attackActions[payload.attackActionId];
      if (!attackAction || attackAction.status !== "open" || attackAction.blockedByResolutionId) {
        throw new Error(`Cannot close inactive Attack action ${payload.attackActionId}`);
      }
      if (payload.reason === "completed" && attackAction.sequence.attacksRemaining !== 0) {
        throw new Error(`Cannot complete Attack action ${payload.attackActionId} with attacks remaining`);
      }
      return {
        ...world,
        attackActions: {
          ...world.attackActions,
          [attackAction.id]: { ...attackAction, status: payload.reason }
        }
      };
    }
    case "GrappleApplied": {
      const grapple = payload.grapple;
      const grappler = world.actors[grapple.grapplerActorId];
      const target2 = world.actors[grapple.targetActorId];
      const source2 = getSystemActionDefinition(SYSTEM_ACTION_IDS.unarmedGrapple);
      if (!target2 || !grappler || world.grapples[grapple.id] || grapple.grapplerActorId === grapple.targetActorId) {
        throw new Error(`Invalid grapple ${grapple.id}`);
      }
      if (!grappler.attackProfile?.graspingParts.includes(grapple.sourcePart) || Object.values(world.grapples).some((active) => active.grapplerActorId === grappler.id && active.sourcePart === grapple.sourcePart) || (grapple.sourcePart === "main_hand" || grapple.sourcePart === "off_hand") && !!grappler.runtime.equipment[grapple.sourcePart] || (target2.attackProfile?.size ?? Number.POSITIVE_INFINITY) > (grappler.attackProfile?.size ?? Number.NEGATIVE_INFINITY) + 1 || !Number.isInteger(grapple.escapeDc) || grapple.escapeDc < 1 || !Number.isFinite(grapple.reachFt) || grapple.reachFt <= 0 || grapple.reachFt !== grappler.attackProfile?.reachFt || source2.sourceEntityIds.some((sourceId) => !grapple.sourceEntityIds.includes(sourceId))) {
        throw new Error(`Grapple ${grapple.id} violates source size, reach, part, DC, or provenance`);
      }
      const effectId = `grapple:${grapple.id}`;
      if (target2.runtime.activeEffects.some((effect) => effect.id === effectId)) {
        throw new Error(`Duplicate grapple effect ${effectId}`);
      }
      return {
        ...world,
        actors: {
          ...world.actors,
          [target2.id]: {
            ...target2,
            runtime: {
              ...target2.runtime,
              activeEffects: [...target2.runtime.activeEffects, {
                id: effectId,
                name: "Grappled",
                mechanics: { kind: "condition", value: "grappled", grappleId: grapple.id },
                expiry: "manual",
                source: grapple.sourceEntityIds[0],
                ownerId: target2.id,
                sourceId: grapple.grapplerActorId
              }]
            }
          }
        },
        grapples: { ...world.grapples, [grapple.id]: { ...grapple } }
      };
    }
    case "GrappleEnded": {
      const grapple = world.grapples[payload.grappleId];
      if (!grapple) throw new Error(`Cannot end inactive grapple ${payload.grappleId}`);
      const target2 = world.actors[grapple.targetActorId];
      if (!target2) throw new Error(`Cannot end grapple for missing target ${grapple.targetActorId}`);
      const grapples = { ...world.grapples };
      delete grapples[grapple.id];
      return {
        ...world,
        actors: {
          ...world.actors,
          [target2.id]: {
            ...target2,
            runtime: {
              ...target2.runtime,
              activeEffects: target2.runtime.activeEffects.filter((effect) => effect.id !== `grapple:${grapple.id}`)
            }
          }
        },
        grapples
      };
    }
    case "ShoveApplied": {
      const target2 = world.actors[payload.targetActorId];
      if (!target2 || !world.actors[payload.sourceActorId]) {
        throw new Error(`Cannot apply shove to ${payload.targetActorId}`);
      }
      if (payload.outcome !== "prone" && payload.outcome !== "push_5ft" || !payload.effectId.trim() || target2.runtime.activeEffects.some((effect) => effect.id === payload.effectId)) {
        throw new Error(`Invalid shove outcome for ${payload.targetActorId}`);
      }
      if (payload.outcome === "push_5ft") return world;
      return {
        ...world,
        actors: {
          ...world.actors,
          [target2.id]: {
            ...target2,
            runtime: {
              ...target2.runtime,
              activeEffects: [...target2.runtime.activeEffects, {
                id: payload.effectId,
                name: "Prone",
                mechanics: { kind: "condition", value: "prone" },
                expiry: "manual",
                source: "system:dnd5e-2024:unarmed-strike:shove",
                ownerId: target2.id,
                sourceId: payload.sourceActorId
              }]
            }
          }
        }
      };
    }
    case "ResolutionOpened":
      if (world.pendingResolution) {
        throw new Error(`Cannot replace active resolution ${world.pendingResolution.id}`);
      }
      return { ...world, pendingResolution: payload.resolution };
    case "ResolutionClosed":
      if (world.pendingResolution?.id !== payload.resolutionId) {
        throw new Error(`Cannot close inactive resolution ${payload.resolutionId}`);
      }
      return { ...world, pendingResolution: null };
    case "ConcentrationSet":
      return {
        ...world,
        concentrations: {
          ...world.concentrations,
          [payload.concentration.sourceActorId]: payload.concentration
        }
      };
    case "ConcentrationCleared": {
      const active = world.concentrations[payload.sourceActorId];
      if (!active || active.id !== payload.concentrationId) {
        throw new Error(`Cannot clear inactive concentration ${payload.concentrationId}`);
      }
      const concentrations = { ...world.concentrations };
      delete concentrations[payload.sourceActorId];
      return { ...world, concentrations };
    }
    case "WorldObjectMutationRecorded": {
      const objects = evolveWorldObjectEvent(world.objects, payload.event);
      const issue = worldObjectLedgerIssue(objects, new Set(Object.keys(world.actors)));
      if (issue) throw new Error(`Invalid canonical world-object ledger: ${issue}`);
      return { ...world, objects };
    }
    case "FamiliarActorUpserted": {
      const owner = world.actors[payload.ownerActorId];
      const familiar = payload.actor.familiarState;
      if (!owner || !familiar || familiar.ownerActorId !== owner.id || familiar.actorId !== payload.actor.id || payload.casting.actionId !== payload.actor.familiarMetadata?.summoningActionId || !Number.isInteger(payload.casting.consumedIncenseGp) || payload.casting.consumedIncenseGp <= 0 || payload.casting.created !== !world.actors[payload.actor.id] || payload.casting.method === "pact_chain_magic_action" !== (familiar.extension === "pact_chain")) {
        throw new Error(`Invalid familiar upsert for ${payload.ownerActorId}`);
      }
      const other = familiarActorsOwnedBy(world, owner.id).find((candidate) => candidate.id !== payload.actor.id);
      if (other) throw new Error(`Actor ${owner.id} already owns familiar ${other.id}`);
      const ownerAfter = ownerWithFamiliarProjection(owner, familiar);
      const issue = familiarActorStateIssue({ actor: payload.actor, owner: ownerAfter });
      if (issue) throw new Error(issue);
      const actors = {
        ...world.actors,
        [owner.id]: ownerAfter,
        [payload.actor.id]: {
          ...JSON.parse(JSON.stringify(payload.actor)),
          lifecycle: payload.actor.lifecycle ?? { status: "alive" }
        }
      };
      const interim = { ...world, actors };
      return { ...interim, scene: sceneWithPresentFamiliar(interim, actors[payload.actor.id]) };
    }
    case "FamiliarStateChanged": {
      const actor2 = world.actors[payload.familiarActorId];
      const owner = world.actors[payload.ownerActorId];
      if (!actor2?.familiarState || !actor2.familiarMetadata || !owner || actor2.familiarState.ownerActorId !== owner.id || payload.familiar.actorId !== actor2.id || payload.familiar.ownerActorId !== owner.id) {
        throw new Error(`Invalid familiar state transition for ${payload.familiarActorId}`);
      }
      const expectedDropped = payload.familiar.presence === "present" ? [] : [
        ...actor2.familiarState.carriedItemIds,
        ...actor2.familiarState.wornItemIds
      ].sort((left, right) => left.localeCompare(right));
      if (!sameJson(payload.droppedItemIds ?? [], expectedDropped)) {
        throw new Error(`Familiar ${actor2.id} has an invalid lifecycle dropped-item projection`);
      }
      const nextActor = {
        ...actor2,
        familiarState: JSON.parse(JSON.stringify(payload.familiar)),
        runtime: {
          ...actor2.runtime,
          resources: {
            ...actor2.runtime.resources,
            reaction: payload.familiar.reactionAvailable ? 1 : 0
          }
        }
      };
      const ownerAfter = ownerWithFamiliarProjection(owner, payload.familiar);
      const issue = familiarActorStateIssue({ actor: nextActor, owner: ownerAfter });
      if (issue) throw new Error(issue);
      const actors = { ...world.actors, [owner.id]: ownerAfter, [actor2.id]: nextActor };
      let interim = { ...world, actors };
      if (payload.familiar.presence === "present") {
        interim = { ...interim, scene: sceneWithPresentFamiliar(interim, nextActor) };
      } else {
        interim = { ...interim, scene: sceneWithoutActor(interim, actor2.id) };
      }
      return interim;
    }
    case "FamiliarActorRemoved": {
      const actor2 = world.actors[payload.familiarActorId];
      const owner = world.actors[payload.ownerActorId];
      if (!actor2?.familiarState || !owner || actor2.familiarState.ownerActorId !== owner.id || payload.reason !== "forever_dismissal" || Object.values(world.attackActions).some((entry) => entry.actorId === actor2.id && entry.status === "open") || Object.values(world.grapples).some((entry) => entry.grapplerActorId === actor2.id || entry.targetActorId === actor2.id)) {
        throw new Error(`Invalid permanent familiar dismissal for ${payload.familiarActorId}`);
      }
      const expectedDropped = [
        ...actor2.familiarState.carriedItemIds,
        ...actor2.familiarState.wornItemIds
      ].sort((left, right) => left.localeCompare(right));
      if (!sameJson(payload.droppedItemIds, expectedDropped)) {
        throw new Error(`Familiar ${actor2.id} has an invalid dropped-item projection`);
      }
      const actors = { ...world.actors };
      delete actors[actor2.id];
      actors[owner.id] = actor2.familiarState.extension === "pact_chain" || owner.warlockPacts?.chain?.activeFamiliar?.actorId === actor2.id ? ownerWithFamiliarProjection(owner, null) : owner;
      const without = { ...world, actors };
      return { ...without, scene: sceneWithoutActor(without, actor2.id) };
    }
    case "ProtectionEffectActivated": {
      const { effect, facts } = payload;
      const protector = world.actors[effect.protectorActorId];
      if (!protector || !world.actors[effect.protectedTargetActorId] || !world.actors[effect.triggeringAttackerActorId] || facts.protectorActorId !== protector.id || facts.targetActorId !== effect.protectedTargetActorId || facts.attackerActorId !== effect.triggeringAttackerActorId || facts.attackId !== effect.triggeringAttackId || facts.protectorHoldingShield !== actorHoldsCanonicalShield(protector) || facts.protectorReactionAvailable !== true || (protector.runtime.resources.reaction ?? 0) < 1 || actorProtectionEffects(protector).some((active) => active.id === effect.id)) {
        throw new Error(`Invalid Protection activation ${effect.id}`);
      }
      const resolved = resolveProtection2024Reaction({
        decision: "use",
        effectId: effect.id,
        source: effect.source,
        facts
      });
      if (resolved.status !== "activated" || !sameJson(resolved.effect, effect)) {
        throw new Error(`Protection activation ${effect.id} does not match its pure contract`);
      }
      const entry = protectionEffectEntry(effect);
      const entryIssue = protectionEffectEntryIssue(entry, protector, world);
      if (entryIssue) throw new Error(entryIssue);
      return {
        ...world,
        actors: {
          ...world.actors,
          [protector.id]: {
            ...protector,
            runtime: {
              ...protector.runtime,
              resources: {
                ...protector.runtime.resources,
                reaction: protector.runtime.resources.reaction - 1
              },
              activeEffects: [...protector.runtime.activeEffects, entry]
            }
          }
        }
      };
    }
    case "ProtectionEffectEnded": {
      const protector = world.actors[payload.protectorActorId];
      const entry = protector?.runtime.activeEffects.find((candidate) => candidate.id === payload.effectId);
      const effect = entry && protector ? actorProtectionEffects({
        ...protector,
        runtime: { ...protector.runtime, activeEffects: [entry] }
      })[0] : void 0;
      if (!protector || !entry || !effect || effect.protectedTargetActorId !== payload.protectedTargetActorId) {
        throw new Error(`Cannot end inactive Protection effect ${payload.effectId}`);
      }
      const advanced = advanceProtection2024Effect(effect, payload.lifecycleEvent);
      if (advanced.status !== "ended" || advanced.reason !== payload.reason) {
        throw new Error(`Protection effect ${payload.effectId} has an invalid terminal observation`);
      }
      return {
        ...world,
        actors: {
          ...world.actors,
          [protector.id]: {
            ...protector,
            runtime: {
              ...protector.runtime,
              activeEffects: protector.runtime.activeEffects.filter((candidate) => candidate.id !== payload.effectId)
            }
          }
        }
      };
    }
    case "PactTomeRestCompleted":
      return evolvePactTomeRestCompleted(world, payload);
    case "PactTomeOwnerDied":
      return evolvePactTomeOwnerDied(world, payload);
    case "PactBladeBonded":
      return applyAuthorizedPactBladeBonded(world, payload);
    case "PactBladeDistanceAdvanced":
      return applyAuthorizedPactBladeDistanceAdvanced(world, payload);
    case "PactBladeEndedOnOwnerDeath":
      return applyAuthorizedPactBladeEndedOnOwnerDeath(world, payload);
    case "PactBladeAttackProjected": {
      const actor2 = world.actors[payload.actorId];
      const invocation = actor2?.warlockPacts?.blade;
      const bond = invocation?.activeBond;
      const object5 = world.objects[payload.weaponObjectId];
      if (!actor2 || !invocation || !bond || payload.revision !== world.revision + 1 || payload.worldRevision !== world.revision || payload.rulesetContentHash !== world.ruleset.contentHash || payload.sourceEntityId !== invocation.sourceEntityId || !actor2.capabilities.featureSources?.[PACT_BLADE_STATE_CAPABILITY]?.includes(payload.sourceEntityId) || payload.weaponObjectId !== bond.weaponObjectId || payload.weaponCardId !== bond.weaponCardId || object5?.itemCardId !== bond.weaponCardId || !["str", "dex", "cha"].includes(payload.projection.attackAbility) || payload.projection.attackAbility !== payload.projection.damageAbility || !payload.projection.damageType || payload.projection.proficient !== true || payload.projection.spellcastingFocus !== true) {
        throw new Error("Pact Blade attack projection diverges from the active Card/Object bond");
      }
      return world;
    }
    case "PactBladeMaterialFocusProjected": {
      const actor2 = world.actors[payload.actorId];
      const invocation = actor2?.warlockPacts?.blade;
      const bond = invocation?.activeBond;
      const object5 = world.objects[payload.weaponObjectId];
      if (!actor2 || !invocation || !bond || payload.revision !== world.revision + 1 || payload.worldRevision !== world.revision || payload.rulesetContentHash !== world.ruleset.contentHash || payload.sourceEntityId !== invocation.sourceEntityId || payload.weaponObjectId !== bond.weaponObjectId || payload.weaponCardId !== bond.weaponCardId || object5?.itemCardId !== bond.weaponCardId || object5.heldByActorId !== actor2.id || object5.heldInHand !== payload.focusHand || payload.components.material !== true || payload.replacesMaterialComponent !== true || payload.preservesCostlyAndConsumedMaterials !== true || payload.replacesVerbalComponent !== false || payload.replacesSomaticComponent !== false) {
        throw new Error("Pact Blade material-focus projection diverges from the held active bond");
      }
      return world;
    }
    case "CommandCommitted":
      return {
        ...world,
        revision: payload.revision,
        logicalClock: payload.logicalClock,
        processedCommandIds: [...world.processedCommandIds, payload.commandId]
      };
  }
}
function foldEvents(world, events) {
  return events.reduce((state, event) => evolve(state, event.payload), world);
}

// src/rules-core/activationCastTime.ts
var UNIT_SECONDS = {
  action: 6,
  bonus_action: 6,
  reaction: 6,
  round: 6,
  minute: 60,
  hour: 3600
};
function parseActivationCastTime(mechanics) {
  const activation = mechanics.activation;
  if (!activation || typeof activation !== "object" || Array.isArray(activation)) {
    return activation === void 0 ? { status: "none" } : { status: "invalid", issue: "activation must be an object" };
  }
  const raw = activation.cast_time;
  if (raw === void 0) return { status: "none" };
  if (!raw || typeof raw !== "object" || Array.isArray(raw)) {
    return { status: "invalid", issue: "activation.cast_time must be an object" };
  }
  const value = raw;
  if (JSON.stringify(Object.keys(value).sort()) !== JSON.stringify(["amount", "unit"])) {
    return { status: "invalid", issue: "activation.cast_time must declare exactly amount and unit" };
  }
  if (!Object.hasOwn(UNIT_SECONDS, String(value.unit ?? ""))) {
    return { status: "invalid", issue: "activation.cast_time.unit is invalid" };
  }
  if (!Number.isInteger(value.amount) || Number(value.amount) < 1) {
    return { status: "invalid", issue: "activation.cast_time.amount must be a positive integer" };
  }
  const unit = value.unit;
  const amount = Number(value.amount);
  const seconds = UNIT_SECONDS[unit] * amount;
  if (!Number.isSafeInteger(seconds) || seconds < 1) {
    return { status: "invalid", issue: "activation.cast_time duration is outside the supported range" };
  }
  return {
    status: "valid",
    policy: {
      unit,
      amount,
      seconds,
      atomicInEncounter: amount === 1 && (unit === "action" || unit === "bonus_action" || unit === "reaction")
    }
  };
}

// src/rules-core/worldSpellPrimitives.ts
function clone5(value) {
  return JSON.parse(JSON.stringify(value));
}
function stableId4(value, label) {
  const normalized3 = value.trim();
  if (!normalized3) throw new Error(`${label} requires a stable non-empty id`);
  return normalized3;
}
function boundedNumber(value, minimum, maximum, label) {
  if (!Number.isFinite(value) || value < minimum || value > maximum) {
    throw new Error(`${label} must be between ${minimum} and ${maximum}`);
  }
  return value;
}
function validFacts2(facts) {
  return ["scenario", "board", "gm_ruling"].includes(facts.factsSource) && Number.isInteger(facts.boardRevision) && facts.boardRevision >= 0 && Number.isFinite(facts.distanceFt) && facts.distanceFt >= 0 && typeof facts.lineOfSight === "boolean";
}
function requireFacts(facts, label, options = {}) {
  if (!validFacts2(facts)) throw new Error(`${label} requires valid explicit world facts`);
  if (options.maxRangeFt !== void 0 && facts.distanceFt > options.maxRangeFt) {
    throw new Error(`${label} target is outside ${options.maxRangeFt} feet`);
  }
  if (options.touched && (facts.touched !== true || facts.distanceFt !== 0)) {
    throw new Error(`${label} requires a touched object at distance 0`);
  }
  if (options.maxVolumeCubicFt !== void 0 && (!Number.isFinite(facts.volumeCubicFt) || facts.volumeCubicFt < 0 || facts.volumeCubicFt > options.maxVolumeCubicFt)) {
    throw new Error(`${label} requires volume at most ${options.maxVolumeCubicFt} cubic foot`);
  }
}
function mutationBuilder(initial) {
  let objects = clone5(initial);
  const events = [];
  return {
    apply(event) {
      objects = evolveWorldObjectEvent(objects, event);
      events.push(clone5(event));
    },
    result() {
      return { objects, events };
    },
    objects() {
      return objects;
    }
  };
}
function createDancingLights(input) {
  const groupId = stableId4(input.groupId, "Dancing Lights group");
  const sourceActorId = stableId4(input.sourceActorId, "Dancing Lights source actor");
  const sourceActionId = stableId4(input.sourceActionId, "Dancing Lights source action");
  const requiredCount = input.form === "medium_humanoid" ? input.policy.combinedFormObjectCount : void 0;
  if (input.placements.length < input.policy.minIndividualLights || input.placements.length > input.policy.maxIndividualLights || requiredCount !== void 0 && input.placements.length !== requiredCount) {
    throw new Error(input.form === "medium_humanoid" ? "Dancing Lights Medium form requires exactly one combined-light object" : `Dancing Lights requires ${input.policy.minIndividualLights} to ${input.policy.maxIndividualLights} individual lights`);
  }
  const ids = input.placements.map((placement) => stableId4(placement.id, "Dancing Light"));
  if (new Set(ids).size !== ids.length) throw new Error("Dancing Light ids must be distinct");
  for (const [index, placement] of input.placements.entries()) {
    boundedNumber(
      placement.distanceFromCasterFt,
      0,
      input.targeting.rangeFt,
      `${ids[index]} caster distance`
    );
    if (input.form === "individual" && input.placements.length > 1 && placement.withinRequiredSeparation !== true) {
      throw new Error(`${ids[index]} must be within ${input.policy.requiredSeparationFt} feet of another Dancing Light`);
    }
  }
  const builder = mutationBuilder(input.objects);
  for (const object5 of Object.values(builder.objects()).sort((left, right) => left.id.localeCompare(right.id))) {
    if (object5.dancingLight && object5.sourceActorId === sourceActorId && object5.sourceActionId === sourceActionId) {
      builder.apply({
        type: "WorldObjectRemoved",
        objectId: object5.id,
        reason: "dancing_lights_replaced"
      });
    }
  }
  for (const [index, placement] of input.placements.entries()) {
    const id = ids[index];
    if (builder.objects()[id]) throw new Error(`World object ${id} already exists`);
    const object5 = {
      id,
      name: input.form === "medium_humanoid" ? "Dancing Lights form" : `Dancing Light ${index + 1}`,
      kind: "spell_effect",
      size: input.form === "medium_humanoid" ? "medium" : "tiny",
      sourceActorId,
      sourceActionId,
      distanceFromSourceFt: placement.distanceFromCasterFt,
      roundsLeft: input.policy.durationRounds,
      magicalAura: { school: "illusion", createdBySpell: true, visible: true },
      dancingLight: { groupId, form: input.form, dimRadiusFt: input.policy.dimRadiusFt },
      tags: ["dancing_lights", `dim_light_${input.policy.dimRadiusFt}_ft`]
    };
    builder.apply({ type: "WorldObjectCreated", object: object5 });
  }
  return builder.result();
}
function moveDancingLights(input) {
  const group = Object.values(input.objects).filter((object5) => object5.sourceActorId === input.sourceActorId && object5.dancingLight?.groupId === input.groupId).sort((left, right) => left.id.localeCompare(right.id));
  if (!group.length) throw new Error(`Unknown Dancing Lights group ${input.groupId}`);
  const factIds = input.resultingFacts.map((facts) => stableId4(facts.lightId, "Dancing Light movement"));
  if (new Set(factIds).size !== factIds.length || factIds.length !== group.length || group.some((object5) => !factIds.includes(object5.id))) {
    throw new Error("Dancing Lights movement requires one distinct post-move fact per light");
  }
  for (const facts of input.resultingFacts) {
    boundedNumber(facts.movementFt, 0, input.policy.maxMoveFt, `${facts.lightId} movement`);
    if (!Number.isFinite(facts.distanceFromCasterFt) || facts.distanceFromCasterFt < 0) {
      throw new Error(`${facts.lightId} caster distance must be non-negative`);
    }
  }
  const byId = new Map(input.resultingFacts.map((facts) => [facts.lightId, facts]));
  const survivors = group.filter((object5) => byId.get(object5.id).distanceFromCasterFt <= input.targeting.rangeFt);
  if (group[0].dancingLight?.form === "individual" && survivors.length > 1) {
    for (const object5 of survivors) {
      if (byId.get(object5.id)?.withinRequiredSeparation !== true) {
        throw new Error(`${object5.id} must remain within ${input.policy.requiredSeparationFt} feet of another Dancing Light`);
      }
    }
  }
  const builder = mutationBuilder(input.objects);
  for (const object5 of group) {
    const facts = byId.get(object5.id);
    if (facts.distanceFromCasterFt > input.targeting.rangeFt) {
      builder.apply({
        type: "WorldObjectRemoved",
        objectId: object5.id,
        reason: "dancing_light_left_spell_range"
      });
      continue;
    }
    builder.apply({
      type: "WorldObjectPatched",
      objectId: object5.id,
      patch: { distanceFromSourceFt: facts.distanceFromCasterFt },
      reason: "dancing_lights_bonus_action_move"
    });
  }
  return builder.result();
}
function resolveDruidcraft(input) {
  stableId4(input.sourceActorId, "Druidcraft source actor");
  stableId4(input.sourceActionId, "Druidcraft source action");
  requireFacts(input.option.facts, "Druidcraft", { maxRangeFt: input.targeting.rangeFt });
  const builder = mutationBuilder(input.objects);
  switch (input.option.kind) {
    case "weather_sensor": {
      const id = stableId4(input.option.id, "Druidcraft Weather Sensor");
      const prediction = input.option.prediction.trim();
      if (!prediction) throw new Error("Druidcraft Weather Sensor requires a prediction");
      const object5 = {
        id,
        name: "Druidcraft Weather Sensor",
        kind: "spell_effect",
        size: "tiny",
        sourceActorId: input.sourceActorId,
        sourceActionId: input.sourceActionId,
        roundsLeft: input.policy.weatherDurationRounds,
        magicalAura: { school: "transmutation", createdBySpell: true, visible: true },
        tags: ["druidcraft", "weather_sensor", `prediction:${prediction}`]
      };
      builder.apply({ type: "WorldObjectCreated", object: object5 });
      break;
    }
    case "bloom": {
      const target2 = builder.objects()[input.option.objectId];
      if (!target2?.plant) throw new Error("Druidcraft Bloom requires a flower, seed pod, or leaf bud");
      builder.apply({
        type: "WorldObjectPatched",
        objectId: target2.id,
        patch: { plant: { ...target2.plant, bloomed: true } },
        reason: "druidcraft_bloom"
      });
      break;
    }
    case "sensory_effect": {
      const id = stableId4(input.option.id, "Druidcraft Sensory Effect");
      const description = input.option.description.trim();
      if (!description) throw new Error("Druidcraft Sensory Effect requires a description");
      boundedNumber(
        input.option.cubeSideFt,
        0,
        input.policy.sensoryCubeSideFt,
        "Druidcraft Sensory Effect cube side"
      );
      const object5 = {
        id,
        name: description,
        kind: "spell_effect",
        size: "medium",
        sourceActorId: input.sourceActorId,
        sourceActionId: input.sourceActionId,
        magicalAura: { school: "transmutation", createdBySpell: true, visible: true },
        tags: ["druidcraft", "instantaneous_sensory_effect"]
      };
      builder.apply({ type: "WorldObjectCreated", object: object5 });
      builder.apply({
        type: "WorldObjectRemoved",
        objectId: id,
        reason: "instantaneous_effect_completed"
      });
      break;
    }
    case "fire_play": {
      const target2 = builder.objects()[input.option.objectId];
      if (!target2?.flame) throw new Error("Druidcraft Fire Play requires a candle, torch, or campfire");
      builder.apply({
        type: "WorldObjectPatched",
        objectId: target2.id,
        patch: { flame: { ...target2.flame, lit: input.option.operation === "light" } },
        reason: "druidcraft_fire_play"
      });
      break;
    }
  }
  return builder.result();
}
function mendWorldObject(input) {
  requireFacts(input.facts, "Mending", {
    maxRangeFt: input.targeting.rangeFt,
    ...input.targeting.requiresTouch ? { touched: true } : {}
  });
  const target2 = input.objects[input.objectId];
  if (!target2?.breakOrTear) throw new Error("Mending requires one explicit break or tear");
  if (target2.breakOrTear.repaired) throw new Error("Mending requires an unrepaired break or tear");
  if (!Number.isFinite(target2.breakOrTear.maxDimensionFt) || target2.breakOrTear.maxDimensionFt < 0 || target2.breakOrTear.maxDimensionFt > input.policy.maxBreakDimensionFt) {
    throw new Error(`Mending cannot repair a break or tear larger than ${input.policy.maxBreakDimensionFt} feet`);
  }
  const builder = mutationBuilder(input.objects);
  builder.apply({
    type: "WorldObjectPatched",
    objectId: target2.id,
    patch: { breakOrTear: { ...target2.breakOrTear, repaired: true } },
    reason: "mending_repaired_break_or_tear"
  });
  return builder.result();
}
function observeDetectPoisonAndDisease(input) {
  const senseRangeFt = input.targeting.area?.radiusFt;
  if (senseRangeFt === void 0) {
    throw new Error("Detect Poison and Disease requires a numeric sensing radius");
  }
  const sensed = validFacts2(input.facts) && input.facts.distanceFt <= senseRangeFt && !magicBlockedBy(input.blockingLayers, input.policy.blockers) && input.object.hazardousSubstance !== void 0;
  if (!sensed) return { sensed: false, locationKnown: false };
  return {
    sensed: true,
    locationKnown: true,
    kind: input.object.hazardousSubstance.specificKind
  };
}
function purifyFoodAndDrink(input) {
  boundedNumber(
    input.sphereCenterDistanceFt,
    0,
    input.targeting.rangeFt,
    "Purify Food and Drink sphere center"
  );
  const builder = mutationBuilder(input.objects);
  for (const objectId of Object.keys(input.factsByObject).sort()) {
    const target2 = builder.objects()[objectId];
    if (!target2) throw new Error(`Unknown world object ${objectId}`);
    const facts = input.factsByObject[objectId];
    requireFacts(facts, `Purify Food and Drink ${objectId}`);
    if (input.policy.requireInArea && facts.inArea !== true || !target2.foodOrDrink || input.policy.excludeMagical && target2.foodOrDrink.magical) continue;
    if (!target2.foodOrDrink.poisoned && !target2.foodOrDrink.rotten) continue;
    builder.apply({
      type: "WorldObjectPatched",
      objectId,
      patch: {
        foodOrDrink: { ...target2.foodOrDrink, poisoned: false, rotten: false }
      },
      reason: "purify_food_and_drink"
    });
  }
  return builder.result();
}
function activePrestidigitationEffects(objects, sourceActorId) {
  return Object.values(objects).flatMap((object5) => {
    const attachments = (object5.prestidigitation ?? []).filter((effect) => effect.sourceActorId === sourceActorId).map((effect) => ({ id: effect.id, objectId: object5.id, kind: "attachment" }));
    const creation = object5.sourceActorId === sourceActorId && object5.tags?.includes("prestidigitation:minor_creation") ? [{ id: object5.id, objectId: object5.id, kind: "creation" }] : [];
    return [...attachments, ...creation];
  }).sort((left, right) => left.id.localeCompare(right.id));
}
function replacePrestidigitationEffect(input) {
  const match = activePrestidigitationEffects(
    input.builder.objects(),
    input.sourceActorId
  ).find((effect) => effect.id === input.effectId);
  if (!match) throw new Error(`Unknown source-owned Prestidigitation effect ${input.effectId}`);
  if (match.kind === "creation") {
    input.builder.apply({
      type: "WorldObjectRemoved",
      objectId: match.objectId,
      reason: "prestidigitation_replaced"
    });
    return;
  }
  const object5 = input.builder.objects()[match.objectId];
  const remaining = object5.prestidigitation.filter((effect) => effect.id !== match.id);
  input.builder.apply({
    type: "WorldObjectPatched",
    objectId: match.objectId,
    patch: remaining.length ? { prestidigitation: remaining } : {},
    ...remaining.length ? {} : { unset: ["prestidigitation"] },
    reason: "prestidigitation_replaced"
  });
}
function preparePrestidigitationSlot(input) {
  if (input.replaceEffectId) replacePrestidigitationEffect({
    ...input,
    effectId: input.replaceEffectId
  });
  if (activePrestidigitationEffects(
    input.builder.objects(),
    input.sourceActorId
  ).length >= input.policy.maxActiveEffects) {
    throw new Error(`Prestidigitation permits at most ${input.policy.maxActiveEffects} active non-instantaneous effects`);
  }
}
function resolvePrestidigitation(input) {
  stableId4(input.sourceActorId, "Prestidigitation source actor");
  stableId4(input.sourceActionId, "Prestidigitation source action");
  requireFacts(input.option.facts, "Prestidigitation", { maxRangeFt: input.targeting.rangeFt });
  const builder = mutationBuilder(input.objects);
  switch (input.option.kind) {
    case "sensory_effect": {
      const id = stableId4(input.option.id, "Prestidigitation Sensory Effect");
      const description = input.option.description.trim();
      if (!description) throw new Error("Prestidigitation Sensory Effect requires a description");
      const object5 = {
        id,
        name: description,
        kind: "spell_effect",
        size: "tiny",
        sourceActorId: input.sourceActorId,
        sourceActionId: input.sourceActionId,
        magicalAura: { school: "transmutation", createdBySpell: true, visible: true },
        tags: ["prestidigitation", "instantaneous_sensory_effect"]
      };
      builder.apply({ type: "WorldObjectCreated", object: object5 });
      builder.apply({
        type: "WorldObjectRemoved",
        objectId: id,
        reason: "instantaneous_effect_completed"
      });
      break;
    }
    case "fire_play": {
      const target2 = builder.objects()[input.option.objectId];
      if (!target2?.flame) throw new Error("Prestidigitation Fire Play requires a candle, torch, or small campfire");
      builder.apply({
        type: "WorldObjectPatched",
        objectId: target2.id,
        patch: { flame: { ...target2.flame, lit: input.option.operation === "light" } },
        reason: "prestidigitation_fire_play"
      });
      break;
    }
    case "clean_or_soil": {
      requireFacts(input.option.facts, "Prestidigitation Clean or Soil", {
        maxRangeFt: input.targeting.rangeFt,
        maxVolumeCubicFt: input.policy.maxVolumeCubicFt
      });
      const target2 = builder.objects()[input.option.objectId];
      if (!target2) throw new Error(`Unknown world object ${input.option.objectId}`);
      builder.apply({
        type: "WorldObjectPatched",
        objectId: target2.id,
        patch: { soiled: input.option.operation === "soil" },
        reason: "prestidigitation_clean_or_soil"
      });
      break;
    }
    case "minor_sensation":
    case "magic_mark": {
      if (input.option.kind === "minor_sensation") {
        requireFacts(input.option.facts, "Prestidigitation Minor Sensation", {
          maxRangeFt: input.targeting.rangeFt,
          maxVolumeCubicFt: input.policy.maxVolumeCubicFt
        });
      }
      const target2 = builder.objects()[input.option.objectId];
      if (!target2) throw new Error(`Unknown world object ${input.option.objectId}`);
      const id = stableId4(input.option.id, "Prestidigitation effect");
      const description = input.option.description.trim();
      if (!description) throw new Error("Prestidigitation effect requires a description");
      preparePrestidigitationSlot({
        builder,
        sourceActorId: input.sourceActorId,
        replaceEffectId: input.option.replaceEffectId,
        policy: input.policy
      });
      if (activePrestidigitationEffects(
        builder.objects(),
        input.sourceActorId
      ).some((effect) => effect.id === id)) {
        throw new Error(`Prestidigitation effect ${id} already exists`);
      }
      const currentTarget = builder.objects()[input.option.objectId];
      if (!currentTarget) {
        throw new Error("Prestidigitation replacement removed the selected target object");
      }
      const attachment = {
        id,
        sourceActorId: input.sourceActorId,
        sourceActionId: input.sourceActionId,
        kind: input.option.kind,
        description,
        roundsLeft: input.policy.attachmentDurationRounds
      };
      builder.apply({
        type: "WorldObjectPatched",
        objectId: currentTarget.id,
        patch: { prestidigitation: [...currentTarget.prestidigitation ?? [], attachment] },
        reason: `prestidigitation_${input.option.kind}`
      });
      break;
    }
    case "minor_creation": {
      if (input.option.fitsInHand !== true) {
        throw new Error("Prestidigitation Minor Creation must fit in the caster\u2019s hand");
      }
      const id = stableId4(input.option.id, "Prestidigitation Minor Creation");
      const description = input.option.description.trim();
      if (!description) throw new Error("Prestidigitation Minor Creation requires a description");
      preparePrestidigitationSlot({
        builder,
        sourceActorId: input.sourceActorId,
        replaceEffectId: input.option.replaceEffectId,
        policy: input.policy
      });
      if (builder.objects()[id]) throw new Error(`World object ${id} already exists`);
      const object5 = {
        id,
        name: description,
        kind: "spell_effect",
        size: input.option.size,
        sourceActorId: input.sourceActorId,
        sourceActionId: input.sourceActionId,
        // The cast happens during the current turn: survive this turn ending,
        // then expire at the end of the caster's next turn.
        sourceTurnEndingsLeft: input.policy.creationSourceTurnEndings,
        magicalAura: { school: "transmutation", createdBySpell: true, visible: true },
        tags: [
          "prestidigitation",
          "prestidigitation:minor_creation",
          "nonmagical_trinket_or_illusory_image",
          "no_damage",
          "no_monetary_worth"
        ]
      };
      builder.apply({ type: "WorldObjectCreated", object: object5 });
      break;
    }
  }
  return builder.result();
}
function endSourceActorTurnWorldObjects(input) {
  const builder = mutationBuilder(input.objects);
  for (const object5 of Object.values(builder.objects()).sort((left, right) => left.id.localeCompare(right.id))) {
    if (object5.sourceActorId !== input.sourceActorId || object5.sourceTurnEndingsLeft === void 0) continue;
    if (!Number.isInteger(object5.sourceTurnEndingsLeft) || object5.sourceTurnEndingsLeft < 1) {
      throw new Error(`Invalid source-turn expiry on ${object5.id}`);
    }
    if (object5.sourceTurnEndingsLeft === 1) {
      builder.apply({
        type: "WorldObjectRemoved",
        objectId: object5.id,
        reason: "source_turn_end_expired"
      });
    } else {
      builder.apply({
        type: "WorldObjectPatched",
        objectId: object5.id,
        patch: { sourceTurnEndingsLeft: object5.sourceTurnEndingsLeft - 1 },
        reason: "source_turn_end_advanced"
      });
    }
  }
  return builder.result();
}

// src/rules-core/armorOfAgathys.ts
var TEMPORARY_HP_MELEE_RETALIATION_EFFECT_KIND = "temporary_hp_melee_retaliation";
function record6(value) {
  return value !== null && typeof value === "object" && !Array.isArray(value) ? value : null;
}
function stable(value, label) {
  const normalized3 = value.trim();
  if (!normalized3) throw new Error(`${label} requires a stable id`);
  return normalized3;
}
function canonicalSources(sourceEntityIds) {
  const result = [...new Set(sourceEntityIds.map((id) => id.trim()).filter(Boolean))].sort();
  if (result.length === 0) throw new Error("Retaliation provenance must not be empty");
  return result;
}
function temporaryHpMeleeRetaliationPolicyFromMechanics(value) {
  const primitive = record6(value);
  if (primitive?.type !== "temporary_hp_melee_retaliation" || !Number.isInteger(primitive.temporary_hp_per_slot) || Number(primitive.temporary_hp_per_slot) < 1 || !Number.isInteger(primitive.retaliation_damage_per_slot) || Number(primitive.retaliation_damage_per_slot) < 1 || typeof primitive.retaliation_damage_type !== "string" || !primitive.retaliation_damage_type.trim() || primitive.retaliation_trigger !== "hit_by_melee_attack_roll" || !Number.isInteger(primitive.duration_rounds) || Number(primitive.duration_rounds) < 1 || primitive.end_when_no_temporary_hp !== true || !Number.isInteger(primitive.minimum_slot_level) || Number(primitive.minimum_slot_level) < 1 || !Number.isInteger(primitive.maximum_slot_level) || Number(primitive.maximum_slot_level) < Number(primitive.minimum_slot_level)) return null;
  return {
    temporaryHpPerSlot: Number(primitive.temporary_hp_per_slot),
    retaliationDamagePerSlot: Number(primitive.retaliation_damage_per_slot),
    retaliationDamageType: primitive.retaliation_damage_type,
    retaliationTrigger: "hit_by_melee_attack_roll",
    durationRounds: Number(primitive.duration_rounds),
    endWhenNoTemporaryHp: true,
    minimumSlotLevel: Number(primitive.minimum_slot_level),
    maximumSlotLevel: Number(primitive.maximum_slot_level)
  };
}
function armorOfAgathysValues(slotLevel, policy) {
  if (!Number.isInteger(slotLevel) || slotLevel < policy.minimumSlotLevel || slotLevel > policy.maximumSlotLevel) {
    throw new Error(
      `Slot-scaled retaliation requires a spell-slot level from ${policy.minimumSlotLevel} to ${policy.maximumSlotLevel}`
    );
  }
  return {
    temporaryHp: slotLevel * policy.temporaryHpPerSlot,
    retaliationDamage: slotLevel * policy.retaliationDamagePerSlot
  };
}
function createArmorOfAgathysEffect(input) {
  const id = stable(input.id, "Temporary HP retaliation effect");
  const actorId = stable(input.actorId, "Temporary HP retaliation actor");
  const actionId = stable(input.actionId, "Temporary HP retaliation action");
  const name = stable(input.name, "Slot-scaled retaliation effect name");
  const { retaliationDamage } = armorOfAgathysValues(input.slotLevel, input.policy);
  return {
    id,
    name,
    mechanics: {
      kind: TEMPORARY_HP_MELEE_RETALIATION_EFFECT_KIND,
      actionId,
      slotLevel: input.slotLevel,
      temporaryHpPerSlot: input.policy.temporaryHpPerSlot,
      retaliationDamagePerSlot: input.policy.retaliationDamagePerSlot,
      retaliationDamage,
      retaliationDamageType: input.policy.retaliationDamageType,
      durationRounds: input.policy.durationRounds,
      minimumSlotLevel: input.policy.minimumSlotLevel,
      maximumSlotLevel: input.policy.maximumSlotLevel,
      endWhenNoTemporaryHp: input.policy.endWhenNoTemporaryHp,
      trigger: { event: input.policy.retaliationTrigger },
      sourceEntityIds: canonicalSources(input.sourceEntityIds)
    },
    roundsLeft: input.policy.durationRounds,
    source: name,
    ownerId: actorId,
    sourceId: actorId
  };
}
function applyArmorOfAgathysCast(input) {
  if (!["take_spell", "keep_current"].includes(input.temporaryHpChoice)) {
    throw new Error("Temporary HP retaliation requires an explicit Temporary HP choice");
  }
  if (!Number.isInteger(input.state.hp.temp) || input.state.hp.temp < 0) {
    throw new Error("Temporary HP retaliation requires a non-negative integer Temporary HP state");
  }
  const issue = armorOfAgathysEffectIssue(input.effect, input.effect.ownerId ?? "");
  if (issue) throw new Error(issue);
  const mechanics = input.effect.mechanics;
  const granted = mechanics.slotLevel * mechanics.temporaryHpPerSlot;
  const temp = input.temporaryHpChoice === "take_spell" ? granted : input.state.hp.temp;
  const activeEffects = input.state.activeEffects.filter((effect) => {
    const candidate = record6(effect.mechanics);
    return !(candidate?.kind === TEMPORARY_HP_MELEE_RETALIATION_EFFECT_KIND && effect.ownerId === input.effect.ownerId && candidate.actionId === mechanics.actionId);
  });
  if (temp > 0) activeEffects.push(JSON.parse(JSON.stringify(input.effect)));
  return {
    ...input.state,
    hp: { ...input.state.hp, temp },
    activeEffects
  };
}
function temporaryHpMeleeRetaliations(input) {
  if (!input.facts.hit || input.facts.attackRollKind !== "melee" || input.facts.attackerActorId === input.facts.defenderActorId || !Number.isInteger(input.facts.temporaryHpBeforeHit) || input.facts.temporaryHpBeforeHit <= 0) return [];
  return input.effects.flatMap((effect) => {
    if (record6(effect.mechanics)?.kind !== TEMPORARY_HP_MELEE_RETALIATION_EFFECT_KIND) return [];
    if (armorOfAgathysEffectIssue(effect, input.facts.defenderActorId)) return [];
    const mechanics = effect.mechanics;
    return [{
      effectId: effect.id,
      sourceActionId: mechanics.actionId,
      damageType: mechanics.retaliationDamageType,
      amount: mechanics.retaliationDamage,
      sourceEntityIds: [...mechanics.sourceEntityIds]
    }];
  }).sort((left, right) => left.effectId.localeCompare(right.effectId));
}
function endArmorOfAgathysWithoutTemporaryHp(state) {
  if (state.hp.temp > 0) return state;
  const activeEffects = state.activeEffects.filter((effect) => record6(effect.mechanics)?.kind !== TEMPORARY_HP_MELEE_RETALIATION_EFFECT_KIND);
  if (activeEffects.length === state.activeEffects.length) return state;
  return { ...state, activeEffects };
}
function armorOfAgathysEffectIssue(value, ownerActorId) {
  const effect = record6(value);
  const mechanics = record6(effect?.mechanics);
  if (mechanics?.kind !== TEMPORARY_HP_MELEE_RETALIATION_EFFECT_KIND) return null;
  if (typeof effect?.id !== "string" || !effect.id.trim() || typeof effect.name !== "string" || !effect.name.trim() || typeof effect.source !== "string" || !effect.source.trim()) {
    return "Temporary HP retaliation effect must retain its stable identity";
  }
  if (!ownerActorId || effect?.ownerId !== ownerActorId || effect.sourceId !== ownerActorId) {
    return "Temporary HP retaliation effect must retain its owner and source actor";
  }
  if (!Number.isInteger(mechanics.durationRounds) || Number(mechanics.durationRounds) < 1 || !Number.isInteger(effect.roundsLeft) || Number(effect.roundsLeft) < 1 || Number(effect.roundsLeft) > Number(mechanics.durationRounds)) {
    return "Temporary HP retaliation duration exceeds its declared policy";
  }
  const slotLevel = Number(mechanics.slotLevel);
  if (!Number.isInteger(mechanics.minimumSlotLevel) || Number(mechanics.minimumSlotLevel) < 1 || !Number.isInteger(mechanics.maximumSlotLevel) || Number(mechanics.maximumSlotLevel) < Number(mechanics.minimumSlotLevel) || !Number.isInteger(slotLevel) || slotLevel < Number(mechanics.minimumSlotLevel) || slotLevel > Number(mechanics.maximumSlotLevel) || !Number.isInteger(mechanics.temporaryHpPerSlot) || Number(mechanics.temporaryHpPerSlot) < 1 || !Number.isInteger(mechanics.retaliationDamagePerSlot) || Number(mechanics.retaliationDamagePerSlot) < 1 || mechanics.retaliationDamage !== slotLevel * Number(mechanics.retaliationDamagePerSlot) || typeof mechanics.retaliationDamageType !== "string" || !mechanics.retaliationDamageType.trim()) {
    return "Temporary HP retaliation values are inconsistent with its declared policy";
  }
  if (typeof mechanics.actionId !== "string" || !mechanics.actionId.trim()) {
    return "Temporary HP retaliation effect must retain its source action";
  }
  const trigger = record6(mechanics.trigger);
  if (mechanics.endWhenNoTemporaryHp !== true || trigger?.event !== "hit_by_melee_attack_roll") {
    return "Temporary HP retaliation effect has invalid trigger or end semantics";
  }
  const rawSources = mechanics.sourceEntityIds;
  const sources = Array.isArray(rawSources) ? rawSources.filter((id) => typeof id === "string" && !!id.trim()) : [];
  if (!Array.isArray(rawSources) || sources.length !== rawSources.length || sources.length === 0 || sources.some((id) => id !== id.trim()) || sources.length !== new Set(sources).size || JSON.stringify(sources) !== JSON.stringify([...sources].sort())) {
    return "Temporary HP retaliation effect must retain non-empty, unique, stable provenance";
  }
  return null;
}

// src/rules-core/actorTraits.ts
function longRestEligibility(traits, providedHours) {
  const profile = traits?.restProfile ?? {
    longRestHours: 8,
    sleepRequired: true,
    sourceEntityIds: ["system:dnd5e-2024:long-rest"]
  };
  if (!Number.isFinite(providedHours) || providedHours < profile.longRestHours) {
    return {
      eligible: false,
      requiredHours: profile.longRestHours,
      providedHours,
      sleepRequired: profile.sleepRequired
    };
  }
  return {
    eligible: true,
    requiredHours: profile.longRestHours,
    sleepRequired: profile.sleepRequired
  };
}

// src/rules-core/spellcastingExecution.ts
function isRecord(value) {
  return typeof value === "object" && value !== null && !Array.isArray(value);
}
function cloneJson5(value) {
  if (Array.isArray(value)) return value.map(cloneJson5);
  if (!isRecord(value)) return value;
  return Object.fromEntries(
    Object.entries(value).map(([key, nested]) => [key, cloneJson5(nested)])
  );
}
function accessError(error) {
  return { ...error, stage: "access" };
}
function definitionError(code, message) {
  return { status: "rejected", stage: "action_definition", code, message };
}
function paymentResources(state, actionId) {
  const resources = /* @__PURE__ */ new Set();
  for (const grant of state.grants) {
    if (grant.actionId !== actionId) continue;
    if (grant.slotResource) resources.add(grant.slotResource);
    if (grant.freeUseResource) resources.add(grant.freeUseResource);
  }
  return resources;
}
function isSlotResource(resource) {
  return resource === "spell_slot" || /^(?:spell_slot|pact_slot|warlock_spell_slot)_\d+$/.test(resource);
}
function canonicalActivationCost(input) {
  const rawActivation = input.action.mechanics.activation;
  if (rawActivation !== void 0 && !isRecord(rawActivation)) {
    return definitionError(
      "MalformedSpellActivation",
      `Spell action ${input.action.id} has a malformed activation object`
    );
  }
  const rawCost = rawActivation?.cost;
  if (rawCost !== void 0 && !Array.isArray(rawCost)) {
    return definitionError(
      "MalformedSpellActivationCost",
      `Spell action ${input.action.id} has a malformed activation cost`
    );
  }
  if (Array.isArray(rawCost) && rawCost.some((entry) => !isRecord(entry))) {
    return definitionError(
      "MalformedSpellActivationCost",
      `Spell action ${input.action.id} has a non-object activation cost entry`
    );
  }
  const sourcePayments = paymentResources(input.accessState, input.action.id);
  const preserved = (rawCost ?? []).filter((entry) => isRecord(entry)).filter((entry) => {
    const resource = typeof entry.resource === "string" ? entry.resource : "";
    return !isSlotResource(resource) && !sourcePayments.has(resource);
  }).map((entry) => cloneJson5(entry));
  if (input.payment.kind === "none") return preserved;
  if (!input.payment.resource) {
    return definitionError(
      "MalformedResolvedSpellPayment",
      `Resolved ${input.payment.kind} payment for ${input.action.id} has no resource`
    );
  }
  return [...preserved, { resource: input.payment.resource, amount: 1 }];
}
function detachedAction(action, activationCost2) {
  const mechanics = cloneJson5(action.mechanics);
  const activation = isRecord(mechanics.activation) ? mechanics.activation : {};
  mechanics.activation = { ...activation, cost: activationCost2 };
  return {
    ...action,
    sourceEntityIds: [...action.sourceEntityIds],
    mechanics,
    ...action.targeting ? {
      targeting: {
        ...action.targeting,
        allowedRelations: [...action.targeting.allowedRelations]
      }
    } : {},
    spell: {
      ...action.spell,
      ...action.spell.components ? { components: { ...action.spell.components } } : {}
    }
  };
}
function prepareSpellExecution(input) {
  const declaration = input.declaration ?? {};
  const resolution = resolveSpellAccess({
    state: input.accessState,
    actionId: input.action.id,
    resources: input.resources,
    ...declaration.grantId !== void 0 ? { grantId: declaration.grantId } : {},
    ...declaration.mode !== void 0 ? { mode: declaration.mode } : {},
    ...declaration.preferFreeUse !== void 0 ? { preferFreeUse: declaration.preferFreeUse } : {}
  });
  if (resolution.status === "rejected") return accessError(resolution);
  if (resolution.grant.level !== input.action.spell.level) {
    return definitionError(
      "SpellGrantLevelMismatch",
      `Grant ${resolution.grant.grantId} has level ${resolution.grant.level}, but ${input.action.id} has level ${input.action.spell.level}`
    );
  }
  const cost = canonicalActivationCost({
    action: input.action,
    accessState: input.accessState,
    payment: resolution.payment
  });
  if (!Array.isArray(cost)) return cost;
  return {
    status: "ready",
    executableAction: detachedAction(input.action, cost),
    payment: { ...resolution.payment },
    provenance: {
      grantId: resolution.grant.grantId,
      sourceId: resolution.grant.sourceId,
      access: resolution.grant.access,
      spellcastingAbility: resolution.grant.spellcastingAbility,
      mode: declaration.mode ?? "normal"
    }
  };
}

// src/rules-core/restDecisions.ts
function rejected3(code, message) {
  return { status: "rejected", code, message };
}
function resolveSlotRecoveryRestDecision(input) {
  const sourceLevel = input.classLevels?.[input.policy.levelSource.classId] ?? 0;
  if (!Number.isInteger(sourceLevel) || sourceLevel < input.policy.levelSource.minimum || sourceLevel > input.policy.levelSource.maximum) {
    return rejected3(
      "RestDecisionInvalidLevelSource",
      `${input.policy.decisionType} requires ${input.policy.levelSource.classId} level ${input.policy.levelSource.minimum}-${input.policy.levelSource.maximum}, got ${sourceLevel}`
    );
  }
  if (input.decision.type !== input.policy.decisionType || !Array.isArray(input.decision.slotLevels) || input.decision.slotLevels.length === 0) {
    return rejected3(
      "RestDecisionSelectionEmpty",
      `${input.policy.decisionType} must select at least one recoverable resource level`
    );
  }
  const { minimumLevel, maximumLevel, prefix, restoreAmount } = input.policy.slotResource;
  if (input.decision.slotLevels.some((level) => !Number.isInteger(level) || level < minimumLevel || level > maximumLevel)) {
    return rejected3(
      "RestDecisionSlotLevelInvalid",
      `${input.policy.decisionType} accepts levels ${minimumLevel}-${maximumLevel}`
    );
  }
  const recoveryBudget = Math.ceil(sourceLevel / input.policy.budget.divisor);
  const selectedTotal = input.decision.slotLevels.reduce((sum, level) => sum + level, 0);
  if (selectedTotal > recoveryBudget) {
    return rejected3(
      "RestDecisionBudgetExceeded",
      `Selected levels total ${selectedTotal}, exceeding the recovery budget ${recoveryBudget}`
    );
  }
  const charge = input.policy.charge;
  if ((input.state.resources[charge.resource] ?? 0) < charge.amount) {
    return rejected3(
      "RestDecisionUnavailable",
      `${input.policy.decisionType} has no remaining ${charge.resource}`
    );
  }
  const requestedByResource = /* @__PURE__ */ new Map();
  for (const level of input.decision.slotLevels) {
    const resource = `${prefix}${level}`;
    requestedByResource.set(resource, (requestedByResource.get(resource) ?? 0) + restoreAmount);
  }
  for (const [resource, amount] of requestedByResource) {
    const current = input.state.resources[resource] ?? 0;
    const maximum = input.state.maxResources[resource] ?? 0;
    if (maximum < 1 || current + amount > maximum) {
      return rejected3(
        "RestDecisionSlotNotExpended",
        `${resource} does not have ${amount} recoverable expended unit(s)`
      );
    }
  }
  const resources = { ...input.state.resources };
  resources[charge.resource] -= charge.amount;
  const restoredResources = [...requestedByResource].map(([resource, amount]) => {
    resources[resource] = (resources[resource] ?? 0) + amount;
    return { resource, amount, current: resources[resource] };
  });
  return {
    status: "allowed",
    state: { ...input.state, resources },
    recoveredSlotLevels: [...input.decision.slotLevels].sort((left, right) => left - right),
    recoveryBudget,
    spentResource: {
      resource: charge.resource,
      amount: charge.amount,
      remaining: resources[charge.resource]
    },
    restoredResources
  };
}

// src/rules-core/dwarfTraits.ts
var STONE_FORMS = ["natural", "worked"];
var STONE_CONTACTS = ["on_surface", "touching_surface"];
function record7(value) {
  return value !== null && typeof value === "object" && !Array.isArray(value) ? value : null;
}
function fixedDurationRounds(value) {
  const duration = record7(value);
  if (!duration || !Number.isFinite(duration.amount) || Number(duration.amount) <= 0) return null;
  const amount = Number(duration.amount);
  if (duration.type === "rounds") {
    return Number.isInteger(amount) ? amount : null;
  }
  if (duration.type === "minutes") {
    const rounds = amount * 10;
    return Number.isInteger(rounds) ? rounds : null;
  }
  if (duration.type === "hours") {
    const rounds = amount * 600;
    return Number.isInteger(rounds) ? rounds : null;
  }
  return null;
}
function stoneworkContactIssue(value) {
  const facts = record7(value);
  if (!facts) return "Stonecunning requires explicit stone-surface contact facts";
  if (facts.material !== "stone") return "Stonecunning requires a stone surface";
  if (!STONE_FORMS.includes(facts.stoneForm)) {
    return "Stonecunning stone must be natural or worked";
  }
  if (!STONE_CONTACTS.includes(
    facts.contact
  )) {
    return "Stonecunning requires standing on or touching the stone surface";
  }
  return null;
}
function stableEntityIds(value) {
  return Array.isArray(value) ? [...new Set(value.filter((id) => typeof id === "string" && id.length > 0))].sort() : [];
}
function runtimeSenseEffectIssue(value, ownerActorId) {
  const effect = record7(value);
  const mechanics = record7(effect?.mechanics);
  if (!mechanics || mechanics.kind !== "grant_sense") return null;
  if (typeof effect?.id !== "string" || !effect.id.trim() || typeof effect.name !== "string" || !effect.name.trim() || typeof effect.source !== "string" || !effect.source.trim()) {
    return "Runtime sense effect requires stable effect and source labels";
  }
  if (effect.ownerId !== ownerActorId || typeof effect.sourceId !== "string" || !effect.sourceId.trim()) {
    return "Runtime sense effect must retain its owner and source actor provenance";
  }
  if (typeof mechanics.sense !== "string" || !mechanics.sense.trim() || !Number.isFinite(mechanics.range) || Number(mechanics.range) <= 0) {
    return "Runtime sense declaration requires a non-empty sense and positive finite range";
  }
  if (mechanics.senseScope !== void 0) {
    const scope = record7(mechanics.senseScope);
    if (!scope || typeof scope.kind !== "string" || !scope.kind.trim()) {
      return "Runtime sense scope must retain a generic non-empty kind";
    }
  }
  if (mechanics.duration !== void 0) {
    const declaredRounds = fixedDurationRounds(mechanics.duration);
    if (declaredRounds === null || !Number.isInteger(effect.roundsLeft) || Number(effect.roundsLeft) < 1 || Number(effect.roundsLeft) > declaredRounds) {
      return "Runtime sense remaining duration must not exceed its declared fixed duration";
    }
  } else if (effect.roundsLeft !== void 0) {
    return "Runtime sense without a fixed duration cannot retain a rounds counter";
  }
  const rawSourceEntityIds = mechanics.sourceEntityIds;
  const sourceEntityIds = stableEntityIds(rawSourceEntityIds);
  if (!Array.isArray(rawSourceEntityIds) || sourceEntityIds.length === 0 || rawSourceEntityIds.some((id) => typeof id !== "string" || id.trim() !== id || id.length === 0) || sourceEntityIds.length !== rawSourceEntityIds.length) {
    return "Runtime sense effect must retain non-empty, unique sourceEntityIds";
  }
  return null;
}

// src/rules-core/lightWeaponExtraAttack.ts
function immutableCardHasLightProperty(card) {
  if (card?.type !== "weapon") return false;
  const parsed = parseWeaponProfile(card);
  return parsed.valid && parsed.profile.properties.includes("light");
}
var LIGHT_WEAPON_EXTRA_ATTACK_USE_PREFIX = "system:dnd5e-2024:light-property-extra-attack:";
function lightWeaponExtraAttackUseKey(attackActionId) {
  return `${LIGHT_WEAPON_EXTRA_ATTACK_USE_PREFIX}${attackActionId}`;
}
function lightWeaponExtraAttackDamageAbility(abilityModifier) {
  if (!Number.isInteger(abilityModifier)) {
    throw new Error("Light extra-attack damage requires an integer weapon ability modifier");
  }
  return abilityModifier < 0 ? "auto" : "none";
}
function cardById2(cards, cardId) {
  return cards.find((card) => card.id === cardId);
}
function lightWeaponExtraAttackEligibility(input) {
  if (input.firedThisTurn.includes(lightWeaponExtraAttackUseKey(input.attackAction.id))) {
    return { eligible: false, issue: "already_used" };
  }
  if (input.attackAction.blockedByResolutionId) return { eligible: false, issue: "attack_action_blocked" };
  if (input.attackAction.status !== "completed") {
    return { eligible: false, issue: "attack_action_not_completed" };
  }
  if (input.attackAction.turnKey !== input.currentTurnKey) return { eligible: false, issue: "wrong_turn" };
  if (input.attackAction.attacksRemaining !== 0) {
    return { eligible: false, issue: "attack_budget_incomplete" };
  }
  const weaponEntries = input.attackAction.entries.filter((entry) => entry.kind === "weapon_attack");
  const qualifyingWeapons = weaponEntries.flatMap((entry) => {
    const card = entry.weaponCardId ? cardById2(input.cards, entry.weaponCardId) : void 0;
    return card && immutableCardHasLightProperty(card) ? [card] : [];
  });
  if (!qualifyingWeapons.length) {
    const weaponEntry = input.attackAction.entries.find((entry) => entry.kind === "weapon_attack");
    if (!weaponEntry) return { eligible: false, issue: "no_qualifying_light_attack" };
    const card = weaponEntry.weaponCardId ? cardById2(input.cards, weaponEntry.weaponCardId) : void 0;
    return card ? { eligible: false, issue: "qualifying_weapon_not_light" } : { eligible: false, issue: "qualifying_weapon_missing" };
  }
  const extraWeapon = cardById2(input.cards, input.selectedWeaponCardId);
  if (!extraWeapon) return { eligible: false, issue: "extra_weapon_missing" };
  if (!immutableCardHasLightProperty(extraWeapon)) {
    return { eligible: false, issue: "extra_weapon_not_light" };
  }
  const extraWeaponHand = input.equipment.main_hand === extraWeapon.id ? "main" : input.equipment.off_hand === extraWeapon.id ? "off" : null;
  if (!extraWeaponHand) return { eligible: false, issue: "extra_weapon_not_equipped" };
  const equippedQualifyingWeapons = qualifyingWeapons.filter((weapon) => weapon.id !== extraWeapon.id && Object.entries(input.equipment).some(([slot, cardId]) => (slot === "main_hand" || slot === "off_hand") && cardId === weapon.id));
  if (!equippedQualifyingWeapons.length) {
    if (qualifyingWeapons.every((weapon) => weapon.id === extraWeapon.id)) {
      return { eligible: false, issue: "same_weapon" };
    }
    return { eligible: false, issue: "qualifying_weapon_not_equipped" };
  }
  const actionEconomy = input.actionEconomy ?? "bonus_action";
  if (actionEconomy === "bonus_action" && (!Number.isFinite(input.bonusActions) || input.bonusActions < 1)) {
    return { eligible: false, issue: "bonus_action_unavailable" };
  }
  return {
    eligible: true,
    facts: {
      qualifyingWeapon: equippedQualifyingWeapons[0],
      extraWeapon,
      extraWeaponHand,
      ...input.actionEconomy ? { actionEconomy } : {}
    }
  };
}

// src/rules-core/conditionsRuntime.ts
function actor(world, actorId) {
  const found = world.actors[actorId];
  if (!found) throw new Error(`Unknown condition interaction actor ${actorId}`);
  return found;
}
function conditionEvalContext(facts) {
  const distancesFt = {};
  for (const observation of facts.distanceObservations ?? []) {
    (distancesFt[observation.actorAId] ??= {})[observation.actorBId] = observation.distanceFt;
    (distancesFt[observation.actorBId] ??= {})[observation.actorAId] = observation.distanceFt;
  }
  const visibility = {};
  for (const observation of facts.visibilityObservations ?? []) {
    (visibility[observation.observerActorId] ??= {})[observation.observedActorId] = observation.canSee;
  }
  return {
    rollerActorId: facts.rollerActorId,
    ...facts.rollTargetActorId ? { rollTargetActorId: facts.rollTargetActorId } : {},
    conditionSourceFacts: Object.fromEntries(
      (facts.sourceObservations ?? []).map((observation) => [
        observation.sourceActorId,
        { lineOfSight: observation.lineOfSight }
      ])
    ),
    distancesFt,
    visibility
  };
}
function conditionInteractionDenied(input) {
  const owner = actor(input.world, input.actorId);
  actor(input.world, input.targetActorId);
  return conditionCapabilityDenied(
    owner.runtime,
    input.capability,
    conditionEvalContext({
      rollerActorId: owner.id,
      rollTargetActorId: input.targetActorId,
      sourceObservations: input.sourceObservations,
      distanceObservations: input.distanceObservations,
      visibilityObservations: input.visibilityObservations
    }),
    owner.passives ?? []
  );
}
function conditionTargetingSightIssue(input) {
  if (!input.requiresSight) return null;
  const source2 = actor(input.world, input.sourceActorId);
  const target2 = actor(input.world, input.targetActorId);
  if (activeConditionWorldFactEnabled(source2.runtime, "cannot_see")) {
    return "source_cannot_see";
  }
  if (activeConditionWorldFactEnabled(
    target2.runtime,
    "cannot_be_targeted_by_requires_sight_unless_seen"
  ) && input.canSeeTarget !== true) {
    return "target_unseen";
  }
  return null;
}
function terminalConditionFacts(world) {
  return Object.values(world.actors).flatMap((owner) => conditionThresholdOutcomes(owner.runtime).map((outcome) => ({
    actorId: owner.id,
    ...outcome
  })));
}

// src/rules-core/handler.ts
var ABILITY_LABEL3 = {
  str: "\u0421\u0418\u041B",
  dex: "\u041B\u0412\u041A",
  con: "\u0422\u0415\u041B",
  int: "\u0418\u041D\u0422",
  wis: "\u041C\u0414\u0420",
  cha: "\u0425\u0410\u0420"
};
function rejected4(world, code, message) {
  return { status: "rejected", code, message, state: world };
}
function currentActor(scene) {
  return scene.initiative[scene.activeIndex] ?? "";
}
function validateCommon(world, command) {
  if (world.processedCommandIds.includes(command.commandId)) {
    return rejected4(world, "DuplicateCommand", `Command ${command.commandId} was already committed`);
  }
  if (!/^[A-Za-z0-9._:-]{1,128}$/.test(command.commandId)) {
    return rejected4(world, "InvalidCommandId", "commandId must be a stable 1-128 character identifier");
  }
  if (command.expectedRevision !== world.revision) {
    return rejected4(world, "StaleRevision", `Expected revision ${command.expectedRevision}, got ${world.revision}`);
  }
  if (command.rulesetContentHash !== world.ruleset.contentHash) {
    return rejected4(world, "RulesetMismatch", "Command was created for another ruleset release");
  }
  if (!world.actors[command.actorId]) {
    return rejected4(world, "ActorNotFound", `Unknown actor ${command.actorId}`);
  }
  if (command.type !== "AdjudicateActorDeath" && world.actors[command.actorId].lifecycle?.status === "dead") {
    return rejected4(world, "ActorDead", `Actor ${command.actorId} has been adjudicated dead`);
  }
  return null;
}
function validateTurn(world, command) {
  if (command.type === "ResolveDecision") return null;
  if (command.type === "SwapInitiative") return null;
  if (command.type === "ReleaseGrapple" || command.type === "BreakGrappleRange") return null;
  if (command.type === "ObserveProtectionProximity") return null;
  if (command.type === "ObservePactBladeDistance" || command.type === "AdjudicateActorDeath") return null;
  if (world.scene.mode !== "encounter" || command.type === "StartEncounter") return null;
  if (command.type === "TakeShortRest" || command.type === "TakeLongRest") {
    return rejected4(world, "InvalidActionTiming", "A rest cannot begin while an encounter is active");
  }
  if (currentActor(world.scene) !== command.actorId) {
    return rejected4(world, "NotActorsTurn", `It is not ${command.actorId}'s turn`);
  }
  if (command.type === "StartTurn" && world.scene.turnStarted) {
    return rejected4(world, "TurnAlreadyStarted", "The active turn has already started");
  }
  if (command.type !== "StartTurn" && !world.scene.turnStarted) {
    return rejected4(world, "TurnNotStarted", "Start the active turn before acting");
  }
  return null;
}
function validateResolutionLock(world, command) {
  if (world.pendingResolution && command.type !== "ResolveDecision" && command.type !== "ReleaseGrapple" && command.type !== "BreakGrappleRange") {
    return rejected4(world, "ResolutionInProgress", `Resolution ${world.pendingResolution.id} must be completed first`);
  }
  if (!world.pendingResolution && command.type === "ResolveDecision") {
    return rejected4(world, "NoPendingResolution", "There is no decision to resolve");
  }
  return null;
}
function factsIssue(action, targetId, facts) {
  const targeting = action.targeting;
  if (!targeting) return null;
  if (!facts) return ["MissingSpatialFacts", `Missing spatial facts for target ${targetId}`];
  if (facts.distanceFt < 0 || facts.distanceFt > targeting.rangeFt) {
    return ["OutOfRange", `${targetId} is outside ${targeting.rangeFt} ft range`];
  }
  if (targeting.requiresLineOfSight && (!facts.lineOfSight || facts.cover === "total")) {
    return ["LineOfSightBlocked", `Line of sight to ${targetId} is blocked`];
  }
  if (!targeting.allowedRelations.includes(facts.relation)) {
    return ["IllegalRelation", `${facts.relation} is not a legal relation for ${action.id}`];
  }
  if (targeting.requiresWilling && facts.willing !== true) {
    return ["TargetNotWilling", `${targetId} has not explicitly consented to ${action.id}`];
  }
  if (targeting.requiresStoneworkContact) {
    const issue = stoneworkContactIssue(facts.stonework);
    if (issue) return ["InvalidFacts", issue];
  }
  return null;
}
function actorCard(actor2, cardId) {
  return [
    ...actor2.character.knownCards ?? [],
    ...actor2.character.equippedCards ?? []
  ].find((card) => card.id === cardId);
}
function armorState(actor2) {
  const bodyId = actor2.runtime.equipment.body;
  if (!bodyId) return "unarmored";
  const bodyCard = actorCard(actor2, bodyId);
  if (!bodyCard) return "unknown";
  return isArmorCard(bodyCard) ? "armored" : "unarmored";
}
function actionValidation(world, action, targetIds, factsByTarget, sourceActorId) {
  const targeting = action.targeting;
  if (new Set(targetIds).size !== targetIds.length) {
    return rejected4(world, "InvalidTargets", "Each target actor may appear only once in an action");
  }
  if (targeting && (targetIds.length < targeting.minTargets || targetIds.length > targeting.maxTargets)) {
    return rejected4(world, "InvalidTargets", `${action.id} requires ${targeting.minTargets}-${targeting.maxTargets} targets`);
  }
  if (sourceActorId && targeting?.allowedRelations.length === 1 && targeting.allowedRelations[0] === "self" && targetIds.some((targetId) => targetId !== sourceActorId)) {
    return rejected4(world, "InvalidTargets", `${action.id} can target only its acting actor`);
  }
  if (targetIds.length > 1) {
    if (hasAttackRoll(action)) {
      return rejected4(world, "InvalidTargets", "Multi-target attack sequences require a dedicated attack-sequence primitive");
    }
    if (!hasTargetSave(action) && !hasIndependentTargetAutoEffects(action) && !magicMissileSpec(action)) {
      return rejected4(world, "InvalidTargets", `${action.id} cannot be executed independently for multiple targets`);
    }
  }
  for (const targetId of targetIds) {
    const target2 = world.actors[targetId];
    if (!target2) return rejected4(world, "ActorNotFound", `Unknown target ${targetId}`);
    const issue = factsIssue(action, targetId, factsByTarget?.[targetId]);
    if (issue) return rejected4(world, issue[0], issue[1]);
    const sightIssue = conditionTargetingSightIssue({
      world,
      sourceActorId,
      targetActorId: targetId,
      requiresSight: targeting?.requiresSight === true,
      canSeeTarget: factsByTarget?.[targetId]?.canSeeTarget
    });
    if (sightIssue === "source_cannot_see") {
      return rejected4(world, "CapabilityDenied", `${sourceActorId} cannot see a required target`);
    }
    if (sightIssue === "target_unseen") {
      return rejected4(world, "CapabilityDenied", `${targetId} cannot be seen by ${sourceActorId}`);
    }
    if (targeting?.requiresUnarmored) {
      const state = armorState(target2);
      if (state === "unknown") {
        return rejected4(
          world,
          "InvalidEquipmentState",
          `${targetId} has an unresolved body-slot Card and cannot be proven unarmored`
        );
      }
      if (state === "armored") {
        return rejected4(world, "TargetArmored", `${targetId} is wearing armor`);
      }
    }
  }
  return null;
}
function actionDefinitionIssue(action) {
  const raw = action;
  const interaction = action.mechanics.interaction;
  if (interaction !== void 0) {
    if (!interaction || typeof interaction !== "object" || Array.isArray(interaction) || interaction.intent !== "harmful") {
      return `${action.id} has an invalid interaction intent marker`;
    }
  }
  if (!Array.isArray(action.sourceEntityIds) || action.sourceEntityIds.length === 0 || action.sourceEntityIds.some((id) => typeof id !== "string" || id.trim().length === 0)) {
    return `${action.id} must have at least one stable sourceEntityId`;
  }
  if (new Set(action.sourceEntityIds).size !== action.sourceEntityIds.length) {
    return `${action.id} contains duplicate sourceEntityIds`;
  }
  const worldSpellPolicy = parseWorldSpellPolicy(action.mechanics);
  if (worldSpellPolicy.status === "invalid") {
    return `${action.id} has invalid data-owned primitive policy: ${worldSpellPolicy.issue}`;
  }
  const castTime = parseActivationCastTime(action.mechanics);
  if (castTime.status === "invalid") {
    return `${action.id} has invalid activation cast time: ${castTime.issue}`;
  }
  if (action.targeting) {
    if (action.targeting.requiresWilling != null && typeof action.targeting.requiresWilling !== "boolean") {
      return `${action.id} has an invalid requiresWilling targeting requirement`;
    }
    if (action.targeting.requiresUnarmored != null && typeof action.targeting.requiresUnarmored !== "boolean") {
      return `${action.id} has an invalid requiresUnarmored targeting requirement`;
    }
    if (action.targeting.requiresStoneworkContact !== void 0 && action.targeting.requiresStoneworkContact !== true) {
      return `${action.id} has an invalid requiresStoneworkContact targeting requirement`;
    }
  }
  if (action.attackReplacement) {
    const replacement = action.attackReplacement;
    if (typeof replacement.replacementKey !== "string" || !replacement.replacementKey.trim()) {
      return `${action.id} has an invalid attack-replacement key`;
    }
    if (replacement.replacesAttacks !== 1 || !Number.isInteger(replacement.totalAttacks) || replacement.totalAttacks < 1 || replacement.oncePerAttackAction !== true) {
      return `${action.id} has an invalid attack-replacement policy`;
    }
    const actionCosts = activationCost(action).filter((cost) => String(cost.resource ?? "") === "action");
    if (actionCosts.length !== 1 || Number(actionCosts[0].amount ?? 1) !== 1) {
      return `${action.id} must spend exactly one Action when used as an attack replacement`;
    }
    if (raw.kind !== "nonSpell" || !hasTargetSave(action)) {
      return `${action.id} attack replacement must be a non-spell target-save action`;
    }
  }
  if (raw.kind === "spell") {
    const spell = raw.spell;
    if (!spell) return `${action.id} is a spell but has no spell metadata`;
    if (!Number.isInteger(spell.level) || Number(spell.level) < 0 || Number(spell.level) > 9) {
      return `${action.id} has an invalid canonical spell level`;
    }
    if (spell.components != null) {
      const components = spell.components;
      if (!components || typeof components !== "object" || typeof components.verbal !== "boolean" || typeof components.somatic !== "boolean" || typeof components.material !== "boolean") {
        return `${action.id} has invalid canonical spell components`;
      }
    }
    return null;
  }
  if (raw.kind !== "nonSpell") return `${action.id} has an unknown action kind`;
  if (raw.spell != null) return `${action.id} is non-spell but has spell metadata`;
  return null;
}
function actionDeclaresHarmfulInteraction(action) {
  const interaction = action.mechanics.interaction;
  return Boolean(interaction && typeof interaction === "object" && !Array.isArray(interaction) && interaction.intent === "harmful");
}
function harmfulConditionRejection(input) {
  if (!input.world.actors[input.attackerActorId]) return null;
  for (const targetActorId of input.targetActorIds) {
    if (!input.world.actors[targetActorId]) continue;
    if (conditionInteractionDenied({
      world: input.world,
      actorId: input.attackerActorId,
      targetActorId,
      capability: "harm"
    })) {
      return rejected4(
        input.world,
        "CapabilityDenied",
        `${input.attackerActorId} cannot harm ${targetActorId} in its current state`
      );
    }
  }
  return null;
}
function spellDeclarationIssue(action, declaration) {
  if (action.kind === "nonSpell") {
    return declaration ? `${action.id} is not a spell` : null;
  }
  if (declaration && declaration.baseLevel !== action.spell.level) {
    return `${action.id} has canonical level ${action.spell.level}, not ${declaration.baseLevel}`;
  }
  if (declaration?.sourceClass != null && declaration.sourceClass !== action.spell.sourceClass) {
    return `${action.id} is not a ${declaration.sourceClass} spell`;
  }
  const castLevel = declaration?.castLevel ?? action.spell.level;
  if (!Number.isInteger(castLevel) || castLevel < action.spell.level || castLevel > 9) {
    return `${action.id} cannot be cast at level ${castLevel}`;
  }
  if (action.spell.level === 0 && castLevel !== 0) {
    return `${action.id} is a cantrip and cannot consume a spell slot`;
  }
  return null;
}
function canonicalSpellContext(action, declaration, prepared, audit) {
  if (action.kind !== "spell") return void 0;
  return {
    baseLevel: action.spell.level,
    castLevel: declaration?.castLevel ?? action.spell.level,
    ...action.spell.sourceClass ? { sourceClass: action.spell.sourceClass } : {},
    ...action.spell.components ? { components: { ...action.spell.components } } : {},
    ...prepared ? {
      grantId: prepared.provenance.grantId,
      sourceId: prepared.provenance.sourceId,
      spellcastingAbility: prepared.provenance.spellcastingAbility,
      mode: prepared.provenance.mode,
      payment: { ...prepared.payment }
    } : {},
    ...audit?.castingTimeAddedSeconds !== void 0 ? { castingTimeAddedSeconds: audit.castingTimeAddedSeconds } : {},
    ...audit?.baseCastingTimeSeconds !== void 0 ? { baseCastingTimeSeconds: audit.baseCastingTimeSeconds } : {},
    ...audit?.focusObjectId ? { focusObjectId: audit.focusObjectId } : {},
    ...audit?.focusHand ? { focusHand: audit.focusHand } : {}
  };
}
function actionObligationIds(action, ...systemIds) {
  return [.../* @__PURE__ */ new Set([
    `entity:${action.id}`,
    ...action.sourceEntityIds.map((id) => `entity:${id}`),
    ...systemIds
  ])];
}
function stableSourceEntityIds(value) {
  return Array.isArray(value) && value.length > 0 && value.every((sourceId) => typeof sourceId === "string" && sourceId.length > 0 && sourceId.trim() === sourceId) && new Set(value).size === value.length;
}
function worldActionPrimitive(action) {
  const primitive = action.mechanics.primitive;
  switch (primitive?.type) {
    case "light_world_object":
    case "minor_illusion_world_object":
    case "burning_hands_objects":
    case "area_object_push":
    case "detect_magic_world_sensing":
    case "dancing_lights_world":
    case "druidcraft_world":
    case "mending_world":
    case "prestidigitation_world":
    case "detect_poison_disease_world":
    case "purify_food_drink_world":
    case "temporary_hp_melee_retaliation":
      return primitive.type;
    default:
      return null;
  }
}
function forcedObjectPushPolicy(action) {
  const primitive = action.mechanics.primitive;
  if (primitive?.type !== "area_object_push" || !Number.isFinite(primitive.object_push_distance_ft) || Number(primitive.object_push_distance_ft) <= 0 || !Number.isFinite(primitive.object_max_distance_ft) || Number(primitive.object_max_distance_ft) <= 0 || primitive.object_area_requirement !== "entirely_in_area" || typeof primitive.exclude_secured_objects !== "boolean" || typeof primitive.exclude_carried_objects !== "boolean") return null;
  return {
    distanceFt: Number(primitive.object_push_distance_ft),
    maxObjectDistanceFt: Number(primitive.object_max_distance_ft),
    areaRequirement: "entirely_in_area",
    excludeSecured: primitive.exclude_secured_objects,
    excludeCarried: primitive.exclude_carried_objects
  };
}
function worldObjectFactsIssue(facts) {
  if (!facts || typeof facts !== "object" || Array.isArray(facts)) {
    return "World-object interaction requires explicit object facts";
  }
  const value = facts;
  if (!["scenario", "board", "gm_ruling"].includes(String(value.factsSource ?? ""))) {
    return "World-object facts require a recognized source";
  }
  if (!Number.isInteger(value.boardRevision) || Number(value.boardRevision) < 0) {
    return "World-object facts require a non-negative board revision";
  }
  if (!Number.isFinite(value.distanceFt) || Number(value.distanceFt) < 0 || typeof value.lineOfSight !== "boolean") {
    return "World-object distance and line of sight facts are malformed";
  }
  for (const key of ["inArea", "entirelyInArea", "touched"]) {
    if (value[key] !== void 0 && typeof value[key] !== "boolean") {
      return `World-object ${key} fact must be boolean`;
    }
  }
  return null;
}
function creationTargetingFactsIssue(facts, targeting) {
  const issue = worldObjectFactsIssue(facts);
  if (issue) return issue;
  const value = facts;
  if (value.distanceFt > targeting.rangeFt) {
    return `World creation point is outside declared range ${targeting.rangeFt}`;
  }
  if (targeting.requiresLineOfSight && value.lineOfSight !== true) {
    return "World creation point requires line of sight";
  }
  return null;
}
function worldObjectEvents(sourceActorId, action, mutations, ...extraObligations) {
  const obligationIds = actionObligationIds(
    action,
    "system:world-object",
    ...extraObligations
  );
  return mutations.map((event) => ({
    sourceActorId,
    obligationIds,
    payload: { type: "WorldObjectMutationRecorded", event }
  }));
}
function validateObjectFactsMap(world, value) {
  if (!value || typeof value !== "object" || Array.isArray(value)) {
    return { rejection: rejected4(world, "InvalidFacts", "Area-object facts must be an object map") };
  }
  const factsByObject = value;
  for (const objectId of Object.keys(factsByObject).sort()) {
    if (!world.objects[objectId]) {
      return { rejection: rejected4(world, "WorldObjectNotFound", `Unknown world object ${objectId}`) };
    }
    const issue = worldObjectFactsIssue(factsByObject[objectId]);
    if (issue) return { rejection: rejected4(world, "InvalidFacts", `${objectId}: ${issue}`) };
  }
  return { factsByObject };
}
var CORE_HIDE_ACTION = {
  id: "core.action.hide",
  name: "Hide",
  kind: "nonSpell",
  sourceEntityIds: ["core:dnd5e-2024:action:hide"],
  mechanics: {
    name: "Hide",
    activation: { mode: "active", cost: [{ resource: "action" }] },
    effects: [{
      resolution: "ability_check",
      ability: "dex",
      skill: "stealth",
      dc: "15",
      on_success: [{
        kind: "condition",
        value: "invisible",
        op: "apply",
        // Kept as canonical data even though the current turn engine only
        // interprets the condition itself. Future trigger commands can expire
        // this effect without changing the action/check contract.
        hidden_end_triggers: [
          "noise_above_whisper",
          "enemy_finds_actor",
          "actor_makes_attack_roll",
          "actor_casts_spell_with_verbal_component"
        ]
      }],
      on_fail: []
    }]
  }
};
function systemActionAsRuleDefinition(id, mechanics) {
  const definition = getSystemActionDefinition(id);
  if (!definition) throw new Error(`Missing immutable system action ${id}`);
  return {
    id: definition.id,
    name: definition.name,
    kind: "nonSpell",
    sourceEntityIds: [...definition.sourceEntityIds],
    mechanics
  };
}
var CORE_ATTACK_ACTION = systemActionAsRuleDefinition(SYSTEM_ACTION_IDS.attack, {
  activation: { mode: "active", cost: [{ resource: "action" }] }
});
var CORE_WEAPON_ATTACK = systemActionAsRuleDefinition(SYSTEM_ACTION_IDS.weaponAttack, {
  activation: { mode: "attack_entry", cost: [] },
  effects: [{
    ability: "auto",
    attack_kind: "weapon_melee",
    resolution: "attack_roll",
    vs: "ac",
    on_hit: [{ ability: "auto", dice: "weapon", kind: "damage", type: "weapon" }]
  }]
});
var CORE_LIGHT_WEAPON_EXTRA_ATTACK = systemActionAsRuleDefinition(
  SYSTEM_ACTION_IDS.lightExtraAttack,
  {
    activation: { mode: "active", cost: [{ resource: "bonus_action" }] },
    effects: [{
      ability: "auto",
      attack_kind: "weapon_melee",
      resolution: "attack_roll",
      vs: "ac",
      tags: ["light_property_extra_attack"],
      on_hit: [{ ability: "none", dice: "weapon", kind: "damage", type: "weapon" }]
    }]
  }
);
var CORE_UNARMED_DAMAGE = systemActionAsRuleDefinition(SYSTEM_ACTION_IDS.unarmedDamage, {
  activation: { mode: "attack_entry", cost: [] },
  effects: [{
    ability: "str",
    attack_kind: "unarmed",
    resolution: "attack_roll",
    vs: "ac",
    on_hit: [{ ability: "str", dice: "1", kind: "damage", type: "bludgeoning" }]
  }]
});
var CORE_STUDY_WORLD_OBJECT_ACTION = {
  id: "core.action.study-world-object",
  name: "Study",
  kind: "nonSpell",
  sourceEntityIds: ["core:dnd5e-2024:action:study"],
  mechanics: { activation: { mode: "active", cost: [{ resource: "action" }] } }
};
var CORE_PHYSICAL_WORLD_INTERACTION = {
  id: "core.interaction.physical-world-object",
  name: "Physical interaction",
  kind: "nonSpell",
  sourceEntityIds: ["core:dnd5e-2024:interaction:physical-object"],
  mechanics: { activation: { mode: "active", cost: [] } }
};
var CORE_WORLD_TIME_ACTION = {
  id: "core.system.world-time",
  name: "World time",
  kind: "nonSpell",
  sourceEntityIds: ["core:dnd5e-2024:time:combat-round"],
  mechanics: { activation: { mode: "system", cost: [] } }
};
var ALERT_INITIATIVE_SWAP_CAPABILITY = "alert.initiative_swap";
function hideEligibilityIssue(facts) {
  if (!facts || typeof facts !== "object") return "Hide requires explicit eligibility facts";
  if (!["scenario", "board", "gm_ruling"].includes(facts.factsSource)) {
    return "Hide facts require a recognized source";
  }
  if (!Number.isInteger(facts.boardRevision) || facts.boardRevision < 0) {
    return "Hide facts require a non-negative board revision";
  }
  if (typeof facts.heavilyObscured !== "boolean" || typeof facts.visibleToAnyEnemy !== "boolean" || !["none", "half", "three_quarters", "total"].includes(facts.cover)) {
    return "Hide facts are malformed";
  }
  const hasRequiredObscurement = facts.heavilyObscured || facts.cover === "three_quarters" || facts.cover === "total";
  if (!hasRequiredObscurement) {
    return "Hide requires Heavy Obscurement, Three-Quarters Cover, or Total Cover";
  }
  if (facts.visibleToAnyEnemy) {
    return "Hide is unavailable while any enemy can see the actor";
  }
  return null;
}
function observableFactProvenanceIssue(facts) {
  if (!facts || typeof facts !== "object") return "Observable event requires explicit facts";
  const record9 = facts;
  if (!["scenario", "board", "gm_ruling"].includes(String(record9.factsSource ?? ""))) {
    return "Observable facts require a recognized source";
  }
  if (!Number.isInteger(record9.boardRevision) || Number(record9.boardRevision) < 0) {
    return "Observable facts require a non-negative board revision";
  }
  return null;
}
function noiseFactsIssue(facts) {
  const provenance = observableFactProvenanceIssue(facts);
  if (provenance) return provenance;
  const record9 = facts;
  if (!["whisper_or_quieter", "above_whisper"].includes(String(record9.loudness ?? ""))) {
    return "Noise facts require a canonical loudness";
  }
  return null;
}
function enemyFindingFactsIssue(facts) {
  const provenance = observableFactProvenanceIssue(facts);
  if (provenance) return ["InvalidFacts", provenance];
  const record9 = facts;
  if (record9.relation !== "enemy") {
    return ["IllegalRelation", "Only an enemy finding the actor ends Hide"];
  }
  if (record9.found !== true) {
    return ["InvalidFacts", "FindHiddenActor requires a positive finding fact"];
  }
  return null;
}
function initiativeSwapFactsIssue(facts, allyControllerId) {
  const provenance = observableFactProvenanceIssue(facts);
  if (provenance) return ["InvalidFacts", provenance];
  const record9 = facts;
  if (record9.relation !== "ally") {
    return ["IllegalRelation", "Alert can swap Initiative only with an ally"];
  }
  if (record9.willing !== true || record9.confirmedByControllerId !== allyControllerId) {
    return ["InvalidFacts", "Alert requires explicit consent from the ally controller"];
  }
  return null;
}
function hazardDefinitionIssue(hazard) {
  const raw = hazard;
  if (!/^[A-Za-z0-9._:-]{1,128}$/.test(String(raw.id ?? ""))) return "Hazard id must be stable";
  if (typeof raw.name !== "string" || !raw.name.trim()) return `${String(raw.id ?? "hazard")} must have a name`;
  if (hazard.sourceKind !== "environment" && hazard.sourceKind !== "system") {
    return `${hazard.id} has an invalid source kind`;
  }
  if (!Array.isArray(hazard.sourceEntityIds) || hazard.sourceEntityIds.length === 0 || hazard.sourceEntityIds.some((id) => typeof id !== "string" || id.trim().length === 0)) {
    return `${hazard.id} must have at least one stable sourceEntityId`;
  }
  if (new Set(hazard.sourceEntityIds).size !== hazard.sourceEntityIds.length) {
    return `${hazard.id} contains duplicate sourceEntityIds`;
  }
  const save = raw.save;
  if (!save || !["str", "dex", "con", "int", "wis", "cha"].includes(String(save.ability ?? ""))) {
    return `${hazard.id} has an invalid saving throw ability`;
  }
  if (!Number.isInteger(save.dc) || Number(save.dc) < 1 || Number(save.dc) > 30) {
    return `${hazard.id} has an invalid saving throw DC`;
  }
  if (!Array.isArray(hazard.onFailure) || hazard.onFailure.length === 0) {
    return `${hazard.id} must define at least one failure consequence`;
  }
  if (hazard.onSuccess != null && !Array.isArray(hazard.onSuccess)) {
    return `${hazard.id} has invalid success consequences`;
  }
  return null;
}
function cloneHazard(hazard) {
  return JSON.parse(JSON.stringify(hazard));
}
function hazardSourceId(hazard) {
  return `${hazard.sourceKind}:${hazard.id}`;
}
function hazardObligationIds(hazard) {
  return [.../* @__PURE__ */ new Set([
    `hazard:${hazard.id}`,
    ...hazard.sourceEntityIds.map((id) => `entity:${id}`),
    "system:hazard-save",
    "system:pending-resolution"
  ])];
}
function hazardAvoidedConditions(hazard) {
  return hazard.onFailure.flatMap((payload) => payload.kind === "condition" && payload.value != null ? [String(payload.value)] : []);
}
function actionDeclaredEvent(input) {
  const { actorId, action, targetIds, timing, spell, facts, obligationIds } = input;
  return {
    sourceActorId: actorId,
    obligationIds,
    payload: {
      type: "ActionDeclared",
      actorId,
      actionId: action.id,
      actionKind: action.kind,
      sourceEntityIds: [...action.sourceEntityIds],
      targetIds: [...targetIds],
      timing,
      ...facts ? { facts } : {},
      ...action.kind === "spell" && spell ? {
        spell: {
          baseLevel: spell.baseLevel,
          castLevel: spell.castLevel,
          ...action.spell.sourceClass ? { sourceClass: action.spell.sourceClass } : {},
          ...spell.components ? { components: { ...spell.components } } : {},
          ...spell.grantId ? { grantId: spell.grantId } : {},
          ...spell.sourceId ? { sourceId: spell.sourceId } : {},
          ...spell.spellcastingAbility ? { spellcastingAbility: spell.spellcastingAbility } : {},
          ...spell.mode ? { mode: spell.mode } : {},
          ...spell.payment ? { payment: { ...spell.payment } } : {},
          ...spell.castingTimeAddedSeconds !== void 0 ? { castingTimeAddedSeconds: spell.castingTimeAddedSeconds } : {},
          ...spell.baseCastingTimeSeconds !== void 0 ? { baseCastingTimeSeconds: spell.baseCastingTimeSeconds } : {},
          ...spell.focusObjectId ? { focusObjectId: spell.focusObjectId } : {},
          ...spell.focusHand ? { focusHand: spell.focusHand } : {}
        }
      } : {}
    }
  };
}
function actorContext(actor2) {
  return { ...actor2.character, ...actor2.passives?.length ? { passives: actor2.passives } : {} };
}
function actorFormulaContext(character) {
  return {
    abilityMods: character.abilityMods,
    profBonus: character.profBonus,
    selfLevel: character.level,
    classLevels: character.classLevels,
    spellcastingMod: character.spellcastingMod,
    characterSpeed: character.characterSpeed,
    variables: character.variables
  };
}
function passiveModifierSourceEntityIds(actor2, options) {
  const passiveOnlyRuntime = { ...actor2.runtime, activeEffects: [] };
  return (actor2.passives ?? []).flatMap((passive) => {
    if (!stableSourceEntityIds(passive.sourceEntityIds)) return [];
    const result = collectRollModifiers(passiveOnlyRuntime, [passive], options);
    const contributes = result.modifiers.length > 0 || result.ops.length > 0 || result.rules.length > 0 || result.hasAdvantage || result.hasDisadvantage || result.autoFail || result.denied;
    return contributes ? [...passive.sourceEntityIds] : [];
  });
}
function effectiveArmorClass(actor2, runtime = actor2.runtime) {
  const withoutTransient = { ...runtime, activeEffects: [] };
  const baseline = armorClassValue(actor2.character, withoutTransient, actor2.passives ?? []).value;
  const projected = armorClassValue(actor2.character, runtime, actor2.passives ?? []).value;
  return (actor2.ac ?? baseline) + (projected - baseline);
}
function actionContext(source2, env, target2, targetRuntime = target2?.runtime, facts, spell) {
  const spellcastingAbility = spell?.spellcastingAbility;
  const character = spellcastingAbility ? {
    ...source2.character,
    spellcastingAbility,
    spellcastingMod: source2.character.abilityMods[spellcastingAbility] ?? 0
  } : source2.character;
  return {
    character,
    selfRuntime: source2.runtime,
    selfId: source2.id,
    passives: source2.passives,
    conditionImmunities: source2.traits?.conditionImmunities,
    grantedEffects: source2.grantedEffects,
    masteryEffects: source2.masteryEffects,
    rng: env.rng,
    nextId: env.nextId,
    ...facts?.nearbyEligibleAllyToTarget != null ? {
      attackFacts: { nearbyEligibleAllyToTarget: facts.nearbyEligibleAllyToTarget }
    } : {},
    ...target2 && facts ? {
      // Relational condition clauses consume the same board/GM observations as
      // targeting. The executor receives facts, never a condition-specific UI
      // flag, and fails closed when an observation is absent.
      conditionSourceFacts: {
        [target2.id]: { lineOfSight: facts.lineOfSight }
      },
      conditionRelationFacts: {
        distancesFt: {
          [source2.id]: { [target2.id]: facts.distanceFt },
          [target2.id]: { [source2.id]: facts.distanceFt }
        },
        visibility: {
          [source2.id]: { [target2.id]: facts.canSeeTarget ?? true },
          [target2.id]: { [source2.id]: facts.targetCanSeeSource ?? true }
        }
      }
    } : {},
    ...target2 ? {
      target: {
        id: target2.id,
        ...Number.isInteger(target2.attackProfile?.size) ? { size: target2.attackProfile.size } : {},
        ac: effectiveArmorClass(target2, targetRuntime ?? target2.runtime),
        characterContext: target2.character,
        passives: target2.passives,
        conditionImmunities: target2.traits?.conditionImmunities,
        // A self target shares the source runtime. Leaving runtimeState unset
        // makes the legacy executor route `who:target` payloads into `state`
        // instead of creating a second, conflicting copy of the same actor.
        ...targetRuntime && target2.id !== source2.id ? { runtimeState: targetRuntime } : {}
      }
    } : {}
  };
}
function engineTrace(actorId, targetIds, events, obligationIds, audit) {
  return events.map((event) => ({
    sourceActorId: audit?.sourceActorId ?? actorId,
    obligationIds,
    payload: {
      type: "EngineEventRecorded",
      actorId,
      targetIds,
      event,
      ...audit?.facts ? { facts: audit.facts } : {}
    }
  }));
}
function damageAdjustmentAudit(events) {
  return events.flatMap((event) => event.type === "narrative" && event.damageAdjustment ? [{
    ...event.damageAdjustment,
    sourceEntityIds: [...event.damageAdjustment.sourceEntityIds]
  }] : []);
}
function differs(before, after) {
  return JSON.stringify(before) !== JSON.stringify(after);
}
function runtimePatch(before, after) {
  const patch = {};
  if (differs(before.hp, after.hp)) patch.hp = after.hp;
  if (differs(before.resources, after.resources)) patch.resources = after.resources;
  if (differs(before.maxResources, after.maxResources)) patch.maxResources = after.maxResources;
  if (differs(before.equipment, after.equipment)) patch.equipment = after.equipment;
  if (differs(before.inventory, after.inventory)) patch.inventory = after.inventory;
  if (differs(before.activeEffects, after.activeEffects)) patch.activeEffects = after.activeEffects;
  if (differs(before.firedThisTurn, after.firedThisTurn)) patch.firedThisTurn = after.firedThisTurn ?? null;
  if (differs(before.firedThisRest, after.firedThisRest)) patch.firedThisRest = after.firedThisRest ?? null;
  return patch;
}
function runtimeTransition(sourceActorId, actorId, before, after, reason, obligationIds) {
  const patch = runtimePatch(before, after);
  if (!Object.keys(patch).length) return [];
  return [{
    sourceActorId,
    obligationIds,
    payload: { type: "ActorRuntimePatched", actorId, patch, reason }
  }];
}
function sourceTurnBoundary(world, sourceActorId, boundary) {
  const runtimes = /* @__PURE__ */ new Map();
  const events = [];
  const obligations = ["system:source-turn-expiry", `system:source-turn-${boundary}`];
  const owners = Object.values(world.actors).sort((left, right) => left.id.localeCompare(right.id));
  for (const owner of owners) {
    const expired = [];
    let changed = false;
    const activeEffects = owner.runtime.activeEffects.flatMap((effect) => {
      const lifecycle = effect.sourceTurnExpiry;
      const matches = lifecycle?.sourceActorId === sourceActorId && lifecycle.ownerActorId === owner.id && effect.sourceId === sourceActorId && effect.ownerId === owner.id;
      if (!matches || !lifecycle) return [effect];
      if (boundary === "start" && lifecycle.boundary === "start") {
        changed = true;
        expired.push({ type: "effect_expired", name: effect.name });
        return [];
      }
      if (boundary === "start" && lifecycle.boundary === "end" && lifecycle.armed !== true) {
        changed = true;
        return [{
          ...effect,
          sourceTurnExpiry: { ...lifecycle, armed: true }
        }];
      }
      if (boundary === "end" && lifecycle.boundary === "end" && lifecycle.armed === true) {
        changed = true;
        expired.push({ type: "effect_expired", name: effect.name });
        return [];
      }
      return [effect];
    });
    if (!changed) continue;
    const after = { ...owner.runtime, activeEffects };
    runtimes.set(owner.id, after);
    events.push(...runtimeTransition(
      sourceActorId,
      owner.id,
      owner.runtime,
      after,
      boundary === "start" ? "start_turn" : "end_turn",
      obligations
    ));
    events.push(...engineTrace(owner.id, [owner.id], expired, obligations, {
      sourceActorId,
      facts: { sourceActorId, ownerActorId: owner.id, boundary }
    }));
  }
  return { runtimes, events };
}
function concentrationLinkedEffectIds(before, after) {
  const beforeIds = new Set(before.activeEffects.map((effect) => effect.id));
  return after.activeEffects.flatMap((effect) => {
    if (beforeIds.has(effect.id)) return [];
    const mechanics = effect.mechanics;
    const duration = mechanics.duration;
    return duration?.concentration === true ? [effect.id] : [];
  });
}
function mergeConcentrationEffectLinks(...groups) {
  const unique = /* @__PURE__ */ new Map();
  for (const link of groups.flat()) unique.set(`${link.actorId}\0${link.effectId}`, { ...link });
  return [...unique.values()].sort((left, right) => left.actorId.localeCompare(right.actorId) || left.effectId.localeCompare(right.effectId));
}
function concentrationWorldObjectCleanup(world, concentration, obligations) {
  return Object.values(world.objects).filter((object5) => object5.sourceActorId === concentration.sourceActorId && object5.sourceActionId === concentration.actionId && object5.dancingLight !== void 0).sort((left, right) => left.id.localeCompare(right.id)).map((object5) => ({
    sourceActorId: concentration.sourceActorId,
    obligationIds: [.../* @__PURE__ */ new Set([
      ...obligations,
      "system:world-object",
      "system:dancing-lights",
      "system:concentration"
    ])],
    payload: {
      type: "WorldObjectMutationRecorded",
      event: {
        type: "WorldObjectRemoved",
        objectId: object5.id,
        reason: "concentration_ended"
      }
    }
  }));
}
function consumedConcentrationLifecycle(input) {
  const afterIds = new Set(input.after.activeEffects.map((effect) => effect.id));
  const removedIds = new Set(input.before.activeEffects.filter((effect) => !afterIds.has(effect.id)).map((effect) => effect.id));
  const concentrations = Object.values(input.world.concentrations).filter((concentration) => concentration.effectLinks.some((link) => link.actorId === input.changedActorId && removedIds.has(link.effectId))).sort((left, right) => left.id.localeCompare(right.id));
  if (!concentrations.length) {
    return {
      transitions: runtimeTransition(
        input.actingActorId,
        input.changedActorId,
        input.before,
        input.after,
        "ability_check",
        input.obligations
      ),
      lifecycle: []
    };
  }
  const obligations = [.../* @__PURE__ */ new Set([
    ...input.obligations,
    "system:concentration-effect-consumed"
  ])];
  const runtimes = /* @__PURE__ */ new Map([
    [input.changedActorId, input.after]
  ]);
  const lifecycle = [];
  for (const concentration of concentrations) {
    for (const link of concentration.effectLinks) {
      const linkedActor = input.world.actors[link.actorId];
      if (!linkedActor) continue;
      const current = runtimes.get(link.actorId) ?? linkedActor.runtime;
      const linkedEffect = current.activeEffects.find((effect) => effect.id === link.effectId);
      if (!linkedEffect) continue;
      runtimes.set(link.actorId, {
        ...current,
        activeEffects: current.activeEffects.filter((effect) => effect.id !== link.effectId)
      });
      lifecycle.push(...engineTrace(concentration.sourceActorId, [link.actorId], [{
        type: "effect_expired",
        name: linkedEffect.name
      }], obligations));
    }
    lifecycle.push(...concentrationWorldObjectCleanup(input.world, concentration, obligations), {
      sourceActorId: concentration.sourceActorId,
      obligationIds: obligations,
      payload: {
        type: "ConcentrationCleared",
        sourceActorId: concentration.sourceActorId,
        concentrationId: concentration.id,
        reason: "effect_consumed"
      }
    });
  }
  const transitions = [...runtimes.entries()].flatMap(([actorId, after]) => {
    const before = input.world.actors[actorId]?.runtime;
    return before ? runtimeTransition(
      input.actingActorId,
      actorId,
      before,
      after,
      "ability_check",
      obligations
    ) : [];
  });
  return { transitions, lifecycle };
}
function actionStateEvents(input) {
  const { world, commandId, source: source2, action, target: target2, obligations } = input;
  const targetUpdates = [
    ...target2 && input.targetAfter ? [{ target: target2, targetAfter: input.targetAfter }] : [],
    ...input.targetUpdates ?? []
  ];
  const runtimes = /* @__PURE__ */ new Map([[source2.id, input.sourceAfter]]);
  for (const update of targetUpdates) runtimes.set(update.target.id, update.targetAfter);
  const lifecycleEvents = [];
  if (action.concentration && input.manageConcentration !== false) {
    const old = world.concentrations[source2.id];
    if (old) {
      const expired = [];
      for (const link of old.effectLinks) {
        const actor2 = world.actors[link.actorId];
        if (!actor2) continue;
        const current = runtimes.get(link.actorId) ?? actor2.runtime;
        const removed = current.activeEffects.find((effect) => effect.id === link.effectId);
        if (!removed) continue;
        runtimes.set(link.actorId, {
          ...current,
          activeEffects: current.activeEffects.filter((effect) => effect.id !== link.effectId)
        });
        expired.push({ type: "effect_expired", name: removed.name });
      }
      if (expired.length) lifecycleEvents.push(...engineTrace(source2.id, [], expired, obligations));
      lifecycleEvents.push(
        ...input.skipReplacedConcentrationWorldObjectCleanup ? [] : concentrationWorldObjectCleanup(
          world,
          old,
          [...obligations, "system:concentration-replace"]
        ),
        {
          sourceActorId: source2.id,
          obligationIds: [...obligations, "system:concentration-replace"],
          payload: {
            type: "ConcentrationCleared",
            sourceActorId: source2.id,
            concentrationId: old.id,
            reason: "replaced"
          }
        }
      );
    }
    const candidateLinks = [
      ...input.additionalConcentrationEffectLinks ?? [],
      ...concentrationLinkedEffectIds(source2.runtime, input.sourceAfter).map((effectId) => ({ actorId: source2.id, effectId })),
      ...targetUpdates.flatMap((update) => concentrationLinkedEffectIds(update.target.runtime, update.targetAfter).map((effectId) => ({ actorId: update.target.id, effectId })))
    ];
    const links = [...new Map(candidateLinks.map((link) => [`${link.actorId}\0${link.effectId}`, link])).values()].sort((left, right) => left.actorId.localeCompare(right.actorId) || left.effectId.localeCompare(right.effectId));
    lifecycleEvents.push({
      sourceActorId: source2.id,
      obligationIds: [...obligations, "system:concentration-start"],
      payload: {
        type: "ConcentrationSet",
        concentration: {
          id: `${commandId}:concentration`,
          sourceActorId: source2.id,
          actionId: action.id,
          startedAtRevision: world.revision,
          effectLinks: links
        }
      }
    });
  }
  for (const [actorId, after] of [...runtimes.entries()].sort(([left], [right]) => left.localeCompare(right))) {
    const concentratingActor = world.actors[actorId];
    const concentration = world.concentrations[actorId];
    if (!concentratingActor || !concentration) continue;
    if (!deniedCapabilities(after, concentratingActor.passives ?? []).has("concentration")) continue;
    for (const link of concentration.effectLinks) {
      const linkedActor = world.actors[link.actorId];
      if (!linkedActor) continue;
      const current = runtimes.get(link.actorId) ?? linkedActor.runtime;
      const removed = current.activeEffects.find((effect) => effect.id === link.effectId);
      if (!removed) continue;
      runtimes.set(link.actorId, {
        ...current,
        activeEffects: current.activeEffects.filter((effect) => effect.id !== link.effectId)
      });
      lifecycleEvents.push(...engineTrace(source2.id, [link.actorId], [{
        type: "effect_expired",
        name: removed.name
      }], [...obligations, "system:concentration-incapacitated"]));
    }
    lifecycleEvents.push(...concentrationWorldObjectCleanup(
      world,
      concentration,
      [...obligations, "system:concentration-incapacitated"]
    ), {
      sourceActorId: source2.id,
      obligationIds: [...obligations, "system:concentration-incapacitated"],
      payload: {
        type: "ConcentrationCleared",
        sourceActorId: actorId,
        concentrationId: concentration.id,
        reason: "incapacitated"
      }
    });
  }
  const transitions = [...runtimes.entries()].flatMap(([actorId, after]) => {
    const before = world.actors[actorId]?.runtime;
    return before ? runtimeTransition(source2.id, actorId, before, after, "action", obligations) : [];
  });
  return [...transitions, ...lifecycleEvents];
}
function damageTaken(before, after) {
  const beforeTotal = before.hp.current + before.hp.temp;
  const afterTotal = after.hp.current + after.hp.temp;
  return Math.max(0, beforeTotal - afterTotal);
}
function concentrationSaveFollowUp(input) {
  const { world, actor: actor2, actorAfter, obligations } = input;
  const concentration = world.concentrations[actor2.id];
  if (!concentration || !actorAfter) return null;
  const damage = damageTaken(actor2.runtime, actorAfter);
  if (damage <= 0) return null;
  const dc = Math.min(30, Math.max(10, Math.floor(damage / 2)));
  return {
    type: "concentration_save",
    actorId: actor2.id,
    concentrationId: concentration.id,
    damage,
    dc,
    obligationIds: [.../* @__PURE__ */ new Set([...obligations, "system:concentration-damage-save"])]
  };
}
function concentrationSaveOpenedEvents(input) {
  const { world, actor: actor2, commandId, env } = input;
  const continuation = concentrationSaveFollowUp(input);
  if (!continuation) return [];
  return [{
    sourceActorId: actor2.id,
    obligationIds: continuation.obligationIds,
    payload: {
      type: "ResolutionOpened",
      resolution: {
        id: env.nextId(),
        type: "concentration_save",
        openedByCommandId: commandId,
        openedAtRevision: world.revision,
        deadlineLogicalClock: world.logicalClock + 10,
        actorId: actor2.id,
        concentrationId: continuation.concentrationId,
        damage: continuation.damage,
        request: {
          id: env.nextId(),
          type: "saving_throw",
          actorId: actor2.id,
          ability: "con",
          dc: continuation.dc,
          avoidsConditions: []
        },
        ...input.followUps?.length ? { followUps: input.followUps } : {}
      }
    }
  }];
}
function masterySaveObligationIds(continuation) {
  return [
    `entity:${continuation.actionId}`,
    `entity:${continuation.mastery.sourceEntityId}`,
    "system:weapon-mastery",
    "system:target-save",
    "system:pending-resolution"
  ];
}
function queuedMasterySaves(input) {
  return (input.deferred ?? []).flatMap((offer) => {
    if (offer.source.kind !== "weapon_mastery") return [];
    return [{
      type: "mastery_save",
      sourceActorId: input.sourceActorId,
      targetActorId: input.targetActorId,
      actionId: input.actionId,
      mastery: {
        sourceEntityId: offer.source.entityId,
        name: offer.source.name,
        effect: JSON.parse(JSON.stringify(offer.effect)),
        ...offer.source.weaponMod == null ? {} : { weaponMod: offer.source.weaponMod }
      },
      save: {
        ability: offer.ability,
        dc: offer.dc,
        avoidsConditions: [...offer.avoidsConditions]
      }
    }];
  });
}
function masterySaveOpenedEvent(input) {
  const { world, commandId, continuation, followUps, env } = input;
  const obligations = masterySaveObligationIds(continuation);
  return {
    sourceActorId: continuation.sourceActorId,
    obligationIds: obligations,
    payload: {
      type: "ResolutionOpened",
      resolution: {
        ...continuation,
        id: env.nextId(),
        openedByCommandId: commandId,
        openedAtRevision: world.revision,
        deadlineLogicalClock: world.logicalClock + 10,
        request: {
          id: env.nextId(),
          type: "saving_throw",
          actorId: continuation.targetActorId,
          ability: continuation.save.ability,
          dc: continuation.save.dc,
          avoidsConditions: [...continuation.save.avoidsConditions]
        },
        followUps
      }
    }
  };
}
function followUpOpenedEvents(input) {
  const { world, commandId, env } = input;
  const remaining = [...input.followUps];
  while (remaining.length) {
    const next = remaining.shift();
    if (next.type === "mastery_save") {
      if (!world.actors[next.sourceActorId] || !world.actors[next.targetActorId]) continue;
      return [masterySaveOpenedEvent({ world, commandId, continuation: next, followUps: remaining, env })];
    }
    const concentration = world.concentrations[next.actorId];
    if (!concentration || concentration.id !== next.concentrationId || input.invalidConcentrationIds?.has(next.concentrationId)) continue;
    return [{
      sourceActorId: next.actorId,
      obligationIds: next.obligationIds,
      payload: {
        type: "ResolutionOpened",
        resolution: {
          id: env.nextId(),
          type: "concentration_save",
          openedByCommandId: commandId,
          openedAtRevision: world.revision,
          deadlineLogicalClock: world.logicalClock + 10,
          actorId: next.actorId,
          concentrationId: next.concentrationId,
          damage: next.damage,
          request: {
            id: env.nextId(),
            type: "saving_throw",
            actorId: next.actorId,
            ability: "con",
            dc: next.dc,
            avoidsConditions: []
          },
          ...remaining.length ? { followUps: remaining } : {}
        }
      }
    }];
  }
  return [];
}
function attackFollowUpEvents(input) {
  const masterySaves = queuedMasterySaves({
    deferred: input.deferred,
    sourceActorId: input.source.id,
    targetActorId: input.target.id,
    actionId: input.action.id
  });
  const concentration = concentrationSaveFollowUp({
    world: input.world,
    actor: input.target,
    actorAfter: input.targetAfter,
    obligations: input.obligations
  });
  const sourceConcentration = concentrationSaveFollowUp({
    world: input.world,
    actor: input.source,
    actorAfter: input.sourceAfter,
    obligations: input.obligations
  });
  const queue = [
    ...masterySaves,
    ...concentration ? [concentration] : [],
    ...sourceConcentration ? [sourceConcentration] : []
  ];
  return followUpOpenedEvents({
    world: input.world,
    commandId: input.commandId,
    followUps: queue,
    env: input.env
  });
}
function executeWorldActionPrimitive(world, command, action, env) {
  const primitive = worldActionPrimitive(action);
  if (!primitive) {
    return command.worldInput ? rejected4(world, "InvalidFacts", `${action.id} does not accept world-object input`) : [];
  }
  const parsedPolicy = parseWorldSpellPolicy(action.mechanics);
  if (parsedPolicy.status === "invalid") {
    return rejected4(world, "InvalidActionDefinition", `${action.id}: ${parsedPolicy.issue}`);
  }
  const managedPolicy = parsedPolicy.status === "valid" ? parsedPolicy : null;
  const source2 = world.actors[command.actorId];
  const payable = canPay(source2.runtime, activationCost(action));
  if (!payable.ok) {
    return rejected4(world, "InsufficientResources", `Missing resources: ${payable.missing.join(", ")}`);
  }
  if (primitive === "detect_magic_world_sensing" || primitive === "detect_poison_disease_world") {
    return command.worldInput ? rejected4(
      world,
      "InvalidFacts",
      `${action.name} casting does not accept observation input`
    ) : [];
  }
  try {
    if (primitive === "light_world_object") {
      if (command.worldInput?.type !== "target_object") {
        return rejected4(world, "InvalidFacts", "Light requires one explicit target-object input");
      }
      if (!world.objects[command.worldInput.objectId]) {
        return rejected4(
          world,
          "WorldObjectNotFound",
          `Unknown world object ${command.worldInput.objectId}`
        );
      }
      const issue = worldObjectFactsIssue(command.worldInput.facts);
      if (issue) return rejected4(world, "InvalidFacts", issue);
      const result2 = attachLight({
        objects: world.objects,
        targetObjectId: command.worldInput.objectId,
        facts: command.worldInput.facts,
        sourceActorId: source2.id,
        sourceActionId: action.id,
        attachmentId: env.nextId(),
        policy: managedPolicy.policy,
        targeting: managedPolicy.targeting
      });
      return worldObjectEvents(source2.id, action, result2.events, "system:light-illumination");
    }
    if (primitive === "minor_illusion_world_object") {
      if (command.worldInput?.type !== "minor_illusion") {
        return rejected4(world, "InvalidFacts", "Minor Illusion requires explicit sound or image input");
      }
      if (typeof command.worldInput.description !== "string" || command.worldInput.form !== "sound" && command.worldInput.form !== "image") {
        return rejected4(world, "InvalidFacts", "Minor Illusion input is malformed");
      }
      const factsIssue2 = creationTargetingFactsIssue(
        command.worldInput.facts,
        managedPolicy.targeting
      );
      if (factsIssue2) return rejected4(world, "InvalidFacts", factsIssue2);
      const result2 = createMinorIllusion({
        objects: world.objects,
        id: env.nextId(),
        sourceActorId: source2.id,
        sourceActionId: action.id,
        form: command.worldInput.form,
        description: command.worldInput.description,
        spellSaveDc: 8 + source2.character.profBonus + (command.spell?.spellcastingAbility ? source2.character.abilityMods[command.spell.spellcastingAbility] ?? 0 : source2.character.spellcastingMod ?? 0),
        ...command.worldInput.imageCubeSideFt === void 0 ? {} : { imageCubeSideFt: command.worldInput.imageCubeSideFt },
        policy: managedPolicy.policy
      });
      return worldObjectEvents(source2.id, action, result2.events, "system:minor-illusion");
    }
    if (primitive === "dancing_lights_world") {
      if (command.worldInput?.type !== "dancing_lights" || !Array.isArray(command.worldInput.placements) || command.worldInput.form !== "individual" && command.worldInput.form !== "medium_humanoid") {
        return rejected4(world, "InvalidFacts", "Dancing Lights requires a canonical placement snapshot");
      }
      const factsIssue2 = creationTargetingFactsIssue(
        command.worldInput.facts,
        managedPolicy.targeting
      );
      if (factsIssue2) return rejected4(world, "InvalidFacts", factsIssue2);
      const groupId = env.nextId();
      const result2 = createDancingLights({
        objects: world.objects,
        groupId,
        sourceActorId: source2.id,
        sourceActionId: action.id,
        form: command.worldInput.form,
        placements: command.worldInput.placements.map((placement) => ({
          id: env.nextId(),
          distanceFromCasterFt: placement.distanceFromCasterFt,
          ...placement.withinRequiredSeparation === void 0 ? {} : { withinRequiredSeparation: placement.withinRequiredSeparation }
        })),
        policy: managedPolicy.policy,
        targeting: managedPolicy.targeting
      });
      return worldObjectEvents(
        source2.id,
        action,
        result2.events,
        "system:dancing-lights",
        "system:concentration"
      );
    }
    if (primitive === "druidcraft_world") {
      if (command.worldInput?.type !== "druidcraft" || !command.worldInput.option || typeof command.worldInput.option !== "object") {
        return rejected4(world, "InvalidFacts", "Druidcraft requires one canonical option");
      }
      const selected = command.worldInput.option;
      let option;
      switch (selected.kind) {
        case "weather_sensor":
          option = { ...selected, id: env.nextId() };
          break;
        case "sensory_effect":
          option = { ...selected, id: env.nextId() };
          break;
        case "bloom":
        case "fire_play":
          option = selected;
          break;
        default:
          return rejected4(world, "InvalidFacts", "Unknown Druidcraft option");
      }
      const result2 = resolveDruidcraft({
        objects: world.objects,
        sourceActorId: source2.id,
        sourceActionId: action.id,
        option,
        policy: managedPolicy.policy,
        targeting: managedPolicy.targeting
      });
      return worldObjectEvents(source2.id, action, result2.events, "system:druidcraft");
    }
    if (primitive === "mending_world") {
      if (command.worldInput?.type !== "mending") {
        return rejected4(world, "InvalidFacts", "Mending requires one touched object");
      }
      if (!world.objects[command.worldInput.objectId]) {
        return rejected4(
          world,
          "WorldObjectNotFound",
          `Unknown world object ${command.worldInput.objectId}`
        );
      }
      const result2 = mendWorldObject({
        objects: world.objects,
        objectId: command.worldInput.objectId,
        facts: command.worldInput.facts,
        policy: managedPolicy.policy,
        targeting: managedPolicy.targeting
      });
      return worldObjectEvents(source2.id, action, result2.events, "system:mending");
    }
    if (primitive === "prestidigitation_world") {
      if (command.worldInput?.type !== "prestidigitation" || !command.worldInput.option || typeof command.worldInput.option !== "object") {
        return rejected4(world, "InvalidFacts", "Prestidigitation requires one canonical option");
      }
      const selected = command.worldInput.option;
      let option;
      switch (selected.kind) {
        case "sensory_effect":
          option = { ...selected, id: env.nextId() };
          break;
        case "minor_sensation":
        case "magic_mark":
          option = { ...selected, id: env.nextId() };
          break;
        case "minor_creation":
          option = { ...selected, id: env.nextId() };
          break;
        case "fire_play":
        case "clean_or_soil":
          option = selected;
          break;
        default:
          return rejected4(world, "InvalidFacts", "Unknown Prestidigitation option");
      }
      const result2 = resolvePrestidigitation({
        objects: world.objects,
        sourceActorId: source2.id,
        sourceActionId: action.id,
        option,
        policy: managedPolicy.policy,
        targeting: managedPolicy.targeting
      });
      return worldObjectEvents(source2.id, action, result2.events, "system:prestidigitation");
    }
    if (primitive === "purify_food_drink_world") {
      if (command.worldInput?.type !== "purify_food_drink") {
        return rejected4(
          world,
          "InvalidFacts",
          "Purify Food and Drink requires an explicit sphere and object snapshot"
        );
      }
      const validated2 = validateObjectFactsMap(world, command.worldInput.factsByObject);
      if ("rejection" in validated2) return validated2.rejection;
      const result2 = purifyFoodAndDrink({
        objects: world.objects,
        sphereCenterDistanceFt: command.worldInput.sphereCenterDistanceFt,
        factsByObject: validated2.factsByObject,
        policy: managedPolicy.policy,
        targeting: managedPolicy.targeting
      });
      return worldObjectEvents(source2.id, action, result2.events, "system:purify-food-drink");
    }
    if (primitive === "temporary_hp_melee_retaliation") {
      if (action.kind !== "spell" || !action.spell) {
        return rejected4(
          world,
          "InvalidActionDefinition",
          `${action.id} declares slot-scaled retaliation without a spell definition`
        );
      }
      const armorPolicy = temporaryHpMeleeRetaliationPolicyFromMechanics(
        action.mechanics.primitive
      );
      if (!armorPolicy) {
        return rejected4(world, "InvalidActionDefinition", `${action.id} has invalid slot-retaliation metadata`);
      }
      const choice = command.choices?.temporary_hp;
      if (choice !== "take_spell" && choice !== "keep_current") {
        return rejected4(
          world,
          "InvalidDecision",
          "Temporary HP retaliation requires temporary_hp = take_spell or keep_current"
        );
      }
      if (!command.spell?.grantId || !command.spell.sourceId || !command.spell.payment || command.spell.payment.kind === "none") {
        return rejected4(
          world,
          "InvalidSpellDeclaration",
          "Temporary HP retaliation requires an exact source-scoped paid spell grant"
        );
      }
      const paidSlotLevel = command.spell.payment.resource?.match(/_(\d+)$/)?.[1];
      if (command.spell.payment.kind === "slot" && Number(paidSlotLevel) !== command.spell.castLevel) {
        return rejected4(
          world,
          "InvalidSpellDeclaration",
          "Retaliation cast level must match its paid slot level"
        );
      }
      if (command.spell.payment.kind === "free_use" && command.spell.castLevel !== action.spell.level) {
        return rejected4(
          world,
          "InvalidSpellDeclaration",
          "A free retaliation use cannot be upcast without a higher-level slot"
        );
      }
      const effect = createArmorOfAgathysEffect({
        id: env.nextId(),
        actorId: source2.id,
        actionId: action.id,
        name: action.name,
        slotLevel: command.spell.castLevel,
        policy: armorPolicy,
        sourceEntityIds: action.sourceEntityIds
      });
      const after = applyArmorOfAgathysCast({
        state: source2.runtime,
        effect,
        temporaryHpChoice: choice
      });
      const obligations = actionObligationIds(
        action,
        "system:temporary-hp-melee-retaliation",
        "system:temporary-hit-points",
        "system:source-turn-expiry"
      );
      return runtimeTransition(source2.id, source2.id, source2.runtime, after, "action", obligations);
    }
    if (command.worldInput?.type !== "area_objects") {
      return rejected4(world, "InvalidFacts", `${action.id} requires an explicit area-object snapshot`);
    }
    const validated = validateObjectFactsMap(world, command.worldInput.factsByObject);
    if ("rejection" in validated) return validated.rejection;
    const objectPushPolicy = primitive === "area_object_push" ? forcedObjectPushPolicy(action) : null;
    if (primitive === "area_object_push" && !objectPushPolicy) {
      return rejected4(world, "InvalidActionDefinition", `${action.id} has invalid object-push metadata`);
    }
    const result = primitive === "burning_hands_objects" ? igniteBurningHandsObjects({
      objects: world.objects,
      factsByObject: validated.factsByObject,
      policy: managedPolicy.policy,
      targeting: managedPolicy.targeting
    }) : pushWorldObjects({
      objects: world.objects,
      factsByObject: validated.factsByObject,
      policy: objectPushPolicy
    });
    return worldObjectEvents(
      source2.id,
      action,
      result.events,
      primitive === "burning_hands_objects" ? "system:environmental-object-ignition" : "system:environmental-object-push"
    );
  } catch (error) {
    return rejected4(
      world,
      "InvalidFacts",
      error instanceof Error ? error.message : "Invalid world-object interaction"
    );
  }
}
function executeUseAction(world, command, action, env, options = {}) {
  const source2 = world.actors[command.actorId];
  const targets = command.targetIds.map((targetId) => world.actors[targetId]);
  const executionTargets = targets.length ? targets : [void 0];
  let sourceAfter = source2.runtime;
  const executions = [];
  for (const [index, target2] of executionTargets.entries()) {
    const sourceAtStep = { ...source2, runtime: sourceAfter };
    const result = executeAction(
      sourceAfter,
      index === 0 ? action.mechanics : withoutActivationCost(action.mechanics),
      {
        ...actionContext(
          sourceAtStep,
          env,
          target2,
          target2?.runtime,
          target2 ? command.factsByTarget?.[target2.id] : void 0,
          command.spell
        ),
        choices: command.choices,
        spell: command.spell,
        suppressSpellCastEvent: index > 0,
        deferTargetSaves: true,
        ...options.externalPrimitiveHandled ? { externalPrimitiveHandled: true } : {}
      }
    );
    const armor = target2 ? resolveTemporaryHpMeleeRetaliationAfterAttack({
      world,
      attacker: sourceAtStep,
      defender: target2,
      attackerAfter: result.state,
      defenderAfter: result.targetState,
      action,
      attackEvents: result.events,
      env
    }) : {
      attackerAfter: result.state,
      defenderAfter: result.targetState,
      retaliationEvents: [],
      retaliationSourceEntityIds: []
    };
    sourceAfter = armor.attackerAfter;
    executions.push({
      target: target2,
      result: {
        ...result,
        state: armor.attackerAfter,
        ...armor.defenderAfter ? { targetState: armor.defenderAfter } : {}
      },
      retaliationEvents: armor.retaliationEvents,
      retaliationSourceEntityIds: armor.retaliationSourceEntityIds
    });
  }
  const deferredTargetSaves = executions.flatMap(({ result }) => result.deferredTargetSaves ?? []);
  const obligationIds = actionObligationIds(
    action,
    hasAttackRoll(action) ? "system:attack-resolution" : "system:action-resolution",
    ...deferredTargetSaves.length ? ["system:pending-resolution"] : [],
    ...executions.some(({ retaliationEvents }) => retaliationEvents.length) ? ["system:temporary-hp-melee-retaliation", "system:retaliation"] : [],
    ...executions.flatMap(({ retaliationSourceEntityIds }) => retaliationSourceEntityIds.map((sourceId) => `entity:${sourceId}`))
  );
  const events = actionStateEvents({
    world,
    commandId: command.commandId,
    source: source2,
    action,
    sourceAfter,
    targetUpdates: executions.flatMap(({ target: target2, result }) => target2 && result.targetState ? [{ target: target2, targetAfter: result.targetState }] : []),
    obligations: obligationIds,
    ...options
  });
  for (const { target: target2, result, retaliationEvents } of executions) {
    events.push(...engineTrace(source2.id, target2 ? [target2.id] : [], result.events, obligationIds));
    if (target2 && retaliationEvents.length) {
      events.push(...engineTrace(
        target2.id,
        [source2.id],
        retaliationEvents,
        obligationIds,
        { sourceActorId: target2.id, facts: { trigger: "temporary_hp_melee_retaliation" } }
      ));
    }
  }
  const followUps = executions.flatMap(({ target: target2, result }) => {
    if (!target2) return [];
    const targetAfter = target2.id === source2.id ? result.state : result.targetState;
    const masterySaves = queuedMasterySaves({
      deferred: result.deferredTargetSaves,
      sourceActorId: source2.id,
      targetActorId: target2.id,
      actionId: action.id
    });
    const concentration = concentrationSaveFollowUp({
      world,
      actor: target2,
      actorAfter: targetAfter,
      obligations: obligationIds
    });
    return [...masterySaves, ...concentration ? [concentration] : []];
  });
  if (executions.some(({ retaliationEvents }) => retaliationEvents.length)) {
    const attackerConcentration = concentrationSaveFollowUp({
      world,
      actor: source2,
      actorAfter: sourceAfter,
      obligations: obligationIds
    });
    if (attackerConcentration) followUps.push(attackerConcentration);
  }
  events.push(...followUpOpenedEvents({
    world,
    commandId: command.commandId,
    followUps,
    env
  }));
  return events;
}
function activationCost(action) {
  const activation = action.mechanics.activation;
  return Array.isArray(activation?.cost) ? activation.cost : [];
}
function activationMode(action) {
  const activation = action.mechanics.activation;
  return String(activation?.mode ?? "active");
}
function reactionTriggers(action) {
  const activation = action.mechanics.activation;
  const trigger = activation?.trigger;
  if (activationMode(action) !== "reaction") return [];
  const declared = [
    ...typeof trigger?.event === "string" ? [trigger.event] : [],
    ...Array.isArray(trigger?.events) ? trigger.events.map(String) : []
  ].filter(Boolean);
  return [...new Set(declared)];
}
function hasReactionTrigger(action, trigger) {
  return reactionTriggers(action).includes(trigger);
}
function requiredActionCapability(action) {
  if (activationMode(action) === "reaction") return "reaction";
  const resources = activationCost(action).map((cost) => String(cost.resource ?? ""));
  return resources.includes("bonus_action") ? "bonus_action" : "action";
}
function withoutActivationCost(mechanics) {
  const activation = mechanics.activation;
  return {
    ...mechanics,
    activation: { ...activation ?? { mode: "active" }, cost: [] }
  };
}
function withoutActionResourceCost(mechanics) {
  const activation = mechanics.activation;
  const costs = Array.isArray(activation?.cost) ? activation.cost : [];
  return {
    ...mechanics,
    activation: {
      ...activation ?? { mode: "attack_entry" },
      mode: "attack_entry",
      cost: costs.filter((cost) => String(cost.resource ?? "") !== "action")
    }
  };
}
function hasAttackRoll(action) {
  const effects = action.mechanics.effects;
  return Array.isArray(effects) && effects.some((effect) => typeof effect === "object" && effect !== null && String(effect.resolution ?? "") === "attack_roll");
}
function hasTargetSave(action) {
  const effects = action.mechanics.effects;
  return Array.isArray(effects) && effects.some((effect) => typeof effect === "object" && effect !== null && String(effect.resolution ?? "") === "save" && String(effect.who ?? "target") === "target");
}
function attackSequenceObligationIds(sequence) {
  return sequence ? ["system:attack-action", "system:attack-replacement"] : [];
}
function attackSequenceContinuationIssue(world, sequence, pending, action) {
  if (pending.attackActionId) {
    const ledger = world.attackActions[pending.attackActionId];
    const replacement2 = action.attackReplacement;
    if (!ledger || !replacement2 || ledger.actorId !== pending.sourceActorId || ledger.status !== "open" || ledger.blockedByResolutionId !== pending.id) {
      return "Attack replacement lost its active canonical Attack-action ledger";
    }
    const matchingEntries2 = ledger.sequence.entries.filter((entry) => entry.kind === "replacement" && entry.actionId === pending.actionId && entry.replacementKey === replacement2.replacementKey);
    if (matchingEntries2.length !== 1 || !ledger.sequence.usedReplacementKeys.includes(replacement2.replacementKey)) {
      return "Attack-action ledger does not contain exactly one canonical replacement entry";
    }
    const sources2 = [...matchingEntries2[0].sourceEntityIds].sort();
    if (JSON.stringify(sources2) !== JSON.stringify([...action.sourceEntityIds].sort())) {
      return "Attack replacement provenance disagrees with the compiled action";
    }
    return null;
  }
  if (!sequence) return null;
  const replacement = action.attackReplacement;
  if (!replacement) return `${action.id} no longer defines the persisted attack replacement`;
  if (sequence.actorId !== pending.sourceActorId) {
    return "Attack sequence source actor disagrees with the saving-throw continuation";
  }
  if (sequence.totalAttacks !== replacement.totalAttacks || !attackSequenceComplete(sequence)) {
    return "Attack sequence budget is incomplete or disagrees with the compiled action";
  }
  const matchingEntries = sequence.entries.filter((entry) => entry.kind === "replacement" && entry.actionId === pending.actionId && entry.replacementKey === replacement.replacementKey);
  if (matchingEntries.length !== 1 || !sequence.usedReplacementKeys.includes(replacement.replacementKey)) {
    return "Attack sequence does not contain exactly one canonical replacement entry";
  }
  const sources = [...matchingEntries[0].sourceEntityIds].sort();
  if (JSON.stringify(sources) !== JSON.stringify([...action.sourceEntityIds].sort())) {
    return "Attack replacement provenance disagrees with the compiled action";
  }
  return null;
}
function hasIndependentTargetAutoEffects(action) {
  const effects = action.mechanics.effects;
  return Array.isArray(effects) && effects.length > 0 && effects.every((effect) => {
    if (typeof effect !== "object" || effect === null) return false;
    const value = effect;
    return String(value.resolution ?? "") === "auto" && String(value.who ?? "target") === "target";
  });
}
function magicMissileSpec(action) {
  const parsed = parseWorldSpellPolicy(action.mechanics);
  if (parsed.status !== "valid" || parsed.primitiveType !== "magic_missile") return null;
  return parsed.policy;
}
function magicMissileAllocation(command, spec) {
  const dartCount = command.spell ? magicMissileDartCount(spec, command.spell.castLevel) : null;
  if (dartCount === null) return { issue: "Magic Missile cast level is outside its declared policy" };
  const selected = command.choices?.[spec.allocationChoiceId];
  if (!Array.isArray(selected) || selected.length !== dartCount || selected.some((targetId) => typeof targetId !== "string" || !targetId)) {
    return { issue: `${spec.allocationChoiceId} must assign exactly ${dartCount} dart target ids` };
  }
  const uniqueInOrder = selected.filter((targetId, index) => selected.indexOf(targetId) === index);
  if (uniqueInOrder.length !== command.targetIds.length || uniqueInOrder.some((targetId, index) => command.targetIds[index] !== targetId)) {
    return { issue: "targetIds must equal the unique dart targets in first-occurrence order" };
  }
  return { dartTargetIds: [...selected], dartCount };
}
function hasMagicMissileImmunity(actor2) {
  return actor2.runtime.activeEffects.some((effect) => effect.mechanics.magic_missile_immunity === true);
}
function grantsMagicMissileImmunity(action) {
  const effects = action.mechanics.effects;
  return Array.isArray(effects) && effects.some((effect) => {
    if (!effect || typeof effect !== "object") return false;
    const interaction = effect;
    const payloads = interaction.result ?? interaction.results;
    return Array.isArray(payloads) && payloads.some((payload) => payload != null && typeof payload === "object" && payload.magic_missile_immunity === true);
  });
}
function attackRollFrom(events) {
  const event = events.find((candidate) => candidate.type === "roll" && candidate.roll.kind === "d20" && candidate.roll.target?.type === "ac");
  return event?.roll ?? null;
}
function isMeleeAttackRollAction(action) {
  const effects = Array.isArray(action.mechanics.effects) ? action.mechanics.effects : [];
  return effects.some((effect) => {
    if (effect.resolution !== "attack_roll") return false;
    const kind = String(effect.attack_kind ?? "");
    return kind === "melee" || kind === "unarmed" || kind === "weapon_melee" || kind === "spell_melee";
  });
}
function resolveTemporaryHpMeleeRetaliationAfterAttack(input) {
  const roll = attackRollFrom(input.attackEvents);
  const defenderAfterHit = input.defenderAfter ? endArmorOfAgathysWithoutTemporaryHp(input.defenderAfter) : void 0;
  const retaliations = temporaryHpMeleeRetaliations({
    effects: input.defender.runtime.activeEffects,
    facts: {
      defenderActorId: input.defender.id,
      attackerActorId: input.attacker.id,
      hit: roll?.outcome === "hit" || roll?.outcome === "crit",
      attackRollKind: isMeleeAttackRollAction(input.action) ? "melee" : "ranged",
      temporaryHpBeforeHit: input.defender.runtime.hp.temp
    }
  });
  if (retaliations.length === 0) {
    return {
      attackerAfter: endArmorOfAgathysWithoutTemporaryHp(input.attackerAfter),
      defenderAfter: defenderAfterHit,
      retaliationEvents: [],
      retaliationSourceEntityIds: []
    };
  }
  let attackerAfter = input.attackerAfter;
  const retaliationEvents = [];
  const retaliationSourceEntityIds = /* @__PURE__ */ new Set();
  for (const retaliation of retaliations) {
    const attackerAtStep = { ...input.attacker, runtime: attackerAfter };
    const damage = applyIncomingDamage(
      attackerAfter,
      retaliation.amount,
      actionContext(attackerAtStep, input.env),
      { damageType: retaliation.damageType }
    );
    attackerAfter = damage.state;
    retaliationEvents.push(...damage.events);
    retaliation.sourceEntityIds.forEach((sourceId) => retaliationSourceEntityIds.add(sourceId));
  }
  return {
    attackerAfter: endArmorOfAgathysWithoutTemporaryHp(attackerAfter),
    defenderAfter: defenderAfterHit,
    retaliationEvents,
    retaliationSourceEntityIds: [...retaliationSourceEntityIds].sort()
  };
}
function relabelAttackRolls(events, label) {
  return events.map((event) => event.type === "roll" && event.roll.kind === "d20" && event.roll.target?.type === "ac" ? { ...event, label } : event);
}
function withoutAttackRoll(events) {
  let skipped = false;
  return events.filter((event) => {
    if (!skipped && event.type === "roll" && event.roll.kind === "d20" && event.roll.target?.type === "ac") {
      skipped = true;
      return false;
    }
    return true;
  });
}
function prepareReactionExecution(actor2, action, declaration) {
  if (action.kind === "nonSpell") {
    if (declaration) {
      return {
        status: "rejected",
        code: "InvalidSpellDeclaration",
        message: `${action.id} is not a spell and cannot accept a spell-source declaration`
      };
    }
    return { status: "ready", action };
  }
  if (!actor2.spellcastingAccess) {
    if (declaration) {
      return {
        status: "rejected",
        code: "InvalidSpellDeclaration",
        message: `${actor2.id} has no source-scoped spell access for ${action.id}`
      };
    }
    return { status: "ready", action, spell: canonicalSpellContext(action) };
  }
  const preparation = prepareSpellExecution({
    action,
    accessState: actor2.spellcastingAccess,
    resources: actor2.runtime.resources,
    declaration
  });
  if (preparation.status === "rejected") {
    return {
      status: "rejected",
      code: preparation.stage === "action_definition" ? "InvalidActionDefinition" : preparation.code === "SpellResourceUnavailable" ? "InsufficientResources" : "InvalidSpellDeclaration",
      message: preparation.message
    };
  }
  if (preparation.provenance.mode === "ritual") {
    return {
      status: "rejected",
      code: "InvalidActionTiming",
      message: "A ritual cannot be cast as a reaction"
    };
  }
  return {
    status: "ready",
    action: preparation.executableAction,
    spell: canonicalSpellContext(preparation.executableAction, void 0, preparation)
  };
}
function sourceScopedReactionOptions(target2, action) {
  if (action.kind !== "spell" || !target2.spellcastingAccess) {
    const execution = prepareReactionExecution(target2, action);
    if (execution.status === "rejected" || !canPay(target2.runtime, activationCost(execution.action)).ok) return [];
    return [{ actionId: action.id, label: action.name }];
  }
  const grantIds = [...new Set(target2.spellcastingAccess.grants.filter((grant) => grant.actionId === action.id).map((grant) => grant.grantId))];
  const spellSources = grantIds.flatMap((grantId) => {
    const execution = prepareSpellExecution({
      action,
      accessState: target2.spellcastingAccess,
      resources: target2.runtime.resources,
      declaration: { grantId }
    });
    if (execution.status === "rejected" || execution.provenance.mode === "ritual" || !canPay(target2.runtime, activationCost(execution.executableAction)).ok) return [];
    return [{
      grantId: execution.provenance.grantId,
      sourceId: execution.provenance.sourceId,
      spellcastingAbility: execution.provenance.spellcastingAbility,
      payment: { ...execution.payment }
    }];
  });
  return spellSources.length ? [{ actionId: action.id, label: action.name, spellSources }] : [];
}
function cloneReactionOption(option) {
  return {
    ...option,
    ...option.spellSources ? {
      spellSources: option.spellSources.map((source2) => ({
        ...source2,
        payment: { ...source2.payment }
      }))
    } : {}
  };
}
function hitReactionOptions(target2, catalog) {
  if (deniedCapabilities(target2.runtime, target2.passives ?? []).has("reaction")) return [];
  return target2.capabilities.actionIds.flatMap((actionId) => {
    const action = catalog.getAction(actionId);
    if (!action || !hasReactionTrigger(action, "hit_by_attack")) return [];
    const [option] = sourceScopedReactionOptions(target2, action);
    return option ? [{ action, option }] : [];
  });
}
function protectionCapabilitySource(actor2) {
  const sourceEntityIds = actor2.capabilities.featureSources?.[PROTECTION_2024_CAPABILITY_ID];
  if (!sourceEntityIds) return null;
  const source2 = {
    ownerActorId: actor2.id,
    capabilityId: PROTECTION_2024_CAPABILITY_ID,
    sourceEntityIds: [...sourceEntityIds]
  };
  return protection2024SourceIssue(source2, actor2.id) ? null : source2;
}
function protectionCandidateShapeIssue(candidate) {
  if (!candidate || typeof candidate !== "object" || !["scenario", "board", "gm_ruling"].includes(candidate.factsSource) || !Number.isInteger(candidate.boardRevision) || candidate.boardRevision < 0 || typeof candidate.protectorActorId !== "string" || !candidate.protectorActorId || candidate.protectorActorId.trim() !== candidate.protectorActorId || typeof candidate.protectorCanSeeAttacker !== "boolean" || !Number.isFinite(candidate.protectorDistanceToTargetFt) || candidate.protectorDistanceToTargetFt < 0) {
    return "Protection requires a complete authoritative geometry and visibility observation";
  }
  return null;
}
function protectionCandidatesForAttack(input) {
  const owners = Object.values(input.world.actors).filter((actor2) => actor2.capabilities.featureSources?.[PROTECTION_2024_CAPABILITY_ID] !== void 0).sort((left, right) => left.id.localeCompare(right.id));
  const candidates = input.command.protectionCandidates ?? [];
  if (candidates.some((candidate) => protectionCandidateShapeIssue(candidate))) {
    return rejected4(input.world, "InvalidFacts", "Protection candidate facts are malformed");
  }
  const candidateIds = candidates.map((candidate) => candidate.protectorActorId);
  if (new Set(candidateIds).size !== candidateIds.length || candidates.some((candidate) => candidate.boardRevision !== input.facts.boardRevision) || JSON.stringify([...candidateIds].sort()) !== JSON.stringify(owners.map((owner) => owner.id))) {
    return rejected4(
      input.world,
      "InvalidFacts",
      "An attack must provide one current Protection observation for every source-owned protector"
    );
  }
  const queued = [];
  for (const owner of owners) {
    const source2 = protectionCapabilitySource(owner);
    if (!source2) {
      return rejected4(input.world, "InvalidActionDefinition", `${owner.id} has forged Protection provenance`);
    }
    const candidate = candidates.find((value) => value.protectorActorId === owner.id);
    const facts = {
      factsSource: candidate.factsSource,
      worldRevision: input.world.revision,
      attackId: input.command.commandId,
      protectorActorId: owner.id,
      attackerActorId: input.source.id,
      targetActorId: input.target.id,
      attackRollStage: "before_roll",
      protectorCanSeeAttacker: candidate.protectorCanSeeAttacker,
      protectorHoldingShield: actorHoldsCanonicalShield(owner),
      protectorReactionAvailable: (owner.runtime.resources.reaction ?? 0) >= 1 && !deniedCapabilities(owner.runtime, owner.passives ?? []).has("reaction"),
      protectorDistanceToTargetFt: candidate.protectorDistanceToTargetFt
    };
    if (getProtection2024Eligibility(facts).eligible) queued.push({ protectorActorId: owner.id, facts });
  }
  return {
    candidates: candidates.map((candidate) => ({ ...candidate })),
    queued
  };
}
function protectionContinuationKind(source2, action) {
  if (action.id === SYSTEM_ACTION_IDS.weaponAttack) {
    const effects = Array.isArray(action.mechanics.effects) ? action.mechanics.effects : [];
    return effects.some((effect) => String(effect.attack_kind).includes("ranged")) ? "weapon_ranged" : "weapon_melee";
  }
  if (action.id === SYSTEM_ACTION_IDS.unarmedDamage) return "unarmed_damage";
  if (familiarAttackRuleAction(source2, action.id)) return "familiar_attack";
  return "catalog";
}
function protectionOpenedEvent(input) {
  const resolutionId = input.env.nextId();
  const requestId = input.env.nextId();
  const continuationKind = input.options.continuationKind ?? protectionContinuationKind(input.world.actors[input.command.actorId], input.action);
  return {
    sourceActorId: input.command.actorId,
    obligationIds: input.obligations,
    payload: {
      type: "ResolutionOpened",
      resolution: {
        id: resolutionId,
        type: "protection_reaction",
        openedByCommandId: input.command.commandId,
        openedAtRevision: input.world.revision,
        deadlineLogicalClock: input.world.logicalClock + 10,
        sourceActorId: input.command.actorId,
        targetActorId: input.command.targetIds[0],
        actionId: input.action.id,
        facts: { ...input.command.factsByTarget[input.command.targetIds[0]] },
        choices: input.command.choices,
        spell: input.command.spell,
        ...input.options.attackActionId ? { attackActionId: input.options.attackActionId } : {},
        attackContinuationKind: continuationKind,
        ...input.options.weaponHand ? { weaponHand: input.options.weaponHand } : {},
        ...input.options.weaponCardId ? { weaponCardId: input.options.weaponCardId } : {},
        ...input.options.pactBladeProjection ? { pactBladeProjection: { ...input.options.pactBladeProjection } } : {},
        preRollDisadvantageReasons: [...input.options.preRollDisadvantageReasons ?? []],
        protectionCandidates: input.candidates.map((candidate) => ({ ...candidate })),
        remainingReactions: input.remaining.map((queued) => ({
          protectorActorId: queued.protectorActorId,
          facts: { ...queued.facts }
        })),
        request: {
          id: requestId,
          type: "reaction",
          actorId: input.current.protectorActorId,
          trigger: {
            type: "protection_before_attack",
            sourceActorId: input.command.actorId,
            targetActorId: input.command.targetIds[0],
            actionId: input.action.id,
            attackId: input.command.commandId
          },
          options: [{
            actionId: PROTECTION_2024_CAPABILITY_ID,
            label: "Fighting Style: Protection"
          }]
        }
      }
    }
  };
}
function protectionEndEvent(input) {
  return {
    sourceActorId: input.sourceActorId,
    obligationIds: [
      "system:fighting-style-protection",
      "system:effect-lifecycle",
      ...input.effect.source.sourceEntityIds.map((id) => `entity:${id}`)
    ],
    payload: {
      type: "ProtectionEffectEnded",
      protectorActorId: input.effect.protectorActorId,
      protectedTargetActorId: input.effect.protectedTargetActorId,
      effectId: input.effect.id,
      reason: "proximity_broken",
      lifecycleEvent: {
        type: "distance_observed",
        factsSource: input.facts.factsSource,
        worldRevision: input.worldRevision,
        protectorActorId: input.effect.protectorActorId,
        protectedTargetActorId: input.effect.protectedTargetActorId,
        distanceFt: input.facts.protectorDistanceToTargetFt
      }
    }
  };
}
function pendingAttackEvents(world, command, action, catalog, env, options = {}) {
  const targetId = command.targetIds[0];
  if (!targetId || !hasAttackRoll(action)) return null;
  const originalSource = world.actors[command.actorId];
  const originalTarget = world.actors[targetId];
  const facts = command.factsByTarget?.[targetId];
  if (!facts) return rejected4(world, "MissingSpatialFacts", `Missing spatial facts for target ${targetId}`);
  const protection = protectionCandidatesForAttack({
    world,
    command,
    source: originalSource,
    target: originalTarget,
    facts
  });
  if ("status" in protection) return protection;
  const candidateByProtector = new Map(protection.candidates.map((candidate) => [candidate.protectorActorId, candidate]));
  let protectionDisadvantage = false;
  const protectionLifecycleEvents = [];
  for (const protector of Object.values(world.actors)) {
    const candidate = candidateByProtector.get(protector.id);
    for (const effect of actorProtectionEffects(protector).filter((active) => active.protectedTargetActorId === targetId)) {
      if (!candidate) {
        return rejected4(world, "InvalidFacts", `Missing current proximity for Protection effect ${effect.id}`);
      }
      const result = resolveProtection2024AttackRoll(effect, {
        factsSource: candidate.factsSource,
        worldRevision: world.revision,
        attackId: command.commandId,
        targetActorId: targetId,
        attackRollStage: "before_roll",
        protectorDistanceToProtectedTargetFt: candidate.protectorDistanceToTargetFt
      });
      if (result.status === "rejected") {
        return rejected4(world, "InvalidFacts", `Protection attack facts were rejected: ${result.reason}`);
      }
      if (result.status === "ended") {
        protectionLifecycleEvents.push(protectionEndEvent({
          effect,
          facts: candidate,
          worldRevision: world.revision,
          sourceActorId: command.actorId
        }));
      } else if (result.imposeDisadvantage) {
        protectionDisadvantage = true;
      }
    }
  }
  const effectiveWorld = protectionLifecycleEvents.length ? foldEvents(world, protectionLifecycleEvents.map((event, ordinal) => ({ ...event, ordinal }))) : world;
  const source2 = effectiveWorld.actors[command.actorId];
  const target2 = options.protectionWindowResolved ? attackTargetWithCover(effectiveWorld.actors[targetId], facts.cover) : effectiveWorld.actors[targetId];
  const obligations = actionObligationIds(
    action,
    "system:attack-resolution",
    "system:pending-resolution",
    ...protection.queued.length || protectionDisadvantage || protectionLifecycleEvents.length ? ["system:fighting-style-protection"] : []
  );
  if (!options.protectionWindowResolved && protection.queued.length) {
    const payable2 = canPay(source2.runtime, activationCost(action));
    if (!payable2.ok) {
      return rejected4(world, "InsufficientResources", `Missing resources: ${payable2.missing.join(", ")}`);
    }
    const paid2 = pay(source2.runtime, activationCost(action));
    const [current, ...remaining] = protection.queued;
    return [
      ...protectionLifecycleEvents,
      ...runtimeTransition(source2.id, source2.id, source2.runtime, paid2.state, "action", obligations),
      ...engineTrace(source2.id, [target2.id], paid2.events, obligations),
      protectionOpenedEvent({
        world: effectiveWorld,
        command,
        action,
        current,
        remaining,
        candidates: protection.candidates,
        options,
        env,
        obligations
      })
    ];
  }
  const availableReactions = hitReactionOptions(target2, catalog);
  if (!availableReactions.length && !options.forceExecution && !protectionDisadvantage && !protectionLifecycleEvents.length) return null;
  const cost = activationCost(action);
  const payable = canPay(source2.runtime, cost);
  if (!payable.ok) {
    return rejected4(world, "InsufficientResources", `Missing resources: ${payable.missing.join(", ")}`);
  }
  const paid = pay(source2.runtime, cost);
  const continuationDisadvantages = options.protectionWindowResolved ? [...options.preRollDisadvantageReasons ?? []] : [];
  const sourceForRoll = protectionDisadvantage || continuationDisadvantages.length ? {
    ...source2,
    passives: [
      ...source2.passives ?? [],
      ...continuationDisadvantages.map(attackDisadvantagePassive),
      ...protectionDisadvantage ? [attackDisadvantagePassive("Fighting Style: Protection")] : []
    ]
  } : source2;
  const preview = executeAction(paid.state, withoutActivationCost(action.mechanics), {
    ...actionContext(sourceForRoll, env, target2, target2.runtime, facts, command.spell),
    ...options.attackActionId ? { attackActionId: options.attackActionId } : {},
    attackCommandId: command.commandId,
    choices: command.choices,
    spell: command.spell,
    pauseAfterAttackRoll: true,
    ...options.externalPrimitiveHandled ? { externalPrimitiveHandled: true } : {}
  });
  const attackRoll = attackRollFrom(preview.events);
  if (!attackRoll) return rejected4(world, "InvalidDecision", `${action.id} did not produce an attack roll`);
  const reactions = attackRoll.outcome === "hit" || attackRoll.outcome === "crit" ? availableReactions : [];
  if (reactions.length) {
    const resolutionId = env.nextId();
    const requestId = env.nextId();
    return [
      ...protectionLifecycleEvents,
      ...runtimeTransition(source2.id, source2.id, source2.runtime, preview.state, "action", obligations),
      ...runtimeTransition(
        source2.id,
        target2.id,
        target2.runtime,
        preview.targetState ?? target2.runtime,
        "action",
        obligations
      ),
      ...engineTrace(source2.id, [target2.id], [
        ...paid.events,
        ...relabelAttackRolls(preview.events, "\u0410\u0442\u0430\u043A\u0430 \u2014 \u0434\u043E \u0440\u0435\u0430\u043A\u0446\u0438\u0438")
      ], obligations),
      {
        sourceActorId: source2.id,
        obligationIds: obligations,
        payload: {
          type: "ResolutionOpened",
          resolution: {
            id: resolutionId,
            type: "attack_reaction",
            openedByCommandId: command.commandId,
            openedAtRevision: world.revision,
            deadlineLogicalClock: world.logicalClock + 10,
            sourceActorId: source2.id,
            targetActorId: target2.id,
            actionId: action.id,
            facts,
            choices: command.choices,
            spell: command.spell,
            attackRoll,
            request: {
              id: requestId,
              type: "reaction",
              actorId: target2.id,
              trigger: {
                type: "hit_by_attack",
                sourceActorId: source2.id,
                actionId: action.id,
                attackTotal: attackRoll.total,
                originalAc: effectiveArmorClass(target2)
              },
              options: reactions.map(({ option }) => cloneReactionOption(option))
            },
            ...options.attackActionId ? { attackActionId: options.attackActionId } : {},
            ...options.weaponHand ? { weaponHand: options.weaponHand } : {},
            ...options.weaponCardId ? { weaponCardId: options.weaponCardId } : {},
            ...options.pactBladeProjection ? { pactBladeProjection: { ...options.pactBladeProjection } } : {}
          }
        }
      }
    ];
  }
  const resumed = executeAction(preview.state, withoutActivationCost(action.mechanics), {
    ...actionContext(sourceForRoll, env, target2, target2.runtime, facts, command.spell),
    ...options.attackActionId ? { attackActionId: options.attackActionId } : {},
    attackCommandId: command.commandId,
    choices: command.choices,
    spell: command.spell,
    forcedAttackRoll: attackRoll,
    deferTargetSaves: true,
    ...options.externalPrimitiveHandled ? { externalPrimitiveHandled: true } : {}
  });
  const events = [
    ...protectionLifecycleEvents,
    ...actionStateEvents({
      world: effectiveWorld,
      commandId: command.commandId,
      source: source2,
      action,
      sourceAfter: resumed.state,
      target: target2,
      targetAfter: resumed.targetState,
      obligations
    }),
    ...engineTrace(source2.id, [target2.id], [
      ...paid.events,
      ...relabelAttackRolls(preview.events, "\u0410\u0442\u0430\u043A\u0430"),
      ...withoutAttackRoll(resumed.events)
    ], obligations)
  ];
  events.push(...attackFollowUpEvents({
    world: effectiveWorld,
    commandId: command.commandId,
    source: source2,
    target: target2,
    targetAfter: resumed.targetState,
    action,
    deferred: resumed.deferredTargetSaves,
    env,
    obligations
  }));
  return events;
}
function magicMissileReactionOptions(target2, catalog) {
  if (deniedCapabilities(target2.runtime, target2.passives ?? []).has("reaction")) return [];
  return target2.capabilities.actionIds.flatMap((actionId) => {
    const action = catalog.getAction(actionId);
    if (!action || !hasReactionTrigger(action, "targeted_by_magic_missile")) return [];
    if (actionDefinitionIssue(action) || spellDeclarationIssue(action) || !grantsMagicMissileImmunity(action)) return [];
    return sourceScopedReactionOptions(target2, action);
  });
}
function magicMissileReactionOpenedEvent(input) {
  const dartCount = input.dartTargetIds.filter((targetId) => targetId === input.current.targetActorId).length;
  return {
    sourceActorId: input.sourceActorId,
    obligationIds: input.obligations,
    payload: {
      type: "ResolutionOpened",
      resolution: {
        id: input.env.nextId(),
        type: "magic_missile_reaction",
        openedByCommandId: input.actionCommandId,
        openedAtRevision: input.world.revision,
        deadlineLogicalClock: input.world.logicalClock + 10,
        sourceActorId: input.sourceActorId,
        targetActorId: input.current.targetActorId,
        actionId: input.actionId,
        spell: input.spell,
        dartTargetIds: [...input.dartTargetIds],
        targets: input.targets.map((entry) => ({
          targetActorId: entry.targetActorId,
          facts: { ...entry.facts }
        })),
        protectedTargetIds: [...input.protectedTargetIds],
        remainingReactions: input.remainingReactions.map((entry) => ({
          targetActorId: entry.targetActorId,
          options: entry.options.map(cloneReactionOption)
        })),
        request: {
          id: input.env.nextId(),
          type: "reaction",
          actorId: input.current.targetActorId,
          trigger: {
            type: "targeted_by_magic_missile",
            sourceActorId: input.sourceActorId,
            actionId: input.actionId,
            dartCount
          },
          options: input.current.options.map(cloneReactionOption)
        }
      }
    }
  };
}
function magicMissileDamageEvents(input) {
  const source2 = input.world.actors[input.sourceActorId];
  const factsByTarget = new Map(input.targets.map((entry) => [entry.targetActorId, entry.facts]));
  const protectedTargets = new Set(input.protectedTargetIds);
  const actorRuntimes = new Map(input.runtimeOverrides ?? []);
  let sourceRuntime = input.sourceRuntime ?? actorRuntimes.get(source2.id) ?? source2.runtime;
  actorRuntimes.delete(source2.id);
  const traces = [];
  const dartMechanics = {
    activation: { mode: "triggered", cost: [] },
    effects: [JSON.parse(JSON.stringify(input.policy.perDartEffect))]
  };
  input.dartTargetIds.forEach((targetActorId, dartIndex) => {
    const target2 = input.world.actors[targetActorId];
    const facts = factsByTarget.get(targetActorId);
    if (!target2 || !facts) return;
    const auditFacts = {
      magicMissile: {
        dartOrdinal: dartIndex + 1,
        simultaneous: input.policy.simultaneous,
        shielded: protectedTargets.has(targetActorId)
      },
      spatialFacts: facts
    };
    if (protectedTargets.has(targetActorId)) {
      traces.push(...engineTrace(source2.id, [targetActorId], [{
        type: "narrative",
        text: `Shield blocks Magic Missile dart ${dartIndex + 1}.`
      }], input.obligations, { facts: auditFacts }));
      return;
    }
    const currentTargetRuntime = target2.id === source2.id ? sourceRuntime : actorRuntimes.get(target2.id) ?? target2.runtime;
    const sourceAtStep = { ...source2, runtime: sourceRuntime };
    const targetAtStep = { ...target2, runtime: currentTargetRuntime };
    const context2 = actionContext(sourceAtStep, input.env, targetAtStep, currentTargetRuntime, facts);
    if (target2.id === source2.id && context2.target) context2.target.runtimeState = currentTargetRuntime;
    const result = executeAction(sourceRuntime, dartMechanics, context2);
    sourceRuntime = target2.id === source2.id ? result.targetState ?? result.state : result.state;
    if (target2.id !== source2.id && result.targetState) actorRuntimes.set(target2.id, result.targetState);
    traces.push(...engineTrace(source2.id, [targetActorId], result.events, input.obligations, {
      facts: auditFacts
    }));
  });
  const events = actionStateEvents({
    world: input.world,
    commandId: input.actionCommandId,
    source: source2,
    action: input.action,
    sourceAfter: sourceRuntime,
    targetUpdates: [...actorRuntimes.entries()].flatMap(([actorId, targetAfter]) => {
      const target2 = input.world.actors[actorId];
      return target2 ? [{ target: target2, targetAfter }] : [];
    }),
    obligations: input.obligations
  });
  events.push(...traces);
  const damagedTargetIds = input.dartTargetIds.filter((targetId, index, all) => !protectedTargets.has(targetId) && all.indexOf(targetId) === index);
  const followUps = damagedTargetIds.flatMap((targetId) => {
    const target2 = input.world.actors[targetId];
    if (!target2) return [];
    const actorAfter = targetId === source2.id ? sourceRuntime : actorRuntimes.get(targetId);
    const continuation = concentrationSaveFollowUp({
      world: input.world,
      actor: target2,
      actorAfter,
      obligations: input.obligations
    });
    return continuation ? [continuation] : [];
  });
  events.push(...followUpOpenedEvents({
    world: input.world,
    commandId: input.followUpCommandId,
    followUps,
    env: input.env
  }));
  return events;
}
function magicMissileEvents(world, command, action, spec, catalog, env) {
  const allocation = magicMissileAllocation(command, spec);
  if ("issue" in allocation) return rejected4(world, "InvalidTargets", allocation.issue);
  if (!command.spell) return rejected4(world, "InvalidSpellDeclaration", "Magic Missile requires canonical spell metadata");
  const source2 = world.actors[command.actorId];
  const payable = canPay(source2.runtime, activationCost(action));
  if (!payable.ok) {
    return rejected4(world, "InsufficientResources", `Missing resources: ${payable.missing.join(", ")}`);
  }
  const declaration = executeAction(source2.runtime, {
    ...action.mechanics,
    effects: []
  }, {
    ...actionContext(source2, env, void 0, void 0, void 0, command.spell),
    choices: command.choices,
    spell: command.spell,
    externalPrimitiveHandled: true
  });
  const obligations = actionObligationIds(
    action,
    "system:magic-missile",
    "system:reaction-window",
    "system:pending-resolution"
  );
  const targets = command.targetIds.map((targetActorId) => ({
    targetActorId,
    facts: { ...command.factsByTarget[targetActorId] }
  }));
  const protectedTargetIds = command.targetIds.filter((targetActorId) => hasMagicMissileImmunity(world.actors[targetActorId]));
  const reactions = command.targetIds.flatMap((targetActorId) => {
    if (protectedTargetIds.includes(targetActorId)) return [];
    const options = magicMissileReactionOptions(world.actors[targetActorId], catalog);
    return options.length ? [{ targetActorId, options }] : [];
  });
  const declarationTrace = engineTrace(source2.id, command.targetIds, declaration.events, obligations);
  const [current, ...remainingReactions] = reactions;
  if (!current) {
    return [
      ...declarationTrace,
      ...magicMissileDamageEvents({
        world,
        actionCommandId: command.commandId,
        sourceActorId: source2.id,
        sourceRuntime: declaration.state,
        action,
        policy: spec,
        dartTargetIds: allocation.dartTargetIds,
        targets,
        protectedTargetIds,
        env,
        obligations,
        followUpCommandId: command.commandId
      })
    ];
  }
  return [
    ...runtimeTransition(source2.id, source2.id, source2.runtime, declaration.state, "action", obligations),
    ...declarationTrace,
    magicMissileReactionOpenedEvent({
      world,
      actionCommandId: command.commandId,
      sourceActorId: source2.id,
      actionId: action.id,
      spell: command.spell,
      dartTargetIds: allocation.dartTargetIds,
      targets,
      protectedTargetIds,
      current,
      remainingReactions,
      env,
      obligations
    })
  ];
}
function pendingSaveEvents(world, command, action, env, attackSequence, attackActionId) {
  if (!command.targetIds.length) return null;
  const source2 = world.actors[command.actorId];
  const queuedTargets = [];
  for (const targetId of command.targetIds) {
    const target3 = world.actors[targetId];
    const facts = command.factsByTarget?.[target3.id];
    if (!facts) return rejected4(world, "MissingSpatialFacts", `Missing spatial facts for target ${target3.id}`);
    const save = readTargetSave(action.mechanics, {
      ...actionContext(source2, env, target3, target3.runtime, facts, command.spell),
      choices: command.choices,
      spell: command.spell
    });
    if (!save) return null;
    queuedTargets.push({
      targetActorId: target3.id,
      facts: { ...facts },
      save: {
        ability: save.ability,
        dc: save.dc,
        avoidsConditions: [...save.avoidsConditions]
      }
    });
  }
  const [first, ...remainingTargets] = queuedTargets;
  if (!first) return null;
  const target2 = world.actors[first.targetActorId];
  const cost = activationCost(action);
  const payable = canPay(source2.runtime, cost);
  if (!payable.ok) {
    return rejected4(world, "InsufficientResources", `Missing resources: ${payable.missing.join(", ")}`);
  }
  const paid = pay(source2.runtime, cost);
  const declarationEvents = [...paid.events];
  const sourceAfterDeclaration = command.spell?.components?.verbal === true ? expireEffectsForTrigger(
    paid.state,
    "actor_casts_spell_with_verbal_component",
    declarationEvents
  ) : paid.state;
  const resolutionId = env.nextId();
  const requestId = env.nextId();
  const obligationIds = actionObligationIds(
    action,
    "system:target-save",
    "system:pending-resolution",
    ...attackSequenceObligationIds(attackSequence)
  );
  const events = [];
  events.push(...runtimeTransition(
    source2.id,
    source2.id,
    source2.runtime,
    sourceAfterDeclaration,
    "action",
    obligationIds
  ));
  events.push(...engineTrace(source2.id, command.targetIds, declarationEvents, obligationIds));
  events.push({
    sourceActorId: source2.id,
    obligationIds,
    payload: {
      type: "ResolutionOpened",
      resolution: {
        id: resolutionId,
        type: "target_save",
        openedByCommandId: command.commandId,
        openedAtRevision: world.revision,
        deadlineLogicalClock: world.logicalClock + 10,
        sourceActorId: source2.id,
        targetActorId: target2.id,
        actionId: action.id,
        facts: first.facts,
        choices: command.choices,
        spell: command.spell,
        request: {
          id: requestId,
          type: "saving_throw",
          actorId: target2.id,
          ability: first.save.ability,
          dc: first.save.dc,
          avoidsConditions: [...first.save.avoidsConditions]
        },
        remainingTargets,
        resolvedTargetIds: [],
        concentrationEffectLinks: [],
        followUps: [],
        spellCastEmitted: false,
        sharedDamageRolls: [],
        ...attackSequence ? {
          attackSequence: JSON.parse(JSON.stringify(attackSequence))
        } : {},
        ...attackActionId ? { attackActionId } : {}
      }
    }
  });
  return events;
}
function executeHide(world, command, env) {
  const actor2 = world.actors[command.actorId];
  const issue = hideEligibilityIssue(command.eligibility);
  if (issue) return rejected4(world, "HideNotEligible", issue);
  if (deniedCapabilities(actor2.runtime, actor2.passives ?? []).has("action")) {
    return rejected4(world, "CapabilityDenied", `${actor2.id} cannot take the Hide action in its current state`);
  }
  const payable = canPay(actor2.runtime, activationCost(CORE_HIDE_ACTION));
  if (!payable.ok) {
    return rejected4(world, "InsufficientResources", `Missing resources: ${payable.missing.join(", ")}`);
  }
  const result = executeAction(actor2.runtime, CORE_HIDE_ACTION.mechanics, actionContext(actor2, env));
  const obligations = actionObligationIds(
    CORE_HIDE_ACTION,
    "system:hide-action",
    "system:ability-check"
  );
  return [
    actionDeclaredEvent({
      actorId: actor2.id,
      action: CORE_HIDE_ACTION,
      targetIds: [],
      timing: "active",
      facts: { hideEligibility: { ...command.eligibility }, dc: 15 },
      obligationIds: obligations
    }),
    ...runtimeTransition(actor2.id, actor2.id, actor2.runtime, result.state, "action", obligations),
    ...engineTrace(actor2.id, [], result.events, obligations)
  ];
}
function recordNoise(world, command) {
  const issue = noiseFactsIssue(command.facts);
  if (issue) return rejected4(world, "InvalidFacts", issue);
  const actor2 = world.actors[command.actorId];
  const louderThanWhisper = command.facts.loudness === "above_whisper";
  const facts = {
    observation: "actor_makes_noise",
    trigger: louderThanWhisper ? "noise_above_whisper" : "noise_at_or_below_whisper",
    noise: { ...command.facts }
  };
  const recorded = [{
    type: "narrative",
    text: louderThanWhisper ? `${actor2.name} \u0438\u0437\u0434\u0430\u0451\u0442 \u0437\u0432\u0443\u043A \u0433\u0440\u043E\u043C\u0447\u0435 \u0448\u0451\u043F\u043E\u0442\u0430.` : `${actor2.name} \u043D\u0435 \u0438\u0437\u0434\u0430\u0451\u0442 \u0437\u0432\u0443\u043A\u0430 \u0433\u0440\u043E\u043C\u0447\u0435 \u0448\u0451\u043F\u043E\u0442\u0430.`
  }];
  const after = louderThanWhisper ? expireEffectsForTrigger(actor2.runtime, "noise_above_whisper", recorded) : actor2.runtime;
  const obligations = ["system:hide-lifecycle", "system:observable-fact"];
  return [
    ...runtimeTransition(actor2.id, actor2.id, actor2.runtime, after, "action", obligations),
    ...engineTrace(actor2.id, [], recorded, obligations, { facts })
  ];
}
function recordEnemyFinding(world, command) {
  const target2 = world.actors[command.targetActorId];
  if (!target2) return rejected4(world, "ActorNotFound", `Unknown target ${command.targetActorId}`);
  if (target2.id === command.actorId) {
    return rejected4(world, "InvalidTargets", "An actor cannot be its own enemy finder");
  }
  const issue = enemyFindingFactsIssue(command.facts);
  if (issue) return rejected4(world, issue[0], issue[1]);
  const finder = world.actors[command.actorId];
  const facts = {
    observation: "enemy_finds_actor",
    targetActorId: target2.id,
    finding: { ...command.facts }
  };
  const recorded = [{
    type: "narrative",
    text: `${finder.name} \u043E\u0431\u043D\u0430\u0440\u0443\u0436\u0438\u0432\u0430\u0435\u0442 ${target2.name}.`
  }];
  const after = expireEffectsForTrigger(target2.runtime, "enemy_finds_actor", recorded);
  const obligations = ["system:hide-lifecycle", "system:observable-fact"];
  return [
    ...runtimeTransition(finder.id, target2.id, target2.runtime, after, "action", obligations),
    ...engineTrace(target2.id, [target2.id], recorded, obligations, {
      sourceActorId: finder.id,
      facts
    })
  ];
}
function swapAlertInitiative(world, command) {
  if (world.scene.mode !== "encounter" || world.scene.round !== 1 || world.scene.activeIndex !== 0 || world.scene.turnStarted) {
    return rejected4(
      world,
      "InvalidActionTiming",
      "Alert Initiative Swap is available only after Initiative and before the first turn starts"
    );
  }
  const actor2 = world.actors[command.actorId];
  const sourceEntityIds = actor2.capabilities.featureSources?.[ALERT_INITIATIVE_SWAP_CAPABILITY];
  if (!stableSourceEntityIds(sourceEntityIds)) {
    return rejected4(
      world,
      "FeatureNotGranted",
      `${actor2.id} does not own a mechanics-declared Alert Initiative Swap capability`
    );
  }
  const ally = world.actors[command.allyActorId];
  if (!ally) return rejected4(world, "ActorNotFound", `Unknown ally ${command.allyActorId}`);
  if (ally.id === actor2.id) return rejected4(world, "InvalidTargets", "Alert requires a different ally");
  const factsIssue2 = initiativeSwapFactsIssue(command.facts, ally.controllerId);
  if (factsIssue2) return rejected4(world, factsIssue2[0], factsIssue2[1]);
  const actorIndex = world.scene.initiative.indexOf(actor2.id);
  const allyIndex = world.scene.initiative.indexOf(ally.id);
  if (actorIndex < 0 || allyIndex < 0) {
    return rejected4(world, "InvalidTargets", "Both creatures must participate in the same combat");
  }
  if (activeConditionsOf(actor2.runtime).has("incapacitated") || activeConditionsOf(ally.runtime).has("incapacitated")) {
    return rejected4(world, "CapabilityDenied", "Incapacitated creatures cannot use Alert Initiative Swap");
  }
  if (world.scene.initiativeSwapActorIds?.includes(actor2.id)) {
    return rejected4(world, "InvalidActionTiming", `${actor2.id} already swapped Initiative after this roll`);
  }
  const initiative = [...world.scene.initiative];
  [initiative[actorIndex], initiative[allyIndex]] = [initiative[allyIndex], initiative[actorIndex]];
  const scene = {
    ...world.scene,
    initiative,
    initiativeSwapActorIds: [...world.scene.initiativeSwapActorIds ?? [], actor2.id]
  };
  const obligations = [
    "system:initiative-swap",
    ...sourceEntityIds.map((sourceId) => `entity:${sourceId}`)
  ];
  const facts = {
    capabilityId: ALERT_INITIATIVE_SWAP_CAPABILITY,
    sourceEntityIds: [...sourceEntityIds],
    consent: { ...command.facts },
    before: { initiative: [...world.scene.initiative] },
    after: { initiative: [...initiative] }
  };
  return [
    ...engineTrace(actor2.id, [ally.id], [{
      type: "narrative",
      text: `${actor2.name} \u043E\u0431\u043C\u0435\u043D\u0438\u0432\u0430\u0435\u0442\u0441\u044F \u0438\u043D\u0438\u0446\u0438\u0430\u0442\u0438\u0432\u043E\u0439 \u0441 ${ally.name} (Alert).`
    }], obligations, { facts }),
    {
      sourceActorId: actor2.id,
      obligationIds: obligations,
      payload: { type: "SceneSet", scene }
    }
  ];
}
function openHazardSave(world, command, catalog, env) {
  if (command.targetActorId !== command.actorId) {
    return rejected4(world, "InvalidTargets", "A hazard must be triggered for the command actor");
  }
  const definition = catalog.getHazard?.(command.hazardId);
  if (!definition) return rejected4(world, "HazardNotFound", `Unknown hazard ${command.hazardId}`);
  const issue = hazardDefinitionIssue(definition);
  if (issue) return rejected4(world, "InvalidHazardDefinition", issue);
  const hazard = cloneHazard(definition);
  const sourceActorId = hazardSourceId(hazard);
  const obligations = hazardObligationIds(hazard);
  const resolutionId = env.nextId();
  const requestId = env.nextId();
  return [
    {
      sourceActorId,
      obligationIds: obligations,
      payload: {
        type: "ResolutionOpened",
        resolution: {
          id: resolutionId,
          type: "hazard_save",
          openedByCommandId: command.commandId,
          openedAtRevision: world.revision,
          deadlineLogicalClock: world.logicalClock + 10,
          targetActorId: command.targetActorId,
          hazard,
          request: {
            id: requestId,
            type: "saving_throw",
            actorId: command.targetActorId,
            ability: hazard.save.ability,
            dc: hazard.save.dc,
            avoidsConditions: hazardAvoidedConditions(hazard)
          }
        }
      }
    }
  ];
}
function manualDecisionRng(command) {
  if (command.response.kind !== "roll" || command.response.roll.mode !== "manual") return null;
  const tape = createStrictRngTape(command.response.roll.dice.map((die, index) => ({
    label: `manual-save-${index + 1}`,
    sides: die.sides,
    value: die.value
  })));
  return { rng: tape.rng, assertExhausted: tape.assertExhausted };
}
function replayableSharedDamageRng(persisted, fallback) {
  const rolls = (persisted ?? []).map((entry) => ({ ...entry }));
  let cursor = 0;
  const rollDie = (sides) => {
    const existing = rolls[cursor];
    if (existing) {
      if (existing.sides !== sides) {
        throw new Error(`Shared damage roll mismatch: requested d${sides}, persisted d${existing.sides}`);
      }
      cursor += 1;
      return existing.value;
    }
    const dieAware = fallback;
    const unit = typeof dieAware.rollDie === "function" ? null : fallback();
    if (unit != null && (!Number.isFinite(unit) || unit < 0 || unit >= 1)) {
      throw new Error(`RNG must return a finite value in [0, 1), got ${unit}`);
    }
    const generated = typeof dieAware.rollDie === "function" ? dieAware.rollDie(sides) : Math.floor(unit * sides) + 1;
    if (!Number.isInteger(generated) || generated < 1 || generated > sides) {
      throw new Error(`Damage RNG returned invalid d${sides} result: ${generated}`);
    }
    rolls.push({ sides, value: generated });
    cursor += 1;
    return generated;
  };
  const rng = Object.assign(
    () => {
      throw new Error("Shared damage rolls must declare die sides");
    },
    { rollDie }
  );
  return {
    rolls,
    rng
  };
}
function resolvePendingSave(world, command, catalog, env) {
  const pending = world.pendingResolution;
  if (!pending || pending.type !== "target_save") {
    return rejected4(world, "NoPendingResolution", "There is no target save to resolve");
  }
  if (pending.id !== command.resolutionId || pending.request.id !== command.requestId) {
    return rejected4(world, "StaleDecision", "Decision does not match the active request");
  }
  if (pending.targetActorId !== command.actorId || pending.request.actorId !== command.actorId) {
    return rejected4(world, "InvalidDecision", "Only the requested actor can resolve this saving throw");
  }
  if (command.response.kind !== "roll") {
    return rejected4(world, "InvalidDecision", "A saving throw requires a roll response");
  }
  const action = catalog.getAction(pending.actionId);
  if (!action) return rejected4(world, "ActionNotFound", `Unknown action ${pending.actionId}`);
  const sequenceIssue = attackSequenceContinuationIssue(world, pending.attackSequence, pending, action);
  if (sequenceIssue) return rejected4(world, "InvalidDecision", sequenceIssue);
  const source2 = world.actors[pending.sourceActorId];
  const target2 = world.actors[pending.targetActorId];
  if (!source2 || !target2) return rejected4(world, "ActorNotFound", "Target-save continuation actor is missing");
  const continuationIds = [
    ...pending.resolvedTargetIds ?? [],
    pending.targetActorId,
    ...(pending.remainingTargets ?? []).map((entry) => entry.targetActorId)
  ];
  if (new Set(continuationIds).size !== continuationIds.length) {
    return rejected4(world, "InvalidDecision", "Target-save continuation contains duplicate actors");
  }
  const currentFactsIssue = factsIssue(action, target2.id, pending.facts);
  if (currentFactsIssue) return rejected4(world, "InvalidDecision", currentFactsIssue[1]);
  for (const entry of pending.remainingTargets ?? []) {
    if (!world.actors[entry.targetActorId]) {
      return rejected4(world, "ActorNotFound", `Unknown queued target ${entry.targetActorId}`);
    }
    const issue = factsIssue(action, entry.targetActorId, entry.facts);
    if (issue) return rejected4(world, "InvalidDecision", issue[1]);
    if (!Number.isInteger(entry.save.dc) || entry.save.dc < 1 || entry.save.dc > 30) {
      return rejected4(world, "InvalidDecision", `Queued target ${entry.targetActorId} has an invalid saving throw DC`);
    }
  }
  if ((pending.sharedDamageRolls ?? []).some(({ sides, value }) => !Number.isInteger(sides) || sides < 2 || !Number.isInteger(value) || value < 1 || value > sides)) {
    return rejected4(world, "InvalidDecision", "Target-save continuation has an invalid shared damage roll");
  }
  const collected = collectRollModifiers(target2.runtime, target2.passives ?? [], {
    roll: "saving_throw",
    filter: { ability: pending.request.ability },
    formulaCtx: actorFormulaContext(target2.character),
    evalCtx: {
      state: target2.runtime,
      activeConditions: activeConditionsOf(target2.runtime),
      savedConditions: new Set(pending.request.avoidsConditions)
    }
  });
  const proficient = target2.character.saveProficiencies?.includes(pending.request.ability);
  const base2 = target2.character.abilityMods[pending.request.ability] ?? 0;
  const manual = manualDecisionRng(command);
  let roll;
  try {
    roll = rollD20({
      advantage: collected.advantage,
      modifiers: [
        { value: base2, source: ABILITY_LABEL3[pending.request.ability] },
        ...proficient ? [{ value: target2.character.profBonus, source: "\u0411\u041C" }] : [],
        ...collected.modifiers
      ],
      target: { type: "dc", value: pending.request.dc },
      rules: collected.rules,
      rng: manual?.rng ?? env.rng
    });
    manual?.assertExhausted();
  } catch (error) {
    return rejected4(world, "InvalidDecision", error instanceof Error ? error.message : "Invalid manual roll");
  }
  const sharedDamage = replayableSharedDamageRng(pending.sharedDamageRolls, env.rng);
  const result = executeAction(
    source2.runtime,
    withoutActivationCost(action.mechanics),
    {
      ...actionContext(source2, env, target2, target2.runtime, pending.facts, pending.spell),
      choices: pending.choices,
      spell: pending.spell,
      suppressSpellCastEvent: pending.spellCastEmitted === true,
      damageRng: sharedDamage.rng,
      forceSaveOutcome: roll.outcome === "success" ? "success" : "fail",
      ...worldActionPrimitive(action) ? { externalPrimitiveHandled: true } : {}
    }
  );
  const obligationIds = actionObligationIds(
    action,
    "system:target-save",
    "system:pending-resolution",
    ...attackSequenceObligationIds(pending.attackSequence),
    ...pending.attackActionId ? ["system:attack-action", "system:attack-replacement"] : []
  );
  const saveEvent = {
    type: "roll",
    label: `\u0421\u043F\u0430\u0441\u0431\u0440\u043E\u0441\u043E\u043A ${ABILITY_LABEL3[pending.request.ability]}`,
    roll: { ...roll, kind: "save" }
  };
  const targetAfter = target2.id === source2.id ? result.state : result.targetState;
  const currentConcentrationLinks = mergeConcentrationEffectLinks(
    concentrationLinkedEffectIds(source2.runtime, result.state).map((effectId) => ({ actorId: source2.id, effectId })),
    targetAfter && target2.id !== source2.id ? concentrationLinkedEffectIds(target2.runtime, targetAfter).map((effectId) => ({ actorId: target2.id, effectId })) : []
  );
  const accumulatedConcentrationLinks = mergeConcentrationEffectLinks(
    pending.concentrationEffectLinks ?? [],
    currentConcentrationLinks
  );
  const damageFollowUp = concentrationSaveFollowUp({
    world,
    actor: target2,
    actorAfter: targetAfter,
    obligations: obligationIds
  });
  const followUps = [
    ...pending.followUps ?? [],
    ...damageFollowUp ? [damageFollowUp] : []
  ];
  const [nextTarget, ...remainingTargets] = pending.remainingTargets ?? [];
  const finalTarget = !nextTarget;
  const events = engineTrace(target2.id, [], [saveEvent], obligationIds);
  events.push(...actionStateEvents({
    world,
    commandId: pending.openedByCommandId,
    source: source2,
    action,
    sourceAfter: result.state,
    ...target2.id === source2.id || !targetAfter ? {} : { target: target2, targetAfter },
    additionalConcentrationEffectLinks: accumulatedConcentrationLinks,
    manageConcentration: finalTarget,
    obligations: obligationIds
  }));
  const damageAdjustments = damageAdjustmentAudit(result.events);
  const resultObligations = [.../* @__PURE__ */ new Set([
    ...obligationIds,
    ...damageAdjustments.flatMap((adjustment) => adjustment.sourceEntityIds.map((sourceId) => `entity:${sourceId}`))
  ])];
  events.push(...engineTrace(
    source2.id,
    [target2.id],
    result.events,
    resultObligations,
    damageAdjustments.length ? { facts: { damageAdjustments } } : void 0
  ));
  events.push({
    sourceActorId: target2.id,
    obligationIds,
    payload: {
      type: "DecisionRecorded",
      resolutionId: pending.id,
      requestId: pending.request.id,
      actorId: target2.id,
      response: command.response
    }
  });
  events.push({
    sourceActorId: target2.id,
    obligationIds,
    payload: { type: "ResolutionClosed", resolutionId: pending.id }
  });
  if (nextTarget) {
    const nextResolutionId = env.nextId();
    const nextRequestId = env.nextId();
    if (pending.attackActionId) {
      events.push(...attackResolutionFinishedEvents({
        attackAction: world.attackActions[pending.attackActionId],
        resolutionId: pending.id,
        actorId: source2.id,
        obligations: obligationIds,
        closeIfComplete: false
      }));
    }
    events.push({
      sourceActorId: source2.id,
      obligationIds,
      payload: {
        type: "ResolutionOpened",
        resolution: {
          id: nextResolutionId,
          type: "target_save",
          openedByCommandId: pending.openedByCommandId,
          openedAtRevision: world.revision,
          deadlineLogicalClock: world.logicalClock + 10,
          sourceActorId: source2.id,
          targetActorId: nextTarget.targetActorId,
          actionId: action.id,
          facts: { ...nextTarget.facts },
          choices: pending.choices,
          spell: pending.spell,
          request: {
            id: nextRequestId,
            type: "saving_throw",
            actorId: nextTarget.targetActorId,
            ability: nextTarget.save.ability,
            dc: nextTarget.save.dc,
            avoidsConditions: [...nextTarget.save.avoidsConditions]
          },
          remainingTargets,
          resolvedTargetIds: [...pending.resolvedTargetIds ?? [], target2.id],
          concentrationEffectLinks: accumulatedConcentrationLinks,
          followUps,
          spellCastEmitted: true,
          sharedDamageRolls: sharedDamage.rolls.map((entry) => ({ ...entry })),
          ...pending.attackSequence ? {
            attackSequence: JSON.parse(JSON.stringify(pending.attackSequence))
          } : {},
          ...pending.attackActionId ? { attackActionId: pending.attackActionId } : {}
        }
      }
    });
    if (pending.attackActionId) {
      events.push(blockAttackActionEvent({
        actorId: source2.id,
        attackActionId: pending.attackActionId,
        resolutionId: nextResolutionId,
        obligations: obligationIds
      }));
    }
  } else {
    if (pending.attackActionId) {
      events.push(...attackResolutionFinishedEvents({
        attackAction: world.attackActions[pending.attackActionId],
        resolutionId: pending.id,
        actorId: source2.id,
        obligations: obligationIds
      }));
    }
    const invalidConcentrationIds = new Set(events.flatMap((event) => event.payload.type === "ConcentrationCleared" ? [event.payload.concentrationId] : []));
    events.push(...followUpOpenedEvents({
      world,
      commandId: command.commandId,
      followUps,
      env,
      invalidConcentrationIds
    }));
  }
  return events;
}
function resolvePendingAttack(world, command, catalog, env) {
  const pending = world.pendingResolution;
  if (!pending || pending.type !== "attack_reaction") {
    return rejected4(world, "NoPendingResolution", "There is no attack reaction to resolve");
  }
  if (pending.id !== command.resolutionId || pending.request.id !== command.requestId) {
    return rejected4(world, "StaleDecision", "Decision does not match the active request");
  }
  if (pending.targetActorId !== command.actorId || pending.request.actorId !== command.actorId) {
    return rejected4(world, "InvalidDecision", "Only the attacked actor can resolve this reaction");
  }
  if (command.response.kind !== "reaction") {
    return rejected4(world, "InvalidDecision", "An attack reaction requires a reaction response");
  }
  if (command.response.actionId === null && command.response.spell !== void 0) {
    return rejected4(world, "InvalidDecision", "A declined reaction cannot select a spell source");
  }
  const source2 = world.actors[pending.sourceActorId];
  const target2 = world.actors[pending.targetActorId];
  let sourceForAttack = source2;
  let pendingWeapon = pending.weaponCardId ? actorCard(source2, pending.weaponCardId) : void 0;
  if (pending.pactBladeProjection) {
    if (pending.actionId !== SYSTEM_ACTION_IDS.weaponAttack || pending.weaponHand !== pending.pactBladeProjection.weaponHand || pending.weaponCardId !== pending.pactBladeProjection.weaponCardId) {
      return rejected4(world, "InvalidDecision", "Attack reaction lost its Pact Blade continuation identity");
    }
    const persisted = persistedPactBladeExecution({
      world,
      catalog,
      source: source2,
      commandId: pending.openedByCommandId,
      projection: pending.pactBladeProjection
    });
    if ("issue" in persisted) return rejected4(world, "InvalidDecision", persisted.issue);
    sourceForAttack = persisted.actor;
    pendingWeapon = persisted.card;
  } else if (pending.weaponHand === void 0 !== (pending.weaponCardId === void 0) || pending.weaponHand && (!pendingWeapon || pendingWeapon.type !== "weapon" || source2.runtime.equipment[pending.weaponHand === "main" ? "main_hand" : "off_hand"] !== pendingWeapon.id)) {
    return rejected4(world, "InvalidDecision", "Attack reaction lost its exact equipped weapon");
  }
  const pendingWeaponRange = pendingWeapon ? weaponRanges(pendingWeapon, pending.facts.distanceFt) : null;
  if (pendingWeapon && !pendingWeaponRange) {
    return rejected4(world, "InvalidDecision", "Attack reaction weapon profile or range is no longer valid");
  }
  const baseAttack = pending.actionId === SYSTEM_ACTION_IDS.weaponAttack ? pending.weaponHand && pendingWeapon ? cleaveWindowFor({
    actor: sourceForAttack,
    weaponCardId: pendingWeapon.id,
    committedByCommandId: pending.openedByCommandId
  }) ? cleaveWeaponAttackAction(sourceForAttack, pending.weaponHand) : weaponAttackAction(pending.weaponHand, pendingWeaponRange.kind) : CORE_WEAPON_ATTACK : pending.actionId === SYSTEM_ACTION_IDS.lightExtraAttack ? pending.weaponHand && pendingWeapon ? lightWeaponExtraAttackAction(
    sourceForAttack,
    pending.weaponHand,
    pendingWeaponRange.kind,
    selectedWeaponUsesMastery(sourceForAttack, pendingWeapon.id, "nick") ? "attack_action" : "bonus_action"
  ) : null : pending.actionId === SYSTEM_ACTION_IDS.unarmedDamage ? CORE_UNARMED_DAMAGE : catalog.getAction(pending.actionId) ?? familiarAttackRuleAction(sourceForAttack, pending.actionId);
  const attack = baseAttack && pending.pactBladeProjection ? pactBladeWeaponAttackAction(baseAttack, pending.pactBladeProjection) : baseAttack;
  if (!attack) return rejected4(world, "ActionNotFound", `Unknown action ${pending.actionId}`);
  const attackDefinitionIssue = actionDefinitionIssue(attack);
  if (attackDefinitionIssue) return rejected4(world, "InvalidActionDefinition", attackDefinitionIssue);
  let targetRuntime = target2.runtime;
  let reactionEvents = [];
  let selectedReaction;
  let selectedReactionSpell;
  const selectedId = command.response.actionId;
  if (selectedId !== null) {
    if (!pending.request.options.some((option) => option.actionId === selectedId)) {
      return rejected4(world, "InvalidDecision", `Reaction ${selectedId} was not offered`);
    }
    if (!target2.capabilities.actionIds.includes(selectedId)) {
      return rejected4(world, "ActionNotGranted", `Actor ${target2.id} does not own reaction ${selectedId}`);
    }
    const reaction = catalog.getAction(selectedId);
    if (!reaction || !hasReactionTrigger(reaction, "hit_by_attack")) {
      return rejected4(world, "InvalidDecision", `Reaction ${selectedId} is no longer valid for this trigger`);
    }
    const definitionIssue = actionDefinitionIssue(reaction);
    if (definitionIssue) return rejected4(world, "InvalidActionDefinition", definitionIssue);
    const declarationIssue = spellDeclarationIssue(reaction);
    if (declarationIssue) return rejected4(world, "InvalidSpellDeclaration", declarationIssue);
    if (deniedCapabilities(targetRuntime, target2.passives ?? []).has("reaction")) {
      return rejected4(world, "CapabilityDenied", `${target2.id} cannot take reactions in its current state`);
    }
    const preparedReaction = prepareReactionExecution(target2, reaction, command.response.spell);
    if (preparedReaction.status === "rejected") {
      return rejected4(world, preparedReaction.code, preparedReaction.message);
    }
    const payable = canPay(targetRuntime, activationCost(preparedReaction.action));
    if (!payable.ok) {
      return rejected4(world, "InsufficientResources", `Missing reaction resources: ${payable.missing.join(", ")}`);
    }
    selectedReaction = preparedReaction.action;
    selectedReactionSpell = preparedReaction.spell;
    const reactionResult = executeAction(targetRuntime, preparedReaction.action.mechanics, {
      ...actionContext(target2, env, void 0, void 0, void 0, selectedReactionSpell),
      spell: selectedReactionSpell
    });
    targetRuntime = reactionResult.state;
    reactionEvents = reactionResult.events;
  }
  const targetAfterReaction = { ...target2, runtime: targetRuntime };
  const resumed = executeAction(sourceForAttack.runtime, withoutActivationCost(attack.mechanics), {
    ...actionContext(sourceForAttack, env, targetAfterReaction, targetRuntime, pending.facts, pending.spell),
    ...pending.attackActionId ? { attackActionId: pending.attackActionId } : {},
    attackCommandId: pending.openedByCommandId,
    choices: pending.choices,
    spell: pending.spell,
    forcedAttackRoll: pending.attackRoll,
    deferTargetSaves: true,
    ...worldActionPrimitive(attack) ? { externalPrimitiveHandled: true } : {}
  });
  const armor = resolveTemporaryHpMeleeRetaliationAfterAttack({
    world,
    attacker: sourceForAttack,
    defender: targetAfterReaction,
    attackerAfter: resumed.state,
    defenderAfter: resumed.targetState ?? targetRuntime,
    action: attack,
    attackEvents: resumed.events,
    env
  });
  const sourceAfter = pending.pactBladeProjection ? withoutPactBladeEquipmentProjection(armor.attackerAfter, source2.runtime) : armor.attackerAfter;
  const finalTargetRuntime = armor.defenderAfter ?? targetRuntime;
  const obligations = [.../* @__PURE__ */ new Set([
    ...actionObligationIds(
      attack,
      "system:attack-resolution",
      "system:reaction-window",
      "system:pending-resolution"
    ),
    ...selectedReaction ? actionObligationIds(selectedReaction) : [],
    ...armor.retaliationEvents.length ? ["system:temporary-hp-melee-retaliation", "system:retaliation"] : [],
    ...armor.retaliationSourceEntityIds.map((sourceId) => `entity:${sourceId}`)
  ])];
  const events = [];
  events.push({
    sourceActorId: target2.id,
    obligationIds: obligations,
    payload: {
      type: "DecisionRecorded",
      resolutionId: pending.id,
      requestId: pending.request.id,
      actorId: target2.id,
      response: command.response
    }
  });
  if (selectedReaction) {
    events.push(actionDeclaredEvent({
      actorId: target2.id,
      action: selectedReaction,
      targetIds: [target2.id],
      timing: "reaction",
      spell: selectedReactionSpell,
      obligationIds: obligations
    }));
  }
  events.push(...actionStateEvents({
    world,
    commandId: pending.openedByCommandId,
    source: sourceForAttack,
    action: attack,
    sourceAfter,
    target: target2,
    targetAfter: finalTargetRuntime,
    obligations
  }));
  if (reactionEvents.length) {
    events.push(...engineTrace(target2.id, [target2.id], reactionEvents, obligations));
  }
  events.push(...engineTrace(source2.id, [target2.id], relabelAttackRolls(
    resumed.events,
    selectedId ? "\u0410\u0442\u0430\u043A\u0430 \u2014 \u043F\u043E\u0441\u043B\u0435 \u0440\u0435\u0430\u043A\u0446\u0438\u0438" : "\u0410\u0442\u0430\u043A\u0430"
  ), obligations));
  events.push(...engineTrace(target2.id, [source2.id], armor.retaliationEvents, obligations, {
    sourceActorId: target2.id,
    facts: { trigger: "temporary_hp_melee_retaliation" }
  }));
  events.push({
    sourceActorId: target2.id,
    obligationIds: obligations,
    payload: { type: "ResolutionClosed", resolutionId: pending.id }
  });
  if (pending.attackActionId) {
    const attackAction = world.attackActions[pending.attackActionId];
    if (!attackAction || attackAction.blockedByResolutionId !== pending.id) {
      return rejected4(world, "InvalidDecision", "Attack reaction lost its canonical Attack-action ledger");
    }
    events.push({
      sourceActorId: source2.id,
      obligationIds: [...obligations, "system:attack-action"],
      payload: {
        type: "AttackActionUnblocked",
        attackActionId: attackAction.id,
        resolutionId: pending.id
      }
    });
    if (attackAction.sequence.attacksRemaining === 0) {
      events.push({
        sourceActorId: source2.id,
        obligationIds: [...obligations, "system:attack-action"],
        payload: { type: "AttackActionClosed", attackActionId: attackAction.id, reason: "completed" }
      });
    }
  }
  events.push(...attackFollowUpEvents({
    world,
    commandId: command.commandId,
    source: sourceForAttack,
    sourceAfter,
    target: target2,
    targetAfter: finalTargetRuntime,
    action: attack,
    deferred: resumed.deferredTargetSaves,
    env,
    obligations
  }));
  return events;
}
function resolveMagicMissileReaction(world, command, catalog, env) {
  const pending = world.pendingResolution;
  if (!pending || pending.type !== "magic_missile_reaction") {
    return rejected4(world, "NoPendingResolution", "There is no Magic Missile reaction to resolve");
  }
  if (pending.id !== command.resolutionId || pending.request.id !== command.requestId) {
    return rejected4(world, "StaleDecision", "Decision does not match the active request");
  }
  if (pending.targetActorId !== command.actorId || pending.request.actorId !== command.actorId) {
    return rejected4(world, "InvalidDecision", "Only the targeted actor can resolve this reaction");
  }
  if (command.response.kind !== "reaction") {
    return rejected4(world, "InvalidDecision", "Magic Missile requires a reaction response");
  }
  if (command.response.actionId === null && command.response.spell !== void 0) {
    return rejected4(world, "InvalidDecision", "A declined reaction cannot select a spell source");
  }
  const source2 = world.actors[pending.sourceActorId];
  const target2 = world.actors[pending.targetActorId];
  const action = catalog.getAction(pending.actionId);
  const spec = action ? magicMissileSpec(action) : null;
  if (!source2 || !target2) return rejected4(world, "ActorNotFound", "Magic Missile continuation actor is missing");
  if (!action) return rejected4(world, "ActionNotFound", `Unknown action ${pending.actionId}`);
  const expectedDartCount = spec && typeof pending.spell.castLevel === "number" ? magicMissileDartCount(spec, pending.spell.castLevel) : null;
  if (!spec || expectedDartCount === null || pending.dartTargetIds.length !== expectedDartCount) {
    return rejected4(world, "InvalidDecision", "Magic Missile continuation metadata is inconsistent");
  }
  if (pending.request.trigger.type !== "targeted_by_magic_missile" || pending.request.trigger.actionId !== action.id || pending.request.trigger.sourceActorId !== source2.id || pending.request.trigger.dartCount !== pending.dartTargetIds.filter((targetId) => targetId === pending.targetActorId).length) {
    return rejected4(world, "InvalidDecision", "Magic Missile reaction trigger is inconsistent");
  }
  const uniqueTargetIds = pending.dartTargetIds.filter((targetId, index, all) => all.indexOf(targetId) === index);
  if (pending.targets.length !== uniqueTargetIds.length || pending.targets.some((entry, index) => entry.targetActorId !== uniqueTargetIds[index])) {
    return rejected4(world, "InvalidDecision", "Magic Missile target snapshots are inconsistent");
  }
  const reactionTargetIds = [pending.targetActorId, ...pending.remainingReactions.map((entry) => entry.targetActorId)];
  if (new Set(reactionTargetIds).size !== reactionTargetIds.length || reactionTargetIds.some((targetId) => !uniqueTargetIds.includes(targetId)) || reactionTargetIds.some((targetId) => pending.protectedTargetIds.includes(targetId))) {
    return rejected4(world, "InvalidDecision", "Magic Missile reaction queue is inconsistent");
  }
  if (new Set(pending.protectedTargetIds).size !== pending.protectedTargetIds.length || pending.protectedTargetIds.some((targetId) => !uniqueTargetIds.includes(targetId)) || pending.protectedTargetIds.some((targetId) => {
    const protectedActor = world.actors[targetId];
    return !protectedActor || !hasMagicMissileImmunity(protectedActor);
  })) {
    return rejected4(world, "InvalidDecision", "Magic Missile protected targets are inconsistent");
  }
  for (const entry of pending.targets) {
    if (!world.actors[entry.targetActorId]) return rejected4(world, "ActorNotFound", `Unknown target ${entry.targetActorId}`);
    const issue = factsIssue(action, entry.targetActorId, entry.facts);
    if (issue) return rejected4(world, "InvalidDecision", issue[1]);
  }
  let selectedReaction;
  let selectedReactionSpell;
  let selectedRuntime;
  let reactionEngineEvents = [];
  const selectedId = command.response.actionId;
  if (selectedId !== null) {
    if (!pending.request.options.some((option) => option.actionId === selectedId)) {
      return rejected4(world, "InvalidDecision", `Reaction ${selectedId} was not offered`);
    }
    if (!target2.capabilities.actionIds.includes(selectedId)) {
      return rejected4(world, "ActionNotGranted", `Actor ${target2.id} does not own reaction ${selectedId}`);
    }
    const reaction = catalog.getAction(selectedId);
    if (!reaction || !hasReactionTrigger(reaction, "targeted_by_magic_missile") || !grantsMagicMissileImmunity(reaction)) {
      return rejected4(world, "InvalidDecision", `Reaction ${selectedId} cannot block Magic Missile`);
    }
    const definitionIssue = actionDefinitionIssue(reaction);
    if (definitionIssue) return rejected4(world, "InvalidActionDefinition", definitionIssue);
    const declarationIssue = spellDeclarationIssue(reaction);
    if (declarationIssue) return rejected4(world, "InvalidSpellDeclaration", declarationIssue);
    if (deniedCapabilities(target2.runtime, target2.passives ?? []).has("reaction")) {
      return rejected4(world, "CapabilityDenied", `${target2.id} cannot take reactions in its current state`);
    }
    const preparedReaction = prepareReactionExecution(target2, reaction, command.response.spell);
    if (preparedReaction.status === "rejected") {
      return rejected4(world, preparedReaction.code, preparedReaction.message);
    }
    const payable = canPay(target2.runtime, activationCost(preparedReaction.action));
    if (!payable.ok) {
      return rejected4(world, "InsufficientResources", `Missing reaction resources: ${payable.missing.join(", ")}`);
    }
    selectedReaction = preparedReaction.action;
    selectedReactionSpell = preparedReaction.spell;
    const reactionResult = executeAction(target2.runtime, preparedReaction.action.mechanics, {
      ...actionContext(target2, env, void 0, void 0, void 0, selectedReactionSpell),
      spell: selectedReactionSpell
    });
    selectedRuntime = reactionResult.state;
    if (!hasMagicMissileImmunity({ ...target2, runtime: selectedRuntime })) {
      return rejected4(world, "InvalidActionDefinition", `Reaction ${selectedId} did not create Magic Missile immunity`);
    }
    reactionEngineEvents = reactionResult.events;
  }
  const protectedTargetIds = selectedReaction ? [.../* @__PURE__ */ new Set([...pending.protectedTargetIds, target2.id])] : [...pending.protectedTargetIds];
  const obligations = [.../* @__PURE__ */ new Set([
    ...actionObligationIds(
      action,
      "system:magic-missile",
      "system:reaction-window",
      "system:pending-resolution"
    ),
    ...selectedReaction ? actionObligationIds(selectedReaction) : []
  ])];
  const events = [{
    sourceActorId: target2.id,
    obligationIds: obligations,
    payload: {
      type: "DecisionRecorded",
      resolutionId: pending.id,
      requestId: pending.request.id,
      actorId: target2.id,
      response: command.response
    }
  }];
  if (selectedReaction) {
    events.push(actionDeclaredEvent({
      actorId: target2.id,
      action: selectedReaction,
      targetIds: [target2.id],
      timing: "reaction",
      spell: selectedReactionSpell,
      facts: {
        trigger: "targeted_by_magic_missile",
        dartCount: pending.request.trigger.dartCount
      },
      obligationIds: obligations
    }));
  }
  if (reactionEngineEvents.length) {
    events.push(...engineTrace(target2.id, [target2.id], reactionEngineEvents, obligations, {
      facts: { trigger: "targeted_by_magic_missile" }
    }));
  }
  events.push({
    sourceActorId: target2.id,
    obligationIds: obligations,
    payload: { type: "ResolutionClosed", resolutionId: pending.id }
  });
  const [nextReaction, ...remainingReactions] = pending.remainingReactions;
  if (nextReaction) {
    if (selectedReaction && selectedRuntime) {
      events.push(...actionStateEvents({
        world,
        commandId: command.commandId,
        source: target2,
        action: selectedReaction,
        sourceAfter: selectedRuntime,
        obligations
      }));
    }
    events.push(magicMissileReactionOpenedEvent({
      world,
      actionCommandId: pending.openedByCommandId,
      sourceActorId: source2.id,
      actionId: action.id,
      spell: pending.spell,
      dartTargetIds: pending.dartTargetIds,
      targets: pending.targets,
      protectedTargetIds,
      current: nextReaction,
      remainingReactions,
      env,
      obligations
    }));
    return events;
  }
  const runtimeOverrides = /* @__PURE__ */ new Map();
  if (selectedRuntime) runtimeOverrides.set(target2.id, selectedRuntime);
  events.push(...magicMissileDamageEvents({
    world,
    actionCommandId: pending.openedByCommandId,
    sourceActorId: source2.id,
    ...source2.id === target2.id && selectedRuntime ? { sourceRuntime: selectedRuntime } : {},
    action,
    policy: spec,
    dartTargetIds: pending.dartTargetIds,
    targets: pending.targets,
    protectedTargetIds,
    runtimeOverrides,
    env,
    obligations,
    followUpCommandId: command.commandId
  }));
  return events;
}
function masteryContinuationIssue(pending) {
  if (typeof pending.mastery.sourceEntityId !== "string" || !pending.mastery.sourceEntityId.trim()) {
    return "Mastery continuation has no source entity";
  }
  const effect = pending.mastery.effect;
  if (effect.resolution !== "save" || String(effect.who ?? "target") !== "target") {
    return "Mastery continuation must contain one target saving throw";
  }
  if (String(effect.ability ?? "dex") !== pending.save.ability || pending.request.ability !== pending.save.ability || pending.request.dc !== pending.save.dc) {
    return "Mastery continuation save metadata is inconsistent";
  }
  if (!Number.isInteger(pending.save.dc) || pending.save.dc < 1 || pending.save.dc > 30) {
    return "Mastery continuation has an invalid saving throw DC";
  }
  return null;
}
function resolveMasterySave(world, command, env) {
  const pending = world.pendingResolution;
  if (!pending || pending.type !== "mastery_save") {
    return rejected4(world, "NoPendingResolution", "There is no weapon mastery save to resolve");
  }
  if (pending.id !== command.resolutionId || pending.request.id !== command.requestId) {
    return rejected4(world, "StaleDecision", "Decision does not match the active request");
  }
  if (pending.targetActorId !== command.actorId || pending.request.actorId !== command.actorId) {
    return rejected4(world, "InvalidDecision", "Only the mastery target can resolve this saving throw");
  }
  if (command.response.kind !== "roll") {
    return rejected4(world, "InvalidDecision", "A weapon mastery saving throw requires a roll response");
  }
  const issue = masteryContinuationIssue(pending);
  if (issue) return rejected4(world, "InvalidDecision", issue);
  const source2 = world.actors[pending.sourceActorId];
  const target2 = world.actors[pending.targetActorId];
  if (!source2 || !target2) return rejected4(world, "ActorNotFound", "Mastery continuation actor is missing");
  const ability = pending.request.ability;
  const collected = collectRollModifiers(target2.runtime, target2.passives ?? [], {
    roll: "saving_throw",
    filter: { ability },
    formulaCtx: actorFormulaContext(target2.character),
    evalCtx: {
      state: target2.runtime,
      activeConditions: activeConditionsOf(target2.runtime),
      savedConditions: new Set(pending.request.avoidsConditions)
    }
  });
  const proficient = target2.character.saveProficiencies?.includes(ability);
  const manual = manualDecisionRng(command);
  let roll;
  try {
    roll = rollD20({
      advantage: collected.advantage,
      modifiers: [
        { value: target2.character.abilityMods[ability] ?? 0, source: ABILITY_LABEL3[ability] },
        ...proficient ? [{ value: target2.character.profBonus, source: "\u0411\u041C" }] : [],
        ...collected.modifiers
      ],
      target: { type: "dc", value: pending.request.dc },
      rules: collected.rules,
      rng: manual?.rng ?? env.rng
    });
    manual?.assertExhausted();
  } catch (error) {
    return rejected4(world, "InvalidDecision", error instanceof Error ? error.message : "Invalid mastery save");
  }
  const masteryAction = {
    id: pending.mastery.sourceEntityId,
    name: pending.mastery.name,
    kind: "nonSpell",
    sourceEntityIds: [pending.mastery.sourceEntityId],
    mechanics: {
      name: pending.mastery.name,
      activation: { mode: "triggered", cost: [] },
      effects: [pending.mastery.effect]
    }
  };
  const result = executeAction(source2.runtime, masteryAction.mechanics, {
    ...actionContext(source2, env, target2, target2.runtime),
    ...pending.mastery.weaponMod == null ? {} : { weaponMod: pending.mastery.weaponMod },
    forceSaveOutcome: roll.outcome === "success" ? "success" : "fail"
  });
  const obligations = masterySaveObligationIds(pending);
  const events = engineTrace(target2.id, [], [{
    type: "roll",
    label: `${pending.mastery.name}: \u0441\u043F\u0430\u0441\u0431\u0440\u043E\u0441\u043E\u043A ${ABILITY_LABEL3[ability]}`,
    roll: { ...roll, kind: "save" }
  }], obligations);
  events.push(...actionStateEvents({
    world,
    commandId: command.commandId,
    source: source2,
    action: masteryAction,
    sourceAfter: result.state,
    target: target2,
    targetAfter: result.targetState,
    obligations
  }));
  events.push(...engineTrace(source2.id, [target2.id], result.events, obligations));
  events.push({
    sourceActorId: target2.id,
    obligationIds: obligations,
    payload: {
      type: "DecisionRecorded",
      resolutionId: pending.id,
      requestId: pending.request.id,
      actorId: target2.id,
      response: command.response
    }
  });
  events.push({
    sourceActorId: target2.id,
    obligationIds: obligations,
    payload: { type: "ResolutionClosed", resolutionId: pending.id }
  });
  const masteryDamageSave = concentrationSaveFollowUp({
    world,
    actor: target2,
    actorAfter: result.targetState,
    obligations
  });
  const followUps = [
    ...pending.followUps ?? [],
    ...masteryDamageSave ? [masteryDamageSave] : []
  ];
  const invalidConcentrationIds = new Set(events.flatMap((event) => event.payload.type === "ConcentrationCleared" ? [event.payload.concentrationId] : []));
  events.push(...followUpOpenedEvents({
    world,
    commandId: command.commandId,
    followUps,
    env,
    invalidConcentrationIds
  }));
  return events;
}
function resolveConcentrationSave(world, command, env) {
  const pending = world.pendingResolution;
  if (!pending || pending.type !== "concentration_save") {
    return rejected4(world, "NoPendingResolution", "There is no concentration save to resolve");
  }
  if (pending.id !== command.resolutionId || pending.request.id !== command.requestId) {
    return rejected4(world, "StaleDecision", "Decision does not match the active request");
  }
  if (pending.actorId !== command.actorId || pending.request.actorId !== command.actorId) {
    return rejected4(world, "InvalidDecision", "Only the concentrating actor can resolve this save");
  }
  if (command.response.kind !== "roll") {
    return rejected4(world, "InvalidDecision", "A concentration save requires a roll response");
  }
  const concentration = world.concentrations[pending.actorId];
  if (!concentration || concentration.id !== pending.concentrationId) {
    return rejected4(world, "StaleDecision", "The referenced concentration is no longer active");
  }
  const actor2 = world.actors[pending.actorId];
  const collected = collectRollModifiers(actor2.runtime, actor2.passives ?? [], {
    roll: "saving_throw",
    filter: { ability: "con", reason: "maintain_concentration" },
    formulaCtx: actorFormulaContext(actor2.character)
  });
  const proficient = actor2.character.saveProficiencies?.includes("con");
  const modifiers = [
    { value: actor2.character.abilityMods.con ?? 0, source: ABILITY_LABEL3.con },
    ...proficient ? [{ value: actor2.character.profBonus, source: "\u0411\u041C" }] : [],
    ...collected.modifiers
  ];
  const manual = manualDecisionRng(command);
  let roll;
  try {
    roll = rollD20({
      advantage: collected.advantage,
      modifiers,
      target: { type: "dc", value: pending.request.dc },
      rules: collected.rules,
      rng: manual?.rng ?? env.rng
    });
    manual?.assertExhausted();
  } catch (error) {
    return rejected4(world, "InvalidDecision", error instanceof Error ? error.message : "Invalid concentration roll");
  }
  const obligations = ["system:concentration-damage-save", "system:pending-resolution"];
  const events = engineTrace(actor2.id, [], [{
    type: "roll",
    label: `\u041A\u043E\u043D\u0446\u0435\u043D\u0442\u0440\u0430\u0446\u0438\u044F (\u0421\u041B ${pending.request.dc})`,
    roll: { ...roll, kind: "save" }
  }], obligations);
  events.push({
    sourceActorId: actor2.id,
    obligationIds: obligations,
    payload: {
      type: "DecisionRecorded",
      resolutionId: pending.id,
      requestId: pending.request.id,
      actorId: actor2.id,
      response: command.response
    }
  });
  if (roll.outcome !== "success") {
    const linkedIdsByActor = /* @__PURE__ */ new Map();
    for (const link of concentration.effectLinks) {
      const ids = linkedIdsByActor.get(link.actorId) ?? /* @__PURE__ */ new Set();
      ids.add(link.effectId);
      linkedIdsByActor.set(link.actorId, ids);
    }
    for (const [linkedActorId, linkedEffectIds] of [...linkedIdsByActor.entries()].sort(([left], [right]) => left.localeCompare(right))) {
      const linkedActor = world.actors[linkedActorId];
      if (!linkedActor) continue;
      const after = {
        ...linkedActor.runtime,
        activeEffects: linkedActor.runtime.activeEffects.filter((effect) => !linkedEffectIds.has(effect.id))
      };
      events.push(...runtimeTransition(actor2.id, linkedActor.id, linkedActor.runtime, after, "action", obligations));
    }
    events.push(...concentrationWorldObjectCleanup(world, concentration, obligations), {
      sourceActorId: actor2.id,
      obligationIds: obligations,
      payload: {
        type: "ConcentrationCleared",
        sourceActorId: actor2.id,
        concentrationId: concentration.id,
        reason: "failed_save"
      }
    });
  }
  events.push({
    sourceActorId: actor2.id,
    obligationIds: obligations,
    payload: { type: "ResolutionClosed", resolutionId: pending.id }
  });
  events.push(...followUpOpenedEvents({
    world,
    commandId: command.commandId,
    followUps: pending.followUps ?? [],
    env,
    ...roll.outcome === "success" ? {} : {
      invalidConcentrationIds: /* @__PURE__ */ new Set([pending.concentrationId])
    }
  }));
  return events;
}
function resolveHazardSave(world, command, env) {
  const pending = world.pendingResolution;
  if (!pending || pending.type !== "hazard_save") {
    return rejected4(world, "NoPendingResolution", "There is no hazard save to resolve");
  }
  if (pending.id !== command.resolutionId || pending.request.id !== command.requestId) {
    return rejected4(world, "StaleDecision", "Decision does not match the active request");
  }
  if (pending.targetActorId !== command.actorId || pending.request.actorId !== command.actorId) {
    return rejected4(world, "InvalidDecision", "Only the affected actor can resolve this hazard save");
  }
  if (command.response.kind !== "roll") {
    return rejected4(world, "InvalidDecision", "A hazard saving throw requires a roll response");
  }
  const definitionIssue = hazardDefinitionIssue(pending.hazard);
  if (definitionIssue) return rejected4(world, "InvalidHazardDefinition", definitionIssue);
  const target2 = world.actors[pending.targetActorId];
  const ability = pending.request.ability;
  const collected = collectRollModifiers(target2.runtime, target2.passives ?? [], {
    roll: "saving_throw",
    filter: { ability },
    formulaCtx: actorFormulaContext(target2.character),
    evalCtx: {
      state: target2.runtime,
      activeConditions: activeConditionsOf(target2.runtime),
      savedConditions: new Set(pending.request.avoidsConditions)
    }
  });
  const proficient = target2.character.saveProficiencies?.includes(ability);
  const manual = manualDecisionRng(command);
  let roll;
  try {
    roll = rollD20({
      advantage: collected.advantage,
      modifiers: [
        { value: target2.character.abilityMods[ability] ?? 0, source: ABILITY_LABEL3[ability] },
        ...proficient ? [{ value: target2.character.profBonus, source: "\u0411\u041C" }] : [],
        ...collected.modifiers
      ],
      target: { type: "dc", value: pending.request.dc },
      rules: collected.rules,
      rng: manual?.rng ?? env.rng
    });
    manual?.assertExhausted();
  } catch (error) {
    return rejected4(world, "InvalidDecision", error instanceof Error ? error.message : "Invalid hazard save");
  }
  const hazard = pending.hazard;
  const sourceActorId = hazardSourceId(hazard);
  const obligations = hazardObligationIds(hazard);
  const result = executeAction(target2.runtime, {
    name: hazard.name,
    activation: { mode: "passive", cost: [] },
    effects: [{
      resolution: "save",
      who: "self",
      ability: hazard.save.ability,
      dc: String(hazard.save.dc),
      on_fail: hazard.onFailure,
      on_success: hazard.onSuccess ?? []
    }]
  }, {
    ...actionContext(target2, env),
    selfId: sourceActorId,
    forceSaveOutcome: roll.outcome === "success" ? "success" : "fail"
  });
  const events = [];
  events.push(...engineTrace(target2.id, [], [{
    type: "roll",
    label: `${hazard.name}: \u0441\u043F\u0430\u0441\u0431\u0440\u043E\u0441\u043E\u043A ${ABILITY_LABEL3[ability]}`,
    roll: { ...roll, kind: "save" }
  }], obligations));
  events.push(...runtimeTransition(
    sourceActorId,
    target2.id,
    target2.runtime,
    result.state,
    "hazard",
    obligations
  ));
  events.push(...engineTrace(sourceActorId, [target2.id], result.events, obligations));
  events.push({
    sourceActorId: target2.id,
    obligationIds: obligations,
    payload: {
      type: "DecisionRecorded",
      resolutionId: pending.id,
      requestId: pending.request.id,
      actorId: target2.id,
      response: command.response
    }
  });
  events.push({
    sourceActorId: target2.id,
    obligationIds: obligations,
    payload: { type: "ResolutionClosed", resolutionId: pending.id }
  });
  events.push(...concentrationSaveOpenedEvents({
    world,
    commandId: command.commandId,
    actor: target2,
    actorAfter: result.state,
    env,
    obligations
  }));
  return events;
}
function studyWorldObject(world, command, env) {
  const actor2 = world.actors[command.actorId];
  const object5 = world.objects[command.objectId];
  if (!object5) {
    return rejected4(world, "WorldObjectNotFound", `Unknown world object ${command.objectId}`);
  }
  if (!object5.illusion) {
    return rejected4(world, "InvalidFacts", `${command.objectId} is not an illusion`);
  }
  const factsIssue2 = worldObjectFactsIssue(command.facts);
  if (factsIssue2) return rejected4(world, "InvalidFacts", factsIssue2);
  if (object5.illusion.form === "image" && !command.facts.lineOfSight) {
    return rejected4(world, "LineOfSightBlocked", "An image illusion must be visible to be studied");
  }
  if (deniedCapabilities(actor2.runtime, actor2.passives ?? []).has("action")) {
    return rejected4(world, "CapabilityDenied", `${actor2.id} cannot take the Study action`);
  }
  const cost = [{ resource: "action" }];
  const payable = canPay(actor2.runtime, cost);
  if (!payable.ok) {
    return rejected4(world, "InsufficientResources", `Missing resources: ${payable.missing.join(", ")}`);
  }
  const paid = pay(actor2.runtime, cost);
  const skill = "investigation";
  const proficient = actor2.character.skillProficiencies?.includes(skill);
  const expertise = actor2.character.skillExpertise?.includes(skill);
  const collected = collectRollModifiers(paid.state, actor2.passives ?? [], {
    roll: "ability_check",
    filter: { ability: "int", skill },
    formulaCtx: actorFormulaContext(actor2.character)
  });
  const proficiency = expertise ? actor2.character.profBonus * 2 : proficient ? actor2.character.profBonus : 0;
  const roll = rollD20({
    advantage: collected.advantage,
    modifiers: [
      { value: actor2.character.abilityMods.int ?? 0, source: ABILITY_LABEL3.int },
      ...proficiency ? [{ value: proficiency, source: expertise ? "\u042D\u043A\u0441\u043F\u0435\u0440\u0442\u0438\u0437\u0430" : "\u0411\u041C" }] : [],
      ...collected.modifiers
    ],
    target: { type: "dc", value: object5.illusion.spellSaveDc },
    rules: collected.rules,
    rng: env.rng
  });
  const checkEvents = [
    ...paid.events,
    { type: "roll", label: "Study (Investigation)", roll: { ...roll, kind: "check" } }
  ];
  const after = consumeNextRollEffects(paid.state, "ability_check", checkEvents, {
    filter: { ability: "int", skill }
  });
  const mutation = studyMinorIllusion({
    objects: world.objects,
    objectId: object5.id,
    actorId: actor2.id,
    checkTotal: roll.total
  });
  const obligations = actionObligationIds(
    CORE_STUDY_WORLD_OBJECT_ACTION,
    "system:ability-check",
    "system:study-action",
    "system:minor-illusion"
  );
  const concentration = consumedConcentrationLifecycle({
    world,
    actingActorId: actor2.id,
    changedActorId: actor2.id,
    before: actor2.runtime,
    after,
    obligations
  });
  return [
    actionDeclaredEvent({
      actorId: actor2.id,
      action: CORE_STUDY_WORLD_OBJECT_ACTION,
      targetIds: [],
      timing: "active",
      facts: {
        objectId: object5.id,
        objectFacts: JSON.parse(JSON.stringify(command.facts))
      },
      obligationIds: obligations
    }),
    ...concentration.transitions,
    ...engineTrace(actor2.id, [], checkEvents, obligations, {
      facts: { objectId: object5.id, spellSaveDc: object5.illusion.spellSaveDc }
    }),
    ...concentration.lifecycle,
    ...worldObjectEvents(
      actor2.id,
      CORE_STUDY_WORLD_OBJECT_ACTION,
      mutation.events,
      "system:study-action",
      "system:minor-illusion"
    )
  ];
}
function physicallyInteractWorldObject(world, command) {
  const actor2 = world.actors[command.actorId];
  if (!world.objects[command.objectId]) {
    return rejected4(world, "WorldObjectNotFound", `Unknown world object ${command.objectId}`);
  }
  const factsIssue2 = worldObjectFactsIssue(command.facts);
  if (factsIssue2) return rejected4(world, "InvalidFacts", factsIssue2);
  if (command.facts.touched !== true || command.facts.distanceFt !== 0) {
    return rejected4(world, "InvalidFacts", "Physical interaction requires touched facts at distance 0");
  }
  if (deniedCapabilities(actor2.runtime, actor2.passives ?? []).has("action")) {
    return rejected4(world, "CapabilityDenied", `${actor2.id} cannot physically interact with the object`);
  }
  try {
    const mutation = physicallyRevealMinorIllusion({
      objects: world.objects,
      objectId: command.objectId,
      actorId: actor2.id
    });
    const obligations = actionObligationIds(
      CORE_PHYSICAL_WORLD_INTERACTION,
      "system:minor-illusion",
      "system:physical-interaction"
    );
    return [
      actionDeclaredEvent({
        actorId: actor2.id,
        action: CORE_PHYSICAL_WORLD_INTERACTION,
        targetIds: [],
        timing: "active",
        facts: {
          objectId: command.objectId,
          objectFacts: JSON.parse(JSON.stringify(command.facts))
        },
        obligationIds: obligations
      }),
      ...worldObjectEvents(
        actor2.id,
        CORE_PHYSICAL_WORLD_INTERACTION,
        mutation.events,
        "system:minor-illusion",
        "system:physical-interaction"
      )
    ];
  } catch (error) {
    return rejected4(
      world,
      "InvalidFacts",
      error instanceof Error ? error.message : "Invalid physical interaction"
    );
  }
}
function magicBlockingLayersIssue(layers) {
  if (!Array.isArray(layers)) return "Detect Magic blocking layers must be an array";
  const materials = ["stone", "common_metal", "lead", "wood", "dirt", "other"];
  for (const layer of layers) {
    if (!layer || typeof layer !== "object" || Array.isArray(layer)) {
      return "Detect Magic contains a malformed blocking layer";
    }
    const value = layer;
    if (!materials.includes(String(value.material ?? "")) || !Number.isFinite(value.thicknessInches) || Number(value.thicknessInches) < 0) {
      return "Detect Magic contains a malformed blocking layer";
    }
  }
  return null;
}
function revealMagicAura(world, command, catalog) {
  const actor2 = world.actors[command.actorId];
  const concentration = world.concentrations[actor2.id];
  if (!concentration || concentration.id !== command.concentrationId) {
    return rejected4(world, "InvalidActionTiming", "The actor is not maintaining this Detect Magic");
  }
  const detectMagic = catalog.getAction(concentration.actionId);
  if (!detectMagic) {
    return rejected4(world, "ActionNotFound", `Unknown concentration action ${concentration.actionId}`);
  }
  if (worldActionPrimitive(detectMagic) !== "detect_magic_world_sensing") {
    return rejected4(world, "InvalidActionDefinition", `${detectMagic.id} is not Detect Magic`);
  }
  const parsedPolicy = parseWorldSpellPolicy(detectMagic.mechanics);
  if (parsedPolicy.status !== "valid" || parsedPolicy.primitiveType !== "detect_magic_world_sensing") {
    return rejected4(
      world,
      "InvalidActionDefinition",
      `${detectMagic.id} has invalid Detect Magic policy${parsedPolicy.status === "invalid" ? `: ${parsedPolicy.issue}` : ""}`
    );
  }
  const detectMagicPolicy = parsedPolicy.policy;
  if (!command.observations || typeof command.observations !== "object" || Array.isArray(command.observations)) {
    return rejected4(world, "InvalidFacts", "Detect Magic requires an explicit observation map");
  }
  if (deniedCapabilities(actor2.runtime, actor2.passives ?? []).has("action")) {
    return rejected4(world, "CapabilityDenied", `${actor2.id} cannot take the Magic action`);
  }
  const cost = [{ resource: "action" }];
  const payable = canPay(actor2.runtime, cost);
  if (!payable.ok) {
    return rejected4(world, "InsufficientResources", `Missing resources: ${payable.missing.join(", ")}`);
  }
  const observationEvents = [];
  for (const objectId of Object.keys(command.observations).sort()) {
    const object5 = world.objects[objectId];
    if (!object5) {
      return rejected4(world, "WorldObjectNotFound", `Unknown world object ${objectId}`);
    }
    const input = command.observations[objectId];
    const factsIssue2 = worldObjectFactsIssue(input?.facts);
    if (factsIssue2) return rejected4(world, "InvalidFacts", `${objectId}: ${factsIssue2}`);
    const layerIssue = magicBlockingLayersIssue(input?.blockingLayers);
    if (layerIssue) return rejected4(world, "InvalidFacts", `${objectId}: ${layerIssue}`);
    const result = observeDetectMagic({
      object: object5,
      facts: input.facts,
      blockingLayers: input.blockingLayers,
      revealAura: true,
      policy: detectMagicPolicy,
      targeting: parsedPolicy.targeting
    });
    observationEvents.push({
      type: "WorldObjectObserved",
      objectId,
      actorId: actor2.id,
      observation: "detect_magic_aura",
      details: {
        ...result,
        facts: JSON.parse(JSON.stringify(input.facts)),
        blockingLayers: JSON.parse(JSON.stringify(input.blockingLayers))
      }
    });
  }
  const paid = pay(actor2.runtime, cost);
  const followUpAction = {
    id: "core.action.detect-magic-aura",
    name: "Detect Magic: reveal aura",
    kind: "nonSpell",
    sourceEntityIds: [.../* @__PURE__ */ new Set([
      "core:dnd5e-2024:action:magic",
      detectMagic.id,
      ...detectMagic.sourceEntityIds
    ])],
    mechanics: { activation: { mode: "active", cost } }
  };
  const obligations = actionObligationIds(
    detectMagic,
    "system:detect-magic",
    "system:magic-action",
    "system:world-object"
  );
  return [
    actionDeclaredEvent({
      actorId: actor2.id,
      action: followUpAction,
      targetIds: [],
      timing: "active",
      facts: {
        concentrationId: concentration.id,
        observations: JSON.parse(JSON.stringify(command.observations))
      },
      obligationIds: obligations
    }),
    ...runtimeTransition(actor2.id, actor2.id, actor2.runtime, paid.state, "action", obligations),
    ...engineTrace(actor2.id, [], paid.events, obligations),
    ...worldObjectEvents(
      actor2.id,
      detectMagic,
      observationEvents,
      "system:detect-magic",
      "system:magic-action"
    )
  ];
}
function moveActiveDancingLights(world, command, catalog) {
  const actor2 = world.actors[command.actorId];
  const concentration = world.concentrations[actor2.id];
  if (!concentration || concentration.id !== command.concentrationId) {
    return rejected4(world, "InvalidActionTiming", "The actor is not maintaining these Dancing Lights");
  }
  const action = catalog.getAction(concentration.actionId);
  if (!action) {
    return rejected4(world, "ActionNotFound", `Unknown concentration action ${concentration.actionId}`);
  }
  if (worldActionPrimitive(action) !== "dancing_lights_world") {
    return rejected4(world, "InvalidActionDefinition", `${action.id} is not Dancing Lights`);
  }
  const parsedPolicy = parseWorldSpellPolicy(action.mechanics);
  if (parsedPolicy.status !== "valid" || parsedPolicy.primitiveType !== "dancing_lights_world") {
    return rejected4(
      world,
      "InvalidActionDefinition",
      `${action.id} has invalid Dancing Lights policy${parsedPolicy.status === "invalid" ? `: ${parsedPolicy.issue}` : ""}`
    );
  }
  if (typeof command.groupId !== "string" || !command.groupId.trim() || !["scenario", "board", "gm_ruling"].includes(command.factsSource) || !Number.isInteger(command.boardRevision) || command.boardRevision < 0 || !Array.isArray(command.resultingFacts)) {
    return rejected4(world, "InvalidFacts", "Dancing Lights movement facts are malformed");
  }
  const group = Object.values(world.objects).filter((object5) => object5.dancingLight?.groupId === command.groupId);
  if (!group.length || group.some((object5) => object5.sourceActorId !== actor2.id || object5.sourceActionId !== action.id)) {
    return rejected4(world, "InvalidFacts", "Unknown source-owned Dancing Lights group");
  }
  if (deniedCapabilities(actor2.runtime, actor2.passives ?? []).has("bonus_action")) {
    return rejected4(world, "CapabilityDenied", `${actor2.id} cannot take a Bonus Action`);
  }
  const cost = [{ resource: "bonus_action" }];
  const payable = canPay(actor2.runtime, cost);
  if (!payable.ok) {
    return rejected4(world, "InsufficientResources", `Missing resources: ${payable.missing.join(", ")}`);
  }
  try {
    const result = moveDancingLights({
      objects: world.objects,
      sourceActorId: actor2.id,
      groupId: command.groupId,
      resultingFacts: command.resultingFacts,
      policy: parsedPolicy.policy,
      targeting: parsedPolicy.targeting
    });
    const paid = pay(actor2.runtime, cost);
    const followUpAction = {
      id: "core.bonus-action.dancing-lights-move",
      name: "Dancing Lights: move lights",
      kind: "nonSpell",
      sourceEntityIds: [.../* @__PURE__ */ new Set([
        "core:dnd5e-2024:bonus-action",
        action.id,
        ...action.sourceEntityIds
      ])],
      mechanics: { activation: { mode: "active", cost } }
    };
    const obligations = actionObligationIds(
      action,
      "system:dancing-lights",
      "system:bonus-action",
      "system:world-object",
      "system:concentration"
    );
    return [
      actionDeclaredEvent({
        actorId: actor2.id,
        action: followUpAction,
        targetIds: [],
        timing: "active",
        facts: {
          concentrationId: concentration.id,
          groupId: command.groupId,
          factsSource: command.factsSource,
          boardRevision: command.boardRevision,
          resultingFacts: JSON.parse(JSON.stringify(command.resultingFacts))
        },
        obligationIds: obligations
      }),
      ...runtimeTransition(actor2.id, actor2.id, actor2.runtime, paid.state, "action", obligations),
      ...engineTrace(actor2.id, [], paid.events, obligations),
      ...worldObjectEvents(actor2.id, action, result.events, "system:dancing-lights", "system:bonus-action")
    ];
  } catch (error) {
    return rejected4(
      world,
      "InvalidFacts",
      error instanceof Error ? error.message : "Invalid Dancing Lights movement"
    );
  }
}
function observeActivePoisonDisease(world, command, catalog) {
  const actor2 = world.actors[command.actorId];
  const concentration = world.concentrations[actor2.id];
  if (!concentration || concentration.id !== command.concentrationId) {
    return rejected4(
      world,
      "InvalidActionTiming",
      "The actor is not maintaining this Detect Poison and Disease"
    );
  }
  const action = catalog.getAction(concentration.actionId);
  if (!action) {
    return rejected4(world, "ActionNotFound", `Unknown concentration action ${concentration.actionId}`);
  }
  if (worldActionPrimitive(action) !== "detect_poison_disease_world") {
    return rejected4(
      world,
      "InvalidActionDefinition",
      `${action.id} is not Detect Poison and Disease`
    );
  }
  const parsedPolicy = parseWorldSpellPolicy(action.mechanics);
  if (parsedPolicy.status !== "valid" || parsedPolicy.primitiveType !== "detect_poison_disease_world") {
    return rejected4(
      world,
      "InvalidActionDefinition",
      `${action.id} has invalid Detect Poison and Disease policy${parsedPolicy.status === "invalid" ? `: ${parsedPolicy.issue}` : ""}`
    );
  }
  if (!command.observations || typeof command.observations !== "object" || Array.isArray(command.observations)) {
    return rejected4(world, "InvalidFacts", "Detect Poison and Disease requires an observation map");
  }
  const observations = [];
  for (const objectId of Object.keys(command.observations).sort()) {
    const object5 = world.objects[objectId];
    if (!object5) {
      return rejected4(world, "WorldObjectNotFound", `Unknown world object ${objectId}`);
    }
    const input = command.observations[objectId];
    const factsIssue2 = worldObjectFactsIssue(input?.facts);
    if (factsIssue2) return rejected4(world, "InvalidFacts", `${objectId}: ${factsIssue2}`);
    const layerIssue = magicBlockingLayersIssue(input?.blockingLayers);
    if (layerIssue) return rejected4(world, "InvalidFacts", `${objectId}: ${layerIssue}`);
    const result = observeDetectPoisonAndDisease({
      object: object5,
      facts: input.facts,
      blockingLayers: input.blockingLayers,
      policy: parsedPolicy.policy,
      targeting: parsedPolicy.targeting
    });
    observations.push({
      type: "WorldObjectObserved",
      objectId,
      actorId: actor2.id,
      observation: "detect_poison_and_disease",
      details: {
        ...result,
        concentrationId: concentration.id,
        facts: JSON.parse(JSON.stringify(input.facts)),
        blockingLayers: JSON.parse(JSON.stringify(input.blockingLayers))
      }
    });
  }
  return worldObjectEvents(
    actor2.id,
    action,
    observations,
    "system:detect-poison-disease",
    "system:concentration"
  );
}
function executeCheck(world, command, env) {
  const actor2 = world.actors[command.actorId];
  const skill = command.skill?.trim().toLowerCase();
  const proficient = !!skill && actor2.character.skillProficiencies?.includes(skill);
  const expertise = !!skill && actor2.character.skillExpertise?.includes(skill);
  const base2 = actor2.character.abilityMods[command.ability] ?? 0;
  const proficiency = expertise ? actor2.character.profBonus * 2 : proficient ? actor2.character.profBonus : 0;
  const collected = collectRollModifiers(actor2.runtime, actor2.passives ?? [], {
    roll: "ability_check",
    filter: { ability: command.ability, ...skill ? { skill } : {} },
    formulaCtx: actorFormulaContext(actor2.character)
  });
  const checkEvents = [];
  const roll = rollD20({
    advantage: collected.advantage,
    modifiers: [
      { value: base2, source: ABILITY_LABEL3[command.ability] },
      ...proficiency ? [{ value: proficiency, source: expertise ? "\u042D\u043A\u0441\u043F\u0435\u0440\u0442\u0438\u0437\u0430" : "\u0411\u041C" }] : [],
      ...collected.modifiers
    ],
    ...command.dc == null ? {} : { target: { type: "dc", value: command.dc } },
    rules: collected.rules,
    rng: env.rng
  });
  checkEvents.push({
    type: "roll",
    label: command.skill ? `\u041F\u0440\u043E\u0432\u0435\u0440\u043A\u0430 (${command.skill})` : `\u041F\u0440\u043E\u0432\u0435\u0440\u043A\u0430 ${ABILITY_LABEL3[command.ability]}`,
    roll: { ...roll, kind: "check" }
  });
  const after = consumeNextRollEffects(actor2.runtime, "ability_check", checkEvents, {
    filter: { ability: command.ability, ...skill ? { skill } : {} }
  });
  const obligations = ["system:ability-check", "system:next-roll-effect"];
  const concentration = consumedConcentrationLifecycle({
    world,
    actingActorId: actor2.id,
    changedActorId: actor2.id,
    before: actor2.runtime,
    after,
    obligations
  });
  return [
    ...concentration.transitions,
    ...engineTrace(actor2.id, [], checkEvents, obligations),
    ...concentration.lifecycle
  ];
}
function executeSave(world, command, env) {
  const actor2 = world.actors[command.actorId];
  const proficient = actor2.character.saveProficiencies?.includes(command.ability);
  const base2 = actor2.character.abilityMods[command.ability] ?? 0;
  const collected = collectRollModifiers(actor2.runtime, actor2.passives ?? [], {
    roll: "saving_throw",
    filter: { ability: command.ability },
    formulaCtx: actorFormulaContext(actor2.character)
  });
  const roll = rollD20({
    advantage: collected.advantage,
    modifiers: [
      { value: base2, source: ABILITY_LABEL3[command.ability] },
      ...proficient ? [{ value: actor2.character.profBonus, source: "\u0411\u041C" }] : [],
      ...collected.modifiers
    ],
    target: { type: "dc", value: command.dc },
    rules: collected.rules,
    rng: env.rng
  });
  return engineTrace(actor2.id, [], [{
    type: "roll",
    label: `\u0421\u043F\u0430\u0441\u0431\u0440\u043E\u0441\u043E\u043A ${ABILITY_LABEL3[command.ability]}`,
    roll: { ...roll, kind: "save" }
  }], ["system:saving-throw"]);
}
function donArmor(world, command) {
  if (world.scene.mode === "encounter") {
    return rejected4(world, "InvalidActionTiming", "Armor can only be donned outside an encounter");
  }
  const actor2 = world.actors[command.actorId];
  const armor = actorCard(actor2, command.armorCardId);
  if (!armor) {
    return rejected4(world, "CardNotFound", `Unknown Card ${command.armorCardId} for ${actor2.id}`);
  }
  const owned = actor2.runtime.inventory.some((entry) => entry.cardId === armor.id && entry.qty > 0) || Object.values(actor2.runtime.equipment).includes(armor.id);
  if (!owned) {
    return rejected4(world, "ItemNotOwned", `${actor2.id} does not own ${armor.id}`);
  }
  if (!isArmorCard(armor)) {
    return rejected4(world, "NotArmor", `${armor.id} is not wearable armor`);
  }
  if (actor2.runtime.equipment.body === armor.id) {
    return rejected4(world, "InvalidEquipmentState", `${actor2.id} is already wearing ${armor.id}`);
  }
  const expiryEvents = [];
  const expired = expireEffectsForTrigger(
    actor2.runtime,
    "wearer_dons_armor",
    expiryEvents
  );
  const remainingIds = new Set(expired.activeEffects.map((effect) => effect.id));
  const endedEffectIds = actor2.runtime.activeEffects.filter((effect) => !remainingIds.has(effect.id)).map((effect) => effect.id);
  const equipment = { ...actor2.runtime.equipment, body: armor.id };
  const obligations = ["system:equipment", "system:effect-lifecycle"];
  return [
    {
      sourceActorId: actor2.id,
      obligationIds: obligations,
      payload: {
        type: "EquipmentChanged",
        actorId: actor2.id,
        operation: "don_armor",
        cardId: armor.id,
        equipment,
        endedEffectIds
      }
    },
    ...engineTrace(actor2.id, [actor2.id], expiryEvents, obligations, {
      facts: { trigger: "wearer_dons_armor", cardId: armor.id }
    })
  ];
}
function spatialFactShapeIssue(facts) {
  if (!facts || !["scenario", "board", "gm_ruling"].includes(facts.factsSource)) {
    return "Action requires explicit scenario, board, or GM facts";
  }
  if (!Number.isInteger(facts.boardRevision) || facts.boardRevision < 0 || !Number.isFinite(facts.distanceFt) || facts.distanceFt < 0 || typeof facts.lineOfSight !== "boolean" || !["none", "half", "three_quarters", "total"].includes(facts.cover) || !["self", "ally", "enemy", "neutral"].includes(facts.relation)) {
    return "Spatial facts are malformed";
  }
  return null;
}
function attackTurnKey(world, actorId) {
  return world.scene.mode === "encounter" ? `encounter:${world.scene.round}:${world.scene.activeIndex}:${actorId}` : `exploration:${world.revision}:${actorId}`;
}
function openAttackAction(world, actorId) {
  return Object.values(world.attackActions).find((entry) => entry.actorId === actorId && entry.status === "open");
}
function validateAttackAction(world, actorId, attackActionId) {
  const attackAction = world.attackActions[attackActionId];
  if (!attackAction || attackAction.actorId !== actorId) {
    return {
      rejection: rejected4(world, "AttackActionNotFound", `Unknown actor-owned Attack action ${attackActionId}`)
    };
  }
  if (attackAction.status !== "open" || attackAction.sequence.attacksRemaining < 1) {
    return {
      rejection: rejected4(world, "AttackActionClosed", `Attack action ${attackActionId} has no attacks remaining`)
    };
  }
  if (attackAction.blockedByResolutionId) {
    return {
      rejection: rejected4(
        world,
        "AttackActionBlocked",
        `Attack action ${attackActionId} is waiting for ${attackAction.blockedByResolutionId}`
      )
    };
  }
  if (attackAction.turnKey !== attackTurnKey(world, actorId)) {
    return {
      rejection: rejected4(world, "AttackActionClosed", `Attack action ${attackActionId} belongs to another turn`)
    };
  }
  return { attackAction };
}
function attackEntryEvent(input) {
  return {
    sourceActorId: input.sourceActorId,
    obligationIds: input.obligations,
    payload: {
      type: "AttackEntryCommitted",
      attackActionId: input.attackActionId,
      entry: JSON.parse(JSON.stringify(input.entry))
    }
  };
}
function completedAttackActionEvent(input) {
  return input.attacksRemaining === 0 ? [{
    sourceActorId: input.actorId,
    obligationIds: input.obligations,
    payload: { type: "AttackActionClosed", attackActionId: input.attackActionId, reason: "completed" }
  }] : [];
}
function beginAttackAction(world, command, catalog, env) {
  const actor2 = world.actors[command.actorId];
  if (actor2.familiarMetadata?.canInitiateAttackAction === false) {
    return rejected4(world, "CapabilityDenied", `${actor2.id} cannot initiate the Attack action`);
  }
  if (deniedCapabilities(actor2.runtime, actor2.passives ?? []).has("action")) {
    return rejected4(world, "CapabilityDenied", `${actor2.id} cannot take the Attack action`);
  }
  if (openAttackAction(world, actor2.id)) {
    return rejected4(world, "InvalidActionTiming", `${actor2.id} already has an open Attack action`);
  }
  const profile = actor2.attackProfile;
  if (!profile || !Number.isInteger(profile.attacksPerAction) || profile.attacksPerAction < 1 || !profile.sourceEntityIds.length) {
    return rejected4(world, "InvalidActionDefinition", `${actor2.id} has no canonical Attack profile`);
  }
  const mainWeaponCardId = actor2.runtime.equipment.main_hand;
  const declared = resolveDeclaredWeaponAction({
    actor: actor2,
    catalog,
    declaredActionId: command.declaredActionId,
    expectedPrimitive: WEAPON_ATTACK_PRIMITIVE,
    weaponCardId: mainWeaponCardId ?? ""
  });
  if (declared.status === "invalid") {
    return rejected4(world, "InvalidActionDefinition", declared.issue);
  }
  const declarationAction = declared.status === "valid" ? declared.action : CORE_ATTACK_ACTION;
  const timingCost = declared.status === "valid" ? declared.cost.filter((entry) => entry.resource === "action") : activationCost(CORE_ATTACK_ACTION);
  const payable = canPay(actor2.runtime, timingCost);
  if (!payable.ok) {
    return rejected4(world, "InsufficientResources", `Missing resources: ${payable.missing.join(", ")}`);
  }
  const paid = pay(actor2.runtime, timingCost);
  const attackActionId = env.nextId();
  const sequence = beginAttackSequence({
    id: attackActionId,
    actorId: actor2.id,
    totalAttacks: profile.attacksPerAction
  });
  const attackAction = {
    id: attackActionId,
    actorId: actor2.id,
    startedAtRevision: world.revision,
    turnKey: attackTurnKey(world, actor2.id),
    status: "open",
    sequence,
    ...declared.status === "valid" ? {
      declaredActionId: declared.action.id,
      declaredActionSourceEntityIds: [...declared.action.sourceEntityIds]
    } : {}
  };
  const obligations = [
    "system:attack-action",
    "system:action-declaration",
    ...declared.status === "valid" ? [`entity:${declared.action.id}`] : [],
    ...declarationAction.sourceEntityIds.map((id) => `entity:${id}`),
    ...profile.sourceEntityIds.map((id) => `entity:${id}`)
  ];
  return [
    actionDeclaredEvent({
      actorId: actor2.id,
      action: declarationAction,
      targetIds: [],
      timing: "active",
      facts: {
        attackActionId,
        attacksPerAction: profile.attacksPerAction,
        ...declared.status === "valid" ? { declaredActionId: declared.action.id } : {},
        attackProfileSourceEntityIds: [...profile.sourceEntityIds]
      },
      obligationIds: obligations
    }),
    ...runtimeTransition(actor2.id, actor2.id, actor2.runtime, paid.state, "action", obligations),
    ...engineTrace(actor2.id, [], paid.events, obligations),
    {
      sourceActorId: actor2.id,
      obligationIds: obligations,
      payload: { type: "AttackActionStarted", attackAction }
    }
  ];
}
function weaponRanges(card, distanceFt) {
  const parsed = parseWeaponProfile(card);
  if (!parsed.valid) return null;
  const mode = weaponAttackModeAtDistance(parsed.profile, distanceFt);
  if (!mode) return null;
  return mode.kind === "melee" ? { kind: "melee", normalFt: mode.reachFt, longFt: mode.reachFt } : { kind: "ranged", normalFt: mode.normalFt, longFt: mode.longFt };
}
function attackDisadvantagePassive(reason) {
  return {
    name: reason,
    kind: "modifier",
    applies_to: { roll: "attack" },
    op: "disadvantage",
    source: reason
  };
}
function attackTargetWithCover(target2, cover) {
  const bonus = cover === "half" ? 2 : cover === "three_quarters" ? 5 : 0;
  return bonus ? { ...target2, ac: (target2.ac ?? effectiveArmorClass(target2)) + bonus } : target2;
}
function weaponAttackAction(hand, rangeKind) {
  const effect = CORE_WEAPON_ATTACK.mechanics.effects[0];
  return {
    ...CORE_WEAPON_ATTACK,
    mechanics: {
      ...CORE_WEAPON_ATTACK.mechanics,
      effects: [{
        ...effect,
        attack_kind: rangeKind === "ranged" ? "weapon_ranged" : "weapon_melee",
        ...hand === "off" ? { tags: ["off_hand"] } : {}
      }]
    }
  };
}
function pactBladeWeaponAttackAction(action, projection) {
  const effects = Array.isArray(action.mechanics.effects) ? action.mechanics.effects : [];
  return {
    ...action,
    mechanics: {
      ...action.mechanics,
      effects: effects.map((effect) => {
        if (effect.resolution !== "attack_roll") return { ...effect };
        const onHit = Array.isArray(effect.on_hit) ? effect.on_hit : [];
        return {
          ...effect,
          ability: projection.attackAbility,
          on_hit: onHit.map((payload) => payload.dice === "weapon" ? { ...payload, ability: projection.damageAbility } : { ...payload })
        };
      })
    }
  };
}
function pactBladeExecutionActor(input) {
  const parsedProfile = parseWeaponProfile(input.card);
  if (!parsedProfile.valid) throw new Error(parsedProfile.issue);
  const mechanics = input.card.mechanics;
  const rawProfile = mechanics.weapon_profile;
  const damageLines = rawProfile.damage_lines;
  const projectedCard = {
    ...input.card,
    damage_type: input.projection.resolvedDamageType,
    mechanics: {
      ...mechanics,
      weapon_profile: {
        ...rawProfile,
        damage_lines: damageLines.map((line, index) => index === 0 ? { ...line, type: input.projection.resolvedDamageType } : { ...line })
      }
    }
  };
  const replaceCard = (cards) => [
    ...(cards ?? []).filter((candidate) => candidate.id !== projectedCard.id),
    projectedCard
  ];
  const proficiencies = new Set(input.actor.character.weaponProficiencies ?? []);
  if (projectedCard.weapon_type) proficiencies.add(projectedCard.weapon_type);
  const slot = input.hand === "main" ? "main_hand" : "off_hand";
  return {
    ...input.actor,
    character: {
      ...input.actor.character,
      knownCards: replaceCard(input.actor.character.knownCards),
      equippedCards: replaceCard(input.actor.character.equippedCards),
      weaponProficiencies: [...proficiencies]
    },
    runtime: {
      ...input.actor.runtime,
      equipment: { ...input.actor.runtime.equipment, [slot]: projectedCard.id }
    }
  };
}
function withoutPactBladeEquipmentProjection(runtime, canonical) {
  return { ...runtime, equipment: canonical.equipment };
}
function pactBladeContinuationProjection(input) {
  return {
    weaponObjectId: input.event.weaponObjectId,
    weaponCardId: input.event.weaponCardId,
    weaponHand: input.selection.hand === "off_hand" ? "off" : "main",
    abilityChoice: input.selection.abilityChoice,
    attackAbility: input.event.projection.attackAbility,
    damageAbility: input.event.projection.damageAbility,
    damageChoice: input.selection.damageType,
    resolvedDamageType: input.event.projection.damageType
  };
}
function persistedPactBladeExecution(input) {
  const planned = planPactBladeAttackProjection({
    world: input.world,
    catalog: input.catalog,
    actorId: input.source.id,
    commandId: input.commandId,
    selection: {
      weaponObjectId: input.projection.weaponObjectId,
      hand: input.projection.weaponHand === "off" ? "off_hand" : "main_hand",
      abilityChoice: input.projection.abilityChoice,
      damageType: input.projection.damageChoice
    }
  });
  if (planned.status === "rejected") return { issue: planned.message };
  const expected = pactBladeContinuationProjection({
    selection: {
      weaponObjectId: input.projection.weaponObjectId,
      hand: input.projection.weaponHand === "off" ? "off_hand" : "main_hand",
      abilityChoice: input.projection.abilityChoice,
      damageType: input.projection.damageChoice
    },
    event: planned.event
  });
  if (JSON.stringify(expected) !== JSON.stringify(input.projection)) {
    return { issue: "Persisted Pact Blade attack projection diverges from canonical state" };
  }
  const card = input.catalog.getCard?.(input.projection.weaponCardId);
  if (!card || card.type !== "weapon") {
    return { issue: "Persisted Pact Blade Card is unavailable or no longer a weapon" };
  }
  const parsedProfile = parseWeaponProfile(card);
  if (!parsedProfile.valid) return { issue: parsedProfile.issue };
  return {
    card,
    actor: pactBladeExecutionActor({
      actor: input.source,
      card,
      hand: input.projection.weaponHand,
      projection: input.projection
    })
  };
}
function lightWeaponExtraAttackAction(source2, hand, rangeKind, actionEconomy = "bonus_action") {
  const weapon = weaponContext(source2.character, hand, source2.runtime.equipment);
  if (!weapon) return null;
  const effect = CORE_LIGHT_WEAPON_EXTRA_ATTACK.mechanics.effects[0];
  const onHit = Array.isArray(effect.on_hit) ? effect.on_hit : [];
  const abilityModifier = source2.character.abilityMods[weapon.ability] ?? 0;
  return {
    ...CORE_LIGHT_WEAPON_EXTRA_ATTACK,
    mechanics: {
      ...CORE_LIGHT_WEAPON_EXTRA_ATTACK.mechanics,
      activation: {
        mode: actionEconomy === "attack_action" ? "attack_entry" : "active",
        cost: actionEconomy === "attack_action" ? [] : [{ resource: "bonus_action" }]
      },
      effects: [{
        ...effect,
        attack_kind: rangeKind === "ranged" ? "weapon_ranged" : "weapon_melee",
        tags: [
          "light_property_extra_attack",
          ...hand === "off" ? ["off_hand"] : []
        ],
        on_hit: onHit.map((payload) => payload.dice === "weapon" ? {
          ...payload,
          ability: lightWeaponExtraAttackDamageAbility(abilityModifier)
        } : payload)
      }]
    }
  };
}
function selectedWeaponUsesMastery(actor2, weaponCardId, type) {
  const hand = actor2.runtime.equipment.main_hand === weaponCardId ? "main" : actor2.runtime.equipment.off_hand === weaponCardId ? "off" : null;
  if (!hand) return false;
  return actorWeaponHasMasteryPrimitive({
    weapon: weaponContext(actor2.character, hand, actor2.runtime.equipment),
    selectedWeaponTypes: actor2.character.weaponMasteries,
    masteryEffects: actor2.masteryEffects,
    type
  });
}
function cleaveWindowFor(input) {
  return input.actor.runtime.activeEffects.find((entry) => {
    const mechanics = entry.mechanics;
    return mechanics.kind === "attack_follow_up" && mechanics.follow_up === "cleave" && mechanics.weaponCardId === input.weaponCardId && (input.attackActionId === void 0 || mechanics.attackActionId === input.attackActionId) && (input.committedByCommandId === void 0 || mechanics.committedByCommandId === input.committedByCommandId);
  });
}
function cleaveWeaponAttackAction(source2, hand) {
  const weapon = weaponContext(source2.character, hand, source2.runtime.equipment);
  if (!weapon) return null;
  const base2 = weaponAttackAction(hand, "melee");
  const effect = base2.mechanics.effects[0];
  const onHit = Array.isArray(effect.on_hit) ? effect.on_hit : [];
  const abilityModifier = source2.character.abilityMods[weapon.ability] ?? 0;
  return {
    ...base2,
    mechanics: {
      ...base2.mechanics,
      activation: { mode: "attack_entry", cost: [] },
      effects: [{
        ...effect,
        tags: [
          "weapon_mastery_cleave_attack",
          ...hand === "off" ? ["off_hand"] : []
        ],
        on_hit: onHit.map((payload) => payload.dice === "weapon" ? {
          ...payload,
          // Cleave keeps a negative ability modifier but omits zero/positive.
          ability: abilityModifier < 0 ? "auto" : "none"
        } : payload)
      }]
    }
  };
}
function blockAttackActionEvent(input) {
  return {
    sourceActorId: input.actorId,
    obligationIds: input.obligations,
    payload: {
      type: "AttackActionBlocked",
      attackActionId: input.attackActionId,
      resolutionId: input.resolutionId
    }
  };
}
function resolveDeclaredWeaponAction(input) {
  if (!input.declaredActionId) return { status: "none" };
  const action = input.catalog.getAction(input.declaredActionId);
  if (!action) {
    return { status: "invalid", issue: `Unknown declared weapon action ${input.declaredActionId}` };
  }
  if (!input.actor.capabilities.actionIds.includes(action.id)) {
    return { status: "invalid", issue: `${input.actor.id} is not granted ${action.id}` };
  }
  const template = parseDeclaredWeaponActionPolicy(action, "template");
  if (template.status !== "valid") return template;
  if (template.policy.primitive !== input.expectedPrimitive) {
    return {
      status: "invalid",
      issue: `${action.id} declares ${template.policy.primitive}, expected ${input.expectedPrimitive}`
    };
  }
  const equipmentSlot = template.policy.hand === "main" ? "main_hand" : "off_hand";
  if (input.actor.runtime.equipment[equipmentSlot] !== input.weaponCardId) {
    return {
      status: "invalid",
      issue: `${action.id} requires the weapon selected in ${equipmentSlot}`
    };
  }
  const cards = new Map([
    ...input.actor.character.knownCards ?? [],
    ...input.actor.character.equippedCards ?? []
  ].map((card) => [card.id, card]));
  let boundMechanics;
  try {
    boundMechanics = bindEquippedWeaponActionContext(
      action.mechanics,
      input.actor.runtime.equipment,
      cards
    );
  } catch (error) {
    return {
      status: "invalid",
      issue: error instanceof Error ? error.message : `${action.id} contextual cost is invalid`
    };
  }
  const bound = {
    ...action,
    mechanics: boundMechanics,
    targeting: compileDeclaredMechanicsTargeting(boundMechanics)
  };
  const parsed = parseDeclaredWeaponActionPolicy(bound, "bound");
  if (parsed.status !== "valid") return parsed;
  return {
    status: "valid",
    action: bound,
    cost: parsed.policy.activationCost.map((entry) => ({ ...entry })),
    sourceEntityIds: [...action.sourceEntityIds]
  };
}
function withoutTimingCost(cost, resource) {
  return cost.filter((entry) => entry.resource !== resource).map((entry) => ({ ...entry }));
}
function actionWithoutActivationCost(action) {
  const activation = action.mechanics.activation;
  return {
    ...action,
    mechanics: {
      ...action.mechanics,
      activation: {
        ...activation && typeof activation === "object" && !Array.isArray(activation) ? activation : {},
        cost: []
      }
    }
  };
}
function performWeaponAttack(world, command, catalog, env) {
  const validated = validateAttackAction(world, command.actorId, command.attackActionId);
  if ("rejection" in validated) return validated.rejection;
  const { attackAction } = validated;
  const source2 = world.actors[command.actorId];
  const declaredWeaponAction = resolveDeclaredWeaponAction({
    actor: source2,
    catalog,
    declaredActionId: command.declaredActionId,
    expectedPrimitive: WEAPON_ATTACK_PRIMITIVE,
    weaponCardId: command.weaponCardId
  });
  if (declaredWeaponAction.status === "invalid") {
    return rejected4(world, "InvalidActionDefinition", declaredWeaponAction.issue);
  }
  if (attackAction.declaredActionId !== command.declaredActionId) {
    return rejected4(
      world,
      "InvalidActionDefinition",
      "Weapon attack declaration does not match the Attack ledger authority"
    );
  }
  if (declaredWeaponAction.status === "valid" && JSON.stringify(attackAction.declaredActionSourceEntityIds) !== JSON.stringify(declaredWeaponAction.action.sourceEntityIds)) {
    return rejected4(
      world,
      "InvalidActionDefinition",
      "Weapon attack declaration provenance differs from the Attack ledger"
    );
  }
  const declaredCost = declaredWeaponAction.status === "valid" ? withoutTimingCost(declaredWeaponAction.cost, "action") : [];
  const payable = canPay(source2.runtime, declaredCost);
  if (!payable.ok) {
    return rejected4(
      world,
      "InsufficientResources",
      `Cannot pay ${command.declaredActionId}: ${payable.missing.join(", ")}`
    );
  }
  const declaredPayment = pay(source2.runtime, declaredCost);
  const target2 = world.actors[command.targetActorId];
  if (!target2) return rejected4(world, "ActorNotFound", `Unknown target ${command.targetActorId}`);
  if (target2.id === source2.id) return rejected4(world, "InvalidTargets", "A weapon attack cannot target its attacker");
  const conditionDenial = harmfulConditionRejection({
    world,
    attackerActorId: source2.id,
    targetActorIds: [target2.id]
  });
  if (conditionDenial) return conditionDenial;
  const factsIssue2 = spatialFactShapeIssue(command.facts);
  if (factsIssue2) return rejected4(world, "InvalidFacts", factsIssue2);
  if (!command.facts.lineOfSight || command.facts.cover === "total") {
    return rejected4(world, "LineOfSightBlocked", `Line of sight to ${target2.id} is blocked`);
  }
  const hasPactObject = command.weaponObjectId !== void 0;
  const hasPactChoice = command.pactBlade !== void 0;
  if (hasPactObject !== hasPactChoice) {
    return rejected4(
      world,
      "InvalidEquipmentState",
      "A Pact Blade attack requires both its concrete item instance and per-attack choices"
    );
  }
  let card = actorCard(source2, command.weaponCardId);
  let hand;
  let executionSource = source2;
  let pactProjection;
  let pactProjectionEvent;
  if (hasPactObject && hasPactChoice) {
    const object5 = world.objects[command.weaponObjectId];
    if (!object5) {
      return rejected4(world, "WorldObjectNotFound", `Unknown world object ${command.weaponObjectId}`);
    }
    if (object5.heldInHand !== "main_hand" && object5.heldInHand !== "off_hand") {
      return rejected4(world, "WeaponNotEquipped", "The active Pact Blade is not held in a hand");
    }
    const selection = {
      weaponObjectId: command.weaponObjectId,
      hand: object5.heldInHand,
      abilityChoice: command.pactBlade.abilityChoice,
      damageType: command.pactBlade.damageType
    };
    const planned = planPactBladeAttackProjection({
      world,
      catalog,
      actorId: source2.id,
      commandId: command.commandId,
      selection
    });
    if (planned.status === "rejected") {
      return rejected4(world, pactBladeRejectionCode(planned.code), planned.message);
    }
    if (planned.event.weaponCardId !== command.weaponCardId) {
      return rejected4(world, "InvalidEquipmentState", "Attack Card is not the active Pact Blade Card");
    }
    card = catalog.getCard?.(planned.event.weaponCardId);
    if (!card) {
      return rejected4(world, "CardNotFound", `Unknown immutable Pact Blade Card ${planned.event.weaponCardId}`);
    }
    const pactWeaponProfile = parseWeaponProfile(card);
    if (!pactWeaponProfile.valid) {
      return rejected4(world, "InvalidEquipmentState", pactWeaponProfile.issue);
    }
    hand = selection.hand === "off_hand" ? "off" : "main";
    pactProjection = pactBladeContinuationProjection({ selection, event: planned.event });
    executionSource = pactBladeExecutionActor({
      actor: source2,
      card,
      hand,
      projection: pactProjection
    });
    pactProjectionEvent = {
      sourceActorId: source2.id,
      obligationIds: [
        "system:pact-blade-attack",
        `entity:${planned.event.sourceEntityId}`,
        `entity:${planned.event.weaponCardId}`
      ],
      payload: planned.event
    };
  } else {
    if (!card) {
      return rejected4(world, "CardNotFound", `Unknown Card ${command.weaponCardId} for ${source2.id}`);
    }
    const equippedCard = card;
    const handEntry = Object.entries(source2.runtime.equipment).find(([slot, cardId]) => (slot === "main_hand" || slot === "off_hand") && cardId === equippedCard.id);
    if (!handEntry) {
      return rejected4(world, "WeaponNotEquipped", `${source2.id} has not equipped ${card.id} in a hand`);
    }
    hand = handEntry[0] === "off_hand" ? "off" : "main";
    if (!source2.runtime.inventory.some((entry2) => entry2.cardId === card.id && entry2.qty > 0) && !Object.values(source2.runtime.equipment).includes(card.id)) {
      return rejected4(world, "ItemNotOwned", `${source2.id} does not own ${card.id}`);
    }
    if (!weaponContext(source2.character, hand, source2.runtime.equipment)) {
      return rejected4(world, "InvalidEquipmentState", `${card.id} cannot resolve from ${hand}_hand`);
    }
  }
  if (!card || card.type !== "weapon") {
    return rejected4(world, "NotWeapon", `${card?.id ?? command.weaponCardId} is not an immutable weapon Card`);
  }
  const profileResult = parseWeaponProfile(card);
  if (!profileResult.valid) {
    return rejected4(world, "InvalidEquipmentState", profileResult.issue);
  }
  const resolvedWeapon = weaponContext(
    executionSource.character,
    hand,
    executionSource.runtime.equipment
  );
  if (!resolvedWeapon || resolvedWeapon.cardId !== card.id) {
    return rejected4(world, "InvalidEquipmentState", `${card.id} has no valid equipped weapon_profile`);
  }
  if (declaredWeaponAction.status === "valid" && (!declaredWeaponAction.action.targeting || command.facts.distanceFt > declaredWeaponAction.action.targeting.rangeFt)) {
    return rejected4(
      world,
      "OutOfRange",
      `${target2.id} is outside the actor-bound weapon targeting contract`
    );
  }
  const range = weaponRanges(card, command.facts.distanceFt);
  if (!range) {
    return rejected4(world, "OutOfRange", `${target2.id} is outside every declared weapon attack mode`);
  }
  const heavy = evaluateWeaponHeavyRule(
    profileResult.profile,
    range.kind,
    executionSource.character.abilityScores
  );
  if (heavy && !heavy.valid) {
    return rejected4(world, "InvalidEquipmentState", heavy.issue);
  }
  const disadvantageReasons = [
    ...heavy?.valid && heavy.disadvantage ? [`Heavy (${heavy.ability.toUpperCase()} below ${heavy.threshold})`] : [],
    ...range.kind === "ranged" && command.facts.distanceFt > range.normalFt ? ["Long range"] : [],
    ...range.kind === "ranged" && command.facts.distanceFt <= 5 && command.facts.relation === "enemy" ? ["Ranged attack in close combat"] : []
  ];
  const baseAction = weaponAttackAction(hand, range.kind);
  const projectedAction = pactProjection ? pactBladeWeaponAttackAction(baseAction, pactProjection) : baseAction;
  const action = declaredWeaponAction.status === "valid" ? {
    ...projectedAction,
    targeting: declaredWeaponAction.action.targeting,
    sourceEntityIds: [.../* @__PURE__ */ new Set([
      ...projectedAction.sourceEntityIds,
      declaredWeaponAction.action.id,
      ...declaredWeaponAction.sourceEntityIds
    ])]
  } : projectedAction;
  const paidExecutionSource = {
    ...executionSource,
    runtime: declaredWeaponAction.status === "valid" ? {
      ...executionSource.runtime,
      resources: declaredPayment.state.resources,
      inventory: declaredPayment.state.inventory
    } : executionSource.runtime
  };
  const sourceForAttack = disadvantageReasons.length ? {
    ...paidExecutionSource,
    passives: [
      ...paidExecutionSource.passives ?? [],
      ...disadvantageReasons.map(attackDisadvantagePassive)
    ]
  } : paidExecutionSource;
  const targetForAttack = attackTargetWithCover(target2, command.facts.cover);
  const nextSequence = performWeaponSequenceAttack({
    sequence: attackAction.sequence,
    actionId: action.id,
    weaponCardId: card.id,
    sourceEntityIds: [
      ...action.sourceEntityIds,
      `card:${card.id}`,
      ...pactProjectionEvent?.payload.type === "PactBladeAttackProjected" ? [pactProjectionEvent.payload.sourceEntityId] : []
    ]
  });
  const entry = nextSequence.entries.at(-1);
  const obligations = [
    "system:attack-action",
    "system:weapon-attack",
    `entity:${card.id}`,
    ...pactProjectionEvent?.payload.type === "PactBladeAttackProjected" ? ["system:pact-blade-attack", `entity:${pactProjectionEvent.payload.sourceEntityId}`] : [],
    ...action.sourceEntityIds.map((id) => `entity:${id}`)
  ];
  const paymentEvents = [
    ...runtimeTransition(
      source2.id,
      source2.id,
      source2.runtime,
      declaredPayment.state,
      "action",
      obligations
    ),
    ...engineTrace(source2.id, [], declaredPayment.events, obligations, {
      facts: command.declaredActionId ? { declaredActionId: command.declaredActionId, contextualWeaponCost: true } : void 0
    })
  ];
  const declaration = actionDeclaredEvent({
    actorId: source2.id,
    action: { ...action, sourceEntityIds: entry.sourceEntityIds },
    targetIds: [target2.id],
    timing: "active",
    facts: {
      attackActionId: attackAction.id,
      weaponCardId: card.id,
      weaponType: resolvedWeapon.weaponType,
      proficient: pactProjection ? true : isWeaponProficient(
        source2.character,
        resolvedWeapon.weaponType,
        resolvedWeapon.proficiencyCategory
      ),
      hand,
      ...pactProjection ? { pactBlade: { ...pactProjection } } : {},
      range,
      disadvantageReasons,
      spatial: { ...command.facts }
    },
    obligationIds: obligations
  });
  const attackCommand = {
    ...command,
    type: "UseAction",
    actionId: action.id,
    targetIds: [target2.id],
    factsByTarget: { [target2.id]: command.facts }
  };
  const pending = pendingAttackEvents(
    { ...world, actors: { ...world.actors, [source2.id]: sourceForAttack, [target2.id]: targetForAttack } },
    attackCommand,
    action,
    catalog,
    env,
    {
      attackActionId: attackAction.id,
      preRollDisadvantageReasons: disadvantageReasons,
      continuationKind: range.kind === "ranged" ? "weapon_ranged" : "weapon_melee",
      weaponHand: hand,
      weaponCardId: card.id,
      ...pactProjection ? { pactBladeProjection: pactProjection } : {}
    }
  );
  if (pending && !Array.isArray(pending)) return pending;
  const entryEvent = attackEntryEvent({
    sourceActorId: source2.id,
    attackActionId: attackAction.id,
    entry,
    obligations
  });
  if (pending) {
    const opened = pending.find((event) => event.payload.type === "ResolutionOpened");
    if (opened?.payload.type === "ResolutionOpened" && (opened.payload.resolution.type === "protection_reaction" || opened.payload.resolution.type === "attack_reaction")) {
      return [
        ...pactProjectionEvent ? [pactProjectionEvent] : [],
        declaration,
        entryEvent,
        ...paymentEvents,
        ...pending,
        blockAttackActionEvent({
          actorId: source2.id,
          attackActionId: attackAction.id,
          resolutionId: opened.payload.resolution.id,
          obligations
        })
      ];
    }
    return [
      ...pactProjectionEvent ? [pactProjectionEvent] : [],
      declaration,
      entryEvent,
      ...paymentEvents,
      ...pending,
      ...completedAttackActionEvent({
        actorId: source2.id,
        attackActionId: attackAction.id,
        attacksRemaining: nextSequence.attacksRemaining,
        obligations
      })
    ];
  }
  const result = executeAction(sourceForAttack.runtime, action.mechanics, {
    ...actionContext(sourceForAttack, env, targetForAttack, target2.runtime, command.facts),
    attackActionId: attackAction.id,
    attackCommandId: command.commandId,
    choices: command.choices,
    deferTargetSaves: true
  });
  const armor = resolveTemporaryHpMeleeRetaliationAfterAttack({
    world,
    attacker: sourceForAttack,
    defender: target2,
    attackerAfter: result.state,
    defenderAfter: result.targetState,
    action,
    attackEvents: result.events,
    env
  });
  const sourceAfter = pactProjection ? withoutPactBladeEquipmentProjection(armor.attackerAfter, source2.runtime) : armor.attackerAfter;
  const targetAfter = armor.defenderAfter;
  const attackObligations = [.../* @__PURE__ */ new Set([
    ...obligations,
    ...armor.retaliationEvents.length ? ["system:temporary-hp-melee-retaliation", "system:retaliation"] : [],
    ...armor.retaliationSourceEntityIds.map((sourceId) => `entity:${sourceId}`)
  ])];
  return [
    ...pactProjectionEvent ? [pactProjectionEvent] : [],
    declaration,
    entryEvent,
    ...paymentEvents,
    ...actionStateEvents({
      world,
      commandId: command.commandId,
      source: sourceForAttack,
      action,
      sourceAfter,
      target: target2,
      targetAfter,
      obligations: attackObligations
    }),
    ...engineTrace(source2.id, [target2.id], result.events, attackObligations, {
      facts: { weaponCardId: card.id, spatial: { ...command.facts } }
    }),
    ...engineTrace(target2.id, [source2.id], armor.retaliationEvents, attackObligations, {
      sourceActorId: target2.id,
      facts: { trigger: "temporary_hp_melee_retaliation" }
    }),
    ...attackFollowUpEvents({
      world,
      commandId: command.commandId,
      source: sourceForAttack,
      sourceAfter,
      target: target2,
      targetAfter,
      action,
      deferred: result.deferredTargetSaves,
      env,
      obligations: attackObligations
    }),
    ...completedAttackActionEvent({
      actorId: source2.id,
      attackActionId: attackAction.id,
      attacksRemaining: nextSequence.attacksRemaining,
      obligations
    })
  ];
}
function lightExtraAttackRejection(world, issue) {
  switch (issue) {
    case "extra_weapon_missing":
      return rejected4(world, "CardNotFound", "The selected extra-attack Card is not actor-owned immutable content");
    case "extra_weapon_not_equipped":
    case "qualifying_weapon_not_equipped":
      return rejected4(world, "WeaponNotEquipped", "The Light attack requires both weapon Cards to remain equipped in distinct hands");
    case "bonus_action_unavailable":
      return rejected4(world, "InsufficientResources", "The Light-property extra attack requires one Bonus Action");
    case "already_used":
    case "attack_action_not_completed":
    case "attack_action_blocked":
    case "wrong_turn":
    case "attack_budget_incomplete":
      return rejected4(world, "InvalidActionTiming", `The Light-property extra attack is unavailable: ${issue}`);
    case "qualifying_weapon_missing":
      return rejected4(world, "InvalidActionDefinition", "The completed Attack ledger references a missing weapon Card");
    default:
      return rejected4(world, "InvalidEquipmentState", `The Light-property extra attack is illegal: ${issue}`);
  }
}
function performLightWeaponExtraAttack(world, command, catalog, env) {
  const source2 = world.actors[command.actorId];
  const declaredWeaponAction = resolveDeclaredWeaponAction({
    actor: source2,
    catalog,
    declaredActionId: command.declaredActionId,
    expectedPrimitive: LIGHT_WEAPON_EXTRA_ATTACK_PRIMITIVE,
    weaponCardId: command.weaponCardId
  });
  if (declaredWeaponAction.status === "invalid") {
    return rejected4(world, "InvalidActionDefinition", declaredWeaponAction.issue);
  }
  const attackAction = world.attackActions[command.attackActionId];
  if (!attackAction || attackAction.actorId !== source2.id) {
    return rejected4(
      world,
      "AttackActionNotFound",
      `Unknown actor-owned Attack action ${command.attackActionId}`
    );
  }
  const cards = [
    ...source2.character.knownCards ?? [],
    ...source2.character.equippedCards ?? []
  ];
  const nickTiming = selectedWeaponUsesMastery(source2, command.weaponCardId, "nick");
  const currentTurnKey = attackTurnKey(world, source2.id);
  if (nickTiming && (source2.runtime.firedThisTurn ?? []).includes(
    weaponMasteryNickUseKey(currentTurnKey)
  )) {
    return rejected4(world, "InvalidActionTiming", "Nick extra attack was already used this turn");
  }
  const eligibility = lightWeaponExtraAttackEligibility({
    attackAction: {
      id: attackAction.id,
      status: attackAction.status,
      turnKey: attackAction.turnKey,
      ...attackAction.blockedByResolutionId ? { blockedByResolutionId: attackAction.blockedByResolutionId } : {},
      attacksRemaining: attackAction.sequence.attacksRemaining,
      entries: attackAction.sequence.entries
    },
    currentTurnKey,
    selectedWeaponCardId: command.weaponCardId,
    cards,
    equipment: source2.runtime.equipment,
    bonusActions: source2.runtime.resources.bonus_action ?? 0,
    firedThisTurn: source2.runtime.firedThisTurn ?? [],
    actionEconomy: nickTiming ? "attack_action" : "bonus_action"
  });
  if (!eligibility.eligible) return lightExtraAttackRejection(world, eligibility.issue);
  const {
    qualifyingWeapon,
    extraWeapon,
    extraWeaponHand: hand
  } = eligibility.facts;
  const actionEconomy = eligibility.facts.actionEconomy ?? "bonus_action";
  const extraWeaponProfile = parseWeaponProfile(extraWeapon);
  if (!extraWeaponProfile.valid) {
    return rejected4(world, "InvalidEquipmentState", extraWeaponProfile.issue);
  }
  if (!source2.runtime.inventory.some((entry) => entry.cardId === extraWeapon.id && entry.qty > 0) && !Object.values(source2.runtime.equipment).includes(extraWeapon.id)) {
    return rejected4(world, "ItemNotOwned", `${source2.id} does not own ${extraWeapon.id}`);
  }
  const selectedWeapon = weaponContext(source2.character, hand, source2.runtime.equipment);
  if (!selectedWeapon || selectedWeapon.cardId !== extraWeapon.id) {
    return rejected4(world, "InvalidEquipmentState", `${extraWeapon.id} cannot resolve from ${hand}_hand`);
  }
  const target2 = world.actors[command.targetActorId];
  if (!target2) return rejected4(world, "ActorNotFound", `Unknown target ${command.targetActorId}`);
  if (target2.id === source2.id) {
    return rejected4(world, "InvalidTargets", "A Light-property weapon attack cannot target its attacker");
  }
  const conditionDenial = harmfulConditionRejection({
    world,
    attackerActorId: source2.id,
    targetActorIds: [target2.id]
  });
  if (conditionDenial) return conditionDenial;
  const spatialIssue = spatialFactShapeIssue(command.facts);
  if (spatialIssue) return rejected4(world, "InvalidFacts", spatialIssue);
  if (!command.facts.lineOfSight || command.facts.cover === "total") {
    return rejected4(world, "LineOfSightBlocked", `Line of sight to ${target2.id} is blocked`);
  }
  if (declaredWeaponAction.status === "valid" && (!declaredWeaponAction.action.targeting || command.facts.distanceFt > declaredWeaponAction.action.targeting.rangeFt)) {
    return rejected4(
      world,
      "OutOfRange",
      `${target2.id} is outside the actor-bound weapon targeting contract`
    );
  }
  const range = weaponRanges(extraWeapon, command.facts.distanceFt);
  if (!range) {
    return rejected4(world, "OutOfRange", `${target2.id} is outside every declared weapon attack mode`);
  }
  const heavy = evaluateWeaponHeavyRule(
    extraWeaponProfile.profile,
    range.kind,
    source2.character.abilityScores
  );
  if (heavy && !heavy.valid) {
    return rejected4(world, "InvalidEquipmentState", heavy.issue);
  }
  const disadvantageReasons = [
    ...heavy?.valid && heavy.disadvantage ? [`Heavy (${heavy.ability.toUpperCase()} below ${heavy.threshold})`] : [],
    ...range.kind === "ranged" && command.facts.distanceFt > range.normalFt ? ["Long range"] : [],
    ...range.kind === "ranged" && command.facts.distanceFt <= 5 && command.facts.relation === "enemy" ? ["Ranged attack in close combat"] : []
  ];
  const declaredCost = declaredWeaponAction.status === "valid" ? actionEconomy === "attack_action" ? withoutTimingCost(declaredWeaponAction.cost, "bonus_action") : declaredWeaponAction.cost : [];
  const payable = canPay(source2.runtime, declaredCost);
  if (!payable.ok) {
    return rejected4(
      world,
      "InsufficientResources",
      `Cannot pay ${command.declaredActionId}: ${payable.missing.join(", ")}`
    );
  }
  const declaredPayment = pay(source2.runtime, declaredCost);
  const markedRuntime = {
    ...declaredPayment.state,
    firedThisTurn: [
      ...declaredPayment.state.firedThisTurn ?? [],
      lightWeaponExtraAttackUseKey(attackAction.id),
      ...actionEconomy === "attack_action" ? [weaponMasteryNickUseKey(currentTurnKey)] : []
    ]
  };
  const markedSource = { ...source2, runtime: markedRuntime };
  const generatedAction = lightWeaponExtraAttackAction(markedSource, hand, range.kind, actionEconomy);
  if (!generatedAction) {
    return rejected4(world, "InvalidEquipmentState", `${extraWeapon.id} cannot build a canonical Light attack`);
  }
  const projectedAction = declaredWeaponAction.status === "valid" ? actionWithoutActivationCost(generatedAction) : generatedAction;
  const action = declaredWeaponAction.status === "valid" ? {
    ...projectedAction,
    targeting: declaredWeaponAction.action.targeting,
    sourceEntityIds: [.../* @__PURE__ */ new Set([
      ...projectedAction.sourceEntityIds,
      declaredWeaponAction.action.id,
      ...declaredWeaponAction.sourceEntityIds
    ])]
  } : projectedAction;
  const sourceForAttack = disadvantageReasons.length ? {
    ...markedSource,
    passives: [
      ...markedSource.passives ?? [],
      ...disadvantageReasons.map(attackDisadvantagePassive)
    ]
  } : markedSource;
  const targetForAttack = attackTargetWithCover(target2, command.facts.cover);
  const passiveDamageSourceIds = passiveModifierSourceEntityIds(source2, {
    roll: "damage",
    filter: {
      attackKind: "weapon",
      extraAttackSource: "light_property",
      abilityModifierAlreadyIncluded: false
    },
    formulaCtx: {
      ...actorFormulaContext(source2.character),
      weaponMod: source2.character.abilityMods[selectedWeapon.ability] ?? 0
    },
    evalCtx: { character: source2.character, state: source2.runtime }
  });
  const obligations = [.../* @__PURE__ */ new Set([
    "system:light-property-extra-attack",
    ...actionEconomy === "attack_action" ? ["system:weapon-mastery", "system:weapon-mastery:nick"] : ["system:bonus-action"],
    `entity:${qualifyingWeapon.id}`,
    `entity:${extraWeapon.id}`,
    ...action.sourceEntityIds.map((id) => `entity:${id}`),
    ...passiveDamageSourceIds.map((id) => `entity:${id}`)
  ])];
  const paymentTrace = engineTrace(source2.id, [], declaredPayment.events, obligations, {
    facts: command.declaredActionId ? { declaredActionId: command.declaredActionId, contextualWeaponCost: true } : void 0
  });
  const declaredAction = {
    ...action,
    sourceEntityIds: [
      ...action.sourceEntityIds,
      `card:${extraWeapon.id}`
    ]
  };
  const declaration = actionDeclaredEvent({
    actorId: source2.id,
    action: declaredAction,
    targetIds: [target2.id],
    timing: "active",
    facts: {
      attackActionId: attackAction.id,
      qualifyingWeaponCardId: qualifyingWeapon.id,
      weaponCardId: extraWeapon.id,
      weaponType: selectedWeapon.weaponType,
      proficient: isWeaponProficient(
        source2.character,
        selectedWeapon.weaponType,
        selectedWeapon.proficiencyCategory
      ),
      hand,
      range,
      actionEconomy,
      disadvantageReasons,
      spatial: { ...command.facts }
    },
    obligationIds: obligations
  });
  const attackCommand = {
    ...command,
    type: "UseAction",
    actionId: action.id,
    targetIds: [target2.id],
    factsByTarget: { [target2.id]: command.facts }
  };
  const pending = pendingAttackEvents(
    {
      ...world,
      actors: {
        ...world.actors,
        [source2.id]: sourceForAttack,
        [target2.id]: targetForAttack
      }
    },
    attackCommand,
    action,
    catalog,
    env,
    {
      preRollDisadvantageReasons: disadvantageReasons,
      continuationKind: range.kind === "ranged" ? "weapon_ranged" : "weapon_melee",
      weaponHand: hand,
      weaponCardId: extraWeapon.id
    }
  );
  if (pending && !Array.isArray(pending)) return pending;
  if (pending) {
    return [
      declaration,
      ...runtimeTransition(
        source2.id,
        source2.id,
        source2.runtime,
        markedRuntime,
        "action",
        obligations
      ),
      ...paymentTrace,
      ...pending
    ];
  }
  const result = executeAction(markedRuntime, action.mechanics, {
    ...actionContext(sourceForAttack, env, targetForAttack, target2.runtime, command.facts)
  });
  const armor = resolveTemporaryHpMeleeRetaliationAfterAttack({
    world,
    attacker: source2,
    defender: target2,
    attackerAfter: result.state,
    defenderAfter: result.targetState,
    action,
    attackEvents: result.events,
    env
  });
  const targetAfter = armor.defenderAfter;
  const attackObligations = [.../* @__PURE__ */ new Set([
    ...obligations,
    ...armor.retaliationEvents.length ? ["system:temporary-hp-melee-retaliation", "system:retaliation"] : [],
    ...armor.retaliationSourceEntityIds.map((sourceId) => `entity:${sourceId}`)
  ])];
  return [
    declaration,
    ...actionStateEvents({
      world,
      commandId: command.commandId,
      source: source2,
      action,
      sourceAfter: armor.attackerAfter,
      target: target2,
      targetAfter,
      obligations: attackObligations
    }),
    ...paymentTrace,
    ...engineTrace(source2.id, [target2.id], result.events, attackObligations, {
      facts: {
        attackActionId: attackAction.id,
        qualifyingWeaponCardId: qualifyingWeapon.id,
        weaponCardId: extraWeapon.id,
        lightPropertyExtraAttack: true,
        actionEconomy,
        spatial: { ...command.facts }
      }
    }),
    ...engineTrace(target2.id, [source2.id], armor.retaliationEvents, attackObligations, {
      sourceActorId: target2.id,
      facts: { trigger: "temporary_hp_melee_retaliation" }
    }),
    ...attackFollowUpEvents({
      world,
      commandId: command.commandId,
      source: source2,
      sourceAfter: armor.attackerAfter,
      target: target2,
      targetAfter,
      action,
      deferred: result.deferredTargetSaves,
      env,
      obligations: attackObligations
    })
  ];
}
function performWeaponMasteryCleaveAttack(world, command, catalog, env) {
  const source2 = world.actors[command.actorId];
  const attackAction = world.attackActions[command.attackActionId];
  if (!attackAction || attackAction.actorId !== source2.id) {
    return rejected4(world, "AttackActionNotFound", `Unknown actor-owned Attack action ${command.attackActionId}`);
  }
  if (attackAction.status === "forfeited" || attackAction.turnKey !== attackTurnKey(world, source2.id) || attackAction.blockedByResolutionId) {
    return rejected4(world, "InvalidActionTiming", "Cleave requires the current unblocked Attack action");
  }
  const window = cleaveWindowFor({
    actor: source2,
    attackActionId: attackAction.id,
    weaponCardId: command.weaponCardId
  });
  if (!window) {
    return rejected4(world, "InvalidActionTiming", "No matching Cleave hit opportunity is open");
  }
  const windowMechanics = window.mechanics;
  if (windowMechanics.committedByCommandId !== void 0) {
    return rejected4(world, "InvalidActionTiming", "This Cleave opportunity was already consumed");
  }
  if (!selectedWeaponUsesMastery(source2, command.weaponCardId, "cleave")) {
    return rejected4(world, "FeatureNotGranted", "The equipped weapon has no selected Cleave mastery");
  }
  const useKey = weaponMasteryCleaveUseKey(attackAction.turnKey);
  if ((source2.runtime.firedThisTurn ?? []).some((key) => key === useKey || key.startsWith(WEAPON_MASTERY_CLEAVE_USE_PREFIX))) {
    return rejected4(world, "InvalidActionTiming", "Cleave was already used this turn");
  }
  const primaryTargetId = String(windowMechanics.primaryTargetActorId ?? "");
  const primary = world.actors[primaryTargetId];
  const target2 = world.actors[command.targetActorId];
  if (!primary || !target2) return rejected4(world, "ActorNotFound", "Cleave lost its primary or secondary target");
  if (target2.id === source2.id || target2.id === primary.id) {
    return rejected4(world, "InvalidTargets", "Cleave requires a different secondary creature");
  }
  const conditionDenial = harmfulConditionRejection({
    world,
    attackerActorId: source2.id,
    targetActorIds: [target2.id]
  });
  if (conditionDenial) return conditionDenial;
  const factsIssue2 = spatialFactShapeIssue(command.facts);
  if (factsIssue2) return rejected4(world, "InvalidFacts", factsIssue2);
  const maxSecondaryDistance = windowMechanics.secondaryWithinPrimaryFt;
  if (typeof maxSecondaryDistance !== "number" || !Number.isFinite(maxSecondaryDistance) || maxSecondaryDistance <= 0) {
    return rejected4(world, "InvalidActionDefinition", "Cleave opportunity has no valid declared secondary-target distance");
  }
  if (!Number.isFinite(command.secondaryDistanceFromPrimaryFt) || command.secondaryDistanceFromPrimaryFt < 0 || command.secondaryDistanceFromPrimaryFt > maxSecondaryDistance) {
    return rejected4(world, "OutOfRange", `Cleave secondary target must be within ${maxSecondaryDistance} ft of the first`);
  }
  if (!command.facts.lineOfSight || command.facts.cover === "total") {
    return rejected4(world, "LineOfSightBlocked", `Line of sight to ${target2.id} is blocked`);
  }
  const card = actorCard(source2, command.weaponCardId);
  if (!card || card.type !== "weapon") {
    return rejected4(world, "NotWeapon", `${command.weaponCardId} is not an immutable weapon Card`);
  }
  const cardProfile = parseWeaponProfile(card);
  if (!cardProfile.valid) {
    return rejected4(world, "InvalidEquipmentState", cardProfile.issue);
  }
  const hand = source2.runtime.equipment.main_hand === card.id ? "main" : source2.runtime.equipment.off_hand === card.id ? "off" : null;
  if (!hand) return rejected4(world, "WeaponNotEquipped", "Cleave requires the same weapon to remain equipped");
  const range = weaponRanges(card, command.facts.distanceFt);
  if (!range || range.kind !== "melee") {
    return rejected4(world, "OutOfRange", "Cleave requires a melee attack within this weapon\u2019s reach");
  }
  const heavy = evaluateWeaponHeavyRule(
    cardProfile.profile,
    "melee",
    source2.character.abilityScores
  );
  if (heavy && !heavy.valid) {
    return rejected4(world, "InvalidEquipmentState", heavy.issue);
  }
  const heavyReason = heavy?.valid && heavy.disadvantage ? `Heavy (${heavy.ability.toUpperCase()} below ${heavy.threshold})` : null;
  const action = cleaveWeaponAttackAction(source2, hand);
  if (!action) return rejected4(world, "InvalidEquipmentState", "Cleave could not build its weapon attack");
  const committedEffects = source2.runtime.activeEffects.map((entry) => entry.id === window.id ? {
    ...entry,
    mechanics: { ...entry.mechanics, committedByCommandId: command.commandId }
  } : entry);
  const markedRuntime = {
    ...source2.runtime,
    activeEffects: committedEffects,
    firedThisTurn: [...source2.runtime.firedThisTurn ?? [], useKey]
  };
  const markedSource = {
    ...source2,
    runtime: markedRuntime,
    ...heavyReason ? {
      passives: [...source2.passives ?? [], attackDisadvantagePassive(heavyReason)]
    } : {}
  };
  const targetForAttack = attackTargetWithCover(target2, command.facts.cover);
  const sourceEntityId = String(windowMechanics.sourceEntityId ?? "");
  const obligations = [
    "system:weapon-mastery",
    "system:weapon-mastery:cleave",
    "system:attack-resolution",
    `entity:${card.id}`,
    ...sourceEntityId ? [`entity:${sourceEntityId}`] : [],
    ...action.sourceEntityIds.map((id) => `entity:${id}`)
  ];
  const declaredAction = {
    ...action,
    sourceEntityIds: [
      ...action.sourceEntityIds,
      `card:${card.id}`,
      ...sourceEntityId ? [sourceEntityId] : []
    ]
  };
  const declaration = actionDeclaredEvent({
    actorId: source2.id,
    action: declaredAction,
    targetIds: [target2.id],
    timing: "active",
    facts: {
      attackActionId: attackAction.id,
      primaryTargetActorId: primary.id,
      secondaryTargetActorId: target2.id,
      secondaryDistanceFromPrimaryFt: command.secondaryDistanceFromPrimaryFt,
      weaponCardId: card.id,
      ...heavyReason ? { disadvantageReasons: [heavyReason] } : {},
      spatial: { ...command.facts }
    },
    obligationIds: obligations
  });
  const attackCommand = {
    ...command,
    type: "UseAction",
    actionId: action.id,
    targetIds: [target2.id],
    factsByTarget: { [target2.id]: command.facts }
  };
  const pending = pendingAttackEvents(
    { ...world, actors: { ...world.actors, [source2.id]: markedSource, [target2.id]: targetForAttack } },
    attackCommand,
    action,
    catalog,
    env,
    {
      continuationKind: "weapon_melee",
      weaponHand: hand,
      weaponCardId: card.id
    }
  );
  if (pending && !Array.isArray(pending)) return pending;
  if (pending) {
    return [
      declaration,
      ...runtimeTransition(source2.id, source2.id, source2.runtime, markedRuntime, "action", obligations),
      ...pending
    ];
  }
  const result = executeAction(markedRuntime, action.mechanics, {
    ...actionContext(markedSource, env, targetForAttack, target2.runtime, command.facts),
    attackActionId: attackAction.id,
    attackCommandId: command.commandId
  });
  const armor = resolveTemporaryHpMeleeRetaliationAfterAttack({
    world,
    attacker: source2,
    defender: target2,
    attackerAfter: result.state,
    defenderAfter: result.targetState,
    action,
    attackEvents: result.events,
    env
  });
  const finalObligations = [.../* @__PURE__ */ new Set([
    ...obligations,
    ...armor.retaliationEvents.length ? ["system:temporary-hp-melee-retaliation", "system:retaliation"] : [],
    ...armor.retaliationSourceEntityIds.map((id) => `entity:${id}`)
  ])];
  return [
    declaration,
    ...actionStateEvents({
      world,
      commandId: command.commandId,
      source: source2,
      action,
      sourceAfter: armor.attackerAfter,
      target: target2,
      targetAfter: armor.defenderAfter,
      obligations: finalObligations
    }),
    ...engineTrace(source2.id, [target2.id], result.events, finalObligations, {
      facts: {
        weaponMastery: "cleave",
        primaryTargetActorId: primary.id,
        secondaryDistanceFromPrimaryFt: command.secondaryDistanceFromPrimaryFt,
        spatial: { ...command.facts }
      }
    }),
    ...engineTrace(target2.id, [source2.id], armor.retaliationEvents, finalObligations, {
      sourceActorId: target2.id,
      facts: { trigger: "temporary_hp_melee_retaliation" }
    }),
    ...attackFollowUpEvents({
      world,
      commandId: command.commandId,
      source: source2,
      sourceAfter: armor.attackerAfter,
      target: target2,
      targetAfter: armor.defenderAfter,
      action,
      deferred: result.deferredTargetSaves,
      env,
      obligations: finalObligations
    })
  ];
}
function freeGraspingPart(world, actor2) {
  const occupied = new Set(Object.values(world.grapples).filter((grapple) => grapple.grapplerActorId === actor2.id).map((grapple) => grapple.sourcePart));
  return actor2.attackProfile?.graspingParts.find((part) => !occupied.has(part) && (part !== "main_hand" && part !== "off_hand" || !actor2.runtime.equipment[part]));
}
function executeUnarmedStrike(world, command, catalog, env) {
  const validated = validateAttackAction(world, command.actorId, command.attackActionId);
  if ("rejection" in validated) return validated.rejection;
  const { attackAction } = validated;
  const source2 = world.actors[command.actorId];
  const target2 = world.actors[command.targetActorId];
  if (!target2) return rejected4(world, "ActorNotFound", `Unknown target ${command.targetActorId}`);
  if (target2.id === source2.id) return rejected4(world, "InvalidTargets", "Unarmed Strike requires another creature");
  const conditionDenial = harmfulConditionRejection({
    world,
    attackerActorId: source2.id,
    targetActorIds: [target2.id]
  });
  if (conditionDenial) return conditionDenial;
  const factsIssue2 = spatialFactShapeIssue(command.facts);
  if (factsIssue2) return rejected4(world, "InvalidFacts", factsIssue2);
  const reachFt = source2.attackProfile?.reachFt;
  if (!Number.isFinite(reachFt) || reachFt <= 0) {
    return rejected4(world, "InvalidActionDefinition", `${source2.id} has no canonical unarmed reach`);
  }
  if (command.facts.distanceFt > reachFt) {
    return rejected4(world, "OutOfRange", `${target2.id} is outside ${reachFt} ft unarmed reach`);
  }
  if (!command.facts.lineOfSight || command.facts.cover === "total") {
    return rejected4(world, "LineOfSightBlocked", `Target ${target2.id} is not observable for Unarmed Strike`);
  }
  const systemId = command.option === "damage" ? SYSTEM_ACTION_IDS.unarmedDamage : command.option === "grapple" ? SYSTEM_ACTION_IDS.unarmedGrapple : SYSTEM_ACTION_IDS.unarmedShove;
  const system = getSystemActionDefinition(systemId);
  if (!system) return rejected4(world, "InvalidActionDefinition", `Missing system action ${systemId}`);
  if (command.option !== "damage") {
    const sourceSize = source2.attackProfile?.size;
    const targetSize = target2.attackProfile?.size;
    if (!Number.isInteger(sourceSize) || !Number.isInteger(targetSize)) {
      return rejected4(world, "InvalidActionDefinition", "Unarmed control requires canonical creature sizes");
    }
    if (targetSize > sourceSize + 1) {
      return rejected4(world, "TargetTooLarge", `${target2.id} is more than one size larger than ${source2.id}`);
    }
  }
  const sourcePart = command.option === "grapple" ? freeGraspingPart(world, source2) : void 0;
  if (command.option === "grapple" && !sourcePart) {
    return rejected4(world, "NoFreeGraspingPart", `${source2.id} has no free part to maintain a grapple`);
  }
  const nextSequence = performUnarmedStrike({
    sequence: attackAction.sequence,
    actionId: system.id,
    option: command.option,
    sourceEntityIds: [...system.sourceEntityIds]
  });
  const entry = nextSequence.entries.at(-1);
  const obligations = [
    "system:attack-action",
    "system:unarmed-strike",
    `system:unarmed-strike:${command.option}`,
    ...system.sourceEntityIds.map((id) => `entity:${id}`)
  ];
  const ruleAction = command.option === "damage" ? CORE_UNARMED_DAMAGE : systemActionAsRuleDefinition(system.id, {
    activation: { mode: "attack_entry", cost: [] }
  });
  const declaration = actionDeclaredEvent({
    actorId: source2.id,
    action: ruleAction,
    targetIds: [target2.id],
    timing: "active",
    facts: {
      attackActionId: attackAction.id,
      option: command.option,
      ...sourcePart ? { sourcePart } : {},
      spatial: { ...command.facts }
    },
    obligationIds: obligations
  });
  const entryEvent = attackEntryEvent({
    sourceActorId: source2.id,
    attackActionId: attackAction.id,
    entry,
    obligations
  });
  if (command.option === "damage") {
    const targetForAttack = attackTargetWithCover(target2, command.facts.cover);
    const attackCommand = {
      ...command,
      type: "UseAction",
      actionId: ruleAction.id,
      targetIds: [target2.id],
      factsByTarget: { [target2.id]: command.facts }
    };
    const pending = pendingAttackEvents(
      { ...world, actors: { ...world.actors, [target2.id]: targetForAttack } },
      attackCommand,
      ruleAction,
      catalog,
      env,
      { attackActionId: attackAction.id, continuationKind: "unarmed_damage" }
    );
    if (pending && !Array.isArray(pending)) return pending;
    if (pending) {
      const opened = pending.find((event) => event.payload.type === "ResolutionOpened");
      if (opened?.payload.type === "ResolutionOpened" && (opened.payload.resolution.type === "protection_reaction" || opened.payload.resolution.type === "attack_reaction")) {
        return [
          declaration,
          entryEvent,
          ...pending,
          blockAttackActionEvent({
            actorId: source2.id,
            attackActionId: attackAction.id,
            resolutionId: opened.payload.resolution.id,
            obligations
          })
        ];
      }
      return [
        declaration,
        entryEvent,
        ...pending,
        ...completedAttackActionEvent({
          actorId: source2.id,
          attackActionId: attackAction.id,
          attacksRemaining: nextSequence.attacksRemaining,
          obligations
        })
      ];
    }
    const result = executeAction(source2.runtime, ruleAction.mechanics, {
      ...actionContext(source2, env, targetForAttack, target2.runtime, command.facts)
    });
    const armor = resolveTemporaryHpMeleeRetaliationAfterAttack({
      world,
      attacker: source2,
      defender: target2,
      attackerAfter: result.state,
      defenderAfter: result.targetState,
      action: ruleAction,
      attackEvents: result.events,
      env
    });
    const attackObligations = [.../* @__PURE__ */ new Set([
      ...obligations,
      ...armor.retaliationEvents.length ? ["system:temporary-hp-melee-retaliation", "system:retaliation"] : [],
      ...armor.retaliationSourceEntityIds.map((sourceId) => `entity:${sourceId}`)
    ])];
    return [
      declaration,
      entryEvent,
      ...actionStateEvents({
        world,
        commandId: command.commandId,
        source: source2,
        action: ruleAction,
        sourceAfter: armor.attackerAfter,
        target: target2,
        targetAfter: armor.defenderAfter,
        obligations: attackObligations
      }),
      ...engineTrace(source2.id, [target2.id], result.events, attackObligations, {
        facts: { option: command.option, spatial: { ...command.facts } }
      }),
      ...engineTrace(target2.id, [source2.id], armor.retaliationEvents, attackObligations, {
        sourceActorId: target2.id,
        facts: { trigger: "temporary_hp_melee_retaliation" }
      }),
      ...attackFollowUpEvents({
        world,
        commandId: command.commandId,
        source: source2,
        sourceAfter: armor.attackerAfter,
        target: target2,
        targetAfter: armor.defenderAfter,
        action: ruleAction,
        deferred: result.deferredTargetSaves,
        env,
        obligations: attackObligations
      }),
      ...completedAttackActionEvent({
        actorId: source2.id,
        attackActionId: attackAction.id,
        attacksRemaining: nextSequence.attacksRemaining,
        obligations
      })
    ];
  }
  const resolutionId = env.nextId();
  const dc = 8 + (source2.character.abilityMods.str ?? 0) + source2.character.profBonus;
  return [
    declaration,
    entryEvent,
    {
      sourceActorId: source2.id,
      obligationIds: [...obligations, "system:target-save", "system:pending-resolution"],
      payload: {
        type: "ResolutionOpened",
        resolution: {
          id: resolutionId,
          type: "unarmed_save",
          openedByCommandId: command.commandId,
          openedAtRevision: world.revision,
          deadlineLogicalClock: world.logicalClock + 10,
          sourceActorId: source2.id,
          targetActorId: target2.id,
          attackActionId: attackAction.id,
          option: command.option,
          facts: { ...command.facts },
          ...sourcePart ? { sourcePart } : {},
          request: {
            id: env.nextId(),
            type: "saving_throw",
            actorId: target2.id,
            ability: "str",
            abilityOptions: ["str", "dex"],
            dc,
            avoidsConditions: [command.option === "grapple" ? "grappled" : "prone"]
          }
        }
      }
    },
    blockAttackActionEvent({
      actorId: source2.id,
      attackActionId: attackAction.id,
      resolutionId,
      obligations
    })
  ];
}
function targetSaveRoll(input) {
  const { target: target2, ability, dc, command, env } = input;
  const collected = collectRollModifiers(target2.runtime, target2.passives ?? [], {
    roll: "saving_throw",
    filter: { ability },
    formulaCtx: actorFormulaContext(target2.character),
    evalCtx: {
      state: target2.runtime,
      activeConditions: activeConditionsOf(target2.runtime),
      savedConditions: /* @__PURE__ */ new Set()
    }
  });
  const proficient = target2.character.saveProficiencies?.includes(ability);
  const manual = manualDecisionRng(command);
  try {
    const roll = rollD20({
      advantage: collected.advantage,
      modifiers: [
        { value: target2.character.abilityMods[ability] ?? 0, source: ABILITY_LABEL3[ability] },
        ...proficient ? [{ value: target2.character.profBonus, source: "\u0411\u041C" }] : [],
        ...collected.modifiers
      ],
      target: { type: "dc", value: dc },
      rules: collected.rules,
      rng: manual?.rng ?? env.rng
    });
    manual?.assertExhausted();
    return {
      roll,
      event: {
        type: "roll",
        label: `\u0421\u043F\u0430\u0441\u0431\u0440\u043E\u0441\u043E\u043A ${ABILITY_LABEL3[ability]}`,
        roll: { ...roll, kind: "save" }
      }
    };
  } catch (error) {
    return { issue: error instanceof Error ? error.message : "Invalid manual roll" };
  }
}
function attackResolutionFinishedEvents(input) {
  return [
    {
      sourceActorId: input.actorId,
      obligationIds: input.obligations,
      payload: {
        type: "AttackActionUnblocked",
        attackActionId: input.attackAction.id,
        resolutionId: input.resolutionId
      }
    },
    ...input.closeIfComplete !== false ? completedAttackActionEvent({
      actorId: input.actorId,
      attackActionId: input.attackAction.id,
      attacksRemaining: input.attackAction.sequence.attacksRemaining,
      obligations: input.obligations
    }) : []
  ];
}
function protectionContinuationAction(pending, source2, catalog) {
  switch (pending.attackContinuationKind) {
    case "catalog":
      return catalog.getAction(pending.actionId) ?? null;
    case "weapon_melee":
    case "weapon_ranged": {
      if (pending.actionId !== SYSTEM_ACTION_IDS.weaponAttack && pending.actionId !== SYSTEM_ACTION_IDS.lightExtraAttack || !pending.weaponHand) return null;
      const slot = pending.weaponHand === "main" ? "main_hand" : "off_hand";
      const card = pending.weaponCardId ? actorCard(source2, pending.weaponCardId) : void 0;
      if (!card || card.type !== "weapon" || source2.runtime.equipment[slot] !== card.id) return null;
      const rangeKind = pending.attackContinuationKind === "weapon_ranged" ? "ranged" : "melee";
      return pending.actionId === SYSTEM_ACTION_IDS.lightExtraAttack ? lightWeaponExtraAttackAction(
        source2,
        pending.weaponHand,
        rangeKind,
        selectedWeaponUsesMastery(source2, card.id, "nick") ? "attack_action" : "bonus_action"
      ) : cleaveWindowFor({
        actor: source2,
        weaponCardId: card.id,
        committedByCommandId: pending.openedByCommandId
      }) ? cleaveWeaponAttackAction(source2, pending.weaponHand) : weaponAttackAction(pending.weaponHand, rangeKind);
    }
    case "unarmed_damage":
      return pending.actionId === SYSTEM_ACTION_IDS.unarmedDamage ? CORE_UNARMED_DAMAGE : null;
    case "familiar_attack":
      return familiarAttackRuleAction(source2, pending.actionId);
    default:
      return null;
  }
}
function resolvePendingProtection(world, command, catalog, env) {
  const pending = world.pendingResolution;
  if (!pending || pending.type !== "protection_reaction") {
    return rejected4(world, "NoPendingResolution", "There is no Protection reaction to resolve");
  }
  const persistedIssue = pendingProtectionResolutionIssue(pending, world);
  if (persistedIssue) return rejected4(world, "InvalidDecision", persistedIssue);
  if (pending.id !== command.resolutionId || pending.request.id !== command.requestId) {
    return rejected4(world, "StaleDecision", "Decision does not match the active Protection request");
  }
  if (pending.request.actorId !== command.actorId) {
    return rejected4(world, "InvalidDecision", "Only the requested protector can resolve this reaction");
  }
  if (command.response.kind !== "reaction") {
    return rejected4(world, "InvalidDecision", "Protection requires a reaction response");
  }
  if (command.response.spell !== void 0) {
    return rejected4(world, "InvalidDecision", "Protection cannot select a spell source");
  }
  const selected = command.response.actionId;
  if (selected !== null && selected !== PROTECTION_2024_CAPABILITY_ID) {
    return rejected4(world, "InvalidDecision", `Reaction ${selected} was not offered`);
  }
  if (selected !== null && !pending.request.options.some((option) => option.actionId === selected)) {
    return rejected4(world, "InvalidDecision", `Reaction ${selected} was not offered`);
  }
  const source2 = world.actors[pending.sourceActorId];
  const target2 = world.actors[pending.targetActorId];
  const protector = world.actors[pending.request.actorId];
  if (!source2 || !target2 || !protector) {
    return rejected4(world, "ActorNotFound", "Protection continuation lost one of its actors");
  }
  let sourceForAttack = source2;
  let pactCard;
  if (pending.pactBladeProjection) {
    if (pending.actionId !== SYSTEM_ACTION_IDS.weaponAttack || pending.weaponHand !== pending.pactBladeProjection.weaponHand || pending.weaponCardId !== pending.pactBladeProjection.weaponCardId) {
      return rejected4(world, "InvalidDecision", "Protection lost its Pact Blade continuation identity");
    }
    const persisted = persistedPactBladeExecution({
      world,
      catalog,
      source: source2,
      commandId: pending.openedByCommandId,
      projection: pending.pactBladeProjection
    });
    if ("issue" in persisted) return rejected4(world, "InvalidDecision", persisted.issue);
    sourceForAttack = persisted.actor;
    pactCard = persisted.card;
  }
  const baseAction = protectionContinuationAction(pending, sourceForAttack, catalog);
  const action = baseAction && pending.pactBladeProjection ? pactBladeWeaponAttackAction(baseAction, pending.pactBladeProjection) : baseAction;
  if (!action || action.id !== pending.actionId || !hasAttackRoll(action)) {
    return rejected4(world, "ActionNotFound", `Unknown Protection continuation ${pending.actionId}`);
  }
  const definitionIssue = actionDefinitionIssue(action);
  if (definitionIssue) return rejected4(world, "InvalidActionDefinition", definitionIssue);
  if (action.kind === "spell" && (!pending.spell || !Number.isInteger(pending.spell.castLevel)) || action.kind === "nonSpell" && pending.spell !== void 0) {
    return rejected4(world, "InvalidSpellDeclaration", "Protection continuation lost its canonical spell context");
  }
  if (pending.attackContinuationKind === "catalog" && !source2.capabilities.actionIds.includes(action.id)) {
    return rejected4(world, "ActionNotGranted", `Actor ${source2.id} no longer owns ${action.id}`);
  }
  const spatialIssue = factsIssue(action, target2.id, pending.facts);
  if (spatialIssue) return rejected4(world, "InvalidDecision", spatialIssue[1]);
  const candidate = pending.protectionCandidates.find((entry) => entry.protectorActorId === protector.id);
  const capabilitySource = protectionCapabilitySource(protector);
  if (!candidate || !capabilitySource) {
    return rejected4(world, "FeatureNotGranted", `${protector.id} has no canonical Protection source`);
  }
  const facts = {
    factsSource: candidate.factsSource,
    worldRevision: world.revision,
    attackId: pending.openedByCommandId,
    protectorActorId: protector.id,
    attackerActorId: source2.id,
    targetActorId: target2.id,
    attackRollStage: "before_roll",
    protectorCanSeeAttacker: candidate.protectorCanSeeAttacker,
    protectorHoldingShield: actorHoldsCanonicalShield(protector),
    protectorReactionAvailable: (protector.runtime.resources.reaction ?? 0) >= 1 && !deniedCapabilities(protector.runtime, protector.passives ?? []).has("reaction"),
    protectorDistanceToTargetFt: candidate.protectorDistanceToTargetFt
  };
  const resolved = resolveProtection2024Reaction({
    decision: selected === null ? "decline" : "use",
    ...selected === null ? {} : { effectId: env.nextId() },
    source: capabilitySource,
    facts
  });
  if (resolved.status === "rejected") {
    return rejected4(world, "InvalidDecision", `Protection is no longer legal: ${resolved.reason}`);
  }
  const obligations = [
    "system:fighting-style-protection",
    "system:reaction-window",
    "system:pending-resolution",
    ...capabilitySource.sourceEntityIds.map((id) => `entity:${id}`)
  ];
  const prefix = [{
    sourceActorId: protector.id,
    obligationIds: obligations,
    payload: {
      type: "DecisionRecorded",
      resolutionId: pending.id,
      requestId: pending.request.id,
      actorId: protector.id,
      response: command.response
    }
  }];
  if (resolved.status === "activated") {
    prefix.push({
      sourceActorId: protector.id,
      obligationIds: [...obligations, "system:effect-lifecycle"],
      payload: { type: "ProtectionEffectActivated", effect: resolved.effect, facts }
    });
  }
  prefix.push({
    sourceActorId: protector.id,
    obligationIds: obligations,
    payload: { type: "ResolutionClosed", resolutionId: pending.id }
  });
  const attackAction = pending.attackActionId ? world.attackActions[pending.attackActionId] : void 0;
  if (pending.attackActionId && (!attackAction || attackAction.status !== "open" || attackAction.blockedByResolutionId !== pending.id)) {
    return rejected4(world, "InvalidDecision", "Protection lost its canonical Attack-action ledger");
  }
  if (attackAction) {
    prefix.push(...attackResolutionFinishedEvents({
      attackAction,
      resolutionId: pending.id,
      actorId: attackAction.actorId,
      obligations: [...obligations, "system:attack-action"],
      closeIfComplete: false
    }));
  }
  const continuationAction = {
    ...action,
    mechanics: withoutActivationCost(action.mechanics)
  };
  const continuationCommand = {
    schemaVersion: 1,
    type: "UseAction",
    commandId: pending.openedByCommandId,
    actorId: source2.id,
    expectedRevision: world.revision,
    rulesetContentHash: world.ruleset.contentHash,
    actionId: action.id,
    targetIds: [target2.id],
    factsByTarget: { [target2.id]: { ...pending.facts } },
    protectionCandidates: pending.protectionCandidates.map((entry) => ({ ...entry })),
    ...pending.choices ? { choices: JSON.parse(JSON.stringify(pending.choices)) } : {},
    ...pending.spell ? { spell: { ...pending.spell } } : {}
  };
  const continuationOptions = {
    ...pending.attackActionId ? { attackActionId: pending.attackActionId } : {},
    preRollDisadvantageReasons: [...pending.preRollDisadvantageReasons],
    protectionWindowResolved: true,
    forceExecution: true,
    continuationKind: pending.attackContinuationKind,
    ...pending.weaponHand ? { weaponHand: pending.weaponHand } : {},
    ...pending.weaponCardId ? { weaponCardId: pending.weaponCardId } : {},
    ...pending.pactBladeProjection ? { pactBladeProjection: { ...pending.pactBladeProjection } } : {}
  };
  if (resolved.status === "declined" && pending.remainingReactions.length) {
    const interim2 = foldEvents(world, prefix.map((event, ordinal) => ({ ...event, ordinal })));
    const [next, ...remaining] = pending.remainingReactions;
    const opened = protectionOpenedEvent({
      world: interim2,
      command: continuationCommand,
      action: continuationAction,
      current: next,
      remaining,
      candidates: pending.protectionCandidates,
      options: continuationOptions,
      env,
      obligations
    });
    const events2 = [...prefix, opened];
    if (attackAction && opened.payload.type === "ResolutionOpened") {
      events2.push(blockAttackActionEvent({
        actorId: attackAction.actorId,
        attackActionId: attackAction.id,
        resolutionId: opened.payload.resolution.id,
        obligations: [...obligations, "system:attack-action"]
      }));
    }
    return events2;
  }
  const interim = foldEvents(world, prefix.map((event, ordinal) => ({ ...event, ordinal })));
  const resumedWorld = pending.pactBladeProjection && pactCard ? {
    ...interim,
    actors: {
      ...interim.actors,
      [source2.id]: pactBladeExecutionActor({
        actor: interim.actors[source2.id],
        card: pactCard,
        hand: pending.pactBladeProjection.weaponHand,
        projection: pending.pactBladeProjection
      })
    }
  } : interim;
  const resumed = pendingAttackEvents(
    resumedWorld,
    continuationCommand,
    continuationAction,
    catalog,
    env,
    continuationOptions
  );
  if (!resumed) {
    return rejected4(world, "InvalidDecision", "Protection continuation did not execute its attack");
  }
  if (!Array.isArray(resumed)) return resumed.status === "rejected" ? rejected4(world, resumed.code, resumed.message) : rejected4(world, "InvalidDecision", "Protection continuation returned an invalid nested result");
  const events = [...prefix, ...resumed];
  if (attackAction) {
    const nextOpened = resumed.find((event) => event.payload.type === "ResolutionOpened");
    if (nextOpened?.payload.type === "ResolutionOpened" && nextOpened.payload.resolution.type === "attack_reaction") {
      events.push(blockAttackActionEvent({
        actorId: attackAction.actorId,
        attackActionId: attackAction.id,
        resolutionId: nextOpened.payload.resolution.id,
        obligations: [...obligations, "system:attack-action"]
      }));
    } else {
      events.push(...completedAttackActionEvent({
        actorId: attackAction.actorId,
        attackActionId: attackAction.id,
        attacksRemaining: attackAction.sequence.attacksRemaining,
        obligations: [...obligations, "system:attack-action"]
      }));
    }
  }
  return events;
}
function resolveUnarmedSave(world, command, env) {
  const pending = world.pendingResolution;
  if (!pending || pending.type !== "unarmed_save") {
    return rejected4(world, "NoPendingResolution", "There is no Unarmed Strike save to resolve");
  }
  if (pending.id !== command.resolutionId || pending.request.id !== command.requestId) {
    return rejected4(world, "StaleDecision", "Decision does not match the active Unarmed Strike save");
  }
  if (pending.targetActorId !== command.actorId || pending.request.actorId !== command.actorId) {
    return rejected4(world, "InvalidDecision", "Only the target can resolve this saving throw");
  }
  if (command.response.kind !== "roll" && command.response.kind !== "voluntary_fail") {
    return rejected4(world, "InvalidDecision", "Unarmed Strike requires a save or voluntary failure");
  }
  const source2 = world.actors[pending.sourceActorId];
  const target2 = world.actors[pending.targetActorId];
  const attackAction = world.attackActions[pending.attackActionId];
  if (!source2 || !target2 || !attackAction || attackAction.actorId !== source2.id || attackAction.blockedByResolutionId !== pending.id) {
    return rejected4(world, "InvalidDecision", "Unarmed Strike continuation lost its actors or Attack ledger");
  }
  const selectedAbility = command.response.selectedAbility;
  if (selectedAbility !== void 0 && selectedAbility !== "str" && selectedAbility !== "dex") {
    return rejected4(world, "InvalidDecision", "Target must choose Strength or Dexterity");
  }
  if (command.response.kind === "roll" && !selectedAbility) {
    return rejected4(world, "InvalidDecision", "A rolled Unarmed Strike save must choose Strength or Dexterity");
  }
  const obligations = [
    "system:attack-action",
    "system:unarmed-strike",
    `system:unarmed-strike:${pending.option}`,
    "system:target-save",
    "system:pending-resolution"
  ];
  let failed = command.response.kind === "voluntary_fail";
  const traceEvents = [];
  if (command.response.kind === "roll") {
    const rolled = targetSaveRoll({
      target: target2,
      ability: selectedAbility,
      dc: pending.request.dc,
      command,
      env
    });
    if ("issue" in rolled) return rejected4(world, "InvalidDecision", rolled.issue);
    failed = rolled.roll.outcome !== "success";
    traceEvents.push(rolled.event);
  } else {
    traceEvents.push({
      type: "narrative",
      text: `${target2.name} \u0434\u043E\u0431\u0440\u043E\u0432\u043E\u043B\u044C\u043D\u043E \u043F\u0440\u043E\u0432\u0430\u043B\u0438\u0432\u0430\u0435\u0442 \u0441\u043F\u0430\u0441\u0431\u0440\u043E\u0441\u043E\u043A Unarmed Strike.`
    });
  }
  const events = [
    ...engineTrace(target2.id, [], traceEvents, obligations, {
      facts: {
        selectedAbility: selectedAbility ?? null,
        voluntaryFailure: command.response.kind === "voluntary_fail"
      }
    }),
    {
      sourceActorId: target2.id,
      obligationIds: obligations,
      payload: {
        type: "DecisionRecorded",
        resolutionId: pending.id,
        requestId: pending.request.id,
        actorId: target2.id,
        response: command.response
      }
    },
    {
      sourceActorId: target2.id,
      obligationIds: obligations,
      payload: { type: "ResolutionClosed", resolutionId: pending.id }
    }
  ];
  if (!failed) {
    events.push(...attackResolutionFinishedEvents({
      attackAction,
      resolutionId: pending.id,
      actorId: source2.id,
      obligations
    }));
    return events;
  }
  if (pending.option === "grapple") {
    if (!pending.sourcePart || !source2.attackProfile?.graspingParts.includes(pending.sourcePart)) {
      return rejected4(world, "InvalidDecision", "Grapple continuation lost its source part");
    }
    if (Object.values(world.grapples).some((grapple2) => grapple2.grapplerActorId === source2.id && grapple2.sourcePart === pending.sourcePart)) {
      return rejected4(world, "InvalidDecision", "Grapple source part became occupied");
    }
    const grapple = {
      id: env.nextId(),
      grapplerActorId: source2.id,
      targetActorId: target2.id,
      sourcePart: pending.sourcePart,
      escapeDc: pending.request.dc,
      reachFt: source2.attackProfile.reachFt,
      sourceEntityIds: [
        ...getSystemActionDefinition(SYSTEM_ACTION_IDS.unarmedGrapple).sourceEntityIds
      ],
      startedAtRevision: world.revision
    };
    events.push({
      sourceActorId: source2.id,
      obligationIds: obligations,
      payload: { type: "GrappleApplied", grapple }
    });
    events.push(...attackResolutionFinishedEvents({
      attackAction,
      resolutionId: pending.id,
      actorId: source2.id,
      obligations
    }));
    return events;
  }
  const shoveResolutionId = env.nextId();
  events.push(...attackResolutionFinishedEvents({
    attackAction,
    resolutionId: pending.id,
    actorId: source2.id,
    obligations,
    closeIfComplete: false
  }));
  events.push({
    sourceActorId: source2.id,
    obligationIds: obligations,
    payload: {
      type: "ResolutionOpened",
      resolution: {
        id: shoveResolutionId,
        type: "shove_outcome",
        openedByCommandId: pending.openedByCommandId,
        openedAtRevision: world.revision,
        deadlineLogicalClock: world.logicalClock + 10,
        sourceActorId: source2.id,
        targetActorId: target2.id,
        attackActionId: attackAction.id,
        facts: { ...pending.facts },
        request: {
          id: env.nextId(),
          type: "shove_outcome",
          actorId: source2.id,
          options: ["push_5ft", "prone"]
        }
      }
    }
  });
  events.push(blockAttackActionEvent({
    actorId: source2.id,
    attackActionId: attackAction.id,
    resolutionId: shoveResolutionId,
    obligations
  }));
  return events;
}
function resolveShoveOutcome(world, command, env) {
  const pending = world.pendingResolution;
  if (!pending || pending.type !== "shove_outcome") {
    return rejected4(world, "NoPendingResolution", "There is no Shove outcome to resolve");
  }
  if (pending.id !== command.resolutionId || pending.request.id !== command.requestId) {
    return rejected4(world, "StaleDecision", "Decision does not match the active Shove choice");
  }
  if (pending.sourceActorId !== command.actorId || pending.request.actorId !== command.actorId) {
    return rejected4(world, "InvalidDecision", "Only the attacker can choose the Shove outcome");
  }
  if (command.response.kind !== "shove_outcome" || !pending.request.options.includes(command.response.outcome)) {
    return rejected4(world, "InvalidDecision", "Shove requires choosing push_5ft or prone");
  }
  const attackAction = world.attackActions[pending.attackActionId];
  if (!attackAction || attackAction.blockedByResolutionId !== pending.id) {
    return rejected4(world, "InvalidDecision", "Shove choice lost its Attack-action ledger");
  }
  const obligations = [
    "system:attack-action",
    "system:unarmed-strike",
    "system:unarmed-strike:shove",
    "system:pending-resolution"
  ];
  return [
    {
      sourceActorId: command.actorId,
      obligationIds: obligations,
      payload: {
        type: "DecisionRecorded",
        resolutionId: pending.id,
        requestId: pending.request.id,
        actorId: command.actorId,
        response: command.response
      }
    },
    {
      sourceActorId: command.actorId,
      obligationIds: obligations,
      payload: {
        type: "ShoveApplied",
        effectId: env.nextId(),
        sourceActorId: command.actorId,
        targetActorId: pending.targetActorId,
        outcome: command.response.outcome,
        facts: { ...pending.facts }
      }
    },
    ...engineTrace(command.actorId, [pending.targetActorId], [{
      type: "narrative",
      text: command.response.outcome === "prone" ? `${world.actors[pending.targetActorId].name} \u0441\u0431\u0438\u0442 \u0441 \u043D\u043E\u0433.` : `${world.actors[pending.targetActorId].name} \u043E\u0442\u0442\u043E\u043B\u043A\u043D\u0443\u0442 \u043D\u0430 5 \u0444\u0443\u0442\u043E\u0432.`
    }], obligations),
    {
      sourceActorId: command.actorId,
      obligationIds: obligations,
      payload: { type: "ResolutionClosed", resolutionId: pending.id }
    },
    ...attackResolutionFinishedEvents({
      attackAction,
      resolutionId: pending.id,
      actorId: command.actorId,
      obligations
    })
  ];
}
function forfeitAttackAction(world, command) {
  const validated = validateAttackAction(world, command.actorId, command.attackActionId);
  if ("rejection" in validated) return validated.rejection;
  const obligations = ["system:attack-action", "system:attack-action-forfeit"];
  return [{
    sourceActorId: command.actorId,
    obligationIds: obligations,
    payload: { type: "AttackActionClosed", attackActionId: command.attackActionId, reason: "forfeited" }
  }];
}
function openEscapeGrapple(world, command, env) {
  const actor2 = world.actors[command.actorId];
  const grapple = world.grapples[command.grappleId];
  if (!grapple || grapple.targetActorId !== actor2.id) {
    return rejected4(world, "GrappleNotFound", `Unknown target-owned grapple ${command.grappleId}`);
  }
  if (command.skill !== "athletics" && command.skill !== "acrobatics") {
    return rejected4(world, "InvalidDecision", "Escape requires Athletics or Acrobatics");
  }
  if (deniedCapabilities(actor2.runtime, actor2.passives ?? []).has("action")) {
    return rejected4(world, "CapabilityDenied", `${actor2.id} cannot take the Escape action`);
  }
  const cost = [{ resource: "action" }];
  const payable = canPay(actor2.runtime, cost);
  if (!payable.ok) {
    return rejected4(world, "InsufficientResources", `Missing resources: ${payable.missing.join(", ")}`);
  }
  const paid = pay(actor2.runtime, cost);
  const action = systemActionAsRuleDefinition(SYSTEM_ACTION_IDS.escapeGrapple, {
    activation: { mode: "active", cost }
  });
  const obligations = actionObligationIds(
    action,
    "system:grapple-lifecycle",
    "system:ability-check",
    "system:pending-resolution"
  );
  const resolutionId = env.nextId();
  return [
    actionDeclaredEvent({
      actorId: actor2.id,
      action,
      targetIds: [actor2.id],
      timing: "active",
      facts: { grappleId: grapple.id, skill: command.skill, escapeDc: grapple.escapeDc },
      obligationIds: obligations
    }),
    ...runtimeTransition(actor2.id, actor2.id, actor2.runtime, paid.state, "action", obligations),
    ...engineTrace(actor2.id, [], paid.events, obligations),
    {
      sourceActorId: actor2.id,
      obligationIds: obligations,
      payload: {
        type: "ResolutionOpened",
        resolution: {
          id: resolutionId,
          type: "escape_grapple",
          openedByCommandId: command.commandId,
          openedAtRevision: world.revision,
          deadlineLogicalClock: world.logicalClock + 10,
          actorId: actor2.id,
          grappleId: grapple.id,
          skill: command.skill,
          request: {
            id: env.nextId(),
            type: "saving_throw",
            actorId: actor2.id,
            ability: command.skill === "athletics" ? "str" : "dex",
            dc: grapple.escapeDc,
            avoidsConditions: ["grappled"]
          }
        }
      }
    }
  ];
}
function resolveEscapeGrapple(world, command, env) {
  const pending = world.pendingResolution;
  if (!pending || pending.type !== "escape_grapple") {
    return rejected4(world, "NoPendingResolution", "There is no Escape Grapple check to resolve");
  }
  if (pending.id !== command.resolutionId || pending.request.id !== command.requestId) {
    return rejected4(world, "StaleDecision", "Decision does not match the active Escape Grapple check");
  }
  if (pending.actorId !== command.actorId || command.response.kind !== "roll") {
    return rejected4(world, "InvalidDecision", "Only the grappled actor can roll the escape check");
  }
  const actor2 = world.actors[pending.actorId];
  const grapple = world.grapples[pending.grappleId];
  if (!actor2 || !grapple || grapple.targetActorId !== actor2.id) {
    return rejected4(world, "InvalidDecision", "Escape continuation lost its active grapple");
  }
  const ability = pending.skill === "athletics" ? "str" : "dex";
  const proficient = actor2.character.skillProficiencies?.includes(pending.skill);
  const expertise = actor2.character.skillExpertise?.includes(pending.skill);
  const collected = collectRollModifiers(actor2.runtime, actor2.passives ?? [], {
    roll: "ability_check",
    filter: { ability, skill: pending.skill },
    formulaCtx: actorFormulaContext(actor2.character)
  });
  const manual = manualDecisionRng(command);
  let roll;
  try {
    roll = rollD20({
      advantage: collected.advantage,
      modifiers: [
        { value: actor2.character.abilityMods[ability] ?? 0, source: ABILITY_LABEL3[ability] },
        ...proficient ? [{
          value: actor2.character.profBonus * (expertise ? 2 : 1),
          source: expertise ? "\u042D\u043A\u0441\u043F\u0435\u0440\u0442\u0438\u0437\u0430" : "\u0411\u041C"
        }] : [],
        ...collected.modifiers
      ],
      target: { type: "dc", value: grapple.escapeDc },
      rules: collected.rules,
      rng: manual?.rng ?? env.rng
    });
    manual?.assertExhausted();
  } catch (error) {
    return rejected4(world, "InvalidDecision", error instanceof Error ? error.message : "Invalid manual roll");
  }
  const obligations = [
    "system:grapple-lifecycle",
    "system:ability-check",
    "system:pending-resolution"
  ];
  return [
    ...engineTrace(actor2.id, [], [{
      type: "roll",
      label: `Escape Grapple (${pending.skill})`,
      roll: { ...roll, kind: "check" }
    }], obligations, { facts: { grappleId: grapple.id, escapeDc: grapple.escapeDc } }),
    {
      sourceActorId: actor2.id,
      obligationIds: obligations,
      payload: {
        type: "DecisionRecorded",
        resolutionId: pending.id,
        requestId: pending.request.id,
        actorId: actor2.id,
        response: command.response
      }
    },
    ...roll.outcome === "success" ? [{
      sourceActorId: actor2.id,
      obligationIds: obligations,
      payload: { type: "GrappleEnded", grappleId: grapple.id, reason: "escaped" }
    }] : [],
    {
      sourceActorId: actor2.id,
      obligationIds: obligations,
      payload: { type: "ResolutionClosed", resolutionId: pending.id }
    }
  ];
}
function releaseGrapple(world, command) {
  const grapple = world.grapples[command.grappleId];
  if (!grapple || grapple.grapplerActorId !== command.actorId) {
    return rejected4(world, "GrappleNotFound", `Unknown grappler-owned grapple ${command.grappleId}`);
  }
  const action = systemActionAsRuleDefinition(SYSTEM_ACTION_IDS.releaseGrapple, {
    activation: { mode: "free", cost: [] }
  });
  const obligations = actionObligationIds(action, "system:grapple-lifecycle");
  return [
    actionDeclaredEvent({
      actorId: command.actorId,
      action,
      targetIds: [grapple.targetActorId],
      timing: "active",
      facts: { grappleId: grapple.id },
      obligationIds: obligations
    }),
    {
      sourceActorId: command.actorId,
      obligationIds: obligations,
      payload: { type: "GrappleEnded", grappleId: grapple.id, reason: "released" }
    },
    ...world.pendingResolution?.type === "escape_grapple" && world.pendingResolution.grappleId === grapple.id ? [{
      sourceActorId: command.actorId,
      obligationIds: [...obligations, "system:pending-resolution"],
      payload: {
        type: "ResolutionClosed",
        resolutionId: world.pendingResolution.id
      }
    }] : []
  ];
}
function breakGrappleRange(world, command) {
  const grapple = world.grapples[command.grappleId];
  if (!grapple || grapple.grapplerActorId !== command.actorId && grapple.targetActorId !== command.actorId) {
    return rejected4(world, "GrappleNotFound", `Unknown participant grapple ${command.grappleId}`);
  }
  const issue = spatialFactShapeIssue(command.facts);
  if (issue) return rejected4(world, "InvalidFacts", issue);
  if (command.facts.distanceFt <= grapple.reachFt) {
    return rejected4(world, "InvalidFacts", `Distance must exceed persisted ${grapple.reachFt} ft reach`);
  }
  const obligations = ["system:grapple-lifecycle", "system:observable-fact"];
  return [
    {
      sourceActorId: command.actorId,
      obligationIds: obligations,
      payload: {
        type: "GrappleEnded",
        grappleId: grapple.id,
        reason: "distance_exceeds_range"
      }
    },
    ...world.pendingResolution?.type === "escape_grapple" && world.pendingResolution.grappleId === grapple.id ? [{
      sourceActorId: command.actorId,
      obligationIds: [...obligations, "system:pending-resolution"],
      payload: {
        type: "ResolutionClosed",
        resolutionId: world.pendingResolution.id
      }
    }] : []
  ];
}
function observeProtectionProximity(world, command) {
  const protector = world.actors[command.protectorActorId];
  const target2 = world.actors[command.protectedTargetActorId];
  if (!protector || !target2 || command.actorId !== protector.id && command.actorId !== target2.id) {
    return rejected4(world, "ActorNotFound", "Protection proximity requires one of its two participants");
  }
  if (!["scenario", "board", "gm_ruling"].includes(command.factsSource) || !Number.isInteger(command.boardRevision) || command.boardRevision < 0 || !Number.isFinite(command.distanceFt) || command.distanceFt < 0) {
    return rejected4(world, "InvalidFacts", "Protection proximity observation is malformed");
  }
  const effects = actorProtectionEffects(protector).filter((effect) => effect.protectedTargetActorId === target2.id);
  if (!effects.length) {
    return rejected4(world, "InvalidDecision", "There is no active Protection effect for these actors");
  }
  if (command.distanceFt <= 5) {
    return rejected4(world, "InvalidFacts", "A maintained proximity observation does not end Protection");
  }
  const events = [];
  for (const effect of effects) {
    const lifecycleEvent = {
      type: "distance_observed",
      factsSource: command.factsSource,
      worldRevision: world.revision,
      protectorActorId: protector.id,
      protectedTargetActorId: target2.id,
      distanceFt: command.distanceFt
    };
    const advanced = advanceProtection2024Effect(effect, lifecycleEvent);
    if (advanced.status !== "ended" || advanced.reason !== "proximity_broken") {
      return rejected4(world, "InvalidFacts", `Protection proximity was rejected: ${advanced.reason}`);
    }
    events.push({
      sourceActorId: command.actorId,
      obligationIds: [
        "system:fighting-style-protection",
        "system:effect-lifecycle",
        "system:observable-fact",
        ...effect.source.sourceEntityIds.map((id) => `entity:${id}`)
      ],
      payload: {
        type: "ProtectionEffectEnded",
        protectorActorId: protector.id,
        protectedTargetActorId: target2.id,
        effectId: effect.id,
        reason: "proximity_broken",
        lifecycleEvent
      }
    });
  }
  return events;
}
var FAMILIAR_SHARED_SENSES_ACTION = {
  id: "system.familiar.shared-senses",
  name: "Familiar Shared Senses",
  kind: "nonSpell",
  sourceEntityIds: ["system:dnd5e-2024:find-familiar:shared-senses"],
  mechanics: { activation: { mode: "active", cost: [{ resource: "bonus_action" }] } }
};
var FAMILIAR_DISMISS_ACTION = {
  id: "system.familiar.dismiss",
  name: "Dismiss Familiar",
  kind: "nonSpell",
  sourceEntityIds: ["system:dnd5e-2024:find-familiar:dismiss"],
  mechanics: { activation: { mode: "active", cost: [{ resource: "action" }] } }
};
var FAMILIAR_REAPPEAR_ACTION = {
  id: "system.familiar.reappear",
  name: "Reappear Familiar",
  kind: "nonSpell",
  sourceEntityIds: ["system:dnd5e-2024:find-familiar:reappear"],
  mechanics: { activation: { mode: "active", cost: [{ resource: "action" }] } }
};
function explicitStringChoice(choices, id) {
  const value = choices?.[id];
  return typeof value === "string" && value.trim() === value && value.length > 0 ? value : null;
}
function familiarObservableFactsIssue(value) {
  if (!value || typeof value !== "object" || Array.isArray(value)) {
    return "Familiar interaction requires explicit board, scenario, or GM facts";
  }
  const facts = value;
  if (!["scenario", "board", "gm_ruling"].includes(String(facts.factsSource ?? "")) || !Number.isInteger(facts.boardRevision) || Number(facts.boardRevision) < 0 || !Number.isFinite(facts.distanceFt) || Number(facts.distanceFt) < 0 || typeof facts.lineOfSight !== "boolean") {
    return "Familiar observable facts are malformed";
  }
  return null;
}
function familiarStateChangedEvent(input) {
  return {
    sourceActorId: input.ownerActorId,
    obligationIds: input.obligations,
    payload: {
      type: "FamiliarStateChanged",
      ownerActorId: input.ownerActorId,
      familiarActorId: input.familiarActorId,
      familiar: JSON.parse(JSON.stringify(input.familiar)),
      ...input.droppedItemIds ? { droppedItemIds: [...input.droppedItemIds] } : {},
      reason: input.reason
    }
  };
}
function executeFindFamiliarCast(input) {
  const { world, command, action, env } = input;
  const owner = world.actors[command.actorId];
  if (action.kind !== "spell" || action.mechanics.primitive?.type !== FIND_FAMILIAR_PRIMITIVE) {
    return rejected4(world, "InvalidActionDefinition", `${action.id} is not canonical Find Familiar`);
  }
  const declaredPolicy = parseFindFamiliarMechanicsPolicy(action.mechanics);
  const declaredCastTime = parseActivationCastTime(action.mechanics);
  if (declaredPolicy.status === "invalid" || declaredCastTime.status !== "valid") {
    return rejected4(
      world,
      "InvalidActionDefinition",
      declaredPolicy.status === "invalid" ? `${action.id}: ${declaredPolicy.issue}` : `${action.id}: Find Familiar requires a declared positive casting time`
    );
  }
  if (command.worldInput) {
    return rejected4(world, "InvalidFacts", "Find Familiar does not accept arbitrary world input");
  }
  const formId = explicitStringChoice(command.choices, FIND_FAMILIAR_FORM_CHOICE);
  const spiritType = explicitStringChoice(command.choices, FIND_FAMILIAR_SPIRIT_CHOICE);
  const method = explicitStringChoice(command.choices, FIND_FAMILIAR_CAST_PATH_CHOICE);
  if (!formId || !spiritType || !method) {
    return rejected4(
      world,
      "InvalidDecision",
      `Find Familiar requires explicit ${FIND_FAMILIAR_FORM_CHOICE}, ${FIND_FAMILIAR_SPIRIT_CHOICE}, and ${FIND_FAMILIAR_CAST_PATH_CHOICE}`
    );
  }
  if (!["celestial", "fey", "fiend"].includes(spiritType) || !["spell_slot", "ritual", "pact_chain_magic_action"].includes(method)) {
    return rejected4(world, "InvalidDecision", "Find Familiar has an invalid spirit or casting path");
  }
  if (world.scene.mode === "encounter" && method !== "pact_chain_magic_action") {
    return rejected4(
      world,
      "InvalidActionTiming",
      "Find Familiar cannot complete its normal or ritual casting time during an encounter"
    );
  }
  if (!command.spell?.grantId || !command.spell.sourceId || !command.spell.payment) {
    return rejected4(world, "InvalidSpellDeclaration", "Find Familiar requires an exact source-scoped spell grant");
  }
  const chain2 = owner.warlockPacts?.chain;
  const chainGrant = !!chain2 && chain2.template.findFamiliarActionId === action.id && action.sourceEntityIds.includes(chain2.sourceEntityId);
  if (method === "pact_chain_magic_action") {
    if (!chainGrant || command.spell.mode !== "normal" || command.spell.payment.kind !== "none") {
      return rejected4(world, "InvalidSpellDeclaration", "Pact Chain casting requires its at-will no-slot action");
    }
  } else if (method === "ritual") {
    if (chainGrant || command.spell.mode !== "ritual" || command.spell.payment.kind !== "none") {
      return rejected4(world, "InvalidSpellDeclaration", "Ritual Find Familiar requires a ritual-capable no-slot grant");
    }
  } else if (chainGrant || command.spell.mode !== "normal" || command.spell.payment.kind !== "slot") {
    return rejected4(world, "InvalidSpellDeclaration", "Normal Find Familiar requires an exact level-1 slot payment");
  }
  const materialCost = findFamiliarMaterialCost(action);
  if (!materialCost) {
    return rejected4(
      world,
      "InvalidActionDefinition",
      "Find Familiar requires one declared material activation cost with persistent binding"
    );
  }
  const incense = owner.runtime.resources[materialCost.resource];
  const incenseMaximum = owner.runtime.maxResources[materialCost.resource];
  if (!Number.isInteger(incense) || !Number.isInteger(incenseMaximum) || incense < materialCost.amount || incense > incenseMaximum || owner.character.resourceRecharge?.[materialCost.resource] !== materialCost.recharge) {
    return rejected4(
      world,
      "InsufficientResources",
      `Find Familiar requires ${materialCost.amount} ${materialCost.binding.currency} in ${materialCost.resource}`
    );
  }
  const owned = familiarActorsOwnedBy(world, owner.id);
  if (owned.length > 1) {
    return rejected4(world, "InvalidDecision", `${owner.id} has more than one canonical familiar`);
  }
  if (chainGrant && chain2?.activeFamiliar && owned[0]?.id !== chain2.activeFamiliar.actorId) {
    return rejected4(world, "InvalidDecision", "Pact Chain has an unmaterialized legacy familiar projection");
  }
  const existing = owned[0] ?? null;
  const familiarActorId = existing?.id ?? env.nextId();
  if (!existing && world.actors[familiarActorId]) {
    return rejected4(world, "InvalidDecision", `Generated familiar actor ${familiarActorId} already exists`);
  }
  const payable = canPay(owner.runtime, activationCost(action));
  if (!payable.ok) {
    return rejected4(world, "InsufficientResources", `Missing resources: ${payable.missing.join(", ")}`);
  }
  try {
    const paymentResource = command.spell.payment.resource;
    const slotCount = command.spell.payment.kind === "slot" && paymentResource ? owner.runtime.resources[paymentResource] ?? 0 : 0;
    const cast = castFindFamiliar({
      familiarActorId,
      ownerActorId: owner.id,
      policy: chainGrant ? { kind: "pact_chain", sourceEntityId: chain2.sourceEntityId } : { kind: "base", sourceEntityId: action.sourceEntityIds[0] },
      method,
      formId,
      spiritType,
      existingFamiliar: existing?.familiarState ?? null,
      resources: { level1SpellSlots: slotCount, incenseGp: incense },
      incenseOfferingGp: materialCost.amount,
      materialCostGp: materialCost.amount,
      baseCastingTimeSeconds: declaredCastTime.policy.seconds,
      mechanicsPolicy: declaredPolicy.policy
    });
    let familiar = cast.familiar;
    if (world.scene.mode === "encounter") {
      const template = getFamiliarActorTemplate(familiar.form.id);
      familiar = rollFamiliarInitiative({
        familiar,
        modifier: template.initiativeModifier,
        rng: env.rng
      });
    }
    const actor2 = materializeCanonicalFamiliarActor({
      familiar,
      owner,
      summoningActionId: action.id
    });
    const paid = pay(owner.runtime, activationCost(action));
    const ownerAfter = paid.state;
    const obligations = actionObligationIds(
      action,
      "system:find-familiar",
      "system:summoned-actor",
      "system:material-component",
      "system:initiative"
    );
    const resourceEvents = [
      ...paid.events,
      {
        type: "narrative",
        text: `Find Familiar: ${familiar.form.name} (${familiar.spiritType}), ${cast.castingTime}.`
      }
    ];
    return [
      ...runtimeTransition(owner.id, owner.id, owner.runtime, ownerAfter, "action", obligations),
      ...engineTrace(owner.id, [actor2.id], resourceEvents, obligations, {
        facts: {
          formId,
          spiritType,
          castingMethod: method,
          castingDuration: cast.castingDuration,
          catalogId: actor2.familiarMetadata.catalogId,
          catalogContentHash: actor2.familiarMetadata.catalogContentHash
        }
      }),
      {
        sourceActorId: owner.id,
        obligationIds: obligations,
        payload: {
          type: "FamiliarActorUpserted",
          ownerActorId: owner.id,
          actor: actor2,
          casting: {
            actionId: action.id,
            method,
            consumedIncenseGp: cast.consumedIncenseGp,
            created: cast.created,
            changedForm: cast.changedForm
          }
        }
      }
    ];
  } catch (error) {
    return rejected4(
      world,
      "InvalidDecision",
      error instanceof Error ? error.message : "Find Familiar choices are invalid"
    );
  }
}
function familiarPolicyFromSummoningAction(familiar, catalog) {
  const summoningActionId = familiar.familiarMetadata?.summoningActionId;
  if (!summoningActionId) {
    return { issue: "Familiar has no summoning-action policy provenance" };
  }
  const action = catalog.getAction(summoningActionId);
  if (!action) return { issue: `Unknown familiar summoning action ${summoningActionId}` };
  const parsed = parseFindFamiliarMechanicsPolicy(action.mechanics);
  return parsed.status === "valid" ? { policy: parsed.policy } : { issue: `${summoningActionId}: ${parsed.issue}` };
}
function activateOwnedFamiliarSharedSenses(world, command, catalog) {
  const owner = world.actors[command.actorId];
  const familiar = requireOwnedFamiliar(world, owner.id, command.familiarActorId);
  if (!familiar?.familiarState) {
    return rejected4(world, "ActorNotFound", `Unknown owner familiar ${command.familiarActorId}`);
  }
  const declaredPolicy = familiarPolicyFromSummoningAction(familiar, catalog);
  if ("issue" in declaredPolicy) {
    return rejected4(world, "InvalidActionDefinition", declaredPolicy.issue);
  }
  const factsIssue2 = familiarObservableFactsIssue(command.facts);
  if (factsIssue2) return rejected4(world, "InvalidFacts", factsIssue2);
  const payable = canPay(owner.runtime, activationCost(FAMILIAR_SHARED_SENSES_ACTION));
  if (!payable.ok) return rejected4(world, "InsufficientResources", "Familiar shared senses requires a Bonus Action");
  try {
    const ownerTurn = world.logicalClock;
    const after = activateFamiliarSharedSenses({
      familiar: familiar.familiarState,
      ownerActorId: owner.id,
      distanceFt: command.facts.distanceFt,
      ownerTurn,
      mechanicsPolicy: declaredPolicy.policy
    });
    const paid = pay(owner.runtime, activationCost(FAMILIAR_SHARED_SENSES_ACTION));
    const obligations = actionObligationIds(
      FAMILIAR_SHARED_SENSES_ACTION,
      "system:find-familiar",
      "system:bonus-action"
    );
    return [
      actionDeclaredEvent({
        actorId: owner.id,
        action: FAMILIAR_SHARED_SENSES_ACTION,
        targetIds: [familiar.id],
        timing: "active",
        facts: { ownerToFamiliar: { ...command.facts } },
        obligationIds: obligations
      }),
      ...runtimeTransition(owner.id, owner.id, owner.runtime, paid.state, "action", obligations),
      ...engineTrace(owner.id, [familiar.id], paid.events, obligations),
      familiarStateChangedEvent({
        ownerActorId: owner.id,
        familiarActorId: familiar.id,
        familiar: after,
        reason: "shared_senses_started",
        obligations
      })
    ];
  } catch (error) {
    return rejected4(world, "InvalidFacts", error instanceof Error ? error.message : "Invalid familiar senses facts");
  }
}
function dismissOwnedFamiliar(world, command) {
  const owner = world.actors[command.actorId];
  const familiar = requireOwnedFamiliar(world, owner.id, command.familiarActorId);
  if (!familiar?.familiarState) {
    return rejected4(world, "ActorNotFound", `Unknown owner familiar ${command.familiarActorId}`);
  }
  const payable = canPay(owner.runtime, activationCost(FAMILIAR_DISMISS_ACTION));
  if (!payable.ok) return rejected4(world, "InsufficientResources", "Dismissing a familiar requires a Magic Action");
  try {
    const result = dismissFamiliar({
      familiar: familiar.familiarState,
      ownerActorId: owner.id,
      mode: command.mode
    });
    const paid = pay(owner.runtime, activationCost(FAMILIAR_DISMISS_ACTION));
    const obligations = actionObligationIds(
      FAMILIAR_DISMISS_ACTION,
      "system:find-familiar",
      "system:familiar-lifecycle"
    );
    return [
      actionDeclaredEvent({
        actorId: owner.id,
        action: FAMILIAR_DISMISS_ACTION,
        targetIds: [familiar.id],
        timing: "active",
        facts: { mode: command.mode },
        obligationIds: obligations
      }),
      ...runtimeTransition(owner.id, owner.id, owner.runtime, paid.state, "action", obligations),
      ...engineTrace(owner.id, [familiar.id], paid.events, obligations),
      ...result.familiar ? [familiarStateChangedEvent({
        ownerActorId: owner.id,
        familiarActorId: familiar.id,
        familiar: result.familiar,
        reason: "temporary_dismissal",
        droppedItemIds: result.droppedItemIds,
        obligations
      })] : [{
        sourceActorId: owner.id,
        obligationIds: obligations,
        payload: {
          type: "FamiliarActorRemoved",
          ownerActorId: owner.id,
          familiarActorId: familiar.id,
          reason: "forever_dismissal",
          droppedItemIds: result.droppedItemIds
        }
      }]
    ];
  } catch (error) {
    return rejected4(world, "InvalidDecision", error instanceof Error ? error.message : "Invalid familiar dismissal");
  }
}
function reappearOwnedFamiliar(world, command, catalog, env) {
  const owner = world.actors[command.actorId];
  const familiar = requireOwnedFamiliar(world, owner.id, command.familiarActorId);
  if (!familiar?.familiarState) {
    return rejected4(world, "ActorNotFound", `Unknown owner familiar ${command.familiarActorId}`);
  }
  const declaredPolicy = familiarPolicyFromSummoningAction(familiar, catalog);
  if ("issue" in declaredPolicy) {
    return rejected4(world, "InvalidActionDefinition", declaredPolicy.issue);
  }
  const factsIssue2 = familiarObservableFactsIssue(command.facts);
  if (factsIssue2 || typeof command.facts.unoccupiedSpace !== "boolean") {
    return rejected4(world, "InvalidFacts", factsIssue2 ?? "Familiar reappearance requires an occupancy fact");
  }
  const payable = canPay(owner.runtime, activationCost(FAMILIAR_REAPPEAR_ACTION));
  if (!payable.ok) return rejected4(world, "InsufficientResources", "Reappearing a familiar requires a Magic Action");
  try {
    let after = reappearFamiliar({
      familiar: familiar.familiarState,
      ownerActorId: owner.id,
      distanceFt: command.facts.distanceFt,
      unoccupiedSpace: command.facts.unoccupiedSpace,
      mechanicsPolicy: declaredPolicy.policy
    });
    if (world.scene.mode === "encounter" && after.initiative.total === null) {
      after = rollFamiliarInitiative({
        familiar: after,
        modifier: familiar.familiarMetadata.initiativeModifier,
        rng: env.rng
      });
    }
    const paid = pay(owner.runtime, activationCost(FAMILIAR_REAPPEAR_ACTION));
    const obligations = actionObligationIds(
      FAMILIAR_REAPPEAR_ACTION,
      "system:find-familiar",
      "system:familiar-lifecycle"
    );
    return [
      actionDeclaredEvent({
        actorId: owner.id,
        action: FAMILIAR_REAPPEAR_ACTION,
        targetIds: [familiar.id],
        timing: "active",
        facts: { reappearance: { ...command.facts } },
        obligationIds: obligations
      }),
      ...runtimeTransition(owner.id, owner.id, owner.runtime, paid.state, "action", obligations),
      ...engineTrace(owner.id, [familiar.id], paid.events, obligations),
      familiarStateChangedEvent({
        ownerActorId: owner.id,
        familiarActorId: familiar.id,
        familiar: after,
        reason: "reappeared",
        obligations
      })
    ];
  } catch (error) {
    return rejected4(world, "InvalidFacts", error instanceof Error ? error.message : "Invalid familiar reappearance");
  }
}
function deliverTouchSpell(world, command, catalog, env) {
  const owner = world.actors[command.actorId];
  const familiar = requireOwnedFamiliar(world, owner.id, command.familiarActorId);
  const target2 = world.actors[command.targetActorId];
  if (!familiar?.familiarState) {
    return rejected4(world, "ActorNotFound", `Unknown owner familiar ${command.familiarActorId}`);
  }
  const declaredFamiliarPolicy = familiarPolicyFromSummoningAction(familiar, catalog);
  if ("issue" in declaredFamiliarPolicy) {
    return rejected4(world, "InvalidActionDefinition", declaredFamiliarPolicy.issue);
  }
  if (!target2) return rejected4(world, "ActorNotFound", `Unknown target ${command.targetActorId}`);
  const connectionIssue = familiarObservableFactsIssue(command.ownerToFamiliarFacts);
  const targetFactsIssue = spatialFactShapeIssue(command.familiarToTargetFacts);
  if (connectionIssue || targetFactsIssue) {
    return rejected4(world, "InvalidFacts", connectionIssue ?? targetFactsIssue);
  }
  const action = catalog.getAction(command.spellActionId);
  if (!action) return rejected4(world, "ActionNotFound", `Unknown action ${command.spellActionId}`);
  const definitionIssue = actionDefinitionIssue(action);
  if (definitionIssue) return rejected4(world, "InvalidActionDefinition", definitionIssue);
  if (!owner.capabilities.actionIds.includes(action.id)) {
    return rejected4(world, "ActionNotGranted", `${owner.id} does not own spell ${action.id}`);
  }
  if (!canonicalTouchSpell(action)) {
    return rejected4(world, "InvalidActionDefinition", `${action.id} is not a canonical Touch spell`);
  }
  const declarationIssue = spellDeclarationIssue(action, command.spell);
  if (declarationIssue) return rejected4(world, "InvalidSpellDeclaration", declarationIssue);
  if (action.kind !== "spell" || !owner.spellcastingAccess) {
    return rejected4(world, "InvalidSpellDeclaration", "Touch delivery requires source-scoped spellcasting access");
  }
  const preparation = prepareSpellExecution({
    action,
    accessState: owner.spellcastingAccess,
    resources: owner.runtime.resources,
    declaration: {
      ...command.spell?.grantId ? { grantId: command.spell.grantId } : {},
      ...command.spell?.mode ? { mode: command.spell.mode } : {},
      ...command.spell?.preferFreeUse !== void 0 ? { preferFreeUse: command.spell.preferFreeUse } : {}
    }
  });
  if (preparation.status === "rejected") {
    return rejected4(
      world,
      preparation.stage === "action_definition" ? "InvalidActionDefinition" : preparation.code === "SpellResourceUnavailable" ? "InsufficientResources" : "InvalidSpellDeclaration",
      preparation.message
    );
  }
  if (preparation.provenance.mode === "ritual") {
    return rejected4(world, "InvalidSpellDeclaration", "A delivered Touch spell cannot use ritual casting");
  }
  const executableAction = preparation.executableAction;
  const validation = actionValidation(
    world,
    executableAction,
    [target2.id],
    { [target2.id]: command.familiarToTargetFacts },
    owner.id
  );
  if (validation) return validation;
  if (actionDeclaresHarmfulInteraction(executableAction)) {
    const conditionDenial = harmfulConditionRejection({
      world,
      // The familiar delivers the spell as if it had cast it; relation-based
      // condition restrictions therefore belong to that concrete actor.
      attackerActorId: familiar.id,
      targetActorIds: [target2.id]
    });
    if (conditionDenial) return conditionDenial;
  }
  const payable = canPay(owner.runtime, activationCost(executableAction));
  if (!payable.ok) return rejected4(world, "InsufficientResources", `Missing resources: ${payable.missing.join(", ")}`);
  try {
    const delivery = deliverTouchSpellThroughFamiliar({
      familiar: familiar.familiarState,
      ownerActorId: owner.id,
      distanceFt: command.ownerToFamiliarFacts.distanceFt,
      spellActionId: action.id,
      spellRange: "touch",
      mechanicsPolicy: declaredFamiliarPolicy.policy
    });
    const spell = canonicalSpellContext(executableAction, command.spell, preparation);
    const authoritative = {
      schemaVersion: 1,
      type: "UseAction",
      commandId: command.commandId,
      expectedRevision: command.expectedRevision,
      rulesetContentHash: command.rulesetContentHash,
      actorId: owner.id,
      actionId: executableAction.id,
      targetIds: [target2.id],
      factsByTarget: { [target2.id]: command.familiarToTargetFacts },
      choices: command.choices,
      ...command.protectionCandidates ? {
        protectionCandidates: command.protectionCandidates.map((candidate) => ({ ...candidate }))
      } : {},
      spell
    };
    const obligations = actionObligationIds(
      executableAction,
      "system:find-familiar",
      "system:familiar-touch-delivery",
      "system:reaction"
    );
    const declaration = actionDeclaredEvent({
      actorId: owner.id,
      action: executableAction,
      targetIds: [target2.id],
      timing: "active",
      spell,
      facts: {
        deliveryActorId: familiar.id,
        ownerToFamiliar: { ...command.ownerToFamiliarFacts },
        familiarToTarget: { ...command.familiarToTargetFacts }
      },
      obligationIds: obligations
    });
    const reactionSpent = familiarStateChangedEvent({
      ownerActorId: owner.id,
      familiarActorId: familiar.id,
      familiar: delivery.familiar,
      reason: "touch_spell_delivered",
      obligations
    });
    const pending = pendingSaveEvents(world, authoritative, executableAction, env) ?? pendingAttackEvents(world, authoritative, executableAction, catalog, env);
    if (pending && !Array.isArray(pending)) return pending;
    return [
      declaration,
      ...pending ?? executeUseAction(world, authoritative, executableAction, env),
      reactionSpent
    ];
  } catch (error) {
    return rejected4(world, "InvalidDecision", error instanceof Error ? error.message : "Invalid familiar touch delivery");
  }
}
function performPactChainFamiliarAttack(world, command, catalog, env) {
  const validated = validateAttackAction(world, command.actorId, command.attackActionId);
  if ("rejection" in validated) return validated.rejection;
  const owner = world.actors[command.actorId];
  const familiar = requireOwnedFamiliar(world, owner.id, command.familiarActorId);
  const target2 = world.actors[command.targetActorId];
  const chain2 = owner.warlockPacts?.chain;
  if (!familiar?.familiarState || !familiar.familiarMetadata || !chain2 || familiar.familiarState.extension !== "pact_chain" || chain2.sourceEntityId !== familiar.familiarState.sourceEntityId || chain2.activeFamiliar?.actorId !== familiar.id) {
    return rejected4(world, "FeatureNotGranted", `${owner.id} has no active canonical Pact Chain familiar`);
  }
  if (!target2) return rejected4(world, "ActorNotFound", `Unknown target ${command.targetActorId}`);
  if (target2.id === familiar.id) return rejected4(world, "InvalidTargets", "A familiar cannot attack itself");
  const conditionDenial = harmfulConditionRejection({
    world,
    // Pact Chain substitutes the familiar's attack, not the owner's attack.
    attackerActorId: familiar.id,
    targetActorIds: [target2.id]
  });
  if (conditionDenial) return conditionDenial;
  const factsIssue2 = spatialFactShapeIssue(command.facts);
  if (factsIssue2) return rejected4(world, "InvalidFacts", factsIssue2);
  let action = familiarAttackRuleAction(familiar, command.familiarActionId);
  if (!action) {
    return rejected4(world, "InvalidActionDefinition", `${command.familiarActionId} is not a pinned familiar Attack`);
  }
  const validation = actionValidation(
    world,
    action,
    [target2.id],
    { [target2.id]: command.facts },
    familiar.id
  );
  if (validation) return validation;
  const pinned = familiar.familiarMetadata.actions.find((entry) => entry.id === action.id);
  const longRange = pinned.attack?.longRangeFt;
  const normalRange = pinned.attack?.normalRangeFt;
  const disadvantageReasons = pinned.attack?.mode === "ranged" && normalRange !== void 0 && longRange !== void 0 && command.facts.distanceFt > normalRange ? ["Long range"] : [];
  const sourceForAttack = disadvantageReasons.length ? {
    ...familiar,
    passives: [...familiar.passives ?? [], ...disadvantageReasons.map(attackDisadvantagePassive)]
  } : familiar;
  const targetForAttack = attackTargetWithCover(target2, command.facts.cover);
  try {
    const substitution = substitutePactChainFamiliarAttack({
      familiar: familiar.familiarState,
      ownerActorId: owner.id,
      policy: { kind: "pact_chain", sourceEntityId: chain2.sourceEntityId },
      sequence: validated.attackAction.sequence,
      familiarAttackActionId: action.id
    });
    const entry = substitution.sequence.entries.at(-1);
    action = {
      ...action,
      sourceEntityIds: [
        .../* @__PURE__ */ new Set([...entry.sourceEntityIds, ...action.sourceEntityIds])
      ]
    };
    const obligations = actionObligationIds(
      action,
      "system:attack-action",
      "system:attack-replacement",
      "system:pact-chain",
      "system:familiar-reaction"
    );
    const declaration = actionDeclaredEvent({
      actorId: familiar.id,
      action,
      targetIds: [target2.id],
      timing: "reaction",
      facts: {
        ownerActorId: owner.id,
        attackActionId: validated.attackAction.id,
        familiarActorId: familiar.id,
        familiarActionId: action.id,
        disadvantageReasons,
        spatial: { ...command.facts }
      },
      obligationIds: obligations
    });
    const entryEvent = attackEntryEvent({
      sourceActorId: owner.id,
      attackActionId: validated.attackAction.id,
      entry,
      obligations
    });
    const attackCommand = {
      ...command,
      type: "UseAction",
      actorId: familiar.id,
      actionId: action.id,
      targetIds: [target2.id],
      factsByTarget: { [target2.id]: command.facts }
    };
    const pending = pendingAttackEvents(
      {
        ...world,
        actors: {
          ...world.actors,
          [familiar.id]: sourceForAttack,
          [target2.id]: targetForAttack
        }
      },
      attackCommand,
      action,
      catalog,
      env,
      {
        attackActionId: validated.attackAction.id,
        preRollDisadvantageReasons: disadvantageReasons,
        continuationKind: "familiar_attack"
      }
    );
    if (pending && !Array.isArray(pending)) return pending;
    const reactionEvent = familiarStateChangedEvent({
      ownerActorId: owner.id,
      familiarActorId: familiar.id,
      familiar: substitution.familiar,
      reason: "chain_attack_reaction",
      obligations
    });
    if (pending) {
      const opened = pending.find((event) => event.payload.type === "ResolutionOpened");
      if (opened?.payload.type === "ResolutionOpened" && (opened.payload.resolution.type === "protection_reaction" || opened.payload.resolution.type === "attack_reaction")) {
        return [
          declaration,
          entryEvent,
          ...pending,
          reactionEvent,
          blockAttackActionEvent({
            actorId: owner.id,
            attackActionId: validated.attackAction.id,
            resolutionId: opened.payload.resolution.id,
            obligations
          })
        ];
      }
      return [
        declaration,
        entryEvent,
        ...pending,
        reactionEvent,
        ...completedAttackActionEvent({
          actorId: owner.id,
          attackActionId: validated.attackAction.id,
          attacksRemaining: substitution.sequence.attacksRemaining,
          obligations
        })
      ];
    }
    const result = executeAction(familiar.runtime, action.mechanics, {
      ...actionContext(sourceForAttack, env, targetForAttack, target2.runtime, command.facts)
    });
    const armor = resolveTemporaryHpMeleeRetaliationAfterAttack({
      world,
      attacker: familiar,
      defender: target2,
      attackerAfter: result.state,
      defenderAfter: result.targetState,
      action,
      attackEvents: result.events,
      env
    });
    const finalObligations = [.../* @__PURE__ */ new Set([
      ...obligations,
      ...armor.retaliationEvents.length ? ["system:temporary-hp-melee-retaliation", "system:retaliation"] : [],
      ...armor.retaliationSourceEntityIds.map((sourceId) => `entity:${sourceId}`)
    ])];
    return [
      declaration,
      entryEvent,
      ...actionStateEvents({
        world,
        commandId: command.commandId,
        source: familiar,
        action,
        sourceAfter: armor.attackerAfter,
        target: target2,
        targetAfter: armor.defenderAfter,
        obligations: finalObligations
      }),
      ...engineTrace(familiar.id, [target2.id], result.events, finalObligations, {
        facts: { ownerActorId: owner.id, spatial: { ...command.facts } }
      }),
      ...engineTrace(target2.id, [familiar.id], armor.retaliationEvents, finalObligations, {
        sourceActorId: target2.id,
        facts: { trigger: "temporary_hp_melee_retaliation" }
      }),
      reactionEvent,
      ...attackFollowUpEvents({
        world,
        commandId: command.commandId,
        source: familiar,
        sourceAfter: armor.attackerAfter,
        target: target2,
        targetAfter: armor.defenderAfter,
        action,
        deferred: result.deferredTargetSaves,
        env,
        obligations: finalObligations
      }),
      ...completedAttackActionEvent({
        actorId: owner.id,
        attackActionId: validated.attackAction.id,
        attacksRemaining: substitution.sequence.attacksRemaining,
        obligations
      })
    ];
  } catch (error) {
    return rejected4(world, "InvalidDecision", error instanceof Error ? error.message : "Invalid Pact Chain attack");
  }
}
function executeAttackReplacement(world, command, catalog, env) {
  const actor2 = world.actors[command.actorId];
  const action = catalog.getAction(command.actionId);
  if (!action) return rejected4(world, "ActionNotFound", `Unknown action ${command.actionId}`);
  const definitionIssue = actionDefinitionIssue(action);
  if (definitionIssue) return rejected4(world, "InvalidActionDefinition", definitionIssue);
  const replacement = action.attackReplacement;
  if (!replacement) {
    return rejected4(world, "InvalidActionTiming", `${action.id} cannot replace an Attack-action attack`);
  }
  if (!actor2.capabilities.actionIds.includes(action.id)) {
    return rejected4(world, "ActionNotGranted", `Actor ${actor2.id} does not own action ${action.id}`);
  }
  if (deniedCapabilities(actor2.runtime, actor2.passives ?? []).has("action")) {
    return rejected4(world, "CapabilityDenied", `${actor2.id} cannot take the Attack action`);
  }
  if (!command.targetIds.length) {
    return rejected4(world, "InvalidTargets", `${action.id} requires at least one factual area target`);
  }
  const validation = actionValidation(
    world,
    action,
    command.targetIds,
    command.factsByTarget,
    actor2.id
  );
  if (validation) return validation;
  const conditionDenial = harmfulConditionRejection({
    world,
    attackerActorId: actor2.id,
    targetActorIds: command.targetIds
  });
  if (conditionDenial) return conditionDenial;
  let startEvents = [];
  let worldWithAttack = world;
  let attackAction = openAttackAction(world, actor2.id);
  if (attackAction) {
    const validated = validateAttackAction(world, actor2.id, attackAction.id);
    if ("rejection" in validated) return validated.rejection;
    attackAction = validated.attackAction;
  } else {
    const started = beginAttackAction(world, { ...command, type: "BeginAttackAction" }, catalog, env);
    if (!Array.isArray(started)) return started;
    startEvents = started;
    worldWithAttack = foldEvents(
      world,
      started.map((event, ordinal) => ({ ...event, ordinal }))
    );
    attackAction = openAttackAction(worldWithAttack, actor2.id);
    if (!attackAction) {
      return rejected4(world, "InvalidActionDefinition", "Attack replacement failed to open its Attack ledger");
    }
  }
  let nextSequence;
  try {
    nextSequence = replaceSequenceAttack({
      sequence: attackAction.sequence,
      actionId: action.id,
      replacementKey: replacement.replacementKey,
      sourceEntityIds: [...action.sourceEntityIds],
      oncePerSequence: replacement.oncePerAttackAction
    });
  } catch (error) {
    return rejected4(
      world,
      "InvalidActionDefinition",
      error instanceof Error ? error.message : "Invalid Attack-action replacement policy"
    );
  }
  const entry = nextSequence.entries.at(-1);
  const entryAction = {
    ...action,
    mechanics: withoutActionResourceCost(action.mechanics)
  };
  const authoritativeCommand = {
    ...command,
    type: "UseAction"
  };
  const pending = pendingSaveEvents(
    worldWithAttack,
    authoritativeCommand,
    entryAction,
    env,
    void 0,
    attackAction.id
  );
  if (!pending) {
    return rejected4(world, "InvalidActionDefinition", `${action.id} did not produce a target save`);
  }
  if (!Array.isArray(pending)) return pending;
  const obligations = actionObligationIds(
    action,
    "system:action-declaration",
    "system:attack-action",
    "system:attack-replacement"
  );
  const opened = pending.find((event) => event.payload.type === "ResolutionOpened");
  if (!opened || opened.payload.type !== "ResolutionOpened") {
    return rejected4(world, "InvalidDecision", "Attack replacement did not open a durable resolution");
  }
  return [
    ...startEvents,
    actionDeclaredEvent({
      actorId: actor2.id,
      action,
      targetIds: command.targetIds,
      timing: "active",
      facts: {
        spatialByTarget: Object.fromEntries(command.targetIds.map((targetId) => [
          targetId,
          command.factsByTarget?.[targetId]
        ])),
        attackActionId: attackAction.id,
        attackEntryOrdinal: entry.ordinal,
        authoritativeAttacksPerAction: attackAction.sequence.totalAttacks,
        replacementPolicy: {
          replacementKey: replacement.replacementKey,
          replacesAttacks: replacement.replacesAttacks,
          oncePerAttackAction: replacement.oncePerAttackAction
        }
      },
      obligationIds: obligations
    }),
    attackEntryEvent({
      sourceActorId: actor2.id,
      attackActionId: attackAction.id,
      entry,
      obligations
    }),
    ...pending,
    blockAttackActionEvent({
      actorId: actor2.id,
      attackActionId: attackAction.id,
      resolutionId: opened.payload.resolution.id,
      obligations
    })
  ];
}
function pactTomeRestRejectionCode(code) {
  if (code === "ActorNotFound") return "ActorNotFound";
  if (code === "FeatureNotGranted") return "FeatureNotGranted";
  if (code === "RulesetMismatch") return "RulesetMismatch";
  if (code === "RevisionConflict") return "StaleRevision";
  if (code === "SpellResourceUnavailable") return "InsufficientResources";
  if (code === "InvalidSelection" || code === "InvalidCatalogAction" || code === "InvalidRestSelection" || code === "InvalidCommand") return "InvalidDecision";
  return "InvalidActionDefinition";
}
function pactTomeRestEvents(input) {
  const planned = planPactTomeRestTransition({
    world: input.world,
    catalog: input.catalog,
    actorId: input.actorId,
    commandId: input.commandId,
    rest: input.rest,
    selection: input.selection
  });
  if (planned.status === "rejected") {
    return rejected4(
      input.world,
      pactTomeRestRejectionCode(planned.code),
      planned.message
    );
  }
  return [{
    sourceActorId: input.actorId,
    obligationIds: [
      "system:pact-tome-rest",
      `system:${input.rest}-rest`,
      `entity:${planned.event.sourceEntityId}`
    ],
    payload: planned.event
  }];
}
function pactBladeRejectionCode(code) {
  if (code === "ActorNotFound") return "ActorNotFound";
  if (code === "FeatureNotGranted") return "FeatureNotGranted";
  if (code === "RulesetMismatch") return "RulesetMismatch";
  if (code === "RevisionConflict" || code === "WorldRevisionConflict") return "StaleRevision";
  if (code === "TurnUnavailable") return "InsufficientResources";
  if (code === "InvalidTouchFacts" || code === "InvalidDistanceFacts" || code === "InvalidDeathFacts" || code === "TouchRequired") return "InvalidFacts";
  if (code === "WeaponNotHeld") return "WeaponNotEquipped";
  if (code === "BladeUnavailable" || code === "WeaponMismatch") return "InvalidEquipmentState";
  if (code === "IllegalWeapon" || code === "IllegalAttackChoice") return "NotWeapon";
  return "InvalidActionDefinition";
}
function pactBladeBondEvents(input) {
  const { world, command, catalog, env } = input;
  let selection;
  if (command.mode === "conjure") {
    selection = {
      mode: "conjure",
      weaponCardId: command.weaponCardId,
      weaponObjectId: env.nextId(),
      conjureHand: command.hand
    };
  } else {
    const object5 = world.objects[command.weaponObjectId];
    if (!object5) {
      return rejected4(world, "WorldObjectNotFound", `Unknown world object ${command.weaponObjectId}`);
    }
    if (object5.kind !== "item" || !object5.itemCardId) {
      return rejected4(
        world,
        "InvalidEquipmentState",
        `${command.weaponObjectId} is not an item with an immutable Card bridge`
      );
    }
    selection = {
      mode: "touch_existing",
      weaponCardId: object5.itemCardId,
      weaponObjectId: object5.id,
      touchFacts: { ...command.facts, touched: command.facts.touched === true }
    };
  }
  const planned = planPactBladeBondTransition({
    world,
    catalog,
    actorId: command.actorId,
    commandId: command.commandId,
    selection
  });
  if (planned.status === "rejected") {
    return rejected4(world, pactBladeRejectionCode(planned.code), planned.message);
  }
  return [{
    sourceActorId: command.actorId,
    obligationIds: [
      "system:pact-blade-bond",
      "system:bonus-action",
      `entity:${planned.event.sourceEntityId}`,
      `entity:${planned.event.activeBlade.weaponCardId}`
    ],
    payload: planned.event
  }];
}
function pactBladeDistanceEvents(input) {
  const planned = planPactBladeDistanceTransition({
    world: input.world,
    catalog: input.catalog,
    actorId: input.command.actorId,
    commandId: input.command.commandId,
    weaponObjectId: input.command.weaponObjectId,
    facts: { ...input.command.facts }
  });
  if (planned.status === "rejected") {
    return rejected4(input.world, pactBladeRejectionCode(planned.code), planned.message);
  }
  return [{
    sourceActorId: input.command.actorId,
    obligationIds: [
      "system:pact-blade-distance",
      "system:explicit-time",
      `entity:${planned.event.sourceEntityId}`
    ],
    payload: planned.event
  }];
}
function adjudicateActorDeathEvents(input) {
  const { world, command, catalog } = input;
  const actor2 = world.actors[command.actorId];
  const fact = command.adjudication;
  if (actor2.lifecycle?.status !== "alive") {
    return rejected4(world, "InvalidFacts", `${actor2.id} has already been adjudicated dead`);
  }
  if (!fact || fact.type !== "ActorDeathAdjudicated" || fact.provenance !== "canonical_actor_lifecycle" || typeof fact.factId !== "string" || !fact.factId.trim() || typeof fact.adjudicatedBy !== "string" || !fact.adjudicatedBy.trim() || fact.actorId !== actor2.id || fact.observedAtWorldRevision !== world.revision || fact.rulesetContentHash !== world.ruleset.contentHash) {
    return rejected4(world, "InvalidFacts", "Actor death requires an exact canonical lifecycle fact");
  }
  const events = [{
    sourceActorId: actor2.id,
    obligationIds: [
      "system:actor-lifecycle",
      ...input.thresholdOrigin ? [
        "system:data-declared-condition-threshold",
        `system:condition:${input.thresholdOrigin.condition}`
      ] : ["system:explicit-death-adjudication"]
    ],
    payload: JSON.parse(JSON.stringify(fact))
  }];
  if (actor2.warlockPacts?.blade?.activeBond && actor2.warlockPacts.blade.lifecyclePolicy.endOnOwnerDeath) {
    const planned = planPactBladeOwnerDeathTransition({
      world,
      catalog,
      actorId: actor2.id,
      commandId: command.commandId,
      deathFact: fact
    });
    if (planned.status === "rejected") {
      return rejected4(world, pactBladeRejectionCode(planned.code), planned.message);
    }
    events.push({
      sourceActorId: actor2.id,
      obligationIds: [
        "system:actor-lifecycle",
        "system:pact-blade-owner-death",
        `entity:${planned.event.sourceEntityId}`
      ],
      payload: planned.event
    });
  }
  if (actor2.warlockPacts?.tome) {
    const planned = planPactTomeOwnerDeathTransition({
      world,
      catalog,
      actorId: actor2.id,
      commandId: command.commandId,
      deathFact: fact
    });
    if (planned.status === "rejected") {
      return rejected4(world, pactTomeRestRejectionCode(planned.code), planned.message);
    }
    events.push({
      sourceActorId: actor2.id,
      obligationIds: [
        "system:actor-lifecycle",
        "system:pact-tome-owner-death",
        `entity:${planned.event.sourceEntityId}`
      ],
      payload: planned.event
    });
  }
  const concentration = world.concentrations[actor2.id];
  if (concentration) {
    const obligations = [
      "system:actor-lifecycle",
      "system:concentration-incapacitated"
    ];
    const linkedRuntimes = /* @__PURE__ */ new Map();
    for (const link of concentration.effectLinks) {
      const linkedActor = world.actors[link.actorId];
      if (!linkedActor) continue;
      const current = linkedRuntimes.get(link.actorId) ?? linkedActor.runtime;
      const linkedEffect = current.activeEffects.find((effect) => effect.id === link.effectId);
      if (!linkedEffect) continue;
      linkedRuntimes.set(link.actorId, {
        ...current,
        activeEffects: current.activeEffects.filter((effect) => effect.id !== link.effectId)
      });
      events.push(...engineTrace(actor2.id, [link.actorId], [{
        type: "effect_expired",
        name: linkedEffect.name
      }], obligations));
    }
    for (const [linkedActorId, after] of [...linkedRuntimes.entries()].sort(([left], [right]) => left.localeCompare(right))) {
      events.push(...runtimeTransition(
        actor2.id,
        linkedActorId,
        world.actors[linkedActorId].runtime,
        after,
        "action",
        obligations
      ));
    }
    events.push(...concentrationWorldObjectCleanup(world, concentration, obligations), {
      sourceActorId: actor2.id,
      obligationIds: obligations,
      payload: {
        type: "ConcentrationCleared",
        sourceActorId: actor2.id,
        concentrationId: concentration.id,
        reason: "incapacitated"
      }
    });
  }
  return events;
}
function automaticTerminalConditionDeaths(input) {
  const terminalByActor = /* @__PURE__ */ new Map();
  for (const fact of terminalConditionFacts(input.world).sort((left, right) => left.actorId.localeCompare(right.actorId) || left.condition.localeCompare(right.condition) || left.level - right.level)) {
    if (!terminalByActor.has(fact.actorId)) terminalByActor.set(fact.actorId, fact);
  }
  let current = input.world;
  const events = [];
  for (const fact of terminalByActor.values()) {
    const actor2 = current.actors[fact.actorId];
    if (!actor2 || actor2.lifecycle?.status !== "alive") continue;
    const terminalCommandId = input.env.nextId();
    const adjudication = {
      type: "ActorDeathAdjudicated",
      provenance: "canonical_actor_lifecycle",
      factId: `${terminalCommandId}:${fact.condition}:${fact.level}`,
      actorId: actor2.id,
      adjudicatedBy: "system:data-declared-condition-threshold",
      observedAtWorldRevision: current.revision,
      rulesetContentHash: current.ruleset.contentHash
    };
    const planned = adjudicateActorDeathEvents({
      world: current,
      command: {
        schemaVersion: input.command.schemaVersion,
        type: "AdjudicateActorDeath",
        commandId: terminalCommandId,
        expectedRevision: current.revision,
        rulesetContentHash: current.ruleset.contentHash,
        actorId: actor2.id,
        adjudication
      },
      catalog: input.catalog,
      thresholdOrigin: { condition: fact.condition, level: fact.level }
    });
    if (!Array.isArray(planned)) return planned;
    events.push(...planned);
    current = foldEvents(
      current,
      planned.map((event, ordinal) => ({ ...event, ordinal }))
    );
  }
  return { events, world: current };
}
function executeCommand(world, command, catalog, env) {
  const actor2 = world.actors[command.actorId];
  switch (command.type) {
    case "StartEncounter": {
      const unique = new Set(command.initiative);
      if (command.initiative.length < 2 || unique.size !== command.initiative.length || command.initiative.some((actorId) => !world.actors[actorId])) {
        return rejected4(world, "InvalidInitiative", "Initiative must contain at least two unique world actors");
      }
      const deadParticipant = command.initiative.find((actorId) => world.actors[actorId].lifecycle?.status === "dead");
      if (deadParticipant) {
        return rejected4(
          world,
          "InvalidInitiative",
          `Adjudicated-dead actor ${deadParticipant} cannot join Initiative`
        );
      }
      const presentFamiliars = Object.values(world.actors).filter((candidate) => candidate.familiarState?.presence === "present");
      const missingFamiliar = presentFamiliars.find((candidate) => !unique.has(candidate.id));
      const unavailableFamiliar = command.initiative.map((actorId) => world.actors[actorId]).find((candidate) => candidate.familiarState && candidate.familiarState.presence !== "present");
      if (missingFamiliar || unavailableFamiliar) {
        return rejected4(
          world,
          "InvalidInitiative",
          missingFamiliar ? `Present familiar ${missingFamiliar.id} must roll its own Initiative` : `Unavailable familiar ${unavailableFamiliar.id} cannot join Initiative`
        );
      }
      const familiarInitiativeEvents = presentFamiliars.filter((candidate) => unique.has(candidate.id)).map((candidate) => familiarStateChangedEvent({
        ownerActorId: candidate.familiarState.ownerActorId,
        familiarActorId: candidate.id,
        familiar: rollFamiliarInitiative({
          familiar: candidate.familiarState,
          modifier: candidate.familiarMetadata.initiativeModifier,
          rng: env.rng
        }),
        reason: "initiative_rolled",
        obligations: ["system:initiative", "system:find-familiar"]
      }));
      return [...familiarInitiativeEvents, {
        sourceActorId: command.actorId,
        obligationIds: ["system:initiative"],
        payload: {
          type: "SceneSet",
          scene: {
            mode: "encounter",
            initiative: [...command.initiative],
            activeIndex: 0,
            round: 1,
            turnStarted: false,
            initiativeSwapActorIds: []
          }
        }
      }];
    }
    case "StartTurn": {
      const boundary = sourceTurnBoundary(world, actor2.id, "start");
      const before = boundary.runtimes.get(actor2.id) ?? actor2.runtime;
      const turnContext = {
        ...actorContext({ ...actor2, runtime: before }),
        rng: env.rng
      };
      const result = startTurn(before, turnContext);
      const scene = world.scene;
      const familiarLifecycleEvents = [];
      if (actor2.familiarState) {
        familiarLifecycleEvents.push(familiarStateChangedEvent({
          ownerActorId: actor2.familiarState.ownerActorId,
          familiarActorId: actor2.id,
          familiar: startFamiliarTurn({
            familiar: actor2.familiarState,
            familiarActorId: actor2.id
          }),
          reason: "turn_started",
          obligations: ["system:turn-start", "system:find-familiar", "system:reaction-refresh"]
        }));
      }
      for (const familiar of familiarActorsOwnedBy(world, actor2.id)) {
        if (!familiar.familiarState) continue;
        const after = startOwnerTurnForFamiliar({
          familiar: familiar.familiarState,
          ownerActorId: actor2.id,
          ownerTurn: world.logicalClock
        });
        if (JSON.stringify(after) !== JSON.stringify(familiar.familiarState)) {
          familiarLifecycleEvents.push(familiarStateChangedEvent({
            ownerActorId: actor2.id,
            familiarActorId: familiar.id,
            familiar: after,
            reason: "shared_senses_ended",
            obligations: ["system:turn-start", "system:find-familiar", "system:effect-lifecycle"]
          }));
        }
      }
      const protectionLifecycleEvents = [];
      for (const effect of actorProtectionEffects(actor2)) {
        const lifecycleEvent = {
          type: "turn_started",
          factsSource: "scenario",
          worldRevision: world.revision,
          actorId: actor2.id
        };
        const advanced = advanceProtection2024Effect(effect, lifecycleEvent);
        if (advanced.status !== "ended" || advanced.reason !== "protector_turn_started") {
          return rejected4(world, "InvalidDecision", `Protection turn lifecycle was rejected: ${advanced.reason}`);
        }
        protectionLifecycleEvents.push({
          sourceActorId: actor2.id,
          obligationIds: [
            "system:turn-start",
            "system:fighting-style-protection",
            "system:effect-lifecycle",
            ...effect.source.sourceEntityIds.map((id) => `entity:${id}`)
          ],
          payload: {
            type: "ProtectionEffectEnded",
            protectorActorId: actor2.id,
            protectedTargetActorId: effect.protectedTargetActorId,
            effectId: effect.id,
            reason: "protector_turn_started",
            lifecycleEvent
          }
        });
      }
      return [
        ...boundary.events,
        ...runtimeTransition(actor2.id, actor2.id, before, result.state, "start_turn", ["system:turn-start"]),
        ...engineTrace(actor2.id, [], result.events, ["system:turn-start"]),
        ...familiarLifecycleEvents,
        ...protectionLifecycleEvents,
        {
          sourceActorId: actor2.id,
          obligationIds: ["system:turn-start"],
          payload: { type: "SceneSet", scene: { ...scene, turnStarted: true } }
        }
      ];
    }
    case "EndTurn": {
      const boundary = sourceTurnBoundary(world, actor2.id, "end");
      const before = boundary.runtimes.get(actor2.id) ?? actor2.runtime;
      const turnContext = { ...actorContext({ ...actor2, runtime: before }), rng: env.rng };
      const result = endTurn(
        before,
        turnContext,
        { advanceRoundDurations: false }
      );
      const scene = world.scene;
      const nextIndex = (scene.activeIndex + 1) % scene.initiative.length;
      const nextRound = nextIndex === 0 ? scene.round + 1 : scene.round;
      const sourceRelative = endSourceActorTurnWorldObjects({
        objects: world.objects,
        sourceActorId: actor2.id
      });
      const elapsed = nextRound > scene.round ? advanceWorldObjectRounds({ objects: sourceRelative.objects, rounds: 1 }) : { objects: sourceRelative.objects, events: [] };
      return [
        ...openAttackAction(world, actor2.id) ? [{
          sourceActorId: actor2.id,
          obligationIds: ["system:attack-action", "system:turn-end"],
          payload: {
            type: "AttackActionClosed",
            attackActionId: openAttackAction(world, actor2.id).id,
            reason: "forfeited"
          }
        }] : [],
        ...boundary.events,
        ...runtimeTransition(actor2.id, actor2.id, before, result.state, "end_turn", ["system:turn-end"]),
        ...engineTrace(actor2.id, [], result.events, ["system:turn-end"]),
        ...worldObjectEvents(
          actor2.id,
          CORE_WORLD_TIME_ACTION,
          sourceRelative.events,
          "system:world-object-source-turn-duration"
        ),
        ...worldObjectEvents(
          actor2.id,
          CORE_WORLD_TIME_ACTION,
          elapsed.events,
          "system:world-object-duration"
        ),
        {
          sourceActorId: actor2.id,
          obligationIds: ["system:turn-order"],
          payload: { type: "SceneSet", scene: { ...scene, activeIndex: nextIndex, round: nextRound, turnStarted: false } }
        }
      ];
    }
    case "TakeShortRest": {
      const context2 = { ...actorContext(actor2), rng: env.rng };
      const result = shortRest(actor2.runtime, context2);
      const rawDecisions = command.decisions ?? [];
      if (!Array.isArray(rawDecisions) || rawDecisions.some((decision) => !decision || typeof decision !== "object" || typeof decision.type !== "string" || !Array.isArray(decision.slotLevels))) {
        return rejected4(world, "InvalidDecision", "Short Rest decisions are malformed");
      }
      const availableRestActions = catalog.listActions?.().filter((action) => action.restDecision?.rest === "short_rest") ?? [];
      const decisions = command.decisions ?? [];
      const selectedPolicies = [];
      for (const decision of decisions) {
        const matches = availableRestActions.flatMap((action) => {
          const policy = action.restDecision;
          if (!policy || policy.decisionType !== decision.type) return [];
          const sources = actor2.capabilities.featureSources?.[policy.capabilityId];
          if (!sources || !action.sourceEntityIds.some((sourceId) => sources.includes(sourceId))) {
            return [];
          }
          return [{ action, policy, sources }];
        });
        if (matches.length === 0) {
          return rejected4(world, "FeatureNotGranted", `${actor2.id} does not own ${decision.type}`);
        }
        if (matches.length !== 1) {
          return rejected4(world, "InvalidDecision", `${decision.type} has ambiguous catalog policies`);
        }
        selectedPolicies.push(matches[0]);
      }
      const countsByAction = /* @__PURE__ */ new Map();
      for (const selection of selectedPolicies) {
        const count = (countsByAction.get(selection.action.id) ?? 0) + 1;
        if (count > selection.policy.maximumPerRest) {
          return rejected4(
            world,
            "InvalidDecision",
            `${selection.policy.decisionType} can be selected at most ${selection.policy.maximumPerRest} time(s) per rest`
          );
        }
        countsByAction.set(selection.action.id, count);
      }
      let stateAfterRest = result.state;
      const recoveryEvents = [];
      for (let index = 0; index < decisions.length; index += 1) {
        const selection = selectedPolicies[index];
        const recovery = resolveSlotRecoveryRestDecision({
          state: stateAfterRest,
          classLevels: actor2.character.classLevels,
          policy: selection.policy,
          decision: decisions[index]
        });
        if (recovery.status === "rejected") {
          return rejected4(world, "InvalidDecision", recovery.message);
        }
        stateAfterRest = recovery.state;
        recoveryEvents.push({
          type: "resource_spent",
          ...recovery.spentResource
        });
        for (const restored of recovery.restoredResources) {
          recoveryEvents.push({
            type: "resource_restored",
            ...restored
          });
        }
      }
      const obligations = [
        "system:short-rest",
        "system:resource-recharge",
        ...selectedPolicies.map(({ policy }) => `capability:${policy.capabilityId}`),
        ...selectedPolicies.flatMap(({ sources }) => sources.map((sourceId) => `entity:${sourceId}`))
      ];
      const tomeEvents = command.pactTome ? pactTomeRestEvents({
        world,
        catalog,
        actorId: actor2.id,
        commandId: command.commandId,
        rest: "short",
        selection: command.pactTome
      }) : [];
      if (!Array.isArray(tomeEvents)) return tomeEvents;
      return [
        ...runtimeTransition(actor2.id, actor2.id, actor2.runtime, stateAfterRest, "short_rest", obligations),
        ...engineTrace(actor2.id, [], [...result.events, ...recoveryEvents], obligations),
        ...tomeEvents
      ];
    }
    case "TakeLongRest": {
      const durationHours = command.durationHours ?? 8;
      const eligibility = longRestEligibility(actor2.traits, durationHours);
      if (!eligibility.eligible) {
        return rejected4(
          world,
          "InvalidFacts",
          `Long Rest requires ${eligibility.requiredHours} hours; received ${durationHours}`
        );
      }
      const context2 = { ...actorContext(actor2), rng: env.rng };
      const result = longRest(actor2.runtime, context2);
      const obligations = [
        "system:long-rest",
        "system:resource-recharge",
        "system:rest-duration",
        ...(actor2.traits?.restProfile?.sourceEntityIds ?? []).map((id) => `entity:${id}`)
      ];
      const tomeEvents = command.pactTome ? pactTomeRestEvents({
        world,
        catalog,
        actorId: actor2.id,
        commandId: command.commandId,
        rest: "long",
        selection: command.pactTome
      }) : [];
      if (!Array.isArray(tomeEvents)) return tomeEvents;
      return [
        ...runtimeTransition(actor2.id, actor2.id, actor2.runtime, result.state, "long_rest", obligations),
        ...engineTrace(actor2.id, [], result.events, obligations),
        ...tomeEvents
      ];
    }
    case "UseAttackReplacement":
      return executeAttackReplacement(world, command, catalog, env);
    case "BeginAttackAction":
      return beginAttackAction(world, command, catalog, env);
    case "PerformWeaponAttack":
      return performWeaponAttack(world, command, catalog, env);
    case "PerformLightWeaponExtraAttack":
      return performLightWeaponExtraAttack(world, command, catalog, env);
    case "PerformWeaponMasteryCleaveAttack":
      return performWeaponMasteryCleaveAttack(world, command, catalog, env);
    case "PerformUnarmedStrike":
      return executeUnarmedStrike(world, command, catalog, env);
    case "PerformPactChainFamiliarAttack":
      return performPactChainFamiliarAttack(world, command, catalog, env);
    case "BondPactBlade":
      return pactBladeBondEvents({ world, command, catalog, env });
    case "ObservePactBladeDistance":
      return pactBladeDistanceEvents({ world, command, catalog });
    case "AdjudicateActorDeath":
      return adjudicateActorDeathEvents({ world, command, catalog });
    case "ForfeitAttackAction":
      return forfeitAttackAction(world, command);
    case "EscapeGrapple":
      return openEscapeGrapple(world, command, env);
    case "ReleaseGrapple":
      return releaseGrapple(world, command);
    case "BreakGrappleRange":
      return breakGrappleRange(world, command);
    case "ObserveProtectionProximity":
      return observeProtectionProximity(world, command);
    case "UseAction": {
      if (actor2.warlockPacts?.blade?.bondActionId === command.actionId) {
        return rejected4(
          world,
          "InvalidActionTiming",
          `${command.actionId} is a canonical Pact Blade transition and must use BondPactBlade`
        );
      }
      const action = catalog.getAction(command.actionId);
      if (!action) return rejected4(world, "ActionNotFound", `Unknown action ${command.actionId}`);
      const definitionIssue = actionDefinitionIssue(action);
      if (definitionIssue) return rejected4(world, "InvalidActionDefinition", definitionIssue);
      if (!actor2.capabilities.actionIds.includes(action.id)) {
        return rejected4(world, "ActionNotGranted", `Actor ${actor2.id} does not own action ${action.id}`);
      }
      if (action.attackReplacement) {
        return rejected4(
          world,
          "InvalidActionTiming",
          `${action.id} must replace an attack through the Attack-action sequence`
        );
      }
      if (activationMode(action) === "reaction") {
        return rejected4(world, "InvalidActionTiming", `${action.id} can only be used in a reaction window`);
      }
      const requiredCapability = requiredActionCapability(action);
      if (deniedCapabilities(actor2.runtime, actor2.passives ?? []).has(requiredCapability)) {
        return rejected4(world, "CapabilityDenied", `${actor2.id} cannot use ${requiredCapability} in its current state`);
      }
      const declarationIssue = spellDeclarationIssue(action, command.spell);
      if (declarationIssue) return rejected4(world, "InvalidSpellDeclaration", declarationIssue);
      let preparedSpell;
      let spellAudit;
      let pactTomeFocusObjectId;
      const pactBladeFocusEvents = [];
      let executableAction = action;
      if (action.kind === "spell" && actor2.spellcastingAccess) {
        const preparation = prepareSpellExecution({
          action,
          accessState: actor2.spellcastingAccess,
          resources: actor2.runtime.resources,
          declaration: {
            ...command.spell?.grantId ? { grantId: command.spell.grantId } : {},
            ...command.spell?.mode ? { mode: command.spell.mode } : {},
            ...command.spell?.preferFreeUse !== void 0 ? { preferFreeUse: command.spell.preferFreeUse } : {}
          }
        });
        if (preparation.status === "rejected") {
          if (preparation.stage === "action_definition") {
            return rejected4(world, "InvalidActionDefinition", preparation.message);
          }
          return rejected4(
            world,
            preparation.code === "SpellResourceUnavailable" ? "InsufficientResources" : "InvalidSpellDeclaration",
            preparation.message
          );
        }
        if (preparation.provenance.mode === "ritual" && world.scene.mode === "encounter") {
          return rejected4(world, "InvalidActionTiming", "A ritual cast requires additional casting time");
        }
        const tomeAudit = pactTomeSpellCastAudit({
          world,
          actorId: actor2.id,
          actionId: preparation.executableAction.id,
          grantId: preparation.provenance.grantId,
          sourceId: preparation.provenance.sourceId,
          mode: preparation.provenance.mode,
          payment: preparation.payment
        });
        if (tomeAudit.status === "rejected") {
          return rejected4(world, "InvalidActionDefinition", tomeAudit.message);
        }
        spellAudit = tomeAudit.status === "ready" ? {
          focusObjectId: tomeAudit.focusObjectId,
          castingTimeAddedSeconds: tomeAudit.castingTimeAddedSeconds
        } : preparation.provenance.mode === "ritual" ? { castingTimeAddedSeconds: PACT_TOME_RITUAL_CASTING_TIME_ADDED_SECONDS } : void 0;
        pactTomeFocusObjectId = tomeAudit.status === "ready" ? tomeAudit.focusObjectId : void 0;
        preparedSpell = preparation;
        executableAction = preparation.executableAction;
      }
      const requestedBladeFocus = command.spell?.focusObjectId !== void 0 || command.spell?.focusHand !== void 0;
      if (requestedBladeFocus) {
        if (!command.spell?.focusObjectId || !command.spell.focusHand) {
          return rejected4(
            world,
            "InvalidSpellDeclaration",
            "A Pact Blade focus requires both its object identity and held hand"
          );
        }
        if (pactTomeFocusObjectId) {
          return rejected4(
            world,
            "InvalidSpellDeclaration",
            "A Pact Tome sourced cast retains its Book of Shadows focus authority"
          );
        }
        const focus = planPactBladeMaterialFocus({
          world,
          catalog,
          actorId: actor2.id,
          commandId: command.commandId,
          actionId: executableAction.id,
          weaponObjectId: command.spell.focusObjectId,
          hand: command.spell.focusHand
        });
        if (focus.status === "rejected") {
          return rejected4(world, pactBladeRejectionCode(focus.code), focus.message);
        }
        spellAudit = {
          ...spellAudit ?? {},
          focusObjectId: focus.event.weaponObjectId,
          focusHand: focus.event.focusHand
        };
        pactBladeFocusEvents.push({
          sourceActorId: actor2.id,
          obligationIds: [
            "system:spell-components",
            "system:pact-blade-material-focus",
            `entity:${focus.event.sourceEntityId}`,
            `entity:${focus.event.actionId}`
          ],
          payload: focus.event
        });
      }
      if (worldActionPrimitive(executableAction) === "temporary_hp_melee_retaliation") {
        executableAction = {
          ...executableAction,
          mechanics: { ...executableAction.mechanics, effects: [] }
        };
      }
      const activationCastTime = parseActivationCastTime(executableAction.mechanics);
      if (activationCastTime.status === "invalid") {
        return rejected4(
          world,
          "InvalidActionDefinition",
          `${executableAction.id}: ${activationCastTime.issue}`
        );
      }
      const executablePrimitive = executableAction.mechanics.primitive;
      if (executablePrimitive?.type === FIND_FAMILIAR_PRIMITIVE) {
        const familiarPolicy = parseFindFamiliarMechanicsPolicy(executableAction.mechanics);
        if (familiarPolicy.status === "invalid") {
          return rejected4(
            world,
            "InvalidActionDefinition",
            `${executableAction.id}: ${familiarPolicy.issue}`
          );
        }
        if (preparedSpell?.provenance.mode === "ritual") {
          spellAudit = {
            ...spellAudit ?? {},
            castingTimeAddedSeconds: familiarPolicy.policy.ritualCastingAddedSeconds
          };
        }
      }
      if (activationCastTime.status === "valid") {
        if (world.scene.mode === "encounter" && !activationCastTime.policy.atomicInEncounter && executablePrimitive?.type !== FIND_FAMILIAR_PRIMITIVE) {
          return rejected4(
            world,
            "InvalidActionTiming",
            `${executableAction.id} requires ${activationCastTime.policy.seconds} seconds and cannot complete atomically in an encounter`
          );
        }
        if (executableAction.kind === "spell") {
          spellAudit = {
            ...spellAudit ?? {},
            baseCastingTimeSeconds: activationCastTime.policy.seconds
          };
        }
      }
      const payable = canPay(actor2.runtime, activationCost(executableAction));
      if (!payable.ok) {
        return rejected4(
          world,
          "InsufficientResources",
          `Missing resources: ${payable.missing.join(", ")}`
        );
      }
      const primitive = executableAction.mechanics.primitive;
      const missileSpec = magicMissileSpec(executableAction);
      if (primitive?.type === "magic_missile" && !missileSpec) {
        return rejected4(world, "InvalidActionDefinition", `${executableAction.id} has invalid Magic Missile primitive metadata`);
      }
      const validation = actionValidation(
        world,
        executableAction,
        command.targetIds,
        command.factsByTarget,
        actor2.id
      );
      if (validation) return validation;
      if (actionDeclaresHarmfulInteraction(executableAction)) {
        const conditionDenial = harmfulConditionRejection({
          world,
          attackerActorId: actor2.id,
          targetActorIds: command.targetIds
        });
        if (conditionDenial) return conditionDenial;
      }
      const spell = canonicalSpellContext(
        executableAction,
        command.spell,
        preparedSpell,
        spellAudit
      );
      const authoritativeCommand = { ...command, spell };
      if (executableAction.mechanics.primitive?.type === FIND_FAMILIAR_PRIMITIVE) {
        const declaration2 = actionDeclaredEvent({
          actorId: actor2.id,
          action: executableAction,
          targetIds: authoritativeCommand.targetIds,
          timing: "active",
          spell,
          facts: {
            choices: JSON.parse(JSON.stringify(authoritativeCommand.choices ?? {}))
          },
          obligationIds: actionObligationIds(executableAction, "system:action-declaration")
        });
        const familiar = executeFindFamiliarCast({
          world,
          command: authoritativeCommand,
          action: executableAction,
          env
        });
        return Array.isArray(familiar) ? [...pactBladeFocusEvents, declaration2, ...familiar] : familiar;
      }
      const missileAllocation = missileSpec ? magicMissileAllocation(authoritativeCommand, missileSpec) : null;
      if (missileAllocation && "issue" in missileAllocation) {
        return rejected4(world, "InvalidTargets", missileAllocation.issue);
      }
      const worldExecution = executeWorldActionPrimitive(
        world,
        authoritativeCommand,
        executableAction,
        env
      );
      if (!Array.isArray(worldExecution)) return worldExecution;
      const declaration = actionDeclaredEvent({
        actorId: actor2.id,
        action: executableAction,
        targetIds: authoritativeCommand.targetIds,
        timing: "active",
        spell,
        ...authoritativeCommand.targetIds.length || authoritativeCommand.worldInput ? {
          facts: {
            ...authoritativeCommand.targetIds.length ? {
              spatialByTarget: Object.fromEntries(authoritativeCommand.targetIds.map((targetId) => [
                targetId,
                authoritativeCommand.factsByTarget?.[targetId]
              ]))
            } : {},
            ...authoritativeCommand.worldInput ? {
              worldInput: JSON.parse(JSON.stringify(authoritativeCommand.worldInput))
            } : {},
            ...missileAllocation && !("issue" in missileAllocation) ? {
              magicMissileDartTargetIds: missileAllocation.dartTargetIds,
              simultaneous: missileSpec?.simultaneous
            } : {}
          }
        } : {},
        obligationIds: actionObligationIds(executableAction, "system:action-declaration")
      });
      if (missileSpec) {
        const missile = magicMissileEvents(
          world,
          authoritativeCommand,
          executableAction,
          missileSpec,
          catalog,
          env
        );
        return Array.isArray(missile) ? [...pactBladeFocusEvents, declaration, ...missile, ...worldExecution] : missile;
      }
      const pending = pendingSaveEvents(world, authoritativeCommand, executableAction, env);
      if (pending) return Array.isArray(pending) ? [...pactBladeFocusEvents, declaration, ...pending, ...worldExecution] : pending;
      const pendingAttack = pendingAttackEvents(
        world,
        authoritativeCommand,
        executableAction,
        catalog,
        env,
        worldActionPrimitive(executableAction) ? { externalPrimitiveHandled: true } : {}
      );
      if (pendingAttack) return Array.isArray(pendingAttack) ? [...pactBladeFocusEvents, declaration, ...pendingAttack, ...worldExecution] : pendingAttack;
      return [
        ...pactBladeFocusEvents,
        declaration,
        ...executeUseAction(world, authoritativeCommand, executableAction, env, {
          skipReplacedConcentrationWorldObjectCleanup: primitive?.type === "dancing_lights_world",
          ...worldActionPrimitive(executableAction) ? { externalPrimitiveHandled: true } : {}
        }),
        ...worldExecution
      ];
    }
    case "AbilityCheck":
      return executeCheck(world, command, env);
    case "AttemptHide":
      return executeHide(world, command, env);
    case "MakeNoise":
      return recordNoise(world, command);
    case "FindHiddenActor":
      return recordEnemyFinding(world, command);
    case "SwapInitiative":
      return swapAlertInitiative(world, command);
    case "TriggerHazard":
      return openHazardSave(world, command, catalog, env);
    case "SavingThrow":
      return executeSave(world, command, env);
    case "StudyWorldObject":
      return studyWorldObject(world, command, env);
    case "PhysicallyInteractWorldObject":
      return physicallyInteractWorldObject(world, command);
    case "RevealMagicAura":
      return revealMagicAura(world, command, catalog);
    case "MoveDancingLights":
      return moveActiveDancingLights(world, command, catalog);
    case "ObservePoisonDisease":
      return observeActivePoisonDisease(world, command, catalog);
    case "DonArmor":
      return donArmor(world, command);
    case "UseFamiliarSharedSenses":
      return activateOwnedFamiliarSharedSenses(world, command, catalog);
    case "DismissFamiliar":
      return dismissOwnedFamiliar(world, command);
    case "ReappearFamiliar":
      return reappearOwnedFamiliar(world, command, catalog, env);
    case "DeliverTouchSpellThroughFamiliar":
      return deliverTouchSpell(world, command, catalog, env);
    case "ResolveDecision":
      return world.pendingResolution?.type === "protection_reaction" ? resolvePendingProtection(world, command, catalog, env) : world.pendingResolution?.type === "attack_reaction" ? resolvePendingAttack(world, command, catalog, env) : world.pendingResolution?.type === "unarmed_save" ? resolveUnarmedSave(world, command, env) : world.pendingResolution?.type === "shove_outcome" ? resolveShoveOutcome(world, command, env) : world.pendingResolution?.type === "escape_grapple" ? resolveEscapeGrapple(world, command, env) : world.pendingResolution?.type === "magic_missile_reaction" ? resolveMagicMissileReaction(world, command, catalog, env) : world.pendingResolution?.type === "mastery_save" ? resolveMasterySave(world, command, env) : world.pendingResolution?.type === "concentration_save" ? resolveConcentrationSave(world, command, env) : world.pendingResolution?.type === "hazard_save" ? resolveHazardSave(world, command, env) : resolvePendingSave(world, command, catalog, env);
  }
}
function handleCommand(world, command, catalog, env) {
  const common = validateCommon(world, command);
  if (common) return common;
  const lock = validateResolutionLock(world, command);
  if (lock) return lock;
  const turn = validateTurn(world, command);
  if (turn) return turn;
  let generatedIdOrdinal = 0;
  const commandEnv = {
    ...env,
    nextId: () => `${command.commandId}:id:${++generatedIdOrdinal}`
  };
  const execution = executeCommand(world, command, catalog, commandEnv);
  if (!Array.isArray(execution)) return execution;
  const provisionalEvents = execution.map((event, ordinal) => ({ ...event, ordinal }));
  const provisional = foldEvents(world, provisionalEvents);
  const terminal = automaticTerminalConditionDeaths({
    world: provisional,
    command,
    catalog,
    env: commandEnv
  });
  if ("status" in terminal) {
    if (terminal.status === "accepted") {
      return rejected4(world, "InvalidDecision", "Terminal condition lifecycle returned an invalid nested acceptance");
    }
    return rejected4(world, terminal.code, terminal.message);
  }
  const postTerminal = terminal.world;
  const automaticArmorOfAgathysEnds = Object.values(postTerminal.actors).flatMap((actor2) => {
    if (actor2.runtime.hp.temp > 0) return [];
    const activeArmor = actor2.runtime.activeEffects.filter((effect) => effect.mechanics.kind === "temporary_hp_melee_retaliation");
    if (!activeArmor.length) return [];
    const after = endArmorOfAgathysWithoutTemporaryHp(actor2.runtime);
    return runtimeTransition(
      command.actorId,
      actor2.id,
      actor2.runtime,
      after,
      "action",
      [.../* @__PURE__ */ new Set([
        "system:temporary-hp-melee-retaliation",
        "system:temporary-hit-points",
        "system:effect-lifecycle",
        ...activeArmor.flatMap((effect) => {
          const mechanics = effect.mechanics;
          return Array.isArray(mechanics.sourceEntityIds) ? mechanics.sourceEntityIds.map((sourceId) => `entity:${String(sourceId)}`) : [];
        })
      ])]
    );
  });
  const automaticFamiliarDisappears = Object.values(postTerminal.actors).flatMap((actor2) => {
    if (!actor2.familiarState || actor2.familiarState.presence !== "present" || actor2.runtime.hp.current > 0) return [];
    const disappearance = familiarDropsToZeroHp(actor2.familiarState);
    if (!disappearance.familiar) return [];
    return [familiarStateChangedEvent({
      ownerActorId: actor2.familiarState.ownerActorId,
      familiarActorId: actor2.id,
      familiar: disappearance.familiar,
      reason: "zero_hp",
      droppedItemIds: disappearance.droppedItemIds,
      obligations: [
        "system:find-familiar",
        "system:familiar-lifecycle",
        "system:zero-hit-points"
      ]
    })];
  });
  const automaticGrappleEnds = Object.values(postTerminal.grapples).filter((grapple) => {
    const grappler = postTerminal.actors[grapple.grapplerActorId];
    return grappler && (grappler.lifecycle?.status === "dead" || activeConditionsOf(grappler.runtime).has("incapacitated"));
  }).map((grapple) => ({
    sourceActorId: grapple.grapplerActorId,
    obligationIds: ["system:grapple-lifecycle", "system:condition:incapacitated"],
    payload: {
      type: "GrappleEnded",
      grappleId: grapple.id,
      reason: "grappler_incapacitated"
    }
  }));
  const nextRevision = world.revision + 1;
  const rawEvents = [
    ...execution,
    ...terminal.events,
    ...automaticArmorOfAgathysEnds,
    ...automaticFamiliarDisappears,
    ...automaticGrappleEnds,
    {
      sourceActorId: command.actorId,
      obligationIds: ["system:command-commit"],
      payload: {
        type: "CommandCommitted",
        commandId: command.commandId,
        revision: nextRevision,
        logicalClock: commandEnv.clock()
      }
    }
  ];
  const events = rawEvents.map((event, ordinal) => ({ ...event, ordinal }));
  return { status: "accepted", events, nextState: foldEvents(world, events) };
}

// src/rules-core/domain.ts
function defaultAttackProfile(actor2) {
  return {
    attacksPerAction: 1,
    size: actor2.character.baseSize ?? 2,
    reachFt: 5,
    graspingParts: ["main_hand", "off_hand"],
    sourceEntityIds: ["system:dnd5e-2024:attack-action"]
  };
}

// src/rules-core/worldMigration.ts
function record8(value, label) {
  if (!value || typeof value !== "object" || Array.isArray(value)) {
    throw new Error(`${label} must be an object`);
  }
  return value;
}
var WORLD_OBJECT_KINDS = /* @__PURE__ */ new Set(["environment", "item", "spell_effect"]);
var WORLD_OBJECT_SIZES = /* @__PURE__ */ new Set([
  "tiny",
  "small",
  "medium",
  "large",
  "huge",
  "gargantuan"
]);
var ABILITIES3 = /* @__PURE__ */ new Set(["str", "dex", "con", "int", "wis", "cha"]);
var SPELL_ACCESS_KINDS2 = /* @__PURE__ */ new Set([
  "cantrip",
  "known",
  "spellbook",
  "always_prepared",
  "innate",
  "ritual_only"
]);
var ATTACK_ACTION_STATUSES = /* @__PURE__ */ new Set(["open", "completed", "forfeited"]);
function positiveInteger3(value, path) {
  if (!Number.isInteger(value) || Number(value) < 1) {
    throw new Error(`${path} must be a positive integer`);
  }
  return Number(value);
}
function positiveFinite(value, path) {
  if (!Number.isFinite(value) || Number(value) <= 0) {
    throw new Error(`${path} must be a positive number`);
  }
  return Number(value);
}
function normalizeAttackProfile(input) {
  if (input.value === void 0) {
    if (!input.legacy) throw new Error(`${input.path} is required in world schema 4`);
    return defaultAttackProfile(input.actor);
  }
  const raw = record8(input.value, input.path);
  const attacksPerAction = positiveInteger3(raw.attacksPerAction, `${input.path}.attacksPerAction`);
  if (!Number.isInteger(raw.size) || Number(raw.size) < 0 || Number(raw.size) > 5) {
    throw new Error(`${input.path}.size must be an integer from 0 to 5`);
  }
  const reachFt = positiveFinite(raw.reachFt, `${input.path}.reachFt`);
  const graspingParts = uniqueStringArray(raw.graspingParts, `${input.path}.graspingParts`);
  const sourceEntityIds = uniqueStringArray(raw.sourceEntityIds, `${input.path}.sourceEntityIds`);
  if (!sourceEntityIds.length) throw new Error(`${input.path}.sourceEntityIds cannot be empty`);
  return {
    attacksPerAction,
    size: Number(raw.size),
    reachFt,
    graspingParts,
    sourceEntityIds
  };
}
function nonBlankString2(value, path) {
  if (typeof value !== "string" || value.trim().length === 0) {
    throw new Error(`${path} must be a non-empty string`);
  }
  return value;
}
function uniqueStringArray(value, path) {
  if (!Array.isArray(value)) throw new Error(`${path} must be an array`);
  const strings = value.map((item, index) => nonBlankString2(item, `${path}[${index}]`));
  if (new Set(strings).size !== strings.length) throw new Error(`${path} must contain unique IDs`);
  return [...strings].sort((left, right) => left.localeCompare(right));
}
function optionalResource(value, path) {
  return value === void 0 ? void 0 : nonBlankString2(value, path);
}
function exactKeys6(value, expected, path) {
  const actual = Object.keys(value).sort();
  const wanted = [...expected].sort();
  if (actual.length !== wanted.length || actual.some((key, index) => key !== wanted[index])) {
    throw new Error(`${path} must contain exactly ${wanted.join(", ")}`);
  }
}
function normalizeResourceRecovery(input) {
  if (input.value === void 0) return void 0;
  const path = `${input.actorPath}.character.resourceRecovery`;
  const declarations = record8(input.value, path);
  const runtime = record8(input.runtime, `${input.actorPath}.runtime`);
  const resources = record8(runtime.resources, `${input.actorPath}.runtime.resources`);
  const maximums = record8(runtime.maxResources, `${input.actorPath}.runtime.maxResources`);
  return Object.fromEntries(Object.entries(declarations).sort(([left], [right]) => left.localeCompare(right)).map(([resource, rawPolicy]) => {
    nonBlankString2(resource, `${path} key`);
    if (!(resource in resources) || !(resource in maximums)) {
      throw new Error(`${path}.${resource} must map to an actor resource and maximum`);
    }
    if (rawPolicy === null) return [resource, null];
    const policyPath = `${path}.${resource}`;
    const policy = record8(rawPolicy, policyPath);
    exactKeys6(policy, ["short_rest", "long_rest"], policyPath);
    const shortRest2 = record8(policy.short_rest, `${policyPath}.short_rest`);
    const longRest2 = record8(policy.long_rest, `${policyPath}.long_rest`);
    exactKeys6(shortRest2, ["mode", "amount"], `${policyPath}.short_rest`);
    exactKeys6(longRest2, ["mode"], `${policyPath}.long_rest`);
    if (shortRest2.mode !== "fixed" || !Number.isSafeInteger(shortRest2.amount) || Number(shortRest2.amount) <= 0) {
      throw new Error(`${policyPath}.short_rest must declare a positive fixed amount`);
    }
    if (longRest2.mode !== "full") {
      throw new Error(`${policyPath}.long_rest must declare full recovery`);
    }
    return [resource, {
      short_rest: { mode: "fixed", amount: Number(shortRest2.amount) },
      long_rest: { mode: "full" }
    }];
  }));
}
function numericResource(recordValue, resource, path) {
  const value = recordValue[resource];
  if (value === void 0) return void 0;
  if (!Number.isInteger(value) || Number(value) < 0) {
    throw new Error(`${path}.${resource} must be a non-negative integer`);
  }
  return Number(value);
}
function normalizeSpellcastingAccess(input) {
  if (input.value === void 0) return void 0;
  const path = `${input.actorPath}.spellcastingAccess`;
  const access = record8(input.value, path);
  if (!Array.isArray(access.grants)) throw new Error(`${path}.grants must be an array`);
  const rawPreparedSources = record8(access.preparedSources, `${path}.preparedSources`);
  const runtime = record8(input.runtime, `${input.actorPath}.runtime`);
  const resources = record8(runtime.resources, `${input.actorPath}.runtime.resources`);
  const maxResources = record8(runtime.maxResources, `${input.actorPath}.runtime.maxResources`);
  const ownedActionIds = new Set(input.actionIds);
  const grants = access.grants.map((rawGrant, index) => {
    const grantPath = `${path}.grants[${index}]`;
    const grant = record8(rawGrant, grantPath);
    const grantId = nonBlankString2(grant.grantId, `${grantPath}.grantId`);
    const actionId = nonBlankString2(grant.actionId, `${grantPath}.actionId`);
    const sourceId = nonBlankString2(grant.sourceId, `${grantPath}.sourceId`);
    if (!ownedActionIds.has(actionId)) {
      throw new Error(`${grantPath}.actionId is not owned by actor capabilities`);
    }
    if (typeof grant.access !== "string" || !SPELL_ACCESS_KINDS2.has(grant.access)) {
      throw new Error(`${grantPath}.access is invalid`);
    }
    const accessKind = grant.access;
    if (!Number.isInteger(grant.level) || Number(grant.level) < 0 || Number(grant.level) > 9) {
      throw new Error(`${grantPath}.level must be an integer from 0 to 9`);
    }
    const level = Number(grant.level);
    if (typeof grant.spellcastingAbility !== "string" || !ABILITIES3.has(grant.spellcastingAbility)) {
      throw new Error(`${grantPath}.spellcastingAbility is invalid`);
    }
    if (grant.ritual !== void 0 && typeof grant.ritual !== "boolean") {
      throw new Error(`${grantPath}.ritual must be boolean when present`);
    }
    const freeUseResource = optionalResource(
      grant.freeUseResource,
      `${grantPath}.freeUseResource`
    );
    const slotResource = optionalResource(grant.slotResource, `${grantPath}.slotResource`);
    if (accessKind === "cantrip" && level !== 0) {
      throw new Error(`${grantPath}: cantrip access requires spell level 0`);
    }
    if (accessKind !== "cantrip" && level === 0) {
      throw new Error(`${grantPath}: level 0 spells require cantrip access`);
    }
    if (level === 0 && (freeUseResource || slotResource)) {
      throw new Error(`${grantPath}: cantrips cannot declare payment resources`);
    }
    if (accessKind === "ritual_only" && grant.ritual !== true) {
      throw new Error(`${grantPath}: ritual_only access requires ritual=true`);
    }
    if (level > 0 && ["known", "spellbook", "always_prepared"].includes(accessKind) && !freeUseResource && !slotResource) {
      throw new Error(`${grantPath}: levelled normal casting requires a resource mapping`);
    }
    if (freeUseResource) {
      const current = numericResource(
        resources,
        freeUseResource,
        `${input.actorPath}.runtime.resources`
      );
      const maximum = numericResource(
        maxResources,
        freeUseResource,
        `${input.actorPath}.runtime.maxResources`
      );
      if (current === void 0 || maximum === void 0 || current > maximum) {
        throw new Error(`${grantPath}.freeUseResource must map to a valid actor resource`);
      }
    }
    if (slotResource) {
      const current = numericResource(resources, slotResource, `${input.actorPath}.runtime.resources`);
      const maximum = numericResource(
        maxResources,
        slotResource,
        `${input.actorPath}.runtime.maxResources`
      );
      if (current === void 0 !== (maximum === void 0) || current !== void 0 && maximum !== void 0 && current > maximum) {
        throw new Error(`${grantPath}.slotResource has an inconsistent actor resource mapping`);
      }
    }
    return {
      grantId,
      actionId,
      sourceId,
      access: accessKind,
      level,
      spellcastingAbility: grant.spellcastingAbility,
      ...grant.ritual === true ? { ritual: true } : {},
      ...freeUseResource ? { freeUseResource } : {},
      ...slotResource ? { slotResource } : {}
    };
  }).sort((left, right) => left.sourceId.localeCompare(right.sourceId) || left.actionId.localeCompare(right.actionId) || left.grantId.localeCompare(right.grantId));
  const grantIds = grants.map((grant) => grant.grantId);
  if (new Set(grantIds).size !== grantIds.length) {
    throw new Error(`${path}.grants must have unique grantId values`);
  }
  const provenanceKeys = grants.map((grant) => `${grant.sourceId}\0${grant.actionId}`);
  if (new Set(provenanceKeys).size !== provenanceKeys.length) {
    throw new Error(`${path}.grants must have unique sourceId/actionId provenance`);
  }
  const preparedSources = {};
  for (const [sourceId, rawSource] of Object.entries(rawPreparedSources).sort(([left], [right]) => left.localeCompare(right))) {
    if (rawSource === void 0) continue;
    nonBlankString2(sourceId, `${path}.preparedSources key`);
    const sourcePath = `${path}.preparedSources.${sourceId}`;
    const source2 = record8(rawSource, sourcePath);
    if (source2.sourceId !== sourceId) throw new Error(`${sourcePath}.sourceId must match its key`);
    if (!Number.isInteger(source2.capacity) || Number(source2.capacity) < 0) {
      throw new Error(`${sourcePath}.capacity must be a non-negative integer`);
    }
    const capacity = Number(source2.capacity);
    const availableActionIds = uniqueStringArray(
      source2.availableActionIds,
      `${sourcePath}.availableActionIds`
    );
    const preparedActionIds = uniqueStringArray(
      source2.preparedActionIds,
      `${sourcePath}.preparedActionIds`
    );
    if (capacity > availableActionIds.length) {
      throw new Error(`${sourcePath}.capacity exceeds available spells`);
    }
    if (preparedActionIds.length !== capacity) {
      throw new Error(`${sourcePath}.preparedActionIds must exactly fill capacity`);
    }
    if (preparedActionIds.some((actionId) => !availableActionIds.includes(actionId))) {
      throw new Error(`${sourcePath} prepares a spell outside its available collection`);
    }
    const spellbookActionIds = grants.filter((grant) => grant.sourceId === sourceId && grant.access === "spellbook").map((grant) => grant.actionId).sort((left, right) => left.localeCompare(right));
    if (!spellbookActionIds.length) {
      throw new Error(`${sourcePath} has no spellbook grants`);
    }
    if (JSON.stringify(availableActionIds) !== JSON.stringify(spellbookActionIds)) {
      throw new Error(`${sourcePath}.availableActionIds must equal its spellbook grants`);
    }
    preparedSources[sourceId] = {
      sourceId,
      capacity,
      availableActionIds,
      preparedActionIds
    };
  }
  for (const grant of grants) {
    if (grant.access === "spellbook" && !preparedSources[grant.sourceId]) {
      throw new Error(`${path}: spellbook grant ${grant.grantId} has no prepared source`);
    }
  }
  return { grants, preparedSources };
}
function normalizeActorTraits(value, actorPath) {
  if (value === void 0) return void 0;
  const path = `${actorPath}.traits`;
  const traits = record8(value, path);
  let conditionImmunities;
  if (traits.conditionImmunities !== void 0) {
    if (!Array.isArray(traits.conditionImmunities)) {
      throw new Error(`${path}.conditionImmunities must be an array`);
    }
    conditionImmunities = traits.conditionImmunities.map((rawImmunity, index) => {
      const immunityPath = `${path}.conditionImmunities[${index}]`;
      const immunity = record8(rawImmunity, immunityPath);
      const sourceEntityIds = uniqueStringArray(
        immunity.sourceEntityIds,
        `${immunityPath}.sourceEntityIds`
      );
      if (!sourceEntityIds.length) throw new Error(`${immunityPath}.sourceEntityIds cannot be empty`);
      const requiredCauseTags = immunity.requiredCauseTags === void 0 ? void 0 : uniqueStringArray(immunity.requiredCauseTags, `${immunityPath}.requiredCauseTags`);
      return {
        condition: nonBlankString2(immunity.condition, `${immunityPath}.condition`),
        ...requiredCauseTags ? { requiredCauseTags } : {},
        sourceEntityIds
      };
    }).sort((left, right) => left.condition.localeCompare(right.condition) || (left.requiredCauseTags ?? []).join("\0").localeCompare((right.requiredCauseTags ?? []).join("\0")));
    const keys = conditionImmunities.map((immunity) => `${immunity.condition}\0${(immunity.requiredCauseTags ?? []).join("\0")}`);
    if (new Set(keys).size !== keys.length) {
      throw new Error(`${path}.conditionImmunities contains duplicate rules`);
    }
  }
  let restProfile;
  if (traits.restProfile !== void 0) {
    const profilePath = `${path}.restProfile`;
    const profile = record8(traits.restProfile, profilePath);
    if (!Number.isFinite(profile.longRestHours) || Number(profile.longRestHours) <= 0 || Number(profile.longRestHours) > 24) {
      throw new Error(`${profilePath}.longRestHours must be greater than 0 and at most 24`);
    }
    if (typeof profile.sleepRequired !== "boolean") {
      throw new Error(`${profilePath}.sleepRequired must be boolean`);
    }
    const sourceEntityIds = uniqueStringArray(
      profile.sourceEntityIds,
      `${profilePath}.sourceEntityIds`
    );
    if (!sourceEntityIds.length) throw new Error(`${profilePath}.sourceEntityIds cannot be empty`);
    restProfile = {
      longRestHours: Number(profile.longRestHours),
      sleepRequired: profile.sleepRequired,
      sourceEntityIds
    };
  }
  return {
    ...conditionImmunities ? { conditionImmunities } : {},
    ...restProfile ? { restProfile } : {}
  };
}
function nonNegativeInteger4(value, path) {
  if (!Number.isInteger(value) || Number(value) < 0) {
    throw new Error(`${path} must be a non-negative integer`);
  }
  return Number(value);
}
function nonNegativeFinite(value, path) {
  if (typeof value !== "number" || !Number.isFinite(value) || value < 0) {
    throw new Error(`${path} must be a non-negative finite number`);
  }
  return value;
}
function booleanValue(value, path) {
  if (typeof value !== "boolean") throw new Error(`${path} must be boolean`);
  return value;
}
function requirePactSource(input) {
  const sources = input.featureSources[input.capabilityId];
  if (!sources?.includes(input.sourceEntityId)) {
    throw new Error(`${input.path}.sourceEntityId is not owned by ${input.capabilityId}`);
  }
}
function normalizePactBladeBond(input) {
  const bond = record8(input.value, input.path);
  if (bond.sourceEntityId !== input.sourceEntityId) {
    throw new Error(`${input.path}.sourceEntityId must match its invocation`);
  }
  if (bond.warlockActorId !== input.actorId) {
    throw new Error(`${input.path}.warlockActorId must match its owner`);
  }
  const weaponObjectId = nonBlankString2(bond.weaponObjectId, `${input.path}.weaponObjectId`);
  const weapon = input.objects[weaponObjectId];
  if (!weapon) throw new Error(`${input.path}.weaponObjectId must exist in world.objects`);
  const weaponCardId = nonBlankString2(bond.weaponCardId, `${input.path}.weaponCardId`);
  if (weapon.kind !== "item" || weapon.itemCardId !== weaponCardId) {
    throw new Error(`${input.path} requires an exact immutable Card-to-item bridge`);
  }
  if (weapon.attunedToActorId !== void 0 && weapon.attunedToActorId !== input.actorId) {
    throw new Error(`${input.path}.weaponObjectId is attuned to another actor`);
  }
  const conjured = booleanValue(bond.conjured, `${input.path}.conjured`);
  if (conjured) {
    if (weapon.ownerActorId !== input.actorId || weapon.sourceActorId !== input.actorId || weapon.sourceActionId !== input.sourceEntityId || !weapon.tags?.includes("pact_weapon")) {
      throw new Error(`${input.path}.weaponObjectId is not the source-owned conjured pact weapon`);
    }
  }
  const continuousSeparationSeconds = nonNegativeFinite(
    bond.continuousSeparationSeconds,
    `${input.path}.continuousSeparationSeconds`
  );
  const lastDistanceBoardRevision = bond.lastDistanceBoardRevision === null ? null : nonNegativeInteger4(
    bond.lastDistanceBoardRevision,
    `${input.path}.lastDistanceBoardRevision`
  );
  return {
    sourceEntityId: input.sourceEntityId,
    warlockActorId: input.actorId,
    weaponObjectId,
    weaponCardId,
    weaponType: nonBlankString2(bond.weaponType, `${input.path}.weaponType`),
    normalDamageType: nonBlankString2(bond.normalDamageType, `${input.path}.normalDamageType`),
    conjured,
    bondedAtRevision: nonNegativeInteger4(bond.bondedAtRevision, `${input.path}.bondedAtRevision`),
    continuousSeparationSeconds,
    lastDistanceBoardRevision
  };
}
function normalizePactChainFamiliar(input) {
  const familiar = record8(input.value, input.path);
  if (familiar.ownerActorId !== input.actorId) {
    throw new Error(`${input.path}.ownerActorId must match its owner`);
  }
  if (familiar.sourceEntityId !== input.sourceEntityId) {
    throw new Error(`${input.path}.sourceEntityId must match its invocation`);
  }
  const actorId = nonBlankString2(familiar.actorId, `${input.path}.actorId`);
  if (actorId === input.actorId) throw new Error(`${input.path}.actorId cannot be its owner`);
  return {
    actorId,
    ownerActorId: input.actorId,
    formId: nonBlankString2(familiar.formId, `${input.path}.formId`),
    sourceEntityId: input.sourceEntityId,
    reactionAvailable: booleanValue(
      familiar.reactionAvailable,
      `${input.path}.reactionAvailable`
    )
  };
}
function normalizeWarlockPacts(input) {
  if (input.value === void 0) return void 0;
  const path = `${input.actorPath}.warlockPacts`;
  const rawStates = record8(input.value, path);
  const unknownKey = Object.keys(rawStates).find((key) => !["blade", "chain", "tome"].includes(key));
  if (unknownKey) throw new Error(`${path}.${unknownKey} is not a supported Pact state`);
  const actionIds = new Set(input.actionIds);
  const states = {};
  if (rawStates.blade !== void 0) {
    const bladePath = `${path}.blade`;
    const blade = record8(rawStates.blade, bladePath);
    if (blade.kind !== "blade") throw new Error(`${bladePath}.kind must be blade`);
    const sourceEntityId = nonBlankString2(blade.sourceEntityId, `${bladePath}.sourceEntityId`);
    if (blade.ownerActorId !== input.actorId) {
      throw new Error(`${bladePath}.ownerActorId must match its actor`);
    }
    requirePactSource({
      featureSources: input.featureSources,
      capabilityId: PACT_BLADE_STATE_CAPABILITY,
      sourceEntityId,
      path: bladePath
    });
    const bondActionId = nonBlankString2(blade.bondActionId, `${bladePath}.bondActionId`);
    if (!actionIds.has(bondActionId)) {
      throw new Error(`${bladePath}.bondActionId is not owned by actor capabilities`);
    }
    const lifecyclePolicy = record8(blade.lifecyclePolicy, `${bladePath}.lifecyclePolicy`);
    const lifecycleIssue = pactBladeLifecyclePolicyIssue(lifecyclePolicy);
    if (lifecycleIssue) throw new Error(`${bladePath}.lifecyclePolicy: ${lifecycleIssue}`);
    states.blade = {
      kind: "blade",
      sourceEntityId,
      ownerActorId: input.actorId,
      bondActionId,
      lifecyclePolicy: JSON.parse(JSON.stringify(lifecyclePolicy)),
      activeBond: blade.activeBond === null ? null : normalizePactBladeBond({
        value: blade.activeBond,
        path: `${bladePath}.activeBond`,
        actorId: input.actorId,
        sourceEntityId,
        objects: input.objects
      })
    };
  }
  if (rawStates.chain !== void 0) {
    const chainPath = `${path}.chain`;
    const chain2 = record8(rawStates.chain, chainPath);
    if (chain2.kind !== "chain") throw new Error(`${chainPath}.kind must be chain`);
    const sourceEntityId = nonBlankString2(chain2.sourceEntityId, `${chainPath}.sourceEntityId`);
    if (chain2.ownerActorId !== input.actorId) {
      throw new Error(`${chainPath}.ownerActorId must match its actor`);
    }
    requirePactSource({
      featureSources: input.featureSources,
      capabilityId: PACT_CHAIN_STATE_CAPABILITY,
      sourceEntityId,
      path: chainPath
    });
    const templatePath = `${chainPath}.template`;
    const template = record8(chain2.template, templatePath);
    const findFamiliarActionId = nonBlankString2(
      template.findFamiliarActionId,
      `${templatePath}.findFamiliarActionId`
    );
    if (!actionIds.has(findFamiliarActionId)) {
      throw new Error(`${templatePath}.findFamiliarActionId is not owned by actor capabilities`);
    }
    if (template.normalFormSource !== "find_familiar_spell") {
      throw new Error(`${templatePath}.normalFormSource must be find_familiar_spell`);
    }
    const specialFormIds = uniqueStringArray(template.specialFormIds, `${templatePath}.specialFormIds`);
    if (JSON.stringify(specialFormIds) !== JSON.stringify([...PACT_CHAIN_SPECIAL_FORMS])) {
      throw new Error(`${templatePath}.specialFormIds must equal the PHB 2024 Pact Chain forms`);
    }
    states.chain = {
      kind: "chain",
      sourceEntityId,
      ownerActorId: input.actorId,
      template: { findFamiliarActionId, normalFormSource: "find_familiar_spell", specialFormIds },
      activeFamiliar: chain2.activeFamiliar === null ? null : normalizePactChainFamiliar({
        value: chain2.activeFamiliar,
        path: `${chainPath}.activeFamiliar`,
        actorId: input.actorId,
        sourceEntityId
      })
    };
  }
  if (rawStates.tome !== void 0) {
    const tomeStatePath = `${path}.tome`;
    const tomeState = record8(rawStates.tome, tomeStatePath);
    if (tomeState.kind !== "tome") throw new Error(`${tomeStatePath}.kind must be tome`);
    const sourceEntityId = nonBlankString2(
      tomeState.sourceEntityId,
      `${tomeStatePath}.sourceEntityId`
    );
    if (tomeState.ownerActorId !== input.actorId) {
      throw new Error(`${tomeStatePath}.ownerActorId must match its actor`);
    }
    requirePactSource({
      featureSources: input.featureSources,
      capabilityId: PACT_TOME_STATE_CAPABILITY,
      sourceEntityId,
      path: tomeStatePath
    });
    const tomePath = `${tomeStatePath}.tome`;
    const tome = record8(tomeState.tome, tomePath);
    if (tome.sourceEntityId !== sourceEntityId || tome.ownerActorId !== input.actorId) {
      throw new Error(`${tomePath} must match its invocation source and owner`);
    }
    const bookObjectId = nonBlankString2(tome.bookObjectId, `${tomePath}.bookObjectId`);
    const bookObject = input.objects[bookObjectId];
    if (!bookObject || bookObject.kind !== "item" || bookObject.ownerActorId !== input.actorId || bookObject.carriedByActorId !== input.actorId || bookObject.sourceActorId !== input.actorId || bookObject.sourceActionId !== sourceEntityId || !bookObject.tags?.includes("book_of_shadows") || !bookObject.tags.includes("spellcasting_focus")) {
      throw new Error(`${tomePath}.bookObjectId is not a carried source-owned Book of Shadows focus`);
    }
    const cantripActionIds = uniqueStringArray(
      tome.cantripActionIds,
      `${tomePath}.cantripActionIds`
    );
    const ritualActionIds = uniqueStringArray(
      tome.ritualActionIds,
      `${tomePath}.ritualActionIds`
    );
    const spellGrantIds = uniqueStringArray(tome.spellGrantIds, `${tomePath}.spellGrantIds`);
    if (cantripActionIds.length !== 3 || ritualActionIds.length !== 2 || spellGrantIds.length !== 5) {
      throw new Error(`${tomePath} requires exactly three cantrips, two rituals, and five grants`);
    }
    if ([...cantripActionIds, ...ritualActionIds].some((actionId) => !actionIds.has(actionId))) {
      throw new Error(`${tomePath} contains a spell action not owned by actor capabilities`);
    }
    if ((/* @__PURE__ */ new Set([...cantripActionIds, ...ritualActionIds])).size !== 5) {
      throw new Error(`${tomePath} spell selections must be distinct`);
    }
    if (tome.createdAfterRest !== "short" && tome.createdAfterRest !== "long") {
      throw new Error(`${tomePath}.createdAfterRest must be short or long`);
    }
    if (!input.spellcastingAccess) {
      throw new Error(`${tomePath} requires actor spellcastingAccess`);
    }
    const grants = input.spellcastingAccess.grants.filter((grant) => spellGrantIds.includes(grant.grantId));
    const grantsFromBook = input.spellcastingAccess.grants.filter((grant) => grant.sourceId === bookObjectId);
    if (grants.length !== 5 || grantsFromBook.length !== 5 || grants.some((grant) => grant.sourceId !== bookObjectId) || grantsFromBook.some((grant) => !spellGrantIds.includes(grant.grantId))) {
      throw new Error(`${tomePath}.spellGrantIds must exactly own the Book of Shadows grants`);
    }
    for (const grant of grants) {
      if (cantripActionIds.includes(grant.actionId)) {
        if (grant.access !== "cantrip" || grant.level !== 0 || grant.spellcastingAbility !== "cha") {
          throw new Error(`${tomePath} has an invalid Book of Shadows cantrip grant`);
        }
      } else if (ritualActionIds.includes(grant.actionId)) {
        if (grant.access !== "always_prepared" || grant.level !== 1 || grant.ritual !== true || grant.spellcastingAbility !== "cha" || !grant.slotResource) {
          throw new Error(`${tomePath} has an invalid prepared Warlock ritual grant`);
        }
      } else {
        throw new Error(`${tomePath} grant points outside its selected spells`);
      }
    }
    states.tome = {
      kind: "tome",
      sourceEntityId,
      ownerActorId: input.actorId,
      tome: {
        sourceEntityId,
        ownerActorId: input.actorId,
        bookObjectId,
        cantripActionIds,
        ritualActionIds,
        spellGrantIds,
        createdAfterRest: tome.createdAfterRest
      }
    };
  }
  if (!Object.keys(states).length) throw new Error(`${path} must contain at least one Pact state`);
  return states;
}
function normalizeWorldObjects(value, required) {
  if (value === void 0) {
    if (required) throw new Error("world.objects is required in world schema 5");
    return {};
  }
  const rawObjects = record8(value, "world.objects");
  return Object.fromEntries(Object.entries(rawObjects).map(([objectId, rawObject]) => {
    const object5 = record8(rawObject, `world.objects.${objectId}`);
    if (typeof object5.id !== "string" || !object5.id || object5.id !== objectId) {
      throw new Error(`world.objects.${objectId}.id must match its key`);
    }
    if (typeof object5.name !== "string" || !object5.name) {
      throw new Error(`world.objects.${objectId}.name is required`);
    }
    if (typeof object5.kind !== "string" || !WORLD_OBJECT_KINDS.has(object5.kind)) {
      throw new Error(`world.objects.${objectId}.kind is invalid`);
    }
    if (typeof object5.size !== "string" || !WORLD_OBJECT_SIZES.has(object5.size)) {
      throw new Error(`world.objects.${objectId}.size is invalid`);
    }
    if (object5.itemCardId !== void 0) {
      nonBlankString2(object5.itemCardId, `world.objects.${objectId}.itemCardId`);
      if (object5.kind !== "item") {
        throw new Error(`world.objects.${objectId}.itemCardId requires an item object`);
      }
    }
    if (object5.attunedToActorId !== void 0) {
      nonBlankString2(object5.attunedToActorId, `world.objects.${objectId}.attunedToActorId`);
      if (object5.kind !== "item") {
        throw new Error(`world.objects.${objectId}.attunedToActorId requires an item object`);
      }
    }
    if (object5.heldByActorId === void 0 !== (object5.heldInHand === void 0)) {
      throw new Error(`world.objects.${objectId} must persist holder and hand together`);
    }
    if (object5.heldByActorId !== void 0) {
      nonBlankString2(object5.heldByActorId, `world.objects.${objectId}.heldByActorId`);
      if (object5.heldInHand !== "main_hand" && object5.heldInHand !== "off_hand") {
        throw new Error(`world.objects.${objectId}.heldInHand is invalid`);
      }
      if (object5.kind !== "item" || object5.carriedByActorId !== object5.heldByActorId) {
        throw new Error(`world.objects.${objectId} held identity must match its item carrier`);
      }
    }
    for (const sourceKey2 of ["sourceActorId", "sourceActionId"]) {
      if (object5[sourceKey2] !== void 0) {
        nonBlankString2(object5[sourceKey2], `world.objects.${objectId}.${sourceKey2}`);
      }
    }
    if (object5.roundsLeft !== void 0) {
      positiveInteger3(object5.roundsLeft, `world.objects.${objectId}.roundsLeft`);
    }
    if (object5.sourceTurnEndingsLeft !== void 0) {
      positiveInteger3(
        object5.sourceTurnEndingsLeft,
        `world.objects.${objectId}.sourceTurnEndingsLeft`
      );
      if (object5.sourceActorId === void 0 || object5.sourceActionId === void 0) {
        throw new Error(
          `world.objects.${objectId} source-turn duration requires source actor and action IDs`
        );
      }
    }
    if (object5.dancingLight !== void 0) {
      const path = `world.objects.${objectId}.dancingLight`;
      const dancingLight = record8(object5.dancingLight, path);
      nonBlankString2(dancingLight.groupId, `${path}.groupId`);
      if (dancingLight.form !== "individual" && dancingLight.form !== "medium_humanoid") {
        throw new Error(`${path}.form is invalid`);
      }
      positiveFinite(dancingLight.dimRadiusFt, `${path}.dimRadiusFt`);
      if (object5.kind !== "spell_effect" || object5.sourceActorId === void 0 || object5.sourceActionId === void 0) {
        throw new Error(`${path} requires a source-owned spell-effect object`);
      }
      positiveInteger3(object5.roundsLeft, `world.objects.${objectId}.roundsLeft`);
      if (!Number.isFinite(object5.distanceFromSourceFt) || Number(object5.distanceFromSourceFt) < 0) {
        throw new Error(`${path} requires a non-negative source distance`);
      }
    }
    if (object5.prestidigitation !== void 0) {
      const path = `world.objects.${objectId}.prestidigitation`;
      if (!Array.isArray(object5.prestidigitation) || !object5.prestidigitation.length) {
        throw new Error(`${path} must be a non-empty array`);
      }
      const ids = /* @__PURE__ */ new Set();
      object5.prestidigitation.forEach((rawAttachment, index) => {
        const attachment = record8(rawAttachment, `${path}[${index}]`);
        const id = nonBlankString2(attachment.id, `${path}[${index}].id`);
        if (ids.has(id)) throw new Error(`${path} contains duplicate effect ${id}`);
        ids.add(id);
        nonBlankString2(attachment.sourceActorId, `${path}[${index}].sourceActorId`);
        nonBlankString2(attachment.sourceActionId, `${path}[${index}].sourceActionId`);
        if (attachment.kind !== "minor_sensation" && attachment.kind !== "magic_mark") {
          throw new Error(`${path}[${index}].kind is invalid`);
        }
        nonBlankString2(attachment.description, `${path}[${index}].description`);
        positiveInteger3(attachment.roundsLeft, `${path}[${index}].roundsLeft`);
      });
    }
    return [objectId, JSON.parse(JSON.stringify(object5))];
  }));
}
function normalizeAttackActions(input) {
  if (input.value === void 0) {
    if (!input.legacy) throw new Error("world.attackActions is required in world schema 4");
    return {};
  }
  const rawActions = record8(input.value, "world.attackActions");
  const openByActor = /* @__PURE__ */ new Set();
  return Object.fromEntries(Object.entries(rawActions).map(([attackActionId, rawAction]) => {
    const path = `world.attackActions.${attackActionId}`;
    const action = record8(rawAction, path);
    if (nonBlankString2(action.id, `${path}.id`) !== attackActionId) {
      throw new Error(`${path}.id must match its key`);
    }
    const actorId = nonBlankString2(action.actorId, `${path}.actorId`);
    const actor2 = input.actors[actorId];
    if (!actor2) throw new Error(`${path}.actorId must reference a world actor`);
    if (typeof action.status !== "string" || !ATTACK_ACTION_STATUSES.has(action.status)) {
      throw new Error(`${path}.status is invalid`);
    }
    const sequence = record8(action.sequence, `${path}.sequence`);
    if (!attackSequenceInvariantHolds(sequence) || sequence.id !== attackActionId || sequence.actorId !== actorId || sequence.totalAttacks !== actor2.attackProfile.attacksPerAction) {
      throw new Error(`${path}.sequence is not a canonical actor Attack budget`);
    }
    if (action.status === "open") {
      if (openByActor.has(actorId)) throw new Error(`${path}: actor has multiple open Attack actions`);
      openByActor.add(actorId);
    }
    if (action.status === "completed" && sequence.attacksRemaining !== 0) {
      throw new Error(`${path}: completed Attack action has attacks remaining`);
    }
    const blockedByResolutionId = action.blockedByResolutionId === void 0 ? void 0 : nonBlankString2(action.blockedByResolutionId, `${path}.blockedByResolutionId`);
    if (blockedByResolutionId && action.status !== "open") {
      throw new Error(`${path}: only an open Attack action can be blocked`);
    }
    const declaredActionId = action.declaredActionId === void 0 ? void 0 : nonBlankString2(action.declaredActionId, `${path}.declaredActionId`);
    let declaredActionSourceEntityIds;
    if (action.declaredActionSourceEntityIds !== void 0) {
      if (!Array.isArray(action.declaredActionSourceEntityIds) || !action.declaredActionSourceEntityIds.length) {
        throw new Error(`${path}.declaredActionSourceEntityIds must be non-empty`);
      }
      const ids = action.declaredActionSourceEntityIds.map((id, index) => nonBlankString2(id, `${path}.declaredActionSourceEntityIds[${index}]`));
      if (new Set(ids).size !== ids.length) {
        throw new Error(`${path}.declaredActionSourceEntityIds must be unique`);
      }
      declaredActionSourceEntityIds = ids;
    }
    if (declaredActionId === void 0 !== (declaredActionSourceEntityIds === void 0)) {
      throw new Error(`${path} must declare action identity and provenance together`);
    }
    const normalized3 = {
      id: attackActionId,
      actorId,
      startedAtRevision: nonNegativeInteger4(action.startedAtRevision, `${path}.startedAtRevision`),
      turnKey: nonBlankString2(action.turnKey, `${path}.turnKey`),
      status: action.status,
      sequence: JSON.parse(JSON.stringify(sequence)),
      ...declaredActionId && declaredActionSourceEntityIds ? { declaredActionId, declaredActionSourceEntityIds } : {},
      ...blockedByResolutionId ? { blockedByResolutionId } : {}
    };
    return [attackActionId, normalized3];
  }));
}
function normalizeGrapples(input) {
  if (input.value === void 0) {
    if (!input.legacy) throw new Error("world.grapples is required in world schema 4");
    return {};
  }
  const rawGrapples = record8(input.value, "world.grapples");
  const occupiedParts = /* @__PURE__ */ new Set();
  return Object.fromEntries(Object.entries(rawGrapples).map(([grappleId, rawGrapple]) => {
    const path = `world.grapples.${grappleId}`;
    const grapple = record8(rawGrapple, path);
    if (nonBlankString2(grapple.id, `${path}.id`) !== grappleId) {
      throw new Error(`${path}.id must match its key`);
    }
    const grapplerActorId = nonBlankString2(grapple.grapplerActorId, `${path}.grapplerActorId`);
    const targetActorId = nonBlankString2(grapple.targetActorId, `${path}.targetActorId`);
    const grappler = input.actors[grapplerActorId];
    const target2 = input.actors[targetActorId];
    if (!grappler || !target2 || grapplerActorId === targetActorId) {
      throw new Error(`${path} must connect two different world actors`);
    }
    const sourcePart = nonBlankString2(grapple.sourcePart, `${path}.sourcePart`);
    if (!grappler.attackProfile.graspingParts.includes(sourcePart)) {
      throw new Error(`${path}.sourcePart is not owned by the grappler profile`);
    }
    const occupiedKey = `${grapplerActorId}\0${sourcePart}`;
    if (occupiedParts.has(occupiedKey)) throw new Error(`${path}.sourcePart already maintains a grapple`);
    occupiedParts.add(occupiedKey);
    const sourceEntityIds = uniqueStringArray(grapple.sourceEntityIds, `${path}.sourceEntityIds`);
    if (!sourceEntityIds.length) throw new Error(`${path}.sourceEntityIds cannot be empty`);
    const grappleSource = getSystemActionDefinition(SYSTEM_ACTION_IDS.unarmedGrapple);
    if (grappleSource.sourceEntityIds.some((sourceId) => !sourceEntityIds.includes(sourceId))) {
      throw new Error(`${path}.sourceEntityIds must retain ruleset Grapple provenance`);
    }
    if (!target2.runtime.activeEffects.some((effect) => effect.id === `grapple:${grappleId}` && effect.ownerId === targetActorId && effect.sourceId === grapplerActorId && effect.mechanics.kind === "condition" && effect.mechanics.value === "grappled" && effect.mechanics.grappleId === grappleId)) {
      throw new Error(`${path} is missing its exact target grapple projection`);
    }
    const normalized3 = {
      id: grappleId,
      grapplerActorId,
      targetActorId,
      sourcePart,
      escapeDc: positiveInteger3(grapple.escapeDc, `${path}.escapeDc`),
      reachFt: positiveFinite(grapple.reachFt, `${path}.reachFt`),
      sourceEntityIds,
      startedAtRevision: nonNegativeInteger4(grapple.startedAtRevision, `${path}.startedAtRevision`)
    };
    return [grappleId, normalized3];
  }));
}
function normalizeActorLifecycle(input) {
  if (input.value === void 0) {
    if (input.schemaVersion >= 5) {
      throw new Error(`${input.path} is required in world schema 5`);
    }
    return { status: "alive" };
  }
  const lifecycle = record8(input.value, input.path);
  if (lifecycle.status === "alive") {
    if (lifecycle.adjudication !== void 0) {
      throw new Error(`${input.path}.adjudication is invalid for a living actor`);
    }
    return { status: "alive" };
  }
  if (lifecycle.status !== "dead") throw new Error(`${input.path}.status is invalid`);
  const factPath = `${input.path}.adjudication`;
  const fact = record8(lifecycle.adjudication, factPath);
  if (fact.type !== "ActorDeathAdjudicated" || fact.provenance !== "canonical_actor_lifecycle") {
    throw new Error(`${factPath} has non-authoritative lifecycle provenance`);
  }
  const factId = nonBlankString2(fact.factId, `${factPath}.factId`);
  const adjudicatedBy = nonBlankString2(fact.adjudicatedBy, `${factPath}.adjudicatedBy`);
  if (fact.actorId !== input.actorId) {
    throw new Error(`${factPath}.actorId must match its actor`);
  }
  const observedAtWorldRevision = nonNegativeInteger4(
    fact.observedAtWorldRevision,
    `${factPath}.observedAtWorldRevision`
  );
  if (observedAtWorldRevision >= input.worldRevision) {
    throw new Error(`${factPath} must precede the persisted committed world revision`);
  }
  if (fact.rulesetContentHash !== input.rulesetContentHash) {
    throw new Error(`${factPath}.rulesetContentHash must match the world ruleset`);
  }
  return {
    status: "dead",
    adjudication: {
      type: "ActorDeathAdjudicated",
      provenance: "canonical_actor_lifecycle",
      factId,
      actorId: input.actorId,
      adjudicatedBy,
      observedAtWorldRevision,
      rulesetContentHash: input.rulesetContentHash
    }
  };
}
function pactBladePendingProjectionIssue(input) {
  if (input.pending.pactBladeProjection === void 0) return null;
  const projection = record8(
    input.pending.pactBladeProjection,
    "world.pendingResolution.pactBladeProjection"
  );
  if (input.pending.actionId !== SYSTEM_ACTION_IDS.weaponAttack || input.pending.attackContinuationKind !== void 0 && input.pending.attackContinuationKind !== "weapon_melee" && input.pending.attackContinuationKind !== "weapon_ranged" || projection.weaponHand !== "main" && projection.weaponHand !== "off" || !["str", "dex", "cha"].includes(projection.abilityChoice) || !["str", "dex", "cha"].includes(projection.attackAbility) || !["str", "dex", "cha"].includes(projection.damageAbility) || !["normal", "necrotic", "psychic", "radiant"].includes(projection.damageChoice) || typeof projection.weaponObjectId !== "string" || !projection.weaponObjectId.trim() || typeof projection.weaponCardId !== "string" || !projection.weaponCardId.trim() || typeof projection.resolvedDamageType !== "string" || !projection.resolvedDamageType.trim()) {
    return "has a malformed Pact Blade attack projection";
  }
  if (input.pending.weaponHand !== projection.weaponHand || input.pending.weaponCardId !== projection.weaponCardId) {
    return "Pact Blade projection diverges from its weapon continuation identity";
  }
  const sourceActorId = input.pending.sourceActorId;
  const source2 = typeof sourceActorId === "string" ? input.actors[sourceActorId] : void 0;
  const bond = source2?.warlockPacts?.blade?.activeBond;
  const object5 = input.objects[projection.weaponObjectId];
  const expectedAbility = projection.abilityChoice;
  const expectedDamage = projection.damageChoice === "normal" ? bond?.normalDamageType : projection.damageChoice;
  if (!source2 || source2.lifecycle?.status !== "alive" || !bond || bond.weaponObjectId !== projection.weaponObjectId || bond.weaponCardId !== projection.weaponCardId || object5?.itemCardId !== projection.weaponCardId || object5.heldByActorId !== source2.id || object5.heldInHand !== (projection.weaponHand === "main" ? "main_hand" : "off_hand") || projection.attackAbility !== expectedAbility || projection.damageAbility !== expectedAbility || projection.resolvedDamageType !== expectedDamage) {
    return "Pact Blade projection diverges from its active held Card/Object bond";
  }
  return null;
}
function migrateWorldState(value) {
  const world = record8(value, "world");
  const schemaVersion = Number(world.schemaVersion);
  if (schemaVersion !== 1 && schemaVersion !== 2 && schemaVersion !== 3 && schemaVersion !== 4 && schemaVersion !== 5) {
    throw new Error(`Unsupported world schema ${String(world.schemaVersion)}`);
  }
  const legacy = schemaVersion < 4;
  if (typeof world.id !== "string" || !world.id) throw new Error("world.id is required");
  const actorsRecord = record8(world.actors, "world.actors");
  for (const [actorId, rawActor] of Object.entries(actorsRecord)) {
    record8(rawActor, `world.actors.${actorId}`);
  }
  const worldRevision = nonNegativeInteger4(world.revision, "world.revision");
  const ruleset = record8(world.ruleset, "world.ruleset");
  const rulesetContentHash = nonBlankString2(ruleset.contentHash, "world.ruleset.contentHash");
  const objects = normalizeWorldObjects(world.objects, schemaVersion >= 5);
  if (schemaVersion >= 5) record8(world.concentrations, "world.concentrations");
  const actors = Object.fromEntries(Object.entries(actorsRecord).map(([actorId, rawActor]) => {
    const actor2 = record8(rawActor, `world.actors.${actorId}`);
    if (actor2.id !== actorId) throw new Error(`world.actors.${actorId}.id must match its key`);
    const capabilities = actor2.capabilities && typeof actor2.capabilities === "object" ? actor2.capabilities : {};
    const actionIds = Array.isArray(capabilities.actionIds) ? capabilities.actionIds.filter((id) => typeof id === "string") : [];
    const rawFeatureSources = capabilities.featureSources && typeof capabilities.featureSources === "object" && !Array.isArray(capabilities.featureSources) ? capabilities.featureSources : {};
    const featureSources = Object.fromEntries(Object.entries(rawFeatureSources).flatMap(([id, sources]) => {
      if (!Array.isArray(sources)) return [];
      const normalized3 = [...new Set(sources.filter((source2) => typeof source2 === "string" && source2.length > 0))];
      return normalized3.length ? [[id, normalized3]] : [];
    }));
    const normalizedSpellcastingAccess = normalizeSpellcastingAccess({
      value: actor2.spellcastingAccess,
      actorPath: `world.actors.${actorId}`,
      actionIds,
      runtime: actor2.runtime
    });
    const normalizedTraits = normalizeActorTraits(actor2.traits, `world.actors.${actorId}`);
    const normalizedWarlockPacts = normalizeWarlockPacts({
      value: actor2.warlockPacts,
      actorId,
      actorPath: `world.actors.${actorId}`,
      actionIds,
      featureSources,
      spellcastingAccess: normalizedSpellcastingAccess,
      objects
    });
    const runtime = record8(actor2.runtime, `world.actors.${actorId}.runtime`);
    const character = record8(actor2.character, `world.actors.${actorId}.character`);
    const normalizedResourceRecovery = normalizeResourceRecovery({
      value: character.resourceRecovery,
      actorPath: `world.actors.${actorId}`,
      runtime
    });
    if (!Array.isArray(runtime.activeEffects)) {
      throw new Error(`world.actors.${actorId}.runtime.activeEffects must be an array`);
    }
    runtime.activeEffects.forEach((effect, index) => {
      const issue = runtimeSenseEffectIssue(effect, actorId);
      if (issue) throw new Error(`world.actors.${actorId}.runtime.activeEffects[${index}]: ${issue}`);
      const armorIssue = armorOfAgathysEffectIssue(effect, actorId);
      if (armorIssue) {
        throw new Error(`world.actors.${actorId}.runtime.activeEffects[${index}]: ${armorIssue}`);
      }
      const mechanics = effect && typeof effect === "object" ? effect.mechanics : void 0;
      if (mechanics?.kind === "temporary_hp_melee_retaliation") {
        const actionId = nonBlankString2(
          mechanics.actionId,
          `world.actors.${actorId}.runtime.activeEffects[${index}].mechanics.actionId`
        );
        if (!actionIds.includes(actionId)) {
          throw new Error(
            `world.actors.${actorId}.runtime.activeEffects[${index}] source action is not actor-owned`
          );
        }
        const hp = record8(runtime.hp, `world.actors.${actorId}.runtime.hp`);
        if (!Number.isInteger(hp.temp) || Number(hp.temp) < 1) {
          throw new Error(
            `world.actors.${actorId}.runtime.activeEffects[${index}] requires positive Temporary HP`
          );
        }
      }
    });
    const retaliationGroups = runtime.activeEffects.flatMap((effect) => effect && typeof effect === "object" && effect.mechanics?.kind === "temporary_hp_melee_retaliation" ? [effect.mechanics.actionId] : []);
    if (new Set(retaliationGroups).size !== retaliationGroups.length) {
      throw new Error(
        `world.actors.${actorId}.runtime.activeEffects has multiple retaliations for one source action`
      );
    }
    const normalizedAttackProfile = normalizeAttackProfile({
      value: actor2.attackProfile,
      actor: actor2,
      path: `world.actors.${actorId}.attackProfile`,
      legacy
    });
    const normalizedActor = {
      ...actor2,
      character: {
        ...character,
        ...normalizedResourceRecovery !== void 0 ? { resourceRecovery: normalizedResourceRecovery } : {}
      },
      capabilities: {
        actionIds: [...new Set(actionIds)].sort(),
        ...featureSources && Object.keys(featureSources).length ? { featureSources } : {}
      },
      ...normalizedSpellcastingAccess ? { spellcastingAccess: normalizedSpellcastingAccess } : {},
      ...normalizedTraits ? { traits: normalizedTraits } : {},
      ...normalizedWarlockPacts ? { warlockPacts: normalizedWarlockPacts } : {},
      lifecycle: normalizeActorLifecycle({
        value: actor2.lifecycle,
        path: `world.actors.${actorId}.lifecycle`,
        actorId,
        schemaVersion,
        worldRevision,
        rulesetContentHash
      }),
      attackProfile: normalizedAttackProfile
    };
    if (!normalizedSpellcastingAccess) delete normalizedActor.spellcastingAccess;
    if (!normalizedTraits) delete normalizedActor.traits;
    if (!normalizedWarlockPacts) delete normalizedActor.warlockPacts;
    if (normalizedResourceRecovery === void 0) delete normalizedActor.character.resourceRecovery;
    return [actorId, normalizedActor];
  }));
  const heldSlots = /* @__PURE__ */ new Set();
  for (const object5 of Object.values(objects)) {
    if (object5.attunedToActorId && !actors[object5.attunedToActorId]) {
      throw new Error(`world.objects.${object5.id}.attunedToActorId must reference a world actor`);
    }
    if (object5.heldByActorId) {
      if (!actors[object5.heldByActorId]) {
        throw new Error(`world.objects.${object5.id}.heldByActorId must reference a world actor`);
      }
      const heldSlot = `${object5.heldByActorId}\0${object5.heldInHand}`;
      if (heldSlots.has(heldSlot)) {
        throw new Error(`world.objects.${object5.id} duplicates a canonical held-item slot`);
      }
      heldSlots.add(heldSlot);
    }
  }
  const bondedObjectIds = /* @__PURE__ */ new Set();
  for (const [actorId, actor2] of Object.entries(actors)) {
    const bond = actor2.warlockPacts?.blade?.activeBond;
    if (!bond) continue;
    if (bondedObjectIds.has(bond.weaponObjectId)) {
      throw new Error(`world.actors.${actorId}.warlockPacts.blade bonds an already bonded item`);
    }
    bondedObjectIds.add(bond.weaponObjectId);
  }
  for (const [ownerActorId, owner] of Object.entries(actors)) {
    const familiarId = owner.warlockPacts?.chain?.activeFamiliar?.actorId;
    if (!familiarId) continue;
    const familiarActor = actors[familiarId];
    if (!familiarActor || familiarActor.kind !== "summonedActor") {
      throw new Error(
        `world.actors.${ownerActorId}.warlockPacts.chain.activeFamiliar.actorId must reference a summonedActor in the same world`
      );
    }
  }
  for (const [actorId, actor2] of Object.entries(actors)) {
    if (actor2.familiarState === void 0 && actor2.familiarMetadata === void 0) continue;
    const ownerId = actor2.familiarState?.ownerActorId;
    const owner = ownerId ? actors[ownerId] : void 0;
    const issue = familiarActorStateIssue({ actor: actor2, owner });
    if (issue) throw new Error(`world.actors.${actorId}: ${issue}`);
  }
  for (const ownerActorId of Object.keys(actors)) {
    const owned = familiarActorsOwnedBy({ actors }, ownerActorId);
    if (owned.length > 1) {
      throw new Error(`world.actors.${ownerActorId} owns multiple canonical familiar actors`);
    }
  }
  const protectionEffectIds = /* @__PURE__ */ new Set();
  for (const [actorId, actor2] of Object.entries(actors)) {
    for (const effect of actor2.runtime.activeEffects) {
      const issue = protectionEffectEntryIssue(effect, actor2, { actors });
      if (issue) throw new Error(`world.actors.${actorId}.runtime.activeEffects: ${issue}`);
      if (effect.mechanics.kind !== "fighting_style_protection_2024") continue;
      if (protectionEffectIds.has(effect.id)) {
        throw new Error(`Protection effect id ${effect.id} is duplicated in world actors`);
      }
      protectionEffectIds.add(effect.id);
    }
  }
  const dancingGroups = /* @__PURE__ */ new Map();
  for (const object5 of Object.values(objects)) {
    if (object5.dancingLight) {
      const source2 = actors[object5.sourceActorId];
      if (!source2 || !source2.capabilities.actionIds.includes(object5.sourceActionId)) {
        throw new Error(
          `world.objects.${object5.id}.dancingLight must retain an actor-owned source action`
        );
      }
      const key = `${object5.sourceActorId}\0${object5.sourceActionId}\0${object5.dancingLight.groupId}`;
      dancingGroups.set(key, [...dancingGroups.get(key) ?? [], object5]);
    }
    for (const [index, attachment] of (object5.prestidigitation ?? []).entries()) {
      const source2 = actors[attachment.sourceActorId];
      if (!source2 || !source2.capabilities.actionIds.includes(attachment.sourceActionId)) {
        throw new Error(
          `world.objects.${object5.id}.prestidigitation[${index}] must retain an actor-owned source action`
        );
      }
    }
  }
  for (const [key, group] of dancingGroups) {
    const [sourceActorId, sourceActionId] = key.split("\0");
    const forms2 = new Set(group.map((object5) => object5.dancingLight.form));
    const expectedCount = group[0].dancingLight.form === "medium_humanoid" ? 1 : void 0;
    if (forms2.size !== 1 || group.length > 4 || expectedCount !== void 0 && group.length !== 1) {
      throw new Error(`Dancing Lights group ${group[0].dancingLight.groupId} has invalid persisted membership`);
    }
    const concentration = world.concentrations && typeof world.concentrations === "object" ? world.concentrations[sourceActorId] : void 0;
    if (!concentration || concentration.actionId !== sourceActionId || typeof concentration.id !== "string" || !concentration.id) {
      throw new Error(
        `Dancing Lights group ${group[0].dancingLight.groupId} requires its exact active concentration`
      );
    }
  }
  const attackActions = normalizeAttackActions({ value: world.attackActions, actors, legacy });
  const grapples = normalizeGrapples({ value: world.grapples, actors, legacy });
  if (!legacy) {
    const pending = world.pendingResolution === null ? null : record8(world.pendingResolution, "world.pendingResolution");
    if (pending?.type === "protection_reaction") {
      const issue = pendingProtectionResolutionIssue(pending, { actors });
      if (issue) throw new Error(`world.pendingResolution: ${issue}`);
      const pactIssue = pactBladePendingProjectionIssue({ pending, actors, objects });
      if (pactIssue) throw new Error(`world.pendingResolution: ${pactIssue}`);
    }
    if (pending?.type === "attack_reaction") {
      const weaponHand = pending.weaponHand;
      const weaponCardId = pending.weaponCardId;
      if (weaponHand === void 0 !== (weaponCardId === void 0) || weaponHand !== void 0 && weaponHand !== "main" && weaponHand !== "off" || weaponCardId !== void 0 && (typeof weaponCardId !== "string" || !weaponCardId.trim())) {
        throw new Error("world.pendingResolution has an invalid weapon continuation identity");
      }
      const pactIssue = pactBladePendingProjectionIssue({ pending, actors, objects });
      if (pactIssue) throw new Error(`world.pendingResolution: ${pactIssue}`);
      if (weaponHand && weaponCardId && pending.pactBladeProjection === void 0) {
        const sourceActorId = nonBlankString2(
          pending.sourceActorId,
          "world.pendingResolution.sourceActorId"
        );
        const source2 = actors[sourceActorId];
        const slot = weaponHand === "main" ? "main_hand" : "off_hand";
        const card = source2 && [
          ...source2.character.knownCards ?? [],
          ...source2.character.equippedCards ?? []
        ].find((candidate) => candidate.id === weaponCardId);
        if (!source2 || !card || card.type !== "weapon" || source2.runtime.equipment[slot] !== card.id) {
          throw new Error("world.pendingResolution weapon must match the source equipped Card");
        }
      }
    }
    for (const [attackActionId, attackAction] of Object.entries(attackActions)) {
      if (!attackAction.blockedByResolutionId) continue;
      if (!pending || pending.id !== attackAction.blockedByResolutionId || pending.attackActionId !== attackActionId) {
        throw new Error(
          `world.attackActions.${attackActionId}.blockedByResolutionId must match the active resolution`
        );
      }
    }
    if (pending?.attackActionId !== void 0) {
      const attackActionId = nonBlankString2(
        pending.attackActionId,
        "world.pendingResolution.attackActionId"
      );
      if (attackActions[attackActionId]?.blockedByResolutionId !== pending.id) {
        throw new Error("world.pendingResolution.attackActionId must reference its blocked Attack action");
      }
    }
  }
  if (!legacy) {
    for (const [actorId, actor2] of Object.entries(actors)) {
      for (const effect of actor2.runtime.activeEffects) {
        if (!effect.id.startsWith("grapple:")) continue;
        const grappleId = effect.id.slice("grapple:".length);
        if (!grapples[grappleId] || grapples[grappleId].targetActorId !== actorId) {
          throw new Error(`world.actors.${actorId}.runtime.activeEffects contains an orphan grapple projection`);
        }
      }
    }
  }
  return {
    ...world,
    schemaVersion: 5,
    actors,
    objects,
    concentrations: world.concentrations && typeof world.concentrations === "object" ? world.concentrations : {},
    attackActions,
    grapples
  };
}

// src/rules-worker/execute.ts
var RULES_WORKER_PROTOCOL_VERSION = 1;
var RULES_WORKER_ENGINE_VERSION = "rules-core-worker-v1";
var RULES_WORKER_SCHEMA_VALIDATION = "rules-core-world-v5-verified";
function assertObject(value, label) {
  if (!value || typeof value !== "object" || Array.isArray(value)) {
    throw new Error(`${label} must be an object`);
  }
}
function assertUint32Tape(value) {
  if (!Array.isArray(value) || value.length < 1 || value.length > 4096) {
    throw new Error("rngTape must contain 1-4096 server-generated uint32 values");
  }
  for (const entry of value) {
    if (!Number.isSafeInteger(entry) || entry < 0 || entry > 4294967295) {
      throw new Error("rngTape contains a value outside uint32");
    }
  }
}
function exactRuleset(actual, expected) {
  return canonicalStringify(actual) === canonicalStringify(expected);
}
async function validateRulesWorkerWorld(raw) {
  assertObject(raw, "rules worker validation request");
  if (raw.protocolVersion !== RULES_WORKER_PROTOCOL_VERSION) {
    throw new Error("unsupported rules worker protocolVersion");
  }
  const certified = await loadCertifiedSheetCombatCatalog();
  const expectedArtifactHash = certified.artifact.source.release.releaseHash;
  if (raw.rulesArtifactHash !== expectedArtifactHash) {
    throw new Error("rulesArtifactHash is not the certified worker artifact");
  }
  const stateHash = await canonicalSha256(raw.world);
  if (raw.stateHash !== stateHash) {
    throw new Error("stateHash does not match canonical WorldState");
  }
  const world = migrateWorldState(structuredClone(raw.world));
  if (canonicalStringify(world) !== canonicalStringify(raw.world)) {
    throw new Error("WorldState must be normalized to the current schema before worker validation");
  }
  if (!exactRuleset(world.ruleset, certified.ruleset)) {
    throw new Error("WorldState ruleset is not the certified worker release");
  }
  for (const actor2 of Object.values(world.actors)) {
    const certifiedActionIds = actor2.capabilities.actionIds.filter((id) => certified.catalog.getAction(id) !== void 0);
    assertCertifiedSheetCombatActorAccess(actor2, certifiedActionIds, certified);
  }
  return {
    protocolVersion: RULES_WORKER_PROTOCOL_VERSION,
    engineVersion: RULES_WORKER_ENGINE_VERSION,
    semanticAuthority: "server_rules_core_verified",
    schemaValidation: RULES_WORKER_SCHEMA_VALIDATION,
    status: "valid",
    rulesArtifactHash: raw.rulesArtifactHash,
    stateHash
  };
}
async function executeRulesWorkerRequest(raw) {
  assertObject(raw, "rules worker request");
  if (raw.protocolVersion !== RULES_WORKER_PROTOCOL_VERSION) {
    throw new Error("unsupported rules worker protocolVersion");
  }
  assertUint32Tape(raw.rngTape);
  const validation = await validateRulesWorkerWorld({
    protocolVersion: raw.protocolVersion,
    rulesArtifactHash: raw.rulesArtifactHash,
    stateHash: raw.baseStateHash,
    world: raw.world
  });
  const certified = await loadCertifiedSheetCombatCatalog();
  const world = structuredClone(raw.world);
  const baseStateHash = validation.stateHash;
  if (raw.command.rulesetContentHash !== world.ruleset.contentHash) {
    throw new Error("command rulesetContentHash differs from WorldState");
  }
  let rngIndex = 0;
  const rng = () => {
    const value = raw.rngTape[rngIndex];
    if (value === void 0) throw new Error("server RNG tape exhausted");
    rngIndex += 1;
    return value / 4294967296;
  };
  const cards = /* @__PURE__ */ new Map();
  for (const actor2 of Object.values(world.actors)) {
    for (const card of [...actor2.character.knownCards ?? [], ...actor2.character.equippedCards ?? []]) {
      const previous = cards.get(card.id);
      if (previous && canonicalStringify(previous) !== canonicalStringify(card)) {
        throw new Error(`WorldState contains conflicting immutable card ${card.id}`);
      }
      cards.set(card.id, structuredClone(card));
    }
  }
  const catalog = {
    ...certified.catalog,
    getCard: (id) => cards.get(id)
  };
  const result = handleCommand(world, structuredClone(raw.command), catalog, {
    rng,
    clock: createLogicalClock(world.logicalClock),
    nextId: createSequentialIdFactory(`worker:${raw.command.commandId}`)
  });
  const rngConsumed = raw.rngTape.slice(0, rngIndex);
  if (result.status === "rejected") {
    return {
      protocolVersion: RULES_WORKER_PROTOCOL_VERSION,
      engineVersion: RULES_WORKER_ENGINE_VERSION,
      semanticAuthority: "server_rules_core_verified",
      schemaValidation: RULES_WORKER_SCHEMA_VALIDATION,
      status: "rejected",
      rulesArtifactHash: raw.rulesArtifactHash,
      baseStateHash,
      stateHash: baseStateHash,
      rngConsumed,
      code: result.code,
      message: result.message
    };
  }
  const stateHash = await canonicalSha256(result.nextState);
  const eventHash = await canonicalSha256(result.events);
  return {
    protocolVersion: RULES_WORKER_PROTOCOL_VERSION,
    engineVersion: RULES_WORKER_ENGINE_VERSION,
    semanticAuthority: "server_rules_core_verified",
    schemaValidation: RULES_WORKER_SCHEMA_VALIDATION,
    status: "accepted",
    rulesArtifactHash: raw.rulesArtifactHash,
    baseStateHash,
    stateHash,
    eventHash,
    rngConsumed,
    events: structuredClone(result.events),
    nextState: structuredClone(result.nextState)
  };
}

// src/rules-worker/config.ts
function resolveRulesWorkerPort(environment) {
  return Number.parseInt(environment.RULES_WORKER_PORT ?? environment.PORT ?? "9090", 10);
}

// src/rules-worker/server.ts
var MAX_BODY_BYTES = 2 << 20;
var port = resolveRulesWorkerPort(process.env);
var secret = process.env.RULES_WORKER_SECRET?.trim() ?? "";
function json(response, status, body) {
  const encoded = JSON.stringify(body);
  response.writeHead(status, {
    "content-type": "application/json; charset=utf-8",
    "content-length": Buffer.byteLength(encoded),
    "cache-control": "no-store"
  });
  response.end(encoded);
}
function authorized(request) {
  if (!secret) return process.env.NODE_ENV !== "production";
  return request.headers.authorization === `Bearer ${secret}`;
}
async function readBody(request) {
  const chunks = [];
  let size = 0;
  for await (const chunk of request) {
    const buffer = Buffer.isBuffer(chunk) ? chunk : Buffer.from(chunk);
    size += buffer.length;
    if (size > MAX_BODY_BYTES) throw new Error("request body is too large");
    chunks.push(buffer);
  }
  return JSON.parse(Buffer.concat(chunks).toString("utf8"));
}
function serverTape(length = 1024) {
  const bytes = randomBytes(length * 4);
  return Array.from({ length }, (_, index) => bytes.readUInt32BE(index * 4));
}
var server = createServer(async (request, response) => {
  if (request.method === "GET" && request.url === "/health") {
    json(response, 200, {
      status: "ok",
      protocolVersion: RULES_WORKER_PROTOCOL_VERSION,
      engineVersion: RULES_WORKER_ENGINE_VERSION,
      rulesArtifactHash: sheetCombatCertification_generated_default.source.release.releaseHash
    });
    return;
  }
  if (request.method !== "POST" || request.url !== "/v1/execute" && request.url !== "/v1/validate") {
    json(response, 404, { error: "not found" });
    return;
  }
  if (!authorized(request)) {
    json(response, 401, { error: "rules worker authentication is required" });
    return;
  }
  try {
    if (request.url === "/v1/validate") {
      const value2 = await readBody(request);
      json(response, 200, await validateRulesWorkerWorld(value2));
      return;
    }
    const value = await readBody(request);
    const result = await executeRulesWorkerRequest({
      ...value,
      rngTape: Array.isArray(value.rngTape) && value.rngTape.length ? value.rngTape : serverTape()
    });
    json(response, 200, result);
  } catch (error) {
    json(response, 422, {
      error: error instanceof Error ? error.message : "rules worker execution failed"
    });
  }
});
if (!Number.isSafeInteger(port) || port < 1 || port > 65535) {
  throw new Error("RULES_WORKER_PORT must be a valid TCP port");
}
if (process.env.NODE_ENV === "production" && !secret) {
  throw new Error("RULES_WORKER_SECRET is required in production");
}
server.listen(port, "127.0.0.1", () => {
  process.stdout.write(`rules worker ${RULES_WORKER_ENGINE_VERSION} listening on 127.0.0.1:${port}
`);
});
