# Что нужно дополнительно описать перед реализацией

## 🔴 КРИТИЧНО (блокирует реализацию)

### 1. **API для выполнения действий**

**Статус:** ❌ Отсутствует полностью

**Что нужно добавить:**
- Endpoint: `POST /api/v4/actions/execute`
- Структура запроса:
  - `character_id` - ID персонажа
  - `action_name` - название действия
  - `parameters` - параметры действия (цель, выбор и т.д.)
- Алгоритм проверки доступности:
  - Проверка ресурсов (достаточно ли main_action, bonus_action и т.д.)
  - Проверка условий выполнения (conditions из действия)
  - Проверка перезарядки (reload)
  - Проверка is_used
- Алгоритм выполнения:
  - Списание ресурсов
  - Применение эффектов от действия
  - Обновление состояния действия (is_used = true)
  - Обработка перезарядки
- Структура ответа (успех/ошибка)
- Примеры запросов и ответов
- Коды ошибок

**Где добавить:** `API_Reference.md` - новый раздел "Выполнение действий"

**Вопросы без ответов:**
- Как передать параметры действия (например, цель атаки)?
- Что происходит, если действие требует выбор цели?
- Как обрабатываются действия с условиями?
- Как обновляется состояние действия после выполнения?

---

### 2. **Механизм применения pure эффектов**

**Статус:** ⚠️ Упомянуто, но алгоритм не описан

**Что нужно добавить:**
- Алгоритм применения pure эффектов при создании персонажа:
  - Когда применяются pure эффекты?
  - В каком порядке применяются?
  - Как обрабатываются конфликты?
- Алгоритм "врастания" pure эффектов в характеристики:
  - Что означает "врастают в характеристики"?
  - Какие характеристики изменяются?
  - Как это отличается от temporary эффектов?
- Алгоритм применения pure эффектов при изменении персонажа:
  - Что происходит при повышении уровня?
  - Что происходит при добавлении нового класса?
  - Что происходит при экипировке предмета с pure эффектом?
- Обработка конфликтов pure эффектов

**Где добавить:** `Effects/Effects.md` - новый раздел "Механизм применения pure эффектов"

**Вопросы без ответов:**
- Когда именно применяются pure эффекты - при создании персонажа или при каждом запросе?
- Как обрабатывается ситуация, когда pure эффект даёт владение, которое уже есть?
- Можно ли отменить pure эффект?

---

### 3. **Детализация расчётов характеристик**

**Статус:** ⚠️ Есть базовые формулы, но нет учёта эффектов

**Что нужно добавить в `Calculations.md`:**
- Учёт эффектов в расчёте КД:
  - Как учитываются модификаторы от эффектов (armor_class_modifier)?
  - Приоритет расчётов (что применяется первым?)
  - Примеры расчёта с эффектами
- Учёт эффектов в расчёте скорости:
  - Как учитываются модификаторы от эффектов (speed_modifier)?
  - Временные модификаторы (например, от действия "Рывок")
- Формулы расчёта сопротивлений (resistances):
  - Как собираются сопротивления из эффектов?
  - Приоритет (resistance vs immunity vs vulnerability)
- Формулы расчёта грузоподъёмности:
  - Базовая формула (STR * 15 фунтов)
  - Модификаторы от расы/эффектов
- Алгоритм применения temporary эффектов в расчёты:
  - Когда пересчитываются temporary эффекты?
  - Как они учитываются в `type=play` режиме?

**Вопросы без ответов:**
- Как рассчитывается КД варвара без доспеха с эффектом unarmored_defense?
- Как учитываются несколько эффектов, модифицирующих скорость?
- Что происходит, если у персонажа есть и resistance, и immunity к одному типу урона?

---

## 🟡 ВАЖНО (нужно для MVP)

### 4. **Активные эффекты и триггеры**

**Статус:** ⚠️ Упомянуто в Inventories/Effects.md, но не детализировано

**Что нужно добавить:**
- Полный список возможных триггеров:
  - `before_attack` - перед атакой
  - `after_attack` - после атаки
  - `on_damage_taken` - при получении урона
  - `on_damage_dealt` - при нанесении урона
  - `before_saving_throw` - перед спасброском
  - `after_saving_throw` - после спасброска
  - И другие...
- Система обработки триггеров:
  - Когда проверяются триггеры?
  - В каком порядке обрабатываются?
  - Как обрабатываются trigger_conditions?
  - Как применяются trigger_effects?
- Механизм создания временных эффектов через grant_effect:
  - Как создаются временные эффекты?
  - Как они связываются с исходным эффектом?
  - Когда они удаляются?
- Примеры различных триггеров

**Где добавить:** `Effects/Effects.md` - новый раздел "Активные эффекты и триггеры"

---

### 5. **Полный список типов эффектов**

**Статус:** ⚠️ Показаны примеры, но нет полного списка

**Что нужно добавить:**
- Полный список всех возможных типов эффектов:
  - `proficiency` - владения ✅ (описано)
  - `damage_roll_modifier` - модификатор урона ✅ (описано)
  - `attack_roll_modifier` - модификатор атаки ✅ (описано)
  - `armor_class_modifier` - модификатор КД ❌ (не описано)
  - `speed_modifier` - модификатор скорости ❌ (не описано)
  - `ability_modifier` - модификатор характеристики ❌ (не описано)
  - `resistance` - сопротивление ✅ (упомянуто)
  - `immunity` - иммунитет ❌ (не описано)
  - `vulnerability` - уязвимость ❌ (не описано)
  - И другие...
- Описание каждого типа эффекта:
  - Структура данных для каждого типа
  - Как применяется каждый тип
  - Примеры использования
- Приоритеты применения типов эффектов

**Где добавить:** `Effects/Effects.md` - дополнить раздел "Действия эффектов"

---

### 6. **Система предупреждений (warnings)**

**Статус:** ⚠️ Упомянуто, но не детализировано

**Что нужно добавить:**
- Полный список предупреждений:
  - `warning_skill_proficiency_not_chosen` ✅ (упомянуто)
  - `warning_armor_without_proficiency` ✅ (упомянуто)
  - `warning_weapon_without_proficiency` ✅ (упомянуто)
  - `warning_attunement_overflow` ✅ (упомянуто)
  - `warning_overweight` ✅ (упомянуто)
  - И другие...
- Когда выдаётся каждое предупреждение:
  - Условия выдачи предупреждения
  - Контекст предупреждения
- Как обрабатываются предупреждения:
  - Можно ли игнорировать предупреждения?
  - Как исправить предупреждение?
  - Влияют ли предупреждения на игровой процесс?
- Структура предупреждений в ответах API

**Где добавить:** `API_Reference.md` - новый раздел "Система предупреждений" или отдельный документ `Warnings.md`

---

### 7. **Валидация при создании персонажа**

**Статус:** ⚠️ Упомянуто, но не детализировано

**Что нужно добавить:**
- Полный список правил валидации:
  - Валидация выбора навыков из класса
  - Валидация характеристик (не больше положенного)
  - Валидация выбора класса/расы/предыстории
  - Валидация уровня класса
- Обработка ошибок:
  - Коды ошибок валидации
  - Сообщения об ошибках
  - Примеры некорректных запросов
- Детализация системы предупреждений при создании

**Где добавить:** Дополнить раздел `POST /api/v4/characters/create` в `API_Reference.md`

---

### 8. **Обработка ошибок API**

**Статус:** ⚠️ Упомянута одна ошибка, но нет полной системы

**Что нужно добавить:**
- Стандартная структура ответа об ошибке:
  - Код ошибки
  - Сообщение об ошибке
  - Дополнительная информация
- Коды ошибок для всех endpoints:
  - 400 - Bad Request
  - 401 - Unauthorized
  - 403 - Forbidden
  - 404 - Not Found
  - 422 - Unprocessable Entity
  - 500 - Internal Server Error
- Специфичные коды ошибок:
  - Недостаточно ресурсов для действия
  - Действие недоступно
  - Предмет не может быть экипирован
  - И другие...
- Примеры ответов с ошибками для каждого endpoint

**Где добавить:** `API_Reference.md` - новый раздел "Обработка ошибок" в начале документа

---

### 9. **Полная структура действия**

**Статус:** ⚠️ Есть пример, но не полная структура

**Что нужно добавить в `Inventories/Actions.md`:**
- Полная структура объекта Action:
  - Все возможные поля
  - Описание каждого поля
  - Обязательные и необязательные поля
- Описание всех типов действий (action_type):
  - `base_action` - базовое действие
  - И другие типы...
- Система перезарядки (reload):
  - `per_turn` - каждый ход
  - `per_short_rest` - каждый короткий отдых
  - `per_long_rest` - каждый длинный отдых
  - `custom` - кастомная перезарядка
- Механизм проверки доступности действия
- Примеры различных типов действий

---

## 🟢 ЖЕЛАТЕЛЬНО (можно добавить позже)

### 10. **Мультиклассирование**

**Статус:** ❌ Не описано

**Что нужно добавить:**
- Правила мультиклассирования
- Ограничения и требования
- Как объединяются владения от разных классов
- Как рассчитываются ресурсы при мультиклассировании

**Где добавить:** Создать документ `Multiclassing.md` или добавить в `Classes.md`

---

### 11. **Повышение уровня**

**Статус:** ❌ Не описано

**Что нужно добавить:**
- API для повышения уровня
- Алгоритм добавления новых способностей
- Система выбора способностей (если есть выбор)

**Где добавить:** Добавить в `API_Reference.md` раздел про повышение уровня

---

### 12. **Заклинания**

**Статус:** ❌ Не описано вообще

**Что нужно добавить:**
- Структура инвентаря заклинаний
- API для работы с заклинаниями
- Система ячеек заклинаний

**Где добавить:** Создать документ `Spells.md` или добавить в `Inventories/`

---

## 📋 Итоговый чеклист

### Критично (блокирует реализацию):
- [ ] API для выполнения действий
- [ ] Механизм применения pure эффектов (алгоритм)
- [ ] Детализация расчётов характеристик (учёт эффектов)

### Важно (нужно для MVP):
- [ ] Активные эффекты и триггеры (полный список)
- [ ] Полный список типов эффектов
- [ ] Система предупреждений (детализация)
- [ ] Валидация при создании персонажа (детализация)
- [ ] Обработка ошибок API
- [ ] Полная структура действия

### Желательно (можно добавить позже):
- [ ] Мультиклассирование
- [ ] Повышение уровня
- [ ] Заклинания

---

## 🎯 Рекомендации по приоритетам

**Перед началом реализации обязательно проработать:**

1. **API для выполнения действий** - без этого невозможно использовать систему
2. **Механизм применения pure эффектов** - без этого невозможно правильно создавать персонажа
3. **Детализация расчётов характеристик** - без этого невозможно правильно отображать персонажа

**Для MVP также важно:**
4. Активные эффекты и триггеры
5. Полный список типов эффектов
6. Система предупреждений
7. Валидация и обработка ошибок
8. Полная структура действия
