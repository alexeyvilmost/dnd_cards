name: bagofholding

services:
  backend:
    depends_on:
      rules-worker:
        condition: service_healthy
    image: bagofholding-backend:${SOURCE_COMMIT:?SOURCE_COMMIT is required}
    env_file:
      - ${APP_ENV_FILE:-/opt/bagofholding/shared/app.env}
      - /opt/bagofholding/shared/rules-worker.env
    environment:
      PORT: "8080"
      SOURCE_COMMIT: ${SOURCE_COMMIT}
      RULES_WORKER_URL: http://rules-worker:8090
      TRUSTED_PROXY_CIDRS: 172.20.0.0/24
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /tmp:size=64m,mode=1777
    security_opt:
      - no-new-privileges:true
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://127.0.0.1:8080/api/health"]
      interval: 15s
      timeout: 5s
      retries: 8
      start_period: 30s
    networks:
      - edge

  rules-worker:
    image: bagofholding-rules-worker:${SOURCE_COMMIT:?SOURCE_COMMIT is required}
    env_file:
      - /opt/bagofholding/shared/rules-worker.env
    environment:
      SOURCE_COMMIT: ${SOURCE_COMMIT}
      PORT: "8090"
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /tmp:size=32m,mode=1777
    volumes:
      - /opt/bagofholding/shared/rules-artifacts:/artifacts
    security_opt:
      - no-new-privileges:true
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:8090/health').then(r=>{if(!r.ok)process.exit(1)}).catch(()=>process.exit(1))"]
      interval: 15s
      timeout: 5s
      retries: 8
      start_period: 15s
    networks:
      - edge

  frontend:
    volumes:
      - /opt/bagofholding/shared/frontend-assets:/var/lib/bagofholding/frontend:ro
    image: bagofholding-frontend:${SOURCE_COMMIT:?SOURCE_COMMIT is required}
    environment:
      PORT: "3000"
      SOURCE_COMMIT: ${SOURCE_COMMIT}
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://127.0.0.1:3000/health"]
      interval: 15s
      timeout: 5s
      retries: 8
      start_period: 15s
    networks:
      - edge

  caddy:
    image: caddy:2.10.2-alpine
    environment:
      APP_DOMAIN: ${APP_DOMAIN:?APP_DOMAIN is required}
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    restart: unless-stopped
    depends_on:
      backend:
        condition: service_healthy
      frontend:
        condition: service_healthy
    networks:
      - edge

networks:
  edge:
    driver: bridge
    ipam:
      config:
        - subnet: 172.20.0.0/24

volumes:
  caddy_data:
  caddy_config:
