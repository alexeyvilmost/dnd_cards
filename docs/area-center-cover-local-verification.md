# Области и центральный луч — локальная проверка 17.09.2026

## Исправления

- `centerCoverTrace` — общий центральный луч расчёта укрытия и визуальной траектории. Стена больше не обходится лучом из угла; объекты вне центрального луча не дают КД.
- Площадные действия проверяют дальность/препятствия до центра отдельно от распространения. Геометрия берётся из данных действия, без ветки для «Брызг кислоты».
- Исполнитель пересобирает список целей. Нельзя исключить поражаемую цель или добавить постороннюю подменой `targetIds`.
- Дальность доставки сохранена отдельно в фактах; радиус может выходить за дальность до центра. Направляющая клетка конуса не считается точкой доставки.

## Проверено

- Полный Vitest: **466 файлов, 4076 тестов**, все прошли.
- TypeScript `--noEmit`, Vite production build, проверка DiceBox assets: успешно. В сборке остаётся предупреждение о крупных чанках.
- HTTP worker / replay: **3 теста**, успешно.
- Регрессии: две разные декларации сферы/цилиндра и типов урона; стена между центром и вторичной целью; закрытый/слишком далёкий центр до расхода действия; граница дальности; подмена целей; центральный луч и обратное направление; крупные footprint и реальные клетки нерегулярных препятствий.
- Снимок пользовательского боя, ревизия 130: старый артефакт отклоняет «Брызги кислоты» в клетку (4,4) из-за кобольда №2. Новый артефакт на копии обрабатывает обоих кобольдов; в реальном бою заклинание не применялось.
- Регрессия прошлого скриншота: луч (6,7) → (17,0), союзник (7,5), бочки (16,2), размер 2×1 — укрытия нет в обоих направлениях.
- После обновления повторно воспроизведены все 8 сегментов журнала (106 команд) исходными артефактами, включая новый baseline: хеши и поток случайности совпали.
- В браузере после перезагрузки сохранены ход лучницы, HP 10/13, действие 1/1 и HP кобольдов 7/7; «Брызги кислоты» доступны. Ошибок в консоли нет. Боевые команды через UI не отправлялись.

## Текущий локальный бой

Worker на `http://localhost:8090`, UI на `http://localhost:3001`.
Новый артефакт: `sha256:81a3aca4d3a2aeaf9ab17dfa9a90c6d4b3ce0ccee45a9e6af3acc3155df9c2dc`.

`scripts/roguelike/area-cover-local-upgrade.mjs` по умолчанию делает dry run. Применён только к указанному пользователем локальному забегу `3ebf1c9c-1022-4aee-80c2-7e88c256a939`:

- ревизия забега 130 → 131;
- состояние боя и поток случайности побайтно по структуре сохранены;
- предыдущие события не обновлялись; добавлена одна запись `local_rules_upgrade` с новым baseline и хешами до/после;
- резервная копия в игнорируемом `outputs/area-cover/before-upgrade-*.json`;
- проверка ожидаемой ревизии и снимка в транзакции, запрет обновления при незавершённом решении; повторный запуск ничего не меняет.

Другие начатые бои не перепривязаны. Коммит, push и прод не затронуты.
