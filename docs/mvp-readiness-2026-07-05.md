# Отчёт готовности MVP — 2026-07-05 (фаза H)

Итог сессии «переход к MVP»: все контентные и функциональные гейты закрыты,
сквозные сценарии прогнаны в браузере против прод-бэкенда.

## Автоматические проверки

| Проверка | Результат |
|---|---|
| `npx tsc --noEmit` | **0 ошибок** (было 288; `npm run build` снова работает локально) |
| `npx vitest run` (основной набор) | 108 passed / 17 файлов |
| `npm run test:mvp` | 70 passed, 6 todo (roadmap-пометки), 7 skipped (гейт MVP_CONTENT) |
| `MVP_CONTENT=1 … content.readiness` (живой прод) | **7/7 passed** — G2–G4, G5, G6, G7, G8–G9, C7, H-гигиена |
| `node scripts/lint-mechanics.mjs` | 244 механики (effects+actions+**spells**), 0 ошибок схемы |

## Контент в проде

- 12 классов (уровни 1–2, ресурсы, кость хитов, `recommended_abilities` для point-buy),
  ячейки заклинаний у 8 кастеров (полные: `min(self_level+1,3)`, полукастеры: 2, колдун: короткий отдых);
- 39 рас (12 + 27 подвидов), 16 предысторий (снаряжение А/Б + золото), 12 origin-черт — все с механиками;
- 99 заклинаний уровней 0–1: **100% с механиками** (51 исполняемая: атака/спас/урон/лечение/temp_hp/состояния/баффы;
  48 narrative с корректной стоимостью — за пределами контракта движка, список кандидатов в
  `scripts/content/batches/data/`). Механики верифицированы по «Книге игрока 2024» (RU) и PHB 2024 (EN),
  адверсариальный ревью внёс 11 исправлений.

## Сквозные сценарии (браузер, localhost:5173 → прод-бэкенд)

**Воин / Человек / Стражник (создание → бой → отдых → level-up):**
- point-buy 27 автозаполнился из класса (0 остатка), бонусы предыстории +2 СИЛ/+1 ИНТ применились к итогам;
- при создании выданы: снаряжение варианта А (8 предметов) + 12 зм, ресурсы (Action Surge, Второе дыхание, Героическое вдохновение от «Находчивого»);
- лист: HP 12 (10+ТЕЛ), КЗ 11 (10+ЛВК), вес 51/255;
- «Надеть» копьё: qty списался из сумки, слот занят; атака: `к20: 16 +3 СИЛ +2 БМ = 21 против КЗ 10 — попадание`, урон 1к8 (универсальный хват) колющим;
- Второе дыхание: лечение `1к10+уровень`, ресурсы списаны; «Новый ход» и «Короткий отдых» отработали;
- «Поднять уровень» → кузница с уровнем 2, конфликт-линтер поймал дубли навыков, после развода — сохранение: HP 20.

**Волшебник / Эльф / Благородный (заклинания):**
- выборы: 3 заговора + 6 заклинаний 1 уровня в книгу; выданные заклинания отображаются;
- ячейки: `spell_slot_1 = 3` (уровень 2);
- Огненный снаряд: бросок атаки заклинанием с модификатором заклинателя и БМ;
- Волшебная стрела: списаны действие + `spell_slot_1` (3→2), урон `к4: 3+3+2 = 11` force + примечание о дротиках.

## Починено по ходу фазы H

1. Универсальное оружие «1к6 (1к8)» роняло атаку — `weaponContext` теперь берёт вторую кость при хвате двумя руками.
2. Второе дыхание не списывало ресурс `second_wind` — стоимость дополнена в проде.
3. Переход «редактирование → создание» в кузнице не сбрасывал черновик (React переиспользует компонент) — сохранение перезаписывало предыдущего персонажа. Добавлен сброс по `editId`.

## Деплой

Бэкенд: Railway автодеплой из main подтверждён (миграции 056–057 раскатились в течение минут).
Фронтенд: прод (`bagofholding.up.railway.app`) отдаёт сборку; ротацию бандла проверять по хэшу `assets/index-*.js` после пуша.

## Осталось за рамками MVP (зафиксировано)

- Апкаст-UI (выбор уровня слота) — движок готов (`ExecuteContext.spell.castLevel`), нет пикера.
- Payload-ы boon / reroll / set_die / transform / grant_action / movement-на-цель / resistance-в-уроне — 6 it.todo.
- Состояния/концентрация/спасброски смерти на листе; кошелёк и покупка на листе; мобильная адаптация (решение №7);
  кости хитов на коротком отдыхе (решение №3 — отложено); общий public-пользователь (решение №5 — осознанно).
