# Loose lower bounds — pinned exact versions (e.g. pydantic 2.9.2) lack prebuilt
# wheels for newer Python (3.14) and fail to compile. Use compatible ranges.
fastapi>=0.115
uvicorn>=0.30
# multipart form parsing for image uploads (POST /images)
python-multipart>=0.0.9
pydantic>=2.9
# PostgreSQL persistence (Railway). Falls back to file storage if no DB env set.
psycopg[binary]>=3.2
python-dotenv>=1.0
requests>=2.32
